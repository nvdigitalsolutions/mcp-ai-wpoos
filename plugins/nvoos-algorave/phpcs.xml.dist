<?xml version="1.0"?>
<ruleset name="NV oOS Algorave">
    <description>WordPress Coding Standards for NV oOS Algorave</description>

    <config name="text_domain" value="nvoos-algorave"/>
    <config name="minimum_wp_version" value="6.0"/>

    <file>includes/</file>
    <file>templates/</file>
    <file>tests/</file>
    <file>nvoos-algorave.php</file>
    <file>uninstall.php</file>

    <exclude-pattern>/vendor/*</exclude-pattern>
    <exclude-pattern>/node_modules/*</exclude-pattern>
    <exclude-pattern>/assets/*</exclude-pattern>
    <exclude-pattern>/languages/*</exclude-pattern>

    <!-- Base: WordPress-Extra -->
    <rule ref="WordPress-Extra"/>

    <!--
        Legacy naming exemptions.
        This plugin keeps the NV_oOS_* global class/file naming scheme of the
        original addon, and slash-style hook names (nvoos_algorave/…) for
        its public filter surface.
    -->
    <rule ref="WordPress.Files.FileName">
        <severity>0</severity>
    </rule>
    <rule ref="PEAR.NamingConventions.ValidClassName">
        <severity>0</severity>
    </rule>
    <rule ref="WordPress.NamingConventions.ValidHookName">
        <severity>0</severity>
    </rule>

    <!--
        Test exemptions: global override and Yoda conditions are standard PHPUnit patterns.
    -->
    <rule ref="WordPress.WP.GlobalVariablesOverride">
        <exclude-pattern>*/tests/*</exclude-pattern>
    </rule>
    <rule ref="WordPress.PHP.YodaConditions">
        <exclude-pattern>*/tests/*</exclude-pattern>
    </rule>

    <!--
        Template files inline shortcode markup; the full render pipeline
        (shortcode_atts, capability gate) lives in the calling class.
    -->
    <rule ref="WordPress.Security.NonceVerification">
        <exclude-pattern>*/templates/*</exclude-pattern>
    </rule>

    <arg name="colors"/>
    <arg value="ps"/>
</ruleset>
