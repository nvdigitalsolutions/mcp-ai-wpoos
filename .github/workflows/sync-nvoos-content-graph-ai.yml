name: Sync nvoos-content-graph-ai

on:
  workflow_dispatch:
  push:
    branches: [main, alpha-working]
    paths:
      - 'plugins/nvoos-content-graph-ai/**'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Split subtree
        run: |
          git subtree split --prefix=plugins/nvoos-content-graph-ai --branch=nvoos-content-graph-ai-split

      - name: Push to nvoos-content-graph-ai
        run: |
          git config --local --unset-all http.https://github.com/.extraheader
          git push https://x-access-token:${{ secrets.NVOOS_CONTENT_GRAPH_AI_REPO_TOKEN }}@github.com/nvdigitalsolutions/nvoos-content-graph-ai.git nvoos-content-graph-ai-split:main --force
