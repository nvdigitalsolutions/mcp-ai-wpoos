name: Sync nvoos-content-graph-pro

on:
  workflow_dispatch:
  push:
    branches: [main, alpha-working]
    paths:
      - 'plugins/nvoos-content-graph-pro/**'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Split subtree
        run: |
          git subtree split --prefix=plugins/nvoos-content-graph-pro --branch=nvoos-content-graph-pro-split

      - name: Push to nvoos-content-graph-pro
        run: |
          git config --local --unset-all http.https://github.com/.extraheader
          git push https://x-access-token:${{ secrets.NVOOS_CONTENT_GRAPH_PRO_REPO_TOKEN }}@github.com/nvdigitalsolutions/nvoos-content-graph-pro.git nvoos-content-graph-pro-split:main --force
