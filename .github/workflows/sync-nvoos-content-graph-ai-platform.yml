name: Sync nvoos-content-graph-ai-platform

on:
  workflow_dispatch:
  push:
    branches: [main, alpha-working]
    paths:
      - 'plugins/nvoos-content-graph-ai-platform/**'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Split subtree
        run: |
          git subtree split --prefix=plugins/nvoos-content-graph-ai-platform --branch=nvoos-content-graph-ai-platform-split

      - name: Push to nvoos-content-graph-ai-platform
        run: |
          git config --local --unset-all http.https://github.com/.extraheader
          git push https://x-access-token:${{ secrets.NVOOS_CONTENT_GRAPH_AI_PLATFORM_REPO_TOKEN }}@github.com/nvdigitalsolutions/nvoos-content-graph-ai-platform.git nvoos-content-graph-ai-platform-split:main --force
