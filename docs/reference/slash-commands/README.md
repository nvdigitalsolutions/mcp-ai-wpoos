# Reference — Slash Commands

Slash command definitions and implementation reference. Each `.md` file
documents one slash command: its syntax, parameters, behavior, and
permissions.

## What belongs here

- agent, broadcast, cost, diagnose, jobs, markup-stats
- mcp-app, model, persona, preset, run
- schedule, schedule-preset, session, skills, status
- tools, workflow-preset

## What doesn't belong here

- Slash command user guides → `../../user-guides/slash-commands/`
- Slash command implementation → `../../developer/`
