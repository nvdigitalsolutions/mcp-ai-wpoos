# Reference — API

REST API, MCP protocol, SSE transport, and authentication reference.

## What belongs here

- REST API endpoint documentation
- MCP and SSE protocol reference
- MCP client configurations
- MCP server authentication
- Authentication methods (nonce, bearer, Auth0, guest)
- JetEngine REST routes
- STDIO transport

## What doesn't belong here

- User-facing API guides → `../../user-guides/`
- Developer REST implementation → `../../developer/`
- Provider API docs → those live with the provider
