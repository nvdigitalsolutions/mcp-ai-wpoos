# Ecosystem Port Tracker

**Living tracker for the additive base+pro → Content Graph ecosystem port.**
Last updated: 2026-09-07 · Plan: [`base-pro-ecosystem-port-plan.md`](plans/base-pro-ecosystem-port-plan.md) · Gaps: [`base-pro-ecosystem-port-gap-analysis.md`](proposals/base-pro-ecosystem-port-gap-analysis.md)

Locked constraints: **D-NOBASE** (zero changes to `mcp-ai-wpoos`), **D-NOCORE** (zero changes to `nvoos-content-graph`), **D-UI** (all UIs ported), **D-SPA** (Pro SPA v2 ported).

| Status | Meaning |
|---|---|
| ✅ Done | Ported, wired, tested in monolith + standalone matrices |
| 🟡 In progress | PR open or partially wired |
| 🔴 Gap | Planned, no implementation yet |
| ⏸️ Deferred | Decision-parked (see plan) |

## Phase 0 — Foundations

| Item | Status | Notes |
|---|---|---|
| Ownership discriminator (`! defined('WP_MCP_AI_PATH')`) | ✅ | Extraction-proven; no base changes |
| AI addon test suite + monolith/standalone CI matrices | ✅ | `plugins/nvoos-content-graph-ai/{phpunit.xml.dist,tests/}`, `.github/workflows/phpunit-ai.yml` |
| Ecosystem asset pipeline (npm workspace) | 🔴 | Phase 0.5 |
| Tracker (this file) | ✅ | Phase 0.6 |

## Wave D — AI runtime → `nvoos-content-graph-ai`

| # | Subsystem | Status | Notes |
|---|---|---|---|
| D1a | Prompt optimizer (cache-hit ordering, cache key, prompt split) | ✅ | `src/Chat/PromptOptimizer.php`; wired into `Rest\ChatController` via `cache_system_prompt` request arg; 10 characterization tests |
| D1b | Chat response cache + SSE rate limiter + semantic cache | ✅ | `src/Chat/{ChatResponseCache,SseRateLimiter,SemanticCache}.php`; wired into `Rest\ChatController` (cache lookup/store on the non-streaming path, 429 + register/release on the streaming path); 20 characterization tests; SemanticCache ships dormant (matches base — no call sites) |
| D1c | Conversation summarizer (BME context strategy) | ✅ | `src/Chat/ConversationSummarizer.php` (decoupled client contract) + `src/Chat/OrchestratorCompletionClient.php` (nvoos/core adapter); 12 characterization tests; wiring into the chat flow follows with the full BME strategy port |
| D1d | Thread manager (threads/messages/checkpoints CRUD + schema) | ✅ | `src/Chat/ThreadManager.php` (byte-identical tables/error codes/envelopes); schema created on CG-AI activation; 13 characterization tests with real DDL |
| D1e | Message attachments + response attachments (attachment pipeline) | ✅ | `src/Chat/{MessageAttachments,ResponseAttachments}.php` (byte-identical meta key `_wp_mcp_ai_openai_file`, filters, error codes, cache group) + `FileServiceBridge`/`FileClientBridge` collaborators; monolith delegates to base factory/client; standalone delegates to the ported `FileServiceFactory`/`OpenAiFileService` (D2f); cleanup/response hooks registered standalone-only via `Plugin.php`; 28 characterization tests |
| D1f | Chat transcript recorder + transcript retention | ✅ | `src/Chat/{ChatTranscriptRecorder,TranscriptRetention}.php` (byte-identical error codes/options, JetEngine CCT + logger gated behind `defined('WP_MCP_AI_PATH')`); recorder invoked on the non-streaming chat path; 4 `transcript_retention_*` setting defaults added; `TranscriptRetention::init()` standalone-only; 10 characterization tests |
| D1g | ChatKit integration (addon definition + registration surface) | ✅ | `src/Chat/ChatKitIntegration.php` (hook names byte-identical; `ADDON_ID` = `nvoos-content-graph-ai`; definition describes CG-AI's real surface — `nvoos-content-graph/v1`, `POST /ai/chat`, `GET /ai/tools`); standalone-only via `Plugin.php`; capability seam delegates to base helper in monolith / same `wp_mcp_ai_chat_capability` filter standalone; 9 characterization tests; documented deviations (download route, surfaces, assistant_id, guest_access) restored as D2/D5/D-UI land |
| D1 | Chat runtime core (clients, router, selector, caches, summarizer, RAG bridge, threads, transcripts, SSE, attachments, quick actions, chatkit) | ✅ | D1a–D1g done (116 ecosystem tests, 400 assertions); remote file-API uploads + ChatKit download route gated on D2 |
| D2a | Zai provider client | ✅ | `src/Provider/ZaiClient.php` extends nvoos/core `OpenAiCompatibleClient` (slug `zai`, default endpoint `https://api.z.ai/api/paas/v4`); registered in `CoreBridge`; `ai_api_key_zai` + `zai_base_url` defaults; 7 characterization tests |
| D2b | Google Maps client (geocode/reverse/nearby/text search) | ✅ | `src/Provider/GoogleMapsClient.php` (byte-identical endpoints/query args/error codes/normalization); settings seam per mode (`google_maps_api_key`); transport-error default branch standalone (full enrichment when `WP_MCP_AI_HTTP` ports); dormant until tools wave; 12 characterization tests |
| D2c | OpenAI Realtime ×3 (voice, translate, whisper) + voice-provider interface | ✅ | `src/Provider/{VoiceProviderInterface,OpenAiRealtimeClient,OpenAiRealtimeTranslateClient,OpenAiRealtimeWhisperClient}.php` (byte-identical endpoints/GA session payloads/token caching/headers/language tables/filters); tool-registry + assistant config monolith-only; dormant until D-UI voice surface; 18 characterization tests |
| D2d | RabbitMQ client | ✅ | `src/Provider/RabbitMqClient.php` (byte-identical topology constants/config keys/constant precedence/job transients/health shapes); `amqp` extension gating preserved; settings seam per mode; dormant until platform queue wave (E2); tests in shared suite |
| D2e | STDIO transport (MCP JSON-RPC server) | ✅ | `src/Provider/StdioTransport.php` (byte-identical protocol version/method routing/error codes/response shapes); tool registry + assistant config seams (base registry monolith / nvoos-core registry standalone); tests in shared suite |
| D2f | File service factory + OpenAI/Gemini file services | ✅ | `src/Chat/{FileServiceFactory,OpenAiFileService,GeminiFileService}.php` (byte-identical provider mapping/multipart uploads/polling/cache keys/error codes); bridges now use them standalone — closes the D1e file-API gap; 13 characterization tests |
| D2 | Providers beyond the 13 (Zai, Google Maps, RabbitMQ, Stdio, Realtime ×3) + file services | ✅ | D2a–D2f done (67 new ecosystem tests); SaaS drivers (FlowHub/PayHere/Printful) remain Pro-tier per gap §4.2/8.3 |
| D3a | Model catalog migration + integrity verifier | ✅ | `src/Model/{ModelCatalogMigration,ModelIntegrityVerifier}.php` + bundled `model-catalog.json`; legacy-id map/option/meta/settings rewriting, blocked models, vulnerability + self-hosted TLS enforcement byte-identical; `run_from_catalog` standalone-only via `Plugin.php`; 11 characterization tests |
| D3b | Model rate-limits CCT + pricing checker | ✅ | `src/Model/{ModelRateLimitsCct,ModelPricingChecker}.php` (JetEngine provisioning + cron byte-identical); both `bootstrap()` standalone-only; 9 characterization tests |
| D3c | Analytics engine | ✅ | `src/Analytics/AnalyticsEngine.php` (trends/statistics/Z-score anomaly/patterns/comparisons/transcript rebuild); usage seam delegates to base `WP_MCP_AI_Tool_Token_Limits` monolith / `ToolTokenLimits` standalone; 11 characterization tests |
| D3d | Usage tracker | ✅ | `src/Analytics/UsageTracker.php` (meta key/totals/normalization/filters/pricing byte-identical; fallback pricing map byte-identical); settings seam per mode; `init()` standalone-only; 8 characterization tests |
| D3e | Token tracking DB + enhanced tracking + optimizer | ✅ | `src/Analytics/{TokenTrackingDatabase,EnhancedTokenTracking,TokenDbOptimizer}.php` (schema/inserts/aggregations/retention/backfill/migration + usermeta index lifecycle byte-identical); `init()` standalone-only; 13 characterization tests |
| D3f | Tool token limits | ✅ | `src/Analytics/ToolTokenLimits.php` (constants/tiers/role map/multipliers/usage shape/session accounting/forecasting/anomaly detection/hooks byte-identical); settings + workload-tier + logging seams per mode (workload tier defaults `medium` standalone until Resource Manager ports in D4); `init()` standalone-only, deactivation hook monolith-skipped; 35 characterization tests |
| D3 | Model management + analytics/token tracking | ✅ | D3a–D3f done (87 new ecosystem tests); suite now 270 tests / ~1171 assertions green in both monolith + standalone matrices, PHPCS-clean |
| D4a | Provider circuit breaker | ✅ | `src/Security/ProviderCircuitBreaker.php` (transient keys/state constants/threshold+timeout filters byte-identical); dormant until providers adopt; 9 characterization tests |
| D4b | Encrypted API key store | ✅ | `src/Security/ApiKeyStore.php` (option prefix/managed keys/migration semantics byte-identical); crypto seam: base `WP_MCP_AI_Encryption` monolith / parent `Remote\Crypto` standalone; logging monolith-only; 10 characterization tests |
| D4c | Security audit logger | ✅ | `src/Security/SecurityAuditLogger.php` (table `wp_mcp_ai_security_log`, event constants, `wp_mcp_ai_security_event` action, REST endpoint `mcp-ai/v1/security/events`, purge cron byte-identical); `register()` + activation table creation standalone-only; 7 characterization tests with real DDL |
| D4d | Request guard | ✅ | `src/Security/RequestGuard.php` (SSE counter keys/REST priorities/body+JSON limits/verbosity stripping/asset-version stripping byte-identical); settings seam per mode; SSE slots delegate to base `WP_MCP_AI_SSE_Rate_Limiter` monolith / ported `SseRateLimiter` standalone; `register()` standalone-only; 11 characterization tests |
| D4e | URL guard (SSRF) | ✅ | `src/Security/UrlGuard.php` (blocked CIDRs/hostnames/error codes/filters byte-identical); loopback-hostname shortcut delegates to base `WP_MCP_AI_HTTP_Helper` monolith, DNS-based checks standalone; 11 characterization tests |
| D4f | Security posture scoring | ✅ | `src/Security/SecurityPosture.php` (23-signal set/weights/grades/quick wins/`wp_mcp_ai_security_posture_signals` filter byte-identical); settings + restriction-registry seams per mode; 7 characterization tests |
| D4g | Concurrency guard + subscriber + exception | ✅ | `src/Security/{ConcurrencyGuard,ConcurrencyGuardSubscriber}.php` + `Exceptions/ConcurrencyLimitReached.php` (slot table/limits map/filter/priorities/envelopes byte-identical); capability-flag + tool-resolution seams per mode; subscriber `register()` standalone-only; 10 characterization tests with real DDL |
| D4h | Cost tracker + subscriber + exception | ✅ | `src/Security/{CostTracker,CostTrackerSubscriber}.php` + `Exceptions/CostBudgetExceeded.php` (pricing map/estimate heuristics/budget meta/spend option/filters/envelopes byte-identical); subscriber `register()` standalone-only; 14 characterization tests |
| D4i | Destructive ops gate + exception | ✅ | `src/Security/DestructiveOpsGate.php` + `Exceptions/DestructiveConfirmationRequired.php` (flag vocabulary/confirmation values/preview payload/rejection action/428 envelope byte-identical); settings + flag + audit seams per mode; `register()` standalone-only; 8 characterization tests |
| D4j | CSP headers | ✅ | `src/Security/CspHeaders.php` (directive set/filters/header names byte-identical); `is_admin_context()`/`send_header()` protected seams for testability; `register()` standalone-only; 3 characterization tests |
| D4k | Load guard | ✅ | `src/Security/LoadGuard.php` (counter key/429 envelope/filter byte-identical); max-concurrency + job-queue aggregation monolith-only (standalone counts the transient counter until Resource Manager ports — tracked); `register()` standalone-only; 5 characterization tests |
| D4 | Security stack (10 classes + circuit breaker) | ✅ | D4a–D4k done (95 new ecosystem tests); suite now 365 tests / ~1585 assertions green in both monolith + standalone matrices, PHPCS-clean |
| D5a | Assistant directory REST controller | ✅ | `src/Rest/AssistantController.php` (routes `mcp-ai/v1/assistants` GET/POST/DELETE, directory contract: summary fields/rest links/capabilities/implementation/X-WP-Total headers, search/include/pagination/_fields, create/delete with meta persistence — byte-identical); auth = CG-AI caps; settings/config/cache/access-validation seams per mode; token scope deferred to D-UI; `registerRoutes()` standalone-only; 17 characterization tests |
| D5b | Tools listing REST controller | ✅ | `src/Rest/ToolsController.php` (route `mcp-ai/v1/tools` GET, `tools` contract name/description/inputSchema, assistant scoping, `_fields` filtering, cache — byte-identical); registry seam: base `WP_MCP_AI_Tool_Registry` monolith / nvoos-core registry via `CoreBridge` standalone (camelCase→snake_case wrapper); `registerRoutes()` standalone-only; 8 characterization tests |
| D5c | MCP JSON-RPC controller | ✅ | `src/Rest/McpController.php` (routes `mcp-ai/v1/mcp` GET/POST/OPTIONS + `/no-sse` + `/sse`; JSON-RPC 2.0 envelope: parse errors -32700, invalid request -32600, header mismatch -32020, method-not-found -32601, batching max 20, notifications 202, `_meta` serverInfo stamp, Mcp-Method/Mcp-Name headers; `server/discover` with version negotiation + inline tools + `wp_mcp_ai_mcp_discover_instructions`/`wp_mcp_ai_discover_include_tools` filters; `tools/list` delegates to `ToolsController` (composition) + annotations + `ttlMs`/`cacheScope`; `resources/list|read`, `prompts/list|get`, `completion/complete`, `logging/setLevel`, `notifications/cancelled`; CORS headers from `cors_allow_origin` settings — byte-identical); deviations documented: SSE sessions deferred (GET /mcp + /sse return discovery JSON, `sse.enabled=false`), `tools/call` execution landed with D8 Cluster 0 (per-mode registry seam, canonical envelope), no OAuth scope enforcement (stub seam), no default-assistant resolution, no token scoping; auth = CG-AI caps; 33 characterization tests |
| D5d | Chat compat REST route | ✅ | `src/Rest/ChatCompatController.php` (route `mcp-ai/v1/chat` POST/GET, POST args byte-identical: assistant_id (int|string), messages (required + base validator rules), options (provider/model/temperature/stream/response_format), professional_prompt; handler translates the base options envelope → flat CG-AI params and delegates to `ChatController::handleChat()` — response envelope (`success`/`data`/`tool_results`/`iterations`/`cost`/`cache_metadata`) already matches the base; temperature clamp [0,2] base contract); deviations documented: `assistant_id`/`professional_prompt`/`options.response_format` accepted but not applied (assistant/profession runtimes not ported — D-UI-6), content-part arrays JSON-encoded, GET SSE handshake → `wp_mcp_ai_sse_chat_deferred` (501, streaming via POST `options.stream`); auth = CG-AI caps; 19 characterization tests |
| D5 | REST surface (mcp-ai/v1 chat/tools/assistants/MCP methods) | 🟡 | D5a–D5d done (77 new ecosystem tests); remaining: file-download/cron-status endpoints — land with the queues wave |
| D6a | WP-CLI core commands | ✅ | `src/Cli/` (`StatusCommand` — environment summary with install mode + runtime facts, base `status` shape; `ToolsCommand` — registry inventory via the same monolith/standalone seam as `ToolsController`, `--filter`; `ProvidersCommand` — provider slugs + credential state via `CredentialResolver` + default flag; `SettingsCommand` — `nvoos_content_graph_settings` reader, secrets refused (list + get); `GraphCommand` — parent-schema row counts with honest `unavailable` for missing tables) + `src/Cli.php` hub registration; data logic in plain static methods (no WP-CLI dependency, testable), `run*()` wrappers thin; 12 characterization tests |
| D6 | WP-CLI surface (20+ commands) | 🟡 | D6a done (6 new commands: status / tools list / providers list / settings list|get / graph stats + existing migrate-keys/key-status = 8); remaining base commands (queue stats, token, plugins, stdio, DLQ/SLA/restrictions) land with their owning waves (queues/tokens/mesh) |
| D7 | Agent memory CCT bridge/migrator/reader | ✅ | `src/Memory/` (`AgentMemoriesCct` — JetEngine CCT registration, slug `ai_agent_memories`/field base 30000/args + meta-fields byte-identical, REST writes disabled; `AgentMemoryCctBridge` — `wp_mcp_ai_memory_stored|deleted` listeners, record mapping/tier classification/content-hash normalisation/upsert-by-context_id/warn throttling byte-identical; `AgentMemoryCctMigrator` — version option + target 2 + enable filter byte-identical, opportunistic version advance when disabled; `AgentMemoryCctReader` — `wp_mcp_ai_recall_memory_candidates` hydration + transient-shaped records, cap 500, error-tolerant queries byte-identical); CCT class resolves per mode (base class monolith / ported class standalone); all `bootstrap()` standalone-only; dormant-until-gaps documented (standalone memory-event emission + recall consumers land with the tools/memory wave); 19 characterization tests |
| D8 | Tool execution port (base tool inventory + `tools/call`) | ✅ | Plan: [`d8-tool-execution-port-plan.md`](plans/d8-tool-execution-port-plan.md). Cluster 0 (execution spine) landed 2026-09-05: `McpController::mcp_tools_call()` executes via the per-mode registry seam (base registry monolith / nvoos-core registry standalone with the WordPress `AuthProvider`), assistant scoping when an `assistant_id` is supplied, the byte-identical tool rate limiter, the before/after execution hooks, `wp_mcp_ai_tool_output` + token-budget adjustment, and MCP content shaping (`is_mcp_content_array` / `convert_to_text_content`). Cluster 1 (registration) landed 2026-09-05: `CoreToolFactory` + a 196-entry `CoreToolManifest` wire the pre-ported nvoos-core inventory into the standalone registry (monolith-gated). Cluster 2 (port the self-contained base tools) landed 2026-09-05: 2a hardened the seven base-coupled adapter tools (`vectorize_image`, `media_library_optimizer`, `run_gemini_managed_agent`, `delegate_to_a2a_agent`, `probe_chat`, `query_mesh_intelligent`, `visualize_workflow_metrics` — `accessDenied`→`forbidden`, defined-gated degradation envelopes); 2b ported 6 tools (taxonomy quartet, `list_mcp_tools`, `get_environment_status`); 2c ported the remaining 26 self-contained tools (`enable_reasoning_mode`, `add_model_config`, `research_model`, `discover_new_models`, the 6-tool security batch, `get_site_health`, `get_system_logs` + validated, and the 13-tool media/memory/content/usage batch) with per-mode seams (`ModelConfigStore` for `wp_mcp_ai_model_configs`, `WordPressNativeTrait` for hooks/cache keys, `ReasoningController`, context-manager/provider/settings/helper seams). Cluster 3 (harness tools) landed 2026-09-05: the 8 harness tools (`evolve_harness`, `apply_prompt_cue`, `list_prompt_cues`, `record_reflection`, `retrieve_with_provenance`, `scope_memory`, `select_prompt_cue`, `self_consistency_vote`) live in `plugins/nvoos-content-graph-ai-platform/src/Harness/Tools/` and surface through the CG-AI core registry via `HarnessToolRegistrar` (standalone-only, monolith no-op); per-mode evolver/bootstrap seams use the Wave C platform ports standalone; registry-dependent paths keep the documented degradation envelopes (`wp_mcp_ai_self_refine_no_registry`, empty `RetrievalHarness` fan-out). Validation: ecosystem suite 611 tests / 5320 assertions green in both matrices; platform harness suite 36 tests green in both platform matrices; live design-stack smoke — 270 tools executing, harness tools exercised end-to-end. Cluster 4 (deferred buckets) is documentation-only and stays truthful via the owning-wave cross-references below |

## Wave D-UI — AI UI → `nvoos-content-graph-ai`

| # | Subsystem | Status | Notes |
|---|---|---|---|
| D-UI-1a | Guest token flow | ✅ | `src/Chat/GuestToken.php` (constants/TTL caps/transient prefix, issuance with origin binding + `wp_mcp_ai_guest_token_issuance_origin`, validation with absolute-max lifetime, assistant scoping, `wp_mcp_ai_guest_token_allowed_origins` allowlist, sliding TTL, header/param extraction — byte-identical); TTL setting seam per mode (`guest_token_lifetime`); REST integration additive: `ChatCompatController::permissions_check` + the core `/ai/chat` permission accept valid assistant-scoped guest tokens; 12 characterization tests |
| D-UI-1b | Frontend chat widget | ✅ | `src/Frontend/ChatShortcode.php` (`[nvoos_content_graph_chat]` shortcode per `CHAT-SHORTCODE-PLAN.md` — unique container IDs, `assistant`/`allow_guests`/`provider`/`model`/`height`/`show_cost`/`placeholder` atts, asset enqueue, `window.NvoosContentGraphChat` inline config, guest-token issuance when `allow_guests` + resolvable assistant); `assets/js/content-graph-ai-chat-frontend.js` (lean vanilla widget: SSE streaming via the shared `NvoosContentGraphAiSse` parser, sessionStorage transcripts, tool cards, cost badge, markdown-lite rendering — aligned with SPA-v2 contract, NOT a byte-port of the base chat.js bundle — documented deviation); widget CSS appended to `content-graph-ai-chat.css`; registered in both modes (tag never collides with the base's `[mcp_ai_chat]`); 9 characterization tests |
| D-UI-1 | Chat widget (chat.js) + guest token flow | ✅ | D-UI-1a + D-UI-1b done (21 new ecosystem tests); the base's full chat.js bundle (voice/webrtc/Pro services) remains with the base hub — the ecosystem ships the aligned lean widget instead (per `CHAT-SHORTCODE-PLAN.md`) |
| D-UI-2 | Chat bubble + blocks + shortcodes | ✅ | `src/Blocks/{Blocks,ChatBlock,ChatBubbleBlock}.php` — hub + `nvoos-content-graph-ai/chat` + `nvoos-content-graph-ai/chat-bubble` blocks (server-rendered wrappers of `[nvoos_content_graph_chat]` with attribute→shortcode mapping, `sanitize_hex_color` colours, `sanitize_text_field` titles/tooltips, category `nvoos-content-graph-ai`); `assets/js/blocks/content-graph-ai-blocks.js` (editor registrations + inspector controls); `assets/js/content-graph-ai-chat-bubble.js` + `assets/css/content-graph-ai-chat-bubble.css` (toggle/close/Escape/click-outside/auto-open/remember-state + `nvoos-cg-bubble:open|close` events, `nvoos-cg-bubble*` classes so both bubbles coexist in monolith); wired into `Plugin.php` in both install modes; deviations: saveTranscript/enableStreaming/allowSensitiveTools/template/showBuildButton dropped (no CG counterpart), eager widget init (no lazy-init); 8 characterization tests |
| D-UI-3 | Elementor chat-family widgets | ✅ | `src/Elementor/{ElementorHub,ChatWidget,ChatBubbleWidget}.php` — `nvoos_cg_chat` (thin wrapper of `[nvoos_content_graph_chat]`, controls: assistant/allow_guests/provider/model/height/show_cost/placeholder) + `nvoos_cg_chat_bubble` (bubble markup mirroring `ChatBubbleBlock::render()` — `nvoos-cg-bubble*` classes, `--nvoos-cg-bubble-*` CSS vars, data attrs; controls: position/size/animation/tooltip, panel title/width/height, auto-open/remember/badge, bubble/header colours); panel category `nvoos-content-graph-ai`; hub no-ops without Elementor (`Widget_Base` guard in widget files + runtime checks); wired into `Plugin.php` in both install modes; deviations: base chat.js widget surface + extra bubble controls (icon/hover/badge colours/panel radius) deferred; 8 characterization tests |
| D-UI-4 | Assistant builder/test/add pages + block set | ✅ | Pages + CPT (31 tests) plus the assistant block set: `AssistantSelectorBlock`, `ToolsGridBlock` (per-mode registry seam: base registry monolith / CoreBridge standalone, single "Other tools" group standalone — deviation), `KnowledgeBaseBlock` (wp/v2/media uploads), `AssistantBuilderBlock` (composite; chat section embeds the CG widget, build submits to the per-mode create action — deviations); editor + frontend JS + CSS; the Build Assistant Prompt tab now embeds the tools grid + KB components; 18 characterization tests green in both matrices. `professional-selector` stays deferred (profession runtime wave); tool presets stay deferred (presets wave) |
| D-UI-5 | Settings shell + sections | ✅ | Validator port (`src/Admin/Settings/SettingsValidator.php` — base method/error-code contract), `AiSection` validate-then-sanitize contract (rejected sections record a settings error and preserve previous values via the parent's merge-on-existing flow), `SettingsRegistry` facade forwarding to the parent plugin's registry (core consumed, never modified) with the public `nvoos_content_graph_ai/register_settings_sections` hook for the platform (Wave E-UI); the four existing sections re-based onto `AiSection` with real validations (provider/model required-when-enabled, temperature 0–2, max-tokens 1–128000); 14 characterization tests green in both matrices. Renderer stays the parent plugin's `Section` renderer (consumed); the base's subtab renderer ports with its owning sections |
| D-UI-6 | Assistant editor + metadata wave | 🟡 | `AssistantPostType` already registers the CPT standalone with 5 REST-visible meta keys (`_wp_mcp_ai_tools`, `_wp_mcp_ai_provider`, `_wp_mcp_ai_model`, `_wp_mcp_ai_temperature`, `_wp_mcp_ai_system_prompt`); `_wp_mcp_ai_memory_files`/`_wp_mcp_ai_primary_roles`/`_wp_mcp_ai_source_profession` are declared but never `register_post_meta()`'d. Remaining: port the base's 14 assistant metaboxes — 4 inline (Context Window Estimator, Available Tools, Prompt Shortcuts, Access Control) + 10 dedicated classes in `includes/assistants/metaboxes/` (Credentials, Defaults, Primary_Roles, Base_Knowledge, Mesh_Routing, Datasets, Skills, MCP_Apps, Harness_Profile, Artifact_Governance); complete `register_meta` parity for the base's ~25 keys (incl. `_wp_mcp_ai_prompt_caching`, `_wp_mcp_ai_vector_store_id`, `_wp_mcp_ai_corpus_name`, `_wp_mcp_ai_skills*`, `_wp_mcp_ai_tool_shortcuts*`); wire assistant-config consumption into the chat flow (today `ChatController` hardcodes `assistant_id = 0` — "Not tied to a specific assistant post"); and port credential issuance (`_wp_mcp_ai_credentials` via `WP_MCP_AI_Credentials`) or record an explicit deferral of token scoping. Exit gate: a standalone chat turn with an `assistant_id` resolves that assistant's system prompt/model/tools |

## Wave E — Operator runtime → `nvoos-content-graph-ai-platform`

| # | Subsystem | Status | Notes |
|---|---|---|---|
| E1 | Workflow engine + CPTs + triggers | 🟡 | Sub-cluster 1 (CPTs) landed: `WorkflowCpt` ported (`src/Workflows/WorkflowCpt.php`): byte-identical `mcp_ai_workflow` CPT args (hidden-menu UI, `manage_options` caps, title/editor/revisions/custom-fields supports), graph/version/tags meta keys + sanitize/auth callbacks, graph read/write with malformed-JSON fallback, the portable export/import JSON contract (`schema_version` 1.0), semver bumping. `WorkflowRunCpt` ported (`src/Workflows/WorkflowRunCpt.php`): byte-identical `mcp_ai_workflow_run` CPT args, ten run meta keys, run lifecycle (create → append events → status transitions with terminal `finished_at` stamping), event-log read path, full run-record shape, four-dimension budget check + `wp_mcp_ai_workflow_run_budget_exceeded` action. `WorkflowTriggerCpt` ported (`src/Workflows/WorkflowTriggerCpt.php`): byte-identical `mcp_ai_trigger` CPT args, five trigger meta keys, enabled-trigger discovery, all six trigger-type hook bridges (`post_status_change`, `cron_schedule`, `a2a_inbound`, `user_registration`, `comment_published`, `file_upload`), fire path (`last_fired_at` stamp + `wp_mcp_ai_trigger_fired` + dispatcher/engine hand-off); execution hand-off resolves per install mode (base `WP_MCP_AI_Workflow_Dispatcher`/`Workflow_Engine_V2` monolith — boot-gated probes — platform `Workflows\Dispatcher`/`WorkflowEngine` once the later sub-clusters port; null → documented no-hand-off degradation standalone until then). Standalone-only wiring via `Plugin::registerWorkflowCpts()` (init priorities 12/13/14/20 — the base loader owns the same registration monolith); inline meta schemas promoted to named constants (additive); 25 characterization tests green in both matrices (real posts, all six trigger bridges exercised). Sub-cluster 2 (dispatcher) landed: `Dispatcher` ported (`src/Workflows/Dispatcher.php`): byte-identical `wp_mcp_ai_workflow_executor` filter contract (null = defer, array|WP_Error = ownership), the default-executor fallback chain (Engine V2 when available and enabled), and the `no_workflow_executor` error envelope; the default executor resolves per install mode (`engine_class()` seam — base `WP_MCP_AI_Workflow_Engine_V2` monolith boot-gated / platform `Workflows\WorkflowEngine` once the engine sub-cluster lands; null → documented `no_workflow_executor` degradation standalone until then); `is_enabled()` method_exists-guarded (documented hardening); static utility, no hooks of its own — `WorkflowTriggerCpt::dispatcher_class()` resolves it standalone; 7 characterization tests green in both matrices. `TriggerRegistry` ported (`src/Workflows/TriggerRegistry.php`): byte-identical singleton lifecycle, the `register()` contract (sanitize_key'd type + wp_parse_args defaults), the `get_triggers()`/`get_trigger()` lookups, the `wp_mcp_ai_register_workflow_triggers` extension action, and the seven built-in trigger definitions with their labels/descriptions/schemas; lazy singleton, no hooks of its own — the trigger admin/REST surfaces (E-UI-2) consume it; 6 characterization tests green in both matrices. `WorkflowEngine` ported (`src/Workflows/WorkflowEngine.php`): byte-identical `wp_mcp_ai_workflow_v2_enabled` gate (default true), `manage_options`/post/CPT guards, `wp_mcp_ai_workflow_v2_before_execute|after_execute` lifecycle actions, `wf2-` run identifiers, durable run records (create → running → step events → terminal status), graph → node-description → `execute_workflow` tool delegation with the byte-identical argument shape, the no-tool/no-registry degradation branches, and the result envelope; workflow/run CPT classes + the tool registry resolve per install mode (base classes monolith boot-gated / platform `WorkflowCpt`+`WorkflowRunCpt` + the AI addon's `CoreBridge` tools standalone — the established `QueueManager` seam); static utility, no hooks of its own — `Dispatcher`'s `engine_class()` + `WorkflowTriggerCpt`'s fallback resolve it standalone; 10 characterization tests green in both matrices (fake-tool delegation chain, run-record roundtrip, budget forwarding); the dispatcher + trigger seam expectations updated to the new engine resolution. `PatternConstants` + `PatternWorkflowTemplates` ported (`src/Workflows/`): byte-identical eight pattern slugs + catalog/validity/description helpers, the eight-template catalog with names/roles/step shapes, `get_workflow_template()`/`get_all_templates()` lookups, the `customize_template()` contract (custom-role merge + `wp_mcp_ai_customize_workflow_template` filter, no mutation), and the toolkit-recommendation path; the toolkit registry resolves per install mode (`get_toolkit_registry()` seam — base `WP_MCP_AI_Toolkit_Registry::get_instance()` monolith boot-gated / null standalone until the Pro-tier registry ports, documented degradation; the base's direct `new` against the protected constructor is replaced by the singleton — documented hardening); 9 characterization tests green in both matrices. `AgenticWorkflowOptimizer` ported (`src/Workflows/AgenticWorkflowOptimizer.php`): byte-identical optimization constants (cache TTL/staleness, `opt-` key prefix, compression threshold, 90% quality bar), the metric collection lifecycle (`start_metrics_collection()` seeding, `record_metric()` with the non-numeric-set quirk, `get_metrics()`/`get_metric_stats()` shapes, the `wp_mcp_ai_metrics_collected` action), the tool-result cache cycle (`should_cache_tool_result()` + `wp_mcp_ai_cacheable_tool` allowlist filter, `cache_tool_result()`/`get_cached_tool_result()`/`check_tool_cache()` with the byte-identical singular/plural metric-key quirk, sanitized `opt-{md5}-{suffix}` transient keys), the gzip+base64 compression contract, the low-necessity iteration tracker (5-consecutive nudge, per-session counters, `reset_low_necessity_count()`), and `predict_optimal_iterations()` (95th percentile × 1.2, clamped 5–50, default 15, int/float return quirk); logging resolves per install mode (`logging_enabled()` — base `WP_MCP_AI_Admin_Settings` monolith is_callable-guarded / `wp_mcp_ai_settings` option standalone; `log_event()` — base `WP_MCP_AI_Logger` monolith, dormant standalone); `get_iteration_history()` dormant placeholder standalone (byte-identical); constructor-driven hooks only (consumers wire it with D-UI-6/E-UI-2) — no `Plugin.php` registration; 16 characterization tests green in both matrices. Remaining: — (E1 wave complete) |
| E2 | Queues/ops (jobs, DLQ, SLA, cron, notifier) | 🟡 | `AsyncJobQueue` ported (`src/Queues/AsyncJobQueue.php`): byte-identical schema/constants/cron hooks/error codes, minute interval registered (documented deviation — the base relies on an external registration), executor filter seam (`nvoos_content_graph_ai_platform/async_job_executors`) so E1 registers executors without touching the queue, dormant method_exists-guarded seams for the Action Scheduler bridge / DLQ / notifier / logger until those pieces port (documented hardening — the base's own calls target methods its DLQ/notifier classes don't expose); standalone-only via `Plugin::registerQueues()`; 19 characterization tests green in both matrices; drive-by fix: `AgentCapabilityBoundaryHooks` extracted to its own PSR-4 file (autoload gap caused a standalone registration fatal). `QueueManager` ported (`src/Queues/QueueManager.php`): byte-identical mode/priority constants, thresholds + estimates table, capability-flag semantics, deferred-result envelope, `wp_mcp_ai_before_tool_execute` filter (priority 5) + `wp_ajax_wp_mcp_ai_queue_status` action; RabbitMQ client + tool registry resolve per install mode (base classes monolith / AI addon's RabbitMqClient + CoreBridge standalone); standalone-only via `Plugin::registerQueueManager()`; 18 characterization tests green in both matrices. `JobQueueManager` ported (`src/Queues/JobQueueManager.php`): byte-identical `mcp_ai_concurrent_jobs` schema, priority/status constants, `DEFAULT_MAX_CONCURRENT`/`DEFAULT_JOB_TIMEOUT`, enqueue validation + callable extraction, atomic `SELECT … FOR UPDATE SKIP LOCKED` claiming with priority ordering, process-cycle envelopes (`success`/`at_capacity`/`no_pending_jobs`), retry-then-fail with `last_error`, stale-job timeout cleanup, stats + clear; custom-table only — the base's deprecated option-fallback storage intentionally dropped (documented deviation); seams: `default_max_concurrent()` (base `WP_MCP_AI_Resource_Manager` monolith / `DEFAULT_MAX_CONCURRENT` standalone), `sla_available()`/`dead_letter_available()`/logging dormant until their owning E2 pieces port; 13 characterization tests green in both matrices (standalone + monolith on WP 6.9 + 7.1) with real DDL. `DeadLetterQueue` ported (`src/Queues/DeadLetterQueue.php`): byte-identical `mcp_ai_dead_letters` schema, type/limit/retention constants (incl. the `TYPE_MESH_QUERY`-not-valid quirk), error codes, item shapes, stats shape, `wp_mcp_ai_dlq_item_added` action, `wp_mcp_ai_dlq_retention_days` filter + `wp_mcp_ai_dlq_cleanup` cron; custom-table only (legacy option storage dropped — documented deviation); retry dispatch resolves per install mode (base manager/notifier/executor monolith — probes gated on the base plugin being booted, documented hardening — platform `JobQueueManager` standalone); `JobQueueManager`'s exhausted-failure forwarding now resolves the DLQ per install mode; standalone-only cron wiring via `Plugin::registerDeadLetterQueue()`; 23 characterization tests green in both matrices with real DDL. `RateLimitManager` ported (`src/Queues/RateLimitManager.php`): byte-identical transient prefix, delay/retry constants, backoff multiplier, `wp_mcp_ai_rate_limit_*` filters, error codes, retriable status/timeout tables, Retry-After resolution, and the `execute_with_retry()` loop (sleep byte-identical — callers control wall-clock via delay options); logging through a dormant seam; no hooks of its own; 21 characterization tests green in both matrices. `SchedulerBridge` ported (`src/Queues/SchedulerBridge.php`): byte-identical `wp_mcp_ai_run_async_job` runner hook + `wp-mcp-ai-jobs` default group, availability contract (`as_enqueue_async_action` presence + `wp_mcp_ai_async_scheduler_bridge_enabled` filter), idempotent `register_hooks()`, group filter with empty-value fallback, enqueue semantics (action ID / `false` = WP-Cron fallback); `run_job()` resolves the executing queue per install mode (base `WP_MCP_AI_Async_Job_Queue` monolith — boot-gated probe — platform `AsyncJobQueue` standalone); `AsyncJobQueue`'s dormant bridge seams now resolve per install mode (`scheduler_bridge_available()` + `scheduler_bridge_class()`), so queued jobs fast-dispatch through the resolved bridge; 16 characterization tests green in both matrices (AS stub when Action Scheduler is absent / real AS API when present), PHPCS-clean. `OutboundWebhook` ported (`src/Queues/OutboundWebhook.php`): byte-identical `wp_mcp_ai_outbound_webhooks` option, subscription lifecycle (`whk_*` lowercase IDs, sanitized URL/events, enabled flag), signed non-blocking dispatch (JSON body, `sha256=` HMAC header pair, event/delivery headers), inbound signature verification via `hash_equals`, and the four core event listeners (`wp_mcp_ai_workflow_run_completed|failed|paused`, `wp_mcp_ai_approval_requested`); standalone-only listener registration via `Plugin::registerOutboundWebhook()` (boot-gated — the base loader owns the same registration monolith); 11 characterization tests green in both matrices (HTTP intercepted via `pre_http_request`), PHPCS-clean. `CronManager` ported (`src/Queues/CronManager.php`): byte-identical `wp_mcp_ai_cron_jobs` option, argument normalisation (positional vs wrapped associative), record/get/remove lifecycle with idempotent re-record (created_at + first_timestamp preserved), WP-Cron unschedule for single + recurring events, retention-window pruning (`cron_job_retention_period`, default 24h, zero-retention immediate removal, missing/zero first-timestamp pruning), stable `md5( wp_json_encode() )` job IDs with the public static wrapper, legacy-entry normalisation on load, corrupted-option reset; standalone-only `init` prune hook via `Plugin::registerCronManager()` (boot-gated); retention setting resolves per install mode (base `WP_MCP_AI_Settings_Registry` monolith / direct `wp_mcp_ai_settings` option read standalone — identical effective behavior); 22 characterization tests green in both matrices, PHPCS-clean. `SlaManager` ported (`src/Queues/SlaManager.php`): byte-identical tier/priority/SLA-target/concurrency constants, capability-flag tier inference (`sla_tier` explicit → realtime/interactive/ui-blocking → background-only/long-running → async/may-timeout), Little's Law capacity math, queue-metrics analysis, tuning recommendations (metric reads `isset`-guarded — documented hardening over the base's unconditional reads), compliance tracking/statistics (same `wp_mcp_ai_sla_compliance_log` option, 1000-entry cap, 24h window + tier filter, percentile interpolation), dashboard data + health grades; settings keep the base option key (`wp_mcp_ai_settings`: `sla_prioritization_enabled`, `sla_*_concurrent`) so tuning survives mode transitions; queue-stats seam resolves per install mode (base `WP_MCP_AI_Job_Queue_Manager` monolith / platform `JobQueueManager` standalone, base probe gated on `defined('WP_MCP_AI_PATH')`); `JobQueueManager`'s dormant SLA seams now resolve per install mode via `sla_class()` (base monolith / platform `SlaManager` standalone, boot-gated probe) and `sla_available()` follows the enabled flag through the resolved class; static utility, no hooks of its own; 31 characterization tests green in both matrices (monolith + standalone on WP 6.9) incl. real-DDL enqueue tier→priority wiring, PHPCS-clean. `JobNotifier` ported (`src/Queues/JobNotifier.php`): byte-identical cache prefix/duration (`wp_mcp_ai_job_status_*`, 3600), webhook option key (`wp_mcp_ai_job_webhooks`), per-job webhook/step caps (10/50), lifecycle hooks (`wp_mcp_ai_job_started|progress|completed|failed` + crawl4ai/web-search/veo adapters), error codes (`invalid_webhook_url|scheme`, `private_ip_blocked`, `webhook_validation_failed`, `invalid_job_id`, `too_many_webhooks`), the `wp_mcp_ai_tool_sla_tier_map` filter, retry transients (`wp_mcp_ai_webhook_retry_*` → DLQ at 3 attempts), Little's Law metrics, the dot-preserving cache-key sanitisation, and the `wp_mcp_ai_emit_sse_event` action (gated on `WP_MCP_AI_SSE_ACTIVE`); the hourly `wp_mcp_ai_cleanup_job_cache` cron registers through a static `schedule_cleanup()` method (documented deviation — same event, no global function); collaborators resolve per install mode (base cron manager / DLQ / job store monolith — boot-gated probes — platform `CronManager` + `DeadLetterQueue` standalone; job store + logger dormant standalone; webhook secret via the AI addon's `ApiKeyStore` standalone); standalone-only hook registration via `Plugin::registerJobNotifier()`. `JobNotifierRestController` + `SseStream` ported (`src/Rest/`): byte-identical `mcp-ai/v1` jobs stream/status/webhooks routes, parameter schemas, permission envelopes (`wp_mcp_ai_missing_credentials`, `rest_invalid_nonce`, `rest_forbidden`, `job_not_found`, `unauthorized`), entity-ownership authorization model, the buffered SSE framing (connected/status/complete/close + heartbeat comments + `wp_mcp_ai_sse_stream_started|chunk_sent|ended` hooks), parameter clamping (10–600 s / 1–30 s), and CORS resolution; authenticator degrades standalone (`wp_mcp_ai_auth_unavailable` 503 for mesh/bearer/guest branches — nonce auth works in both modes); security headers monolith-only (documented); standalone-only route registration via `Plugin::registerJobNotifierRest()`. 54 characterization tests green in both matrices (35 notifier + 19 REST, real-DDL DLQ forwarding). Remaining: — (E2 wave complete) |
| E3 | Approvals | ✅ | `ApprovalQueue` ported (`src/Approvals/ApprovalQueue.php`): byte-identical `mcp_ai_approval` CPT + args (hidden, `title`+`custom-fields` supports, no rewrite/query_var/REST), meta keys (`_wp_mcp_ai_approval_*`), error codes (`approval_missing_tool|not_found|already_resolved|forbidden`), TTL floor (60 s, default 24 h), post-status → approval-status mapping (`pending`/`publish`→approved/`private`→denied/`trash`→expired), resolved-by/resolved-at/note audit meta, and the `wp_mcp_ai_approval_queued|approved|denied` + `wp_mcp_ai_approvals_cleanup_done` actions (cleanup payload byte-identical: the full expiry-probe result, not just the trashed subset); weekly `wp_mcp_ai_approval_cleanup` cron; standalone-only wiring via `Plugin::registerApprovals()` (CPT priority 11, cron priority 1 — the base loader owns the same registration monolith); literal meta keys/limits promoted to named constants (additive only); 18 characterization tests green in both matrices (real posts, both modes). Remaining: approvals REST controller + admin page land with E-UI-2 |
| E4 | Tenant + integrations (OAuth, Google Calendar, site-builder, conversation-import) | 🟡 | Sub-cluster 1 (tenant) landed: `TenantContext` ported (`src/Tenant/TenantContext.php`): byte-identical singleton lifecycle, the four-source resolution order (header `X-WP-MCP-AI-Tenant` → user meta `_wp_mcp_ai_tenant` → assistant meta `_wp_mcp_ai_bound_tenant` via `assistant_id` → multisite blog ID), the per-source error codes, the fail-closed `tenant_not_resolved` envelope, request-lifetime caching, and the set/accessor contract. `TenantRepository` ported (`src/Tenant/TenantRepository.php`): byte-identical context binding (sanitize_key/absint), strict-mode flag, the prepared `tenant_where()` fragment with the `1=1` bypass, the fail-closed `require_tenant()` RuntimeException, the `tenant_meta_query()` clause, and `save_tenant_meta()` post stamping. `TenantOptions` ported (`src/Tenant/TenantOptions.php`): byte-identical `wp_mcp_ai_{type}_{id}_{key}` scoped + `wp_mcp_ai_{type}_{key}` type-level key formats, get/update/delete wrappers with autoload pass-through, `from_context()` null-on-unresolved factory; resolves this package's `TenantContext` (documented deviation). `TenantDatabase` ported (`src/Tenant/TenantDatabase.php`): byte-identical `mcp_ai_tenants` + `mcp_ai_tenant_user_map` dbDelta schemas, `1.0.0` version + `wp_mcp_ai_tenant_db_version` gate, cached `tables_installed()` SHOW TABLES probe, drop_tables() uninstall path, assign_user()/get_user_tenants() with the primary user-meta side write; standalone-only hook registration via `Plugin::registerTenant()` (the base tenant init.php owns the same `admin_init`/`wp_mcp_ai_activate` hooks monolith). `TenantFeatureFlags` ported (`src/Tenant/TenantFeatureFlags.php`): byte-identical option keys, `WP_MCP_AI_TENANT_ISOLATION` constant override, settings-toggle read, global-on→opt-out / global-off→opt-in toolkit resolution, `require_isolation()`, the LIKE-based enabled-toolkit scan. `TenantMigration` ported (`src/Tenant/TenantMigration.php`): byte-identical tenant column/index DDL, INFORMATION_SCHEMA column probe, SHOW INDEX probe, CPT meta migration, tenant_id=0 backfill. `TenantBootstrap` ported (`src/Tenant/TenantBootstrap.php`, wraps the base init.php): byte-identical hook surface — `rest_api_init` REST-meta registration (`_tenant_type` string / `_tenant_id` integer on public post types, `edit_posts` auth), conditional admin Tenant column, `pre_get_posts` read filters (broad 10 + scoped-only 15), the `wp_mcp_ai_tenant_scoped_post_types` filterable registry (36 slugs), `save_post` 20 auto-stamp, the 18-table migratable registry, and the `wp_mcp_ai_after_plugin_upgrade` bulk migration; global functions → static methods (documented deviation); standalone-only via `Plugin::registerTenant()`. `TenantCliCommand` ported (`src/Tenant/TenantCliCommand.php`): byte-identical `wp mcp tenant` list/create/assign/migrate/status/toggle surface; self-registers on autoload under the `WP_CLI` gate (documented deviation). 78 characterization tests green in both matrices (7 files; real-DDL suites suspend the harness TEMPORARY-table rewrite); full platform suite 588 tests, 0 failures, 0 risky in both matrices. Sub-cluster 2 (OAuth) landed: `OAuthManager` ported (`src/Integrations/OAuthManager.php`): byte-identical `admin_init` callback router (`wp_mcp_ai_oauth` query param), the Gmail/Drive/Yahoo/Calendar start handlers (nonce + capability + credentials gates, `wp_generate_uuid4` state with `wp_mcp_ai_{service}_oauth_state_{md5}` transients, League OAuth2 client with manual-URL fallback), the callback handlers (state consumption, token exchange, profile fetch, settings persistence, redirect envelopes), the disconnect handlers, the `allowed_redirect_hosts` filter, the manual Google authorize-URL builder, and the manual token-exchange helper with its `token_exchange_failed|rejected`/`invalid_token_response` error codes; settings access resolves per install mode (`settings()` seam — base `WP_MCP_AI_Admin_Settings::get_settings()` monolith boot-gated / `wp_mcp_ai_settings` option standalone); the Google Calendar flow delegates to the base `WP_MCP_AI_Google_OAuth_Service`+`WP_MCP_AI_Google_Calendar_Scopes` monolith (boot-gated probes) and degrades standalone with a documented `calendar_error` redirect until the calendar sub-cluster ports. `GithubOAuthHandler` ported (`src/Integrations/GithubOAuthHandler.php`): byte-identical endpoints/scopes, start/callback flows with the `wp_mcp_ai_github_oauth_scope|authorize_endpoint|token_endpoint` filters, the `wp_mcp_ai_github_state_{md5}` transient, `/user` profile fetch, the `wp_mcp_ai_github_oauth_redirect_terminate` test filter, and the settings-error notice envelope; settings/log/sanitizer resolve per install mode (base `WP_MCP_AI_Admin_Settings`/`WP_MCP_AI_Admin_Settings_Base` monolith boot-gated / option read + dormant log + pass-through sanitizer standalone — documented). `MetaOAuthHandler` ported (`src/Integrations/MetaOAuthHandler.php`): byte-identical endpoints/scopes, start/callback/disconnect flows with the Meta scope/endpoint filters, the `wp_mcp_ai_meta_state_{md5}` transient, GET token exchange + `/me` profile fetch, and the `redirect_and_exit()` blocked-redirect contract (WPDieException under PHPUnit); same per-mode seams; monolith quirk preserved: the legacy defaults-driven sanitizer drops `meta_connected_user_name|id` (asserted per mode). `MailjetOAuthHandler` ported (`src/Integrations/MailjetOAuthHandler.php`): byte-identical endpoints, start/callback/disconnect flows, the `wp_mcp_ai_mailjet_oauth_state_{state}` transient, the `is_connected()`/`get_access_token()` contract with the 60-second expiry buffer and refresh path (`wp_mcp_ai_mailjet_no_token|no_refresh_token|no_credentials|refresh_failed|invalid_refresh_response`); no hooks (byte-identical — the base wires only the webhook handler). `QuickbooksOAuthHandler` ported (`src/Integrations/QuickbooksOAuthHandler.php`): byte-identical endpoints/scopes, start/callback (incl. the `realmId` gate + Basic-auth token exchange)/disconnect flows, the `wp_mcp_ai_quickbooks_oauth_state_{state}` transient, the token contract + refresh path (`wp_mcp_ai_quickbooks_no_token|no_refresh_token|no_credentials|refresh_failed|invalid_refresh_response`), and the `admin_notices` transient display; standalone-only wiring via `Plugin::registerIntegrations()` (the base admin-settings component + github/meta/quickbooks init files own the same hooks monolith); 56 characterization tests green in both matrices (5 files; HTTP intercepted via `pre_http_request`); full platform suite 669 tests, 0 failures, 0 risky in both matrices. Sub-cluster 3 (Google Calendar) landed: `GoogleOAuthService` ported (`src/Google/GoogleOAuthService.php`): byte-identical endpoints/constants, the single-use user-bound `wp_mcp_ai_{service}_oauth_state_{md5}` state transient (delete-before-validate replay protection), the byte-stable redirect-URI builders, the offline/consent authorize-URL builder, the `allowed_redirect_hosts` filter, the authorization-code exchange, the refresh-token minting with the `wp_mcp_ai_google_access_token_{md5}` cache + 300 s margin, revocation, and the token-response parser with its `wp_mcp_ai_oauth_*` error codes. `GoogleCalendarScopes` ported (`src/Google/GoogleCalendarScopes.php`): byte-identical profile constants (minimal/standard/full, standard default), the eight scope URLs, the `wp_mcp_ai_google_calendar_scope_profiles` filter, profile normalisation, the %20-healing grant parser, the implied-scope map, the empty-grant-allows `has_scope()`, and the `wp_mcp_ai_calendar_missing_scope` error builder. `GoogleCalendarClient` ported (`src/Google/GoogleCalendarClient.php`): byte-identical constants (API base, result caps, retry budget, backoff ceiling, channel TTL caps, `SYNC_FORBIDDEN_PARAMS`), the CalendarList/Events/FreeBusy/push-channel surface, `build_sync_params()` full-vs-incremental split, `clamp_max_results()`, token-driven `paginate()`, the authenticated retry loop, the three-way 410 discrimination, the 403/429/5xx retry classification, the filtered `wp_mcp_ai_google_calendar_retry_backoff` sleep, and the sync/auth probes. `GoogleCalendarCredentials` ported (`src/Google/GoogleCalendarCredentials.php`): byte-identical source constants, the connection → settings → filter resolution order, the Pro Remote Sites validation/decryption flow, the settings-level surface, the legacy filter surface, the lazy token-provider `make_client()`, the JWT bearer service-account minting, RS256 assertion signing, the IANA timezone fallback, and the scope/calendar-ID helpers; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator): base monolith collaborators monolith / this package's `Google\*` classes standalone, `WP_MCP_AI_Admin_Settings::get_settings()` monolith / `wp_mcp_ai_settings` option standalone, Remote Sites monolith-only (standalone → `wp_mcp_ai_calendar_pro_required`). `GoogleCalendarSync` ported (`src/Google/GoogleCalendarSync.php`): byte-identical constants (state option, sync/renew hooks, 6-hour interval, 365-day lookback, 40-page cap, 5-failure threshold), the per-target state store, the full/incremental `run()` with transparent `410 fullSyncRequired` downgrade and the `wp_mcp_ai_google_calendar_full_sync_required`/`wp_mcp_ai_google_calendar_synced` action envelopes, the paginated `execute_pass()`, the cancelled-exception-aware `classify_events()`, the jittered interval + site offset, the recurring safety-net scheduling, and the settings + Pro Remote Sites target enumeration (both surfaces monolith-only; standalone yields the `wp_mcp_ai_google_calendar_sync_targets` filter alone). `GoogleCalendarPush` ported (`src/Google/GoogleCalendarPush.php`): byte-identical constants (REST namespace/route, channels option, 7-day TTL, 1-day renewal threshold), the `rest_api_init` webhook route, the header-token permission callback, the high-water-mark-deduplicated notification handler, the HTTPS/public-host/private-range eligibility gate, the channel record lifecycle, the write-before-watch flow, the credential-loss-safe teardown, and the replacement-first renewal. `GoogleCalendarBootstrap` ported (`src/Google/GoogleCalendarBootstrap.php`, wraps the base init.php): byte-identical hook surface — the `cron_schedules` filter, the sync/renew cron actions, the push receiver, and the connection-gated initial scheduling; the base's global functions → static methods (`has_connection`/`run_sync`/`schedule_renewal`); standalone-only via `Plugin::registerGoogleCalendar()`. `OAuthManager` calendar seams upgraded to resolve the platform `Google\GoogleOAuthService`+`Google\GoogleCalendarScopes` standalone (monolith unchanged); 126 characterization tests green in both matrices (7 files; HTTP intercepted via `pre_http_request`, retry-backoff zeroed); full platform suite 795 tests, 0 failures, 0 risky in both matrices. Sub-cluster 4 (content-assistant) landed: `ContentAssistantMetabox` ported (`src/ContentAssistant/ContentAssistantMetabox.php`): byte-identical metabox ID, `add_meta_boxes` registration for the settings-driven post-type set, the `wp_mcp_ai_content_assistant_post_types`/`wp_mcp_ai_content_assistant_quick_actions` filters, the post-edit-screen asset enqueue with the `wpMcpAiContentAssistant` localization envelope, the permission/enabled/no-assistants render gates, the quick-action buttons, the chat modal markup, and the `mcp_ai_assistant` WP_Query enumeration; own assets (admin-content-assistant.css/.js) copied byte-identically into the platform assets tree and resolved per mode (`WP_MCP_AI_URL`/`WP_MCP_AI_VERSION` monolith / platform constants standalone); the chat-bundle block is monolith-only via the `defined( 'WP_MCP_AI_PATH' ) && class_exists( 'WP_MCP_AI_Shortcode' )` gate (bare class_exists resolves the classmapped base shortcode standalone, whose asset registration would fatal — documented degradation: standalone renders the selector/quick actions/modal without the embedded chat). `ContentAssistantBootstrap` ported (`src/ContentAssistant/ContentAssistantBootstrap.php`, wraps the base init.php): byte-identical `enable_content_assistant_metabox` gate (default-on) with the `wp_mcp_ai_content_assistant_enabled` filter; global function → static methods (`is_enabled()`/`register()`); standalone-only via `Plugin::registerContentAssistant()`. 22 characterization tests green in both matrices (1 file); full platform suite 817 tests, 0 failures, 0 risky in both matrices. Sub-cluster 5 (site-builder) landed: `SiteNodeInterface` ported (`src/SiteBuilder/SiteNodeInterface.php`): byte-identical node contract (slug/name/description/category, typed input/output port definitions, execute()). `SiteNodeRegistry` ported (`src/SiteBuilder/SiteNodeRegistry.php`): byte-identical singleton lifecycle, idempotent `init()` with the `wp_mcp_ai_register_site_nodes` action, the `wp_mcp_ai_default_site_nodes` filter, last-registered-wins registration, slug lookups, category filtering, the stable category→name front-end palette serialisation, `execute_node()` with the `wp_mcp_ai_site_node_not_found` envelope; the built-in node map resolves per install mode via `default_nodes()` (base `WP_MCP_AI_Site_Node_*` classes monolith / platform `SiteBuilder\Nodes\*` classes standalone) and `register_node()` validates through the per-mode `is_node_instance()` seam (fail-soft — documented hardening over the base's interface type-hint, whose fixed hint cannot accept monolith base nodes). `SitePipelineExecutor` ported (`src/SiteBuilder/SitePipelineExecutor.php`): byte-identical cache constants, Kahn topological sort with the `wp_mcp_ai_site_pipeline_cycle` envelope, incoming-edge resolution, static + edge-overlay input resolution with the multi-source array rule, `md5`-hashed ksort'd cache keys, the two-tier in-memory + transient cache, the empty/missing-slug/missing-dependency envelopes, and the SQL-backed `clear_cache()`/`clear_all_caches()`; `get_registry()` always resolves this package's registry (which handles the per-mode node resolution). `SiteBlueprintCompiler` ported (`src/SiteBuilder/SiteBlueprintCompiler.php`): byte-identical blueprint JSON contract, the `wp_mcp_ai_site_blueprint_directories` filter, sanitised-slug loading, required-key validation, listings/summaries, `{placeholder}` substitution, `{slug}__` ID/edge prefixing, the `wp_mcp_ai_site_blueprint_empty` envelope, in-memory cache; the base's `DEFAULT_DIR` constant becomes the per-mode `default_dir()` method (base `config/site-blueprints/` monolith / the platform's byte-identical blueprint copies standalone — a compile-time constant cannot reference a mode-dependent path). The three built-in nodes ported to `src/SiteBuilder/Nodes/` (`SiteNodeWpQuery` with the 100-post cap + seven-field summary shape, `SiteNodeTextBlock` with the 15-tag whitelist, `SiteNodeFlexContainer` with the three flex-property whitelists; `className` cast documented for strict_types). Blueprint JSONs copied byte-identically into `plugins/nvoos-content-graph-ai-platform/config/site-blueprints/`. No `Plugin.php` registration — byte-identical dormancy (the base ships no bootstrap for this subsystem; consumers call `SiteNodeRegistry::get_instance()->init()` directly). 50 characterization tests green in both matrices (4 files; e2e compile → execute for both shipped blueprints); full platform suite 867 tests, 0 failures, 0 risky in both matrices. Sub-cluster 6 (conversation-import) landed: all 16 base `includes/conversation-import/` classes ported to `src/ConversationImport/` (AdapterInterface, Conversation value object, FormatDetector, five adapters chatgpt/gemini/claude/sharegpt/openai_jsonl, Archive, Media, CctWriter, Manager, Deleter, Privacy, MemoryMiner, QueueBridge): byte-identical session-key/dedupe-hash computation, adapter extraction contracts (ChatGPT current-node branch resolution, Gemini HTML stripping, Claude sender/block mapping, ShareGPT role mapping, OpenAI JSONL prompt/completion fallback), the 128 MB detector cap with JSON/JSONL decoding, the zip-slip archive guard, media sideloading with the dedupe-preserving placeholder rewrite, the CCT record mapping (`import-{platform}` assistant IDs, provenance metadata), the run lifecycle (dry-run, skip/refresh policies, batch/limit caps, per-batch progress callbacks, checkpoint resume, the `wp_mcp_ai_conversation_import_completed` action), the deleter, the GDPR exporter/eraser, the memory-mining bridge, and the async queue bridge; per-mode seams throughout (`defined( 'WP_MCP_AI_PATH' )` discriminator — base JetEngine CCT / settings registry / mining tool / logger / async queue monolith, documented degradation envelopes standalone `wp_mcp_ai_import_jetengine_missing` / `wp_mcp_ai_import_mine_unavailable`; conversation boundaries via `is_conversation_instance()` since monolith flows base-adapter output); standalone-only wiring via `Plugin::registerConversationImport()` (Privacy + MemoryMiner bootstrap only — the base loader requires all 16 unconditionally and owns the same hooks monolith); 46 characterization tests green in both matrices (3 files: core/manager/privacy-deleter-queue); full platform suite 913 tests, 0 failures, 0 risky in both matrices. Remaining: — (E4 wave complete) |
| E5 | A2A REST receive routes | ✅ | `A2aRestController` ported (`src/Rest/A2aRestController.php`): byte-identical `mcp-ai/v1/a2a` route surface (JSON-RPC 2.0 POST, agent-card GET, per-assistant card GET, webhook receiver POST), parameter schemas, the A2A-Version header gate, the `a2a_disabled`/`a2a_version_not_supported` permission envelopes, the full JSON-RPC method router (`message/send|stream`, `tasks/get|list|cancel`, push-config CRUD, `agent/authenticatedExtendedCard`), and the JSON-RPC error map (−32601…−32009, default −32603); the A2A collaborator stack resolves per install mode (base `WP_MCP_AI_A2A_*` monolith — boot-gated probes — platform `A2A\*` classes standalone); auth resolves per mode (base authenticator + security manager monolith; standalone token branches degrade `wp_mcp_ai_auth_unavailable` 503, nonce auth works in both modes); chat pipeline + SSE handler monolith-only seams (`a2a_processing_error` degradation standalone — upgraded when the standalone chat flow lands with E1/D-UI-6); standalone-only route registration via `Plugin::registerA2aRest()` (request-level `a2a_disabled` gate byte-identical; the base's boot-time gate is documented as a deviation); 31 characterization tests green in both matrices (real tasks/cards/configs/webhook roundtrips). Closes the MIGRATION-GAPS deferred gap |
| E6 | Engine pieces (OOS/markup/paper-store/OKF/crawler) per decision D4 | ✅ | Decision D4 resolved (2026-09-06): fold into `nvoos-content-graph-ai` under the `Engine\` namespace — E6 ports land in the AI addon, not the platform (gap-analysis §10 updated). Sub-cluster 1 (OOS shadow) landed: `OosShadowRunner` ported (`src/Engine/OosShadowRunner.php`): byte-identical store constants (`wp_mcp_ai_oos_shadow_runs`, 100 cap, newest first, non-autoloaded), the `wp_mcp_ai_before_chat_request` priority-1 subscriber, the full gate chain (shadow enabled → engine flag off → real REST request → array payloads → sampling), same-request `handleChat` with deadline `CancellationToken`, the `shadow_mode`/no-stream option mutation, the parity record shape (incl. the `shadow: write-class tool suppressed` counter + `has_response`), the contained-failure error record, and the `oos_shadow_run` audit (monolith-only logger). `OosEngineFlags` ported (`src/Engine/OosEngineFlags.php`): byte-identical option keys/filters (`enable_oos_shadow`, `oos_shadow_sample_rate`, `enable_oos_engine`, `wp_mcp_ai_oos_shadow_*` filters, `WP_MCP_AI_OOS_ENGINE`, `X-WP-MCP-AI-Engine` header, `?engine=oos` probes) + the write-class classifier; global functions → static methods with `function_exists()` deferral (monolith byte-identical). `OosShadowSuppression` ported (`src/Engine/OosShadowSuppression.php`): byte-identical `tools/execute` waterfall (priority 20) with the synthetic `(shadow: write-class tool suppressed)` result — safety invariant 1; the base factory owns the same listener monolith; idempotent `register()` (documented hardening). `OosParityCommand` ported (`src/Cli/OosParityCommand.php`): byte-identical aggregate totals/rates + per-run diff table (`last` shorthand); CLI path `wp nvoos-cg-ai oos parity [diff <run-id>]` standalone-only (base owns `wp mcp-ai oos parity`); no `WP_CLI_*` symbols at class-load time per the `src/Cli/README.md` convention. Per-mode seams: orchestrator (`wp_mcp_ai_oos_orchestrator()` monolith / `CoreBridge::instance()->chat` standalone, deliberately untyped — base factory declares no return type), assistant config (base CPT monolith / empty standalone — byte-identical CPT-absent degradation), audit log monolith-only. Standalone-only wiring via `Plugin::registerEngine()` (+ CLI registration in `Cli::registerCommands()`); the runner hook is dormant standalone (no standalone emitter of `wp_mcp_ai_before_chat_request` yet — byte-identical dormancy). 17 characterization tests green in both matrices (1 file); full AI-addon ecosystem suite 628 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 2 (markup) landed: all 15 base markup classes ported to `src/Engine/Markup/` (`MarkupAwareToolInterface`; `MarkupRequest`/`MarkupResult` value objects; `MarkupElicitation` widget + `elicitation/create` envelope builders; `MarkupStore` transient storage with the per-assistant 16-request cap + daily cleanup cron; `MarkupValidator` W3C Web Annotation validation; `MarkupRasterizer` GD mask rasterization with structured-shape degradation; `MarkupLoopInterceptor` `wp_mcp_ai_pre_execute_tool` short-circuit; `MarkupRestController` fetch/submit routes; `MarkupAssets`; `MarkupAdminPage` URL-mode fallback; `MarkupTelemetry` outcome counters; `MarkupStatsSlashCommand` on `wp_mcp_ai_default_slash_commands_loaded` (supersedes the platform's dormant E2 blanket-port copy — documented); `MarkupTelemetryAdminPage` with parent-slug dormancy standalone (documented deviation); `MarkupBootstrap` wrapping the base `markup-init.php` hook surface): byte-identical constants/option keys/transient prefixes/error codes/hook names; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — base `WP_MCP_AI_Tool_Registry` + `WP_MCP_AI_Markup_Aware_Tool_Interface` monolith / `CoreBridge::instance()->tools` + this package's interface standalone; base asset constants monolith / addon constants standalone; audit logging monolith-only); the base's Konva widget assets copied byte-identically into the addon asset tree (local MIT bundle, no CDN); standalone-only wiring via `Plugin::registerEngine()` → `MarkupBootstrap::register()`; 60 characterization tests green in both matrices (3 files: core/loop-rest/telemetry-ui); full AI-addon ecosystem suite 688 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 3 (paper-store) landed: all 8 base `includes/paper-store/` classes ported to `src/Engine/PaperStore/` (`PaperDriverInterface`; `PaperJsonDriver` with the `id`/`type`/`title` required-field enforcement on read AND write, the recursive UTF-8 walk, `sanitize_key()` on `id`/`type`, the `created_at`/`updated_at` ISO-8601 timestamp contract, and the atomic-write delegation; `PaperIndex` with the `_indexes/{collection}.idx.json` structure, lazy `wp_parse_args`-healed load, the `wp_mcp_ai_paper_index_max_tags` filter applied on the rebuild path only (preserved base quirk), the flock non-blocking 3-second save, and the `wp_mcp_ai_paper_index_rebuilt` stats payload; `PaperRepository` with the `wp_mcp_ai_paper_record_before_save` filter, save-time defaults (type → collection, author_id → current user, status → `published`), immutable-field update protection, and the `wp_mcp_ai_paper_record_saved|deleted` actions; `PaperQuery` with the index-resolved/post-filtered clause split, the tags `IN` `reset( $value )` quirk, status `IN` merge, date-bucket resolution, loose comparisons, and immutable builder semantics; `PaperStoreManager` singleton with the `wp_mcp_ai_paper_store_root` filter (default `uploads/mcp-ai-wpoos/paper-store` — data survives mode transitions), security-file placement, the `wp_mcp_ai_paper_driver` filter, repository caching, the realpath-prefix `validate_path()` traversal guard, and `list_collections()`; `PaperStoreRemoteTrait` with the `pro_required`/`invalid_connection`/`disabled_connection` envelopes + `connection_id` schema fragment; `PaperStoreBootstrap` wrapping the base `paper-store-init.php` `wp_mcp_ai_bootstrapped` priority-30 tool-registration surface): byte-identical error codes/option-adjacent paths/hook names; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — base `WP_MCP_AI\Filesystem\WP_MCP_AI_Filesystem_Service` atomic I/O monolith / native `file_put_contents( LOCK_EX )` fallback standalone; base `WP_MCP_AI_Pro_Remote_Site_Manager` monolith / `pro_required` envelope standalone); the six base `paper_store_*` MCP tools + the paper-store REST controller remain base-owned until the Paper Store tool wave (documented deferral — the `register_tools()` seam resolves per mode as a documented no-op in both); standalone-only wiring via `Plugin::registerEngine()` → `PaperStoreBootstrap::register()`; 68 characterization tests green in both matrices (2 files: core/repository-query-bootstrap); full AI-addon ecosystem suite 756 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 4 (OKF) landed: all 5 base `includes/okf/` classes ported to `src/Engine/Okf/` (`OkfParser` with the byte-identical indentation-aware recursive-descent YAML-subset parsing + serialization; `OkfReader` with lazy concept caching, the lexical + symlink-aware realpath containment, browse index-or-scan, depth-clamped traversal, the v0.2 trust tiers + staleness helpers, multi-criteria search, and the advisory broken-link report; `OkfWriter` with the `okf_version: "0.2"` root-index stamping (v0.2 §12), the `.deleted.<timestamp>` soft-delete, index regeneration, the full v0.2 `validate_bundle()` report shape, log.md maintenance, and `ensure_bundle_root()`; `OkfBundleManager` as the single audited bundle resolver with the `wp_mcp_ai_okf_knowledge_root` filter (default `mcp-ai-wpoos/knowledge` — data survives mode transitions), realpath containment, the protected/standard bundle sets (`skill-knowledge` protected), listing/descriptors, create/rename/archive/delete, ZIP export/import with the ZipSlip + symlink + entry-count (5000) + size (25 MB) defenses, raw-concept saves with reserved-filename protection, and security-guard files; `OkfSkillKnowledgeGenerator` with the byte-identical `wp_mcp_ai_okf_skill_knowledge_generated` fingerprint option and the `wp_mcp_ai_bootstrapped` priority-32 `maybe_generate` hook, plus per-mode fingerprint + skills-source roots (base+Pro monolith byte-identical; addon root standalone — none shipped, so generation degrades with the byte-identical 'No bundled skills found' error without persisting the option); `OkfBootstrap` wrapping the base `okf-init.php` hook surface): byte-identical error codes/hook names/default paths; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — base `WP_MCP_AI\Filesystem\WP_MCP_AI_Filesystem_Service` atomic I/O monolith / native `file_put_contents( LOCK_EX )` fallback standalone); the ten base `okf_*` MCP tools remain base-owned until the OKF tool wave (documented deferral — `register_tools()` resolves per mode as a documented no-op in both); the base writer's bare-prefix containment quirk (a `../` concept ID writes into the parent directory; the reader refuses to resolve it back) is preserved byte-identically and characterized; standalone-only wiring via `Plugin::registerEngine()` → `OkfBootstrap::register()`; 61 characterization tests green in both matrices (2 files: core/bundle-manager incl. real ZipArchive round-trips and the monolith 74-skill generation); full AI-addon ecosystem suite 817 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 5 (crawler) landed: the base `includes/crawler/` coordinator ported to `src/Engine/Crawler/` (`Crawler` with the byte-identical `wp_mcp_ai_crawl4ai_job_` transient store + md5-hashed storage keys, the `wp_mcp_ai_crawl4ai_poll_task` cron hook with the `wp_mcp_ai_crawl4ai_auto_spawn_cron` filter, the `FILTER_VALIDATE_URL`-gated base-URL validation, the 30 s/600 s defaults with 5 s/60 s clamps, the `wp_mcp_ai_crawl4ai_poll_lock_` cooperative tick lock (30 s TTL), the inline-async-tick shutdown kick, interim progress persistence, the exponential-backoff retry path (30/60/120 s, 300 s cap, 3 retries, timeout-final), dead-letter forwarding, and the `wp_mcp_ai_crawl4ai_job_registered|completed|failed` + `wp_mcp_ai_crawl4ai_response` surfaces; `InlineAsyncTickTrait` ported alongside as the crawler's required collaborator — byte-identical two-layer tick lock, `DISABLE_WP_CRON`-aware loop decision, escape hatch, and observability action): byte-identical constants/transient prefixes/hook names/error codes; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — base `WP_MCP_AI_Admin_Settings` / `WP_MCP_AI_Tool_Run_Crawl4AI_Job::check_remote_task()` / `WP_MCP_AI_Crawl4AI_Local_API::cache_task_result()` / `WP_MCP_AI_Logger` / `WP_MCP_AI_Dead_Letter_Queue` / `WP_MCP_AI_Cron_Manager` monolith — settings option read + documented `wp_mcp_ai_crawl4ai_check_unavailable` error + dormant result-cache/log/DLQ/cron-manager standalone); the base `run_crawl4ai_job` validated tool + Crawl4AI tool family remain base-owned until the Crawl4AI tool wave (documented deferral); standalone-only wiring via `Plugin::registerEngine()` → `Crawler::init()`; 25 characterization tests green in both matrices (1 file; real transient/cron round-trips, HTTP intercepted); full AI-addon ecosystem suite 842 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 6 (oos-bridge wave-1 helpers) landed — E6 complete: the remaining oos-bridge flag helpers added to `OosEngineFlags` (`session_log_enabled()` / `session_telemetry_enabled()` with the byte-identical `enable_oos_session_log` / `enable_oos_session_telemetry` settings + filters; `canary_enabled()` with the `wp_mcp_ai_oos_canary` gate + `_wp_mcp_ai_engine` post-meta probe) and the five wave-1 service-bridge factories ported to `OosServiceBridges` (`src/Engine/OosServiceBridges.php` — `semantic_compressor()`, `data_budget_tracker()`, `erlang_c()`, `error_tracking()`, `cost_tracking()`): each defers to the base global function monolith (byte-identical — including the data-budget factory's discarded engine-enabled branch quirk, characterized in tests, and the per-factory `static $instance` caches) and resolves standalone bodies (the real `nvoos/core` + `nvoos/wordpress-adapter` implementations — the addon has no legacy engine path, documented; the error-tracking resolver is the exception: its framework adapter delegates to the base legacy singleton whose globals are absent standalone, so standalone resolves a native fallback carrying the adapter's own legacy-absent degradation semantics — `err_fallback_` ids, empty recent, 0.0 rate, `isEnabled` false); 12 characterization tests green in both matrices (1 file; contract + math + per-mode resolution incl. the data-budget quirk); full AI-addon ecosystem suite 854 tests, 0 failures in both matrices, PHPCS-clean. Remaining: the six oos-bridge wave-2 factory bridges (`wp_mcp_ai_oos_model_catalog` / `token_budget` / `file_validation` / `file_upload` / `file_orchestration` / `rate_limiter` from `includes/bootstrap/oos-bridge-wave2.php`) stay base-owned pending their consumer ports (not in the E6 tracker scope) |

## Wave E-UI — Operator admin UI → platform

| # | Subsystem | Status | Notes |
|---|---|---|---|
| E-UI-1 | Dashboards (multi-agent, orchestration, slash-commands, run timeline) | ✅ | Sub-cluster 1 (multi-agent) landed: `MultiAgentDashboard` ported (`src/Admin/Dashboards/MultiAgentDashboard.php`): byte-identical `PAGE_SLUG` (`mcp-ai-multi-agent`), `NONCE_ACTION` (`wp_mcp_ai_multi_agent`), AJAX action names (`wp_ajax_wp_mcp_ai_get_multi_agent_stats` / `wp_ajax_wp_mcp_ai_reinstall_agents`), menu/enqueue/render registration, the admin-gate render (`wp_die` non-managers), the nonce+capability AJAX gates, the statistics shape, the workflow-pattern classification, and the reinstall flow; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — base `WP_MCP_AI_Default_Assistants` monolith / `false`+`WP_Error('wp_mcp_ai_default_assistants_unavailable')` standalone; meta-key map base `WP_MCP_AI_Assistant_CPT::META_*` monolith / AI addon `AssistantPostType::META_*` standalone (byte-identical values); JetEngine via `function_exists('wp_mcp_ai_is_jetengine_available')` deferral; the chat-bundle + test-chat modal are monolith-only — standalone renders agent cards without the Test button); the four operational dashboards mount as submenu pages under the platform's NV Platform menu (`PlatformDashboard::PAGE_SLUG`) — routing-review note: the base's tab/sub-tab/view routing lives in the settings dashboard and Pro orchestration settings, so these single-page section-composed surfaces do NOT replicate settings-level routing; the orchestration dashboard's own section view-routing lands with its sub-cluster; own assets (admin-multi-agent-dashboard.css/js + shared admin-monitor/admin-test-assistant) copied byte-identically into the platform assets tree; standalone-only wiring via `Plugin::registerDashboards()` (the base admin owns the same page monolith); 16 characterization tests green in both matrices (exposer fixture + AJAX capture harness); full platform suite 929 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 2 (orchestration) landed: `OrchestrationDashboard` ported (`src/Admin/Dashboards/OrchestrationDashboard.php`): byte-identical `PAGE_SLUG` (`mcp-ai-orchestration`), `NONCE_ACTION` (`wp_mcp_ai_orchestration`), the six AJAX actions (`wp_ajax_wp_mcp_ai_run_orchestration_seeder|get_orchestration_stats|get_recent_workflows|execute_workflow|restart_workflow|refresh_memory_stats`), the `wpMcpAiOrchestration` localize envelope, the orchestration statistics shape (total/seeded/roles/with_task_patterns/seeder_version), the status banner thresholds (<50 warning, <90 info), the statistics cards, the system-status monitor (cron/async/health/SSE), the role distribution chart, the quick actions, the documentation links, the recent-workflows transient list, the agent memory stats widget (Phase 4a/4b/5: type/wing/importance breakdowns, wings/rooms, mined count, persistent CCT vs cache split, retrieval-path chart, RAG features, context health metrics) and the workflow execute/restart AJAX flows; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — profession meta keys base `WP_MCP_AI_Profession_CPT` monolith / platform `Professions\ProfessionCpt` standalone (byte-identical values); seeder base `WP_MCP_AI_Profession_Orchestration_Seeder` monolith / platform `Professions\ProfessionOrchestrationSeeder` standalone (same `seed_all()` contract); tool inventory base `WP_MCP_AI_Tool_Registry` monolith / AI addon `CoreBridge::instance()->tools` standalone normalized to slug/description entries; SSE probe base `WP_MCP_AI_SSE_Stream` monolith / platform `Rest\SseStream` standalone; context-health manager base `WP_MCP_AI_Agent_Context_Manager` monolith / null standalone (health block degrades away — documented); workflow coordinator base-owned and not yet ported — standalone `ajax_execute_workflow` degrades with the byte-identical 'Workflow coordinator not available.' envelope (documented); all `WP_MCP_AI_Logger` calls route through a dormant seam); the two base-settings cross-links resolve per mode via `settings_page_url()` (monolith `wp-mcp-ai-dashboard&tab=…` / standalone NV Platform dashboard page — documented); own assets (admin-orchestration-dashboard.css/.js) copied byte-identically; standalone-only wiring via `Plugin::registerDashboards()` (the base admin owns the same page monolith); 29 characterization tests green in both matrices (exposer fixture + AJAX capture harness + ctx/workflow transient namespace isolation); full platform suite 958 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 3 (slash-commands) landed: `SlashCommandsDashboard` ported (`src/Admin/Dashboards/SlashCommandsDashboard.php`): byte-identical `MAX_HISTORY_ENTRIES`/`HISTORY_OUTPUT_PREVIEW_LENGTH` constants, `PAGE_SLUG` (`mcp-ai-slash-commands`), `NONCE_ACTION` (`wp_mcp_ai_slash_commands`), the five AJAX actions (`wp_ajax_wp_mcp_ai_execute_command|get_command_history|get_history_entry|clear_command_history|execute_slash_workflow`), the `wpMcpAiSlashCommands` localize envelope, the `edit_posts` (Contributor+) menu capability, the four in-page tabs (commands/workflows/history/test with the `$_GET['tab']` whitelist), the command statistics + global/toolkit grouping + help display, the workflows table + details + execution output boxes, the history table + details box, the command tester, the handler-driven command list (with the toolkit re-registration call), the orchestrator-driven workflow list (built-in + uploads-directory YAML), the `wp_mcp_ai_slash_command_history` option flow (100-entry cap, 500-char preview truncation, `uniqid('exec_')` IDs), and the execute-command / execute-workflow / get-history / get-entry / clear-history AJAX flows; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — the handler + orchestrator resolve through the `wp_mcp_ai_get_slash_command_handler()` / `wp_mcp_ai_get_workflow_orchestrator()` globals, base monolith / platform shim standalone, so those call sites stay byte-identical; toolkit grouping via `toolkit_manager()`/`toolkit_name()` (base `WP_MCP_AI_Slash_Command_Toolkit_Manager` + `WP_MCP_AI_Toolkit_Registry` monolith / platform `SlashCommandToolkitManager` + slug-derived fallback name standalone — the base's dead `$toolkits` assignment dropped, documented); the base's `private` helpers become `protected` (documented, test exposure); the `class_exists('WP_MCP_AI_Logger')` guards stay byte-identical (dormant standalone)); own assets (admin-slash-commands-dashboard.css/.js) copied byte-identically; standalone-only wiring via `Plugin::registerDashboards()` (the base admin owns the same page monolith); 20 characterization tests green in both matrices (exposer fixture + AJAX capture harness + standalone shim preload in setUp); full platform suite 978 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 4 (run-timeline) landed — E-UI-1 complete: `RunTimelineDashboard` ported (`src/Admin/Dashboards/RunTimelineDashboard.php`): byte-identical `RUNS_PER_PAGE`/`CACHE_PREFIX` constants, `PAGE_SLUG` (`mcp-ai-run-timeline`), `NONCE_ACTION` (`wp_mcp_ai_run_timeline`), the two AJAX actions (`wp_ajax_wp_mcp_ai_run_timeline_get_run|list_runs`), the `wpMcpAiRunTimeline` localize envelope (incl. the six-string i18n block), the sidebar run-list + detail render surface, the OTel configuration notice, the assistant filter options, the metric-store-driven run summaries (grouped by `context.run_id`, started-at ordering, token/cost/latency aggregation, pagination math) with the reasoning-trace post-meta fallback scan, the full run detail loader (per-step metric rows, meta-trace hydration, the `wp_mcp_ai_run_timeline_detail` filter), and the try/catch AJAX handlers with their 403/400/404/500 envelopes; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — metric store base `WP_MCP_AI_Metric_Event_Store` monolith / platform `Measurement\MetricEventStore` standalone (same contract); reasoning trace base `WP_MCP_AI_Reasoning_Trace` monolith / platform `Harness\ReasoningTrace` standalone (byte-identical `META_KEY` + `sanitize()`); OTel exporter probe monolith-only (standalone always shows the configure notice — documented); observability settings link via `observability_settings_url()` (monolith byte-identical / standalone NV Platform dashboard page — documented); the base's `private` helpers become `protected` (documented, test exposure); the safe-log `class_exists('WP_MCP_AI_Logger')` guards stay byte-identical (dormant standalone)); own assets (admin-run-timeline.css/.js) copied byte-identically; standalone-only wiring via `Plugin::registerDashboards()` (the base admin owns the same page monolith); 15 characterization tests green in both matrices (exposer fixture + AJAX capture harness; nonce gate asserted through the ported Throwable-guard 500 envelope per the suite's `wp_doing_ajax` filter contract — success paths asserted via string matching due to the guard re-catching the harness's WPAjaxDieContinueException, a byte-identical harness artifact); full platform suite 993 tests, 0 failures in both matrices, PHPCS-clean. Remaining: — (E-UI-1 wave complete) |
| E-UI-2 | Managers (tool/token/cron/DAG/DLQ/approvals) | 🟡 | Sub-cluster 1 (approvals UI) landed: `ApprovalsManager` ported (`src/Admin/Managers/ApprovalsManager.php`): byte-identical `PAGE_SLUG` (`mcp-ai-approvals`) with the pending-count awaiting-mod badge menu title, `NONCE_ACTION` (`wp_mcp_ai_approvals`), the two AJAX actions (`wp_ajax_wp_mcp_ai_list_approvals|resolve_approval`), the `wpMcpAiApprovals` localize envelope (nine-string i18n block), the toolbar + assistant filter + seven-column table render surface, the pending-count probe, and the list/resolve AJAX flows (requester display-name + formatted-date enrichment, approve/deny transitions with notes, 403/400 envelopes); per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — approval queue base `WP_MCP_AI_Approval_Queue` monolith / platform `Approvals\ApprovalQueue` standalone (same contract — the E3 port)); the base's `private` helpers become `protected` (documented, test exposure); own asset (admin-approvals.js) copied byte-identically; standalone-only wiring via `Plugin::registerManagers()` (the base admin owns the same page monolith); 13 characterization tests green in both matrices (exposer fixture + AJAX capture harness, real enqueued approvals); full platform suite 1006 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 2 (token manager) landed: `TokenManager` ported (`src/Admin/Managers/TokenManager.php`): byte-identical `PAGE_SLUG` (`wp-mcp-ai-token-manager`), the two `admin_post_wp_mcp_ai_token_manager_revoke|delete` actions with per-token nonces, the inline-stylesheet enqueue + `restrictions-admin.js` asset with the `wpMcpAiRestrictionsAdmin` localize envelope, the intro / restricted-users panel / action notices / statistics cards / credentials table (status pill, revocation audit line, per-row revoke + delete forms) / empty state / security-note render surface, the credentials listing (all assistants, newest-first via `_sort_timestamp`), the statistics shape (total/active/revoked/assistants), the user display-name helper, and the revoke/delete redirect flows; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — credentials store base `WP_MCP_AI_Credentials` monolith / null standalone (no ported credentials store yet — the listing degrades to the byte-identical 'No Tokens Issued' empty state and the handlers redirect with `action=error`, documented); the restrictions panel probe boot-gated (standalone hidden — documented)); the base's `private` helpers become `protected` (documented, test exposure); own asset (restrictions-admin.js) copied byte-identically; standalone-only wiring via `Plugin::registerManagers()` (the base admin owns the same page monolith); 16 characterization tests green in both matrices (exposer fixture + redirect-intercept harness + real base-store credentials monolith); full platform suite 1022 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 3 (cron manager) landed: `CronManagerPage` ported (`src/Admin/Managers/CronManagerPage.php`): byte-identical `PAGE_SLUG` (`wp-mcp-ai-cron-manager`), `NONCE_ACTION` (`wp_mcp_ai_cron_manager`), the `admin_post_wp_mcp_ai_delete_cron` delete handler with per-job nonces, the `wp_ajax_wp_mcp_ai_get_cron_manager_stats` stats handler, the inline-stylesheet + shared monitor stylesheet + `admin-cron-manager.js` asset enqueues with the `wpMcpAiCronManager` localize envelope, the auto-refresh controls, the retention-period intro, the updated/error notices, the statistics cards, the eight-column jobs table (status pill, next-run human time, schedule-type pill, pretty-printed args, creator, created-at, per-row delete form), the empty state, the DLQ/SLA job-queue-health section (tier table + tuning recommendations), the job-store section, and the delete/redirect flow; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — runtime cron manager base `WP_MCP_AI_Cron_Manager` monolith / platform `Queues\CronManager` standalone, DLQ stats base `WP_MCP_AI_Dead_Letter_Queue` / platform `Queues\DeadLetterQueue`, SLA stats base `WP_MCP_AI_SLA_Manager` / platform `Queues\SlaManager` (all byte-compatible static contracts — the E2 ports), job store base `WP_MCP_AI_Job_Store` monolith / null standalone (section hidden, documented), retention period base `WP_MCP_AI_Settings_Registry` monolith / `wp_mcp_ai_settings` option standalone, DLQ cross-link byte-identical in both modes (standalone resolves once the DLQ manager sub-cluster ports — documented forward-reference)); the base's `private` helpers become `protected` (documented, test exposure); own asset (admin-cron-manager.js) copied byte-identically; standalone-only wiring via `Plugin::registerManagers()` (the base admin owns the same page monolith); 23 characterization tests green in both matrices (exposer fixture + redirect-intercept harness + AJAX capture helpers); full platform suite 1045 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 4 (DAG builder) landed: `DagBuilder` ported (`src/Admin/Managers/DagBuilder.php`): byte-identical `PAGE_SLUG` (`wp-mcp-ai-dag-builder`), the `mcpAiDagBuilder` localize envelope (ajaxUrl/restUrl/wp_rest nonce/workflowId/version), the workflow list sidebar (New Workflow button, version badges, Edit/Run actions, empty state), the canvas root with the resolved workflow id, and the query-string workflow-id resolution with CPT ownership check (deduplicated into the `resolve_workflow_id()` helper — additive); per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — workflow CPT base `WP_MCP_AI_Workflow_CPT` monolith / platform `Workflows\WorkflowCpt` standalone (byte-identical `CPT`/`META_VERSION` constants — the E1 port; null → `workflow_id` degrades to 0, documented guard)); the base's `private` helpers become `protected` (documented, test exposure); own assets (admin-dag-builder.css/js) copied byte-identically; standalone-only wiring via `Plugin::registerManagers()` (the base admin owns the same page monolith); 14 characterization tests green in both matrices (exposer fixture + real workflow posts); full platform suite 1059 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 5 (DLQ manager) landed: `DlqManager` ported (`src/Admin/Managers/DlqManager.php`): byte-identical `PAGE_SLUG` (`wp-mcp-ai-dlq-manager`), the `admin_post_wp_mcp_ai_dlq_bulk_action|single_action` handlers with their nonce actions, the inline-stylesheet enqueue (`wp-mcp-ai-dlq-inline`), the intro / notices / statistics cards / filter form / seven-column items table (type badge, identifier, failure reason, retry count, human-time added, per-row retry/dismiss/delete links) / empty state render surface, the bulk-action processed/errors redirect envelope, the single-action success/error redirect envelopes, the type-badge and item-action helpers, and the page-URL builder; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — dead-letter queue base `WP_MCP_AI_Dead_Letter_Queue` monolith / platform `Queues\DeadLetterQueue` standalone (byte-compatible static `get_all()`/`get_stats()`/`retry()`/`dismiss()`/`remove()` contract — the E2 port; null → empty listing + zeroed stats, documented guard)); the base's `private` helpers become `protected` (documented, test exposure); standalone-only wiring via `Plugin::registerManagers()` (the base admin owns the same page monolith); 27 characterization tests green in both matrices (exposer fixture + real DLQ table via the E2 DDL-suspension pattern); full platform suite 1086 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 6 (media-library columns) landed: `MediaLibraryColumns` ported (`src/Admin/Managers/MediaLibraryColumns.php`): byte-identical singleton contract (`get_instance()`/`init()`), `_wp_mcp_ai_usage` meta key, the `wp_mcp_ai_usage` AI Usage column, the no-usage dash + tokens/cost/operations badge render surface, the token-count and cost formatters, the attachment-usage reader, the per-attachment usage tracker (`wp_mcp_ai_after_tool_execution`, image-tool allowlist, argument/result attachment-id extraction, accumulation with per-tool counts, provider/model stamps), and the media-page-only inline stylesheet; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — cost calculator base-owned (monolith-only; standalone cost stays 0.0, documented); constructor wiring extracted into a protected `wire_hooks()` (additive — wp-phpunit hook snapshot restore, documented)); the base's `private` constructor/helpers become `protected` (documented, test exposure); singleton invoked standalone-only via `Plugin::registerManagers()` (the base loader owns the same integration monolith); 20 characterization tests green in both matrices (exposer fixture with re-armed wiring); full platform suite 1106 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 7 (asset inventory, final) landed: `AssetInventoryPage` ported (`src/Admin/Managers/AssetInventoryPage.php`): byte-identical `PAGE_SLUG` (`nvoos-asset-inventory`), the `wpMcpAiAssetInventory` localize envelope (wp_rest nonce, `mcp-ai/v1/assets` REST apiUrl, four-string i18n block), the Discover Assets action, the stats grid (total + per-classification cards, last-updated), the classification/type filter selects, the six-column assets table (classification badges, per-row data-* attributes), the empty state, and the ISO 27001 A.5.9 about section; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — inventory engine base `WP_MCP_AI_Asset_Inventory` monolith / null standalone (engine not yet ported — empty-state degradation, documented forward-reference); fleet capability via the base helper monolith / inline replication of the default + `wp_mcp_ai_fleet_capability` filter standalone); the enqueue gate keeps the base's three hook suffixes + adds the platform's title-derived `nv-platform_page_nvoos-asset-inventory` (additive); the base's `private` helpers become `protected` (documented, test exposure); own assets (asset-inventory.css/js) copied byte-identically; standalone-only wiring via `Plugin::registerManagers()` (the base admin owns the same page monolith); 11 characterization tests green in both matrices (exposer fixture + seeded base-owned inventory monolith); full platform suite 1117 tests, 0 failures in both matrices, PHPCS-clean. Remaining: — (E-UI-2 wave complete) |
| E-UI-3 | Integrations screens | 🟡 | Sub-cluster 1 (profession pages) landed: `ProfessionResearchPage` + `ProfessionSettings` ported (`src/Admin/Integrations/` — the E-UI-3 wave folder): byte-identical page slugs (`research-profession`, `wp-mcp-ai-profession-settings`) under the profession CPT menu, the three-workflow research surface (chat embed + no-assistant notice, import form, review quality dashboard) with the `wpMcpAiResearchPage` envelope, the three registered profession default settings, the tabbed settings render (overview/configuration/tools/help), and the nonce-gated save flow (temperature clamp + settings-updated redirect); per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — profession post type base `WP_MCP_AI_Profession_CPT` monolith / platform `Professions\ProfessionCpt` standalone (byte-identical `POST_TYPE`); chat shortcode base `WP_MCP_AI_Shortcode` monolith / null standalone (assets skipped, documented); provider list base `WP_MCP_AI_Admin_Settings::get_available_providers()` monolith / empty standalone); own assets (enhanced-research-page.css/js) copied byte-identically; standalone-only wiring via `Plugin::registerIntegrationsScreens()` (the base loader owns the same pages monolith); 24 characterization tests green in both matrices (exposer fixtures + seeded professions/assistants); full platform suite 1141 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 2 (team pages) landed: `TeamResearchPage` + `TeamSettings` ported (`src/Admin/Integrations/`): byte-identical page slugs (`research-team`, `wp-mcp-ai-team-settings`) under the team CPT menu, the three-workflow research surface (chat embed + no-assistant notice, import form, review quality dashboard with members/orchestration/driver metrics, View Professions cross-link) with the `wpMcpAiResearchPage` envelope (entityType=team), the four registered team default settings (incl. driver_assistant), the tabbed settings render (overview/configuration/tools/help with the team orchestration card), and the nonce-gated save flow (temperature clamp + settings-updated redirect); per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — team post type base `WP_MCP_AI_Team_CPT` monolith / platform `Teams\TeamCpt` standalone (byte-identical `POST_TYPE`); chat shortcode base monolith / null standalone; provider list base monolith / empty standalone); the enhanced-research-page assets shared byte-identically with sub-cluster 1; standalone-only wiring via `Plugin::registerIntegrationsScreens()`; 24 characterization tests green in both matrices; full platform suite 1165 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 3 (Elementor + WooCommerce screens) landed: `ElementorIntegration` + `WooCommerceIntegration` ported (`src/Admin/Integrations/`): byte-identical page slugs (`wp-mcp-ai-elementor`, `wp-mcp-ai-woocommerce`), the `admin_post_wp_mcp_ai_save_elementor_settings|_woocommerce_settings` handlers with their nonce actions, the active/inactive status banners (third-party probes — byte-identical), the checkbox settings forms, the widget showcase grid, the five-tool status table, the Full Version note, and the save redirects; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — settings store base `WP_MCP_AI_Admin_Settings::OPTION_NAME` monolith / the byte-identical `wp_mcp_ai_settings` option key standalone); standalone-only wiring via `Plugin::registerIntegrationsScreens()`; 15 characterization tests green in both matrices; full platform suite 1180 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 4 (JetEngine + Plugins screens, final) landed: `JetEngineIntegration` + `PluginsIntegration` ported (`src/Admin/Integrations/`): byte-identical page slugs (`wp-mcp-ai-jetengine`, `wp-mcp-ai-plugins`), the `admin_post_wp_mcp_ai_save_jetengine_settings|_plugins_settings` handlers with their nonce actions (incl. the byte-identical `wp_mcp_ai_plugins_integration_redirect_terminate` filter), the active/inactive banners, the CCT/tools + MCP sections (availability banner, endpoint, integration/context/cache-TTL fields, MCP tool table), the four-section plugins form (JetEngine/WooCommerce/Elementor/Newsletter probes + warning notices), and the save redirects; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — settings store base `WP_MCP_AI_Admin_Settings::OPTION_NAME` monolith / byte-identical `wp_mcp_ai_settings` option standalone; MCP-server probe base `WP_MCP_AI_JetEngine_Compat` monolith / false standalone, documented); standalone-only wiring via `Plugin::registerIntegrationsScreens()`; 15 characterization tests green in both matrices (probe-conditional assertions — the CI environment does not load the third-party plugins); full platform suite 1195 tests, 0 failures in both matrices, PHPCS-clean. Remaining: — (E-UI-3 wave complete) |

## Wave F — Pro runtime → pro addon(s) (decision D5)

| # | Subsystem | Status | Notes |
|---|---|---|---|
| F1 | pro-core (module registry, vault, vector-storage, skills-manager, toolkit data-store factory, privacy) | ✅ | Sub-cluster 1 (addon skeleton + module registry) landed: new `plugins/nvoos-content-graph-pro` addon (entry `nvoos-content-graph-pro.php` with `Requires Plugins: nvoos-content-graph, nvoos-content-graph-ai, nvoos-content-graph-ai-platform`, boot at `plugins_loaded` 15, monolith-mode guard on `defined( 'WP_MCP_AI_PRO_PATH' )` — the base Pro addon keeps owning the subsystems monolith; Composer classmap + spl fallback autoloader (`WP_MCP_AI_Foo_Bar` → `src/class-wp-mcp-ai-foo-bar.php`, refused in monolith mode); `NvoosContentGraphPro\Plugin` composition root that only boots the registry); `WP_MCP_AI_Pro_Module_Registry` ported (`src/class-wp-mcp-ai-pro-module-registry.php`) with byte-identical mechanics (idempotent `boot()`, admin `check_context()`, `requires`/`requires_fn`/`files` gates, Kahn topological `resolve_order()` with circular fallback, `debug_log()`, `get_loaded_modules()`/`is_loaded()`); documented deviations (strict-types coercion guard for a non-array `wp_mcp_ai_settings` option; `final` dropped and `private` members/seams → `protected` for test exposure; `define_modules()` defines the F1-only subset — `privacy`, `toolkit_data_store`, `pro_skills_manager`, `toolkit_vault`, `vector_storage` — each with a `files` guard so `boot()` degrades gracefully while module files land in F1-2..F1-5; `NVOOS_CONTENT_GRAPH_PRO_PATH . 'src/'` path root; two standalone-only modules with no monolith registry counterpart — `toolkit_data_store` (monolith loads the factory on demand) and `vector_storage` (monolith registers the prepare-file tool via `wp_mcp_ai_pro_register_tools()`); additive `get_modules()`/`is_bootstrapped()` accessors); new dual-matrix CI (`phpunit-pro.yml`, `WP_MCP_AI_PRO_STANDALONE=1` standalone); `bin/port-cluster.sh` gained `PORT_STANDALONE_VAR`; 12 characterization tests green in both matrices (monolith: base registry public surface + idempotent boot; standalone: full seams via the helper subclass), PHPCS-clean. Sub-cluster 2 (privacy) landed: `WP_MCP_AI_Pro_Privacy` ported (`src/class-wp-mcp-ai-pro-privacy.php`): byte-identical `PAGE_SIZE` (10), `init()` filter wiring (`wp_privacy_personal_data_exporters|erasers` at priority 15), the exporter/eraser registration shapes (`wp-mcp-ai-pro-health-wellness` always; `wp-mcp-ai-pro-imaging-studies` gated on `class_exists( 'WP_MCP_AI_Imaging_Study_CPT' )` — byte-identical gate), the health-wellness export (author-matched, all-status, paginated, six-CPT label map, internal `_edit_`/`_wp_` meta skipped, per-record group/item envelopes) and erase (hard-delete bypassing trash, removed/retained/messages/done envelope), the imaging export (seven-field de-identified metadata shape, no pixel data) and erase (on-disk DICOM dir deletion gated to inside the uploads basedir — path-traversal guard with the byte-identical skip message — then hard-delete); documented deviations (strict types; text domain `mcp-ai-wpoos-pro` → `nvoos-content-graph-pro`; `get_health_cpt_map()`/`delete_directory_recursively()` → `protected` for test exposure; the F4 health/imaging CPTs degrade to empty results standalone until ported — documented forward-reference); the `privacy` registry module now boots standalone (factory `require_once` + `WP_MCP_AI_Pro_Privacy::init()`, byte-identical with the monolith); 13 characterization tests green in both matrices (11 running against the base class monolith — byte-identical surface proven; 13 standalone incl. the two seam tests; in-process CPT registration fixtures), PHPCS-clean. Sub-cluster 3a (toolkit data-store factory) landed: `WP_MCP_AI_Toolkit_Data_Store_Factory` + `WP_MCP_AI_Toolkit_CPT_Store` (490 lines) + `WP_MCP_AI_Toolkit_CCT_Store` (657 lines) + the `WP_MCP_AI_Toolkit_Data_Store` interface ported (`src/class-wp-mcp-ai-toolkit-data-store-factory.php`, `src/data-stores/`, `src/interfaces/`): byte-identical backend selection (JetEngine CCT preferred when `jet_engine()` exists + `enable_jetengine_cct` + the CCT module resolves, else CPT fallback), `get_tenant_store()` with the byte-identical `class_exists('WP_MCP_AI_Tenant_Context')` bypass guard, the CPT store's canonical CRM slug map (`crm/leads` → `mcp_ai_lead`, `contacts` → `mcp_crm_contacts`, …), the 20-char slug truncation, the `per_page` → `posts_per_page` alias, tenant-isolated CRUD (hard-delete, `_edit_`/`_wp_` meta filtering, `suppress_filters`), and the CCT store's `cct_not_available` degradation; documented deviations (strict types; `src/` path swaps; text domain; the monolith-gated tenant-repository require + new standalone bridge `src/class-wp-mcp-ai-tenant-repository.php` — a global-name alias of the Platform addon's `Tenant\TenantRepository` (Wave E4 port, byte-compatible surface incl. the protected `$tenant_type`/`$tenant_id`/`$strict` state) — documented new class with no monolith counterpart); the registry's `toolkit_data_store` module files guard now covers the interface + bridge and the factory loads them eagerly (standalone-only module — documented); 11 characterization tests green in both matrices (probe-conditional JetEngine assertions — CI has no JetEngine, local Docker monolith exercises the CCT probe), PHPCS-clean. Sub-cluster 3b-1 (skill catalogue service + REST) landed: `WP_MCP_AI_Skill_Catalogue_Service` (943 lines) + `WP_MCP_AI_Skill_Catalogue_REST_Controller` (246 lines) ported (`src/services/`, `src/rest/`): byte-identical constants (`OPTION_SOURCES` `wp_mcp_ai_skill_catalogue_sources`, `TRANSIENT_PREFIX` `wp_mcp_ai_skill_cat_`, `CRON_HOOK` `wp_mcp_ai_skill_catalogue_refresh`, 4 MB response cap, 20 s timeout), the source registration/normalization/manifest/install/has_update flows, the SSRF-safe fetch chain (https-required, bad-host, DNS-failure, private/reserved-IP rejection, curl CURLOPT_RESOLVE DNS pinning — byte-identical ignore), the `mcp-ai-pro/v1` catalogue routes (`/catalogues`, `/{id}/skills|install|refresh`, `manage_options` gate), and the `wp_mcp_ai_skill_registry_missing` degradation; documented deviations (strict types; text domain; per-mode `registry_class()` seam — base `WP_MCP_AI_Skill_Registry` monolith / Platform addon's `Skills\SkillRegistry` standalone, same `instance()`/`install_skill()`/`get_skill()`/`load_skills()` contract); the entry's spl autoloader now probes the known `src/` subtree roots (`admin/`, `data-stores/`, `interfaces/`, `rest/`, `services/`, `tools/`, `vault/`) so ported files mirror the base `includes/` layout (README porting rule updated); 9 characterization tests green in both matrices (routes gated 403 for subscribers via dispatch), PHPCS-clean. Sub-cluster 3b-2 (skill-manager REST controller) landed: `WP_MCP_AI_Skill_Manager_REST_Controller` (516 lines) ported (`src/rest/`): byte-identical `mcp-ai-pro/v1` skills CRUD routes (GET/POST `/skills`, POST `/skills/install-url`, GET/PUT/DELETE `/skills/{name}` with the `[a-z0-9][a-z0-9-]{0,62}[a-z0-9]?` name pattern), the `manage_options` gate, the list/get/create/update/delete/install-url flows (raw-content handling documented upstream — parser validates inside `install_skill()`), and the byte-identical error codes (`rest_skill_not_found` 404, `rest_skill_name_mismatch` 422, `rest_skill_invalid_url` for non-HTTPS); documented deviations (strict types; text domain; per-mode seams — `get_registry()` resolves the base `WP_MCP_AI_Skill_Registry` (with its base-path requires) monolith / the Platform addon's `Skills\SkillRegistry` standalone, and the parser instantiation resolves the base `WP_MCP_AI_Skill_Parser` monolith / the Platform addon's `Skills\SkillParser` standalone, same `parse()` contract); 6 characterization tests green in both matrices (route gate, admin list, install→read→delete round-trip against the test-env uploads skills dir, 422 name-mismatch without touching the skill, parser-error surface, HTTPS-only install-url), PHPCS-clean. Sub-cluster 3b-3 (skill admin pages + init, skills-manager complete) landed: the three admin pages ported (`src/admin/class-wp-mcp-ai-skill-manager-admin-page.php` 1,673 lines, `-skill-research-admin-page.php` 432, `-skill-settings-admin-page.php` 769) + `src/skills-manager-init.php`: byte-identical page slugs (`wp-mcp-ai-skill-manager`, `wp-mcp-ai-skill-settings`, `research-skill`), the five manager AJAX actions (`wp_ajax_wp_mcp_ai_skill_manager_upload|install_url|save|delete|generate_skill` with `NONCE_ACTION` `wp_mcp_ai_skill_manager`), the upload (fileinfo MIME gate + 8 MB cap), save/delete/generate flows, the settings page's three registered options (`wp_mcp_ai_skills_enabled`, `wp_mcp_ai_skill_auto_inject`, `wp_mcp_ai_skill_max_per_assistant` with the byte-identical 1..100 clamp), the tabbed settings render with the two nonce fields (`wp_mcp_ai_skill_settings_nonce`/`wp_mcp_ai_skill_catalogues_nonce`), the catalogue sources management (restore-defaults flow), the research page's import form + review workflow, the catalogue-service cron wiring (`CRON_HOOK` + daily schedule), and the init's admin-gated page boot + REST controller construction; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps with the skill-manager assets copied byte-identically into this addon's assets tree; per-mode seams — `get_registry()`/`registry_instance()` resolve the base skill registry/parser monolith / the Platform addon's `Skills` ports standalone; `menu_parent()` resolves the base assistant-CPT menu monolith / the Platform addon's `PlatformDashboard::PAGE_SLUG` standalone (the assistant CPT stays base-owned by plan decision — no dead menus); the research page's `is_page_hook()` gate adds the title-derived `nv-platform_page_research-skill` suffix standalone and `research_assets_url()/version()` serve the Platform addon's byte-identical enhanced-research-page assets standalone (shared with E-UI-3); the chat-embed `class_exists('WP_MCP_AI_Shortcode')` guard stays byte-identical — standalone the chat panel degrades away; the init's cron schedule is standalone-gated); the registry's `pro_skills_manager` module now boots standalone (files guard satisfied — init file exists); 11 characterization tests green in both matrices (menu/AJAX hook wiring, per-mode menu registration, page renders, settings save via the redirect-intercept harness, save/delete/generate AJAX flows via the capture harness, nonce-rejection die message, standalone module boot), PHPCS-clean. Sub-cluster 4a (vault storage core) landed: `WP_MCP_AI_Vault_Encryption_Service` (706 lines) + `WP_MCP_AI_Vault_Item_CPT` (853) + `WP_MCP_AI_Vault_Folder_CPT` (175) + `WP_MCP_AI_Vault_Conflict_Resolver` (363) ported (`src/vault/`): byte-identical crypto constants (PBKDF2 100k, `aes-256-gcm`, TOTP step 30/length 6/drift 1), the per-user key derivation (auto-provisioned `_vault_encryption_salt`, tenant-scoped master key), the array-envelope encrypt/decrypt (iv/ciphertext/auth_tag base64), password generation + strength + TOTP generation/verification/QR URI, the `mcp_vault_item`/`mcp_vault_folder` CPT registrations (the folder CPT self-boots at file load, byte-identical) with the four item types (`login`/`note`/`card`/`identity`, invalid → `login`) and vault capability maps, and the conflict resolver's five strategies (local/remote/newest/merge/manual — `modified`/`created` timestamp comparison, option-backed queue + logs); documented deviations (strict types; text domain; phpcs ruleset additions — the vault custom capabilities allowlist + the base64 obfuscation-heuristic exclusion for the byte-identical crypto payload encoding); 8 characterization tests green in both matrices (AUTH_KEY test-sentinel fixture — the docker env lacks wp-config salts, production unaffected), PHPCS-clean. Sub-cluster 4b (vault wiring, vault complete) landed: the vault REST controller (952 lines) + the Bitwarden import/export (967) + sync service (417) + background sync (247) + the password-vault admin page (1,286) + the two vault tools (339 + 463) + `src/tools/vault/init.php` ported: byte-identical `mcp-ai/v1/vault` REST routes (`/items`, `/items/{id}`, `manage_options` gate), the `wp_mcp_ai_pro_tools` tool filter (vault access/manage + the F2 generate-password forward path), the init flow (all vault files + background-sync + admin-gated vault admin page + REST hook + `init` priority 20), the admin page's own top-level Password Vault menu with the `password-vault-admin.css/js` assets copied byte-identically; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps incl. the debug-log block; phpcs ignores for the byte-identical file-upload/params); NEW standalone-only wiring (no monolith counterpart — documented): `WP_MCP_AI_Pro_Tool_Adapter` (WP-style tool → `NvoosContentGraph\Contracts\Tool`) + `wp_mcp_ai_pro_register_vault_ecosystem_tools()` — the init registers the two vault tools into the ecosystem graph ToolRegistry and wraps them into the nvoos/core registry via the AI addon's GraphToolAdapter (the graph `register_tools` action fires at plugins_loaded 10, before this addon boots at 15, so registration is direct; the byte-identical `wp_mcp_ai_pro_tools` filter stays inert standalone); the registry's `toolkit_vault` module now boots standalone (files guard satisfied); 8 characterization tests green in both matrices (init hooks + tool-filter shape, REST routes + 403 gate, tool definitions + manage_options enforcement, standalone-only ecosystem registration + adapter contract + module boot), PHPCS-clean. Sub-cluster 5 (vector-storage adapter, F1 complete) landed: `WP_MCP_AI_Vector_Store_Adapter` (730 lines) ported (`src/services/`): byte-identical settings option (`wp_mcp_ai_vector_store_settings`), the three backends (openai/pgvector/qdrant with the key/label/configured/description listing), namespace resolution + persisted registry (`wp_mcp_ai_vector_namespaces`), the upsert/query/delete dispatch (openai via the byte-identical `class_exists( 'WP_MCP_AI_OpenAI_Client' )` guard, pgvector/qdrant stub responses with byte-identical unconfigured degradation incl. the `delete → true` stub contract), and the `wp_mcp_ai_vector_store_namespace` filter; documented deviations (strict types; text domain; per-mode credential seam `has_credentials()`/`get_api_key()` — the base plugin owns `WP_MCP_AI_Credential_Resolver` monolith, standalone it is a D8 forward-reference so credential-backed backends report unconfigured); the prepare-file tool stays monolith-registered via `wp_mcp_ai_pro_register_tools()` and is a D8 forward-reference standalone (needs the base tool infrastructure — `WP_MCP_AI_Tool_Interface` + the chat-response trait + `WP_MCP_AI_File_Preprocessing_Helper` — ported out of `includes/`; the root classmap makes porting copies into this addon a double-declaration hazard, documented in the registry docblock); the registry's `vector_storage` module boots standalone with the adapter files guard; 5 characterization tests green in both matrices (singleton/backends, pgvector DSN configuration, stub degradation, standalone qdrant credential seam, module boot), PHPCS-clean. Remaining: — (Wave F1 complete — pro-core: module registry + privacy + toolkit data-store factory + skills-manager + vault + vector-storage). Next: Wave F2 pro-business toolkits. |
| F2–F6 | business / media / dev / healthcare / legal / education / content / data / platform toolkits | 🟡 | Sub-cluster 1 (F2 pilot — CRM data layer) landed: the CRM shared engine classes (`WP_MCP_AI_CRM_Engine`, `_Codes`, `_Audit`, `_Capabilities`, `_Consent`, `_Pipeline_Stages`, `_Classifier` — `src/tools/crm/`, 7 files) + the eight CRM CPTs (`WP_MCP_AI_Company_CPT` `mcp_ai_company`, `WP_MCP_AI_Lead_CPT` `mcp_ai_lead`, `WP_MCP_AI_Deal_CPT` `mcp_ai_deal`, `WP_MCP_AI_CRM_Activity_CPT` `mcp_ai_crm_activity`, `WP_MCP_AI_Support_Ticket_CPT` `mcp_ai_ticket`, `WP_MCP_AI_Customer_CPT` `mcp_ai_customer`, `WP_MCP_AI_Sequence_CPT` `mcp_ai_sequence`, `WP_MCP_AI_CRM_Workflow_Rule_CPT` `mcp_ai_crm_wf_rule` — `src/class-wp-mcp-ai-*-cpt.php`) ported byte-identical: the byte-identical `enable_crm_toolkit` enabled gates (registry module + init + per-CPT `init()`), the pipeline-stage map (`prospecting`/`qualification`/… with the byte-identical probabilities + `get_open_stages()` stage-map filter), the code-pack registry, the capabilities map, the engine settings accessors, and the per-CPT admin-column/sortable filters; documented deviations (strict types; text domain; `src/` path swaps; slimmed standalone init — admin pages/REST/research-add/inbound/tools deferred to later F2 sub-clusters, each file-gated; `class_exists( 'WP_MCP_AI_Pro_Remote_Site_Manager' )` guards stay byte-identical — dormant until F6); the registry's `toolkit_crm` module carries the byte-identical enabled gate + files guard; 9 characterization tests green in both matrices (slug constants, CPT registrations, pipeline/codes/capabilities/engine contracts, standalone enabled-gate + init wiring), PHPCS-clean; the F1-1 registry tests updated for the sixth module. Sub-cluster 2 dependency finding (⚠️ BLOCKED on D8): the CRM tool files (~132) implement the base-owned `WP_MCP_AI_Tool_Interface`/`WP_MCP_AI_Tool_Capability_Flags_Interface` and use the base-owned `WP_MCP_AI_Tool_Chat_Response`/`_Envelope` traits + `WP_MCP_AI_Validator_Service` (all in `includes/`, served by the root classmap even standalone — copying them into this addon is a double-declaration hazard, same as the F1 prepare-file tool). The tool sub-clusters therefore wait for D8's base-tool infrastructure port; the non-tool F2 slices (CRM admin pages, CRM REST controller, ICP module, and the other toolkits' engines/CPTs/services) are unblocked. Sub-cluster 3 (D8-compat tool infrastructure — unblocks the tool sub-clusters) landed: the four base-owned tool symbols ported into the addon (`src/interfaces/interface-wp-mcp-ai-tool.php` — the grouped `WP_MCP_AI_Tool_Interface`/`WP_MCP_AI_Tool_Capability_Flags_Interface`/shortcuts/fallback/model-requirements/rules/flow-stage/data-contract/context-restrictions/sensitive-result interfaces; `src/interfaces/interface-wp-mcp-ai-tool-safety-profile.php`; `src/trait-wp-mcp-ai-tool-envelope.php`; `src/trait-wp-mcp-ai-tool-chat-response.php`) byte-identical, so ported Pro tools can `implements WP_MCP_AI_Tool_Interface` / `use WP_MCP_AI_Tool_Chat_Response` without the base plugin: in monolith installs the base plugin owns the symbols and the entry's `WP_MCP_AI_PRO_PATH` guard skips the addon's copies; standalone, the traits resolve only from the addon (the root classmap excludes `includes/tools/`) while the grouped interfaces may resolve from the root classmap in the monorepo test matrix (real standalone installs have no root vendor, so the addon's copies serve there); documented deviations (strict types; text domain `nvoos-content-graph-pro`; the entry's spl autoloader now also probes the `trait-` filename prefix); scope note: the base namespaced `WP_MCP_AI\Validators` service was NOT ported — the Pro toolkits use the Pro-owned global `WP_MCP_AI_Validator_Service` (in `addons/pro/includes/services/`), so no consumer exists; 5 characterization tests green in both matrices (symbol loadability, per-mode trait serving source via ReflectionClass, canonical success-envelope shape — never a `success => false` array, chat-response message guarantee, ensure_response_message standardization), PHPCS-clean. Sub-cluster 4 (proof pair — first CRM tool, D8-compat layer validated) landed: `WP_MCP_AI_Tool_Create_Company` ported (`src/tools/crm/class-wp-mcp-ai-tool-create-company.php`) byte-identical — the tool now loads standalone via the D8-compat trait/interface copies; the CRM init gained the new standalone-only tool wiring (documented deviation 5): a `wp_mcp_ai_pro_tools` filter (`wp_mcp_ai_pro_register_crm_tools()` — subset of the monolith's inline `$crm_tools` map, inert standalone) + `wp_mcp_ai_pro_register_crm_ecosystem_tools()` registering the tool into the graph ToolRegistry via `WP_MCP_AI_Pro_Tool_Adapter` and wrapping into the nvoos/core registry via GraphToolAdapter (same wiring as the vault); 8 characterization tests green in both matrices (byte-identical tool surface — slug/capability/schema/definition/flags, per-mode serving source, availability tracking the Company CPT, execute() success + missing-field WP_Error + capability gate, standalone filter shape + ecosystem registration), PHPCS-clean. Sub-cluster 5 (CRM companies tool batch — first fan-out) landed: `WP_MCP_AI_Tool_Get_Companies`, `WP_MCP_AI_Tool_Research_Company`, and `WP_MCP_AI_Tool_Archive_Stale_Contacts` ported byte-identical (`src/tools/crm/`); documented deviations (strict types; text domain; the research-company base web-search require is monolith-gated — standalone the base `WP_MCP_AI_Tool_Web_Search` stays a D8 forward-reference and `is_available()`/execute degrade through the byte-identical `class_exists` guard); the init's tool filter + ecosystem registration now carry all four ported tools; 8 characterization tests green in both matrices (surface contracts, get-companies execute with industry filtering, research-company per-mode availability + standalone unavailable WP_Error degradation, archive-stale-contacts dry-run preview + live draft archival with meta, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 6 (Pro validator service — unblocks the leads batch) landed: `WP_MCP_AI_Validator_Service` (365 lines) ported byte-identical (`src/services/`): the email/phone/URL/credit-card (Luhn) validators with the byte-identical filter seams (`wp_mcp_ai_validator_email|phone|url|credit_card`), the per-type `sanitize_input()` switch, the required/type/sanitize `validate_fields()` envelope, the disposable-domain allowlist, and the MX-record probe; documented deviations (strict types; text domain; the node_modules availability probe resolves from `NVOOS_CONTENT_GRAPH_PRO_PATH` — standalone the NPM packages don't ship so `is_available()` reports false and the PHP-native paths serve; strict-types coercion guards in `has_mx_records()`/`is_disposable_email()` — the base relies on coercing `strrchr()`'s false return to ''); 10 characterization tests green in both matrices (serving source, all validator contracts, field-envelope, disposable allowlist, malformed-MX degradation, boolean availability), PHPCS-clean. Sub-cluster 7 (CRM leads/customers tool batch) landed: the six leads tools (`create/list/get/update/delete` lead + `convert-lead-to-customer`) and `WP_MCP_AI_Tool_Create_Customer` ported byte-identical (`src/tools/crm/leads/`, `src/tools/crm/customers/`): the data-store-backed CRUD flows (tenant store via `WP_MCP_AI_Toolkit_Data_Store_Factory`, capability-map gates — delete defaults to `manage_options` — the delete confirmation gate, the validator-backed email/phone checks, lifecycle-stage resolution via the engine, the convert flow creating customer + optional deal records, and the audit-log records); documented deviations (strict types; text domain; validator-service requires resolve from `NVOOS_CONTENT_GRAPH_PRO_PATH . 'src/services/'`); the entry's spl autoloader gained the nested `src/tools/crm/leads/` + `src/tools/crm/customers/` subtree probes; the init's tool filter + ecosystem registration now carry all eleven ported tools; 9 characterization tests green in both matrices (surface contracts incl. the capability map, create-lead execute + malformed-email gates, full leads CRUD cycle, delete confirmation gate, create-customer, convert-with-deal, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 8 (CRM deals tool batch) landed: the six deals tools (`create/list/get/update/delete` deal + `move-deal-stage`) ported byte-identical (`src/tools/crm/deals/`): the lead-linked create flow (required `lead_id`, `default_stage()` → `prospecting`, `default_currency` USD, win-probability stamping), the closed-deal update guard, the `confirm`-gated delete, the stage-move envelope (`previous_stage`/`new_stage`/`win_probability`/`is_won`), and the audit-log records; documented deviations (strict types; text domain — the deals files carry no path constants, so no path swaps); the entry's spl autoloader gained the `src/tools/crm/deals/` subtree probe; the init's tool filter + ecosystem registration now carry all seventeen ported tools; 7 characterization tests green in both matrices (surface contracts incl. the capability map, create-deal + missing-lead rejection, deals CRUD cycle incl. the confirmation gate, move-deal-stage incl. invalid-stage rejection, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 9 (CRM activities tool batch) landed: the five activities tools (`create/list/get/complete/snooze` CRM activity) ported byte-identical (`src/tools/crm/activities/`): the CPT-direct create flow (`mcp_ai_crm_activity` posts, the byte-identical `ACTIVITY_TYPES` enum fallback to `note`, schema-only title requirement — execute() stays byte-identically lenient with an empty title), the meta-stamped complete (`completed`/`completed_at`/`outcome`) and snooze (`due_date`/`snooze_count`) flows, and the audit-log records; documented deviations (strict types; text domain — no path constants, so no path swaps); the entry's spl autoloader gained the `src/tools/crm/activities/` subtree probe; the init's tool filter + ecosystem registration now carry all twenty-two ported tools; 6 characterization tests green in both matrices (surface contracts, create + the byte-identical untitled leniency, complete/snooze meta stamps + ghost-ID rejection, list/get flows, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 10 (CRM analytics/routing tool batch) landed: the five pipeline analytics tools (`get-pipeline-view`, `get-conversion-funnel`, `forecast-pipeline-revenue`, `identify-top-customers`, `identify-top-clients`) and the two routing tools (`assign-lead-to-owner`, `rotate-leads`) ported byte-identical (`src/tools/crm/analytics/`, `src/tools/crm/routing/`): the read-only pipeline/funnel/revenue aggregations over the data stores, the top-customer/client ranking flows, the owner-assignment flow (`contact_owner` meta + engine routing fallback), and the `manage_options`-gated rotation; documented deviations (strict types; text domain; phpcs ruleset addition — the `WordPress.WP.PostsPerPage` sniff at severity 0 for the byte-identical 500-1000 pipeline query limits); the entry's spl autoloader gained the `analytics/` + `routing/` subtree probes; the init's tool filter + ecosystem registration now carry all twenty-nine ported tools; 6 characterization tests green in both matrices (surface contracts incl. the rotate-leads `manage_options` gate, analytics smoke on an empty CRM, assign-lead-to-owner meta stamp + ghost-ID rejection, rotate-leads smoke, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 11 (CRM REST controller slice) landed: `WP_MCP_AI_CRM_REST_Controller` (512 lines) ported byte-identical (`src/rest/`): the six `mcp-ai-pro/v1/crm` routes (`/contacts`, `/contacts/{id}`, `/deals`, `/deals/{id}`, `/pipeline`, `/kpis`) with the byte-identical `edit_posts` gate (`rest_forbidden` + `rest_authorization_required_code()`), the batch-loaded-meta collection shapes with `X-WP-Total`/`X-WP-TotalPages` headers, the single-record 404 contracts, the ten-stage pipeline aggregation, and the KPI totals (`wp_count_posts`-backed — registered-CPT-gated, matching the monolith where the CRM init registers the CPTs); documented deviations (strict types; text domain; phpcs inline ignores for the byte-identical unused `$request` handler parameters); the CRM init now requires + boots the controller inside the enabled block (deviation 3 updated); 8 characterization tests green in both matrices (serving source, route registration incl. regex-normalized `{id}` keys, permission gate 403/200, contacts collection/single + non-lead 404, deals collection/single, pipeline stage bucketing, KPI totals with registered CPTs, standalone init wiring), PHPCS-clean. Sub-cluster 12 (CRM ICP + admin slice) landed: `WP_MCP_AI_ICP_Profile` (459), `WP_MCP_AI_ICP_Scorer` (1,650), `WP_MCP_AI_Tool_Compute_ICP_Score` (783), `WP_MCP_AI_Tool_Manage_ICP_Profile` (1,153) ported byte-identical (`src/tools/crm/icp/`) plus `WP_MCP_AI_CRM_Admin_Menu` (119) + `WP_MCP_AI_ICP_Admin_Page` (1,731) (`src/admin/`): the option-backed profile store (`OPTION_KEY` `wp_mcp_ai_icp_profiles`, id-keyed CRUD, last-profile delete guard, default management, validation, scoring dimensions, example profile), the scorer's seven-dimension compute (`total_score`/`fit_score`/`intent_score`/`economic_score`/`tier` + decay + `score_against_all_profiles`), the compute tool's pre-flight contracts (Company-CPT availability, data-source resolution, 3-point completeness gate), the manage tool's action contract (`list/get/create/update/delete/set_default` with the `manage_options` gate), the CRM admin menu (`PARENT_SLUG` `nvoos-crm-dashboard`, admin_menu 25/28 — the command-center landing page stays a documented forward-reference), and the ICP admin page (`PAGE_SLUG` `nvoos-crm-icp-profiles`, admin_menu 30, nonce-checked handlers, inline asset JS); documented deviations (strict types; text domain; phpcs ruleset additions — `UnusedFunctionParameter`/`NonceVerification`/`MissingTranslatorsComment` severity 0 + `urlencode` exclusion for the byte-identical patterns); ⚠ TWO UPSTREAM LATENT BUGS found and pinned as-is (byte-identical — follow-up issue candidates): (1) `compute-icp-score`'s `resolve_profile()` casts the profile id to int and passes it to `compute_score()`'s array-typed parameter — the compute step TypeError-crashes once data completeness passes (only the pre-flight paths are test-pinned); (2) `manage-icp-profile`'s `handle_create()` never stamps the generated slug into the data's `id` field, so `validate_profile()` rejects every create with `icp_validation_missing_id`; the entry's spl autoloader gained the `src/tools/crm/icp/` probe; the CRM init gained the Phase-G ICP requires + the admin-menu/ICP-admin wiring (is_admin-gated); the init's tool filter + ecosystem registration now carry all thirty-one ported tools; 8 characterization tests green in both matrices (serving sources ×6, profile CRUD + last-profile guard, scorer compute + all-profiles, compute tool pre-flight contracts, manage tool action contract + pinned create contract, admin menu + ICP page constants/hooks, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 13 (CRM per-CPT settings pages batch) landed: the five settings pages (`WP_MCP_AI_Company_Settings_Page` `wp_mcp_ai_company_settings`, `WP_MCP_AI_Lead_Settings_Page` `wp_mcp_ai_lead_settings`, `WP_MCP_AI_Deal_Settings_Page` `wp_mcp_ai_deal_settings`, `WP_MCP_AI_Support_Ticket_Settings_Page` `wp_mcp_ai_ticket_settings`, `WP_MCP_AI_Customer_Settings_Page` `wp_mcp_ai_customer_settings`) ported byte-identical (`src/admin/`): the per-CPT submenu registration (admin_menu 25, CPT-edit-parent, `manage_options`), the `admin_init` option-group registration with per-page sanitize callbacks, and the byte-identical sanitization contracts (negative assistant-id clamp-to-0, status allowlist rejection, per-field sanitizers); documented deviations (strict types; text domain); the CRM init's admin block now requires + inits all five pages (deviation 3 updated); 6 characterization tests green in both matrices (serving sources ×5, page constants, init hooks, registered settings groups, company sanitize contract, standalone init wiring), PHPCS-clean. Sub-cluster 14 (CRM blueprints admin page) landed: `WP_MCP_AI_CRM_Blueprints_Page` (476) ported byte-identical (`src/admin/`) with the eight example blueprint JSONs + `crm-blueprints.js` copied byte-identically (`src/tools/crm/examples/`, `assets/js/`): the `nvoos-crm-blueprints` page (admin_menu 27), the asset enqueue, the two install/details AJAX hooks with the `wp_mcp_ai_crm_bp` nonce, and the byte-identical blueprint-installer degrade guards; documented deviations (strict types; text domain; `BLUEPRINTS_DIR` resolves from `NVOOS_CONTENT_GRAPH_PRO_PATH . 'src/tools/crm/examples'`; asset URL/version swaps; the installer requires resolve to the not-yet-ported orchestration path — forward-reference, degrade-gated); the CRM init's admin block now requires + inits the page (deviation 3 updated); 5 characterization tests green in both matrices (serving source, per-mode constants incl. the examples path, init hooks incl. the AJAX pair, the eight shipped example files, standalone wiring), PHPCS-clean. ⚠ CRM SETTINGS PAGE DEFERRED (documented block): `WP_MCP_AI_CRM_Settings_Page` extends `WP_MCP_AI_Toolkit_Settings_Base` (1,258 lines) which drags in the base-owned MCP server chain (`WP_MCP_AI_Toolkit_Server_Base`/`_Interface`, `WP_MCP_AI_Toolkit_MCP_REST_Controller`, `WP_MCP_AI_Toolkit_MCP_Audit_Log`, `WP_MCP_AI_Remote_Capabilities_Loader`) — a separate MCP-infrastructure slice. Sub-cluster 15 (inbound dependency slice — message log) landed: `WP_MCP_AI_CRM_Message_Log` (539) ported byte-identical (`src/tools/crm/`): the toolkit-gated `mcp_crm_message` CPT, the structured `log()` flow (sanitize-at-entry meta set, `_logged_at`, the `wp_mcp_ai_crm_message_logged` action, audit record), the `DEDUP_OPTION`-backed idempotent dedup (`is_duplicate`/`find_by_message_id`, 10k cap), the contact/ticket-scoped queries with the `format_message()` shape, the channel volume stats (total + by_channel), and the link helpers; documented deviations (strict types; text domain — no path constants in the file); loaded on demand by the inbound tools (next sub-cluster) via the existing `src/tools/crm/` autoload probe; 7 characterization tests green in both matrices (serving source, registration gate, log round-trip with meta, dedup idempotency, scoped queries, volume stats shape, link helpers), PHPCS-clean. ⚠ AUDIT COMPLETE for the inbound/outbound batches (tracker-documented): the 15 tools need `WP_MCP_AI_CRM_Message_Log` (landed) + already-ported engine/audit/classifier/consent/codes; the remaining refs are in-batch cross-refs or `class_exists`-guarded dormant (`WP_MCP_AI_Admin_Settings` base-owned classmapped, `WP_MCP_AI_Pro_Remote_Site_Manager` F6, `WP_MCP_AI_CRM_Ticket_Notifications` support-dir, `WP_MCP_AI_Pro_Tool_Send_WhatsApp_Message` comms-toolkit). Sub-cluster 16 (CRM inbound tool batch) landed: the seven inbound tools (`evaluate-inbound-message`, `classify-message-intent`, `extract-lead-from-message`, `detect-buying-signals`, `score-lead`, `qualify-lead-bant`, `qualify-lead-meddic`) ported byte-identical (`src/tools/crm/inbound/`): the LLM-backed pipeline pre-flights (toolkit availability, `edit_posts` gate, message-log persistence via the landed Message_Log, the classifier-driven steps), the lead-backed resolution gates (`not_found` before any LLM work), and the byte-identical evaluate leniency (schema-only `message_body` — execute degrades gracefully in the no-API-key env); documented deviations (strict types; text domain — no path constants); the entry's spl autoloader gained the `src/tools/crm/inbound/` probe; the init's tool filter + ecosystem registration now carry all thirty-eight ported tools; 6 characterization tests green in both matrices (surface contracts, serving sources ×4, pre-flight + forbidden + byte-identical lenient evaluate, lead-resolution gates ×3, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 17 (CRM outbound tool batch) landed: the eight outbound tools (`send-lead-email`, `send-lead-sms`, `send-lead-whatsapp`, `send-lead-dm`, `log-call-outcome`, `draft-lead-reply`, `auto-reply-inbound`, `schedule-follow-up`) ported byte-identical (`src/tools/crm/outbound/`): the contact-data gates (`no_email`/`no_phone`), the consent + DNC gates via the ported `WP_MCP_AI_CRM_Consent`/`_Engine`, the lead-resolution `not_found` gate, the channel send flows (external — untested), the `wp_mcp_ai_crm_before_outbound_send` veto filter, the auto-reply channel dispatch (in-batch requires path-swapped), and the follow-up activity creation; documented deviations (strict types; text domain; the auto-reply's two in-batch requires resolve from `NVOOS_CONTENT_GRAPH_PRO_PATH . 'src/tools/crm/outbound/'`); the entry's spl autoloader gained the `src/tools/crm/outbound/` probe; the init's tool filter + ecosystem registration now carry all forty-six ported tools; 9 characterization tests green in both matrices (surface contracts incl. required args, serving sources ×4, no-email/no-phone gates, consent gate, call-outcome not_found, schedule-follow-up activity creation, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 16 (CRM command-center admin page) landed: `WP_MCP_AI_CRM_Command_Center_Page` (4,588 lines) ported byte-identical (`src/admin/`): the `nvoos-crm-command-center` page under the `nvoos-crm-dashboard` parent (admin_menu 26), the seven CC AJAX endpoints (`wp_ajax_wp_mcp_ai_crm_cc_get_dashboard|get_pipeline|refresh_all_sources|hygiene_add|hygiene_remove|merge_duplicate|lead_tags_update` with `NONCE_ACTION` `wp_mcp_ai_crm_cc`), the inline asset block, and the overview/leads/pipeline/activities/sequences/support/analytics/top-customers/top-clients/duplicates/configuration tab renders; documented deviations (strict types; text domain; the 12 tool-file path references resolve from `NVOOS_CONTENT_GRAPH_PRO_PATH . 'src/tools/crm/'`); forward-references: the compliance, inbound Gmail-import, Upwork, and LinkedIn tool files land with the F2 tool remainder — the byte-identical `file_exists` guards degrade those tabs until then, and the `class_exists`-guarded `WP_MCP_AI_Pro_Remote_Site_Manager`/`WP_MCP_AI_Admin_Settings` refs stay inert standalone; the CRM init's admin block now wires the page after the admin menu; 4 characterization tests green in both matrices (serving source, constants incl. the `nvoos-crm-dashboard` parent, init hooks + all seven AJAX endpoints, standalone init wiring), PHPCS-clean. Sub-cluster 17 (CRM core + email-search batch) landed: `WP_MCP_AI_Tool_Manage_CRM_Contact`, `WP_MCP_AI_Tool_CRM_Email_Search_Leads|Correspondence|Accounting`, and `WP_MCP_AI_Tool_CRM_Capture_Interaction` ported byte-identical (`src/tools/crm/`) plus their support symbols — the `WP_MCP_AI_CRM_Relevance_Search` trait (540 lines, `src/traits/`), the abstract `WP_MCP_AI_Pro_Capture_Tool_Base` (`src/tools/capture/`), and the `WP_MCP_AI_CRM_Gmail_Client` service (475 lines, `src/services/`): the store-backed contact CRUD (`manage_crm_contact` actions create/read/update/delete/list/search), the three cached/WP-Cron email-search tools over the TF-IDF/BM25 trait (lead scoring, correspondence response analytics, accounting categorisation with the `class_exists`-guarded QuickBooks/`WP_MCP_AI_Cache_Helper` seams), the MemPalace `account/{account_id}` capture tool (rooms interactions/objections/next-actions), and the Gmail OAuth client; documented deviations (strict types; text domain; `src/` path swaps incl. the trait/capture/gmail requires; the Gmail client's two remote-site-manager requires are file-gated — F6 forward-reference with the byte-identical `class_exists` re-check; the Gmail import bridge in email-search-leads stays a `file_exists`-gated forward-reference; the base-owned `WP_MCP_AI_Memory_Capture_Service` stays served by the root classmap — not copied, same ownership boundary as the D8 tool infra); the entry's spl autoloader gained the `src/tools/capture/` + `src/traits/` probes; the init's tool filter + ecosystem registration now carry all fifty-one ported tools; 7 characterization tests green in both matrices (serving sources ×8, surfaces, email-search forbidden gate, manage-contact invalid-action, capture argument gates, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 18 (CRM upwork batch) landed: `WP_MCP_AI_Tool_Search_Upwork_Jobs`, `WP_MCP_AI_Tool_Score_Upwork_Job`, and `WP_MCP_AI_Tool_Draft_Upwork_Proposal` ported byte-identical (`src/tools/crm/upwork/`) plus the `WP_MCP_AI_Upwork_Client` GraphQL API client (490 lines, `src/`): the Upwork GraphQL marketplace search (query/filter/budget/experience-level schema, 1-50 limit), the weighted job scoring, the AI proposal drafting (with the `class_exists`-guarded OpenAI/Gemini/Anthropic provider chain), and the client's OAuth 2.0 token flow (transient-cached access tokens, byte-identical endpoint/rate-limit/backoff constants); documented deviations (strict types; text domain; the client require resolves from `NVOOS_CONTENT_GRAPH_PRO_PATH . 'src/'`; wave-proof `class_exists` guards — `WP_MCP_AI_Tool_Registry` in `execute_fallback()` and `WP_MCP_AI_Credential_Resolver` in `get_ai_provider()` are base-owned (root classmap); real standalone installs degrade to the same fallback-unavailable/no-ai-provider errors); the entry's spl autoloader gained the `src/tools/crm/upwork/` probe; the init's tool filter + ecosystem registration now carry all fifty-four ported tools; 5 characterization tests green in both matrices (serving sources ×4, surfaces, client constants, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 19 (CRM sequences batch) landed: the seven outreach-sequence tools (`WP_MCP_AI_Tool_Create|Update|Delete|List_Outreach_Sequence`, `Enroll_Lead_In_Sequence`, `Manage_Sequence_State`, `Get_Sequence_Performance`) ported byte-identical (`src/tools/crm/sequences/`): the multi-step cadence definition (`{ channel, template_id, wait_hours, branch_on_reply }` step schema), the idempotent enrollment with suppression check, the pause/resume/exit state transitions, the `manage_options`-gated delete, and the enrollment/completion/step-level performance metrics; documented deviations (strict types; text domain — no path constants); the entry's spl autoloader gained the `src/tools/crm/sequences/` probe; the init's tool filter + ecosystem registration now carry all sixty-one ported tools; 5 characterization tests green in both matrices (serving sources ×7, surfaces incl. the delete `manage_options` gate, create-sequence forbidden gate, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 20 (CRM workflow-rules + routing batch) landed: the six command-center tools (`WP_MCP_AI_Tool_Create_Crm_Workflow_Rule`, `Manage_Workflow_Rules`, `Simulate_Workflow_Rule`, `Get_Workflow_Inbox`, `Auto_Route_Inbound_Message`, `Get_Owner_Workload`) ported byte-identical (`src/tools/crm/command-center/`): the if-this-then-that rule create/manage/simulate flows (simulate is a dry-run against historical messages), the unified inbox queue (hot leads + overdue follow-ups + pending approvals + unread replies), the workload-aware inbound routing, and the owner workload report (active leads + overdue tasks + response SLA) with the `manage_options`-gated rule-management surfaces; documented deviations (strict types; text domain — no path constants); the entry's spl autoloader gained the `src/tools/crm/command-center/` probe; the init's tool filter + ecosystem registration now carry all sixty-seven ported tools; 4 characterization tests green in both matrices (serving sources ×6, surfaces incl. the manage_options gates, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 21 (CRM consent/DNC batch) landed: the five compliance tools (`WP_MCP_AI_Tool_Record_Consent`, `Revoke_Consent`, `Process_Opt_Out`, `Check_Dnc_Status`, `Get_Consent_Audit`) ported byte-identical (`src/tools/crm/compliance/`): the channel-specific consent record with legal basis + evidence, the cross-channel revocation with DNC propagation, the channel/global opt-out flow (DNC listing + consent revocation + the default pseudonymisation "safe delete" with `preserve_record` escape hatch), the internal + filter-extensible federal/state DNC check, and the full regulator/DSAR audit trail with the `manage_options`-gated revoke/audit surfaces; documented deviations (strict types; text domain — no path constants); the entry's spl autoloader gained the `src/tools/crm/compliance/` probe; the init's tool filter + ecosystem registration now carry all seventy-two ported tools; 4 characterization tests green in both matrices (serving sources ×5, surfaces incl. the manage_options gates, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 22 (CRM hygiene + dedup batch) landed: the six compliance tools (`WP_MCP_AI_Tool_Classify_Email_Hygiene`, `Manage_Email_Hygiene`, `Prune_CRM_Messages`, `Repair_CRM_Data`, `Detect_Duplicates`, `Merge_Duplicates`) ported byte-identical (`src/tools/crm/compliance/`): the email hygiene classifier (spam/promotional/priority/notification heuristics), the exclude/priority list CRUD, the auditable message pruning (dry-run + retention rules), the data-quality repair (broken dates, generic titles, malformed meta), and the configurable dedup pair (exact-email/normalised-phone/fuzzy-name+company/name+domain matching with the survivor-wins + vacant-field-fill merge rules and child-record reassignment) with the capability-map `manage_options` gates; documented deviations (strict types; text domain — no path constants); the init's tool filter + ecosystem registration now carry all seventy-eight ported tools; 4 characterization tests green in both matrices (serving sources ×6, surfaces incl. the capability-map gates, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 23 (CRM imports + connect batch) landed: `WP_MCP_AI_Tool_Import_CRM_Csv`, `WP_MCP_AI_Tool_Connect_To_External_Crm`, and `WP_MCP_AI_Tool_Import_CRM_Blueprint` ported byte-identical (`src/tools/crm/compliance/`, `src/tools/crm/examples/`): the bulk lead CSV import (field mapping, dedupe, dry-run), the OAuth connection config/test for HubSpot/Salesforce/Pipedrive (`manage_options`-gated), and the curated blueprint installer delegation; documented deviations (strict types; text domain; `BLUEPRINTS_DIR` resolves from `NVOOS_CONTENT_GRAPH_PRO_PATH . 'src/tools/crm/examples'`; the installer require resolves from `src/tools/orchestration/` — not yet ported — and gained a wave-proof `class_exists` re-check returning the same installer-missing degradation instead of a fatal); the entry's spl autoloader gained the `src/tools/crm/examples/` probe; the init's tool filter + ecosystem registration now carry all eighty-one ported tools; 5 characterization tests green in both matrices (serving sources ×3, surfaces incl. per-mode BLUEPRINTS_DIR + capability gates, standalone installer degradation, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 24 (CRM customers batch) landed: the four customer tools (`WP_MCP_AI_Tool_Get_Customer`, `Update_Customer`, `Delete_Customer`, `List_Customers`) ported byte-identical (`src/tools/crm/customers/`): the single-record retrieval (`post_type_exists( 'mcp_ai_customer' )` availability), the validator-backed update, the compliance-aware trash-then-restore delete with `force_delete` escape hatch, and the searchable listing; documented deviations (strict types; text domain; the validator-service requires resolve from `NVOOS_CONTENT_GRAPH_PRO_PATH . 'src/services/'`); the init's tool filter + ecosystem registration now carry all eighty-five ported tools; 4 characterization tests green in both matrices (serving sources ×4, surfaces incl. the capability-map fallbacks (edit_posts/delete_posts), standalone filter/registration coverage), PHPCS-clean. Sub-cluster 25 (CRM engagement batch) landed: `WP_MCP_AI_Tool_Get_Contact_Interactions`, `WP_MCP_AI_Tool_Recalculate_Engagement_Scores`, and `WP_MCP_AI_Tool_Scan_Duplicate_Contacts` ported byte-identical (`src/tools/crm/`): the per-contact interaction timeline, the engagement-score recalculation, and the duplicate-contact scan with the `read`-gated read-only surfaces; documented deviations (strict types; text domain — no path constants); the init's tool filter + ecosystem registration now carry all eighty-eight ported tools; 4 characterization tests green in both matrices (serving sources ×3, surfaces incl. the read/edit_posts capability mix, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 26 (CRM CC-page extras, CRM tool surface complete) landed: the eight command-center-referenced tools (`WP_MCP_AI_Tool_Import_Gmail_To_CRM`, `Import_Upwork_Project`, `List_Upwork_Contracts`, `Sync_Upwork_Tasks`, `Import_Linkedin_Profile`, `Save_Linkedin_Job`, `Score_Linkedin_Job`, `Search_Linkedin_Jobs`) ported byte-identical (`src/tools/crm/inbound|upwork|linkedin/`) plus the `WP_MCP_AI_LinkedIn_Client` (376 lines, `src/`): the Gmail-to-CRM inbox import pipeline (message-log + evaluate-inbound integration with the `file_exists`-gated pipeline seam), the Upwork project import/contracts/tasks sync over the ported Upwork client, the LinkedIn profile import + job save/score/search over the ported LinkedIn client, and the `class_exists`-guarded dormant seams (`WP_MCP_AI_Project_CPT`/`Task_CPT` PM CPTs, remote-site manager, admin settings, web-search); these files exist in the base tree but are NOT part of the monolith's `$crm_tools` map — the standalone filter/ecosystem registrations carry them (documented deviation 5 updated); the entry's spl autoloader gained the `src/tools/crm/linkedin/` probe; the init's tool filter + ecosystem registration now carry all ninety-six ported tools (the full 86-entry monolith CRM map + 8 CC extras); 4 characterization tests green in both matrices (serving sources ×9, surfaces, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 27 (MCP toolkit-server core slice) landed: the eight MCP server symbols (`WP_MCP_AI_Toolkit_Server_Interface` + `_Registry` + `_Base` + `_MCP_Audit_Log` + `_MCP_REST_Controller` + `_OAuth_Server` + `_OAuth_REST` + `_Pro_Toolkit_Server_Token` — 4,233 lines) ported byte-identical (`src/mcp-servers/`): the toolkit-server registry (interface-gated `register()`/`get()`/`all()`, idempotent `bootstrap()`), the 200-entry ring-buffer audit log, the MCP JSON-RPC REST controller (mcp-ai-pro/v1/mcp routes on `rest_api_init`, the base-owned `WP_MCP_AI_Tool_Registry`/`REST_Authenticator`/`Assistant_CPT`/metabox seams stay classmap-served behind the byte-identical `class_exists` guards), the OAuth server + REST surfaces, and the toolkit-server token lifecycle (`mcptk_` prefix, 10-token cap, hash_equals validation); documented deviations (strict types; text domain — no path constants); the entry's spl autoloader now strips the `_Interface`/`_Trait` FQCN suffix when probing the `interface-`/`trait-` file prefixes (fixes standalone resolution of `WP_MCP_AI_Toolkit_Server_Interface` and the D8 grouped interfaces) and gained the `src/mcp-servers/` probe; 6 characterization tests green in both matrices (serving sources ×8, registry register/get, audit-log record, token constants + generate/validate round trip, route registration via rest_api_init), PHPCS-clean. Sub-cluster 28 (MCP settings-base slice, CRM toolkit complete) landed: `WP_MCP_AI_Toolkit_Settings_Base` (1,258 lines), `WP_MCP_AI_Remote_Capabilities_Loader` (144), and `WP_MCP_AI_CRM_Settings_Page` (1,581) ported byte-identical (`src/admin/`, `src/admin/remote-capabilities/`): the shared toolkit settings base (admin_menu 30 + admin_init settings registration, the per-toolkit page slug/option/parent surface, the remote-capabilities loader seam, the research-add probe), the per-toolkit capability descriptions loader, and the CRM settings page (toolkit surface crm/`wp_mcp_ai_crm_toolkit_settings`/`wp-mcp-ai-crm-toolkit-settings` under the `nvoos-crm-dashboard` parent, self-booting at file load byte-identical with the base init's `new WP_MCP_AI_CRM_Settings_Page()`); documented deviations (strict types; text domain; `src/` path swaps; the remote-site-manager require is file-gated with a class_exists fallback to an empty connection list — F6 forward-reference; the research-add probe resolves to the not-yet-ported `src/research-add/` path behind the byte-identical `file_exists` guard); the entry's spl autoloader gained the `src/admin/remote-capabilities/` probe; the CRM init's admin block now wires the settings page after the command center (deviation 3 updated); 5 characterization tests green in both matrices (serving sources ×3, loader contracts, CRM page surface, settings-base hooks, standalone init wiring), PHPCS-clean. Remaining: — (Wave F2 CRM toolkit complete: data layer + 96 tools + command center + blueprints + per-CPT pages + ICP + REST + message log + MCP server core + settings base). Next: e-commerce/PM/financial/calendar/social toolkits (F2–F6). |
| F2-E | e-commerce toolkit | 🟡 | Sub-cluster 1 (data layer) landed: `WP_MCP_AI_Sync_Log_Manager` (672 lines, `src/`), the `wp_mcp_ai_is_ecommerce_toolkit_enabled()` helper (33), `WP_MCP_AI_Ecommerce_Optimization` (207), the three shared traits (`WP_MCP_AI_Woo_Price_Qty_Updater` 369, `WP_MCP_AI_Shopify_Connection_Resolver` 206, `WP_MCP_AI_Shopify_Smart_Search` 309), and the slimmed `src/tools/ecommerce/init.php` ported byte-identical: the sync-log run lifecycle (string run IDs, MAX_RUNS 50/MAX_ITEMS_PER_RUN 200/DEFAULT_RETENTION_DAYS 30, Action Scheduler pruning behind the byte-identical `function_exists` guards), the enablement gate, the performance optimization cron, and the init's admin block (4 page requires file-gated — the pages land with the F2-E admin slice) + asset enqueue (the base CSS file does not ship in `addons/pro/assets/css/`, so the byte-identical `file_exists` guard keeps the enqueue inert); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps; local vars prefixed `$nvoos_content_graph_pro_*` (same pattern as the CRM init); the `woocommerce_no_stock` WC-core hook carries an inline phpcs ignore); the entry's spl autoloader gained the `src/tools/ecommerce/` probe; 5 characterization tests green in both matrices (serving sources ×5, sync-log constants + run round trip, enablement helper, standalone init wiring), PHPCS-clean. Sub-cluster 2 (products batch) landed: the seven product-management tools (`WP_MCP_AI_Tool_Create_Product_Advanced`, `Bulk_Update_Products`, `Update_Woo_Product_Price`, `Update_Woo_Product_Qty`, `Import_Products_CSV`, `Export_Products_Report`, `Sync_Product_Inventory`) ported byte-identical (`src/tools/ecommerce/`): the advanced product creation, the trait-backed price/qty updates (`manage_woocommerce` execute gates), the CSV import reusing the create tool, the Node.js-backed Excel export (`generate-excel.js` copied to `scripts/`), and the sync-log-manager-backed inventory sync; documented deviations (strict types; text domain; trait/script paths resolve from `NVOOS_CONTENT_GRAPH_PRO_PATH`; phpcs ruleset additions — `/scripts/*` excluded from lint, WooCommerce core caps allowlisted); NEW standalone-only wiring (deviation, same as the CRM init): `wp_mcp_ai_pro_register_ecommerce_tools()` filter + ecosystem registration; the registry gained the `toolkit_ecommerce` module (byte-identical `enable_ecommerce_toolkit` gate — the F1 registry test updated for the seventh module); 5 characterization tests green in both matrices (serving sources ×7, surfaces, create-product forbidden gate, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 3 (orders batch) landed: the four order-management tools (`WP_MCP_AI_Tool_Bulk_Order_Status_Update`, `Get_Order_Analytics`, `Process_Order_Workflow`, `Refund_Order_Advanced`) ported byte-identical (`src/tools/ecommerce/`): the bulk status update, the order analytics aggregation, the order workflow processor, and the advanced refund flow with the `woocommerce_order_status_changed`/`woocommerce_order_refunded` WC-core hooks carrying inline phpcs ignores; documented deviations (strict types; text domain — no path constants); the init's tool filter + ecosystem registration now carry all eleven ported e-commerce tools; 4 characterization tests green in both matrices (serving sources ×4, surfaces, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 4 (customers + inventory batch) landed: the six tools (`WP_MCP_AI_Tool_Segment_Customers`, `Customer_Lifetime_Value`, `Export_Customer_Data`, `Track_Inventory_Movement`, `Low_Stock_Alert_Automation`, `Inventory_Forecast`) ported byte-identical (`src/tools/ecommerce/`): the customer segmentation, the lifetime-value calculation, the customer data export, the inventory movement tracker, the low-stock alert automation (with the `class_exists`-guarded `WP_MCP_AI_Tool_Artifact_Helper` fallback), and the inventory forecasting; documented deviations (strict types; text domain — no path constants); the init's tool filter + ecosystem registration now carry all seventeen ported e-commerce tools; 4 characterization tests green in both matrices (serving sources ×6, surfaces, standalone filter/registration coverage), PHPCS-clean; the data-layer init test's enqueue-hook assertion is now first-loader-gated (the WP test framework's per-test hook backup/restore resets hooks registered by an earlier test's first init load — documented). Sub-cluster 5 (marketing batch) landed: the six tools (`WP_MCP_AI_Tool_Create_Discount_Campaign`, `Abandoned_Cart_Recovery`, `Get_Abandoned_Carts`, `Send_Cart_Recovery_Email`, `Upsell_Recommendations`, `Sales_Performance_Dashboard`) ported byte-identical (`src/tools/ecommerce/`): the discount campaign creation, the abandoned-cart recovery + retrieval + email flows (`manage_woocommerce`-gated for the cart reads/emails), the upsell recommendations, and the sales performance dashboard; documented deviations (strict types; text domain — no path constants); the init's tool filter + ecosystem registration now carry all twenty-three ported e-commerce tools; 4 characterization tests green in both matrices (serving sources ×6, surfaces incl. the manage_woocommerce gates, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 6 (shipping + invoice batch) landed: `WP_MCP_AI_Tool_Shipping_Box_Packer` (951 lines), `Shipping_Rate_Estimator` (1,383), and `Generate_WooCommerce_Order_Invoice_PDF` (487) ported byte-identical (`src/tools/ecommerce/`): the 3D box-packing solver, the multi-carrier rate estimation (with the `class_exists`-guarded remote-site-manager seam + version swap), and the Node.js invoice PDF generation (the `generate-invoice.js` worker does not ship in the base scripts dir either — byte-identical upstream gap documented; the two `@unlink` temp cleanups gained inline ignores); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` refs); phpcs ruleset addition — `WordPress.DB.PreparedSQLPlaceholders.ReplacementsWrongNumber` severity 0 (byte-identical base query shapes in the e-commerce analytics tools); the init's tool filter + ecosystem registration now carry all twenty-six ported e-commerce tools (the full `enable_ecommerce_toolkit` map); 4 characterization tests green in both matrices (serving sources ×3, surfaces, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 7 (woo tools batch) landed: the four WooCommerce tools (`WP_MCP_AI_Pro_Tool_Woo_Products` 1,548 lines, `Woo_Orders`, `Woo_Customers`, `Woo_Coupons`) ported byte-identical (`src/tools/ecommerce/`): the trait-backed product CRUD, the order/customer/coupon management surfaces, and the standalone `wp_mcp_ai_pro_is_woocommerce_tools_enabled()` gate (enabled by default unless `enable_woocommerce_tools` is falsy — the monolith defines it inline; documented deviation); phpcs ruleset addition — WooCommerce core caps `publish_products`/`delete_products`/`manage_product_terms`/`edit_product_terms`/`read_shop_order`/`edit_shop_order` allowlisted; the init's tool filter + ecosystem registration now carry all thirty ported e-commerce tools; 5 characterization tests green in both matrices (serving sources ×4, surfaces, enablement gate, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 8 (shopify batch) landed: the five Shopify tools (`WP_MCP_AI_Pro_Tool_Shopify_Products` 865 lines, `Orders`, `Customers`, `Inventory`, `Catalog`) ported byte-identical (`src/tools/ecommerce/`) plus the `WP_MCP_AI_Shopify_Client` (1,395 lines, `src/`) and the D8-compat `WP_MCP_AI_Tool_Product_Card` trait copy (865 lines, `src/tools/` — base-owned in `includes/tools/`, excluded from the root classmap, so the addon serves its own copy standalone with the `nvoos-content-graph-pro` text domain; documented): the Shopify Admin GraphQL API client (API version 2025-01/2025-04, 5 MB response cap, discover.shopifyapps.com catalog), the connection-resolver + smart-search trait-backed product/order/customer/inventory/catalog surfaces (the F6 remote-shopify-connection tool stays deferred — the resolver degrades through the `class_exists`-guarded remote-site-manager seam); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` refs); the init's tool filter + ecosystem registration now carry all thirty-five ported e-commerce tools; 5 characterization tests green in both matrices (serving sources ×6 + per-mode trait source, surfaces, client constants, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 9 (always-on extras batch) landed: the eight always-on e-commerce tools (`WP_MCP_AI_Pro_Tool_Get_Import_Duty` 401 lines, `Get_QuickBooks_Report` 368, `QuickBooks_Desktop_Sync` 807, `Printful` 547, `Product_Actualization` 1,761, `Lookup_Product_Price` 1,393, `Validate_Image_For_Product` 1,259, `Validate_Image_For_Vehicle` 1,331) ported byte-identical (`src/tools/ecommerce/`): the ITA tariff duty lookup, the QuickBooks Online reports + QODBC desktop sync (base-owned admin-settings/remote-site-manager seams stay classmap-served), the Printful print-on-demand flows (monolith-gated printful-client require + `wp_mcp_ai_printful_unavailable` re-check degradation), the product actualization compositor (monolith-gated message-attachments require with the `$product_id = 0` fallback + `file_exists`-guarded remove-background resolution), the Vision/Crawl4AI price lookup, and the two vision-model image validators (the D8 interface/chat-response requires are `interface_exists`/`trait_exists`-guarded — the root classmap serves the base copies in the monorepo test matrix, so an unguarded require would double-declare; openai-client/attachment-resolver/admin-settings requires stay monolith-gated); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` refs; phpcs ruleset additions — `error_log` + `file_get_contents` severity 0 for the byte-identical diagnostic/image-bytes patterns); the init's tool filter + ecosystem registration now carry all forty-three ported e-commerce tools; 6 characterization tests green in both matrices (serving sources ×8, surfaces, capabilities + capability flags, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 10 (e-commerce admin pages batch, F2 e-commerce complete) landed: the four admin pages (`WP_MCP_AI_Ecommerce_Settings_Page` 189 lines, `Product_Research_Page` 770, `Product_Consolidate_Page` 612, `Product_Settings_Page` 399) plus their shared bases and traits (`WP_MCP_AI_CPT_Settings_Page_Base` 344, `WP_MCP_AI_Consolidate_Add_Base` 828, the research-page featured-image 126 + enhancements 534 traits) ported byte-identical (`src/admin/`): the E-commerce Toolkit settings page (toolkit-settings-base child, separate `wp-mcp-ai-ecommerce-toolkit` top-level menu at admin_menu 25, `has_remote_sites`), the Research & Add page (submenu `research-product`, `edit_products`, self-boot init at file load, the AJAX create/import handlers with the byte-identical `class_exists`-guarded create-woo-product/shortcode seams), the Consolidate & Add page (submenu `product-consolidate`, singleton init, the consolidate-add-base data-quality scoring + tenant-store flows), and the Product Settings page (cpt-settings-base child, `wp_mcp_ai_product_settings`, the e-commerce toolkit settings group + the byte-identical negative-assistant-id clamp); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps incl. the consolidate JS enqueue — the product-consolidate.js asset does not ship in the base either, byte-identical upstream gap; the `includes/admin/*` + `includes/class-wp-mcp-ai-toolkit-data-store-factory.php` requires resolve from the addon's already-ported `src/` copies; the `__DIR__` trait/base requires resolve from the same `src/admin/` batch); the e-commerce init's four file-gated admin-page requires now fire (gate targets present); 6 characterization tests green in both matrices (serving sources ×8, page slugs + constructor props via reflection, init/AJAX hook wiring, registered settings groups, sanitize clamp contract, standalone gate targets + research-page self-boot), PHPCS-clean. Remaining: — (Wave F2 e-commerce complete — 43 tools + data layer + admin pages). Next: F2 PM/financial/calendar/social toolkits. Sub-cluster 1 (F2 PM toolkit data layer) landed: the PM shared engine classes (`WP_MCP_AI_PM_Engine` 695 lines, `_Codes` 246, `_Pipeline_Stages` 210, `_Capabilities` 197, `_Workflow_Engine` 243 — `src/tools/project-management/`) + the three PM CPTs (`WP_MCP_AI_Project_CPT` `mcp_ai_project`, `WP_MCP_AI_Task_CPT` `mcp_ai_task`, `WP_MCP_AI_Event_CPT` `mcp_ai_event` — self-booting at file load, byte-identical) + `WP_MCP_AI_PM_Notification_Manager` (479 — `src/`) ported byte-identical: the byte-identical `enable_project_management` enabled gate, the engine settings accessors + status/priority/portfolio-health/count/deadline/stale-task contracts, the code-pack registry, the pipeline-stage map (idea→archived, `default_stage()` `planning`), the role/capability maps, and the workflow-rule engine; documented deviations (strict types; text domain; slimmed standalone init — blueprint installer, tool files, admin menu/command-center/settings/blueprints pages, research-add, and the metabox/admin-columns/AI-actions admin slices deferred to later F2 PM sub-clusters, each file-gated; the `WP_MCP_AI_PRO_VERSION` guards swap to `NVOOS_CONTENT_GRAPH_PRO_VERSION`; new standalone-only tool wiring — `wp_mcp_ai_pro_register_pm_tools()` filter + `wp_mcp_ai_pro_register_pm_ecosystem_tools()` with empty maps that fill as the PM tool batches land); the registry gained the `toolkit_project_management` module (byte-identical `enable_project_management` gate; the monolith's `jetengine_meta_helper` dep stays a documented dormant forward-reference — the F1 registry test updated for the eighth module); the entry's spl autoloader gained the `src/tools/project-management/` probe; 5 characterization tests green in both matrices (serving sources ×9, engine/codes/pipeline/capabilities contracts, CPT registrations, standalone enabled-gate + init hook wiring + auxiliary task-plan/template CPTs, registry module), PHPCS-clean. Remaining: PM tool batches (core/sprints/analytics/reports/risk/templates/examples/command-center), blueprint installer, PM admin slice. Sub-cluster 2 (PM core CRUD + dependency tool batch) landed: the eleven core tools (`WP_MCP_AI_Tool_Create_Project` 330 lines, `Update_Project`, `Delete_Project`, `List_Projects`, `Create_Task`, `Update_Task`, `Delete_Task`, `List_Tasks`, `Add_Task_Dependency`, `Remove_Task_Dependency`, `Get_Task_Dependencies` — 2,719 lines total) ported byte-identical (`src/tools/project-management/`): the project/task CRUD flows (edit_posts gates, the create-project update path via `project_id`, the `_project_*`/`_task_*` meta stamps, the `delete_posts` execute gate on delete, the status allowlists with planning/todo fallbacks), and the dependency graph (depends-on/blocks meta pair, self-dependency + cycle rejection, the enriched get shape with `can_start`); documented deviations (strict types; text domain — no path constants, so no path swaps); the init's tool filter + ecosystem registration now carry all eleven ported PM tools; 9 characterization tests green in both matrices (serving sources ×11, surfaces, create-project execute + update path, list-projects, delete-project hard-delete + missing-id gate, create-task title gate + meta, dependency add/list/remove flow, standalone filter/registration coverage), PHPCS-clean; the data-layer init-gate test's hook assertions are now first-loader-gated (the core-tools tests require the init earlier in-process — documented). Remaining: PARA + capture-decision batch, analytics/risk, command-center/workflow, templates/sprints, reports/blueprint-import, blueprint installer, PM admin slice. Sub-cluster 3 (PM PARA + capture-decision batch) landed: the PARA subsystem (taxonomy 957-line `src/para/` slice: `WP_MCP_AI_PARA_Taxonomy` `mcp_ai_para` with the four locked roots projects/areas/resources/archives, `_Area_CPT` `mcp_ai_area`, `_Lifecycle`, `_Admin_Columns`, self-booting init) + the seven PARA tools (`para_classify_item`, `para_create_area`, `para_list_areas`, `para_move_to_archives`, `para_promote_resource_to_project`, `para_update_area`, `para_weekly_review`) + `pm_capture_decision` (MemPalace, extends the already-ported `WP_MCP_AI_Pro_Capture_Tool_Base`) ported byte-identical (`src/para/`, `src/tools/project-management/`): the opt-in `enable_para_organization` + PM-enabled gate, the area CRUD meta (`_para_standard`/`_para_owner`/`_para_review_cadence`), the auto-classify assign flow, the root-term seeding + protection; documented deviations (strict types; text domain; the capture-base require resolves from the addon's `src/tools/capture/` copy); the registry gained the `pro_para` module (no gate, byte-identical with the monolith — the F1 registry test updated for the ninth module); the entry's spl autoloader gained the `src/para/` probe; the init's tool filter + ecosystem registration now carry all nineteen ported PM tools; 11 characterization tests green in both matrices (serving sources ×12, PARA constants, surfaces ×8, capture-base extension, create-area + meta, list-areas, classify-item + assign, weekly-review smoke, standalone filter/registration + registry module), PHPCS-clean. Remaining: analytics/risk, command-center/workflow, templates/sprints, reports/blueprint-import, blueprint installer, PM admin slice. Sub-cluster 4 (PM analytics + risk tool batch) landed: the six analytics tools (`Get_Burndown_Chart`, `Get_Team_Velocity`, `Get_Portfolio_Health`, `Get_Resource_Utilization`, `Get_Project_Timeline`, `Forecast_Completion`) and the three risk tools (`Assess_Project_Risk`, `Detect_Stale_Tasks`, `Identify_Blockers`) ported byte-identical (`src/tools/project-management/analytics/` + `risk/`, 2,187 lines total): the read-only PM-enabled availability gates (available by default when PM is on), the `edit_posts` capability contracts, the project-scoped forecast/risk gates (`wp_mcp_ai_project_not_found`), and the stale-task envelope; documented deviations (strict types; text domain — no path constants, so no path swaps); the entry's spl autoloader gained the `analytics/` + `risk/` subtree probes; the init's tool filter + ecosystem registration now carry all twenty-eight ported PM tools; 6 characterization tests green in both matrices (serving sources ×9, surfaces, analytics smoke incl. project-scoped forecast, risk smoke incl. stale-task envelope, standalone filter/registration coverage), PHPCS-clean. Remaining: command-center/workflow, templates/sprints, reports/blueprint-import, blueprint installer, PM admin slice. Sub-cluster 5 (PM command-center + workflow tool batch) landed: the four command-center query tools (`Get_PM_KPIs`, `Get_Project_Pipeline`, `Get_Upcoming_Deadlines`, `Get_My_Tasks`) and the three workflow-rule tools (`Create_PM_Workflow_Rule`, `List_PM_Workflow_Rules`, `Simulate_PM_Workflow_Rule`) ported byte-identical (`src/tools/project-management/command-center/` + `workflow/`, 1,607 lines total): the read-only KPI/pipeline/deadlines/my-tasks aggregations, the workflow-rule CRUD with the byte-identical `VALID_TRIGGER_TYPES`/`VALID_ACTION_TYPES` enums, and the engine-backed simulation; ⚠ UPSTREAM LATENT BUG pinned as-is (byte-identical — follow-up issue candidate): `sanitize_key()` strips the dots from `trigger_type`, so none of the five dotted `VALID_TRIGGER_TYPES` can ever pass the create tool's validity check (`wp_mcp_ai_invalid_trigger`); documented deviations (strict types; text domain — no path constants; the PM init's monolith guard became a FULL-BODY runtime wrap (deviation 6) — the base PM init declares the same global helper functions, and the collision is a COMPILE-TIME fatal, so a top-level `return` guard cannot help; the function declarations now register only when the runtime block executes); the entry's spl autoloader gained the `command-center/` + `workflow/` subtree probes; the init's tool filter + ecosystem registration now carry all thirty-five ported PM tools; 6 characterization tests green in both matrices (serving sources ×7, surfaces, command-center smoke, workflow-rule flow incl. the pinned trigger-rejection contract + direct-insert list/simulate, standalone filter/registration coverage), PHPCS-clean. Remaining: templates/sprints, reports/blueprint-import, blueprint installer, PM admin slice. Sub-cluster 6 (PM templates + sprints tool batch) landed: the three task-template tools (`Create_Task_Template`, `List_Task_Templates`, `Instantiate_Task_Template`) and the three sprint tools (`Create_Sprint`, `Plan_Sprint`, `Close_Sprint`) ported byte-identical (`src/tools/project-management/templates/` + `sprints/`, 1,576 lines total): the markdown-backed template flow (checkbox task lists, `_template_task_count`/`_template_category` meta, the content-parsing instantiate that creates linked tasks), and the sprint lifecycle (create stamps `_sprint_status` `planning` + `_sprint_project_id`, plan auto-selects backlog tasks and only flips to `active` when tasks were actually planned, close requires `active` and stamps `completed`); documented deviations (strict types; text domain — no path constants, so no path swaps); the entry's spl autoloader gained the `templates/` + `sprints/` subtree probes; the init's tool filter + ecosystem registration now carry all forty-one ported PM tools; 6 characterization tests green in both matrices (serving sources ×6, surfaces, template create/list/instantiate flow, sprint create→plan→close lifecycle incl. the byte-identical active-gate, standalone filter/registration coverage), PHPCS-clean. Remaining: reports/blueprint-import, blueprint installer, PM admin slice. Sub-cluster 7 (PM reports + blueprint-import batch, PM tools complete) landed: the two reporting tools (`Generate_Status_Report`, `Export_Project_CSV`) and the blueprint-import tool (`WP_MCP_AI_Tool_Import_Project_Management_Blueprint` — ⚠ the base map key `WP_MCP_AI_Tool_Import_PM_Blueprint` does not match the file's actual class; the addon registers the real class, documented upstream quirk) ported byte-identical (`src/tools/project-management/reports/` + `examples/`, 1,310 lines) plus the shared `WP_MCP_AI_Blueprint_Installer` (495 lines, `src/tools/orchestration/`) and the six blueprint JSON assets copied byte-identical: the markdown/html status-report flow, the CSV export envelope, the blueprint load/list/install/label contracts (the CRM import's installer seam now RESOLVES standalone — the CRM imports test updated from the degrade-pin to the install-pin), and the mcp_ai_assistant install flow; documented deviations (strict types; text domain; `BLUEPRINTS_DIR`/installer-fallback paths resolve from `src/`); the entry's spl autoloader gained the `reports/` + `examples/` + `orchestration/` subtree probes; the PM init's deferred installer gate now fires; the init's tool filter + ecosystem registration now carry all forty-four ported PM tools (the full monolith PM tool map minus the research-toolkit `Research_Project` — that lands with its own toolkit); 7 characterization tests green in both matrices (serving sources ×4, surfaces, installer load/list/label contracts, report + CSV execute, blueprint import → assistant post, standalone filter/registration coverage), PHPCS-clean. Remaining: PM admin slice (admin menu, command-center page, toolkit settings page, blueprints page, research-add, metaboxes, project/event/task research+settings+consolidate pages). Sub-cluster 8 (PM admin slice A) landed: the core PM admin surface ported byte-identical (`src/admin/` + `src/research-add/`, 12 files): `WP_MCP_AI_PM_Admin_Menu` (145 lines, `PARENT_SLUG` `nvoos-pm-dashboard`, top-level menu at 25 + submenus at 28 with the byte-identical `self::$registered` first-loader guard), `WP_MCP_AI_PM_Command_Center_Page` (1,737, `nvoos-pm-command-center`, the five AJAX KPIs/pipeline/deadlines/activity/ingest handlers, the class_exists-guarded engine/CCT/assistant-service seams, the file-gated upwork/inbound sync requires), `WP_MCP_AI_Project_Management_Toolkit_Settings_Page` (616, toolkit-settings-base child), `WP_MCP_AI_PM_Blueprints_Page` (475, `nvoos-pm-blueprints`, installer + BLUEPRINTS_DIR seams now resolve — pm-blueprints.js asset copied), `WP_MCP_AI_Research_Add_Base` (783, new dependency for the deferred research-add slice), `WP_MCP_AI_Project_Management_Research_Add` (381), the three metaboxes (200/358/359), the admin columns (423), the AI actions (500 — admin-pm-ai-actions.js copied) and the bulk AI (399) handlers; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` swaps); the PM init's five admin-page gates + the admin_init metabox loader now fire (all gate targets present); 5 characterization tests green in both matrices (serving sources ×12, page constants, init/AJAX hook wiring with the first-loader-aware admin-menu assertion, blueprints-page seams, standalone gate targets), PHPCS-clean. Remaining: PM research/settings/consolidate pages (project/event/task — 6 files, ~3,414 lines). Sub-cluster 9 (PM admin slice B, PM complete) landed: the six research/settings/consolidate pages ported byte-identical (`src/admin/`): `WP_MCP_AI_Project_Research_Page` (746 lines, `research-project`, self-booting init at file load, the create-project seam), `WP_MCP_AI_Project_Settings_Page` (186, `wp_mcp_ai_project_settings`/`project-settings`), `WP_MCP_AI_Event_Research_Page` (746, `research-event`, the create-event seam stays class_exists-guarded — dormant until the calendar-booking wave), `WP_MCP_AI_Event_Settings_Page` (152, `wp_mcp_ai_event_settings`/`event-settings`), `WP_MCP_AI_Event_Consolidate_Page` (598, consolidate-add-base child, the event-consolidate.js enqueue stays byte-identical — upstream asset gap), `WP_MCP_AI_Task_Research_Page` (986, `research-task`, the create-task tool require resolves from `src/tools/project-management/`); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` swaps; the `__DIR__` trait/base requires resolve from the addon's `src/admin/` copies); the PM init's six remaining admin-page gates now fire (PM admin slice complete — all gate targets present); 4 characterization tests green in both matrices (serving sources ×6, page slugs/options via reflection, init/AJAX hook wiring, standalone gate targets), PHPCS-clean. Remaining: — (Wave F2 PM complete — 44 tools + data layer + PARA + admin slices). Next: F2 financial/calendar/social toolkits. Sub-cluster 1 (F2 calendar-booking data layer) landed: the three calendar CPTs (`WP_MCP_AI_Appointment_CPT` `mcp_appointment`, `_Service_CPT` `mcp_service`, `_Staff_CPT` `mcp_staff` — `src/calendar-booking/`, with their metabox loaders), the seven metabox classes (`src/metaboxes/`, 1,569 lines), the booking-adapter interface + factory (`src/adapters/`, the JetEngine/JetBooking concrete adapters stay dormant — byte-identical conditional requires), the orchestration optimizer (`src/tools/calendar-booking/`, 191 lines — business-hours autoload fix, appointment pruning, schedule cap, orphan detection), and the slimmed standalone calendar init (full-body runtime monolith guard — the global admin-style enqueue function collides compile-time with the base copy; deviation 5) + new standalone-only tool wiring (empty maps that fill as the calendar tool batches land); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` swaps; phpcs ruleset addition — `Squiz.PHP.CommentedOutCode` severity 0 for the byte-identical commented-out adapter snippet); the registry gained the `toolkit_calendar_booking` + `booking_adapters` modules (the F1 registry test updated for the eleventh module); the entry's spl autoloader gained the `adapters/` + `calendar-booking/` + `metaboxes/` + `tools/calendar-booking/` probes; 6 characterization tests green in both matrices (serving sources ×13, CPT constants, adapter-factory dormant probes, optimizer hooks, standalone init wiring + registry modules), PHPCS-clean. Remaining: calendar tool batches (event CRUD/appointment/services/slots/ICS/jetappointment/jetbooking), calendar admin slice. Sub-cluster 2 (calendar event tool batch) landed: the six event tools (`Create_Event`, `Update_Event`, `Delete_Event`, `List_Events`, `Get_Calendar_View`, `Export_Calendar_ICS`) ported byte-identical (`src/tools/calendar-booking/`, 2,003 lines): the event CRUD lifecycle (missing-title gate, `mcp_ai_event` posts), the date-gated calendar view, and the ICS export (project event/task collection, the date-range filter); documented deviations (strict types; text domain; the base-owned `WP_MCP_AI_Tool_Document_Response` trait gets a D8-style addon copy `src/tools/trait-wp-mcp-ai-tool-document-response.php` — `includes/tools/` is classmap-excluded — with the wave-proof trait_exists-guarded require; the vendor `assets/vendor/ics/index.js` asset copied byte-identical; the base-owned `\WP_MCP_AI\Services\WP_MCP_AI_Process_Service` seam stays classmap-served); the calendar init's tool filter + ecosystem registration now carry all six ported calendar tools; 6 characterization tests green in both matrices (serving sources ×6, surfaces, event CRUD lifecycle, calendar-view date gate + smoke, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 3 (calendar services + booking-ops batch) landed: the six service/booking-ops tools (`Create_Service`, `Import_Services`, `Get_No_Show_Appointments`, `Get_Unconfirmed_Bookings`, `Send_Booking_Confirmations`, `Send_Reschedule_Invitation`) ported byte-identical (`src/tools/calendar-booking/`, 2,310 lines): the service CRUD flow (name gate, `mcp_service` posts), the import-services file flow (CSV/JSON with the per-row sanitizers), the read-only no-show/unconfirmed queries (`read` capability — byte-identical variance), and the booking-confirmation/reschedule senders (booking-id gates); documented deviations (strict types; text domain; phpcs ruleset additions — the seven file-operation/`@unlink`/silenced-error/assignment-in-condition relaxations for the byte-identical import flow); the calendar init's tool filter + ecosystem registration now carry all twelve ported calendar tools; 7 characterization tests green in both matrices (serving sources, surfaces incl. the `read` variance, create-service execute, query smoke, send gates, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 4 (calendar jet-sync batch) landed: the seven JetAppointment/JetBooking tools (`Sync_From_JetAppointment`, `Sync_To_JetAppointment`, `Sync_From_JetBooking`, `Get_JetAppointment_Providers`, `Get_JetAppointment_Services`, `Get_JetBooking_Units`, `Get_JetBooking_Instances`) ported byte-identical (`src/tools/calendar-booking/`, 2,703 lines) plus the two concrete adapters (`WP_MCP_AI_JetAppointment_Adapter` 699 lines, `WP_MCP_AI_JetBooking_Adapter` 737 lines — `src/adapters/`, the data-layer PR left them dormant; they stay conditionally required behind the byte-identical `jet_engine()`/`Jet_Booking` gates): the import/export sync flows (idempotent `_jetappointment_id` meta dedupe, unsynced-appointment query, per-item error collection) and the read-only provider/service/unit/instance queries (`read` capability — byte-identical variance); documented deviations (strict types; text domain — no path constants, so no path swaps); the calendar init's tool filter + ecosystem registration now carry all nineteen ported calendar tools; 5 characterization tests green in both matrices (serving sources ×9, surfaces incl. the `read` variance, the adapter-unavailable fail-closed gates for all seven tools, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 5 (calendar appointment/slot extras batch) landed: the fifteen tree-only tools (`Block_Time_Slot`, `Cancel_Appointment`, `Check_Availability`, `Create_Appointment`, `Update_Appointment`, `Get_Appointment_Details`, `Get_Available_Slots`, `Generate_Booking_Link`, `Reschedule_Appointment`, `Send_Appointment_Reminder`, `Send_Booking_Confirmation`, `Set_Availability_Rules`, `Optimize_Schedule`, `Sync_Google_Calendar`, `Sync_Outlook_Calendar`) ported byte-identical (`src/tools/calendar-booking/`, 4,269 lines): the manage_options-gated appointment flows (missing-appointment-id/missing-id argument gates), the check-availability/available-slots read paths (missing-time/invalid-time-format/invalid-date gates), and the google/outlook sync entry points; the base-owned `WP_MCP_AI_Tool_Artifact_Helper` (`includes/services/`) and `WP_MCP_AI_Google_Calendar_Credentials`/`_Scopes` (`includes/google/`) collaborators stay classmap-served (documented); documented deviations (strict types; text domain — no path constants, so no path swaps); TREE-ONLY: these files exist in the base tree but are NOT part of the monolith's inline calendar tool map — the filter/ecosystem additions are the standalone registrations (CRM CC-extras precedent, documented in the init header deviation 6); the calendar init's tool filter + ecosystem registration now carry all thirty-four ported calendar tools; 8 characterization tests green in both matrices (serving sources ×15, surfaces, the thirteen manage_options gates, the time/date gates, the admin-path argument gates, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 6 (calendar admin slice, Wave F2 calendar-booking complete) landed: the two admin pages ported byte-identical (`src/admin/`): `WP_MCP_AI_Calendar_Booking_Research_Page` (384 lines, `research-appointment` submenu under `mcp_appointment`, the five shared research-page traits, the two AJAX actions `wp_ajax_wp_mcp_ai_create_appointment_from_research`/`wp_ajax_wp_mcp_ai_import_appointment`, the `WP_MCP_AI_Shortcode` chat-embed guard stays byte-identical) and `WP_MCP_AI_Calendar_Booking_Settings_Page` (152 lines, extends the ported `WP_MCP_AI_Toolkit_Settings_Base` — toolkit slug `calendar_booking`, option `wp_mcp_ai_calendar_booking_toolkit_settings`, page slug `wp-mcp-ai-calendar-booking-toolkit-settings`, self-boot at file load byte-identical); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_PATH` swap with the `src/` root for the toolkit-settings-base require); the slim init's file-gated admin block now fires standalone (deviation 4 updated); 4 characterization tests green in both matrices (serving sources ×2, page constants, research init hooks, standalone gate targets), PHPCS-clean. Sub-cluster 1 (F2 financial-planning data layer) landed: `WP_MCP_AI_Financial_Account_CPT` ported byte-identical (`src/class-wp-mcp-ai-financial-account-cpt.php`, 381 lines): the `mcp_ai_fin_account` POST_TYPE + `mcp_ai_account_type` taxonomy registrations, the two metaboxes, the save_post meta handler, and the file-scope settings-gated self-boot; the slim standalone init ported (`src/tools/financial-planning/init.php`): byte-identical `enable_financial_planner_toolkit` enabled gate + the base-version skip, the CPT require, the JetEngine meta-helper guard (base-owned classmap-served seam), the admin-page requires deferred file-gated (financial admin slice), the global `wp_mcp_ai_enqueue_financial_planner_toolkit_admin_styles()` enqueue function with the `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps (the CSS asset stays an upstream gap — byte-identical file_exists degradation), and the full-body runtime monolith guard (deviation 5 — compile-time collision on the global enqueue function, calendar/PM pattern); NOTE: the base tree ships this init but NOTHING loads it monolith (no base registry module — byte-identical dormancy), so the standalone registry gained a standalone-only `toolkit_financial_planning` module (files guard on the init — toolkit_data_store/vector_storage precedent; the F1 registry test updated for the twelfth module); new standalone-only tool wiring scaffolding (deviation 6 — `wp_mcp_ai_pro_register_financial_tools()` filter + `wp_mcp_ai_pro_register_financial_ecosystem_tools()`, empty lists filling as the financial tool batches land); the entry's spl autoloader gained the `src/tools/financial-planning/` probe; documented deviations (strict types; text domain; `src/` path swaps; phpcs ruleset addition — `PrefixAllGlobals.NonPrefixedVariableFound` severity 0 for the byte-identical file-scope `$settings` self-boot block); 4 characterization tests green in both matrices (serving source, CPT contracts, standalone init gate + tool wiring, standalone registry module), PHPCS-clean. Sub-cluster 2 (financial tools batch A) landed: the eighteen retirement/budget/investment/debt tools (`Retirement_Calculator`, `IRA_Roth_Comparison`, `Withdrawal_Strategy_Planner`, `Social_Security_Optimizer`, `Pension_Analyzer`, `Budget_Planner`, `Expense_Tracker`, `Net_Worth_Calculator`, `Cash_Flow_Analyzer`, `Bank_Account_Sync`, `Portfolio_Visualizer`, `Asset_Allocation_Planner`, `Investment_Return_Calculator`, `Rebalancing_Analyzer`, `Tax_Loss_Harvesting_Tracker`, `Debt_Payoff_Calculator`, `Mortgage_Calculator`, `Credit_Score_Tracker`) ported byte-identical (`src/tools/financial-planning/`) plus `WP_MCP_AI_YFinance_Service` (457 lines, `src/services/` — the file_exists-guarded require target for the market tools): the edit_posts permission gates, the argument gates (invalid-current-age/invalid-income/invalid-amount), the PhpSpreadsheet class_exists degradation (mortgage xlsx export — the Pro addon vendor autoload probe swaps to `NVOOS_CONTENT_GRAPH_PRO_PATH . 'vendor/autoload.php', absent standalone so the byte-identical degradation holds); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_PATH` swaps incl. `src/` root; phpcs ruleset addition — `CurrentTimeTimestamp.Requested` severity 0 for the byte-identical stamp call); the financial init's tool filter + ecosystem registration now carry all eighteen ported financial tools; 6 characterization tests green in both matrices (serving sources ×19, surfaces, the eighteen permission gates, the three argument gates, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 3 (financial tools batch B) landed: the sixteen goal/literacy/market/transaction tools (`Savings_Goal_Planner`, `Emergency_Fund_Calculator`, `Financial_Health_Score`, `Tax_Estimator`, `College_Savings_Calculator`, `Insurance_Needs_Analyzer`, `Financial_News_Aggregator`, `Stock_Data_Fetcher`, `Market_Sentiment_Analyzer`, `Market_Forecast_Analyzer`, `Investment_Signal_Tracker`, `Financial_Logic_Visualizer`, `Financial_Report_Generator`, `Financial_Search`, `Get_Uncategorised_Transactions` (`read` capability — byte-identical variance), `Categorise_Transactions`) ported byte-identical (`src/tools/financial-planning/`) plus the tree-only `WP_MCP_AI_Tool_Import_Financial_Planning_Blueprint` (`src/tools/financial-planning/examples/` — NOT in the monolith's inline map, standalone-only registration, CRM CC-extras precedent) with the `budget-coach.json` blueprint copied byte-identical: the edit_posts gates, the argument gates (invalid-expenses/missing-query/missing-title/invalid-report-type/missing-category/invalid-action — the savings-goal default action is `list`, pinned), the installer-delegated import flow (`blueprint_not_found` fail-closed, the class_exists-guarded `src/tools/orchestration/class-wp-mcp-ai-blueprint-installer.php` require), and the byte-identical upstream quirk (unkeyed `$arguments['blueprint']` read → PHP 8 warning, kept as-is); the entry's spl autoloader gained the `src/tools/financial-planning/examples/` probe; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_PATH` swaps incl. `src/` root); the financial init's tool filter + ecosystem registration now carry all thirty-five ported financial tools; 8 characterization tests green in both matrices (serving sources ×17, surfaces incl. the `read` variance, the sixteen permission gates, the five argument gates, import-blueprint fail-closed + per-mode blueprints dir, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 4 (financial admin slice, Wave F2 financial-planning complete) landed: the five admin files ported byte-identical: `WP_MCP_AI_CPT_Settings_Page_Base` (344 lines, `src/admin/` — the shared CPT-settings base, a dependency of the financial CPT settings page), `WP_MCP_AI_Financial_Planner_CPT_Settings_Page` (293, self-boot at file load), `WP_MCP_AI_Financial_Account_Research_Page` (842, `research-financial-account` submenu, the shared research-page traits, the two AJAX actions `wp_ajax_wp_mcp_ai_create_financial_account_from_research`/`wp_ajax_wp_mcp_ai_import_financial_account`, the `WP_MCP_AI_Shortcode` guard byte-identical), `WP_MCP_AI_Financial_Planner_Settings_Page` (354, extends the ported toolkit-settings-base — toolkit slug `financial_planner`, option `wp_mcp_ai_financial_planner_toolkit_settings`, page slug `wp-mcp-ai-financial-planner-toolkit-settings`, self-boot), and `WP_MCP_AI_Financial_Planner_Research_Add` (434, `src/research-add/` — the toolkit-settings-base research-add probe now resolves); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` swaps with the `src/` root for the base-class/yfinance-service requires); the slim init's file-gated admin block now fires standalone (deviation 4 updated); 4 characterization tests green in both matrices (serving sources ×5, page constants, research init hooks, standalone gate targets), PHPCS-clean. Sub-cluster 1 (F2 social-media data layer) landed: `WP_MCP_AI_Social_Media_Optimization` ported byte-identical (`src/tools/social-media/`, 281 lines): the `social_sched_post` CPT registration, the publish/cron hooks (`wp_mcp_ai_publish_scheduled_post`, `wp_mcp_ai_sm_daily_cleanup`), the `wp_mcp_ai_autorespond_templates` autoload fix, the MAX_TEMPLATES 50 cap, and the 30-day retention cleanup; the slim standalone init ported (`src/tools/social-media/init.php`): byte-identical `enable_social_media_toolkit` enabled gate + base-version skip, the optimization require + `::init()`, the admin settings page deferred file-gated (social admin slice), the global `wp_mcp_ai_enqueue_social_media_toolkit_admin_styles()` enqueue function with the `NVOOS_CONTENT_GRAPH_PRO_*` swaps (CSS asset upstream gap — byte-identical file_exists degradation), and the full-body runtime monolith guard (deviation 4 — compile-time collision on the global enqueue function, calendar/financial pattern); NOTE: the base tree ships this init but NOTHING loads it monolith (no base registry module — byte-identical dormancy), so the standalone registry gained a standalone-only `toolkit_social_media` module (files guard on the init — financial-planning precedent; the F1 registry test updated for the thirteenth module); new standalone-only tool wiring scaffolding (deviation 5 — `wp_mcp_ai_pro_register_social_tools()` filter + `wp_mcp_ai_pro_register_social_ecosystem_tools()`, empty lists filling as the social tool batches land); the entry's spl autoloader gained the `src/tools/social-media/` probe; documented deviations (strict types; text domain; `src/` path swaps); 4 characterization tests green in both matrices (serving source, optimizer contracts + hooks, standalone init gate + tool wiring, standalone registry module), PHPCS-clean. Sub-cluster 2 (social tools batch A) landed: the eleven always-on Pro tools (`WP_MCP_AI_Pro_Tool_Download_Google_Maps_Images`, `_Download_Facebook_Page_Images`, `_Download_Instagram_Page_Images`, `_Post_Facebook_Instagram`, `_Post_Tiktok_Video`, `_Post_Linkedin_Update`, `_Post_Google_Business_Update`, `_Get_Facebook_Instagram_Insights`, `_Get_Tiktok_Insights`, `_Get_Linkedin_Insights`, `_Get_Google_Business_Insights`) ported byte-identical (`src/tools/social-media/`): the platform download/post/insights flows with the filterable `manage_options` execute gates (`wp_mcp_ai_*_capability` filters) and the byte-identical per-platform error codes (`wp_mcp_ai_meta_insights_forbidden`, `wp_mcp_ai_tiktok_insights_forbidden`, `wp_mcp_ai_linkedin_insights_forbidden`, `wp_mcp_ai_google_business_insights_forbidden`), the missing-params/invalid-platform/missing-token argument gates, and the base-owned `WP_MCP_AI_Logger` seam (classmap-served); documented deviations (strict types; text domain \xE2\x80\x94 no path constants, so no path swaps); the social init's tool filter + ecosystem registration now carry all eleven ported social tools; 6 characterization tests green in both matrices (serving sources ×11, surfaces, the eleven fail-closed gates with their per-platform codes, the manage_options-vs-edit_posts gate split, the four argument gates, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 3 (social tools batch B) landed: the twenty-one gated tools (`Post_To_Multiple_Platforms`, `Schedule_Social_Post`, `Bulk_Schedule_Posts`, `Auto_Optimize_Images`, `Create_Social_Video`, `Monitor_Mentions_Replies`, `Auto_Respond_Messages`, `Moderate_Comments`, `Get_Cross_Platform_Analytics`, `Track_Hashtag_Performance`, `Competitor_Analysis`, `Influencer_Identification`, `Get_Social_Analytics`, `Create_Content_Calendar`, `Generate_Post_Ideas`, `Social_Listening_Trends`, `Social_Capture_Post_Performance`, `Get_Content_Calendar`, `Generate_Social_Captions`, `Schedule_Social_Posts`, `Publish_To_Social`) ported byte-identical (`src/tools/social-media/`): the edit_posts gates with the two capability variances (`Get_Content_Calendar` `read`, `Publish_To_Social` `publish_posts`), the toolkit enabled gates, the argument gates (invalid-start-date/missing-content), the capture tool\x27s no-user-gate delegation to the ported `src/tools/capture/class-wp-mcp-ai-pro-capture-tool-base.php` (`capture_missing_wing_key` fail-closed — the `WP_MCP_AI_Memory_Capture_Service` tier constant stays classmap-served), and the base-owned `WP_MCP_AI_Analytics_Service`/`WP_MCP_AI_Admin_Settings_Base` class_exists-guarded seams; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_PATH` swap for the capture-base require; phpcs ruleset addition — `ForLoopWithTestFunctionCall.NotAllowed` severity 0 for the byte-identical loop); the social init's tool filter + ecosystem registration now carry all thirty-two ported social tools; 7 characterization tests green in both matrices (serving sources ×21, surfaces incl. the two cap variances, the twenty fail-closed gates, the capture wing-key gate, the two argument gates, standalone filter/registration coverage), PHPCS-clean. Sub-cluster 4 (social admin slice, Wave F2 social-media complete) landed: `WP_MCP_AI_Social_Media_Settings_Page` (161 lines) ported byte-identical (`src/admin/`): extends the ported toolkit-settings-base (toolkit slug `social_media`, option `wp_mcp_ai_social_media_toolkit_settings`, page slug `wp-mcp-ai-social-media-toolkit-settings`), self-boot at file load byte-identical; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_PATH` swap with the `src/` root for the toolkit-settings-base require); the slim init's file-gated admin block now fires standalone (deviation 3 updated); 3 characterization tests green in both matrices (serving source, page constants, standalone gate target), PHPCS-clean. Sub-cluster 1 (F2 mcp-servers framework) landed: the three framework files ported byte-identical (`src/mcp-servers/`): `WP_MCP_AI_Pro_Toolkit_MCP_Observability_Card` (175 lines, `wp_mcp_ai_performance_section_after_components` render hook), `WP_MCP_AI_Pro_Well_Known_MCP` (188, `wp_mcp_ai_well_known_mcp` query var + rewrite/query_vars/template_redirect/canonical-redirect hooks), and `WP_MCP_AI_Scheduled_Toolkit_Server_Trait` (269, the Action Scheduler-backed sync-server contract); the slim standalone init ported (`src/mcp-servers/mcp-servers-init.php`): byte-identical wiring (init-12 registry bootstrap, the `wp_mcp_ai_register_toolkit_servers` registration action, REST/audit-log/OAuth-REST inits, the is_admin observability card, the well-known endpoint, the `admin_post_wp_mcp_ai_save_toolkit_mcp_server` config-save handler with the byte-identical nonce/capability/whitelist shape); documented deviations (strict types; text domain; the 33 server requires file-gated + the registration loop class_exists-guarded — the server classes fill as the server batches land; no monolith guard needed — no global declarations and the standalone registry boot is monolith-guarded); the standalone registry gained the `mcp_servers_framework` module (mirrors the base registry module; the F1 registry test updated for the fourteenth module); the entry's spl autoloader gained the `src/mcp-servers/servers/` probe; 4 characterization tests green in both matrices (serving sources ×3, framework surfaces, init file wiring surface + graceful guarded-loop degradation — hook assertions avoided because the WP test framework restores hooks between tests — and the standalone registry module boot), PHPCS-clean. Sub-cluster 2 (mcp-servers batch A) landed: the nineteen Phase 1+2 servers (`CRM`, `Healthcare` (slug `health` — byte-identical), `Architectural_Design`, `AI_Tool_Builder`, `Calendar_Booking`, `CRE_Debt`, `DJ_Management`, `Document_Generation`, `ECA_Management`, `Ecommerce`, `Financial_Planner`, `Image_Production`, `Law_Firm`, `Media_Toolkit`, `Multilingual`, `Project_Management`, `Regulatory_Registration`, `Social_Media`, `Video_Production`) ported byte-identical (`src/mcp-servers/servers/`): all extend the ported toolkit-server base with the byte-identical slug/name/tool-surface contracts; NOTE: the eca file/class-name mismatch (`class-wp-mcp-ai-eca-mcp-server.php` declares `WP_MCP_AI_ECA_Management_MCP_Server`) is served by the init's explicit require, not the spl autoloader (byte-identical with the monolith classmap); the slim init's file-gated requires now resolve for all nineteen (deviation 3 updated); documented deviations (strict types; text domain \xE2\x80\x94 no path constants); 4 characterization tests green in both matrices (serving sources ×19, surfaces ×19 incl. the `health` slug variance, registry roundtrip, standalone gate targets), PHPCS-clean. Sub-cluster 3 (mcp-servers batch B, Wave F2 mcp-servers complete) landed: the fourteen Phase 6+8 + DietPi servers (`Analytics`, `Architect_Agent`, `Chat_Channels`, `Cloudways`, `Comic_Creation`, `Extended_Cognition`, `Healthcare_Imaging`, `Healthcare_Wellness`, `Site_Creator`, `DietPi`, `Pro_Scheduler`, `FlowHub`, `Shopify_Sync`, `EZuite`) ported byte-identical (`src/mcp-servers/servers/`): all extend the ported toolkit-server base with the byte-identical slug/name/tool-surface contracts; the slim init's file-gated requires now resolve for all thirty-three servers (deviation 3 complete — the guarded registration loop can register the full set); documented deviations (strict types; text domain \xE2\x80\x94 no path constants); 3 characterization tests green in both matrices (serving sources ×14, surfaces ×14, standalone gate targets), PHPCS-clean. Sub-cluster 1 (F6 remote-sites data layer) landed: `WP_MCP_AI_Remote_Connection` (305 lines) + `WP_MCP_AI_Pro_Remote_Site_Manager` (4,110) ported byte-identical (`src/`): the connection wrapper defaults (url empty/verify_ssl true/timeout 30) and the GET/POST request surface, the manager's static contracts (`OPTION_NAME` `wp_mcp_ai_pro_remote_sites`, the `AUTH_TYPES`/`CREDENTIAL_FIELDS` maps, the `v2.` encryption prefix, `get_all_connections()`/`get_connection()`/`save_connection()`/`delete_connection()`/`test_connection()` with the per-type test routers); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_PATH` swaps with the `src/` root; per-mode seams — the base-owned `WP_MCP_AI_PATH` google-calendar-credentials/printful-client requires gain defined-guards and the not-yet-ported google-service-account/shopify-client requires gain file_exists guards (graceful standalone degrade until those files land; the composio require already had the byte-identical guard and now resolves against the absent standalone path)); the standalone registry gained the `remote_connection` module (mirrors the base module — the manager itself stays class_exists-served; the F1 registry test updated for the fifteenth module); 4 characterization tests green in both matrices (serving sources ×2, manager contracts, connection defaults + graceful request degradation, standalone registry module), PHPCS-clean. Sub-cluster 2 (F6 remote-sites admin slice, Wave F6 remote-sites complete) landed: `WP_MCP_AI_Pro_Remote_Sites_Admin` (16,724 lines), `WP_MCP_AI_Pro_Metabox_Remote_Connections` (188), and `WP_MCP_AI_Pro_Webhook_Status_Page` (1,289) ported byte-identical (`src/admin/`) plus the base's `assets/css/remote-sites-admin.css` (+min/map) copied: the admin UI (submenu under nvoos-pro-dashboard at admin_menu 30, 30+ AJAX test handlers, per-type OAuth/connection flows), the assistant-edit metabox (`_wp_mcp_ai_pro_remote_connections` meta key, self-booting in admin), and the webhook-status page (`nvoos-pro-webhook-status` slug, admin_menu 26, health-check/set/delete AJAX contracts); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps with the `src/` root; per-mode seams - the base-owned `WP_MCP_AI_PATH` Google OAuth/Calendar requires (5 sites: three edit-form views, one alt-syntax view, the shared require method) gain `defined( 'WP_MCP_AI_PATH' )` guards with empty-state degrade, the two OAuth handlers gain class_exists redirect guards, the four google-service-account requires gain file_exists guards + `wp_send_json_error` degrade, and the bottom mesh-peer require gains a file_exists guard - all until those files land); the standalone registry gained the `admin_remote_sites` module (mirrors the base module, context `admin`, plus the webhook-status require - the base loads it via `wp_mcp_ai_pro_load_admin_sections()`, which the standalone addon does not yet carry; the F1 registry test updated for the sixteenth module); 5 characterization tests green in both matrices (serving sources x3, class contracts incl. the webhook URL/label/color maps, admin/metabox/webhook constructor hook wiring, per-mode seam pins, standalone registry module), PHPCS-clean. Sub-cluster 3 (F6 remote-connections tools, remote-sites toolkit fully complete) landed: `WP_MCP_AI_Tool_Remote_WP_Connection` (2,664 lines) and `WP_MCP_AI_Tool_Remote_Shopify_Connection` (260) ported byte-identical (`src/tools/remote-connections/`): the read-only remote WP/WC/JetEngine/Paper-Store surface (list_connections envelope, the edit_posts gate with the telegram-mini-app read fallback, the missing/invalid/disabled/not-enabled connection WP_Error contracts, the rate limiter, the product-card-trait fields) and the shopify discovery/test surface (list_connections/test_connection, the edit_posts + guest gate, the resolver-trait-backed connection resolution); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_PATH` swaps with the `src/` root - the manager, resolver trait, and shopify client all resolve from the addon's already-ported copies); NEW standalone-only wiring (deviation, financial-planning precedent - the base tree ships no remote-connections init and registers both tools inline via `wp_mcp_ai_pro_register_tools()`): a slim `src/tools/remote-connections/init.php` with a full-body `! defined( 'WP_MCP_AI_PATH' )` guard carrying `wp_mcp_ai_pro_register_remote_connections_tools()` + `wp_mcp_ai_pro_register_remote_connections_ecosystem_tools()`; the standalone registry gained the standalone-only `remote_connections` module (the F1 registry test updated for the seventeenth module); the entry's spl autoloader gained the `src/tools/remote-connections/` subtree probe (the classes resolve standalone at class-declaration time - the D8 interface/product-card trait/resolver trait copies autoload through the existing probes); 6 characterization tests green in both matrices (tool surfaces, serving sources x2, both execute contracts incl. the capability gates, standalone filter shape + ecosystem registration), PHPCS-clean. Sub-cluster 1 (F2 video-production toolkit data layer + gated tool batch) landed: the slimmed `src/tools/video-production/init.php` (enabled gate `enable_video_production_toolkit`, file-gated admin-settings + research-add requires - both deferred, the byte-identical `wp_mcp_ai_enqueue_video_production_toolkit_admin_styles()` helper behind a full-body `! defined( 'WP_MCP_AI_PATH' )` guard - the base init declares the same global) + the seventeen gated tools ported byte-identical (`src/tools/video-production/`): the creation batch (create-video-from-images, add-watermark, generate-captions, merge-videos), the editing batch (trim-video, resize-resolution, adjust-speed), the optimization batch (compress, convert-format, optimize-for-platform), the analysis batch (extract-metadata, generate-thumbnails), and the queue/transcription batch (get-queued-videos, get-videos-without-thumbnails, get-videos-without-transcripts, upload-video-batch, transcribe-video) with the byte-identical capability variance (upload_files editing tools, read queue reads, edit_posts transcribe) and the trim-video stub execute envelope; documented deviations (strict types; text domain - no path constants in the gated batch; the D8-compat interface copies resolve via the entry's spl autoloader); NEW standalone-only wiring (deviation, financial precedent): `wp_mcp_ai_pro_register_video_production_tools()` + `wp_mcp_ai_pro_register_video_production_ecosystem_tools()` carrying all seventeen; the standalone registry gained the standalone-only `toolkit_video_production` module (the F1 registry test updated for the eighteenth module); the entry's spl autoloader gained the `src/tools/video-production/` probe; 6 characterization tests green in both matrices (trim/queue surfaces, serving sources x2, trim stub execute, gated file inventory, standalone filter shape + ecosystem registration), PHPCS-clean. Sub-cluster 2 (video-production admin slice) landed: `WP_MCP_AI_Video_Production_Settings_Page` (158 lines) + `WP_MCP_AI_Video_Production_Research_Add` (390) ported byte-identical (`src/admin/`, `src/research-add/`): the toolkit-settings-base child (toolkit_slug `video_production`, option_name `wp_mcp_ai_video_production_toolkit_settings`, page_slug `wp-mcp-ai-video-production-toolkit-settings`, has_research/has_remote_sites, self-booting in admin) and the research-add-base child (projects/scenes/assets entity types, CPT/CCT field-schema filters); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` swaps - the shared base classes resolve from the already-ported `src/admin/` copies); the video init's file-gated admin requires now fire (gate targets present); 4 characterization tests green in both matrices (serving sources x2, settings props, research-add entity types, standalone init gate targets), PHPCS-clean. Sub-cluster 3 (video-production blueprint slice) landed: `WP_MCP_AI_Tool_Import_Video_Production_Blueprint` (157 lines, `src/tools/video-production/examples/`) ported byte-identical plus the two blueprint JSONs (`production-manager.json`, `video-editor.json`) copied: the tree-only import surface (BLUEPRINTS_DIR/`BLUEPRINT_SLUGS` constants, blueprint+overwrite schema, edit_posts gate, the shared blueprint-installer require - resolves from the already-ported `src/tools/orchestration/` copy); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_PATH` swaps); the video init's tool filter + ecosystem registration now carry it (the base registers it nowhere - CRM CC-extras precedent); the entry's spl autoloader gained the `src/tools/video-production/examples/` probe; the toolkit tests extended (surface + BLUEPRINTS_DIR + filter count 18 + JSON inventory + ecosystem registration), PHPCS-clean. Sub-cluster 4 (video-production services slice) landed: the two Pro-owned services `WP_MCP_AI_Video_Frame_Extractor_Service` (525 lines) + `WP_MCP_AI_Fluent_FFmpeg_Service` (735) ported byte-identical (`src/services/`) plus five base-owned D8-compat copies (same pattern as the tool-interface/chat-response/envelope copies): `WP_MCP_AI_Tool_LLM_Sanitizer_Interface` (46, `src/interfaces/`), `WP_MCP_AI_Attachment_File_Resolver` (409), `WP_MCP_AI_NodeJS_Subprocess` (226), `WP_MCP_AI_Media_Worker_Client` (580) (`src/traits/`), `WP_MCP_AI_Media_URL_Utils` (88, `src/`), and the namespaced `WP_MCP_AI\Services\WP_MCP_AI_Process_Service` (348, `src/services/` - kept namespaced, explicit-require-served, needs symfony/process which the monorepo root vendor ships; real standalone installs should add the dependency - flagged for the release wave): the frame-extractor contracts (default/max frame counts 10/20, ffmpeg availability probe, duration degradation), the fluent-ffmpeg availability probe (bundle + node + ffmpeg), and the media-url-utils static contracts; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` swaps; the attachment-resolver's base-owned message-attachments/openai-client requires gain `defined( 'WP_MCP_AI_PATH' )` guards - graceful standalone degrade until those base files land as D8 copies); ⚠ NOTE: in the standalone TEST matrix the monorepo root classmap may serve some of these symbols from the base tree (real standalone installs have no root vendor, so the addon's copies serve there - the serving-source assertions accept either source, same as the D8-compat interface copies); 6 characterization tests green in both matrices (serving sources, process-service source, frame-extractor/fluent-ffmpeg/media-url contracts, attachment-resolver seam pins), PHPCS-clean. Sub-cluster 5 (video-production exec-service tool batch, Wave F2 video-production complete) landed: the four always-on tools `WP_MCP_AI_Tool_Transcode_Video` (527 lines), `Extract_Video_Frames` (685), `Get_Video_Metadata` (497), and `Create_Remotion_Video` (741) ported byte-identical (`src/tools/video-production/`): the fluent-ffmpeg transcode surface, the frame-extraction flow (llm-sanitizer + attachment-resolver + media-url-utils + frame-extractor + Process service), the ffprobe metadata flow (missing-video / forbidden WP_Error contracts, the execute gate on `upload_files` even though `get_required_capability()` is `edit_posts` - byte-identical), and the Remotion render surface (nodejs-subprocess trait, `requires_base_pro()` true, remotion-bundle runtime probes); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_PATH` swaps; per-file seams - the base-owned `WP_MCP_AI_PATH` interface/trait/utility requires gain exists-check seams resolving from the addon's D8-compat `src/` copies and the namespaced Process service gains an explicit-require seam; phpcs ruleset additions - `file_system_operations_mkdir`/`_rmdir` severity 0 for the byte-identical temp-frame-dir patterns); the video init's tool filter + ecosystem registration now carry all twenty-two video tools (the four exec tools are unconditional in the base `$pro_tools` map - no enable gate); the toolkit tests extended (exec-tool surfaces incl. the requires_base_pro variance, get-video-metadata execute contracts, filter count 22, ecosystem registration), PHPCS-clean. Sub-cluster 1 (F2 analytics toolkit, Wave F2 analytics complete) landed: the slimmed `src/tools/analytics/init.php` (enabled gate `enable_analytics_toolkit`, file-gated admin-settings require - deferred, the byte-identical `wp_mcp_ai_enqueue_analytics_toolkit_admin_styles()` helper behind a full-body `! defined( 'WP_MCP_AI_PATH' )` guard) + the twelve gated tools ported byte-identical (`src/tools/analytics/`): the data-collection batch (collect-custom-metrics, data-warehouse-sync, real-time-event-tracking - the two `ABSPATH . wp-admin/includes/upgrade.php` requires stay core-WP), the reporting batch (generate-executive-dashboard, cohort-analysis, funnel-analysis, attribution-modeling), the predictive batch (revenue-forecast, churn-prediction, customer-segmentation-ml), and the export batch (export-analytics-api, create-custom-report) with the byte-identical capability variance (manage_options on the funnel tool) + the tree-only import-analytics-blueprint tool (157 lines, `examples/`, its `WP_MCP_AI_PRO_PATH` refs swap to `NVOOS_CONTENT_GRAPH_PRO_PATH` with the `src/` root; the blueprint JSON copied); documented deviations (strict types; text domain - the gated batch carries no path constants; the D8-compat interface copies resolve via the entry's spl autoloader); NEW standalone-only wiring (deviation, video/financial precedent): `wp_mcp_ai_pro_register_analytics_tools()` + `wp_mcp_ai_pro_register_analytics_ecosystem_tools()` carrying all thirteen; the standalone registry gained the standalone-only `toolkit_analytics` module (the F1 registry test updated for the nineteenth module); the entry's spl autoloader gained the `src/tools/analytics/` + `examples/` probes; 5 characterization tests green in both matrices (funnel/revenue/import surfaces, serving sources, funnel execute contracts incl. the custom-steps rejection, standalone filter shape + ecosystem registration), PHPCS-clean. Sub-cluster 1 (F2 multilingual toolkit, Wave F2 multilingual complete) landed: the slimmed `src/tools/multilingual/init.php` (enabled gate `enable_multilingual_toolkit`, file-gated admin-settings require - deferred, the byte-identical `wp_mcp_ai_enqueue_multilingual_toolkit_admin_styles()` helper behind a full-body `! defined( 'WP_MCP_AI_PATH' )` guard) + the ten gated tools ported byte-identical (`src/tools/multilingual/`): the translation-management batch (auto-translate-content, translate-woocommerce-products, translation-memory-search, export-import-translations), the localization batch (detect-content-language, localize-dates-currencies, rtl-content-optimization), and the QA batch (translation-quality-check, find-untranslated-strings, multilingual-seo-audit) with the byte-identical language-detection-service requires + the `WP_MCP_AI_Language_Detection_Service` (322 lines, `src/services/` - media-worker-client trait from the video-services D8-compat slice, node_modules runtime probes swapped) + the tree-only import-multilingual-blueprint tool (its `WP_MCP_AI_PRO_PATH` refs swap to `NVOOS_CONTENT_GRAPH_PRO_PATH`; the blueprint JSON copied); documented deviations (strict types; text domain; the D8-compat interface copies resolve via the entry's spl autoloader); NEW standalone-only wiring (deviation, analytics precedent): `wp_mcp_ai_pro_register_multilingual_tools()` + `wp_mcp_ai_pro_register_multilingual_ecosystem_tools()` carrying all eleven; the standalone registry gained the standalone-only `toolkit_multilingual` module (the F1 registry test updated for the twentieth module); the entry's spl autoloader gained the `src/tools/multilingual/` + `examples/` probes; 5 characterization tests green in both matrices (detect/import surfaces, serving sources incl. the service, detect execute contracts incl. the no-text/post-not-found gates, standalone filter shape + ecosystem registration), PHPCS-clean. Sub-cluster 1 (F2 cloudways infra slice) landed: `WP_MCP_AI_Cloudways_Client` (401 lines, `src/cloudways/`), the helpers file (119 - global `wp_mcp_ai_is_cloudways_toolkit_enabled()`/`wp_mcp_ai_cloudways_has_credentials()` functions, no class), `WP_MCP_AI_Tool_Cloudways_Base` (226, `src/tools/cloudways/` - abstract, manage_options capability, envelope-trait canonical responses, class_exists-wrapped declaration), and `WP_MCP_AI_Cloudways_Settings_Page` (253, `src/admin/`, toolkit_slug `cloudways`) ported byte-identical + the slim `src/tools/cloudways/init.php` (full-body `! defined( 'WP_MCP_AI_PATH' )` guard - the helpers declare globals the base copy also declares; client/helpers/base requires class_exists-guarded; the disconnect admin-post handler stays byte-identical; the settings-page require file-gated); documented deviations (strict types; text domain incl. one upstream base-domain string in the client swapped to `nvoos-content-graph-pro`; `NVOOS_CONTENT_GRAPH_PRO_PATH` swaps); NEW standalone-only wiring (deviation): `wp_mcp_ai_pro_register_cloudways_tools()` + `wp_mcp_ai_pro_register_cloudways_ecosystem_tools()` with empty maps that fill as the cloudways tool batch lands; the standalone registry gained the standalone-only `toolkit_cloudways` module (the F1 registry test updated for the twenty-first module); the entry's spl autoloader gained the `src/cloudways/` + `src/tools/cloudways/` probes; 4 characterization tests green in both matrices (serving sources, enablement/credentials helper contracts, settings page slug, standalone init targets + filter shape), PHPCS-clean. Sub-cluster 2 (F2 cloudways tool batch, Wave F2 cloudways complete) landed: all sixty Cloudways tools ported byte-identical (`src/tools/cloudways/`, extends the shared base - phase-1 reads through phase-4 addons/DNS/Cloudflare/SSH/Git/Copilot/advanced, the byte-identical `manage_options` capability via the base and the per-tool flag merges); documented deviations (strict types; text domain - no path constants; the shared base class resolves via the entry's spl autoloader); the init's tool filter + ecosystem registration now carry all sixty (generated from the base file list, 60/60 matched); 5 new characterization tests green in both matrices (list-servers surface, serving sources, no-credentials execute degradation, standalone filter shape + ecosystem registration) + the infra test's filter-shape assertion extended to 60, PHPCS-clean. Sub-cluster 1 (F2 dj-management toolkit, Wave F2 dj-management core complete) landed: the slimmed `src/tools/dj-management/init.php` (enabled gate `enable_dj_management_toolkit`, file-gated admin-settings require - deferred, the byte-identical `wp_mcp_ai_enqueue_dj_management_toolkit_admin_styles()` helper behind a full-body `! defined( 'WP_MCP_AI_PATH' )` guard) + the twenty non-jukebox tools ported byte-identical (`src/tools/dj-management/`): the two base-registered tools (get-trending-tracks with the byte-identical `success => false` disabled-toolkit envelope + the `read` capability, update-playlist-rotation) plus the eighteen tree-only tools (equipment/client/event/playlist/contract/communication batches) + the tree-only import-dj-management-blueprint tool (its `WP_MCP_AI_PRO_PATH` refs swap; the two blueprint JSONs copied); documented deviations (strict types; text domain - the D8-compat interface copies resolve via the entry's spl autoloader; the equipment-report's phpspreadsheet autoload probe resolves from `NVOOS_CONTENT_GRAPH_PRO_PATH`); NEW standalone-only wiring (deviation, cloudways precedent): `wp_mcp_ai_pro_register_dj_management_tools()` + `wp_mcp_ai_pro_register_dj_management_ecosystem_tools()` carrying all twenty-one; the standalone registry gained the standalone-only `toolkit_dj_management` module (the F1 registry test updated for the twenty-second module); the entry's spl autoloader gained the `src/tools/dj-management/` + `examples/` probes; 5 characterization tests green in both matrices (trending/rotation/import surfaces, serving sources, trending disabled-toolkit execute envelope, standalone filter shape + ecosystem registration), PHPCS-clean. Sub-cluster 1 (F2 image-production data layer + D8 image-base slice) landed: `WP_MCP_AI_Image_Template_CPT` (440 lines, `src/`, POST_TYPE `mcp_ai_image_tpl`, init-hook registration) + the `remove-background.php` helper (235, global `wp_mcp_ai_remove_image_background()`) ported byte-identical plus three base-owned D8-compat copies: `WP_MCP_AI_Tool_Image_Base` (1,309, `src/tools/` - abstract, implements Tool/LLM-Sanitizer/Capability-Flags interfaces; its five base-owned trait requires gain exists-check seams resolving from the addon's D8-compat copies), `WP_MCP_AI_SVG_Vectorizer` (268, `src/traits/`, the `bin/vectorize-image.js` runtime ref swaps to `NVOOS_CONTENT_GRAPH_PRO_PATH` - the script is a release-wave asset), and `WP_MCP_AI_Tool_Image_Response` (234, `src/tools/`) + the slim `src/tools/image-production/init.php` (full-body `! defined( 'WP_MCP_AI_PATH' )` guard; CPT require; the byte-identical JetEngine guard; file-gated admin/research-add/harmonization requires; the byte-identical CPT init hook at priority 5; the byte-identical enqueue helper); documented deviations (strict types; text domain incl. upstream mixed base-domain strings in the CPT/helper swapped; `NVOOS_CONTENT_GRAPH_PRO_*` swaps); NEW standalone-only wiring (deviation): `wp_mcp_ai_pro_register_image_production_tools()` + `wp_mcp_ai_pro_register_image_production_ecosystem_tools()` with empty maps that fill as the image-production tool batches land; the standalone registry gained the standalone-only `toolkit_image_production` module (the F1 registry test updated for the twenty-third module); the entry's spl autoloader gained the `src/tools/image-production/` probe; 5 characterization tests green in both matrices (serving sources, CPT contracts, image-base abstract/interface contracts, remove-background helper degradation, standalone init targets + filter shape), PHPCS-clean. Sub-cluster 2 (F2 image-production top-level tool batch) landed: the twenty-two top-level tools ported byte-identical (`src/tools/image-production/`): the query batch (get-images-without-alt, get-unoptimised-images, get-unwatermarked-images - the base registers these + the two batch tools in its gated map), the image-base-backed edit batch (apply-artistic-style, colorize, enhance-quality, resize-smart, upscale-ai, remove-background, remove-image-background, optimize-for-web, compress, convert-format, image-inpainting, generate-responsive-images), the direct-implementing batch (apply-watermark-batch, optimise-images-batch, optimize-image-sharp with the nodejs-subprocess trait + sharp vendor probes, batch-process-images, generate-image-ai, generate-image-variations, text-to-image-prompt-optimizer) + the tree-only import-image-production-blueprint tool (its `WP_MCP_AI_PRO_PATH` refs swap; the blueprint JSON copied); documented deviations (strict types; text domain; per-file seams - the base-owned `WP_MCP_AI_PATH` interface/image-base/nodejs-subprocess requires gain exists-check seams resolving from the addon's D8-compat `src/` copies; phpcbf whitespace on the sharp tool's upstream equals-alignment); the init's tool filter + ecosystem registration now carry all twenty-three; the entry's spl autoloader gained the `src/tools/image-production/examples/` probe; 5 new characterization tests green in both matrices (alt/upscale/import surfaces, serving sources, get-images-without-alt smoke, standalone filter shape + ecosystem registration) + the data-layer filter-shape assertion extended to 37, PHPCS-clean. Sub-cluster 3 (F2 image-production harmonization sub-toolkit, Wave F2 image-production core complete) landed: the nineteen harmonization files ported byte-identical (`src/tools/image-production/harmonization/`): the fourteen tools (generate-scene-background, adapt-background-for-subject, outpaint, refine-subject-matte, auto-clean-white-background, harmonize-color, relight-subject, generate-shadow, generate-reflection, refine-composite-boundary, analyze-scene-lighting, suggest-placement, harmonize-image-into-background, harmonize-batch - all extending `WP_MCP_AI_Tool_Harmonization_Base`), the `WP_MCP_AI_Tool_Harmonization` trait, the compositor + lighting-analyzer shared classes, and the harmonization-init loader; documented deviations (strict types; text domain; the byte-identical `defined( 'WP_MCP_AI_PATH' )` interface/message-attachments guards degrade standalone - the D8-compat interface copy resolves via the spl autoloader; the `__DIR__` requires stay portable); STANDALONE-ONLY WIRING (deviation - the base registers these via the `wp_mcp_ai_register_tools` action, which never fires standalone): the image-production init's tool filter + ecosystem registration now carry all thirty-seven tools (the fourteen harmonization tools appended); the entry's spl autoloader gained the `src/tools/image-production/harmonization/` probe; the toolkit tests extended (harmonize-color surface + availability bool, filter count 37, ecosystem registration), PHPCS-clean. Sub-cluster 4 (F2 image-production admin slice, Wave F2 image-production complete) landed: `WP_MCP_AI_Image_Production_Settings_Page` (567 lines, `src/admin/`), `WP_MCP_AI_Image_Template_Research_Page` (728, self-booting at file load), and `WP_MCP_AI_Image_Production_Research_Add` (385, `src/research-add/`) ported byte-identical: the CPT-settings-base child (option_name `wp_mcp_ai_image_production_settings`, page_slug `image-production-settings`), the research page (admin_menu under the image-template CPT, create/import AJAX handlers, the shared research-page traits resolve from the already-ported `src/admin/` copies), and the research-add-base child; ⚠ file/class name mismatch pinned (the cpt-settings file declares the Settings_Page class - neither the spl autoloader nor the base classmap can derive the file, ECA-server precedent; the init's explicit require serves it, the tests require per matrix); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` swaps); the image-production init's file-gated admin/research-add requires now fire (gate targets present); 5 characterization tests green in both matrices (serving sources, settings props, research-page hook wiring, research-add entity types, standalone init gate targets), PHPCS-clean. Sub-cluster 2 (F2 comic-creation tool batch) landed: the twelve gated tools ported byte-identical (`src/tools/comic-creation/`): the creation batch (generate-comic-script, breakdown-comic-panels, generate-character-sheet, generate-comic-panel, create-comic-layout), the editing batch (add-speech-bubbles, colorize-comic-panel, ink-comic-panel, letter-comic-panel), and the finish batch (export-comic-cbz, upscale-comic-page, apply-comic-style) - all `edit_posts`-gated, implementing the D8-compat interface pair, with the deterministic simulated-script flow in generate-comic-script (forbidden/missing-premise gates + `mcp_ai_comic_script` post creation with the JSON content + meta stamps); plus the tree-only import-comic-creation-blueprint tool (`src/tools/comic-creation/examples/`, `requires_base_pro()` true, `enable_comic_creation_toolkit` availability gate) with the `comic-artist.json` blueprint copied byte-identical; documented deviations (strict types; text domain; the base-owned interface/Logger requires gain exists-check seams resolving from the addon's D8-compat `src/` copies - the Logger landed with the comic data layer; the import tool's BLUEPRINTS_DIR + installer require resolve from `NVOOS_CONTENT_GRAPH_PRO_PATH` with the `src/` root); the init's tool filter + ecosystem registration now carry all thirteen ported tools (the 12-entry monolith map + the tree-only import tool - CRM CC-extras precedent); the entry's spl autoloader gained the `src/tools/comic-creation/examples/` probe; the data-layer filter-shape assertion extended to 13; 5 new characterization tests green in both matrices (surfaces incl. the import `requires_base_pro` + blueprint slugs, serving sources x3, generate-comic-script execute cycle, standalone filter shape + JSON inventory, ecosystem registration), PHPCS-clean. Sub-cluster 3 (F2 comic-creation admin slice) landed: `WP_MCP_AI_Comic_Settings_Page` (371 lines) + `WP_MCP_AI_Comic_Research_Page` (728) ported byte-identical (`src/admin/`): the toolkit-settings-base child (toolkit_slug `comic_creation`, option_name `wp_mcp_ai_comic_creation_settings`, page_slug `comic-creation-settings`, `has_research` true, self-booting at file load byte-identical) and the Research & Add page (`PAGE_SLUG` `research-comic` submenu under the `mcp_ai_comic` CPT, the two AJAX actions `wp_ajax_wp_mcp_ai_create_comic_from_research`/`wp_ajax_wp_mcp_ai_import_comic` with the `wp_mcp_ai_research_comic` nonce, the shared research-page traits resolve from the already-ported `src/admin/` copies, the `WP_MCP_AI_Shortcode` chat-embed guard + the byte-identical `WP_MCP_AI_URL` enhanced-research-page asset refs stay base-owned — page-hook-gated, PM/calendar precedent); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_PATH` swap with the `src/` root for the toolkit-settings-base require); the comic init's file-gated settings/research requires now fire (gate targets present — the consolidate + research-add gates stay dormant until the next slice); 5 characterization tests green in both matrices (serving sources x2, settings props, research constants, init/AJAX hook wiring, standalone gate targets), PHPCS-clean. Sub-cluster 4 (F2 comic-creation final slice, Wave F2 comic-creation complete) landed: `WP_MCP_AI_Comic_Consolidate_Page` (1,099 lines, `src/admin/`) + `WP_MCP_AI_Comic_Research_Add` (450, `src/research-add/`) ported byte-identical: the consolidate-add-base child (`PAGE_SLUG` `consolidate-comic`, the `comics/panels/characters/scripts` entity types + `cbz/csv/json` import formats, the `WP_MCP_AI_Shortcode` chat-embed guard, the `WP_MCP_AI_PRO_URL`/`_VERSION` consolidate JS enqueue swapped to `NVOOS_CONTENT_GRAPH_PRO_*` — the comic-consolidate.js asset does not ship in the base either, byte-identical upstream gap) and the research-add-base child (constructor toolkit_slug `comic_creation`, the same four entity types, the `wp_mcp_ai_toolkit_cpt_field_schema`/`_cct_field_schema` filters with the per-entity schema switches); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_PATH` swap with the `src/` root for the research-add-base require); the comic init's file-gated consolidate/research-add requires now fire (all four comic gate targets present — the toolkit port is complete); 4 characterization tests green in both matrices (serving sources x2, consolidate contracts + entity types + import formats, research-add contracts + schema-filter passthrough, standalone gate targets), PHPCS-clean. Sub-cluster 2 (F2 dj-management jukebox slice) landed: `WP_MCP_AI_Jukebox_Service` (357 lines, `src/services/`) + the two always-on jukebox tools (`WP_MCP_AI_Tool_Generate_Jukebox_Music` 491, `WP_MCP_AI_Tool_Check_Jukebox_Status` 153, `src/tools/dj-management/` — both `edit_posts`-gated, unconditional in the base `$pro_tools` map so no enable gate) + the base-owned `WP_MCP_AI_Tool_Audio_Response` D8-compat copy (`src/tools/trait-wp-mcp-ai-tool-audio-response.php`, base domain `mcp-ai-wpoos` swapped, doc-response-trait precedent) ported byte-identical; documented deviations (strict types; text domain; per-file seams — the base-owned interface/Logger/media-url-utils requires gain exists-check seams resolving from the addon's D8-compat `src/` copies, the namespaced Process service gains the explicit-require seam, the audio-response trait require is wave-proof-guarded with the addon's D8-style `src/tools/` copy as the standalone source, and the jukebox-service require resolves from `NVOOS_CONTENT_GRAPH_PRO_PATH`); the dj init's tool filter + ecosystem registration now carry all twenty-three ported DJ tools (the two jukebox tools appended — unconditional video-exec precedent); the dj toolkit tests extended (jukebox surfaces + service constants, filter count 23, ecosystem registration), PHPCS-clean. Sub-cluster 3 (F2 dj-management admin slice, Wave F2 dj-management complete) landed: `WP_MCP_AI_DJ_Management_Settings_Page` (158 lines) ported byte-identical (`src/admin/`): extends the ported toolkit-settings-base (toolkit slug `dj_management`, option `wp_mcp_ai_dj_management_toolkit_settings`, page slug `wp-mcp-ai-dj-management-toolkit-settings`, self-boot at file load byte-identical); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_PATH` swap with the `src/` root for the toolkit-settings-base require); the slim init's file-gated admin block now fires standalone (deviation 3 resolved); 3 characterization tests green in both matrices (serving source, settings props, standalone gate target), PHPCS-clean. Sub-cluster 1 (F2 ai-tool-builder toolkit, Wave F2 ai-tool-builder complete) landed: the ten meta-tools (`Analyze_Tool_Security`, `Benchmark_Tool_Performance`, `Check_Tool_Compliance`, `Generate_Tool_Documentation`, `Generate_Tool_Logic`, `Generate_Tool_Parameters`, `Generate_Tool_Scaffold`, `Generate_Tool_Tests`, `Refactor_Tool_Code`, `Validate_Tool_Schema` — all `edit_posts`-gated, no file requires, `src/tools/ai-tool-builder/`) + the tree-only import-blueprint tool (`examples/`, `requires_base_pro()` true, `tool-developer` blueprint + JSON copied) + the slim init (`enable_ai_tool_builder_toolkit` gate, full-body `! defined( 'WP_MCP_AI_PATH' )` guard — the global enqueue helper collides compile-time; file-gated settings-page require; the byte-identical enqueue helper) + `WP_MCP_AI_AI_Tool_Builder_Settings_Page` (`src/admin/`, toolkit slug `ai_tool_builder`, self-booting at file load) ported byte-identical; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` swaps — the scaffold/tests tools write to `src/tools/` + `tests/tools/`, the compliance tool's `../../../vendor/bin/phpcs` probe, the import tool's BLUEPRINTS_DIR + installer require resolve from the already-ported `src/tools/orchestration/` copy; phpcs ruleset addition — `strip_tags` severity 0 for the byte-identical doc-tool usage); NOTE: the base registers these tools NOWHERE (tree-only "Phase 2.9 planned" files — the base registry's dynamic `toolkit_ai_tool_builder` module only boots the init monolith), so the standalone filter + ecosystem registrations are the ONLY registrations (CRM CC-extras precedent) carrying all eleven; the standalone registry gained the `toolkit_ai_tool_builder` module (files guard on the init; the F1 registry test updated for the twenty-fifth module); the entry's spl autoloader gained the `src/tools/ai-tool-builder/` + `examples/` probes; 6 characterization tests green in both matrices (surfaces incl. `requires_base_pro` + blueprint slugs, serving sources x3, validate-schema execute contracts, standalone filter shape 11 + JSON inventory, ecosystem registration, settings-page gate target), PHPCS-clean. Sub-cluster 1 (F2 architect-agent toolkit) landed: the six self-editing tools (`Manage_Files` `edit_posts`, `Execute_Shell_Command`/`Git_Inspect`/`Git_Change`/`Search_Codebase` `edit_plugins`, + the legacy `Git_Operations` loadable-but-unregistered) + the `WP_MCP_AI_Tool_Git_Helpers` trait + the slim init (`enable_architect_agent_toolkit` gate, full-body `! defined( 'WP_MCP_AI_PATH' )` guard, file-gated settings require, the byte-identical enqueue helper) + the D8-compat proc-helper copies (`src/tools/architect-agent/helpers.php` — `wp_mcp_ai_run_process`/`wp_mcp_ai_run_shell`/`wp_mcp_ai_find_binary` byte-identical from `includes/bootstrap/helpers.php`, function_exists-guarded; ⚠ upstream latent bug pinned: `wp_mcp_ai_find_binary` returns `$result['ok']` but the process envelope carries no `ok` key) + `WP_MCP_AI_Architect_Agent_Settings_Page` (`src/admin/`) ported byte-identical; documented deviations (strict types; text domain incl. upstream mixed base-domain strings swapped in four files — cloudways-client precedent; `WP_MCP_AI_PATH` workspace-root refs swap to `NVOOS_CONTENT_GRAPH_PRO_PATH`; phpcs ruleset additions — `proc_open` + `fread` severity 0 for the byte-identical proc helpers); the base registers the five live tools via the tool registry + `register_deprecated_alias( 'git_operations', 'git_inspect' )` monolith — the standalone filter + ecosystem registrations carry the five (git-operations stays unregistered, byte-identical); the standalone registry gained the `toolkit_architect_agent` module (files guard; the F1 registry test updated for the twenty-sixth module); the entry's spl autoloader gained the `src/tools/architect-agent/` probe; 6 characterization tests green in both matrices (surfaces incl. the edit_posts/edit_plugins split, serving sources x3, D8 proc-helper presence, standalone filter shape 5 + git-operations exclusion, ecosystem registration, settings-page gate target — with the per-matrix trait require mirroring the base init's explicit require), PHPCS-clean. Sub-cluster 1 (F2 architectural-design data layer) landed: the four architectural CPTs (`WP_MCP_AI_Architectural_Project_CPT` `mcp_ai_arch_proj`, `_Drawing_CPT` `mcp_ai_arch_draw`, `_Specification_CPT` `mcp_ai_arch_spec`, `_Precedent_CPT` `mcp_ai_arch_prec` — `src/`, the standard `enable_architectural_design_toolkit` + base-version init gates) + the five engine classes (`WP_MCP_AI_Architectural_Engine` 786, `_Codes` 711, `_Sustainability` 858, `_Interop` 613, `_Precedents_Engine` 238 — `src/tools/architectural-design/`, no path refs) + the slim init ported (`src/tools/architectural-design/init.php`): byte-identical CPT requires + the JetEngine meta-field guard + the four `::init()` calls, the is_admin settings/research page block file-gated (admin slice), the `admin_init` metabox loader file-gated, the byte-identical enqueue helper, and the full-body runtime monolith guard (deviation 4 — the global enqueue/admin/tool-loader helpers collide compile-time with the base copy); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps; phpcs ruleset addition — `PrefixAllGlobals.NonPrefixedFunctionFound` severity 0 for the byte-identical global `esc_xml_safe()` helper in the codes registry); new standalone-only tool wiring scaffolding (deviation 5 — `wp_mcp_ai_pro_register_architectural_design_tools()` filter + `wp_mcp_ai_pro_register_architectural_design_ecosystem_tools()`, empty lists filling as the architectural-design tool batch lands); the standalone registry gained the `toolkit_architectural_design` module (files guard on the init — the F1 registry test updated for the twenty-seventh module); the entry's spl autoloader gained the `src/tools/architectural-design/` probe; 4 characterization tests green in both matrices (serving sources x7, the four CPT constants + registrations, engine/codes contracts, standalone init gate targets + empty filter), PHPCS-clean. Sub-cluster 2 (F2 architectural-design tool batch) landed: the thirty-nine loader tools, the always-on `WP_MCP_AI_Tool_Generate_Architectural_Drawing`, and the tree-only `WP_MCP_AI_Tool_Import_Architectural_Design_Blueprint` (+ the three blueprint JSONs copied) ported byte-identical (`src/tools/architectural-design/` incl. the floor-planning/visualization/documentation/analysis-compliance/estimation-scheduling/regional-compliance/sustainability/interoperability/project-delivery/precedents subdirs) plus the base-owned `WP_MCP_AI_Tool_Video_Response` D8-compat copy (`src/tools/trait-wp-mcp-ai-tool-video-response.php`, image-response precedent); documented deviations (strict types; text domain; per-file seams — the base-owned interface/Logger/media-url-utils requires gain exists-check seams resolving from the addon's D8-compat `src/` copies, the image/video-response + nodejs-subprocess + media-worker-client traits are wave-proof-guarded, the openai/gemini client requires in the drawing tool stay monolith-gated — e-commerce precedent, and the import tool's BLUEPRINTS_DIR + installer require resolve from `NVOOS_CONTENT_GRAPH_PRO_PATH`; phpcs ruleset addition — `rename_rename` severity 0 for the byte-identical temp-output rename in the drawing tool); the init's tool filter + ecosystem registration now carry all forty-one ported tools (the 39 loader classes in the base order + the drawing tool + the import tool — CRM CC-extras precedent); the entry's spl autoloader gained the eleven `src/tools/architectural-design/*` subtree probes; the data-layer filter-shape assertion extended to 41; 5 new characterization tests green in both matrices (surfaces incl. `requires_base_pro` + availability, serving sources x3, engine-backed execute smoke, standalone filter shape 41 + JSON inventory, ecosystem registration), PHPCS-clean. Sub-cluster 3 (F2 architectural-design admin slice, Wave F2 architectural-design complete) landed: the ten admin files ported byte-identical (`src/admin/`): `WP_MCP_AI_Architectural_Design_Settings_Page` (398 lines, toolkit-settings-base child — toolkit slug `architectural_design`, self-booting at file load), the three per-CPT settings pages (cpt-settings-base children, `wp_mcp_ai_architectural_project|drawing|specification_settings`), the three metabox classes (no requires), and the three research pages (748/755/793, `__DIR__` trait requires portable, the `WP_MCP_AI_Shortcode` guards byte-identical); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_PATH` swaps with the `src/` root for the base-class requires); the architectural-design init's file-gated settings/research/metabox targets now fire (all nine gate targets present — the toolkit port is complete); 3 characterization tests green in both matrices (serving sources x4, settings props, standalone gate targets x9), PHPCS-clean. Sub-cluster 1 (F2 site-creator data layer) landed: `WP_MCP_AI_Site_Template_CPT` (315 lines, `src/` — instance-based, the hardcoded `mcp_site_template` slug, toolkit-gated registration) + the two design services (`WP_MCP_AI_Design_Extractor_Service` 652 + `WP_MCP_AI_Design_Snippet_Renderer` 689 — `src/site-creator-toolkit/`, no deps) + `WP_MCP_AI_Theme_Json_Generator` (741 — `src/helpers/`, new dir) + the base-owned `WP_MCP_AI_Upgrader_Skin` D8-compat copy (`src/class-wp-mcp-ai-upgrader-skin.php`, 58 lines — classmap-served monolith, the install tools need it at execute time) + the slim init (`src/tools/site-creator-toolkit/init.php`): the is_admin settings-page + CPT block file-gated, the byte-identical `wp_mcp_ai_init_site_creator_admin()` + enqueue helper, the full-body runtime monolith guard (deviation 4 — the global admin/enqueue/tool-loader helpers collide compile-time), and the base `WP_MCP_AI_PRO_VERSION` top-level guard dropped (deviation 3 — the standalone addon IS the Pro addon); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps; phpcbf whitespace on the extractor-service upstream equals-alignment — image-production sharp-tool precedent); new standalone-only tool wiring scaffolding (deviation 5 — `wp_mcp_ai_pro_register_site_creator_tools()` filter + `wp_mcp_ai_pro_register_site_creator_ecosystem_tools()`, empty lists filling as the site-creator tool batch lands); the standalone registry gained the `toolkit_site_creator` module (files guard on the init — the F1 registry test updated for the twenty-eighth module); the entry's spl autoloader gained the `src/tools/site-creator-toolkit/` (+ `examples/`) + `src/site-creator-toolkit/` + `src/helpers/` probes; 4 characterization tests green in both matrices (serving sources x4, site-template CPT contracts, design-service construction, standalone init gate targets + empty filter), PHPCS-clean. Sub-cluster 2 (F2 site-creator tool batch) landed: the thirty-three site-creator tools ported byte-identical (`src/tools/site-creator-toolkit/` + `examples/`): the five always-on Pro tools (`WP_MCP_AI_Pro_Tool_Site_Creator`, `Install_And_Activate_Plugin`, `Install_And_Activate_Theme`, `Update_Option`, `Elementor` — the monolith's unconditional `$pro_tools` map), the twenty-seven loader tools (research x5, page-building x5, section x6, widget x4, template x4, integration x3, plus `Research_Site_Best_Practices`), and the tree-only `WP_MCP_AI_Tool_Import_Site_Creator_Blueprint` (with the two JSON blueprints `wordpress-site-builder.json`/`remote-site-administrator.json` copied to `examples/`); documented deviations (strict types; text domain; the base-owned interface/Logger requires gain the D8-compat exists-check seams; the `WP_MCP_AI_PATH` upgrader-skin requires gain monolith-gated exists-check seams resolving from `src/class-wp-mcp-ai-upgrader-skin.php`; the unconditional design-service requires gain exists-check seams resolving from `src/site-creator-toolkit/`; the theme-json-generator requires/helper paths gain seams resolving from `src/helpers/` (the base site-creator tool's `dirname( __DIR__ )` path resolves into `includes/tools/helpers/`, which does not exist upstream — byte-identical gap documented); the `BLUEPRINTS_DIR` const + blueprint-installer require swap to `NVOOS_CONTENT_GRAPH_PRO_PATH` with the `src/` root; the mixed base-domain `mcp-ai-wpoos` strings in suggest-template-patterns swap to `nvoos-content-graph-pro` (cloudways-client precedent)); the init's tool filter + ecosystem registration now carry all thirty-three ported tools; 5 characterization tests green in both matrices (tool surfaces x4, serving sources x4, the deterministic update-option execute contract, the 33-entry standalone filter shape + JSON inventory, the ecosystem registration), PHPCS-clean; the data-layer init-gate test's empty-filter assertion now counts the thirty-three-entry map. Sub-cluster 3 (F2 site-creator admin slice, site-creator complete) landed: `WP_MCP_AI_Site_Creator_Toolkit_Settings_Page` (781 lines) ported byte-identical (`src/admin/`): the top-level `nvoos-site-creator` menu (admin_menu priority 20, `manage_options`, dashicons-admin-site-alt3, position 31) with the five submenus (overview/tools/templates/research/consolidate) and the five `wp_mcp_ai_settings`-aware render blocks (the `class_exists( 'WP_MCP_AI_Shortcode' )` guards stay byte-identical — dormant standalone forward-reference); documented deviations (strict types; text domain — no path constants, so no path swaps); the site-creator init's file-gated settings-page require now fires (gate target present); 4 characterization tests green in both matrices (serving source, constructor hook priority, five-menu registration, standalone init gate target), PHPCS-clean. Riding along: the `Test_Pro_Privacy` health-wellness export test's order-dependent assertions made order-independent (`assertEqualsCanonicalizing` item ids + group selection by item id — `get_posts()` defaults to `date DESC` and same-second posts tie arbitrarily, which failed the standalone matrix once on the #6524 post-merge run; the privacy class stays byte-identical). Remaining: — (Wave F2 site-creator complete — 33 tools + data layer + admin page). Next: F2 document-generation toolkit. Sub-cluster 1 (F2 document-generation data layer) landed: `WP_MCP_AI_Document_Template_CPT` (445 lines, `src/` — static-init, `POST_TYPE` `mcp_ai_doc_tpl` + `TAXONOMY_CATEGORY` `mcp_ai_doc_tpl_cat`, the base-version/toolkit gates in `init()`, the init-hook registration callbacks) + `WP_MCP_AI_Document_Gen_Optimization` (144 — `src/tools/document-generation/`, `AUDIT_PRUNE_HOOK` `wp_mcp_ai_dg_weekly_audit_prune`, weekly audit-log pruning schedule + QMS audit-schema autoload fix, the byte-identical direct-DB phpcs disables) + `WP_MCP_AI_HTML_Formatter` (541 — `src/tools/document-generation/`, the create_document/text_to_html/sections_to_html/create_table/create_list/page-break utilities) ported byte-identical + the slim `src/tools/document-generation/init.php`: the byte-identical CPT require + JetEngine meta helper + CPT init hook priority 5 + enqueue helper, the admin settings/research-page requires + research-add require file-gated (they land with the docgen admin slice), the optimization init inside the byte-identical enabled block, the full-body runtime monolith guard (deviation 4 — the global enqueue helper collides compile-time; note the base init has no top-level version guard, so none is dropped); new standalone-only tool wiring (deviation 5 — `wp_mcp_ai_pro_register_document_generation_tools()` filter + `wp_mcp_ai_pro_register_document_generation_ecosystem_tools()`, empty maps filling as the docgen tool batches land); the standalone registry gained the `toolkit_document_generation` module (files guard on the init — the F1 registry test updated for the twenty-ninth module); the entry's spl autoloader gained the `src/tools/document-generation/` (+ `examples/`) probes; 5 characterization tests green in both matrices (serving sources x3, CPT constants + registration callbacks, optimization hook wiring, formatter paragraph/table contracts, standalone init gate targets + empty filter), PHPCS-clean. Remaining: docgen tool batches (35 tools + import blueprint), docgen admin slice (settings page + research page + research-add). Sub-cluster 2 (F2 document-generation tool batch) landed: the thirty-four docgen tools ported byte-identical (`src/tools/document-generation/` + `examples/`): the eleven always-on email/QMS tools (`Generate_Email_Template` + the ten QMS workflow tools — the monolith's unconditional `$pro_tools` map), the twenty-one toolkit-gated tools (the core Pro_PDF/Pro_Word/Pro_Excel + Generate_PDF/Word/Excel + Extract_PDF_Text/OCR_PDF_Text/Pro_Document_OCR/HTML_To_PDF/Merge_PDFs/Add_Watermark_To_PDF/Generate_Invoice_PDF + Excel_Data_Import/Export + DocGen_Capture_Style_Memory + Get_Expired_Documents/Get_Uninvoiced_Orders/Archive_Documents/Generate_Invoice_Batch), the two tree-only OCR tools (Pro_Batch_OCR/Pro_Unlimited_OCR — not in the monolith map, standalone-only registrations), and the tree-only import-blueprint tool (JSON `document-production-specialist.json` copied); plus the three Pro-owned services ported byte-identical (`src/services/`: OCR_Service 1,899 lines, MJML_Service 313, Structured_Extraction_Service 373) and the base-owned D8-compat copies `WP_MCP_AI_Self_Hosted_OCR_Client` (653 — `src/`) + `WP_MCP_AI_Vision_Request_Timeout` trait (141 — `src/traits/`); the four Node.js workers copied byte-identical (`bin/generate-pdf|word|excel.bundle.js` + `node-services/pdf-extract-service.js` — new `bin/` + `node-services/` dirs, the base paths swap constant-only); documented deviations (strict types; text domain; the base-owned interface/Logger/trait/class requires gain exists-check seams resolving from the D8-compat `src/` copies — the self-hosted-ocr seam uses the no-autoload `! defined( 'WP_MCP_AI_PATH' ) && ! class_exists( ..., false )` form because the root classmap serves the base copy (which references `WP_MCP_AI_PATH`) in the monorepo test matrix; the monolith-gated openai/gemini/ollama client requires stay monolith-gated; the mixed base-domain `mcp-ai-wpoos` strings in pro-pdf/pro-word/pro-excel-document/self-hosted-ocr swap to `nvoos-content-graph-pro` (cloudways-client precedent); phpcs ruleset addition — `WordPress.WP.I18n.NonSingularStringLiteralText` severity 0 for the byte-identical concatenated-literal descriptions in pro-batch-ocr/pro-unlimited-ocr; phpcbf whitespace on the upstream indentation in extract-pdf-text/ocr-pdf-text/import-blueprint (image-production sharp-tool precedent)); the init's tool filter + ecosystem registration now carry all thirty-four tools; 5 characterization tests green in both matrices (tool surfaces x4, serving sources x4, the deterministic QMS-create degradation contract, the 34-entry standalone filter shape + JSON/JS inventory, the ecosystem registration), PHPCS-clean; the data-layer init-gate test's empty-filter assertion now counts the thirty-four-entry map. Remaining: docgen admin slice (settings page + research page + research-add); the QMS shared data layer (`addons/pro/includes/qms/` — 8 classes, the ten QMS tools' execute-time forward-reference, degrade through the capability gate standalone). Sub-cluster 3 (F2 document-generation QMS layer + admin slice, document-generation complete) landed: the eight QMS classes ported byte-identical (`src/qms/`): `WP_MCP_AI_QMS_Capabilities` (`CAP` `manage_qms`, the pro-only/toolkit/`enable_qms_compliance` is_enabled gate), `_Audit_Log` (immutable audit table + hash chaining), `_Taxonomy` (`mcp_ai_qms_doc_type`), `_Doc_Record_CPT` (`mcp_ai_doc_record`, the statuses map, settings-gated register), `_Workflow` (release-signature filter), `_Retention` (weekly purge), `_PARA_Bridge`, and the self-booting `qms-init.php` (`__DIR__` requires + `::init()` calls); plus the three admin files byte-identical (`src/admin/` + `src/research-add/`): the settings page (867 — `WP_MCP_AI_Document_Generation_Settings_Page` extends the already-ported cpt-settings-page-base, `page_slug` `document-generation-settings`, the `WP_MCP_AI_PRO_PATH` echo/bin-dir/vendor-path refs swap to `NVOOS_CONTENT_GRAPH_PRO_PATH`; note the class name deliberately does not match its filename upstream, so neither classmap serves it — the init's explicit require carries it), the research page (728 — the `__DIR__` research-page traits resolve from the already-ported `src/admin/` copies, `PAGE_SLUG` `research-document-template`), and the research-add (304 — extends the already-ported research-add-base); the docgen init's file-gated settings/research/research-add requires now fire (gate targets present); the standalone registry gained the `pro_qms` module (mirrors the monolith's `pro_qms` — the F1 registry test updated for the thirtieth module); the entry's spl autoloader gained the `src/qms/` probe; 6 characterization tests green in both matrices (serving sources x6, QMS constants, settings-gated doc-record CPT registration, settings-page props, research-page constants + hooks, standalone init gate targets + first-loader-gated QMS init wiring), PHPCS-clean. Remaining: — (Wave F2 document-generation complete — 34 tools + data layer + QMS layer + admin slice). Next: F2 regulatory-registration toolkit. Sub-cluster 1 (F2 regulatory-registration data layer) landed: `WP_MCP_AI_Regulatory_Registration_CPT` (477 lines, `src/` — static-init self-booting, the five post-type constants `mcp_ai_reg_product`/`mcp_ai_registration`/`mcp_ai_reg_document`/`mcp_ai_reg_country`/`mcp_ai_requirement`, the base-version/toolkit gates with the disabled-notice wiring, the init-hook registration callbacks) + `WP_MCP_AI_Migrate_Requirement_Post_Type` (233 — `src/migrations/`, the old-to-new requirement-post-type migration with get_status/run) ported byte-identical + the slim `src/tools/regulatory-registration/init.php`: the byte-identical migration require + admin_init closure, the self-booting CPT require, the nine admin-page requires file-gated (they land with the regulatory-registration admin slice), the byte-identical enqueue helper + the four default-category/status/doc-type/country seeding helpers, the full-body runtime monolith guard (deviation 4 — the five global helpers collide compile-time), and the byte-identical `WP_MCP_AI_PRO_VERSION` is_pro_active check kept as-is (defined-const check, standalone-safe); new standalone-only tool wiring (deviation 5 — `wp_mcp_ai_pro_register_regulatory_registration_tools()` filter + `wp_mcp_ai_pro_register_regulatory_registration_ecosystem_tools()`, empty maps filling as the regulatory-registration tool batches land); the standalone registry gained the `toolkit_regulatory_registration` module (files guard on the init — the F1 registry test updated for the thirty-first module); the entry's spl autoloader gained the `src/tools/regulatory-registration/` (+ `examples/`) + `src/migrations/` probes; 5 characterization tests green in both matrices (serving sources x2, the five post-type constants, settings-gated init + five-CPT registration, migration status shape, standalone init gate targets + empty filter), PHPCS-clean. Remaining: regulatory-registration tool batches (60 tools + import blueprint), regulatory-registration admin slice (9 pages). Sub-cluster 1 (F2 comic-creation data layer) landed: the base-owned `WP_MCP_AI_Logger` D8-compat copy (2,573 lines, `src/class-wp-mcp-ai-logger.php` - ported from `includes/`, self-contained, same ownership boundary as the D8 tool-infra copies) + the four comic CPTs ported byte-identical (`src/`): `WP_MCP_AI_Comic_CPT` `mcp_ai_comic` (+ `mcp_ai_comic_style` taxonomy, the settings-gated full-functionality init, the config/research metaboxes, the panel/script admin columns + row actions), `WP_MCP_AI_Comic_Panel_CPT` `mcp_ai_comic_panel`, `WP_MCP_AI_Comic_Character_CPT` `mcp_ai_comic_char`, `WP_MCP_AI_Comic_Script_CPT` `mcp_ai_comic_script` + the slim `src/tools/comic-creation/init.php` (full-body `! defined( 'WP_MCP_AI_PATH' )` guard - the global `wp_mcp_ai_enqueue_comic_creation_toolkit_admin_styles()` helper collides compile-time with the base copy; the four CPT requires; the settings/research/research-add/consolidate admin requires file-gated - the pages land with the later comic-creation slices; the byte-identical CPT init hook at priority 5; the byte-identical enqueue helper); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps); NEW standalone-only wiring (deviation, image-production precedent): `wp_mcp_ai_pro_register_comic_creation_tools()` + `wp_mcp_ai_pro_register_comic_creation_ecosystem_tools()` with empty maps that fill as the comic tool batch lands (the twelve tools need the Logger - now ported); the standalone registry gained the standalone-only `toolkit_comic_creation` module (the F1 registry test updated for the twenty-fourth module); the entry's spl autoloader gained the `src/tools/comic-creation/` probe; 4 characterization tests green in both matrices (serving sources x5, logger constants, the four CPT registrations, standalone init targets + filter shape), PHPCS-clean. Sub-cluster 2 (F2 regulatory-registration tool batch) landed: the sixty ported tools (the fifty-nine `enable_regulatory_registration_toolkit`-gated tools + the tree-only import-blueprint tool) ported byte-identical (`src/tools/regulatory-registration/` + `examples/`): the product/registration/document/country/requirement CRUD flows, the workflow-rule engine surface, the authority submit/sync flows (MOHAP + NMRA), the document version tracking + upload + validation, the expiry monitoring + alerts, the compliance certificate/report/pipeline/country-performance/cost-analysis/expiry-forecast/PDF-dossier/submission-pack generators, the Excel import/export pair with the `validate-excel-import` preflight, the INCI ingredient validator, and the two example blueprints (product-registration-specialist.json + regulatory-affairs-manager.json); the base-owned `WP_MCP_AI_Tool_Restrict_From_Chat_Client` trait ported as a D8-compat copy (`src/tools/trait-wp-mcp-ai-tool-restrict-from-chat-client.php` — base-owned in `includes/tools/`, classmap-excluded, self-contained, served standalone by the entry's `trait-` prefix probe); documented deviations (strict types; text domain `nvoos-content-graph-pro` (source domain `mcp-ai-wpoos` for the trait); `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps; the base-owned interface/Logger requires gain `interface_exists`/`class_exists` seams resolving from the addon's D8-compat `src/` copies); the init's tool filter + ecosystem registration now carry all sixty ported tools (the import-blueprint tool is standalone-only — the monolith map carries fifty-nine); 5 characterization tests green in both matrices (surfaces incl. the `requires_base_pro()` blueprint contract, serving sources x3, the graceful `wp_mcp_ai_missing_param` execute degrade, standalone filter shape (60) + blueprint JSON file targets, ecosystem registration into the graph + nvoos/core registries), PHPCS-clean (2 byte-identical `array_keys` strict-comparison warnings carry from source). Sub-cluster 3 (F2 regulatory-registration admin slice — toolkit complete) landed: the nine admin pages ported byte-identical (`src/admin/`): `WP_MCP_AI_Regulatory_Product_Settings_Page` (cpt-settings-base child, post_type `mcp_ai_reg_product`, page_slug `regulatory-product-settings`, option_name `wp_mcp_ai_reg_product_settings`, the `wp_mcp_ai_regulatory_product_settings`→`wp_mcp_ai_reg_product_settings` option migration), `WP_MCP_AI_Reg_Product_Research_Page` (PAGE_SLUG `wp-mcp-ai-reg-product-research`, self-boot init at file load, the six research AJAX actions, the consolidate CSS/JS enqueues), `WP_MCP_AI_Registration_Settings_Page` (option_name `wp_mcp_ai_registration_settings`, page_slug `registration-settings`), `WP_MCP_AI_Registration_Dashboard_Page` (admin_menu 10, submenu `wp-mcp-ai-registration-dashboard`), `WP_MCP_AI_Registration_Research_Page` (PAGE_SLUG `wp-mcp-ai-registration-research`), `WP_MCP_AI_Reg_Document_Page` (admin_menu 23, `wp-mcp-ai-reg-documents`), `WP_MCP_AI_Reg_Document_Research_Page` (PAGE_SLUG `wp-mcp-ai-reg-document-research`), `WP_MCP_AI_Reg_Country_Config_Page` (admin_menu 24, `manage_options`, `wp-mcp-ai-reg-country-config`), and `WP_MCP_AI_Reg_Migration_Page` (admin_menu 25, `wp-mcp-ai-reg-migration`, the Excel import nonce flow); the two consolidate assets (`reg-product-consolidate.css/.js`) copied byte-identically into the addon's assets tree; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps; the `__DIR__` trait requires + cpt-settings-base require resolve from the already-ported `src/admin/` copies; the base-owned `WP_MCP_AI_URL`/`WP_MCP_AI_VERSION` enqueue refs stay byte-identical; phpcs ruleset addition — `WordPress.DB.PreparedSQLPlaceholders.UnfinishedPrepare` severity 0 for the byte-identical IN($placeholders) query in the country-config page); the init's nine file-gated admin-page requires now fire (gate targets present); 6 characterization tests green in both matrices (serving sources x9, research slugs + settings constructor props, init hook wiring incl. the 10/23/24/25 admin_menu priorities + research AJAX hooks, registered settings groups, the inherited assistant-id clamp contract, standalone gate targets + research-page self-boot), PHPCS-clean. Remaining: — (regulatory-registration toolkit complete: data layer + 60 tools + 9-page admin slice). Next: F4 healthcare, law-firm, and cre-debt toolkits. Sub-cluster 1 (F4 healthcare data layer) landed: the seven shared engine classes (`WP_MCP_AI_Healthcare_Engine` 527 lines, `_Codes` 240, `_FHIR` 476, `_Audit` 183, `_Capabilities` 125, `_Optimization` 283, `WP_MCP_AI_OpenMed_Client` 340 — `src/tools/healthcare/`) + the vitals data classes (`WP_MCP_AI_Healthcare_Vaccination_Schedules` 403, `_Vital_Log_CPT` 125 — `src/tools/healthcare/vitals/`) + the eight `includes/` data classes (`WP_MCP_AI_Health_Wellness_CPT` 600 — self-booting at file load, the six post types `mcp_ai_member`/`mcp_ai_policy`/`mcp_ai_med_record`/`mcp_ai_checkup`/`mcp_ai_prescription`/`mcp_ai_allergy` settings-gated; `_Meta_Boxes` 1,549; `WP_MCP_AI_Imaging_Capabilities` 90; `_Audit_Log` 142; `WP_MCP_AI_DICOM_Metadata` 491; `WP_MCP_AI_Imaging_Study_CPT` 315 — `mcp_ai_imaging_study`; `WP_MCP_AI_Imaging_REST_Controller` 1,222; `WP_MCP_AI_JetEngine_Vitals_Log_CCT` 2,010) + the `healthcare-imaging-toolkit-init.php` BC shim + the medical-record migration (`WP_MCP_AI_Migrate_Medical_Record_Post_Type`, `mcp_ai_medical_record`→`mcp_ai_med_record`) ported byte-identical; the three slim standalone inits (`src/tools/healthcare/init.php` — the byte-identical eager engine loads + PHI gate + unconditional wellness sub-init + imaging-init file-gated on `enable_healthcare_imaging` + vitals file-gated on the BC `enable_medical_vitals` default + optimization init + the OpenMed tool requires/registration file-gated until the tool batch lands; `src/tools/healthcare/imaging-init.php` — the capabilities/audit/DICOM/study-CPT/REST boot sequence + the imaging admin page file-gated; `src/tools/healthcare/wellness/init.php` — full-body `! defined( 'WP_MCP_AI_PATH' )` guard (the three global helpers collide compile-time with the base copy), the PHI multisite gate, the migration admin-init hook, the wellness CPT/meta-boxes/JetEngine-vitals-CCT requires, the seven admin-page requires file-gated, the byte-identical enqueue + member-id + read-audit helpers); documented deviations (strict types; text domain incl. the openmed-client's two source `nvoos-embedded` strings normalized; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps; `$nvoos_content_graph_pro_*` local-var prefixing; the `defined( 'WP_MCP_AI_PRO_VERSION' )` base-mode gates stay byte-identical); NEW standalone-only wiring (deviation): `wp_mcp_ai_pro_register_healthcare_tools()` filter + `wp_mcp_ai_pro_register_healthcare_ecosystem_tools()` with empty maps that fill as the healthcare tool batches land; the registry gained the standalone-only `toolkit_healthcare` module (the monolith's `jetengine_meta_helper` dep stays a documented dormant forward-reference — the F1 registry test updated for the thirty-second module); the entry's spl autoloader gained the `src/tools/healthcare/` (+ wellness/vitals/imaging/interop/examples) probes; phpcs ruleset additions — the imaging custom capabilities allowlisted + the vault-style `custom_capabilities` list; 10 characterization tests green in both matrices (serving sources x18, engine constants/conversions/BMI, wellness CPT constants + settings-gated registration, imaging CPT/capabilities/registration, vital-log CPT, FHIR builders, code-pack registry, migration status, standalone init targets + empty filter + global helpers), PHPCS-clean. Remaining: healthcare tool batches (wellness CRUD + breadth, vitals, imaging + DICOMweb, interop + FHIR export, OpenMed pair + blueprint), healthcare admin slice (8 pages), then law-firm + cre-debt toolkits. Sub-cluster 2 (F4 healthcare wellness CRUD batch 1) landed: the eleven member/policy tools ported byte-identical (`src/tools/healthcare/wellness/members/` + `policies/`): the five member CRUD flows (`create_member` incl. the person/pet taxonomy term + `_member_*` meta stamps + the `wp_mcp_ai_missing_name` gate, `list/get/update/delete_member`) and the six policy tools (`create_policy` with the content-media trait, `list/get/update/delete_policy`, `search_policies` with the relevance-search trait); the base-owned `WP_MCP_AI_Tool_Content_Media` trait ported as a D8-compat copy (`src/tools/trait-wp-mcp-ai-tool-content-media.php` — base-owned in `includes/tools/`, classmap-excluded, self-contained, served standalone by the entry's `trait-` prefix probe); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps; the create-policy trait require gains a `trait_exists`-gated seam resolving from the addon's D8-compat `src/tools/` copy; the search-policies relevance-search require resolves from the already-ported `src/traits/` copy; the create-policy source's two unreachable-code phpcs warnings carry byte-identical); the init's tool filter + ecosystem registration now carry all eleven ported tools; the entry's spl autoloader gained the wellness sub-subtree probes (allergies/checkups/medical-records/members/policies/prescriptions/reminders-research); 5 characterization tests green in both matrices (serving sources x11 + trait, surfaces x11, the create-member execute round trip, the content-media trait contract, standalone filter shape (11) + ecosystem registration), PHPCS-clean. Remaining: healthcare tool batches (wellness CRUD batches 2-3 + breadth, vitals, imaging + DICOMweb, interop + FHIR export, OpenMed pair + blueprint), healthcare admin slice (8 pages), then law-firm + cre-debt toolkits. Sub-cluster 3 (F4 healthcare wellness CRUD batch 2) landed: the twelve prescription/medical-record tools ported byte-identical (`src/tools/healthcare/wellness/prescriptions/` + `medical-records/`): the six prescription tools (`create_prescription` with the member/drug/dosage meta + the `wp_mcp_ai_missing_member` gate, `list/get/update/delete_prescription`, `search_prescriptions` with the relevance-search trait) and the six medical-record tools (`create_medical_record` with the record-type taxonomy + `_med_record_*` meta + the `wp_mcp_ai_missing_member` first gate, `list/get/update/delete_medical_record`, `search_medical_records`); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps — the search tools' relevance-search trait requires resolve from the already-ported `src/traits/` copy); the init's tool filter + ecosystem registration now carry all twenty-three wellness CRUD tools; 3 characterization tests green in both matrices (serving sources x12, surfaces x12, the create-medical-record missing-member gate; monolith deterministic per-matrix requires in setUp), PHPCS-clean. Remaining: healthcare tool batches (wellness CRUD batch 3 (checkups + allergies) + breadth, vitals, imaging + DICOMweb, interop + FHIR export, OpenMed pair + blueprint), healthcare admin slice (8 pages), then law-firm + cre-debt toolkits. Sub-cluster 4 (F4 healthcare wellness CRUD batch 3) landed: the eleven checkup/allergy tools ported byte-identical (`src/tools/healthcare/wellness/checkups/` + `allergies/`): the six checkup tools (`create_checkup` with the `wp_mcp_ai_missing_member`/`_missing_title`/`_missing_datetime` gates, `list/get/update/delete_checkup`, `get_upcoming_checkups`) and the five allergy tools (`create_allergy` with the `wp_mcp_ai_missing_member`/`_missing_allergen`/`_missing_severity` gates, `list/get/update/delete_allergy`); documented deviations (strict types; text domain — no path constants, so no path swaps); the init's tool filter + ecosystem registration now carry all thirty-four wellness CRUD tools; 3 characterization tests green in both matrices (serving sources x11, surfaces x11, the create-checkup/create-allergy first gates; monolith deterministic per-matrix requires in setUp), PHPCS-clean. Remaining: healthcare tool batches (wellness breadth, vitals, imaging + DICOMweb, interop + FHIR export, OpenMed pair + blueprint), healthcare admin slice (8 pages), then law-firm + cre-debt toolkits. Sub-cluster 5 (F4 healthcare wellness breadth batch) landed: the seventeen breadth tools ported byte-identical (`src/tools/healthcare/wellness/` + `reminders-research/` + the healthcare root): the nine wellness-root tools (`check_member_allergies`, `generate_visit_summary`, `get_health_timeline`, `get_recent_health_appointments` (`read` gate), `link_prescription_to_record`, `manage_care_plan`, `merge_duplicate_members`, `send_appointment_followup`, `verify_prescription_interactions`), the seven reminders-research tools (`get_member_health_summary`, `get_medication_schedule`, `generate_health_chart` (`read_private_posts` gate + the media-worker-client trait via the entry's `src/traits/` probe + the chart.js vendor path swap), `create_health_reminder`, `compile_health_research_data`, `guide_health_record_creation`, `parse_health_information`), and `health_capture_encounter` (extends the already-ported capture-tool base — `src/tools/capture/` path swap); the `research_policy` tool stays deferred to the F6 research cluster (cross-toolkit map reference); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps); the init's tool filter + ecosystem registration now carry all fifty-one wellness tools; 3 characterization tests green in both matrices (serving sources x17, surfaces x17 incl. the non-edit_posts gates, the capture-base inheritance; monolith deterministic per-matrix requires in setUp), PHPCS-clean. Remaining: healthcare tool batches (vitals, imaging + DICOMweb, interop + FHIR export, OpenMed pair + blueprint), healthcare admin slice (8 pages), then law-firm + cre-debt toolkits. Sub-cluster 6 (F4 healthcare vitals batch) landed: the eight vitals tools ported byte-identical (`src/tools/healthcare/vitals/`): `flag_abnormal_vitals`, `analyze_vital_trends`, `compute_bmi_and_growth_percentile` (the engine-backed BMI/pediatric-band contract with the `wp_mcp_ai_invalid_input` gate), `get_vaccination_schedule` (the vaccination-schedules data class), `import_vitals`, `log_health_metrics`, `log_vital_signs`, and `track_vaccinations`; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps where the source references the Pro addon path); the init's tool filter + ecosystem registration now carry all fifty-nine wellness + vitals tools; 3 characterization tests green in both matrices (serving sources x8, surfaces x8, the deterministic BMI contract; monolith deterministic per-matrix requires in setUp), PHPCS-clean. Remaining: healthcare tool batches (imaging + DICOMweb, interop + FHIR export, OpenMed pair + blueprint), healthcare admin slice (8 pages), then law-firm + cre-debt toolkits. Sub-cluster 7 (F4 healthcare imaging + DICOMweb batch) landed: the eight imaging tools + the DICOMweb client helper ported byte-identical (`src/tools/healthcare/imaging/`): `manage_imaging_studies`, `interpret_imaging_study`, `connect_dicomweb` (QIDO-RS/WADO-RS/STOW-RS + bearer-token auth, static `WP_MCP_AI_DICOMweb_Client` calls), `import_dicom_study`, `export_dicom_study`, `attach_radiology_report`, `compare_imaging_studies`, and `get_imaging_hanging_protocol` + `WP_MCP_AI_DICOMweb_Client` (the connection-option/save/get contract); the slim imaging-init now requires the DICOMweb client eagerly under the imaging gate (mirrors the monolith's eager map require — documented deviation); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps); the init's tool filter + ecosystem registration now carry all sixty-seven wellness + vitals + imaging tools; 3 characterization tests green in both matrices (serving sources x9, surfaces x8, the DICOMweb client contract; monolith deterministic per-matrix requires in setUp), PHPCS-clean. Remaining: healthcare tool batches (interop + FHIR export, OpenMed pair + blueprint), healthcare admin slice (8 pages), then law-firm + cre-debt toolkits. Sub-cluster 8 (F4 healthcare interop + OpenMed + blueprint batch — tool port complete) landed: the five interop tools (`import_fhir_bundle`, `export_ccda_document`, `import_hl7v2_message`, `connect_to_ehr`, `export_fhir_data`), the healthcare blueprint importer + the four JSON blueprints (`general-clinic`, `personal-health-tracker`, `radiology-review`, `veterinary-practice` — the blueprint installer resolves from the already-ported `src/tools/orchestration/` copy), and the OpenMed pair (`deidentify_health_record`, `extract_clinical_entities`) ported byte-identical; the base-owned `WP_MCP_AI_Tool_Default_Capability` trait ported as a D8-compat copy (`src/tools/` — serves real standalone installs via the addon interface's eager require); the two OpenMed tools carry a matrix-aware standalone seam (documented deviation): the base interface eagerly requires the base trait copy, and the monorepo test matrices serve the base interface via the root classmap even standalone — the seam loads the base copy in the test matrices so the class compiles against the same symbol (PHP resolves class traits before the implements clause; the two copies would otherwise double-declare); the slim init's file-gated OpenMed tool requires now fire (the `wp_mcp_ai_register_tools` registration is byte-identical); documented deviations (strict types; text domain incl. the two tools' source `nvoos-embedded` strings normalized (the openmed-client precedent); `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps); the init's tool filter + ecosystem registration now carry all seventy-five healthcare tools (the full wellness/vitals/imaging/interop/OpenMed map + blueprint); 3 characterization tests green in both matrices (serving sources x8, surfaces x8, the blueprint directory + JSON contracts; monolith deterministic per-matrix requires in setUp), PHPCS-clean. Remaining: healthcare admin slice (8 pages), then law-firm + cre-debt toolkits. Sub-cluster 9 (F4 healthcare admin slice — healthcare complete) landed: the eight admin pages ported byte-identical (`src/admin/`): `WP_MCP_AI_Imaging_Admin_Page` (PAGE_SLUG `healthcare-imaging-viewer`, admin_menu 30, the cornerstone vendor dir + viewer CSS/JS enqueues with the `defined( 'WP_MCP_AI_PRO_VERSION' )` gate swapped to the always-defined addon constant), `WP_MCP_AI_Member_Settings_Page` (cpt-settings-base child, `wp_mcp_ai_member_settings`/`mcp_ai_member`/`member-settings`), `WP_MCP_AI_Member_Research_Page` (research-add-base child), `WP_MCP_AI_Policy_Research_Page` (PAGE_SLUG `research-policy` + the `__DIR__` research traits), `WP_MCP_AI_Policy_Settings_Page` (`wp_mcp_ai_policy_settings`/`mcp_ai_policy`/`policy-settings`), `WP_MCP_AI_Health_Records_Consolidate_Page` (the consolidate CSS/JS enqueues), `WP_MCP_AI_Health_Wellness_Dashboard_Page` (PAGE_SLUG `health-wellness-dashboard`, admin_menu 24 + the 9999 landing-page redirect), and `WP_MCP_AI_Medical_Vitals_Dashboard_Page` (PAGE_SLUG `medical-vitals-dashboard`, admin_menu 25); the four page assets (`imaging-viewer.js/.css`, `health-consolidate.css/.js`) copied byte-identically into the addon's assets tree; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps incl. the imaging version gate; the base-owned `__DIR__` trait requires + cpt-settings-base/research-add-base requires resolve from the already-ported `src/admin/` copies); the wellness init's seven file-gated admin-page requires + the imaging-init's admin-page gate now fire (gate targets present); 4 characterization tests green in both matrices (serving sources x8, page slugs + settings constructor props, the 24/25/30 admin_menu priorities, standalone gate targets; monolith deterministic per-matrix requires in setUp), PHPCS-clean. Remaining: — (Wave F4 healthcare complete: data layer + 75 tools + 8-page admin slice). Next: F4 law-firm toolkit, then cre-debt. Sub-cluster 1 (F4 law-firm data layer) landed: `WP_MCP_AI_Law_Firm_CPT` (1,114 lines, `src/` — the five post types `mcp_ai_lf_matter`/`_client`/`_document`/`_time_entry`/`_trust_txn` + the meta-box/save-meta hook wiring), `WP_MCP_AI_Law_Firm_Access` (68 — the matter/client permission contract) and `WP_MCP_AI_Law_Firm_Calculator` (486 — the business-days/filing-deadline/statute-of-limitations/fee/lodestar/present-value math) ported byte-identical (`src/tools/law-firm/`) + the `admin-law-firm-toolkit.css` asset copied byte-identically; the slim standalone init (`src/tools/law-firm/init.php` — full-body `! defined( 'WP_MCP_AI_PATH' )` guard (the global enqueue helper collides compile-time with the base copy), the byte-identical `enable_law_firm_toolkit` + `! $is_base` gate, the CPT require + init, the three admin-page requires file-gated (settings/research/dashboard — the pages land with the law-firm admin slice), the byte-identical enqueue helper, and NEW standalone-only wiring — `wp_mcp_ai_pro_register_law_firm_tools()` filter + ecosystem registration with empty maps that fill as the law-firm tool batches land); the registry gained the standalone-only `toolkit_law_firm` module (the F1 registry test updated for the thirty-third module); the entry's spl autoloader gained the `src/tools/law-firm/` (+ billing-trust/compliance-ethics/document-automation/examples/intake-management/litigation-support/matter-management/research-analytics) probes; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps; `$nvoos_content_graph_pro_*` local-var prefixing in the slim init); 6 characterization tests green in both matrices (serving sources x3, the five post-type constants, CPT init + registration, the calculator contracts, the access permission contract, standalone init targets + empty filter + global helper), PHPCS-clean. Remaining: law-firm tool batches (62 tools across matter-management/billing-trust/intake-management/litigation-support/compliance-ethics/document-automation/research-analytics + blueprint), law-firm admin slice (3 pages), then cre-debt toolkit. Sub-cluster 9 (F4 law-firm admin slice — law-firm toolkit complete) landed: the three admin pages ported byte-identical (`src/admin/`): `WP_MCP_AI_Law_Firm_Settings_Page` (extends the ported toolkit-settings-base — toolkit slug `law_firm`, option `wp_mcp_ai_law_firm_settings`, page slug `law-firm-settings`, `has_research` true; ⚠ dormant-require base quirk preserved — neither the base nor the standalone init instantiates the page, byte-identical), `WP_MCP_AI_Law_Firm_Research_Page` (PAGE_SLUG `research-law-firm` submenu under the `mcp_ai_lf_matter` CPT, the two AJAX actions `wp_ajax_wp_mcp_ai_create_lf_matter_from_research`/`wp_ajax_wp_mcp_ai_create_lf_client_from_research`, the `__DIR__` research-page trait requires resolve from the already-ported `src/admin/` copies, the `WP_MCP_AI_Shortcode` chat-embed guard byte-identical), and `WP_MCP_AI_Law_Firm_Dashboard_Page` (PAGE_SLUG `law-firm-dashboard`, admin_menu 24, the base-owned `WP_MCP_AI_URL` chart.js ref stays byte-identical — page-hook-gated); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` swaps); the slim init's three file-gated admin-page requires now fire (gate targets present — the in-admin wiring is asserted through the pages' own `::init()` calls because the standalone matrix boots with `is_admin()` false, PM/calendar precedent); 5 characterization tests green in both matrices (serving sources x3, settings props, research/dashboard constants + hook wiring, standalone gate targets), PHPCS-clean. Remaining: — (Wave F4 law-firm complete: data layer + 63 tools + 3-page admin slice). Next: cre-debt toolkit. Sub-cluster 1 (F4 cre-debt data layer) landed: `WP_MCP_AI_CRE_Debt_CPT` (688 lines, `src/` — the two post types `mcp_ai_cre_loan`/`mcp_ai_cre_property` + the save-meta hook wiring), `WP_MCP_AI_CRE_Debt_Calculator` (681 — `src/tools/cre-debt/`, the monthly-payment/IO-payment/amortization-schedule/NOI/DSCR/LTV/debt-yield/direct-cap/NPV/IRR/size-loan math) ported byte-identical + the `admin-cre-debt-toolkit.css` asset copied byte-identically; the slim standalone init (`src/tools/cre-debt/init.php` — full-body `! defined( 'WP_MCP_AI_PATH' )` guard (the global enqueue helper collides compile-time), the byte-identical `enable_cre_debt_toolkit` + `! $is_base` gate, the CPT require + init, the three admin-page requires file-gated (settings/research/dashboard — the pages land with the cre-debt admin slice, the dashboard gate keyed to the same `enable_portfolio_dashboard` sub-setting), the byte-identical enqueue helper with the `NVOOS_CONTENT_GRAPH_PRO_*` path/URL/version swaps, and NEW standalone-only wiring — `wp_mcp_ai_pro_register_cre_debt_tools()` filter + ecosystem registration with empty maps that fill as the cre-debt tool batches land); the standalone registry gained the `toolkit_cre_debt` module (the base registers `cre_debt` via its dynamic conditional-toolkit loop — files guard on the init; the F1 registry test updated for the thirty-fourth module); the entry's spl autoloader gained the `src/tools/cre-debt/` (+ asset-management/cmbs/debt-fund/examples/originations/underwriting) probes; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` swaps; `$nvoos_content_graph_pro_*` local-var prefixing in the slim init); 5 characterization tests green in both matrices (serving sources x2, the two post-type constants + registration, the calculator contracts, standalone init targets + empty filter + global helper), PHPCS-clean. Remaining: — (Wave F4 cre-debt complete: data layer + 58 tools + 3-page admin slice). Sub-cluster 7 (F4 cre-debt admin slice — cre-debt toolkit + Wave F4 complete) landed: the three admin pages ported byte-identical (`src/admin/`): `WP_MCP_AI_CRE_Debt_Settings_Page` (extends the ported cpt-settings-page-base — option `wp_mcp_ai_cre_debt_settings`, post type `mcp_ai_cre_loan`, page slug `cre-debt-settings`; ⚠ dormant-require base quirk preserved — neither the base nor the standalone init instantiates the page, byte-identical), `WP_MCP_AI_CRE_Debt_Research_Page` (PAGE_SLUG `research-cre-debt` submenu under the `mcp_ai_cre_loan` CPT, the two AJAX actions `wp_ajax_wp_mcp_ai_create_cre_loan_from_research`/`wp_ajax_wp_mcp_ai_create_cre_property_from_research`, the `__DIR__` research-page trait requires resolve from the already-ported `src/admin/` copies, the `WP_MCP_AI_Shortcode` chat-embed guard byte-identical), and `WP_MCP_AI_CRE_Debt_Dashboard_Page` (PAGE_SLUG `cre-debt-dashboard`, the `wp_ajax_wp_mcp_ai_cre_dashboard_filter` filter handler, the base-owned `WP_MCP_AI_URL`/`WP_MCP_AI_VERSION` chart.js refs stay byte-identical — page-hook-gated); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` swaps); the slim init's three file-gated admin-page requires now fire (gate targets present — the in-admin wiring is asserted through the pages' own `::init()` calls because the standalone matrix boots with `is_admin()` false, PM/calendar precedent); 5 characterization tests green in both matrices (serving sources x3, settings props, research/dashboard constants + hook wiring, standalone gate targets), PHPCS-clean. | Sub-cluster 1 (F5 quiz-management data layer) landed: `WP_MCP_AI_Quiz_CPT` (655 lines, `src/` — the two post types `mcp_ai_quiz`/`mcp_ai_submission` + the metabox loader + save/sync hook wiring), `WP_MCP_AI_JetEngine_Quizzes_CCT` (415 — `SLUG` `quizzes`, `FIELD_ID_BASE` 30000), and the three metabox classes (`WP_MCP_AI_Quiz_Metabox_Base` abstract + `_Details`/`_Questions` — `src/metaboxes/`) ported byte-identical + the `assets/css/quiz-admin.css` (+min/map) assets copied byte-identically; the slim standalone init (`src/tools/quiz-management/init.php` — full-body `! defined( 'WP_MCP_AI_PATH' )` guard (the global enqueue helper collides compile-time with the base copy), the CPT require, the byte-identical JetEngine meta-helper/CCT guards, the two admin-page requires file-gated (research + settings — the pages land with the quiz admin slice), the byte-identical enqueue helper with the `NVOOS_CONTENT_GRAPH_PRO_*` swaps, and NEW standalone-only wiring — `wp_mcp_ai_pro_register_quiz_tools()` filter + ecosystem registration with empty maps that fill as the quiz tool batch lands); the standalone registry gained the `toolkit_quiz` module (the base registers it with the dormant `jetengine_meta_helper` dep — healthcare precedent; the F1 registry test updated for the thirty-fifth module); the entry's spl autoloader gained the `src/tools/quiz-management/` probe; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps; `$nvoos_content_graph_pro_*` local-var prefixing in the slim init); 6 characterization tests green in both matrices (serving sources x5, the two post-type constants + CCT constants + settings-gated registration, the metabox hierarchy, standalone init targets + empty filter + global helpers), PHPCS-clean. Remaining: quiz tool batch (the 11 `enable_quiz_system`-gated tools + `Render_Math_Equation` from `includes/tools/math/`), quiz admin slice (2 pages), then eca-management, then chat-channels. Sub-cluster 2 (F5 quiz-management tool batch) landed: the eleven quiz tools (`Create_Quiz`, `Get_Quiz`, `List_Quizzes`, `Update_Quiz`, `Delete_Quiz`, `Submit_Quiz_Answer`, `Grade_Quiz`, `Get_Quiz_Submissions`, `Get_Quiz_Results`, `Get_Quiz_Analytics`, `Research_Quiz_Topic` — `src/tools/quiz-management/`) + the `Render_Math_Equation` tool (`src/tools/math/`) ported byte-identical plus the base-owned `WP_MCP_AI_Tool_Math_Response` D8-compat trait copy (`src/tools/trait-wp-mcp-ai-tool-math-response.php` — base domain `mcp-ai-wpoos` swapped, the `WP_MCP_AI_PATH`/`WP_MCP_AI_FILE` fallback refs stay byte-identical, defined-guarded monolith-only): the uniform `edit_posts` capability surface, the deterministic gate contracts (`missing_quiz_id`/`missing_title`/`missing_submission_id` first gates), the render-math `requires_base_pro()` true + the KaTeX vendor/node_modules path swaps, and the research tool's LLM/web-search client refs all `class_exists`-guarded; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps; per-file seams — the create-quiz content-media require gains a `trait_exists`-gated seam resolving from the already-ported `src/tools/` copy, the research tool gains the chat-response trait seam (the source declares no require — the monolith serves it at base boot), and the render-math math-response require gains the seam resolving from the new D8 copy plus the media-worker-client seam (the base classmap serves `includes/traits/`); phpcs ruleset additions — `serialize_serialize` + `Squiz.PHP.NonExecutableCode.Unreachable` severity 0 for the byte-identical KaTeX cache-key serialize + the unreachable trailing return; phpcbf whitespace on the upstream equals-alignment in get-quiz-results/update-quiz); the init's tool filter + ecosystem registration now carry all twelve ported tools (the full monolith `$quiz_tools` map); the entry's spl autoloader gained the `src/tools/math/` probe; the data-layer filter-shape assertion extended to 12; 4 new characterization tests green in both matrices (serving sources x4 incl. the trait, the 12-slug uniform-capability surface, the three deterministic gates, standalone filter shape 12 + ecosystem registration), PHPCS-clean. Remaining: quiz admin slice (2 pages), then eca-management, then chat-channels. Sub-cluster 3 (F5 quiz-management admin slice — quiz toolkit complete) landed: the two admin pages ported byte-identical (`src/admin/`): `WP_MCP_AI_Quiz_Research_Page` (764 lines, PAGE_SLUG `research-quiz` submenu under the `mcp_ai_quiz` CPT, the two AJAX actions `wp_ajax_wp_mcp_ai_create_quiz_from_research`/`wp_ajax_wp_mcp_ai_import_quiz`, the `__DIR__` research-page trait requires resolve from the already-ported `src/admin/` copies, the `WP_MCP_AI_Shortcode` chat-embed guard + the base-owned `WP_MCP_AI_URL`/`WP_MCP_AI_VERSION` enhanced-research-page asset refs stay byte-identical — page-hook-gated, PM/calendar precedent, the `class_exists( 'WP_MCP_AI_Tool_Create_Quiz' )` create seam now resolves standalone via the ported tool) and `WP_MCP_AI_Quiz_Settings_Page` (247, cpt-settings-base child — option `wp_mcp_ai_quiz_settings`, post type `mcp_ai_quiz`, page slug `quiz-settings`; ⚠ dormant-require base quirk preserved — neither the base nor the standalone init instantiates the page, byte-identical); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` swaps); the slim init's two file-gated admin-page requires now fire (gate targets present — the in-admin wiring is asserted through the pages' own `::init()` calls because the standalone matrix boots with `is_admin()` false, PM/calendar precedent); 4 characterization tests green in both matrices (serving sources x2, settings props, research constants + hook wiring, standalone gate targets), PHPCS-clean. Remaining: — (Wave F5 quiz-management complete: data layer + 12 tools + 2-page admin slice). Next: eca-management, then chat-channels. Sub-cluster 1 (F5 eca-management data layer) landed: `WP_MCP_AI_ECA_CPT` (415 lines, `src/` — the two post types `mcp_ai_eca`/`mcp_ai_student` + taxonomies + the metabox loader + save/notice/enqueue hook wiring), the four metabox classes (`WP_MCP_AI_ECA_Metabox_Base` abstract + `_Details`/`_Schedule`/`_Enrollment` — `src/metaboxes/`), and the ECA DB layer (`src/eca/`: the 23-line `init.php` + `WP_MCP_AI_ECA_Enrollments_DB` 330 + `WP_MCP_AI_ECA_Attendance_DB` 348 — the custom-table DDL (`mcp_ai_eca_enrollments`/`mcp_ai_eca_attendance`), the version-option constants, the enroll/withdraw/get-enrollments + mark/get-attendance/rate contracts with the `invalid_eca`/`invalid_student`/`invalid_status`/`invalid_tenant_type` first gates) ported byte-identical; the slim standalone init (`src/tools/eca-management/init.php` — full-body `! defined( 'WP_MCP_AI_PATH' )` guard (the global enqueue + REST-route helpers collide compile-time), the CPT + eca-DB requires, the byte-identical JetEngine meta-helper guards, the REST-controller require file-gated until the ECA REST slice lands, the four admin-page requires file-gated inside the byte-identical enabled/base-version/pro-active gate (they land with the ECA admin slice), the byte-identical enqueue + REST-route helpers with the `NVOOS_CONTENT_GRAPH_PRO_*` swaps (the byte-identical `defined( 'WP_MCP_AI_PRO_VERSION' )` pro-active checks stay as-is — regulatory-registration precedent), and NEW standalone-only wiring — `wp_mcp_ai_pro_register_eca_tools()` filter + ecosystem registration with empty maps that fill as the ECA tool batch lands); the standalone registry gained the `toolkit_eca` module (the base registers it with the dormant `jetengine_meta_helper` dep — healthcare precedent; the F1 registry test updated for the thirty-sixth module); the entry's spl autoloader gained the `src/eca/` + `src/tools/eca-management/` probes; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps; `$nvoos_content_graph_pro_*` local-var prefixing in the slim init); 6 characterization tests green in both matrices (serving sources x5, the two post-type constants + settings-gated registration, the DB constants + deterministic first gates, the metabox hierarchy, standalone init targets + empty filter + global helpers), PHPCS-clean. Remaining: ECA tool batches (35 tools + ISAMS pair + blueprint), ECA REST slice (1 controller), ECA admin slice (4 pages), then chat-channels. Sub-cluster 2 (F5 eca-management tool batch) landed: the thirty-five monolith-map tools (the ECA/student CRUD batches, the ISAMS/SOCS sync batch, the attendance/participation/timetable/waitlist batch, the notification/parent-report batch, the analytics/export batch, the term/workflow-rule/CSV-import batch — `src/tools/eca-management/`) + the tree-only `Import_ECA_Management_Blueprint` tool (`examples/`, `requires_base_pro()` true, the two blueprint JSONs copied) ported byte-identical: the uniform `edit_posts` capability surface, the deterministic gate contracts (`missing_id`/`missing_name` first gates — the create-eca multisite wrong-site gate stays byte-identical), and the DB-layer-backed enroll/attendance flows; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps; per-file seams — the create-eca content-media require gains the `trait_exists`-gated seam resolving from the already-ported `src/tools/` copy and the research-eca tool gains the chat-response trait seam (the source declares no require — quiz research-tool precedent); phpcs ruleset addition — `Universal.Operators.StrictComparisons` severity 0 for the byte-identical loose `!=` meta-diff probe in update-eca); the init's tool filter + ecosystem registration now carry all thirty-six ported tools (35 monolith + the tree-only import — CRM CC-extras precedent); the entry's spl autoloader gained the `src/tools/eca-management/examples/` probe; the data-layer filter-shape assertion extended to 36; 4 new characterization tests green in both matrices (serving sources x4, the 36-slug uniform-capability surface incl. the import `requires_base_pro`, the three deterministic gates, standalone filter shape 36 + blueprint JSON inventory + ecosystem registration), PHPCS-clean. Remaining: ECA REST slice (1 controller), ECA admin slice (4 pages), then chat-channels. Sub-cluster 3 (F5 eca-management REST slice) landed: `WP_MCP_AI_ECA_REST_Controller` (782 lines) ported byte-identical (`src/rest/`): the four `mcp-ai/v1` routes (`/ecas`, `/ecas/{id}`, `/students`, `/students/{id}`) with the `manage_options` read/write/delete permission gates, the collection/single shapes, and the byte-identical error envelopes; documented deviations (strict types; text domain — the controller carries no path constants, so no path swaps); the slim init's file-gated REST require now fires (gate target present); 3 characterization tests green in both matrices (serving source, route registration + subscriber 403 via dispatch, standalone gate target + global helper), PHPCS-clean. Remaining: ECA admin slice (4 pages), then chat-channels. Sub-cluster 4 (F5 eca-management admin slice — eca toolkit complete) landed: the four admin pages ported byte-identical (`src/admin/`): `WP_MCP_AI_ECA_Research_Page` (745 lines, PAGE_SLUG `research-eca` submenu under the `mcp_ai_eca` CPT, the two AJAX actions `wp_ajax_wp_mcp_ai_create_eca_from_research`/`wp_ajax_wp_mcp_ai_import_eca`, the `__DIR__` research-page trait requires resolve from the already-ported `src/admin/` copies, the `WP_MCP_AI_Shortcode` chat-embed guard + the base-owned `WP_MCP_AI_URL`/`WP_MCP_AI_VERSION` enhanced-research-page asset refs stay byte-identical — page-hook-gated, PM/calendar precedent), `WP_MCP_AI_ECA_Settings_Page` (223, cpt-settings-base child — option `wp_mcp_ai_eca_settings`, post type `mcp_ai_eca`, page slug `eca-settings`; ⚠ dormant-require base quirk preserved byte-identical), `WP_MCP_AI_ECA_Dashboard_Page` (773, PAGE_SLUG `eca-dashboard`, the `wp_ajax_wp_mcp_ai_eca_dashboard_data` data handler, the base-owned chart.min.js refs stay byte-identical), and `WP_MCP_AI_ECA_Consolidate_Page` (728, PAGE_SLUG `consolidate-eca`, the four AJAX handlers incl. the document upload, the `ABSPATH . wp-admin` core requires); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` swaps); the slim init's four file-gated admin-page requires now fire (gate targets present — the in-admin wiring is asserted through the pages' own `::init()` calls because the standalone matrix boots with `is_admin()` false, PM/calendar precedent); 4 characterization tests green in both matrices (serving sources x4, settings props, the three page constants + hook wiring, standalone gate targets), PHPCS-clean. Remaining: — (Wave F5 eca-management complete: data layer + 36 tools + REST controller + 4-page admin slice). Next: chat-channels. Sub-cluster 1 (F5 chat-channels data layer) landed: the four channel data classes (`WP_MCP_AI_Channel_Contacts_CPT` 375 + `_Messages_CPT` 242 — `mcp_chan_contact`/`mcp_chan_message`; `WP_MCP_AI_Channel_Contacts_CCT` 830 — `SLUG` `channel_contacts`, `FIELD_ID_BASE` 42000, the new/active/resolved/blocked status map; `WP_MCP_AI_Channel_Messages_CCT` 749 — `channel_messages`/41000), the Google Chat webhook layer (`google-chat-webhook-init.php` 47 + the `src/ChatChannels/class-wp-mcp-ai-google-chat-webhook-handler.php` 406 — the legacy-handler superseded-by-controller rest_api_init wiring), and the `WP_MCP_AI_Chat_Channels_Optimization` class (332 — `OPTIMIZE_HOOK` `wp_mcp_ai_cc_daily_optimize`, 90-day retention, the settings option) ported byte-identical; the slim standalone init (`src/tools/chat-channels/init.php` — full-body `! defined( 'WP_MCP_AI_PATH' )` guard (the global enqueue + tool-loader + rate-limit helpers collide compile-time), the byte-identical enabled/base gates, the webhook-init + CCT/CPT + optimization requires with the `NVOOS_CONTENT_GRAPH_PRO_*` swaps, the four REST-controller + admin menu/settings requires keep their byte-identical file_exists guards (those slices land with later sub-clusters), the byte-identical tool-loader list + `wp_mcp_ai_load_pro_tools` action (inert standalone) + rate-limit helper, and NEW standalone-only wiring — `wp_mcp_ai_pro_register_chat_channels_tools()` filter + ecosystem registration with empty maps that fill as the chat-channels tool batch lands); the standalone registry gained the `chat_channels` module (the base registers it via its dynamic conditional-toolkit loop — cre-debt precedent; the F1 registry test updated for the thirty-seventh module); the entry's spl autoloader gained the `src/ChatChannels/` + `src/tools/chat-channels/` probes; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps; `$nvoos_content_graph_pro_*` local-var prefixing in the slim init); 5 characterization tests green in both matrices (serving sources x6 incl. the per-mode webhook-handler path shapes, the CPT/CCT constants + status map, the optimization constants, the no-gate CPT bootstrap + direct registration, standalone init targets + empty filter + the four global helpers), PHPCS-clean. Remaining: chat-channels tool batches (44 tools incl. the google-service-account service), chat-channels REST slices (4 webhook controllers + inbox controller), chat-channels admin slice (menu + settings page). Sub-cluster 2 (F5 chat-channels tool batch) landed: the fifty loader tools (the Google Chat/Telegram/WhatsApp/Slack/Discord/Teams/Outlook/OneDrive/Messenger/Twitter/Apple/iCloud platform batches + the unified broadcast + the schedule-notify-sms tool — `src/tools/chat-channels/`) + the `WP_MCP_AI_Pro_Google_Service_Account` service (315 — JWT token flow, `TOKEN_URI`/`TOKEN_CACHE_PREFIX` constants) + the tree-only `Import_Chat_Channels_Blueprint` tool (`examples/`, `requires_base_pro()` true, the blueprint JSON copied) + the base-owned `WP_MCP_AI_Cron_Manager` D8-compat copy (340 — `src/class-wp-mcp-ai-cron-manager.php`, self-contained option-backed job store) ported byte-identical: the uniform `edit_posts` declaration surface with the byte-identical `manage_options` execute-gate variance (the filterable `wp_mcp_ai_*_capability` defaults), the deterministic gate contracts (`missing_discord_token`/`missing_slack_token`), and the telegram-webhook manage flow's byte-identical file_exists-guarded controller require + class_exists-guarded remote-site-manager require (both now resolve from the ported copies); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps; per-file seams — the base-owned interface/logger/restrict-trait requires gain exists-check seams resolving from the addon's D8-compat `src/` copies (49 files each, the restrict trait ×2) and the schedule-notify-sms cron-manager require gains the monolith-gated exists-check seam resolving from the new D8 copy); the init's tool filter + ecosystem registration now carry all fifty-one ported tools (the 50 loader list + the tree-only import — CRM CC-extras precedent); the entry's spl autoloader gained the `src/tools/chat-channels/examples/` probe; the data-layer filter-shape assertion extended to 51; 4 new characterization tests green in both matrices (serving sources x5 incl. the per-matrix cron-manager copy, the 51-slug uniform-capability surface incl. the import `requires_base_pro`, the two deterministic gates, standalone filter shape 51 + blueprint JSON + ecosystem registration), PHPCS-clean. Remaining: chat-channels REST slices (4 webhook controllers + inbox controller), chat-channels admin slice (menu + settings page). Sub-cluster 3a (F5 chat-channels REST slice A) landed: `WP_MCP_AI_Chat_Channels_REST_Controller` (1,800 lines) + `WP_MCP_AI_Google_Chat_Webhook_Controller` (2,489) ported byte-identical (`src/rest/`): the `mcp-ai-pro/v1/chat-channels` inbox routes (conversations/reply/contacts with the `manage_options` gate) and the `mcp-ai/v1` google-chat webhook routes (the OIDC token validation + conversation history + async AI-reply scheduling surface); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps — the CCT/service-account requires now resolve from the already-ported copies; the webhook controller's base-owned logger require gains the exists-check seam resolving from the D8-compat copy; the inbox controller's `defined( 'WP_MCP_AI_PRO_PATH' )` remote-site-manager guard stays byte-identical — the entry spl autoloader serves the ported manager); the slim init's inbox + webhook REST gate targets now fire; 4 characterization tests green in both matrices (serving sources x2, the inbox + webhook route registrations, standalone gate targets), PHPCS-clean. Remaining: chat-channels REST slice B (apple + outlook + icloud + telegram webhook controllers), chat-channels admin slice (menu + settings page). Sub-cluster 3b (F5 chat-channels REST slice B — REST complete) landed: the four webhook controllers ported byte-identical (`src/rest/`): `WP_MCP_AI_Apple_Messages_Webhook_Controller` (733), `WP_MCP_AI_Outlook_Webhook_Controller` (946 — the five class_exists-guarded remote-site-manager requires now resolve from the ported manager), `WP_MCP_AI_ICloud_Webhook_Controller` (665), and `WP_MCP_AI_Telegram_Webhook_Controller` (4,522 — the four upstream base-domain `mcp-ai-wpoos` strings swap to `nvoos-content-graph-pro`, cloudways-client precedent); documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps — the CCT requires resolve from the already-ported copies; the base-owned logger require gains the exists-check seam resolving from the D8-compat copy); the slim init's apple/outlook/icloud REST gate targets now fire; 3 characterization tests green in both matrices (serving sources x4, the controller contracts, standalone gate targets), PHPCS-clean. Remaining: chat-channels admin slice (menu + settings page). Sub-cluster 4 (F5 chat-channels admin slice — chat-channels toolkit complete) landed: `WP_MCP_AI_Chat_Channels_Menu` (993) + `WP_MCP_AI_Chat_Channels_Settings_Page` (2,258) ported byte-identical (`src/admin/`) plus the `chat-channels-inbox.css`/`.js` assets copied byte-identical: the top-level `wp-mcp-ai-chat-channels` menu (admin_menu 20, `manage_options`, position 58, the dashboard/inbox/contacts/automation submenus + the settings redirect), the toolkit-settings-base child (`chat_channels`/`wp_mcp_ai_chat_channels_toolkit_settings`/`wp-mcp-ai-chat-channels-toolkit-settings`, no research/remote-sites), the WhatsApp phone-number AJAX handler + the TMA builder asset enqueue; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps — the base-owned `WP_MCP_AI_PATH` chart ref stays byte-identical; the two telegram-mini-app-templates requires gain file_exists guards — forward-reference, the 10.5k-line templates file lands with a later sub-cluster); the slim init's menu + settings gate targets now fire (gate targets present — the in-admin wiring is asserted through the pages' own construction because the standalone matrix boots with `is_admin()` false, PM/calendar precedent); 4 characterization tests green in both matrices (serving sources x2, menu constants + hook wiring, settings props, standalone gate targets + assets), PHPCS-clean. Remaining: — (Wave F5 complete: quiz-management + eca-management + chat-channels — data layers + 99 tools + 7 REST controllers + admin slices). Next: Wave F6 pro-data + pro-platform (analytics ✓, extended cognition, places, orchestration, research/research-add, cloudways ✓, Google Workspace, email marketing, remote connections ✓, capture ✓, DietPi, Composio, mcp-servers ✓, schedule-anything) + the telegram-mini-app-templates forward-reference. Sub-cluster 1 (F6 places data layer) landed: `WP_MCP_AI_Place_CPT` (306 lines, `src/` — `POST_TYPE` `mcp_ai_place`, the admin-columns/save-meta/enqueue wiring + the four metabox requires) + the four place metabox classes (`WP_MCP_AI_Place_Metabox_Base` abstract + `_Location`/`_Contact`/`_Details` — `src/metaboxes/places/`) ported byte-identical; the slim standalone init (`src/tools/places/init.php` — full-body `! defined( 'WP_MCP_AI_PATH' )` guard (the global CPT registration helper collides compile-time), the byte-identical `enable_places_management` + base-version gate with the CPT + two taxonomies (`mcp_ai_place_type`/`mcp_ai_place_tag`) + the ten default place types, the CPT require, the JetEngine meta-helper guard, the two admin-page requires file-gated (research + settings — the pages land with the places admin slice), and NEW standalone-only wiring — `wp_mcp_ai_pro_register_places_tools()` filter + ecosystem registration with empty maps that fill as the places tool batch lands); the standalone registry gained the `toolkit_places` module (the base registers it with the dormant `jetengine_meta_helper` dep — healthcare precedent; the F1 registry test updated for the thirty-eighth module); the entry's spl autoloader gained the `src/metaboxes/places/` + `src/tools/places/` probes; documented deviations (strict types; text domain; `NVOOS_CONTENT_GRAPH_PRO_*` constant swaps; `$nvoos_content_graph_pro_*` local-var prefixing in the slim init); 5 characterization tests green in both matrices (serving sources x5, the CPT constant, the settings-gated CPT + taxonomy registration, the metabox hierarchy, standalone init targets + empty filter + global helper), PHPCS-clean. Remaining: places tool batch (12 tools + the always-on find-bookable-places), places admin slice (research + settings pages), then the F6 extended-cognition/orchestration/research-add/Google-Workspace/email-marketing/DietPi/Composio/schedule-anything slices + the telegram-mini-app-templates forward-reference.
| F7 | Node-service bridges | 🔴 | |

## Wave F-UI — Pro UI + SPA v2

| # | Subsystem | Status | Notes |
|---|---|---|---|
| F-UI-1 | SPA v2 build pipeline in ecosystem npm workspace | 🔴 | |
| F-UI-2 | SPA v2 runtime port + loaders | 🔴 | |
| F-UI-3 | Pro toolkit blocks/shortcodes/inline-assistant + admin screens | 🔴 | |
| F-UI-4 | SPA v2 pro-toolkit slices | 🔴 | |

## Wave G — Release & distribution (release-gated)

| # | Item | Status | Notes |
|---|---|---|---|
| G1 | Ecosystem version bumps + `Requires Plugins` audit | 🔴 | |
| G2 | wp.org Base tier mapping (decision D6) | 🔴 | |
| G3 | Optional NEW bundle plugin | 🔴 | Never the base plugin (D-NOBASE) |
| G4 | Docs sweep + migration guide | 🔴 | |
