# Ecosystem Port Tracker

**Living tracker for the additive base+pro → Content Graph ecosystem port.**
Last updated: 2026-09-07 · Plan: [`base-pro-ecosystem-port-plan.md`](plans/base-pro-ecosystem-port-plan.md) · Gaps: [`base-pro-ecosystem-port-gap-analysis.md`](proposals/base-pro-ecosystem-port-gap-analysis.md)

Locked constraints: **D-NOBASE** (zero changes to `mcp-ai-wpoos`), **D-NOCORE** (zero changes to `nvoos-content-graph`), **D-UI** (all UIs ported), **D-SPA** (Pro SPA v2 ported).

| Status | Meaning |
|---|---|
| ✅ Done | Ported, wired, tested in monolith + standalone matrices |
| 🟡 In progress | PR open or partially wired |
| 🔴 Gap | Planned, no implementation yet |
| ⏸️ Deferred | Decision-parked (see plan) |

## Phase 0 — Foundations

| Item | Status | Notes |
|---|---|---|
| Ownership discriminator (`! defined('WP_MCP_AI_PATH')`) | ✅ | Extraction-proven; no base changes |
| AI addon test suite + monolith/standalone CI matrices | ✅ | `plugins/nvoos-content-graph-ai/{phpunit.xml.dist,tests/}`, `.github/workflows/phpunit-ai.yml` |
| Ecosystem asset pipeline (npm workspace) | 🔴 | Phase 0.5 |
| Tracker (this file) | ✅ | Phase 0.6 |

## Wave D — AI runtime → `nvoos-content-graph-ai`

| # | Subsystem | Status | Notes |
|---|---|---|---|
| D1a | Prompt optimizer (cache-hit ordering, cache key, prompt split) | ✅ | `src/Chat/PromptOptimizer.php`; wired into `Rest\ChatController` via `cache_system_prompt` request arg; 10 characterization tests |
| D1b | Chat response cache + SSE rate limiter + semantic cache | ✅ | `src/Chat/{ChatResponseCache,SseRateLimiter,SemanticCache}.php`; wired into `Rest\ChatController` (cache lookup/store on the non-streaming path, 429 + register/release on the streaming path); 20 characterization tests; SemanticCache ships dormant (matches base — no call sites) |
| D1c | Conversation summarizer (BME context strategy) | ✅ | `src/Chat/ConversationSummarizer.php` (decoupled client contract) + `src/Chat/OrchestratorCompletionClient.php` (nvoos/core adapter); 12 characterization tests; wiring into the chat flow follows with the full BME strategy port |
| D1d | Thread manager (threads/messages/checkpoints CRUD + schema) | ✅ | `src/Chat/ThreadManager.php` (byte-identical tables/error codes/envelopes); schema created on CG-AI activation; 13 characterization tests with real DDL |
| D1e | Message attachments + response attachments (attachment pipeline) | ✅ | `src/Chat/{MessageAttachments,ResponseAttachments}.php` (byte-identical meta key `_wp_mcp_ai_openai_file`, filters, error codes, cache group) + `FileServiceBridge`/`FileClientBridge` collaborators; monolith delegates to base factory/client; standalone delegates to the ported `FileServiceFactory`/`OpenAiFileService` (D2f); cleanup/response hooks registered standalone-only via `Plugin.php`; 28 characterization tests |
| D1f | Chat transcript recorder + transcript retention | ✅ | `src/Chat/{ChatTranscriptRecorder,TranscriptRetention}.php` (byte-identical error codes/options, JetEngine CCT + logger gated behind `defined('WP_MCP_AI_PATH')`); recorder invoked on the non-streaming chat path; 4 `transcript_retention_*` setting defaults added; `TranscriptRetention::init()` standalone-only; 10 characterization tests |
| D1g | ChatKit integration (addon definition + registration surface) | ✅ | `src/Chat/ChatKitIntegration.php` (hook names byte-identical; `ADDON_ID` = `nvoos-content-graph-ai`; definition describes CG-AI's real surface — `nvoos-content-graph/v1`, `POST /ai/chat`, `GET /ai/tools`); standalone-only via `Plugin.php`; capability seam delegates to base helper in monolith / same `wp_mcp_ai_chat_capability` filter standalone; 9 characterization tests; documented deviations (download route, surfaces, assistant_id, guest_access) restored as D2/D5/D-UI land |
| D1 | Chat runtime core (clients, router, selector, caches, summarizer, RAG bridge, threads, transcripts, SSE, attachments, quick actions, chatkit) | ✅ | D1a–D1g done (116 ecosystem tests, 400 assertions); remote file-API uploads + ChatKit download route gated on D2 |
| D2a | Zai provider client | ✅ | `src/Provider/ZaiClient.php` extends nvoos/core `OpenAiCompatibleClient` (slug `zai`, default endpoint `https://api.z.ai/api/paas/v4`); registered in `CoreBridge`; `ai_api_key_zai` + `zai_base_url` defaults; 7 characterization tests |
| D2b | Google Maps client (geocode/reverse/nearby/text search) | ✅ | `src/Provider/GoogleMapsClient.php` (byte-identical endpoints/query args/error codes/normalization); settings seam per mode (`google_maps_api_key`); transport-error default branch standalone (full enrichment when `WP_MCP_AI_HTTP` ports); dormant until tools wave; 12 characterization tests |
| D2c | OpenAI Realtime ×3 (voice, translate, whisper) + voice-provider interface | ✅ | `src/Provider/{VoiceProviderInterface,OpenAiRealtimeClient,OpenAiRealtimeTranslateClient,OpenAiRealtimeWhisperClient}.php` (byte-identical endpoints/GA session payloads/token caching/headers/language tables/filters); tool-registry + assistant config monolith-only; dormant until D-UI voice surface; 18 characterization tests |
| D2d | RabbitMQ client | ✅ | `src/Provider/RabbitMqClient.php` (byte-identical topology constants/config keys/constant precedence/job transients/health shapes); `amqp` extension gating preserved; settings seam per mode; dormant until platform queue wave (E2); tests in shared suite |
| D2e | STDIO transport (MCP JSON-RPC server) | ✅ | `src/Provider/StdioTransport.php` (byte-identical protocol version/method routing/error codes/response shapes); tool registry + assistant config seams (base registry monolith / nvoos-core registry standalone); tests in shared suite |
| D2f | File service factory + OpenAI/Gemini file services | ✅ | `src/Chat/{FileServiceFactory,OpenAiFileService,GeminiFileService}.php` (byte-identical provider mapping/multipart uploads/polling/cache keys/error codes); bridges now use them standalone — closes the D1e file-API gap; 13 characterization tests |
| D2 | Providers beyond the 13 (Zai, Google Maps, RabbitMQ, Stdio, Realtime ×3) + file services | ✅ | D2a–D2f done (67 new ecosystem tests); SaaS drivers (FlowHub/PayHere/Printful) remain Pro-tier per gap §4.2/8.3 |
| D3a | Model catalog migration + integrity verifier | ✅ | `src/Model/{ModelCatalogMigration,ModelIntegrityVerifier}.php` + bundled `model-catalog.json`; legacy-id map/option/meta/settings rewriting, blocked models, vulnerability + self-hosted TLS enforcement byte-identical; `run_from_catalog` standalone-only via `Plugin.php`; 11 characterization tests |
| D3b | Model rate-limits CCT + pricing checker | ✅ | `src/Model/{ModelRateLimitsCct,ModelPricingChecker}.php` (JetEngine provisioning + cron byte-identical); both `bootstrap()` standalone-only; 9 characterization tests |
| D3c | Analytics engine | ✅ | `src/Analytics/AnalyticsEngine.php` (trends/statistics/Z-score anomaly/patterns/comparisons/transcript rebuild); usage seam delegates to base `WP_MCP_AI_Tool_Token_Limits` monolith / `ToolTokenLimits` standalone; 11 characterization tests |
| D3d | Usage tracker | ✅ | `src/Analytics/UsageTracker.php` (meta key/totals/normalization/filters/pricing byte-identical; fallback pricing map byte-identical); settings seam per mode; `init()` standalone-only; 8 characterization tests |
| D3e | Token tracking DB + enhanced tracking + optimizer | ✅ | `src/Analytics/{TokenTrackingDatabase,EnhancedTokenTracking,TokenDbOptimizer}.php` (schema/inserts/aggregations/retention/backfill/migration + usermeta index lifecycle byte-identical); `init()` standalone-only; 13 characterization tests |
| D3f | Tool token limits | ✅ | `src/Analytics/ToolTokenLimits.php` (constants/tiers/role map/multipliers/usage shape/session accounting/forecasting/anomaly detection/hooks byte-identical); settings + workload-tier + logging seams per mode (workload tier defaults `medium` standalone until Resource Manager ports in D4); `init()` standalone-only, deactivation hook monolith-skipped; 35 characterization tests |
| D3 | Model management + analytics/token tracking | ✅ | D3a–D3f done (87 new ecosystem tests); suite now 270 tests / ~1171 assertions green in both monolith + standalone matrices, PHPCS-clean |
| D4a | Provider circuit breaker | ✅ | `src/Security/ProviderCircuitBreaker.php` (transient keys/state constants/threshold+timeout filters byte-identical); dormant until providers adopt; 9 characterization tests |
| D4b | Encrypted API key store | ✅ | `src/Security/ApiKeyStore.php` (option prefix/managed keys/migration semantics byte-identical); crypto seam: base `WP_MCP_AI_Encryption` monolith / parent `Remote\Crypto` standalone; logging monolith-only; 10 characterization tests |
| D4c | Security audit logger | ✅ | `src/Security/SecurityAuditLogger.php` (table `wp_mcp_ai_security_log`, event constants, `wp_mcp_ai_security_event` action, REST endpoint `mcp-ai/v1/security/events`, purge cron byte-identical); `register()` + activation table creation standalone-only; 7 characterization tests with real DDL |
| D4d | Request guard | ✅ | `src/Security/RequestGuard.php` (SSE counter keys/REST priorities/body+JSON limits/verbosity stripping/asset-version stripping byte-identical); settings seam per mode; SSE slots delegate to base `WP_MCP_AI_SSE_Rate_Limiter` monolith / ported `SseRateLimiter` standalone; `register()` standalone-only; 11 characterization tests |
| D4e | URL guard (SSRF) | ✅ | `src/Security/UrlGuard.php` (blocked CIDRs/hostnames/error codes/filters byte-identical); loopback-hostname shortcut delegates to base `WP_MCP_AI_HTTP_Helper` monolith, DNS-based checks standalone; 11 characterization tests |
| D4f | Security posture scoring | ✅ | `src/Security/SecurityPosture.php` (23-signal set/weights/grades/quick wins/`wp_mcp_ai_security_posture_signals` filter byte-identical); settings + restriction-registry seams per mode; 7 characterization tests |
| D4g | Concurrency guard + subscriber + exception | ✅ | `src/Security/{ConcurrencyGuard,ConcurrencyGuardSubscriber}.php` + `Exceptions/ConcurrencyLimitReached.php` (slot table/limits map/filter/priorities/envelopes byte-identical); capability-flag + tool-resolution seams per mode; subscriber `register()` standalone-only; 10 characterization tests with real DDL |
| D4h | Cost tracker + subscriber + exception | ✅ | `src/Security/{CostTracker,CostTrackerSubscriber}.php` + `Exceptions/CostBudgetExceeded.php` (pricing map/estimate heuristics/budget meta/spend option/filters/envelopes byte-identical); subscriber `register()` standalone-only; 14 characterization tests |
| D4i | Destructive ops gate + exception | ✅ | `src/Security/DestructiveOpsGate.php` + `Exceptions/DestructiveConfirmationRequired.php` (flag vocabulary/confirmation values/preview payload/rejection action/428 envelope byte-identical); settings + flag + audit seams per mode; `register()` standalone-only; 8 characterization tests |
| D4j | CSP headers | ✅ | `src/Security/CspHeaders.php` (directive set/filters/header names byte-identical); `is_admin_context()`/`send_header()` protected seams for testability; `register()` standalone-only; 3 characterization tests |
| D4k | Load guard | ✅ | `src/Security/LoadGuard.php` (counter key/429 envelope/filter byte-identical); max-concurrency + job-queue aggregation monolith-only (standalone counts the transient counter until Resource Manager ports — tracked); `register()` standalone-only; 5 characterization tests |
| D4 | Security stack (10 classes + circuit breaker) | ✅ | D4a–D4k done (95 new ecosystem tests); suite now 365 tests / ~1585 assertions green in both monolith + standalone matrices, PHPCS-clean |
| D5a | Assistant directory REST controller | ✅ | `src/Rest/AssistantController.php` (routes `mcp-ai/v1/assistants` GET/POST/DELETE, directory contract: summary fields/rest links/capabilities/implementation/X-WP-Total headers, search/include/pagination/_fields, create/delete with meta persistence — byte-identical); auth = CG-AI caps; settings/config/cache/access-validation seams per mode; token scope deferred to D-UI; `registerRoutes()` standalone-only; 17 characterization tests |
| D5b | Tools listing REST controller | ✅ | `src/Rest/ToolsController.php` (route `mcp-ai/v1/tools` GET, `tools` contract name/description/inputSchema, assistant scoping, `_fields` filtering, cache — byte-identical); registry seam: base `WP_MCP_AI_Tool_Registry` monolith / nvoos-core registry via `CoreBridge` standalone (camelCase→snake_case wrapper); `registerRoutes()` standalone-only; 8 characterization tests |
| D5c | MCP JSON-RPC controller | ✅ | `src/Rest/McpController.php` (routes `mcp-ai/v1/mcp` GET/POST/OPTIONS + `/no-sse` + `/sse`; JSON-RPC 2.0 envelope: parse errors -32700, invalid request -32600, header mismatch -32020, method-not-found -32601, batching max 20, notifications 202, `_meta` serverInfo stamp, Mcp-Method/Mcp-Name headers; `server/discover` with version negotiation + inline tools + `wp_mcp_ai_mcp_discover_instructions`/`wp_mcp_ai_discover_include_tools` filters; `tools/list` delegates to `ToolsController` (composition) + annotations + `ttlMs`/`cacheScope`; `resources/list|read`, `prompts/list|get`, `completion/complete`, `logging/setLevel`, `notifications/cancelled`; CORS headers from `cors_allow_origin` settings — byte-identical); deviations documented: SSE sessions deferred (GET /mcp + /sse return discovery JSON, `sse.enabled=false`), `tools/call` execution landed with D8 Cluster 0 (per-mode registry seam, canonical envelope), no OAuth scope enforcement (stub seam), no default-assistant resolution, no token scoping; auth = CG-AI caps; 33 characterization tests |
| D5d | Chat compat REST route | ✅ | `src/Rest/ChatCompatController.php` (route `mcp-ai/v1/chat` POST/GET, POST args byte-identical: assistant_id (int|string), messages (required + base validator rules), options (provider/model/temperature/stream/response_format), professional_prompt; handler translates the base options envelope → flat CG-AI params and delegates to `ChatController::handleChat()` — response envelope (`success`/`data`/`tool_results`/`iterations`/`cost`/`cache_metadata`) already matches the base; temperature clamp [0,2] base contract); deviations documented: `assistant_id`/`professional_prompt`/`options.response_format` accepted but not applied (assistant/profession runtimes not ported — D-UI-6), content-part arrays JSON-encoded, GET SSE handshake → `wp_mcp_ai_sse_chat_deferred` (501, streaming via POST `options.stream`); auth = CG-AI caps; 19 characterization tests |
| D5 | REST surface (mcp-ai/v1 chat/tools/assistants/MCP methods) | 🟡 | D5a–D5d done (77 new ecosystem tests); remaining: file-download/cron-status endpoints — land with the queues wave |
| D6a | WP-CLI core commands | ✅ | `src/Cli/` (`StatusCommand` — environment summary with install mode + runtime facts, base `status` shape; `ToolsCommand` — registry inventory via the same monolith/standalone seam as `ToolsController`, `--filter`; `ProvidersCommand` — provider slugs + credential state via `CredentialResolver` + default flag; `SettingsCommand` — `nvoos_content_graph_settings` reader, secrets refused (list + get); `GraphCommand` — parent-schema row counts with honest `unavailable` for missing tables) + `src/Cli.php` hub registration; data logic in plain static methods (no WP-CLI dependency, testable), `run*()` wrappers thin; 12 characterization tests |
| D6 | WP-CLI surface (20+ commands) | 🟡 | D6a done (6 new commands: status / tools list / providers list / settings list|get / graph stats + existing migrate-keys/key-status = 8); remaining base commands (queue stats, token, plugins, stdio, DLQ/SLA/restrictions) land with their owning waves (queues/tokens/mesh) |
| D7 | Agent memory CCT bridge/migrator/reader | ✅ | `src/Memory/` (`AgentMemoriesCct` — JetEngine CCT registration, slug `ai_agent_memories`/field base 30000/args + meta-fields byte-identical, REST writes disabled; `AgentMemoryCctBridge` — `wp_mcp_ai_memory_stored|deleted` listeners, record mapping/tier classification/content-hash normalisation/upsert-by-context_id/warn throttling byte-identical; `AgentMemoryCctMigrator` — version option + target 2 + enable filter byte-identical, opportunistic version advance when disabled; `AgentMemoryCctReader` — `wp_mcp_ai_recall_memory_candidates` hydration + transient-shaped records, cap 500, error-tolerant queries byte-identical); CCT class resolves per mode (base class monolith / ported class standalone); all `bootstrap()` standalone-only; dormant-until-gaps documented (standalone memory-event emission + recall consumers land with the tools/memory wave); 19 characterization tests |
| D8 | Tool execution port (base tool inventory + `tools/call`) | ✅ | Plan: [`d8-tool-execution-port-plan.md`](plans/d8-tool-execution-port-plan.md). Cluster 0 (execution spine) landed 2026-09-05: `McpController::mcp_tools_call()` executes via the per-mode registry seam (base registry monolith / nvoos-core registry standalone with the WordPress `AuthProvider`), assistant scoping when an `assistant_id` is supplied, the byte-identical tool rate limiter, the before/after execution hooks, `wp_mcp_ai_tool_output` + token-budget adjustment, and MCP content shaping (`is_mcp_content_array` / `convert_to_text_content`). Cluster 1 (registration) landed 2026-09-05: `CoreToolFactory` + a 196-entry `CoreToolManifest` wire the pre-ported nvoos-core inventory into the standalone registry (monolith-gated). Cluster 2 (port the self-contained base tools) landed 2026-09-05: 2a hardened the seven base-coupled adapter tools (`vectorize_image`, `media_library_optimizer`, `run_gemini_managed_agent`, `delegate_to_a2a_agent`, `probe_chat`, `query_mesh_intelligent`, `visualize_workflow_metrics` — `accessDenied`→`forbidden`, defined-gated degradation envelopes); 2b ported 6 tools (taxonomy quartet, `list_mcp_tools`, `get_environment_status`); 2c ported the remaining 26 self-contained tools (`enable_reasoning_mode`, `add_model_config`, `research_model`, `discover_new_models`, the 6-tool security batch, `get_site_health`, `get_system_logs` + validated, and the 13-tool media/memory/content/usage batch) with per-mode seams (`ModelConfigStore` for `wp_mcp_ai_model_configs`, `WordPressNativeTrait` for hooks/cache keys, `ReasoningController`, context-manager/provider/settings/helper seams). Cluster 3 (harness tools) landed 2026-09-05: the 8 harness tools (`evolve_harness`, `apply_prompt_cue`, `list_prompt_cues`, `record_reflection`, `retrieve_with_provenance`, `scope_memory`, `select_prompt_cue`, `self_consistency_vote`) live in `plugins/nvoos-content-graph-ai-platform/src/Harness/Tools/` and surface through the CG-AI core registry via `HarnessToolRegistrar` (standalone-only, monolith no-op); per-mode evolver/bootstrap seams use the Wave C platform ports standalone; registry-dependent paths keep the documented degradation envelopes (`wp_mcp_ai_self_refine_no_registry`, empty `RetrievalHarness` fan-out). Validation: ecosystem suite 611 tests / 5320 assertions green in both matrices; platform harness suite 36 tests green in both platform matrices; live design-stack smoke — 270 tools executing, harness tools exercised end-to-end. Cluster 4 (deferred buckets) is documentation-only and stays truthful via the owning-wave cross-references below |

## Wave D-UI — AI UI → `nvoos-content-graph-ai`

| # | Subsystem | Status | Notes |
|---|---|---|---|
| D-UI-1a | Guest token flow | ✅ | `src/Chat/GuestToken.php` (constants/TTL caps/transient prefix, issuance with origin binding + `wp_mcp_ai_guest_token_issuance_origin`, validation with absolute-max lifetime, assistant scoping, `wp_mcp_ai_guest_token_allowed_origins` allowlist, sliding TTL, header/param extraction — byte-identical); TTL setting seam per mode (`guest_token_lifetime`); REST integration additive: `ChatCompatController::permissions_check` + the core `/ai/chat` permission accept valid assistant-scoped guest tokens; 12 characterization tests |
| D-UI-1b | Frontend chat widget | ✅ | `src/Frontend/ChatShortcode.php` (`[nvoos_content_graph_chat]` shortcode per `CHAT-SHORTCODE-PLAN.md` — unique container IDs, `assistant`/`allow_guests`/`provider`/`model`/`height`/`show_cost`/`placeholder` atts, asset enqueue, `window.NvoosContentGraphChat` inline config, guest-token issuance when `allow_guests` + resolvable assistant); `assets/js/content-graph-ai-chat-frontend.js` (lean vanilla widget: SSE streaming via the shared `NvoosContentGraphAiSse` parser, sessionStorage transcripts, tool cards, cost badge, markdown-lite rendering — aligned with SPA-v2 contract, NOT a byte-port of the base chat.js bundle — documented deviation); widget CSS appended to `content-graph-ai-chat.css`; registered in both modes (tag never collides with the base's `[mcp_ai_chat]`); 9 characterization tests |
| D-UI-1 | Chat widget (chat.js) + guest token flow | ✅ | D-UI-1a + D-UI-1b done (21 new ecosystem tests); the base's full chat.js bundle (voice/webrtc/Pro services) remains with the base hub — the ecosystem ships the aligned lean widget instead (per `CHAT-SHORTCODE-PLAN.md`) |
| D-UI-2 | Chat bubble + blocks + shortcodes | ✅ | `src/Blocks/{Blocks,ChatBlock,ChatBubbleBlock}.php` — hub + `nvoos-content-graph-ai/chat` + `nvoos-content-graph-ai/chat-bubble` blocks (server-rendered wrappers of `[nvoos_content_graph_chat]` with attribute→shortcode mapping, `sanitize_hex_color` colours, `sanitize_text_field` titles/tooltips, category `nvoos-content-graph-ai`); `assets/js/blocks/content-graph-ai-blocks.js` (editor registrations + inspector controls); `assets/js/content-graph-ai-chat-bubble.js` + `assets/css/content-graph-ai-chat-bubble.css` (toggle/close/Escape/click-outside/auto-open/remember-state + `nvoos-cg-bubble:open|close` events, `nvoos-cg-bubble*` classes so both bubbles coexist in monolith); wired into `Plugin.php` in both install modes; deviations: saveTranscript/enableStreaming/allowSensitiveTools/template/showBuildButton dropped (no CG counterpart), eager widget init (no lazy-init); 8 characterization tests |
| D-UI-3 | Elementor chat-family widgets | ✅ | `src/Elementor/{ElementorHub,ChatWidget,ChatBubbleWidget}.php` — `nvoos_cg_chat` (thin wrapper of `[nvoos_content_graph_chat]`, controls: assistant/allow_guests/provider/model/height/show_cost/placeholder) + `nvoos_cg_chat_bubble` (bubble markup mirroring `ChatBubbleBlock::render()` — `nvoos-cg-bubble*` classes, `--nvoos-cg-bubble-*` CSS vars, data attrs; controls: position/size/animation/tooltip, panel title/width/height, auto-open/remember/badge, bubble/header colours); panel category `nvoos-content-graph-ai`; hub no-ops without Elementor (`Widget_Base` guard in widget files + runtime checks); wired into `Plugin.php` in both install modes; deviations: base chat.js widget surface + extra bubble controls (icon/hover/badge colours/panel radius) deferred; 8 characterization tests |
| D-UI-4 | Assistant builder/test/add pages + block set | ✅ | Pages + CPT (31 tests) plus the assistant block set: `AssistantSelectorBlock`, `ToolsGridBlock` (per-mode registry seam: base registry monolith / CoreBridge standalone, single "Other tools" group standalone — deviation), `KnowledgeBaseBlock` (wp/v2/media uploads), `AssistantBuilderBlock` (composite; chat section embeds the CG widget, build submits to the per-mode create action — deviations); editor + frontend JS + CSS; the Build Assistant Prompt tab now embeds the tools grid + KB components; 18 characterization tests green in both matrices. `professional-selector` stays deferred (profession runtime wave); tool presets stay deferred (presets wave) |
| D-UI-5 | Settings shell + sections | ✅ | Validator port (`src/Admin/Settings/SettingsValidator.php` — base method/error-code contract), `AiSection` validate-then-sanitize contract (rejected sections record a settings error and preserve previous values via the parent's merge-on-existing flow), `SettingsRegistry` facade forwarding to the parent plugin's registry (core consumed, never modified) with the public `nvoos_content_graph_ai/register_settings_sections` hook for the platform (Wave E-UI); the four existing sections re-based onto `AiSection` with real validations (provider/model required-when-enabled, temperature 0–2, max-tokens 1–128000); 14 characterization tests green in both matrices. Renderer stays the parent plugin's `Section` renderer (consumed); the base's subtab renderer ports with its owning sections |
| D-UI-6 | Assistant editor + metadata wave | 🟡 | `AssistantPostType` already registers the CPT standalone with 5 REST-visible meta keys (`_wp_mcp_ai_tools`, `_wp_mcp_ai_provider`, `_wp_mcp_ai_model`, `_wp_mcp_ai_temperature`, `_wp_mcp_ai_system_prompt`); `_wp_mcp_ai_memory_files`/`_wp_mcp_ai_primary_roles`/`_wp_mcp_ai_source_profession` are declared but never `register_post_meta()`'d. Remaining: port the base's 14 assistant metaboxes — 4 inline (Context Window Estimator, Available Tools, Prompt Shortcuts, Access Control) + 10 dedicated classes in `includes/assistants/metaboxes/` (Credentials, Defaults, Primary_Roles, Base_Knowledge, Mesh_Routing, Datasets, Skills, MCP_Apps, Harness_Profile, Artifact_Governance); complete `register_meta` parity for the base's ~25 keys (incl. `_wp_mcp_ai_prompt_caching`, `_wp_mcp_ai_vector_store_id`, `_wp_mcp_ai_corpus_name`, `_wp_mcp_ai_skills*`, `_wp_mcp_ai_tool_shortcuts*`); wire assistant-config consumption into the chat flow (today `ChatController` hardcodes `assistant_id = 0` — "Not tied to a specific assistant post"); and port credential issuance (`_wp_mcp_ai_credentials` via `WP_MCP_AI_Credentials`) or record an explicit deferral of token scoping. Exit gate: a standalone chat turn with an `assistant_id` resolves that assistant's system prompt/model/tools |

## Wave E — Operator runtime → `nvoos-content-graph-ai-platform`

| # | Subsystem | Status | Notes |
|---|---|---|---|
| E1 | Workflow engine + CPTs + triggers | 🟡 | Sub-cluster 1 (CPTs) landed: `WorkflowCpt` ported (`src/Workflows/WorkflowCpt.php`): byte-identical `mcp_ai_workflow` CPT args (hidden-menu UI, `manage_options` caps, title/editor/revisions/custom-fields supports), graph/version/tags meta keys + sanitize/auth callbacks, graph read/write with malformed-JSON fallback, the portable export/import JSON contract (`schema_version` 1.0), semver bumping. `WorkflowRunCpt` ported (`src/Workflows/WorkflowRunCpt.php`): byte-identical `mcp_ai_workflow_run` CPT args, ten run meta keys, run lifecycle (create → append events → status transitions with terminal `finished_at` stamping), event-log read path, full run-record shape, four-dimension budget check + `wp_mcp_ai_workflow_run_budget_exceeded` action. `WorkflowTriggerCpt` ported (`src/Workflows/WorkflowTriggerCpt.php`): byte-identical `mcp_ai_trigger` CPT args, five trigger meta keys, enabled-trigger discovery, all six trigger-type hook bridges (`post_status_change`, `cron_schedule`, `a2a_inbound`, `user_registration`, `comment_published`, `file_upload`), fire path (`last_fired_at` stamp + `wp_mcp_ai_trigger_fired` + dispatcher/engine hand-off); execution hand-off resolves per install mode (base `WP_MCP_AI_Workflow_Dispatcher`/`Workflow_Engine_V2` monolith — boot-gated probes — platform `Workflows\Dispatcher`/`WorkflowEngine` once the later sub-clusters port; null → documented no-hand-off degradation standalone until then). Standalone-only wiring via `Plugin::registerWorkflowCpts()` (init priorities 12/13/14/20 — the base loader owns the same registration monolith); inline meta schemas promoted to named constants (additive); 25 characterization tests green in both matrices (real posts, all six trigger bridges exercised). Sub-cluster 2 (dispatcher) landed: `Dispatcher` ported (`src/Workflows/Dispatcher.php`): byte-identical `wp_mcp_ai_workflow_executor` filter contract (null = defer, array|WP_Error = ownership), the default-executor fallback chain (Engine V2 when available and enabled), and the `no_workflow_executor` error envelope; the default executor resolves per install mode (`engine_class()` seam — base `WP_MCP_AI_Workflow_Engine_V2` monolith boot-gated / platform `Workflows\WorkflowEngine` once the engine sub-cluster lands; null → documented `no_workflow_executor` degradation standalone until then); `is_enabled()` method_exists-guarded (documented hardening); static utility, no hooks of its own — `WorkflowTriggerCpt::dispatcher_class()` resolves it standalone; 7 characterization tests green in both matrices. `TriggerRegistry` ported (`src/Workflows/TriggerRegistry.php`): byte-identical singleton lifecycle, the `register()` contract (sanitize_key'd type + wp_parse_args defaults), the `get_triggers()`/`get_trigger()` lookups, the `wp_mcp_ai_register_workflow_triggers` extension action, and the seven built-in trigger definitions with their labels/descriptions/schemas; lazy singleton, no hooks of its own — the trigger admin/REST surfaces (E-UI-2) consume it; 6 characterization tests green in both matrices. `WorkflowEngine` ported (`src/Workflows/WorkflowEngine.php`): byte-identical `wp_mcp_ai_workflow_v2_enabled` gate (default true), `manage_options`/post/CPT guards, `wp_mcp_ai_workflow_v2_before_execute|after_execute` lifecycle actions, `wf2-` run identifiers, durable run records (create → running → step events → terminal status), graph → node-description → `execute_workflow` tool delegation with the byte-identical argument shape, the no-tool/no-registry degradation branches, and the result envelope; workflow/run CPT classes + the tool registry resolve per install mode (base classes monolith boot-gated / platform `WorkflowCpt`+`WorkflowRunCpt` + the AI addon's `CoreBridge` tools standalone — the established `QueueManager` seam); static utility, no hooks of its own — `Dispatcher`'s `engine_class()` + `WorkflowTriggerCpt`'s fallback resolve it standalone; 10 characterization tests green in both matrices (fake-tool delegation chain, run-record roundtrip, budget forwarding); the dispatcher + trigger seam expectations updated to the new engine resolution. `PatternConstants` + `PatternWorkflowTemplates` ported (`src/Workflows/`): byte-identical eight pattern slugs + catalog/validity/description helpers, the eight-template catalog with names/roles/step shapes, `get_workflow_template()`/`get_all_templates()` lookups, the `customize_template()` contract (custom-role merge + `wp_mcp_ai_customize_workflow_template` filter, no mutation), and the toolkit-recommendation path; the toolkit registry resolves per install mode (`get_toolkit_registry()` seam — base `WP_MCP_AI_Toolkit_Registry::get_instance()` monolith boot-gated / null standalone until the Pro-tier registry ports, documented degradation; the base's direct `new` against the protected constructor is replaced by the singleton — documented hardening); 9 characterization tests green in both matrices. `AgenticWorkflowOptimizer` ported (`src/Workflows/AgenticWorkflowOptimizer.php`): byte-identical optimization constants (cache TTL/staleness, `opt-` key prefix, compression threshold, 90% quality bar), the metric collection lifecycle (`start_metrics_collection()` seeding, `record_metric()` with the non-numeric-set quirk, `get_metrics()`/`get_metric_stats()` shapes, the `wp_mcp_ai_metrics_collected` action), the tool-result cache cycle (`should_cache_tool_result()` + `wp_mcp_ai_cacheable_tool` allowlist filter, `cache_tool_result()`/`get_cached_tool_result()`/`check_tool_cache()` with the byte-identical singular/plural metric-key quirk, sanitized `opt-{md5}-{suffix}` transient keys), the gzip+base64 compression contract, the low-necessity iteration tracker (5-consecutive nudge, per-session counters, `reset_low_necessity_count()`), and `predict_optimal_iterations()` (95th percentile × 1.2, clamped 5–50, default 15, int/float return quirk); logging resolves per install mode (`logging_enabled()` — base `WP_MCP_AI_Admin_Settings` monolith is_callable-guarded / `wp_mcp_ai_settings` option standalone; `log_event()` — base `WP_MCP_AI_Logger` monolith, dormant standalone); `get_iteration_history()` dormant placeholder standalone (byte-identical); constructor-driven hooks only (consumers wire it with D-UI-6/E-UI-2) — no `Plugin.php` registration; 16 characterization tests green in both matrices. Remaining: — (E1 wave complete) |
| E2 | Queues/ops (jobs, DLQ, SLA, cron, notifier) | 🟡 | `AsyncJobQueue` ported (`src/Queues/AsyncJobQueue.php`): byte-identical schema/constants/cron hooks/error codes, minute interval registered (documented deviation — the base relies on an external registration), executor filter seam (`nvoos_content_graph_ai_platform/async_job_executors`) so E1 registers executors without touching the queue, dormant method_exists-guarded seams for the Action Scheduler bridge / DLQ / notifier / logger until those pieces port (documented hardening — the base's own calls target methods its DLQ/notifier classes don't expose); standalone-only via `Plugin::registerQueues()`; 19 characterization tests green in both matrices; drive-by fix: `AgentCapabilityBoundaryHooks` extracted to its own PSR-4 file (autoload gap caused a standalone registration fatal). `QueueManager` ported (`src/Queues/QueueManager.php`): byte-identical mode/priority constants, thresholds + estimates table, capability-flag semantics, deferred-result envelope, `wp_mcp_ai_before_tool_execute` filter (priority 5) + `wp_ajax_wp_mcp_ai_queue_status` action; RabbitMQ client + tool registry resolve per install mode (base classes monolith / AI addon's RabbitMqClient + CoreBridge standalone); standalone-only via `Plugin::registerQueueManager()`; 18 characterization tests green in both matrices. `JobQueueManager` ported (`src/Queues/JobQueueManager.php`): byte-identical `mcp_ai_concurrent_jobs` schema, priority/status constants, `DEFAULT_MAX_CONCURRENT`/`DEFAULT_JOB_TIMEOUT`, enqueue validation + callable extraction, atomic `SELECT … FOR UPDATE SKIP LOCKED` claiming with priority ordering, process-cycle envelopes (`success`/`at_capacity`/`no_pending_jobs`), retry-then-fail with `last_error`, stale-job timeout cleanup, stats + clear; custom-table only — the base's deprecated option-fallback storage intentionally dropped (documented deviation); seams: `default_max_concurrent()` (base `WP_MCP_AI_Resource_Manager` monolith / `DEFAULT_MAX_CONCURRENT` standalone), `sla_available()`/`dead_letter_available()`/logging dormant until their owning E2 pieces port; 13 characterization tests green in both matrices (standalone + monolith on WP 6.9 + 7.1) with real DDL. `DeadLetterQueue` ported (`src/Queues/DeadLetterQueue.php`): byte-identical `mcp_ai_dead_letters` schema, type/limit/retention constants (incl. the `TYPE_MESH_QUERY`-not-valid quirk), error codes, item shapes, stats shape, `wp_mcp_ai_dlq_item_added` action, `wp_mcp_ai_dlq_retention_days` filter + `wp_mcp_ai_dlq_cleanup` cron; custom-table only (legacy option storage dropped — documented deviation); retry dispatch resolves per install mode (base manager/notifier/executor monolith — probes gated on the base plugin being booted, documented hardening — platform `JobQueueManager` standalone); `JobQueueManager`'s exhausted-failure forwarding now resolves the DLQ per install mode; standalone-only cron wiring via `Plugin::registerDeadLetterQueue()`; 23 characterization tests green in both matrices with real DDL. `RateLimitManager` ported (`src/Queues/RateLimitManager.php`): byte-identical transient prefix, delay/retry constants, backoff multiplier, `wp_mcp_ai_rate_limit_*` filters, error codes, retriable status/timeout tables, Retry-After resolution, and the `execute_with_retry()` loop (sleep byte-identical — callers control wall-clock via delay options); logging through a dormant seam; no hooks of its own; 21 characterization tests green in both matrices. `SchedulerBridge` ported (`src/Queues/SchedulerBridge.php`): byte-identical `wp_mcp_ai_run_async_job` runner hook + `wp-mcp-ai-jobs` default group, availability contract (`as_enqueue_async_action` presence + `wp_mcp_ai_async_scheduler_bridge_enabled` filter), idempotent `register_hooks()`, group filter with empty-value fallback, enqueue semantics (action ID / `false` = WP-Cron fallback); `run_job()` resolves the executing queue per install mode (base `WP_MCP_AI_Async_Job_Queue` monolith — boot-gated probe — platform `AsyncJobQueue` standalone); `AsyncJobQueue`'s dormant bridge seams now resolve per install mode (`scheduler_bridge_available()` + `scheduler_bridge_class()`), so queued jobs fast-dispatch through the resolved bridge; 16 characterization tests green in both matrices (AS stub when Action Scheduler is absent / real AS API when present), PHPCS-clean. `OutboundWebhook` ported (`src/Queues/OutboundWebhook.php`): byte-identical `wp_mcp_ai_outbound_webhooks` option, subscription lifecycle (`whk_*` lowercase IDs, sanitized URL/events, enabled flag), signed non-blocking dispatch (JSON body, `sha256=` HMAC header pair, event/delivery headers), inbound signature verification via `hash_equals`, and the four core event listeners (`wp_mcp_ai_workflow_run_completed|failed|paused`, `wp_mcp_ai_approval_requested`); standalone-only listener registration via `Plugin::registerOutboundWebhook()` (boot-gated — the base loader owns the same registration monolith); 11 characterization tests green in both matrices (HTTP intercepted via `pre_http_request`), PHPCS-clean. `CronManager` ported (`src/Queues/CronManager.php`): byte-identical `wp_mcp_ai_cron_jobs` option, argument normalisation (positional vs wrapped associative), record/get/remove lifecycle with idempotent re-record (created_at + first_timestamp preserved), WP-Cron unschedule for single + recurring events, retention-window pruning (`cron_job_retention_period`, default 24h, zero-retention immediate removal, missing/zero first-timestamp pruning), stable `md5( wp_json_encode() )` job IDs with the public static wrapper, legacy-entry normalisation on load, corrupted-option reset; standalone-only `init` prune hook via `Plugin::registerCronManager()` (boot-gated); retention setting resolves per install mode (base `WP_MCP_AI_Settings_Registry` monolith / direct `wp_mcp_ai_settings` option read standalone — identical effective behavior); 22 characterization tests green in both matrices, PHPCS-clean. `SlaManager` ported (`src/Queues/SlaManager.php`): byte-identical tier/priority/SLA-target/concurrency constants, capability-flag tier inference (`sla_tier` explicit → realtime/interactive/ui-blocking → background-only/long-running → async/may-timeout), Little's Law capacity math, queue-metrics analysis, tuning recommendations (metric reads `isset`-guarded — documented hardening over the base's unconditional reads), compliance tracking/statistics (same `wp_mcp_ai_sla_compliance_log` option, 1000-entry cap, 24h window + tier filter, percentile interpolation), dashboard data + health grades; settings keep the base option key (`wp_mcp_ai_settings`: `sla_prioritization_enabled`, `sla_*_concurrent`) so tuning survives mode transitions; queue-stats seam resolves per install mode (base `WP_MCP_AI_Job_Queue_Manager` monolith / platform `JobQueueManager` standalone, base probe gated on `defined('WP_MCP_AI_PATH')`); `JobQueueManager`'s dormant SLA seams now resolve per install mode via `sla_class()` (base monolith / platform `SlaManager` standalone, boot-gated probe) and `sla_available()` follows the enabled flag through the resolved class; static utility, no hooks of its own; 31 characterization tests green in both matrices (monolith + standalone on WP 6.9) incl. real-DDL enqueue tier→priority wiring, PHPCS-clean. `JobNotifier` ported (`src/Queues/JobNotifier.php`): byte-identical cache prefix/duration (`wp_mcp_ai_job_status_*`, 3600), webhook option key (`wp_mcp_ai_job_webhooks`), per-job webhook/step caps (10/50), lifecycle hooks (`wp_mcp_ai_job_started|progress|completed|failed` + crawl4ai/web-search/veo adapters), error codes (`invalid_webhook_url|scheme`, `private_ip_blocked`, `webhook_validation_failed`, `invalid_job_id`, `too_many_webhooks`), the `wp_mcp_ai_tool_sla_tier_map` filter, retry transients (`wp_mcp_ai_webhook_retry_*` → DLQ at 3 attempts), Little's Law metrics, the dot-preserving cache-key sanitisation, and the `wp_mcp_ai_emit_sse_event` action (gated on `WP_MCP_AI_SSE_ACTIVE`); the hourly `wp_mcp_ai_cleanup_job_cache` cron registers through a static `schedule_cleanup()` method (documented deviation — same event, no global function); collaborators resolve per install mode (base cron manager / DLQ / job store monolith — boot-gated probes — platform `CronManager` + `DeadLetterQueue` standalone; job store + logger dormant standalone; webhook secret via the AI addon's `ApiKeyStore` standalone); standalone-only hook registration via `Plugin::registerJobNotifier()`. `JobNotifierRestController` + `SseStream` ported (`src/Rest/`): byte-identical `mcp-ai/v1` jobs stream/status/webhooks routes, parameter schemas, permission envelopes (`wp_mcp_ai_missing_credentials`, `rest_invalid_nonce`, `rest_forbidden`, `job_not_found`, `unauthorized`), entity-ownership authorization model, the buffered SSE framing (connected/status/complete/close + heartbeat comments + `wp_mcp_ai_sse_stream_started|chunk_sent|ended` hooks), parameter clamping (10–600 s / 1–30 s), and CORS resolution; authenticator degrades standalone (`wp_mcp_ai_auth_unavailable` 503 for mesh/bearer/guest branches — nonce auth works in both modes); security headers monolith-only (documented); standalone-only route registration via `Plugin::registerJobNotifierRest()`. 54 characterization tests green in both matrices (35 notifier + 19 REST, real-DDL DLQ forwarding). Remaining: — (E2 wave complete) |
| E3 | Approvals | ✅ | `ApprovalQueue` ported (`src/Approvals/ApprovalQueue.php`): byte-identical `mcp_ai_approval` CPT + args (hidden, `title`+`custom-fields` supports, no rewrite/query_var/REST), meta keys (`_wp_mcp_ai_approval_*`), error codes (`approval_missing_tool|not_found|already_resolved|forbidden`), TTL floor (60 s, default 24 h), post-status → approval-status mapping (`pending`/`publish`→approved/`private`→denied/`trash`→expired), resolved-by/resolved-at/note audit meta, and the `wp_mcp_ai_approval_queued|approved|denied` + `wp_mcp_ai_approvals_cleanup_done` actions (cleanup payload byte-identical: the full expiry-probe result, not just the trashed subset); weekly `wp_mcp_ai_approval_cleanup` cron; standalone-only wiring via `Plugin::registerApprovals()` (CPT priority 11, cron priority 1 — the base loader owns the same registration monolith); literal meta keys/limits promoted to named constants (additive only); 18 characterization tests green in both matrices (real posts, both modes). Remaining: approvals REST controller + admin page land with E-UI-2 |
| E4 | Tenant + integrations (OAuth, Google Calendar, site-builder, conversation-import) | 🟡 | Sub-cluster 1 (tenant) landed: `TenantContext` ported (`src/Tenant/TenantContext.php`): byte-identical singleton lifecycle, the four-source resolution order (header `X-WP-MCP-AI-Tenant` → user meta `_wp_mcp_ai_tenant` → assistant meta `_wp_mcp_ai_bound_tenant` via `assistant_id` → multisite blog ID), the per-source error codes, the fail-closed `tenant_not_resolved` envelope, request-lifetime caching, and the set/accessor contract. `TenantRepository` ported (`src/Tenant/TenantRepository.php`): byte-identical context binding (sanitize_key/absint), strict-mode flag, the prepared `tenant_where()` fragment with the `1=1` bypass, the fail-closed `require_tenant()` RuntimeException, the `tenant_meta_query()` clause, and `save_tenant_meta()` post stamping. `TenantOptions` ported (`src/Tenant/TenantOptions.php`): byte-identical `wp_mcp_ai_{type}_{id}_{key}` scoped + `wp_mcp_ai_{type}_{key}` type-level key formats, get/update/delete wrappers with autoload pass-through, `from_context()` null-on-unresolved factory; resolves this package's `TenantContext` (documented deviation). `TenantDatabase` ported (`src/Tenant/TenantDatabase.php`): byte-identical `mcp_ai_tenants` + `mcp_ai_tenant_user_map` dbDelta schemas, `1.0.0` version + `wp_mcp_ai_tenant_db_version` gate, cached `tables_installed()` SHOW TABLES probe, drop_tables() uninstall path, assign_user()/get_user_tenants() with the primary user-meta side write; standalone-only hook registration via `Plugin::registerTenant()` (the base tenant init.php owns the same `admin_init`/`wp_mcp_ai_activate` hooks monolith). `TenantFeatureFlags` ported (`src/Tenant/TenantFeatureFlags.php`): byte-identical option keys, `WP_MCP_AI_TENANT_ISOLATION` constant override, settings-toggle read, global-on→opt-out / global-off→opt-in toolkit resolution, `require_isolation()`, the LIKE-based enabled-toolkit scan. `TenantMigration` ported (`src/Tenant/TenantMigration.php`): byte-identical tenant column/index DDL, INFORMATION_SCHEMA column probe, SHOW INDEX probe, CPT meta migration, tenant_id=0 backfill. `TenantBootstrap` ported (`src/Tenant/TenantBootstrap.php`, wraps the base init.php): byte-identical hook surface — `rest_api_init` REST-meta registration (`_tenant_type` string / `_tenant_id` integer on public post types, `edit_posts` auth), conditional admin Tenant column, `pre_get_posts` read filters (broad 10 + scoped-only 15), the `wp_mcp_ai_tenant_scoped_post_types` filterable registry (36 slugs), `save_post` 20 auto-stamp, the 18-table migratable registry, and the `wp_mcp_ai_after_plugin_upgrade` bulk migration; global functions → static methods (documented deviation); standalone-only via `Plugin::registerTenant()`. `TenantCliCommand` ported (`src/Tenant/TenantCliCommand.php`): byte-identical `wp mcp tenant` list/create/assign/migrate/status/toggle surface; self-registers on autoload under the `WP_CLI` gate (documented deviation). 78 characterization tests green in both matrices (7 files; real-DDL suites suspend the harness TEMPORARY-table rewrite); full platform suite 588 tests, 0 failures, 0 risky in both matrices. Sub-cluster 2 (OAuth) landed: `OAuthManager` ported (`src/Integrations/OAuthManager.php`): byte-identical `admin_init` callback router (`wp_mcp_ai_oauth` query param), the Gmail/Drive/Yahoo/Calendar start handlers (nonce + capability + credentials gates, `wp_generate_uuid4` state with `wp_mcp_ai_{service}_oauth_state_{md5}` transients, League OAuth2 client with manual-URL fallback), the callback handlers (state consumption, token exchange, profile fetch, settings persistence, redirect envelopes), the disconnect handlers, the `allowed_redirect_hosts` filter, the manual Google authorize-URL builder, and the manual token-exchange helper with its `token_exchange_failed|rejected`/`invalid_token_response` error codes; settings access resolves per install mode (`settings()` seam — base `WP_MCP_AI_Admin_Settings::get_settings()` monolith boot-gated / `wp_mcp_ai_settings` option standalone); the Google Calendar flow delegates to the base `WP_MCP_AI_Google_OAuth_Service`+`WP_MCP_AI_Google_Calendar_Scopes` monolith (boot-gated probes) and degrades standalone with a documented `calendar_error` redirect until the calendar sub-cluster ports. `GithubOAuthHandler` ported (`src/Integrations/GithubOAuthHandler.php`): byte-identical endpoints/scopes, start/callback flows with the `wp_mcp_ai_github_oauth_scope|authorize_endpoint|token_endpoint` filters, the `wp_mcp_ai_github_state_{md5}` transient, `/user` profile fetch, the `wp_mcp_ai_github_oauth_redirect_terminate` test filter, and the settings-error notice envelope; settings/log/sanitizer resolve per install mode (base `WP_MCP_AI_Admin_Settings`/`WP_MCP_AI_Admin_Settings_Base` monolith boot-gated / option read + dormant log + pass-through sanitizer standalone — documented). `MetaOAuthHandler` ported (`src/Integrations/MetaOAuthHandler.php`): byte-identical endpoints/scopes, start/callback/disconnect flows with the Meta scope/endpoint filters, the `wp_mcp_ai_meta_state_{md5}` transient, GET token exchange + `/me` profile fetch, and the `redirect_and_exit()` blocked-redirect contract (WPDieException under PHPUnit); same per-mode seams; monolith quirk preserved: the legacy defaults-driven sanitizer drops `meta_connected_user_name|id` (asserted per mode). `MailjetOAuthHandler` ported (`src/Integrations/MailjetOAuthHandler.php`): byte-identical endpoints, start/callback/disconnect flows, the `wp_mcp_ai_mailjet_oauth_state_{state}` transient, the `is_connected()`/`get_access_token()` contract with the 60-second expiry buffer and refresh path (`wp_mcp_ai_mailjet_no_token|no_refresh_token|no_credentials|refresh_failed|invalid_refresh_response`); no hooks (byte-identical — the base wires only the webhook handler). `QuickbooksOAuthHandler` ported (`src/Integrations/QuickbooksOAuthHandler.php`): byte-identical endpoints/scopes, start/callback (incl. the `realmId` gate + Basic-auth token exchange)/disconnect flows, the `wp_mcp_ai_quickbooks_oauth_state_{state}` transient, the token contract + refresh path (`wp_mcp_ai_quickbooks_no_token|no_refresh_token|no_credentials|refresh_failed|invalid_refresh_response`), and the `admin_notices` transient display; standalone-only wiring via `Plugin::registerIntegrations()` (the base admin-settings component + github/meta/quickbooks init files own the same hooks monolith); 56 characterization tests green in both matrices (5 files; HTTP intercepted via `pre_http_request`); full platform suite 669 tests, 0 failures, 0 risky in both matrices. Sub-cluster 3 (Google Calendar) landed: `GoogleOAuthService` ported (`src/Google/GoogleOAuthService.php`): byte-identical endpoints/constants, the single-use user-bound `wp_mcp_ai_{service}_oauth_state_{md5}` state transient (delete-before-validate replay protection), the byte-stable redirect-URI builders, the offline/consent authorize-URL builder, the `allowed_redirect_hosts` filter, the authorization-code exchange, the refresh-token minting with the `wp_mcp_ai_google_access_token_{md5}` cache + 300 s margin, revocation, and the token-response parser with its `wp_mcp_ai_oauth_*` error codes. `GoogleCalendarScopes` ported (`src/Google/GoogleCalendarScopes.php`): byte-identical profile constants (minimal/standard/full, standard default), the eight scope URLs, the `wp_mcp_ai_google_calendar_scope_profiles` filter, profile normalisation, the %20-healing grant parser, the implied-scope map, the empty-grant-allows `has_scope()`, and the `wp_mcp_ai_calendar_missing_scope` error builder. `GoogleCalendarClient` ported (`src/Google/GoogleCalendarClient.php`): byte-identical constants (API base, result caps, retry budget, backoff ceiling, channel TTL caps, `SYNC_FORBIDDEN_PARAMS`), the CalendarList/Events/FreeBusy/push-channel surface, `build_sync_params()` full-vs-incremental split, `clamp_max_results()`, token-driven `paginate()`, the authenticated retry loop, the three-way 410 discrimination, the 403/429/5xx retry classification, the filtered `wp_mcp_ai_google_calendar_retry_backoff` sleep, and the sync/auth probes. `GoogleCalendarCredentials` ported (`src/Google/GoogleCalendarCredentials.php`): byte-identical source constants, the connection → settings → filter resolution order, the Pro Remote Sites validation/decryption flow, the settings-level surface, the legacy filter surface, the lazy token-provider `make_client()`, the JWT bearer service-account minting, RS256 assertion signing, the IANA timezone fallback, and the scope/calendar-ID helpers; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator): base monolith collaborators monolith / this package's `Google\*` classes standalone, `WP_MCP_AI_Admin_Settings::get_settings()` monolith / `wp_mcp_ai_settings` option standalone, Remote Sites monolith-only (standalone → `wp_mcp_ai_calendar_pro_required`). `GoogleCalendarSync` ported (`src/Google/GoogleCalendarSync.php`): byte-identical constants (state option, sync/renew hooks, 6-hour interval, 365-day lookback, 40-page cap, 5-failure threshold), the per-target state store, the full/incremental `run()` with transparent `410 fullSyncRequired` downgrade and the `wp_mcp_ai_google_calendar_full_sync_required`/`wp_mcp_ai_google_calendar_synced` action envelopes, the paginated `execute_pass()`, the cancelled-exception-aware `classify_events()`, the jittered interval + site offset, the recurring safety-net scheduling, and the settings + Pro Remote Sites target enumeration (both surfaces monolith-only; standalone yields the `wp_mcp_ai_google_calendar_sync_targets` filter alone). `GoogleCalendarPush` ported (`src/Google/GoogleCalendarPush.php`): byte-identical constants (REST namespace/route, channels option, 7-day TTL, 1-day renewal threshold), the `rest_api_init` webhook route, the header-token permission callback, the high-water-mark-deduplicated notification handler, the HTTPS/public-host/private-range eligibility gate, the channel record lifecycle, the write-before-watch flow, the credential-loss-safe teardown, and the replacement-first renewal. `GoogleCalendarBootstrap` ported (`src/Google/GoogleCalendarBootstrap.php`, wraps the base init.php): byte-identical hook surface — the `cron_schedules` filter, the sync/renew cron actions, the push receiver, and the connection-gated initial scheduling; the base's global functions → static methods (`has_connection`/`run_sync`/`schedule_renewal`); standalone-only via `Plugin::registerGoogleCalendar()`. `OAuthManager` calendar seams upgraded to resolve the platform `Google\GoogleOAuthService`+`Google\GoogleCalendarScopes` standalone (monolith unchanged); 126 characterization tests green in both matrices (7 files; HTTP intercepted via `pre_http_request`, retry-backoff zeroed); full platform suite 795 tests, 0 failures, 0 risky in both matrices. Sub-cluster 4 (content-assistant) landed: `ContentAssistantMetabox` ported (`src/ContentAssistant/ContentAssistantMetabox.php`): byte-identical metabox ID, `add_meta_boxes` registration for the settings-driven post-type set, the `wp_mcp_ai_content_assistant_post_types`/`wp_mcp_ai_content_assistant_quick_actions` filters, the post-edit-screen asset enqueue with the `wpMcpAiContentAssistant` localization envelope, the permission/enabled/no-assistants render gates, the quick-action buttons, the chat modal markup, and the `mcp_ai_assistant` WP_Query enumeration; own assets (admin-content-assistant.css/.js) copied byte-identically into the platform assets tree and resolved per mode (`WP_MCP_AI_URL`/`WP_MCP_AI_VERSION` monolith / platform constants standalone); the chat-bundle block is monolith-only via the `defined( 'WP_MCP_AI_PATH' ) && class_exists( 'WP_MCP_AI_Shortcode' )` gate (bare class_exists resolves the classmapped base shortcode standalone, whose asset registration would fatal — documented degradation: standalone renders the selector/quick actions/modal without the embedded chat). `ContentAssistantBootstrap` ported (`src/ContentAssistant/ContentAssistantBootstrap.php`, wraps the base init.php): byte-identical `enable_content_assistant_metabox` gate (default-on) with the `wp_mcp_ai_content_assistant_enabled` filter; global function → static methods (`is_enabled()`/`register()`); standalone-only via `Plugin::registerContentAssistant()`. 22 characterization tests green in both matrices (1 file); full platform suite 817 tests, 0 failures, 0 risky in both matrices. Sub-cluster 5 (site-builder) landed: `SiteNodeInterface` ported (`src/SiteBuilder/SiteNodeInterface.php`): byte-identical node contract (slug/name/description/category, typed input/output port definitions, execute()). `SiteNodeRegistry` ported (`src/SiteBuilder/SiteNodeRegistry.php`): byte-identical singleton lifecycle, idempotent `init()` with the `wp_mcp_ai_register_site_nodes` action, the `wp_mcp_ai_default_site_nodes` filter, last-registered-wins registration, slug lookups, category filtering, the stable category→name front-end palette serialisation, `execute_node()` with the `wp_mcp_ai_site_node_not_found` envelope; the built-in node map resolves per install mode via `default_nodes()` (base `WP_MCP_AI_Site_Node_*` classes monolith / platform `SiteBuilder\Nodes\*` classes standalone) and `register_node()` validates through the per-mode `is_node_instance()` seam (fail-soft — documented hardening over the base's interface type-hint, whose fixed hint cannot accept monolith base nodes). `SitePipelineExecutor` ported (`src/SiteBuilder/SitePipelineExecutor.php`): byte-identical cache constants, Kahn topological sort with the `wp_mcp_ai_site_pipeline_cycle` envelope, incoming-edge resolution, static + edge-overlay input resolution with the multi-source array rule, `md5`-hashed ksort'd cache keys, the two-tier in-memory + transient cache, the empty/missing-slug/missing-dependency envelopes, and the SQL-backed `clear_cache()`/`clear_all_caches()`; `get_registry()` always resolves this package's registry (which handles the per-mode node resolution). `SiteBlueprintCompiler` ported (`src/SiteBuilder/SiteBlueprintCompiler.php`): byte-identical blueprint JSON contract, the `wp_mcp_ai_site_blueprint_directories` filter, sanitised-slug loading, required-key validation, listings/summaries, `{placeholder}` substitution, `{slug}__` ID/edge prefixing, the `wp_mcp_ai_site_blueprint_empty` envelope, in-memory cache; the base's `DEFAULT_DIR` constant becomes the per-mode `default_dir()` method (base `config/site-blueprints/` monolith / the platform's byte-identical blueprint copies standalone — a compile-time constant cannot reference a mode-dependent path). The three built-in nodes ported to `src/SiteBuilder/Nodes/` (`SiteNodeWpQuery` with the 100-post cap + seven-field summary shape, `SiteNodeTextBlock` with the 15-tag whitelist, `SiteNodeFlexContainer` with the three flex-property whitelists; `className` cast documented for strict_types). Blueprint JSONs copied byte-identically into `plugins/nvoos-content-graph-ai-platform/config/site-blueprints/`. No `Plugin.php` registration — byte-identical dormancy (the base ships no bootstrap for this subsystem; consumers call `SiteNodeRegistry::get_instance()->init()` directly). 50 characterization tests green in both matrices (4 files; e2e compile → execute for both shipped blueprints); full platform suite 867 tests, 0 failures, 0 risky in both matrices. Sub-cluster 6 (conversation-import) landed: all 16 base `includes/conversation-import/` classes ported to `src/ConversationImport/` (AdapterInterface, Conversation value object, FormatDetector, five adapters chatgpt/gemini/claude/sharegpt/openai_jsonl, Archive, Media, CctWriter, Manager, Deleter, Privacy, MemoryMiner, QueueBridge): byte-identical session-key/dedupe-hash computation, adapter extraction contracts (ChatGPT current-node branch resolution, Gemini HTML stripping, Claude sender/block mapping, ShareGPT role mapping, OpenAI JSONL prompt/completion fallback), the 128 MB detector cap with JSON/JSONL decoding, the zip-slip archive guard, media sideloading with the dedupe-preserving placeholder rewrite, the CCT record mapping (`import-{platform}` assistant IDs, provenance metadata), the run lifecycle (dry-run, skip/refresh policies, batch/limit caps, per-batch progress callbacks, checkpoint resume, the `wp_mcp_ai_conversation_import_completed` action), the deleter, the GDPR exporter/eraser, the memory-mining bridge, and the async queue bridge; per-mode seams throughout (`defined( 'WP_MCP_AI_PATH' )` discriminator — base JetEngine CCT / settings registry / mining tool / logger / async queue monolith, documented degradation envelopes standalone `wp_mcp_ai_import_jetengine_missing` / `wp_mcp_ai_import_mine_unavailable`; conversation boundaries via `is_conversation_instance()` since monolith flows base-adapter output); standalone-only wiring via `Plugin::registerConversationImport()` (Privacy + MemoryMiner bootstrap only — the base loader requires all 16 unconditionally and owns the same hooks monolith); 46 characterization tests green in both matrices (3 files: core/manager/privacy-deleter-queue); full platform suite 913 tests, 0 failures, 0 risky in both matrices. Remaining: — (E4 wave complete) |
| E5 | A2A REST receive routes | ✅ | `A2aRestController` ported (`src/Rest/A2aRestController.php`): byte-identical `mcp-ai/v1/a2a` route surface (JSON-RPC 2.0 POST, agent-card GET, per-assistant card GET, webhook receiver POST), parameter schemas, the A2A-Version header gate, the `a2a_disabled`/`a2a_version_not_supported` permission envelopes, the full JSON-RPC method router (`message/send|stream`, `tasks/get|list|cancel`, push-config CRUD, `agent/authenticatedExtendedCard`), and the JSON-RPC error map (−32601…−32009, default −32603); the A2A collaborator stack resolves per install mode (base `WP_MCP_AI_A2A_*` monolith — boot-gated probes — platform `A2A\*` classes standalone); auth resolves per mode (base authenticator + security manager monolith; standalone token branches degrade `wp_mcp_ai_auth_unavailable` 503, nonce auth works in both modes); chat pipeline + SSE handler monolith-only seams (`a2a_processing_error` degradation standalone — upgraded when the standalone chat flow lands with E1/D-UI-6); standalone-only route registration via `Plugin::registerA2aRest()` (request-level `a2a_disabled` gate byte-identical; the base's boot-time gate is documented as a deviation); 31 characterization tests green in both matrices (real tasks/cards/configs/webhook roundtrips). Closes the MIGRATION-GAPS deferred gap |
| E6 | Engine pieces (OOS/markup/paper-store/OKF/crawler) per decision D4 | ✅ | Decision D4 resolved (2026-09-06): fold into `nvoos-content-graph-ai` under the `Engine\` namespace — E6 ports land in the AI addon, not the platform (gap-analysis §10 updated). Sub-cluster 1 (OOS shadow) landed: `OosShadowRunner` ported (`src/Engine/OosShadowRunner.php`): byte-identical store constants (`wp_mcp_ai_oos_shadow_runs`, 100 cap, newest first, non-autoloaded), the `wp_mcp_ai_before_chat_request` priority-1 subscriber, the full gate chain (shadow enabled → engine flag off → real REST request → array payloads → sampling), same-request `handleChat` with deadline `CancellationToken`, the `shadow_mode`/no-stream option mutation, the parity record shape (incl. the `shadow: write-class tool suppressed` counter + `has_response`), the contained-failure error record, and the `oos_shadow_run` audit (monolith-only logger). `OosEngineFlags` ported (`src/Engine/OosEngineFlags.php`): byte-identical option keys/filters (`enable_oos_shadow`, `oos_shadow_sample_rate`, `enable_oos_engine`, `wp_mcp_ai_oos_shadow_*` filters, `WP_MCP_AI_OOS_ENGINE`, `X-WP-MCP-AI-Engine` header, `?engine=oos` probes) + the write-class classifier; global functions → static methods with `function_exists()` deferral (monolith byte-identical). `OosShadowSuppression` ported (`src/Engine/OosShadowSuppression.php`): byte-identical `tools/execute` waterfall (priority 20) with the synthetic `(shadow: write-class tool suppressed)` result — safety invariant 1; the base factory owns the same listener monolith; idempotent `register()` (documented hardening). `OosParityCommand` ported (`src/Cli/OosParityCommand.php`): byte-identical aggregate totals/rates + per-run diff table (`last` shorthand); CLI path `wp nvoos-cg-ai oos parity [diff <run-id>]` standalone-only (base owns `wp mcp-ai oos parity`); no `WP_CLI_*` symbols at class-load time per the `src/Cli/README.md` convention. Per-mode seams: orchestrator (`wp_mcp_ai_oos_orchestrator()` monolith / `CoreBridge::instance()->chat` standalone, deliberately untyped — base factory declares no return type), assistant config (base CPT monolith / empty standalone — byte-identical CPT-absent degradation), audit log monolith-only. Standalone-only wiring via `Plugin::registerEngine()` (+ CLI registration in `Cli::registerCommands()`); the runner hook is dormant standalone (no standalone emitter of `wp_mcp_ai_before_chat_request` yet — byte-identical dormancy). 17 characterization tests green in both matrices (1 file); full AI-addon ecosystem suite 628 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 2 (markup) landed: all 15 base markup classes ported to `src/Engine/Markup/` (`MarkupAwareToolInterface`; `MarkupRequest`/`MarkupResult` value objects; `MarkupElicitation` widget + `elicitation/create` envelope builders; `MarkupStore` transient storage with the per-assistant 16-request cap + daily cleanup cron; `MarkupValidator` W3C Web Annotation validation; `MarkupRasterizer` GD mask rasterization with structured-shape degradation; `MarkupLoopInterceptor` `wp_mcp_ai_pre_execute_tool` short-circuit; `MarkupRestController` fetch/submit routes; `MarkupAssets`; `MarkupAdminPage` URL-mode fallback; `MarkupTelemetry` outcome counters; `MarkupStatsSlashCommand` on `wp_mcp_ai_default_slash_commands_loaded` (supersedes the platform's dormant E2 blanket-port copy — documented); `MarkupTelemetryAdminPage` with parent-slug dormancy standalone (documented deviation); `MarkupBootstrap` wrapping the base `markup-init.php` hook surface): byte-identical constants/option keys/transient prefixes/error codes/hook names; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — base `WP_MCP_AI_Tool_Registry` + `WP_MCP_AI_Markup_Aware_Tool_Interface` monolith / `CoreBridge::instance()->tools` + this package's interface standalone; base asset constants monolith / addon constants standalone; audit logging monolith-only); the base's Konva widget assets copied byte-identically into the addon asset tree (local MIT bundle, no CDN); standalone-only wiring via `Plugin::registerEngine()` → `MarkupBootstrap::register()`; 60 characterization tests green in both matrices (3 files: core/loop-rest/telemetry-ui); full AI-addon ecosystem suite 688 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 3 (paper-store) landed: all 8 base `includes/paper-store/` classes ported to `src/Engine/PaperStore/` (`PaperDriverInterface`; `PaperJsonDriver` with the `id`/`type`/`title` required-field enforcement on read AND write, the recursive UTF-8 walk, `sanitize_key()` on `id`/`type`, the `created_at`/`updated_at` ISO-8601 timestamp contract, and the atomic-write delegation; `PaperIndex` with the `_indexes/{collection}.idx.json` structure, lazy `wp_parse_args`-healed load, the `wp_mcp_ai_paper_index_max_tags` filter applied on the rebuild path only (preserved base quirk), the flock non-blocking 3-second save, and the `wp_mcp_ai_paper_index_rebuilt` stats payload; `PaperRepository` with the `wp_mcp_ai_paper_record_before_save` filter, save-time defaults (type → collection, author_id → current user, status → `published`), immutable-field update protection, and the `wp_mcp_ai_paper_record_saved|deleted` actions; `PaperQuery` with the index-resolved/post-filtered clause split, the tags `IN` `reset( $value )` quirk, status `IN` merge, date-bucket resolution, loose comparisons, and immutable builder semantics; `PaperStoreManager` singleton with the `wp_mcp_ai_paper_store_root` filter (default `uploads/mcp-ai-wpoos/paper-store` — data survives mode transitions), security-file placement, the `wp_mcp_ai_paper_driver` filter, repository caching, the realpath-prefix `validate_path()` traversal guard, and `list_collections()`; `PaperStoreRemoteTrait` with the `pro_required`/`invalid_connection`/`disabled_connection` envelopes + `connection_id` schema fragment; `PaperStoreBootstrap` wrapping the base `paper-store-init.php` `wp_mcp_ai_bootstrapped` priority-30 tool-registration surface): byte-identical error codes/option-adjacent paths/hook names; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — base `WP_MCP_AI\Filesystem\WP_MCP_AI_Filesystem_Service` atomic I/O monolith / native `file_put_contents( LOCK_EX )` fallback standalone; base `WP_MCP_AI_Pro_Remote_Site_Manager` monolith / `pro_required` envelope standalone); the six base `paper_store_*` MCP tools + the paper-store REST controller remain base-owned until the Paper Store tool wave (documented deferral — the `register_tools()` seam resolves per mode as a documented no-op in both); standalone-only wiring via `Plugin::registerEngine()` → `PaperStoreBootstrap::register()`; 68 characterization tests green in both matrices (2 files: core/repository-query-bootstrap); full AI-addon ecosystem suite 756 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 4 (OKF) landed: all 5 base `includes/okf/` classes ported to `src/Engine/Okf/` (`OkfParser` with the byte-identical indentation-aware recursive-descent YAML-subset parsing + serialization; `OkfReader` with lazy concept caching, the lexical + symlink-aware realpath containment, browse index-or-scan, depth-clamped traversal, the v0.2 trust tiers + staleness helpers, multi-criteria search, and the advisory broken-link report; `OkfWriter` with the `okf_version: "0.2"` root-index stamping (v0.2 §12), the `.deleted.<timestamp>` soft-delete, index regeneration, the full v0.2 `validate_bundle()` report shape, log.md maintenance, and `ensure_bundle_root()`; `OkfBundleManager` as the single audited bundle resolver with the `wp_mcp_ai_okf_knowledge_root` filter (default `mcp-ai-wpoos/knowledge` — data survives mode transitions), realpath containment, the protected/standard bundle sets (`skill-knowledge` protected), listing/descriptors, create/rename/archive/delete, ZIP export/import with the ZipSlip + symlink + entry-count (5000) + size (25 MB) defenses, raw-concept saves with reserved-filename protection, and security-guard files; `OkfSkillKnowledgeGenerator` with the byte-identical `wp_mcp_ai_okf_skill_knowledge_generated` fingerprint option and the `wp_mcp_ai_bootstrapped` priority-32 `maybe_generate` hook, plus per-mode fingerprint + skills-source roots (base+Pro monolith byte-identical; addon root standalone — none shipped, so generation degrades with the byte-identical 'No bundled skills found' error without persisting the option); `OkfBootstrap` wrapping the base `okf-init.php` hook surface): byte-identical error codes/hook names/default paths; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — base `WP_MCP_AI\Filesystem\WP_MCP_AI_Filesystem_Service` atomic I/O monolith / native `file_put_contents( LOCK_EX )` fallback standalone); the ten base `okf_*` MCP tools remain base-owned until the OKF tool wave (documented deferral — `register_tools()` resolves per mode as a documented no-op in both); the base writer's bare-prefix containment quirk (a `../` concept ID writes into the parent directory; the reader refuses to resolve it back) is preserved byte-identically and characterized; standalone-only wiring via `Plugin::registerEngine()` → `OkfBootstrap::register()`; 61 characterization tests green in both matrices (2 files: core/bundle-manager incl. real ZipArchive round-trips and the monolith 74-skill generation); full AI-addon ecosystem suite 817 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 5 (crawler) landed: the base `includes/crawler/` coordinator ported to `src/Engine/Crawler/` (`Crawler` with the byte-identical `wp_mcp_ai_crawl4ai_job_` transient store + md5-hashed storage keys, the `wp_mcp_ai_crawl4ai_poll_task` cron hook with the `wp_mcp_ai_crawl4ai_auto_spawn_cron` filter, the `FILTER_VALIDATE_URL`-gated base-URL validation, the 30 s/600 s defaults with 5 s/60 s clamps, the `wp_mcp_ai_crawl4ai_poll_lock_` cooperative tick lock (30 s TTL), the inline-async-tick shutdown kick, interim progress persistence, the exponential-backoff retry path (30/60/120 s, 300 s cap, 3 retries, timeout-final), dead-letter forwarding, and the `wp_mcp_ai_crawl4ai_job_registered|completed|failed` + `wp_mcp_ai_crawl4ai_response` surfaces; `InlineAsyncTickTrait` ported alongside as the crawler's required collaborator — byte-identical two-layer tick lock, `DISABLE_WP_CRON`-aware loop decision, escape hatch, and observability action): byte-identical constants/transient prefixes/hook names/error codes; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — base `WP_MCP_AI_Admin_Settings` / `WP_MCP_AI_Tool_Run_Crawl4AI_Job::check_remote_task()` / `WP_MCP_AI_Crawl4AI_Local_API::cache_task_result()` / `WP_MCP_AI_Logger` / `WP_MCP_AI_Dead_Letter_Queue` / `WP_MCP_AI_Cron_Manager` monolith — settings option read + documented `wp_mcp_ai_crawl4ai_check_unavailable` error + dormant result-cache/log/DLQ/cron-manager standalone); the base `run_crawl4ai_job` validated tool + Crawl4AI tool family remain base-owned until the Crawl4AI tool wave (documented deferral); standalone-only wiring via `Plugin::registerEngine()` → `Crawler::init()`; 25 characterization tests green in both matrices (1 file; real transient/cron round-trips, HTTP intercepted); full AI-addon ecosystem suite 842 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 6 (oos-bridge wave-1 helpers) landed — E6 complete: the remaining oos-bridge flag helpers added to `OosEngineFlags` (`session_log_enabled()` / `session_telemetry_enabled()` with the byte-identical `enable_oos_session_log` / `enable_oos_session_telemetry` settings + filters; `canary_enabled()` with the `wp_mcp_ai_oos_canary` gate + `_wp_mcp_ai_engine` post-meta probe) and the five wave-1 service-bridge factories ported to `OosServiceBridges` (`src/Engine/OosServiceBridges.php` — `semantic_compressor()`, `data_budget_tracker()`, `erlang_c()`, `error_tracking()`, `cost_tracking()`): each defers to the base global function monolith (byte-identical — including the data-budget factory's discarded engine-enabled branch quirk, characterized in tests, and the per-factory `static $instance` caches) and resolves standalone bodies (the real `nvoos/core` + `nvoos/wordpress-adapter` implementations — the addon has no legacy engine path, documented; the error-tracking resolver is the exception: its framework adapter delegates to the base legacy singleton whose globals are absent standalone, so standalone resolves a native fallback carrying the adapter's own legacy-absent degradation semantics — `err_fallback_` ids, empty recent, 0.0 rate, `isEnabled` false); 12 characterization tests green in both matrices (1 file; contract + math + per-mode resolution incl. the data-budget quirk); full AI-addon ecosystem suite 854 tests, 0 failures in both matrices, PHPCS-clean. Remaining: the six oos-bridge wave-2 factory bridges (`wp_mcp_ai_oos_model_catalog` / `token_budget` / `file_validation` / `file_upload` / `file_orchestration` / `rate_limiter` from `includes/bootstrap/oos-bridge-wave2.php`) stay base-owned pending their consumer ports (not in the E6 tracker scope) |

## Wave E-UI — Operator admin UI → platform

| # | Subsystem | Status | Notes |
|---|---|---|---|
| E-UI-1 | Dashboards (multi-agent, orchestration, slash-commands, run timeline) | 🟡 | Sub-cluster 1 (multi-agent) landed: `MultiAgentDashboard` ported (`src/Admin/Dashboards/MultiAgentDashboard.php`): byte-identical `PAGE_SLUG` (`mcp-ai-multi-agent`), `NONCE_ACTION` (`wp_mcp_ai_multi_agent`), AJAX action names (`wp_ajax_wp_mcp_ai_get_multi_agent_stats` / `wp_ajax_wp_mcp_ai_reinstall_agents`), menu/enqueue/render registration, the admin-gate render (`wp_die` non-managers), the nonce+capability AJAX gates, the statistics shape, the workflow-pattern classification, and the reinstall flow; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — base `WP_MCP_AI_Default_Assistants` monolith / `false`+`WP_Error('wp_mcp_ai_default_assistants_unavailable')` standalone; meta-key map base `WP_MCP_AI_Assistant_CPT::META_*` monolith / AI addon `AssistantPostType::META_*` standalone (byte-identical values); JetEngine via `function_exists('wp_mcp_ai_is_jetengine_available')` deferral; the chat-bundle + test-chat modal are monolith-only — standalone renders agent cards without the Test button); the four operational dashboards mount as submenu pages under the platform's NV Platform menu (`PlatformDashboard::PAGE_SLUG`) — routing-review note: the base's tab/sub-tab/view routing lives in the settings dashboard and Pro orchestration settings, so these single-page section-composed surfaces do NOT replicate settings-level routing; the orchestration dashboard's own section view-routing lands with its sub-cluster; own assets (admin-multi-agent-dashboard.css/js + shared admin-monitor/admin-test-assistant) copied byte-identically into the platform assets tree; standalone-only wiring via `Plugin::registerDashboards()` (the base admin owns the same page monolith); 16 characterization tests green in both matrices (exposer fixture + AJAX capture harness); full platform suite 929 tests, 0 failures in both matrices, PHPCS-clean. Sub-cluster 2 (orchestration) landed: `OrchestrationDashboard` ported (`src/Admin/Dashboards/OrchestrationDashboard.php`): byte-identical `PAGE_SLUG` (`mcp-ai-orchestration`), `NONCE_ACTION` (`wp_mcp_ai_orchestration`), the six AJAX actions (`wp_ajax_wp_mcp_ai_run_orchestration_seeder|get_orchestration_stats|get_recent_workflows|execute_workflow|restart_workflow|refresh_memory_stats`), the `wpMcpAiOrchestration` localize envelope, the orchestration statistics shape (total/seeded/roles/with_task_patterns/seeder_version), the status banner thresholds (<50 warning, <90 info), the statistics cards, the system-status monitor (cron/async/health/SSE), the role distribution chart, the quick actions, the documentation links, the recent-workflows transient list, the agent memory stats widget (Phase 4a/4b/5: type/wing/importance breakdowns, wings/rooms, mined count, persistent CCT vs cache split, retrieval-path chart, RAG features, context health metrics) and the workflow execute/restart AJAX flows; per-mode seams (`defined( 'WP_MCP_AI_PATH' )` discriminator — profession meta keys base `WP_MCP_AI_Profession_CPT` monolith / platform `Professions\ProfessionCpt` standalone (byte-identical values); seeder base `WP_MCP_AI_Profession_Orchestration_Seeder` monolith / platform `Professions\ProfessionOrchestrationSeeder` standalone (same `seed_all()` contract); tool inventory base `WP_MCP_AI_Tool_Registry` monolith / AI addon `CoreBridge::instance()->tools` standalone normalized to slug/description entries; SSE probe base `WP_MCP_AI_SSE_Stream` monolith / platform `Rest\SseStream` standalone; context-health manager base `WP_MCP_AI_Agent_Context_Manager` monolith / null standalone (health block degrades away — documented); workflow coordinator base-owned and not yet ported — standalone `ajax_execute_workflow` degrades with the byte-identical 'Workflow coordinator not available.' envelope (documented); all `WP_MCP_AI_Logger` calls route through a dormant seam); the two base-settings cross-links resolve per mode via `settings_page_url()` (monolith `wp-mcp-ai-dashboard&tab=…` / standalone NV Platform dashboard page — documented); own assets (admin-orchestration-dashboard.css/.js) copied byte-identically; standalone-only wiring via `Plugin::registerDashboards()` (the base admin owns the same page monolith); 29 characterization tests green in both matrices (exposer fixture + AJAX capture harness + ctx/workflow transient namespace isolation); full platform suite 958 tests, 0 failures in both matrices, PHPCS-clean. Remaining: slash-commands, run-timeline sub-clusters |
| E-UI-2 | Managers (tool/token/cron/DAG/DLQ/approvals) | 🔴 | |
| E-UI-3 | Integrations screens | 🔴 | |

## Wave F — Pro runtime → pro addon(s) (decision D5)

| # | Subsystem | Status | Notes |
|---|---|---|---|
| F1 | pro-core (module registry, vault, vector-storage, skills-manager) | 🔴 | |
| F2–F6 | business / media / dev / healthcare / legal / education / content / data / platform toolkits | 🔴 | |
| F7 | Node-service bridges | 🔴 | |

## Wave F-UI — Pro UI + SPA v2

| # | Subsystem | Status | Notes |
|---|---|---|---|
| F-UI-1 | SPA v2 build pipeline in ecosystem npm workspace | 🔴 | |
| F-UI-2 | SPA v2 runtime port + loaders | 🔴 | |
| F-UI-3 | Pro toolkit blocks/shortcodes/inline-assistant + admin screens | 🔴 | |
| F-UI-4 | SPA v2 pro-toolkit slices | 🔴 | |

## Wave G — Release & distribution (release-gated)

| # | Item | Status | Notes |
|---|---|---|---|
| G1 | Ecosystem version bumps + `Requires Plugins` audit | 🔴 | |
| G2 | wp.org Base tier mapping (decision D6) | 🔴 | |
| G3 | Optional NEW bundle plugin | 🔴 | Never the base plugin (D-NOBASE) |
| G4 | Docs sweep + migration guide | 🔴 | |
