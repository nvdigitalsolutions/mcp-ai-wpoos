# Docs & Release Catch-Up Plan — v1.1.80 (PRs #6621–#6632)

## 1. Context: What changed since the last docs catch-up

The Sep 13 catch-up merged as `3bf4e2d8b7` ("chore: 1.1.79 docs catch-up (#6620)") and covered PRs #6609–#6619. **12 merged PRs** (#6621–#6632) and **7 build commits** landed afterward, none present on any release-note surface. The window is the **Shopify catalog-mode trilogy + CG Pro parity** (#6623/#6624/#6630), the **WP-CLI fatals + streaming** (#6625/#6626), the **assistant export/import across all surfaces** (#6628), the **Security Center Usage Monitor sub-tab** (#6632), the **WhatsApp webhook self-tests + ecosystem-port re-sync** (#6622), the **OKF editor save-context fix** (#6631), and three skill/docs PRs (#6621 docs-hub CSS, #6627 skill verification + brand-provisioning skill, #6629 skill-template WPCS).

> **Note on #6632:** the PR was open at pass start; it was squash-merged into `alpha-working` at the user's request (`2b654d994d`) so it is inside this window.

### Production-touching PRs

| PR | Production files | Change |
|---|---|---|
| #6622 | `addons/pro/includes/admin/class-wp-mcp-ai-pro-remote-sites-admin.php` + CG Pro port re-sync | **WhatsApp webhook self-tests** — verification-handshake replay, positive/negative HMAC-SHA256 signature validation, and the Graph API `subscribed_apps` subscription check (shadow-delivery detection) on the Remote Sites edit form; 8 new tests (45/45 WP 6.9 + 8/8 WP 7.1). CG Pro `src/admin/class-wp-mcp-ai-pro-remote-sites-admin.php` re-synced byte-identical (deviation documented: the WhatsApp webhook REST controller stays base-owned, so the two signature self-tests degrade with the byte-identical `rest_no_route` message standalone). |
| #6623 | `addons/pro/includes/class-wp-mcp-ai-shopify-client.php`, `-pro-remote-sites-admin.php`, `-shopify-sync-cct-manager.php`, `-shopify-sync-toolkit-settings-page.php` | **Shopify Catalog 401s fixed** — token transient capped at 60 min (Shopify can advertise a day-long `expires_in` while the JWT dies after 60 min), `read_global_api_catalog_search` scope validated from the token response, one-shot 401 purge-and-retry, and connection saves purge cached tokens for old + new client IDs. **JetEngine sync gate fixed** — `is_jetengine_active()` now accepts the `JET_ENGINE_VERSION` constant **or** `jet_engine()->get_version()` (bundled builds), and the System Status row uses the same gate with a distinct "version undetectable" warning. 161 tests green. |
| #6624 | `addons/pro/includes/class-wp-mcp-ai-shopify-client.php` (+ UCP constants/helpers), `-pro-remote-site-manager.php`, `-pro-remote-sites-admin.php`, `-shopify-sync-cct-manager.php`, **NEW** `addons/pro/includes/rest/class-wp-mcp-ai-ucp-agent-profile-controller.php` + CG Pro port of all four | **Shopify Storefront Catalog (UCP MCP) mode** — keyless `storefront_catalog` connection mode with UCP JSON-RPC helpers (`tools/list` handshake, `search`/`lookup`/`get_product`, no result caching per UCP guidelines); sync rejected with `wp_mcp_ai_shopify_ucp_no_cct_sync`; new public `/wp-json/mcp-ai/v1/ucp/agent-profile` route serving the site's UCP platform profile (public by design, no secrets). CG Pro port byte-identical (Wave F2-E cluster). 14 monolith + 10 CG Pro characterization tests. |
| #6625 | `includes/cli/class-wp-mcp-ai-cli-base-command.php`, `class-wp-mcp-ai-cli-chat-command.php` | **WP-CLI fatals fixed** — `format_output()` passed an inline array literal to `WP_CLI\Formatter::__construct()` whose first parameter is by-reference (PHP 8 fatal on every base-class command incl. all Pro CLI); now uses by-value `WP_CLI\Utils\format_items()` with field derivation. `wp mcp-ai chat` constructed the language-model router with zero arguments (`ArgumentCountError`); new `get_model_router()` resolver prefers the DI container and falls back to direct construction. |
| #6626 | `includes/cli/class-wp-mcp-ai-cli-chat-command.php` | **`wp mcp-ai chat --stream` now streams** — native cURL SSE path for the 9 raw-SSE providers (`stream_callback` forwards `delta.content` as it arrives), simulated 50-char/10ms chunking fallback, both paths honor the `wp_mcp_ai_disable_native_streaming` / `wp_mcp_ai_native_streaming_providers` filters, `stream: true` only sent with an active callback, and Gemini content-part arrays are flattened to text. |
| #6628 | **NEW** `includes/assistants/class-wp-mcp-ai-assistant-portability.php`, `includes/admin/class-wp-mcp-ai-admin-assistant-portability.php`, `includes/rest/class-wp-mcp-ai-rest-assistant-portability-controller.php`, **NEW** `includes/tools/class-wp-mcp-ai-tool-{export,import,duplicate}-assistant.php`, **NEW Pro** `addons/pro/includes/tools/orchestration/class-wp-mcp-ai-tool-export-assistant-blueprint.php`, `includes/cli/class-wp-mcp-ai-cli-assistant-command.php`, `includes/admin/export/class-wp-mcp-ai-export-provider-assistants.php`, `includes/bootstrap/loader.php`, `includes/class-wp-mcp-ai-rest.php`, tool registry + toolkit metadata + recommendations | **Assistant export/import across all surfaces** — canonical `WP_MCP_AI_Assistant_Portability` engine with a versioned `nvoos-assistant` JSON bundle (format_version 1): WP-CLI `assistant export|import` rewritten (legacy files still import; the old export lost `_wp_mcp_ai_*` config — fixed), REST `POST /mcp-ai/v1/assistants/export|import` (admin-only, schema-validated, 2 MB cap, dry-run), admin row/bulk actions + Import/Export page, and **3 base tools** (`export_assistant`, `import_assistant`, `duplicate_assistant`) + **1 Pro tool** (`export_assistant_blueprint`). Credential hashes never exported and stripped from imports (filterable denylist); the backup export provider now shares the engine denylist (it previously included credential hashes). 34 new tests + 105 regression tests green on WP 6.9/7.1. |
| #6630 | Pro Shopify client/manager/admin/sync/CCT + UCP controller; CG Pro parity repairs | **Shopify Global Catalog MCP mode** — keyless cross-merchant UCP search at `https://catalog.shopify.com/api/ucp/mcp` (50-result/50-id clamps, shared `ucp_rpc()`); REST `catalog_api` mode marked deprecated in the UI; the UCP profile field pre-fills with the site's own endpoint only when publicly reachable. **CG Pro parity restored** — Shop Domain input-value fix, the Catalog API 401 cluster (60-min TTL cap, purge-retry, save-handler invalidation), and the missing `global_catalog` wiring. +5 tests each side. |
| #6631 | `includes/admin/class-wp-mcp-ai-okf-bundle-manager-admin-page.php` | **OKF editor keeps its context on save** — `okfReload()` reloads the current URL instead of the bare page URL, so saves stay on the Editor tab with the same bundle/concept; concept deletion returns to the bundle's Browser tab (the concept file no longer exists). 20 tests / 56 assertions green. |
| #6632 | `includes/admin/sections/class-wp-mcp-ai-section-security.php`, `includes/admin/class-wp-mcp-ai-security-monitor-admin.php`, `includes/class-wp-mcp-ai-nefarious-usage-monitor.php`, `includes/rest/class-wp-mcp-ai-rest-security-center-controller.php` | **Security Center Usage Monitor sub-tab** — sixth Security sub-tab renders monitor status cards, emergency-shutdown recovery, editable monitor config (toggles/limits/threshold/pattern list), and a severity-tiered triage table of the violation log; the admin notice deep-links to it and shows the most recent violation type + message. **2 new REST endpoints** `POST /mcp-ai/v1/security/clear-violations` + `/clear-shutdown` (`manage_options` + cookie-auth nonce). **Bug fix** — `sanitize_monitor_settings()` only updates submitted keys (unrelated saves no longer silently disable the monitor). **Hardening** — malformed admin-edited patterns dropped at sanitize time and skipped at scan time; empty list restores defaults. Shared `get_violation_type_label()` / `get_violation_severity()` helpers. 80 tests / 435 assertions green. |

### Skills & docs-only

- **#6621** — docs-hub sub-project frontend: code-block strings white on the dark code background, heading-anchor `#` uses `color: inherit` (no version bump; 0.4.6 track unchanged).
- **#6627** — `design-ai-assistant-admin` skill rewritten to the code-verified surface (`_wp_mcp_ai_*` / `_wp_mcp_ai_peer_*` keys, real CLI commands, toolkit_cpt caveat, legacy-key quirk); `mcp-ai-wpoos-plugin` skill gains Design Stack deployment facts + WP-CLI provisioning recipe + fixed-fatal notes; **new skill `design-brand-assistant-provisioning`** (Aerlinn pattern); bookkeeping → 57 skills in `AGENTS.md` + `.github/copilot-instructions.md` (the `README.md` repo-map row was missed — folded in this pass).
- **#6629** — the brand-provisioning template's 67 WPCS errors fixed (phpcbf + hand fixes: `@package` tag, `$title`/`$post_id` renames, nowdoc → single-quoted prompt string, `esc_html()` on CLI output); skill docs kept in sync.

### Build-only (7 commits)

compiled assets/distributions ×5, SPA addon ZIPs ×1, **oOS WP.org packages v1.1.79 ×1** (7255abede9 — the 1.1.79 package builds landed in-window, built before #6625/#6626 merged).

### Key scope rules for this pass

- **Historical entries stay.** Old CHANGELOG/README per-version blocks (`[1.1.79]` and earlier) are history and are not rewritten — with one deliberate exception below.
- **Tool counts DO change this window.** #6628 registers **3 new base tools** (`export_assistant`, `import_assistant`, `duplicate_assistant`) and **1 new Pro tool** (`export_assistant_blueprint`). Figures move **~303 → ~306 base, ~1,265 → ~1,266 Pro (~1,568 → ~1,572 total)** with the live-registry caveat. The optional count-sweep commit (A3 §5) is included this pass.
- **CHANGELOG relocation (deliberate):** #6625/#6626 appended their entries "under 1.1.79" after the 1.1.79 release shipped (the 1.1.79 wp.org packages were built before those fixes merged), and #6632 added an `[Unreleased]` block. The 1.1.80 pass **moves the WP-CLI formatter/chat/stream blocks into `[1.1.80]`** and folds the `[Unreleased]` block into it — the released `[1.1.79]` block returns to what the 1.1.79 pass shipped (its historical content is otherwise untouched).
- **Skill count 57 → 58** — #6627 added `design-brand-assistant-provisioning` (bookkeeping landed in AGENTS.md + copilot-instructions; README.md repo-map missed) and #6628 added `mcp-ai-wpoos-assistant-portability` (no bookkeeping). Per AGENTS.md §6 this pass folds **58** (20 wp-* + 32 design-* + 6 mcp-ai-wpoos guides) into `AGENTS.md`, `.github/copilot-instructions.md`, and the `README.md` repo-map row.
- **Sub-project version tracks unchanged:** nvoos-content-graph **1.0.8**, nvoos-content-graph-ai **1.0.4**, nvoos-content-graph-ai-platform **2.0.0**, nvoos-content-graph-pro **1.0.0** (byte-identical port patches only — F2-E/F2-F/D-UI-7 tracker rows), Checkout API **0.1.2**, Docs Hub **0.4.6** (#6621 is a CSS fix, no bump), Comic Reader **0.5.0**, Media Worker **v3.2.0**.
- **Model catalog:** unchanged (**v2026.09.10**) — no model PRs in-window.
- **`@since` sweep:** no new anomaly groups — every file added this window is tagged `@since 1.1.80` (portability engine/admin/REST, 3 base tools, Pro blueprint tool, UCP profile controller on both sides); modified files carry only pre-existing OI-1 history.
- **Housekeeping executed this pass** — remove stale build ZIPs: all `build/**/*1.1.78*` (root 9 + optional-components 2 + toolkit-addons 19 = 30 files), superseded by the in-window 1.1.79 wp.org package builds. The 1.1.79 ZIP set is current, not stale.
- **Parked items stay parked** (OI-1/OI-2 in `docs-catch-up-open-items.md`): no `@since` reconciliation, no Docker live-count re-derivation. OI-2's "current figure" label refreshed to the v1.1.80 baseline.
- **Unrelated working-tree noise excluded** — untracked `docs/user-guides/assistants/CONTENT-GRAPH-MARKETING-ASSISTANT.md` (flagged in #6630 as unrelated), root `vendor/`, `package-lock.json` are never staged.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.80] - 2026-09-15` section above `[1.1.79]`: fold the `[Unreleased]` security-monitor block; relocate the #6625/#6626 WP-CLI blocks up from `[1.1.79]`; add Added (assistant export/import across all surfaces #6628 incl. 3 base + 1 Pro tools; WhatsApp webhook self-tests #6622; Shopify Storefront + Global Catalog UCP modes #6624/#6630; Security Center Usage Monitor sub-tab + clear REST endpoints #6632; `wp mcp-ai chat --stream` #6626), Fixed (WP-CLI formatter/chat fatals #6625; Shopify Catalog 401 + JetEngine gate #6623; monitor sanitize-clobber + malformed patterns #6632; OKF editor save context #6631), Skills/Sub-projects (#6621/#6627/#6629), Versioning (1.1.80; counts **~306 base + ~1,266 Pro (~1,572 total)**; catalog unchanged v2026.09.10; skill count **58**; stale 1.1.78 ZIPs removed).
2. **`readme.txt`** — Stable tag 1.1.80; new `= 1.1.80 - September 15, 2026 =` changelog paragraph (one paragraph, wp.org style).
3. **`README.md`** — version header → 1.1.80; Release Date 2026-09-15; new `🆕 v1.1.80 Highlights` paragraph; new "What's New at a Glance (v1.1.80)" section above v1.1.79; new "Latest Updates (v1.1.80 — September 2026)" block above v1.1.79; repo-map skill row 56/31 → **58/32** (+ design-brand-assistant-provisioning + mcp-ai-wpoos-assistant-portability); current-state count mentions → ~306/~1,266/~1,572 (lines 669/671/741).
4. **`docs/QUICK_REFERENCE.md`** — version + last-updated → 1.1.80/September 15; v1.1.80 entry above v1.1.79.
5. **`docs/DOCUMENTATION_INDEX.md`** — Plugin Version + Last Updated → 1.1.80/September 15; new SEPTEMBER 15 UPDATE banner above the v1.1.79 one; new `## 🆕 September 2026 — v1.1.80` section header + "New and updated documents" block.
6. **`docs/project/FOR_REVIEWERS.md`** — current version → 1.1.80 (September 2026); count mentions → ~1,572/~306/~1,266.

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed stamp + "New in v1.1.80" paragraph: **+3 base tools** (`export_assistant`, `import_assistant`, `duplicate_assistant`, PR #6628) and **+1 Pro tool** (`export_assistant_blueprint`) — new figures ~306 base + ~1,266 Pro (~1,572 total); #6625/#6626/#6631/#6632 add no tool slugs.
8. **`.context/security-checklist.md`** — v1.1.80 update line: #6628 credential-redaction denylist (hashes never exported, stripped from imports, backup provider now shares the denylist — previously it exported credential hashes); #6632 usage-monitor hardening (submit-key-only sanitize, malformed-pattern validation at sanitize + skip at scan, `manage_options` + nonce REST clear endpoints); #6622 WhatsApp webhook signature self-tests; Shopify UCP token handling (#6623 401 purge-retry + 60-min TTL cap, keyless UCP modes #6624/#6630).
9. **`.context/testing.md`** — last-reviewed + v1.1.80 note: portability 34 new + 105 regression (WP 6.9/7.1); Shopify suites 161 + storefront 14/10 (dual-matrix) + global-catalog +5 each; WhatsApp remote-sites 45/45 + 8/8; usage-monitor 80/435; OKF 20/56; CLI fixes validated live in the Design Stack (provider list, chat, --stream native + simulated).
10. **`.context/rest-api.md`** — v1.1.80 note: 4 new base REST routes — `POST /mcp-ai/v1/assistants/export|import` (admin-only, nonce/bearer, schema-validated, 2 MB cap, dry-run), `POST /mcp-ai/v1/security/clear-violations` + `/clear-shutdown` (`manage_options` + cookie-auth nonce); new public `GET /wp-json/mcp-ai/v1/ucp/agent-profile` (Pro, no secrets).
11. **`.context/README.md`, `.context/conventions.md`, `.context/pro-vs-base.md`** — count mentions → ~306/~1,266/~1,572.
12. **`CLAUDE.md`** — header stamp → September 15, 2026 · 2.33; v1.1.80 notes (portability engine + redaction, Shopify UCP modes, WP-CLI fixes + streaming, usage-monitor sub-tab, skill count 58); count mentions → ~306/~1,266/~1,572 (lines 20/53/57/189).
13. **`MAINTAINER_MAP.md`** — last-reviewed bump → September 15, 2026 (v1.1.80).
14. **`.bmad/README.md`** — last-reviewed bump → v1.1.80.
15. **All three `mcp-ai-wpoos-plugin` SKILL.md copies** — `plugin-version`/`plugin-version-tested`/`last-updated` → 1.1.80/2026-09-15 (the `.agents/skills/` copy also gains a v1.1.80 note section above the v1.1.79 block).
16. **`.agents/skills/mcp-ai-wpoos-updates/SKILL.md`** — frontmatter → 1.1.80/2026-09-15; body references (distilled-plan range, template-plan names, commit-structure mirror, references list) → v1.1.80.
17. **Skill-count bookkeeping (AGENTS.md §6)** — `AGENTS.md` §1 inventory row + coding-time-vs-runtime paragraph, `.github/copilot-instructions.md` multi-agent bullet + repo-tree comment: 57 → **58** (+ `mcp-ai-wpoos-assistant-portability`). (The README repo-map row is folded into P0-3; `design-brand-assistant-provisioning`'s missed README row lands there too.)

### P2 — Operational & feature docs

18. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" stamp → September 15, 2026 (v1.1.80); addon rows verified unchanged (no sub-project bumps in-window).
19. **`docs/reference/tools/tool-reference.md`** — Last Updated → September 15, 2026 (v1.1.80); Tool Count → ~306 base + ~1,266 Pro (~1,572 total) — +3 base +1 Pro from #6628.
20. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-15 (v1.1.80 pass); Scope line gains v1.1.80; OI-2 current-figure label → v1.1.80 (~306 + ~1,266 = ~1,572, delta: #6628 +3 base +1 Pro); no new open items this pass.
21. **`docs/project/plans/v1.1.80-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

22. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.80 (`package-lock.json` intentionally untouched).

### Count sweep (executed this pass — counts changed)

23. Grep `~303`, `1,265`, `1,568` across live surfaces and reconcile to ~306/~1,266/~1,572: `docs/getting-started/USE_CASES_AND_QUICKSTARTS.md` (prose + Rev table 3.1 row), `docs/getting-started/_USE_CASES_FACT_SHEET.md` (reconciled rows + guidance). Historical per-version blocks, plans, proposals, and archives stay.

### Housekeeping (executed this pass)

24. **Remove stale build ZIPs** — `git rm` all `build/**/*1.1.78*` (root 9 + optional-components 2 + toolkit-addons 19 = 30 files), superseded by the in-window 1.1.79 wp.org package builds. Commit: `chore: remove stale 1.1.78 build ZIPs [skip ci]`.

### Verify-only (no changes expected)

- Skill count after bookkeeping equals the actual **58** directories under `.agents/skills/` (20 wp-* + 32 design-* + 6 mcp-ai-wpoos guides).
- `addons/pro/README.md` — toolkit count stays 31 (the blueprint tool registers in the existing orchestration toolkit; no new toolkit).
- `docs/project/proposals/` — no new proposals in-window.
- `docs/project/ecosystem-port-tracker.md` — maintained by the port loop itself (#6622 F2-F6, #6624 F2-E, #6628 D-UI-7 rows landed in-window).
- `addons/docs-hub/CHANGELOG.md`, `plugins/nvoos-content-graph/CHANGELOG.md`, `addons/checkout-api/CHANGELOG.md` — own tracks; no sub-project bumps in-window (#6621 is a CSS fix without a bump).
- `docs/reference/models/*` — no model changes in-window.
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" description — base count 306 still satisfies "300+".

---

## 3. Execution log (2026-09-15)

- **Orientation** — window mapped from `3bf4e2d8b7` (1.1.79 catch-up merge #6620) to `2b654d994d` (#6632 merge): 12 merged PRs (#6621–#6632) + 7 build commits, no direct commits; all PR descriptions read in full via the GitHub API; classification recorded in §1. `@since` sweep on every added file found no new anomaly groups (all tagged 1.1.80). #6632 squash-merged at the user's request during orientation.
- **P3 / P0 / P1 / P2 / count sweep / housekeeping** — see §4 commits 1–6.

## 4. Commit breakdown (mirror v1.1.58–v1.1.79)

1. `chore: bump version to 1.1.80` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.80 changelog and release notes` — `CHANGELOG.md`, `readme.txt`, `README.md`, `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.80 changes` — `.context/*` (7 files), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies, `.agents/skills/mcp-ai-wpoos-updates/SKILL.md`, `AGENTS.md`, `.github/copilot-instructions.md`
4. `docs: reconcile 1.1.80 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/plans/docs-catch-up-open-items.md`, `docs/project/plans/v1.1.80-docs-catch-up.md`
5. `docs: reconcile remaining 1.1.80 tool counts` — `docs/getting-started/USE_CASES_AND_QUICKSTARTS.md`, `docs/getting-started/_USE_CASES_FACT_SHEET.md` (counts changed this window)
6. `chore: remove stale 1.1.78 build ZIPs [skip ci]` — `build/**/*1.1.78*` (housekeeping)

## 5. Validation

- `php -l` on every bumped PHP file
- `package.json` JSON parse
- `git grep "1\.1\.79"` sweep — remaining hits are intentional history (previous-release sections, `@since` docblocks, plans, archive)
- Count sweep — `git grep "1,568\|1,265\|~303"` reconciled on live surfaces; historical per-version blocks/plans/archive untouched
- Stale-ZIP sweep — `git ls-files 'build/*'` re-checked: no 1.1.78 files remain; the 1.1.79 set is current
- Skill-count verification — 58 directories under `.agents/skills/` == bookkeeping claims in AGENTS.md / copilot-instructions / README

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue #5968) and Docker-based live-count re-derivation (OI-2, issue #5967) stay parked. OI-2's current-figure label refreshed to the v1.1.80 baseline (~306 base + ~1,266 Pro, ~1,572 total; delta: #6628 +3 base +1 Pro).
- **No new items raised this pass.** The `@since` sweep found no new anomaly groups.
- **Noted observations (other workstreams):** #6630 advises rotating a dev client ID/secret for the deprecated REST Catalog API that was pasted in chat during debugging; the #6622/#6624/#6630 CG Pro ports and the #6628 D-UI-7 port cluster are the ecosystem-port loop's own tracker concerns; #6632's legacy `admin_post` clear handlers remain as fallbacks alongside the new REST routes.
- **Housekeeping executed this pass:** stale 1.1.78 build ZIPs removed (30 files).
