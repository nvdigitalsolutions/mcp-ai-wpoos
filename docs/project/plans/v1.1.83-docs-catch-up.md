# Docs & Release Catch-Up Plan — v1.1.83 (PRs #6677–#6723)

## 1. Context: What changed since the last docs catch-up

The Sep 18 catch-up merged as `1da29e73cb` ("chore: 1.1.82 docs catch-up (#6676)") and covered PRs #6645–#6674. **46 merged PRs** (#6677–#6684, #6686–#6723) and **23 build commits** landed afterward, none present on any release-note surface. The window is dominated by the **Tool Description Engineering program** — the model-facing usage-guidance sweep that touched every tool class in the base+pro trees (~1,487 tools across 1,584 files) — plus the **Pro bootstrap / Telegram / Gmail / skill / OKF hardening**, the **Upwork search & workflow-delivery series**, **npm advisory fixes**, and the **Playground Ollama demo permalink fix**.

### Production-touching PRs (non-sweep)

| PR | Production files | Change |
|---|---|---|
| #6677 | `addons/pro/mcp-ai-wpoos-pro.php` (+ module-registry require), telegram tool, Pro Gmail/Drive clients, base `search_gmail`, OKF bundle manager, skill tools; CG Pro + CG AI mirrors; `tests/test-pro-telegram-message-chunking.php` (new, 6) + extended `test-tool-load-skill.php`/`test-okf-bundle-manager.php` | **Pro bootstrap incomplete-install guard** — the unconditional require of `class-wp-mcp-ai-pro-module-registry.php` fataled the whole site on a partial deploy; now `file_exists`-checked with degradation to `wp_mcp_ai_pro_incomplete_install_notice` + a WP_DEBUG log line. **Telegram auto-chunking** — `send_telegram_message` splits >4,096-char messages at paragraph → line → hard boundaries (soft chunks keep `parse_mode`; hard splits drop it; mid-sequence failure returns `wp_mcp_ai_telegram_chunk_error` with failed index/count; `chunk=false` restores legacy single-send). **Gmail `connection_id="settings"`** resolves to the settings-based fallback across the four Pro Gmail tools, the Drive client, and base `search_gmail` (whose not-found error points at `list_gmail_connections`). **Skill/OKF self-correction** — `load_skill` not-assigned appends `Assigned skills: …`; `okf_bundle_not_found` appends `Available bundles: …` (new `list_bundle_names()`). |
| #6678 | `addons/pro/includes/tools/crm/upwork/class-wp-mcp-ai-tool-search-upwork-jobs.php` + CG Pro copy + `plugins/nvoos-content-graph-pro/tests/test-crm-tools-upwork.php` | **Upwork web_search-mode gating** — `has_valid_connection()` gains the mode + credential gates the sibling tools already had; `web_search` mode (or API mode without full OAuth credentials) no longer routes into the GraphQL API and dies on the missing refresh token. 3-branch regression test. |
| #6679 | result-delivery service (`envelope_data_to_text()`/`envelope_data_to_markdown()`), `search_upwork_jobs` (monolith + CG Pro), `upwork_job_discovery_scan` preset, skill docs (plugin-skill chat REST recipe + DuckDuckGo gotcha, test-suite pattern 48, design-crm) + `includes/bundled-skills/` sync; `tests/test-upwork-job-search-fallback.php` (new, 12) + 4 delivery-format tests | **Schedule result-delivery formatting** — workflow `full` deliveries render `steps` as a compact execution log (`1. Search for new matching jobs (search_upwork_jobs) — completed · 0.94s`, failed steps surface errors); null/empty values skipped. **Upwork search enhancements** — fallback drops category landing pages, extracts `job_type`/`budget`/`published` from SERP snippets, merges a second broad pass with URL dedupe; new `sort` (`recency`\|`best_match`) + `location` args wired to settings `default_location`/`default_sort`; derived `url`; preset stops sending the invalid `job_type: all` GraphQL filter. |
| #6680 | `search_upwork_jobs` (monolith + CG Pro), `WP_MCP_AI_Pro_Schedule_Manager::build_workflow_structured_response()` | **Fallback always runs when thin** — the broad second pass now always runs when the first pass yields fewer than `min(limit, 5)` jobs (previously skipped entirely with no keyword filters — the preset's default shape); primary query restricts to `site:upwork.com/freelance-jobs/apply`; extended category detection (`/apply/{category}/` without `~jobId`); generic recency tail without filters. **Digest enrichment** — item lines append budget/contract-type/recency with site-format dates. |
| #6681 | 8 npm lockfiles (root, saas-controller, pro, pro/assets/spa, media-worker) | **npm advisory fixes** — adm-zip 0.6.1 (CVE-2026-77301, override `>=0.6.1` + symlink-overwrite fix), js-yaml 4.3.2 (GHSA-2883-xcg3-v3hh), colord 2.10.0 (GHSA-2wm5-q62r-hmrv); hand-edited lock entries (version/resolved/integrity) to avoid whole-file rewrites; `npm install --package-lock-only --dry-run` up to date in all 5 directories. |
| #6682 | `WP_MCP_AI_Pro_Schedule_Manager::build_workflow_structured_response()` | **Full workflow results delivered** — the hard-coded `min( 5, count( $items ) )` item cap raised to **50** (the search tools' hard limit); new `wp_mcp_ai_pro_workflow_response_item_cap` filter; each item's URL ships as an indented line under the item. |
| #6683 | `blueprints/ollama-demo.php` + regenerated JSON, `bin/capture-real-page.sh`, docs (README "Try It on Your PC", `docs/user-guides/playground-demo.md`, `blueprints/README.md` Validation section, `docs/getting-started/QUICK_START_5_MINUTES.md`, playground-demos skill) + `.gitignore` (`/verify-out/`) | **Playground demo landing 404 fixed** — fresh installs default to plain permalinks, so `/ollama-test-lab/` 404'd; the seed now switches to `/%postname%/` and flushes rewrite rules. Local `npx -y @wp-playground/cli server` is now the documented primary test path; the capture harness waits for the lazy seed via the REST index and follows the auto-login 302 with a cookie jar. |
| #6684 | `addons/pro/includes/services/class-wp-mcp-ai-markdown-converter.php` + Teams webhook converter + `tests/test-pro-result-delivery-email-format.php` | **Digest ordered lists render 1–10** — both markdown→HTML converters only grouped consecutive numbered lines, so each indented URL line closed the list and every item rendered as "1."; ordered-list parsing now folds indented continuation lines into their list item with a `<br>`. |

### Tool Description Engineering (production-touching, the sweep)

| PR | What |
|---|---|
| #6686 | **Infrastructure** — `WP_MCP_AI_Tool_Usage_Guidance_Interface` (`when_to_use`/`when_not_to_use`/`related_tools`/`notes`); `WP_MCP_AI_Tool_Registry::get_model_facing_description()` assembles a `[Usage: …]` suffix on the model-facing payload (REST chat path, Tool Service `/tools`, `list_mcp_tools`); new `WPMCPAI.Tools.ToolDescriptionGuidance` sniff (advisory severity 0 at roll-out); 12-tool exemplar sweep; **adaptive tool cap** `WP_MCP_AI_Tool_Payload_Advisor` (40 tools ≤128K / 64 ≤256K / 100 above; opt-in site option `wp_mcp_ai_adaptive_tool_cap` + per-assistant three-state override in the Default Settings metabox; default off); **lazy schema loading** (`tool_slug` + `include_schemas` on `list_mcp_tools`); proposal + `docs/features/tool-description-guidelines.md`. |
| #6695 | **Legacy wrapper passthrough** — `WP_MCP_AI_Legacy_Tool_Wrapper` implements the guidance interface and forwards from a `get_usage_guidance()` method or a `usage_guidance` definition key (method wins) — legacy-format classes opt in without an interface migration. |
| #6687–#6723 | **Phase 2 sweep (37 PRs: 34 tool clusters + the #6695 wrapper passthrough + the #6723 final gate)** — guidance added to every base+pro tool class: base b1 37, b2 66, b3 66, b4 96; Pro p1 60, p2 60, p3 51, p4 44, p5 43, p6 13, p7 37, p8 35, p9 30, p10 31, p11 32, p12 24, p13 37, p14 23, p15 22, p16 44, p17 15, p18a 59, p18b 52, p19 58, p20 75, p21 63, p22 41, p23 23 (legacy passthrough), p24 39, p25 26, p26 34, p27 27, p28 25, p29 22, p30 19 (~1,487 tools; ~228 base + ~1,259 Pro). In-cluster collateral fixes: 5 regulatory tools migrated to the canonical `WP_Error` envelope (#6689); 14 pre-existing whitespace indentation errors in `ocr-pdf-text.php` (#6697); the sniff's abstract-class skip bounds inverted (#6700); 2 pre-existing WPCS errors in swept CRM files (#6707); a pre-existing parse error in `deidentify-health-record.php` (1-token fix, #6709); the string-based enumeration missed `generate-tool-scaffold` (#6713). |
| #6723 | **Final gate** — guidance sniff severity raised 0 → 5 in `phpcs.xml.dist` (warnings; CI error thresholds unaffected — every new tool class without guidance surfaces on PRs); full-tree gate passes **1,584/1,584 files, 0 warnings**; `.context/tool-registry.md` TDE section + proposal Phase 2 marked complete. Follow-up noted for owners: ~966 files outside the two trees (875 CG Pro mirror + standalone addons + fixtures) still lack guidance. |

### Release-sync PR

- #6685 — `alpha-working` → `main` squashed sync (build-only; 7 stale 1.1.80/docs-hub-0.4.6 ZIPs dropped from main, matching the 1.1.82 pass removals).

### Build-only (23 commits)

compiled assets/distributions ×18, **oOS WP.org packages v1.1.82 ×3** (`e53fe5bfe1`, `37726e7b06`, `4399538b5c` — the 1.1.82 wp.org package set is current), nvoos-content-graph standalone ZIP v1.0.8 ×1, nvoos-content-graph-ai ZIP v1.0.4 ×1. No direct commits in-window.

### Key scope rules for this pass

- **Historical entries stay.** Old CHANGELOG/README per-version blocks (`[1.1.82]` and earlier) are history and are not rewritten.
- **Tool counts DO NOT change this window.** No PR registers or removes tools — the guidance sweep adds an interface + text fields to existing tool classes. Figures stay **~306 base + ~1,279 Pro (~1,585 total)** with the live-registry caveat. No count-sweep commit this pass.
- **CHANGELOG:** no in-window `[1.1.83]` section exists — the pass creates it from scratch (date 2026-09-21).
- **README highlight rotation:** the pass replaces the v1.1.82 highlights paragraph with a v1.1.83 one and adds "What's New at a Glance (v1.1.83)" + "Latest Updates (v1.1.83)" above the v1.1.82 blocks.
- **Skill count stays 59** — no new coding-time skills in-window; the test-suite skill gained pattern 48 (#6679, done in-window — frontmatter already says 48), and the plugin/design-crm/playground-demos skills were updated in-window (#6679/#6683).
- **Sub-project version tracks:** all unchanged — Docs Hub 0.4.7, nvoos-content-graph 1.0.8, nvoos-content-graph-ai 1.0.4 (ZIP rebuilds only), content-graph-ai-platform 2.0.0, content-graph-pro 1.0.0 (mirror patches only, no port waves), Checkout API 0.1.2, Comic Reader 0.5.0, Media Worker v3.2.0.
- **Model catalog:** unchanged (**v2026.09.10**) — no model PRs in-window.
- **`@since` sweep:** 37 new `@since 1.1.83` tags in-window — all correct for the 1.1.83 ship. **Three new OI-1 anomaly groups** (recorded, not fixed): group 19 `@since 1.1.84` (`WP_MCP_AI_Legacy_Tool_Wrapper::get_usage_guidance()`, #6695 — ahead of ship); group 20 `@since 1.1.82` ×2 (`wp_mcp_ai_pro_incomplete_install_notice()` + `WP_MCP_AI_OKF_Bundle_Manager::list_bundle_names()`, #6677 — one version behind); group 21 `@since 1.1.0` ×5 on guidance-swept financial-planning methods (#6692) + group 18 extension (`@since 1.1.80` ×3 re-added on three of the #6639 financial tools by the #6692 sweep).
- **Housekeeping executed this pass** — remove stale build ZIPs: all `build/**/*1.1.81*` (root 9 + optional-components 2 + toolkit-addons 19 = 30 files), superseded by the in-window 1.1.82 wp.org package builds. The 1.1.82 ZIP set is current, not stale.
- **Parked items stay parked** (OI-1/OI-2 in `docs-catch-up-open-items.md`): no `@since` reconciliation, no Docker live-count re-derivation. OI-2's "current figure" label refreshed to the v1.1.83 baseline.
- **Unrelated working-tree noise excluded** — untracked `docs/user-guides/assistants/CONTENT-GRAPH-MARKETING-ASSISTANT.md` (excluded from every commit), root `vendor/`, `package-lock.json`, `.codex-wordpress/` runtime copies are never staged.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.83] - 2026-09-21` section: Added (Tool Description Engineering #6686/#6695/#6687–#6723; adaptive tool cap + lazy schema loading #6686; Pro bootstrap incomplete-install guard + Telegram chunking #6677; Upwork enhancements #6679/#6680; full workflow results #6682; Playground permalink + npx primary #6683), Fixed (Upwork gating #6678; Gmail "settings" + skill/OKF self-correction #6677; result-delivery formatting + digest lists #6679/#6684; npm advisories #6681), Changed (regulatory envelope #6689), Versioning block (counts unchanged ~306/~1,279/~1,585; catalog v2026.09.10; skills 59; stale 1.1.81 ZIPs removed).
2. **`readme.txt`** — Stable tag 1.1.83; new `= 1.1.83 - September 21, 2026 =` changelog paragraph (one paragraph, wp.org style).
3. **`README.md`** — version header → 1.1.83; Release Date 2026-09-21; new `🆕 v1.1.83 Highlights` paragraph replacing the v1.1.82 one; new "What's New at a Glance (v1.1.83)" section above v1.1.82; new "Latest Updates (v1.1.83 — September 2026)" block above v1.1.82; count mentions stay ~306/~1,279/~1,585.
4. **`docs/QUICK_REFERENCE.md`** — version + last-updated → 1.1.83/September 21; v1.1.83 entry above v1.1.82.
5. **`docs/DOCUMENTATION_INDEX.md`** — Plugin Version + Last Updated → 1.1.83/September 21; new SEPTEMBER 21 UPDATE banner above the v1.1.82 one.
6. **`docs/project/FOR_REVIEWERS.md`** — current version → 1.1.83 (September 2026).

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed stamp + "New in v1.1.83 (counts unchanged — ~306 base + ~1,279 Pro, ~1,585 total)" paragraph: no tools added or removed; the TDE program is a contract addition on existing classes (interface + text fields + sniff), not registrations; #6677–#6684/#6689 keep the same slugs. (The TDE section was stamped "(v1.1.83)" in-window by #6723 — verify-only.)
8. **`.context/security-checklist.md`** — v1.1.83 update line: guidance is model-facing text (keep fields factual, never embed secrets); adaptive tool cap opt-in default off; Pro bootstrap degradation pattern; Telegram hard-split `parse_mode` drop; Gmail "settings" fallback chain; Upwork gates byte-identical; both markdown converters stay in sync; npm pins.
9. **`.context/testing.md`** — last-reviewed + v1.1.83 note: TDE suites 25/25 + #6695 45/45 + per-cluster registry smoke suites; new telegram-chunking (6) + extended skill/OKF suites; Upwork fallback (12) + delivery-format 83/83, 150/150, 152/152, 30/30; CG Pro upwork green both matrices. The `mcp-ai-wpoos-test-suite` skill moves **47 → 48** patterns.
10. **`.context/rest-api.md`** — v1.1.83 note: no new REST routes this window (the `[Usage]` suffix changes payload content on the chat path + `/tools`; `list_mcp_tools` gains arguments — description/argument-level, not route-level).
11. **`CLAUDE.md`** — header stamp → September 21, 2026 · 2.36; v1.1.83 notes (TDE sweep + sniff severity 5, adaptive cap + lazy schemas, Pro bootstrap guard, Telegram chunking, Gmail/skill/OKF self-correction, Upwork + workflow delivery, Playground permalink, npm advisories, regulatory envelope).
12. **`MAINTAINER_MAP.md`** — last-reviewed bump → September 21, 2026 (v1.1.83).
13. **`.bmad/README.md`** — last-reviewed bump → v1.1.83.
14. **All three `mcp-ai-wpoos-plugin` SKILL.md copies** — `plugin-version`/`plugin-version-tested`/`last-updated` → 1.1.83/2026-09-21 (the `.agents/skills/` copy also gains a v1.1.83 note section above the v1.1.82 block).
15. **`.agents/skills/mcp-ai-wpoos-updates/SKILL.md`** — frontmatter → 1.1.83/2026-09-21; body references (distilled-plan range, template-plan names, commit-structure mirror, references list) → v1.1.83.
16. **Skill-count bookkeeping (AGENTS.md §6)** — verify-only: 59 skills, no new skills in-window; the test-suite skill's pattern count (48) was already updated in-window by #6679.

### P2 — Operational & feature docs

17. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" stamp → September 21, 2026 (v1.1.83); all addon rows verified unchanged (no sub-project bumps in-window).
18. **`docs/reference/tools/tool-reference.md`** — Last Updated → September 21, 2026 (v1.1.83); Tool Count line → "unchanged in v1.1.83" appended (no registrations; the TDE sweep adds guidance to existing tools).
19. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-21 (v1.1.83 pass); Scope line gains v1.1.83; OI-1 groups 19/20/21 added + group 18 annotated with the three #6692 re-additions (recorded, not fixed); OI-2 current-figure label → v1.1.83 (unchanged counts). No other new OI groups this pass.
20. **`docs/project/plans/v1.1.83-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

21. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.83 (`package-lock.json` intentionally untouched).

### Housekeeping (executed this pass)

22. **Remove stale build ZIPs** — `git rm` all `build/**/*1.1.81*` (root 9 + optional-components 2 + toolkit-addons 19 = 30 files, ZIPs **and** `.sha256`), superseded by the in-window 1.1.82 wp.org package builds. Commit: `chore: remove stale 1.1.81 build ZIPs [skip ci]`.

### Verify-only (no changes expected)

- Skill count after bookkeeping equals the actual **59** directories under `.agents/skills/`.
- `AGENTS.md` / `.github/copilot-instructions.md` / `README.md` repo-map skill counts — already 59.
- `addons/pro/README.md` — toolkit count stays 31 (no new tools or toolkits in-window).
- `docs/project/ecosystem-port-tracker.md` — no port-loop PRs in-window (the CG Pro/CG AI Upwork + Telegram + OKF mirrors in #6677/#6678/#6679 are mirror patches, not port waves).
- `addons/docs-hub/CHANGELOG.md`, `plugins/nvoos-content-graph/CHANGELOG.md`, `addons/checkout-api/CHANGELOG.md` — own tracks, unchanged in-window.
- `docs/reference/models/*` — no model changes in-window.
- Proposals — the TDE proposal (`tool-description-engineering-proposal.md`) was marked Phase 2 complete in-window by #6723; no index changes needed this pass.
- `.agents/skills/mcp-ai-wpoos-test-suite/SKILL.md` — frontmatter already says **48** patterns (updated in-window by #6679).
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" description — base count 306 still satisfies "300+".

---

## 3. Execution log (2026-09-21)

- **Orientation** — window mapped from `1da29e73cb` (1.1.82 catch-up merge #6676) to `b495800670` (origin/alpha-working HEAD): 46 merged PRs (#6677–#6684, #6686–#6723) + 23 build commits, no direct commits. All PR descriptions read in full via `gh pr list` (GitHub API MCP tools unavailable per the skill). `@since` sweep on the window diff found **37 correct `1.1.83` tags** and **three new anomaly groups** (19: `@since 1.1.84` legacy-wrapper; 20: `@since 1.1.82` ×2 #6677; 21: `@since 1.1.0` ×5 + group-18 extension ×3 from the #6692 sweep) — recorded in the open-items tracker, not fixed.
- **Commits 1–5** — version bump, release notes, agent context, reference surfaces, stale-ZIP removal (see §4).
- **Post-pass sweeps** — `git grep "1\.1\.82"` + count sweeps executed after all commits (see §5).

## 4. Commit breakdown (mirror v1.1.58–v1.1.82)

1. `chore: bump version to 1.1.83` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.83 changelog and release notes` — `CHANGELOG.md` (new [1.1.83] section; date 2026-09-21; Versioning block), `readme.txt`, `README.md`, `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.83 changes` — `.context/*` (4 files), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies, `.agents/skills/mcp-ai-wpoos-updates/SKILL.md`
4. `docs: reconcile 1.1.83 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/plans/docs-catch-up-open-items.md`, `docs/project/plans/v1.1.83-docs-catch-up.md`
5. `chore: remove stale 1.1.81 build ZIPs [skip ci]` — `build/**/*1.1.81*` (30 files: 9 root + 2 optional-components + 19 toolkit-addons)

(No count-sweep commit this pass — tool counts unchanged.)

## 5. Validation

- `php -l` on every bumped PHP file
- `package.json` JSON parse
- `git grep "1\.1\.82"` sweep — remaining hits must be intentional history (previous-release sections, `@since` docblocks, plans, archive, the 1.1.82 build ZIP names)
- Count sweep — counts unchanged in-window; verify `~306`/`~1,279`/`~1,585` surfaces carry the live-registry caveat; historical per-version blocks/plans/archive untouched
- Stale-ZIP sweep — `git ls-files 'build/*'` re-checked: no 1.1.81 files remain; the 1.1.82 set is current
- Skill-count verification — 59 directories under `.agents/skills/` == bookkeeping claims in AGENTS.md / copilot-instructions / README

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue #5968) and Docker-based live-count re-derivation (OI-2, issue #5967) stay parked. OI-2's current-figure label refreshed to the v1.1.83 baseline (~306 base + ~1,279 Pro, ~1,585 total; no tool registrations in-window).
- **New items raised this pass:** OI-1 groups **19** (`@since 1.1.84`, legacy-wrapper `get_usage_guidance()`, #6695), **20** (`@since 1.1.82` ×2 — `wp_mcp_ai_pro_incomplete_install_notice()` + OKF `list_bundle_names()`, #6677), and **21** (`@since 1.1.0` ×5 on #6692-swept financial-planning methods) plus a **group-18 extension** (`@since 1.1.80` ×3 re-added by the #6692 sweep). All recorded in the tracker, not fixed.
- **Noted observations (other workstreams):** #6723's follow-up — ~966 files outside the two swept trees (875 `nvoos-content-graph-pro` mirror files + standalone addons + fixtures) still lack usage guidance (the sniff surfaces these as warnings) — is the ecosystem-port loop's concern; the cluster PR bodies reference `docs/project/proposals/tool-description-engineering-phase2-clusters.md`, which does not exist in the repo (the main TDE proposal is the file of record).
- **Housekeeping executed this pass:** stale 1.1.81 build ZIPs removed (30 files), superseded by the in-window 1.1.82 wp.org package builds.
