# Docs & Release Catch-Up Plan — v1.1.60 (PRs #5897–#5905)

> **Prepared:** 2026-08-21 · **Status:** ✅ **Executed & committed 2026-08-21** (all P0–P2 items complete; see execution log below)
> **Scope:** documentation/context updates + version bump to **1.1.60** (no production-code changes)
> **Input:** full review of every commit on `alpha-working` from 2026-08-19 through 2026-08-21
> **Predecessor:** [`v1.1.59-docs-catch-up.md`](v1.1.59-docs-catch-up.md) (executed Aug 19; covered PRs #5892–#5895)

---

## 1. Context: What changed since the last docs catch-up

The Aug 19 catch-up covered everything through PR #5895 and wrote the `[1.1.59]` changelog. **Five PR groups merged afterward** were absent from most release-note surfaces:

| PR / Commit | Change | Doc status after this pass |
|---|---|---|
| #5901 | **Restricted-user flagging & unblocking** — `WP_MCP_AI_Restriction_Registry` (persistent, audit-logged restriction records with auto-expiry), OOS `RateLimiter` adapter `wp_mcp_ai_rate_limit_exceeded` hook + enumerable key index, filterable chat rate limits (`wp_mcp_ai_chat_rate_limit` / `wp_mcp_ai_chat_rate_limit_window`), Token Manager "Restricted Users" panel + Pro Command Center Restrictions tab, REST/AJAX/WP-CLI operations, IETF rate-limit headers, `restriction_registry_on` posture signal | ✅ changelog (in-commit) + feature doc (in-commit); this pass: README/QUICK_REFERENCE/index/rest-api reference, `.context/*`, folder READMEs, skills, MAINTAINER_MAP, CLAUDE.md |
| #5898 | **Conversation import → transcript CCT** — `includes/conversation-import/` subsystem (17 classes), 4 JetEngine-gated tools, admin page, `wp mcp-ai conversation-import` CLI, GDPR + memory mining | ✅ user guide + plan + tool reference + folder README + DOCUMENTATION_INDEX (in-commit); this pass: CHANGELOG, README/readme.txt/QUICK_REFERENCE, counts, admin/cli folder READMEs, skills, MAINTAINER_MAP |
| #5903 | **Tool schema normalization for provider payloads** — DeepSeek client, REST `/tools` output, `WP_MCP_AI_Tool_Service`, `ChatOrchestrator`; registry logs registrations skipped for a missing tool contract | ✅ this pass (changelog, README, CLAUDE.md, `.context/tool-registry.md`) |
| #5905 | **WP_Error fatals fix** in memory/REST paths | ✅ this pass (changelog, README, CLAUDE.md) |
| #5897/#5899/#5904 | **nvoos-content-graph wp.org resubmission → v1.0.3** (WPCS/review fixes, screenshots, build ZIP) | ✅ plugin-internal docs in-commit; this pass: changelog, README, DOCUMENTATION_INDEX version bump |

Build-only commits (ZIPs, temporary secret-probe workflow add/remove) need no doc action.

### Drift found while executing

- **Tool counts** (~1,543) were stale: PR #5898 registered 4 new tools in the registry's `$extended_tools` map (JetEngine-gated). New figures: **~300 base + ~1,247 Pro (~1,547 total)** with the live-registry caveat.
- **Security posture signal count** was documented as "21 signals" (checklist) and "~17 signals" (folder README); the base class defines **23** (PR #5901 added `restriction_registry_on`). Reconciled to 23 everywhere.
- **Coding-time skill count** said "21" in `MAINTAINER_MAP.md` and `docs/project/FOR_REVIEWERS.md` (actual: 52 after the design-elementor-template-kits addition).
- **Security class count** said "7" in `MAINTAINER_MAP.md`; the canonical count (copilot-instructions, FOR_REVIEWERS) is 10. Reconciled.
- `includes/rest/README.md` linked to `docs/reference/rest-api.md` (broken); actual path is `docs/reference/api/rest-api.md`. Fixed.
- `MAINTAINER_MAP.md` carried July-era tool figures (~195/~830/~234 class files). Reconciled to ~300/~1,547.

### Key scope rules for this pass

- **Historical entries stay.** Old changelog/README per-version blocks (`[1.1.59]` and earlier) are history and are not rewritten, including their tool counts.
- **Tool counts are approximations** ("~" + `WP_MCP_AI_Tool_Registry::get_tools()` authoritative). Figures are deltas from the established 1.1.59 baseline (1,543 + 4 import tools = ~1,547; the +4 sit in the extended/full map, so Pro goes ~1,243 → ~1,247 while base stays ~300).
- **Version bump:** 1.1.60 everywhere (plugin headers, `WP_MCP_AI_VERSION`, `WP_MCP_AI_PRO_VERSION`, `package.json`, `readme.txt` Stable tag, docs). Media Worker stays on its own 3.2.0 track; nvoos-content-graph on its own 1.0.3 track.

---

## 2. Work items (priority order)

### P0 — Correctness gaps

- [x] `CHANGELOG.md` — `[Unreleased]` → `[1.1.60] - 2026-08-21`; added entries for PRs #5898, #5903, #5905, #5897/#5899/#5904 + Versioning block.
- [x] Tool counts → ~1,547 (~300 base + ~1,247 Pro) in `.context/tool-registry.md`, `.context/pro-vs-base.md`, `.context/conventions.md`, `.context/README.md`, `CLAUDE.md`, `.github/copilot-instructions.md`, both `mcp-ai-wpoos-plugin` SKILL.md copies, `docs/reference/tools/tool-reference.md`, `README.md` (4 spots), `MAINTAINER_MAP.md`, `docs/project/FOR_REVIEWERS.md`.
- [x] Folder READMEs: `includes/rest/README.md` (restrictions controller + rate-limit headers + new tests + fixed broken link), `includes/cli/README.md` (restrictions + conversation-import commands), `includes/admin/README.md` (conversation-import admin + token manager panel + AJAX actions), `includes/security/README.md` (23 signals + restriction registry).
- [x] Security posture signals 21 → 23 in `.context/security-checklist.md` (+ v1.1.60 review line).

### P1 — Feature/reference surfaces

- [x] `docs/features/security/README.md` — indexed `user-restrictions.md`.
- [x] `docs/reference/api/rest-api.md` — new "User restrictions" section (routes table, permissions, IETF headers, filters).
- [x] `.context/rest-api.md` — restriction routes + controller/headers notes.
- [x] `README.md` — "What's New at a Glance (v1.1.60)" + "Latest Updates (v1.1.60)" block.
- [x] `readme.txt` — Stable tag 1.1.60 + new changelog section.
- [x] `docs/QUICK_REFERENCE.md` — version/date + v1.1.60 bullet.
- [x] `docs/DOCUMENTATION_INDEX.md` — user-restrictions/rest-api/changelog entries (Aug 21 section) + content-graph v1.0.3.
- [x] `MAINTAINER_MAP.md` — new classes/folders, skill/security counts, boot-table counts.
- [x] Skills — both `mcp-ai-wpoos-plugin` SKILL.md copies: tool counts, restrictions + conversation-import operational sections, version-tested stamps.
- [x] `CLAUDE.md` — v1.1.60 feature bullets + counts.
- [x] `.context/oos-engine.md` — bridge rate-limit filters + `wp_mcp_ai_rate_limit_exceeded`.
- [x] `.context/tool-registry.md` — stamp + "New in v1.1.60" (import tools, schema normalization).

### P2 — Stale references

- [x] `docs/project/FOR_REVIEWERS.md` — version, counts, skills 21 → 52, two new "Key Files" rows.
- [x] `.bmad/README.md` — last-reviewed stamp.
- [x] This plan file (self).

---

## 3. Validation

- [x] `composer run docs:check-folder-readmes` — folder README completeness check.
- [x] `php -l` on changed PHP version-bearing files.
- [x] No historical changelog/README per-version blocks rewritten.

---

## 4. Post-pass notes

- The 4 conversation-import tools register in the registry's extended map but no-op without JetEngine; they count toward the full-version total, not the ~300 base figure.
- `docs/reference/admin/security-settings.md` intentionally unchanged — the restriction-notice toggle lives in Settings → Orchestration, which that file does not cover (the toggle is documented in `user-restrictions.md`).
- `.context/media-worker.md` intentionally unchanged — no worker-side changes landed in this window (worker still v3.2.0).
- `AGENTS.md` was already current (52 skills) and needed no edits.
