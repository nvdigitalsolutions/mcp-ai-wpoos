# Docs & Release Catch-Up Plan — v1.1.83 Post-Window (PRs #6726–#6743)

## 1. Context: What changed since the v1.1.83 catch-up

The v1.1.83 catch-up merged as `4c18a9ef57` ("chore: 1.1.83 docs catch-up (#6725)") on 2026-09-21 and covered PRs #6677–#6723. **18 more PRs** (#6726–#6743) and **9 build commits** landed on `alpha-working` afterward — still on the 1.1.83 line (no version bump; the 1.1.83 wp.org package set was re-cut after the merges). None are on any release-note surface, and three claims made by the #6725 pass are now stale: the two changelog "PR pending" markers (the Jev work), the "no tool registrations in-window" count claim (`typesafe_decide` is a new base registration), and the "Model catalog v2026.09.10 (unchanged)" claim (#6728 bumped the catalog to v2026.09.21 with three Jev decision entries).

### Production-touching PRs

| PR | Files | Change |
|---|---|---|
| #6726 | `includes/admin/class-wp-mcp-ai-settings-dashboard.php` + `tests/test-usage-monitor-settings-save.php` (new, 3) | **Usage-monitor settings save fix** — `handle_save_settings()` (the dashboard form's handler) never applied the `wp_mcp_ai_admin_settings_sanitize` bridge filter that `sanitize_monitor_settings()` hooks, so monitor toggle/limits silently no-oped and sites flipped to "Disabled" by the pre-#6632 naive-merge bug could never re-enable. The bridge now runs with the raw posted input before sanitization; handlers no-op when their fields are absent (unrelated saves unaffected). |
| #6727 | `includes/class-wp-mcp-ai-default-assistants.php` + `includes/bootstrap/activation.php` + `tests/test-assistant-builder-assistant.php` (new) | **"The Assistant Builder" meta-assistant seeded** — a pre-configured assistant that designs/builds/verifies other assistants (7-phase workflow, 10-component prompt framework, 20 base tool slugs + Pro `export_assistant_blueprint`; `openai`/`gpt-4.1`/temp 0.3). Appended to `get_default_assistants()` (default roster 6 → 7); idempotent `install_assistant_builder_assistant()` + one-shot `admin_init` backfill (`wp_mcp_ai_assistant_builder_backfilled`) so pre-existing rosters get it self-healingly. |
| #6728 | `includes/interfaces/interface-wp-mcp-ai-decision-client.php` (new), `includes/class-wp-mcp-ai-typesafe-client.php` (new), `includes/class-wp-mcp-ai-openrouter-client.php`, `includes/tools/class-wp-mcp-ai-tool-typesafe-decide.php` (new), `includes/class-wp-mcp-ai-tool-registry.php`, provider surfaces/diagnostics/CLI/WP 7.0 bridge/cost-calculator/token-usage, model catalog (→ v2026.09.21, +3 entries), Pro `WP_MCP_AI_Pro_Jev_Classifier` + parallel-model-dispatcher cascade + research filter seam, proposal/plan docs | **TypeSafe Jev "System One" decision provider + Pro integrations** (the "PR pending" work). Decision-only provider deliberately separate from chat: typed choice/score/noul over state, `Interface_WP_MCP_AI_Decision_Client` + native client (429/`retry-after`, versioned-model logging, `test_connection()`), OpenRouter Decisions bridge (filterable endpoint, actionable 404), **new base tool `typesafe_decide`** (all three primitives, canonical envelope + two-gate sanitisation, adversarial-state caveat, `manage_options`-gated) — **+1 base registration**. Pro: fail-open `decide()`/`classify_prompt()`/`filter_sources_by_relevance()` (kill-switch `wp_mcp_ai_jev_classifier_enabled`), opt-in dispatcher `jev_routing` (REST `compare-models` passes it through), opt-in `enable_jev_research_filter` seam on `research_eca`/`generate_research_report`; fixes the pre-existing dispatcher `chat_completion()` bug. Deferred in-PR: NV Cloud passthrough (OpenRouter Decisions GA), Cloudflare direct, CG port (ecosystem-port cluster). |
| #6729–#6738 | ID-handoff contract annotations across base + Pro + CG Pro mirrors; `tests/fixtures/tool-contract-manifest.php`, `tests/helpers/trait-wp-mcp-ai-tool-id-handoff-test-helper.php`, L1 (`test-tool-id-handoff-contract.php`) + L2 (`test-tool-id-handoff-round-trip.php`) suites; `includes/helpers/class-wp-mcp-ai-tool-presets-helper.php` (#6743) | **P3 data-contract rollout waves 1a–2f** — cron/post (`post_id` additive key, #6729), term/assistant (#6730), vector-store/batch (#6731), Pro schedule `schedule_id` + manifest `scope=pro` gate (#6732), `toolkit_cpt` `item_id` (#6734), medical-record `record_id` both trees byte-identical (#6735), plan/post/assistant expansions (#6736), calendar/WPCode (#6737 — also fixes `format_code_prettier`'s four `success=false` envelopes to canonical `WP_Error` and registers `wpcode_edit_snippets` as a custom capability), session/member/webchat room (#6738 — **production fix**: five webchat tools called the nonexistent `WP_MCP_AI_Logger::log_activity()`; now `log_event( 'activity', … )`). Contract annotations on existing tools — **no registrations**. |
| #6739 | `docs/project/proposals/P3-data-contract-rollout-plan-2026-09.md`, `.context/tool-registry.md`, `docs/reference/tools/tool-reference.md`, `UNIX_THEORY_COMPLIANCE_ENHANCEMENT_PROPOSAL.md`, `tests/fixtures/tool-contract-manifest.php` | **P3 Phase-5 closure** — status → COMPLETE, 15-family inventory + verified out-of-scope list; static completeness sweep of every `WP_MCP_AI_Tool_Data_Contract_Interface` implementation; manifest documents remaining candidate keys as verified out-of-scope. |
| #6740 | `addons/embedded/includes/webchat/tools/class-wp-mcp-ai-tool-get-webchat-messages.php` + 2 more webchat tools; CG Pro member mirrors ×4 + `plugins/nvoos-content-graph-pro/src/interfaces/interface-wp-mcp-ai-tool.php` | **Webchat SQL prepare warnings fixed (root cause)** — list/count queries rebuilt with explicit placeholders in a `message_type` branch (behavior-identical). **Guidance drift closed** — the three remaining webchat-folder tools gain the usage-guidance interface; CG Pro member mirrors port the guidance; the CG interface copy gains `WP_MCP_AI_Tool_Usage_Guidance_Interface` (standalone-resolution fatal fix). Remaining drift (~957 CG mirror files) → issue #6741 (ecosystem-port track). |
| #6743 | `includes/helpers/class-wp-mcp-ai-tool-presets-helper.php`, `includes/tools/class-wp-mcp-ai-tool-typesafe-decide.php`, `tests/tools/.coverage-manifest.txt`, default-assistant tests | **CI-repair + production coverage** — `typesafe_decide` joins the `ai_ml` preset and declares `items` on array-typed schema nodes; coverage manifest entry; default-assistant tests updated 6 → 7 with the `assistant-builder` slug; order-dependent install failures fixed via `setUp()` state reset. |

### Docs-only PRs

- #6733 — P3 vocabulary/status documentation (`.context/tool-registry.md` + `tool-reference.md` data-contracts section + proposal statuses). Markdown only.
- #6742 — skill updates only: ecosystem-port skill (CG interface port rule + guidance-drift pitfall, #6741) and test-suite skill **patterns 49** (`log_activity()` → `log_event( 'activity', … )`) **and 50** (`$wpdb->prepare()` placeholder-count warnings from interpolated fragments + array-spread args).

### Build-only (9 commits)

compiled assets/distributions ×6, **oOS WP.org packages v1.1.83 ×3** (`b2ffb6976a`, `7317db0c40`, `43bef61f79` — the 1.1.83 wp.org package set now contains the post-window code). No direct commits in-window.

### Key scope rules for this pass

- **The `[1.1.83]` section is the current release** — extended in place; `[1.1.82]` and earlier stay untouched.
- **Counts change:** +1 base (`typesafe_decide`, #6728) → **~307 base + ~1,279 Pro (~1,586 total)** with the live-registry caveat. Historical per-version blocks keep their counts.
- **Model catalog:** v2026.09.21 (+3 Jev decision entries, #6728) — correct the "v2026.09.10 unchanged" claims on the current surfaces.
- **No version bump** — 1.1.83 is already everywhere (verify-only).
- **Skill count stays 59** — two skills updated in-window (#6742), none added.
- **Sub-project tracks:** all unchanged — Docs Hub 0.4.7, nvoos-content-graph 1.0.8, nvoos-content-graph-ai 1.0.4, content-graph-ai-platform 2.0.0, content-graph-pro 1.0.0 (mirror patches only, no port waves), Checkout API 0.1.2, Comic Reader 0.5.0, Media Worker v3.2.0.
- **Housekeeping executed this pass** — remove stale build ZIPs: all `build/**/*1.1.82*` (root 9 + optional-components 2 + toolkit-addons 19 = 30 files), superseded by the in-window 1.1.83 wp.org package builds.
- **Parked items stay parked** (OI-1/OI-2): record new `@since` anomalies, never fix.
- **Unrelated working-tree noise excluded** — untracked `docs/user-guides/assistants/CONTENT-GRAPH-MARKETING-ASSISTANT.md`, `.codex-wordpress/` runtime copies, root `vendor/`, `package-lock.json` are never staged.
- **#6741 (CG guidance drift, ~957 mirror files)** was filed in-window by #6740 — ecosystem-port track, not this pass.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — `[1.1.83]` section extended: the two "PR pending" headings → "(PR #6728)"; new Added subsections — Assistant Builder meta-assistant (#6727), P3 ID-handoff data contracts (#6729–#6739); new Fixed subsections — usage-monitor settings save (#6726), webchat `log_activity()` latent fatal + SQL prepare warnings + guidance drift close-out (#6738/#6740), `format_code_prettier` canonical envelope (#6737); Tool Description Engineering section gains the #6740 close-out line (webchat + CG member mirrors; residual drift ~957 → issue #6741). Versioning block: tool count → **~307 base + ~1,279 Pro (~1,586 total)** (+1 base `typesafe_decide`, #6728); model catalog → **v2026.09.21** (+3 Jev decision entries); skills — test-suite patterns 49/50 + ecosystem-port/port-skill updates (#6742); stale ZIPs — 1.1.82 set removed (30 files).
2. **`readme.txt`** — `= 1.1.83 - September 21, 2026 =` paragraph extended (Jev decision provider shipped, Assistant Builder, P3 ID-handoff contracts, usage-monitor fix, webchat fixes); tool count → ~307/~1,586 (+1 base).
3. **`README.md`** — v1.1.83 Highlights paragraph + "September 21, 2026" updates block extended; Versioning block corrected (counts, catalog); evergreen count lines ~306→~307 / ~1,585→~1,586 (skip `[1.1.82]`-and-earlier history).
4. **`docs/QUICK_REFERENCE.md`** — v1.1.83 entry extended + count corrected; plan link gains the post plan.
5. **`docs/DOCUMENTATION_INDEX.md`** — SEPTEMBER 21 banner extended + count corrected.
6. **`docs/project/FOR_REVIEWERS.md`** — evergreen count lines ~306→~307 / ~1,585→~1,586 (verify-only otherwise: "Current version 1.1.83 (September 2026)" is correct).

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed → September 22, 2026 (v1.1.83 post); "New in v1.1.83" paragraph amended: +1 base registration `typesafe_decide` (#6728 — decision-only, never a chat provider); the P3 waves (#6729–#6738) are contract annotations on existing tools; #6740 closes the webchat guidance gap + CG member mirrors. Counts move to ~307/~1,586.
8. **`.context/security-checklist.md`** — v1.1.83 post line: `typesafe_decide` `manage_options` gate + adversarial-state caveat; decision-client/chat separation invariant; usage-monitor bridge filter runs on raw input pre-sanitize with no-op-when-absent handlers; webchat `log_event( 'activity', … )` (no hand-rolled `log_activity()`); explicit-placeholder SQL rebuild; P3 contract annotations are metadata-only.
9. **`.context/testing.md`** — last-reviewed + v1.1.83 post note: usage-monitor save regression suite (75/631 with neighbors); assistant-builder suite (5/5, 10/10 combined); typesafe client/decide suites; permanent P3 L1/L2 suites + coverage-manifest single-source-of-truth; webchat L1/L2 17/17 (556) + CG dual matrix; `mcp-ai-wpoos-test-suite` skill → **49/50** patterns (done in-window by #6742).
10. **`.context/rest-api.md`** — v1.1.83 post note: `compare-models` passes `jev_routing` through (argument-level, no new routes); no new routes in the P3 waves.
11. **`CLAUDE.md`** — stamp → September 22, 2026 · 2.37; v1.1.83 bullet extended (Jev shipped #6728, Assistant Builder #6727, P3 contracts #6729–#6739, usage-monitor #6726, webchat #6738/#6740); evergreen count lines ~306→~307 / ~1,585→~1,586; catalog mention → v2026.09.21.
12. **`MAINTAINER_MAP.md`** — last-reviewed → September 22, 2026 (v1.1.83 post).
13. **`.bmad/README.md`** — last-reviewed → September 2026 (v1.1.83 post).
14. **Three `mcp-ai-wpoos-plugin` SKILL.md copies** — `last-updated` → 2026-09-22 (version stays 1.1.83); the `.agents/skills/` copy's v1.1.83 note section gains the post-window items.
15. **`.agents/skills/mcp-ai-wpoos-updates/SKILL.md`** — references: latest executed plan → `v1.1.83-post-docs-catch-up.md`; template list gains it.
16. **Skill-count bookkeeping (AGENTS.md §6)** — verify-only: 59 skills, none added; the test-suite skill's pattern count (49/50) and the ecosystem-port skill were updated in-window by #6742.

### P2 — Operational & feature docs

17. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" → September 22, 2026 (v1.1.83 post); addon rows unchanged.
18. **`docs/reference/tools/tool-reference.md`** — Last Updated → September 22, 2026 (v1.1.83 post — P3 rollout complete); Tool Count line gains "+1 base in v1.1.83 post (`typesafe_decide`, #6728)".
19. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-22 (v1.1.83 post pass); OI-1: extend group 19 (`@since 1.1.84` ×3, `includes/class-wp-mcp-ai-default-assistants.php`, #6727), extend group 2 (`@since 1.2.0` ×2, new `includes/interfaces/interface-wp-mcp-ai-decision-client.php`, #6728), add group 22 (`@since 1.9.0` ×10 — jev-classifier 8 + dispatcher 1 + presets-helper 1, #6728/#6743), add group 23 (`@since 2026.09` ×3 — openrouter-client 1 + typesafe-client 2, #6728); OI-2 current-figure label → v1.1.83 post (~307 base + ~1,279 Pro, ~1,586 total).
20. **`docs/project/plans/v1.1.83-post-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

21. Verify-only — version is already 1.1.83 across `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json` (bumped by #6725). No change.

### Housekeeping (executed this pass)

22. **Remove stale build ZIPs** — `git rm` all `build/**/*1.1.82*` (root 9 + optional-components 2 + toolkit-addons 19 = 30 files, ZIPs **and** `.sha256`), superseded by the in-window 1.1.83 wp.org package builds. Commit: `chore: remove stale 1.1.82 build ZIPs [skip ci]`.

### Verify-only (no changes expected)

- Skill count after bookkeeping equals the actual **59** directories under `.agents/skills/`.
- `AGENTS.md` / `.github/copilot-instructions.md` skill counts — already 59 (copilot-instructions count lines are updated as part of the count sweep).
- `addons/pro/README.md` — toolkit count stays 31.
- `docs/project/ecosystem-port-tracker.md` — no port-loop PRs in-window (the CG Pro medical-record/member mirrors in #6735/#6740 are mirror patches, not port waves).
- Sub-project changelogs (`addons/docs-hub/CHANGELOG.md`, `plugins/nvoos-content-graph/CHANGELOG.md`, `addons/checkout-api/CHANGELOG.md`) — own tracks, unchanged.
- Proposals — the P3 rollout plan + Unix Theory proposal statuses were closed out in-window by #6733/#6739; the Jev proposal/plan shipped with #6728; no index changes needed.
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" description — base count 307 still satisfies "300+".
- `tests/test-model-catalog.php` and model suites — the in-window catalog bump (v2026.09.21, Jev decision entries) shipped with its own test updates in #6728; model-track files beyond the changelog mention are not edited here.

---

## 3. Execution log (2026-09-22)

- **Orientation** — window mapped from `4c18a9ef57` (v1.1.83 catch-up merge #6725) to `5c34fcf4bc` (origin/alpha-working HEAD): 18 merged PRs (#6726–#6743) + 9 build commits, no direct commits. All PR descriptions read in full via `gh pr list` (`gh` CLI per the updates skill; GitHub API MCP tools unavailable). `@since` sweep on the window diff found the new OI-1 groups recorded in §2.19.
- **Claims verified against the tree** — `typesafe_decide` registration confirmed in `includes/class-wp-mcp-ai-tool-registry.php` (class map → tool registration); model catalog read directly (version `2026.09.21`, 3 Jev entries); version constants confirmed at 1.1.83.
- **Commits 1–5** — release notes, agent context, reference surfaces, count sweep, stale-ZIP removal (see §4).
- **Post-pass sweeps** — `git grep "~306\|~1,585\|v2026\.09\.10"` + `1\.1\.82` ZIP sweeps executed after all commits (see §5).

## 4. Commit breakdown (mirror v1.1.58–v1.1.83)

1. `docs: add 1.1.83 post-window changelog and release notes` — `CHANGELOG.md` ([1.1.83] extension), `readme.txt`, `README.md` (highlights + September 21 block + evergreen count lines), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
2. `docs: update agent context for 1.1.83 post-window changes` — `.context/*` (tool-registry, security-checklist, testing, rest-api, conventions, pro-vs-base, README), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies, `.agents/skills/mcp-ai-wpoos-updates/SKILL.md`, `.github/copilot-instructions.md` (count lines)
3. `docs: reconcile 1.1.83 post-window reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/plans/docs-catch-up-open-items.md`, `docs/project/plans/v1.1.83-post-docs-catch-up.md`
4. `chore: remove stale 1.1.82 build ZIPs [skip ci]` — `build/**/*1.1.82*` (30 files: 9 root + 2 optional-components + 19 toolkit-addons)

(No separate count-sweep commit — the count changes (~306→~307 / ~1,585→~1,586 / catalog v2026.09.21) are entangled with content edits in the same files and ride along in commits 1–2; historical per-version blocks, plans, proposals, and the archive are untouched.)

## 5. Validation

- `php -l` on every changed PHP file (none expected — this pass touches docs; verify-only).
- `git grep "PR pending"` — no remaining hits on current release surfaces.
- `git grep "~306\|~1,585\|v2026\.09\.10"` sweep — remaining hits must be intentional history only (`[1.1.82]`-and-earlier blocks, `@since` docblocks, plans, archive).
- Count sweep — `~307`/`~1,279`/`~1,586` surfaces carry the live-registry caveat.
- Stale-ZIP sweep — `git ls-files 'build/*'` re-checked: no 1.1.82 files remain; the 1.1.83 set is current.
- Skill-count verification — 59 directories under `.agents/skills/`.

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue #5968) and Docker-based live-count re-derivation (OI-2, issue #5967) stay parked. OI-2's current-figure label refreshed to the v1.1.83 post baseline (~307 base + ~1,279 Pro, ~1,586 total; +1 base `typesafe_decide` from #6728).
- **New items raised this pass:** OI-1 group **19 extension** (`@since 1.1.84` ×3 — Assistant Builder methods in `includes/class-wp-mcp-ai-default-assistants.php`, #6727), **group 2 extension** (`@since 1.2.0` ×2 — new `includes/interfaces/interface-wp-mcp-ai-decision-client.php`, #6728), **group 22** (`@since 1.9.0` ×10 — Jev classifier 8 / dispatcher 1 / presets-helper 1, #6728/#6743), and **group 23** (`@since 2026.09` ×3 — date-format tags on the Jev OpenRouter/TypeSafe clients, #6728). All recorded in the tracker, not fixed.
- **Noted observations (other workstreams):** #6741 (guidance drift across ~957 CG Pro mirror files) is the ecosystem-port loop's concern; #6728's deferred items (NV Cloud passthrough on OpenRouter Decisions GA, Cloudflare direct, CG port cluster) are documented in the PR/plan.
