# Docs & Release Catch-Up Plan — v1.1.77 (PRs #6551–#6594)

> **Prepared:** 2026-09-11 · **Status:** ✅ Executed
> **Scope:** documentation/context updates + version bump to **1.1.77** (no production-code changes) + requested housekeeping (stale build ZIPs)
> **Input:** full review of every PR merged on `alpha-working` after the v1.1.76 catch-up (merge `556eae9d0e`, PR #6553) through PR #6594 (tip `5a8baefdce`), including a read of every non-port PR description and the two unnumbered direct commits in the window
> **Predecessor:** [`v1.1.76-docs-catch-up.md`](v1.1.76-docs-catch-up.md) (executed Sep 10; covered PRs #6504–#6550)

---

## 1. Context: What changed since the last docs catch-up

The Sep 10 catch-up merged as PR #6553 and covered PRs #6504–#6550. **26 port PRs** (#6551–#6584), **12 non-port PRs** (#6555, #6557, #6561, #6568, #6570, #6571, #6573, #6585–#6594), one unnumbered direct commit (`6cd58234ef`), and 15 build commits landed afterward, none present on any release-note surface. The window is the **DeepSeek V4.1 Flash model refresh (standalone Track B), the base+pro gating fix with its new regression matrix, the Knowledge Graph Companion onboarding preset, the Pro WP-CLI load-order guard, the memory CCT phantom-slug fix, the checkout launch-complete series (Checkout API 0.1.1 + Content Graph 1.0.7), a security sweep, the content-graph build race fix, and Wave G of the ecosystem port (law-firm/cre-debt/quiz/eca/chat-channels complete, places started)**.

### Production-touching PRs (everything else is sub-project ports, docs, or build-only)

| PR | Production files | Change |
|---|---|---|
| #6555 | `includes/data/model-catalog.json` + ~30 derived files (migration map, settings defaults, selector, router, DeepSeek client, token-budget service, cost calculator, usage tracker, model service, admin sections, provider diagnostics, deep-research tool, 10 Pro research/orchestration tools) | **DeepSeek V4.1 Flash catalog refresh (standalone Track B)** — new `deepseek-flash` (active, vision, $0.15/$0.60 off-peak); `deepseek-v4-flash` + `deepseek-v4-flash-vision-exp` removed (retired); `deepseek-v4-pro` deprecated (sunset 2026-09-14, fallback `deepseek-flash`); migration map rewrites all six legacy DeepSeek ids; catalog version 2026.09.05 → **2026.09.10**. Cost calculator gains **peak/off-peak pricing** (`PEAK_WINDOWS` const — Mon–Fri 01:00–04:00 + 06:00–10:00 UTC; `is_peak_time()`, `get_model_pricing_at()`, `calculate_cost_at()` with injectable timestamps). Legacy `calculate_cost()`/`get_model_pricing()` untouched and time-independent. Model suites 170 tests / 6,677 assertions green. `lib/core/` mirrors + `plugins/nvoos-content-graph-ai/` catalog flagged, not edited (own tracks). |
| #6561 | 21 toolkit `addons/pro/includes/tools/*/init.php` gates + `class-wp-mcp-ai-pro-module-registry.php`, `rest/class-wp-mcp-ai-telegram-mini-app-controller.php`, `admin/class-wp-mcp-ai-pro-cpt-ai-integration.php`, `class-wp-mcp-ai-site-template-cpt.php`, `addons/embedded` (webchat), ~550 Pro tool files, `tests/basepro/` (15 tests), `phpunit-basepro.xml.dist`, `.github/workflows/phpunit.yml`, `composer.json` | **Base+pro gating fix + regression matrix** — Pro toolkits silently failed to load in base+pro installs (gates read `wp_mcp_ai_is_base_version()` as "no Pro" even with `WP_MCP_AI_PRO_VERSION` defined; the complete build worked because the base constant is undefined there). All gates now use the Regulatory Registration pattern `if ( $is_enabled && ( ! $is_base || defined( 'WP_MCP_AI_PRO_VERSION' ) ) )`. ~550 Pro tool `is_available()`/`get_unavailable_reason()` gates gain the same escape; CG Pro ported copies mirrored byte-identically. New base+pro regression matrix: `phpunit-basepro.xml.dist` boots the real base+pro shape, `tests/basepro/` pins 15 contracts, new `base-pro` CI job, `composer run test:basepro`. |
| #6570 | `includes/admin/class-wp-mcp-ai-onboarding-wizard.php`, `tests/test-onboarding-wizard.php` | **Knowledge Graph Companion onboarding preset** — new featured `knowledge_graph` preset (listed first, temperature 0.3, standard `mcp_ai_assistant` CPT): graph-teaching system prompt (GraphRAG reasoning, node/edge/community teaching, progressive disclosure, honesty rule), 20 graph-aware base tools always included, all 14 `graphify_*` tools appended when the bundled Graphify addon is loaded + enabled (`wp_mcp_ai_onboarding_graph_tools_active` seam). `is_content_graph_detected()` (standalone plugin / Graphify addon / any graph memory-bridge retriever, `wp_mcp_ai_onboarding_graph_detected` seam) + `get_effective_preset_selection()` auto-select when detected and nothing saved — saved selections win. `⭐ Featured` badge + full-width featured card. 8 new tests (31/31 on WP 6.9 + 7.1). |
| #6585 | `addons/pro/mcp-ai-wpoos-pro.php`, `addons/pro/includes/cli/class-wp-mcp-ai-pro-cli-base-command.php` | **Pro WP-CLI load-order guard** — the Pro addon fataled under WP-CLI with `Undefined constant WP_MCP_AI_PATH` when activated before the base plugin (live on victory.nvdigital.solutions). CLI require loop extracted into `wp_mcp_ai_pro_load_cli_commands()`: loads immediately when the base constant exists or `plugins_loaded` fired, otherwise defers to `plugins_loaded` (priority 30); base command returns early on undefined constant (defense in depth). |
| #6591 | `addons/graphify/includes/class-nvoos-graphify-nvoos-bridge.php`, `addons/pro/includes/class-wp-mcp-ai-memory-retention.php`, `includes/class-wp-mcp-ai-jetengine-agent-memories-cct.php` (docblock), `addons/graphify/tests/test-nvoos-bridge.php` | **Memory CCT phantom slug** — the CCT registers as `ai_agent_memories` but the Graphify bridge keyed label/content maps, MemPalace edge guard, and slug guesser on `ai_chat_agent_memories`, and Pro retention queried `wp_jet_cct_ai_chat_agent_memories` — so graph builds never mapped memory fields/edges and the dormancy sweep, per-user cap, expiry pruning, and Memory Health stats silently did nothing. All consumers now use the canonical slug; retention checks the canonical table first with a legacy-table fallback. |
| #6592 | root + addon lockfiles (`package-lock.json` ×7, pro/assets/spa, …) | **NPM security sweep** — sharp 0.35.3→0.35.4, nodemailer →9.1.1 (+ nested mailparser override), joi →18.2.8 (17.13.7 in pro/assets/spa), postcss-selector-parser →6.1.3/7.1.6; all same-major. `@ai-sdk/provider-utils` cross-major bump intentionally deferred. |
| #6593 | 3 content-graph build workflows (`.github/workflows/`) | **ZIP publish race fix** — build/publish derived the plugin version from different checkouts; `build` declares `outputs: version, artifact`, `publish`/`release` consume `needs.build.outputs.*`, per-ref concurrency guard with cancel-in-progress. |
| `6cd58234ef` (direct, no PR) | `.agents/skills/mcp-ai-wpoos-plugin/SKILL.md`, `.agents/skills/mcp-ai-wpoos-test-suite/SKILL.md`, `plugins/nvoos-content-graph/src/Admin/SettingsPage.php` (i18n array realign only) | **Checkout diagnostics in skills** — purchase-modal redirect root cause, release-tag URL convention, browser/server diagnostic recipes, standalone plugin-suite Docker runner; SettingsPage i18n array realigned for the new `stripe_setup_error` key (WPCS alignment). |

### Checkout launch-complete series (sub-projects `addons/checkout-api/` + `plugins/nvoos-content-graph/`)

- **#6557** — JetEngine CCT source selection on the Content Graph Sources tab (`SourcesCctsSection`, `excluded_cct_slugs` setting, `Detector::getCctTypes()` extraction, filter still applied last); 99 tests / 534 assertions green.
- **#6568** — Checkout admin settings form redirect fixed (browsers drop nested forms — the product/price admin-post form is now standalone); Stripe product/price IDs survive saves; nonce-protected "Test connection" balance probe (`GET /v1/balance`).
- **#6571** — Purchase modal trust/value/post-purchase content: 1-year updates + email support scope, 30-day guarantee, "Included in NV oOS Complete" block, EU withdrawal waiver, success-screen checklist; filterable roadmap/changelog/support-email defaults; ToS §7.1 updated (republish required before launch).
- **#6573** — Checkout connectivity diagnostics: vendor public `GET /health` + admin REST-endpoints self-check; client `GET /payments/health` (deliberately unthrottled); styled purchase-modal form fields.
- **#6587** — Stripe boolean serialization: `stringify_booleans()` converts `true` → literal `'true'` for form-encoded bodies (every live PaymentIntent had failed on `Invalid boolean: 1`).
- **#6588** — Per-CCT status notes in the Sources grid ("table not created yet", "no items yet", "N items indexed", "Excluded") via `Detector::inspectCctTypes()`; `detectCcts()` per-type report surfaced as `cct_types` in build summaries.
- **#6589** — Stripe error mapping: 4xx → **424** with Stripe's message (shown in-modal, no redirect); transport/5xx → **502** (release-page fallback). **Checkout API version bump 0.1.0 → 0.1.1**.
- **#6590** — sub-project docs catch-up: Content Graph **1.0.6 → 1.0.7** (header + constant + README + vendor-API docs) and Checkout API **0.1.1** docs (`@since` reconciliation, `GET /health` contract, 424/502 semantics, boolean-serialization requirement).
- **#6594** — Stripe element name fix (`create('paymentElement')` → `create('payment')` — `paymentElement` is the React component name; Stripe.js requires `payment`) + in-modal `stripe_setup_error` instead of the misleading redirect; release-tag convention `nvdigital-oos-v{VERSION}` in vendor `default_zip_source()` + client `Payments::zipUrl()`.

### Docs/skills-only

- **#6586** — skills update: new "Checkout Connectivity Diagnostics & Pro CLI Load-Order Guard (v1.1.77+)" section in the `mcp-ai-wpoos-plugin` skill; six new test-suite patterns (42–47) in `mcp-ai-wpoos-test-suite`.
- **`6cd58234ef`** (direct) — see production table (docs + i18n array realign only).

### Port PRs (sub-project track only — byte-identical into `plugins/nvoos-content-graph-pro/`, v1.0.0 unchanged)

#6551/#6552/#6554/#6556/#6558/#6559 (law-firm intake-management, litigation-support, compliance-ethics, document-automation, research-analytics, admin slice — **law-firm complete**), #6560/#6562–#6567 (cre-debt data layer → originations/underwriting/cmbs/debt-fund/asset-management + admin slice, 7 batches — **cre-debt complete**), #6569/#6572/#6574 (quiz data/tools/admin — **quiz complete**), #6575–#6578 (eca data/tools/REST/admin — **eca complete**), #6579–#6583 (chat-channels data/tools/REST A/B/admin — **chat-channels complete**), #6584 (places data layer — **places started**). Each carries its own `tests/` suite. `docs/project/ecosystem-port-tracker.md` maintained by the port loop itself (no action).

### Build-only

compiled-assets/distributions ×6, nvoos-content-graph v1.0.6 ZIP ×3 → v1.0.7 ZIP ×3, nvoos-content-graph-ai-platform v2.0.0 ZIP ×1, checkout-api v0.1.1 ZIP (in the compiled-assets commits), **oOS WP.org packages v1.1.76 ×2 (the current ZIP set — supersedes 1.1.75)**.

### Key scope rules for this pass

- **Historical entries stay.** Old changelog/README per-version blocks (`[1.1.76]` and earlier) are history and are not rewritten.
- **Tool counts do NOT change this window.** #6561 flips existing availability gates, #6570 adds a preset referencing existing slugs, #6555 is model-side, #6591 is service-side. No new slugs. Figures stay **~303 base + ~1,265 Pro (~1,568 total)** with the live-registry caveat — count mentions verified no-change on every surface.
- **Sub-project version tracks:** nvoos-content-graph **1.0.6 → 1.0.7** (bumped in-window by #6590), Checkout API **0.1.0 → 0.1.1** (bumped in-window by #6589/#6590), nvoos-content-graph-ai **1.0.4** (unchanged), nvoos-content-graph-ai-platform **2.0.0** (unchanged), nvoos-content-graph-pro **1.0.0** (unchanged — wave slices land without a bump), Docs Hub **0.4.3**, Comic Reader **0.5.0**, Media Worker **v3.2.0** (all unchanged).
- **Model catalog:** version bumped in-window to **v2026.09.10** (DeepSeek V4.1 Flash refresh, #6555); the Track B run already recorded itself in the model docs (`model-update-process-2026-07.md` "This Month's Changes" + `keeping-the-model-catalog-up-to-date.md` last-reviewed) — the catch-up folds it into the changelog per B3, no further model-doc edits.
- **Skill count stays 56** — no new skill directories in-window; #6586 + `6cd58234ef` extend the existing `mcp-ai-wpoos-plugin` + `mcp-ai-wpoos-test-suite` skills (test-suite patterns **40 → 47**; the skill's frontmatter description still said 40 — corrected this pass).
- **New `@since` anomaly group (OI-1 group 15):** eleven `@since 1.1.76` tags added in-window but shipping in 1.1.77 — `is_graph_tools_active()`, `is_content_graph_detected()`, `get_graphify_tool_slugs()`, `get_knowledge_graph_preset_tools()`, `get_effective_preset_selection()` (7 instances) in `includes/admin/class-wp-mcp-ai-onboarding-wizard.php` (PR #6570) + `is_peak_time()`, `get_model_pricing_at()`, `calculate_cost_at()` (4 instances) in `includes/class-wp-mcp-ai-cost-calculator.php` (PR #6555). Recorded in the open-items tracker — **not** fixed in-pass (parked). The checkout-api `@since 0.1.1` ×4 and content-graph `@since 1.0.7` ×7 tags added in-window are correct (their own version tracks).
- **Parked items stay parked** (OI-1/OI-2 in `docs-catch-up-open-items.md`): no `@since` reconciliation, no Docker live-count re-derivation. OI-2's "current figure" label refreshed to the v1.1.77 baseline (figures unchanged).
- **Housekeeping executed this pass** — remove stale build ZIPs: all `build/**/*1.1.75*` (30 files: 9 top-level incl. 3 `.sha256` + 2 optional-components + 19 toolkit-addons), superseded by the in-window 1.1.76 wp.org package builds. The 1.1.76 ZIP set is current, not stale.
- **Unrelated working-tree noise excluded** — untracked dirs/logs are not staged; `package-lock.json` untouched.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.77] - 2026-09-11` section above `[1.1.76]`: Added (DeepSeek V4.1 Flash refresh + peak/off-peak pricing #6555; Knowledge Graph Companion preset #6570; base+pro regression matrix #6561), Fixed (base+pro toolkit gating #6561; Pro WP-CLI load-order fatal #6585; memory CCT phantom slug #6591), Security (npm bumps #6592), Changed (content-graph build race #6593; checkout launch series complete #6568/#6571/#6573/#6587/#6588/#6589/#6590/#6594; Wave G ports #6551–#6584), Versioning (1.1.77, counts unchanged ~303 + ~1,265 (~1,568), content-graph 1.0.7, checkout-api 0.1.1, catalog v2026.09.10, skill count 56, stale 1.1.75 ZIPs removed).
2. **`readme.txt`** — Stable tag 1.1.77; new `= 1.1.77 - September 11, 2026 =` changelog paragraph (one paragraph, wp.org style).
3. **`README.md`** — version header → 1.1.77; Release Date 2026-09-11; new `🆕 v1.1.77 Highlights` paragraph; new "What's New at a Glance (v1.1.77)" section above v1.1.76; new "Latest Updates (v1.1.77 — September 2026)" block above v1.1.76. Count mentions verified unchanged (~1,568).
4. **`docs/QUICK_REFERENCE.md`** — version + last-updated → 1.1.77/September 11; v1.1.77 entry above v1.1.76.
5. **`docs/DOCUMENTATION_INDEX.md`** — Plugin Version + Last Updated → 1.1.77/September 11; new SEPTEMBER 11 UPDATE banner above the v1.1.76 one; new `## 🆕 September 2026 — v1.1.77` section header + "New and updated documents" block (incl. the model docs + sub-project docs updated in-window).
6. **`docs/project/FOR_REVIEWERS.md`** — current version → 1.1.77 (September 2026); count mentions verified unchanged (~1,568).

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed stamp + "New in v1.1.77 (counts unchanged)" paragraph (no new slugs: #6561 flips gates, #6570 preset references existing slugs, #6555 model-side, #6591 service-side).
8. **`.context/security-checklist.md`** — v1.1.77 update line: base+pro gate escape (availability ≠ authorization); Pro WP-CLI load-order guard (both guards kept); memory CCT canonical-slug + legacy-fallback read path; checkout sub-project security (nonce-protected read-only balance probe, 424/502 contract never leaks stack details, masked Stripe keys); npm bumps; DeepSeek retirement/migration.
9. **`.context/testing.md`** — last-reviewed + v1.1.77 note: new `tests/basepro/` matrix (15 tests, `phpunit-basepro.xml.dist`, base-pro CI job, `composer run test:basepro`); DeepSeek model-suite alignment (170/6,677 green); onboarding-wizard +8 (31/31 both matrices); graphify bridge canonical slug (17/27); checkout-api suite growth (51→63→67); test-suite skill **40 → 47** patterns.
10. **`.context/rest-api.md`** — v1.1.77 note: no base REST changes; sub-project Checkout API 0.1.1 gains public `GET /health` + 424/502 `/payments/session` contract; Content Graph 1.0.7 gains admin-only unthrottled `GET /payments/health`.
11. **`CLAUDE.md`** — header stamp → September 11, 2026 · 2.30; v1.1.77 notes (DeepSeek refresh, KG preset, base+pro gating + matrix, Pro CLI guard, memory CCT slug, checkout series, security/CI sweeps, Wave G); count mentions verified unchanged (~1,568).
12. **`MAINTAINER_MAP.md`** — last-reviewed bump → September 11, 2026 (v1.1.77).
13. **`.bmad/README.md`** — last-reviewed bump → v1.1.77.
14. **All three `mcp-ai-wpoos-plugin` SKILL.md copies** — `plugin-version`/`plugin-version-tested`/`last-updated` → 1.1.77/2026-09-11 (the `.agents/skills/` copy also gains a v1.1.77 note section above the v1.1.76 block).
15. **`.agents/skills/mcp-ai-wpoos-updates/SKILL.md`** — frontmatter → 1.1.77/2026-09-11; body references (distilled-plan range, template-plan names, commit-structure mirror, references list) → v1.1.76.
16. **`.agents/skills/mcp-ai-wpoos-test-suite/SKILL.md`** — frontmatter description "40 recurring root-cause patterns" → **47** (patterns 41–47 landed in-window; the description was stale).

### P2 — Operational & feature docs

17. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" stamp → September 11, 2026 (v1.1.77); Checkout API row 0.1.0 → **0.1.1**.
18. **`docs/reference/tools/tool-reference.md`** — Last Updated → September 11, 2026 (v1.1.77); Tool Count verified unchanged.
19. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-11 (v1.1.77 pass); Scope line gains v1.1.77; **new OI-1 group 15** (`@since 1.1.76` ×11 — recorded, not fixed); OI-2 current-figure label → v1.1.77 (figures unchanged).
20. **`docs/project/plans/v1.1.77-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

21. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.77 (`package-lock.json` intentionally untouched).

### Housekeeping (executed this pass)

22. **Remove stale build ZIPs** — `git rm` all `build/**/*1.1.75*` (30 files: 9 top-level incl. 3 `.sha256` + 2 optional-components + 19 toolkit-addons), superseded by the in-window 1.1.76 wp.org package builds. Commit: `chore: remove stale 1.1.75 build ZIPs [skip ci]`.

### Verify-only (no changes expected)

- Skill count after bookkeeping equals the actual 56 directories under `.agents/skills/` (no new skills in-window — #6586/`6cd58234ef` extend existing skills).
- `addons/pro/README.md` — toolkit count stays 31 (no new tools).
- `docs/project/proposals/` — no new proposals in-window.
- `.context/README.md`, `.context/conventions.md`, `.context/pro-vs-base.md` — count mentions (~1,568) unchanged.
- `addons/docs-hub/CHANGELOG.md`, `addons/comic-reader/CHANGELOG.md`, `addons/checkout-api/CHANGELOG.md`, `plugins/nvoos-content-graph/CHANGELOG.md` — own tracks; checkout-api 0.1.1 + content-graph 1.0.7 changelog entries landed in-window (#6589/#6590).
- `docs/project/ecosystem-port-tracker.md` — maintained by the port loop itself; no action.
- `docs/reference/models/model-update-process-2026-07.md` + `keeping-the-model-catalog-up-to-date.md` — already updated by #6555's own docs commit; verified, no action.
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" description — base count unchanged.

---

## 3. Execution log (2026-09-11)

- **Orientation** — window mapped: 26 port PRs (#6551–#6584) + 12 non-port PRs (#6555/#6557/#6561/#6568/#6570/#6571/#6573/#6585–#6594) + 1 unnumbered direct commit (`6cd58234ef`) + 15 build commits; all non-port PR descriptions read in full; classification recorded in §1. `@since` diff sweep found the OI-1 group 15 candidates (onboarding-wizard ×7, cost-calculator ×4).
- **P3** — version bump committed `2777c5734f` (`chore: bump version to 1.1.77`); `package-lock.json` untouched; `php -l` + `package.json` JSON parse green.
- **P0** — release-note surfaces committed `f554478dfb` (`docs: add 1.1.77 changelog and release notes`).
- **P1** — agent context committed `d2db484623` (`docs: update agent context for 1.1.77 changes`): 4 `.context/` files, `CLAUDE.md` (2.30), `MAINTAINER_MAP.md`, `.bmad/README.md`, 3 `mcp-ai-wpoos-plugin` SKILL.md copies (frontmatter + new v1.1.77 section in the `.agents` copy), `mcp-ai-wpoos-updates` SKILL.md (frontmatter + plan references → v1.1.76), `mcp-ai-wpoos-test-suite` SKILL.md (description 40 → 47).
- **P2** — this commit (`docs: reconcile 1.1.77 reference surfaces`): `ADDON_INVENTORY.md` (stamp + checkout-api 0.1.1) + `tool-reference.md` stamp, open-items tracker (last-reviewed, scope, OI-1 group 15, OI-2 label refresh), this plan.
- **Housekeeping** — stale 1.1.75 build ZIPs removed in the following commit (`chore: remove stale 1.1.75 build ZIPs [skip ci]`).
- **Validation** — §5 checks all green (see below).

## 4. Commit breakdown (mirror v1.1.58–v1.1.76)

1. `chore: bump version to 1.1.77` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.77 changelog and release notes` — `CHANGELOG.md`, `readme.txt`, `README.md`, `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.77 changes` — `.context/*` (4 files), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies, `.agents/skills/mcp-ai-wpoos-updates/SKILL.md`, `.agents/skills/mcp-ai-wpoos-test-suite/SKILL.md`
4. `docs: reconcile 1.1.77 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/plans/docs-catch-up-open-items.md`, `docs/project/plans/v1.1.77-docs-catch-up.md`
5. `chore: remove stale 1.1.75 build ZIPs [skip ci]` — `build/**/*1.1.75*` (30 files), housekeeping

> Note: no separate count-sweep commit this pass — tool counts did not change.

## 5. Validation

- `php -l` on the three bumped PHP files ✅
- `package.json` JSON parse ✅
- `mb_check_encoding` (UTF-8) on all 18 edited markdown/text files ✅
- `git grep "1\.1\.76"` sweep — remaining hits are intentional history (previous-release sections, `@since` docblocks incl. the new OI-1 group 15, plans, archive) ✅
- `git grep "1,265"` / `"1,568"` sweep — live surfaces only + historical per-version blocks; no stale counts introduced ✅
- Stale-ZIP sweep — `git ls-files 'build/*'` re-checked: no 1.1.75 files remain; 1.1.76 set is current ✅
- Skill-count verification — 56 directories under `.agents/skills/` == unchanged counts in AGENTS.md / copilot-instructions / README ✅

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue [#5968](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5968)) and Docker-based live-count re-derivation (OI-2, issue [#5967](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5967)) stay parked. OI-2's current-figure label refreshed to the v1.1.77 baseline (figures unchanged — ~303 base + ~1,265 Pro, ~1,568 total).
- **New this pass:** eleven `@since 1.1.76` tags added in-window but shipping in 1.1.77 — `is_graph_tools_active()`, `is_content_graph_detected()`, `get_graphify_tool_slugs()`, `get_knowledge_graph_preset_tools()`, `get_effective_preset_selection()` in `includes/admin/class-wp-mcp-ai-onboarding-wizard.php` (PR #6570) + `is_peak_time()`, `get_model_pricing_at()`, `calculate_cost_at()` in `includes/class-wp-mcp-ai-cost-calculator.php` (PR #6555) recorded as OI-1 group 15 (parked, not fixed).
- **Noted observation (sub-project/port tracks):** #6591 flags `class-nvoos-graphify-nvoos-bridge.php` as in-scope for the ecosystem port — the next port wave must pick up the memory CCT slug change or the byte-identity verifier will flag drift; #6594 notes the checkout addon must be redeployed to the vendor server and the ToS republished at nvdigitalsolutions.com before launch; #6587/#6589 follow the same vendor-redeploy note. Handled by those workstreams, not the docs pass.
- **Housekeeping executed this pass:** stale 1.1.75 build ZIPs (30 files) removed.
