# Docs & Release Catch-Up Plan — v1.1.59 (PRs #5892–#5895)

> **Prepared:** 2026-08-19 · **Status:** ✅ **Executed & committed 2026-08-19** (all P0–P3 items complete; see execution log below)
> **Scope:** documentation/context updates + version bump to **1.1.59** (no production-code changes)
> **Input:** full review of every commit on `alpha-working` from 2026-08-17 through 2026-08-19
> **Predecessor:** [`v1.1.58-docs-catch-up.md`](v1.1.58-docs-catch-up.md) (executed Aug 18; covered PRs #5881–#5890)

---

## 1. Context: What changed since the last docs catch-up

The Aug 18 catch-up covered everything through PR #5890 and wrote the `[1.1.58]` changelog. **Four PRs merged afterward** (plus two workflow commits) were absent from every release-note surface, and two facts in the catch-up itself had drifted:

| PR / Commit | Change | Doc status after this pass |
|---|---|---|
| #5892 | **Media Worker crawling + Crawl4AI facade** — native `/api/crawl/*` endpoints (markdown, markdown-batch, links), two-tier static/browser extraction behind the shared SSRF guard, `/api/crawl4ai/*` facade for `run_crawl4ai_job` remote mode, `llm-extract.js`; worker bumped **v3.2.0**; toolkit memory estimate accounts for the sidecar | ✅ changelog/readme/index/context + addon README (updated in-commit) + plan 031 |
| #5893 | **Research tools multi-provider** — shared embedding-provider abstraction (new Gemini embedding provider), `deep_research` provider-chain retries/no-empty-cache, new base tools `list_terms` + `list_taxonomies` | ✅ changelog/readme/index/context; tool reference + skills updated in-commit |
| #5894 | **Docs Hub 0.4.1** — new `wp_mcp_ai_plugin_updated` updater action, cache-clear + async rebuild on update/activation, `admin_init` version-mismatch guard, slug-map link resolution + clamped suggestions | ✅ changelog/readme/index/context + docs-hub README + addon CHANGELOG (updated in-commit) |
| #5895 | **Tool registration + QA** — legacy-tool auto-wrap, `wp_mcp_ai_tools_init` hook, `unavailable_tool_slugs`, ~32 orphaned base tools registered; QA container memory limits | ✅ changelog/readme/index/context; tests updated in-commit |
| `9a678c817`, `0defffe40`, `7d4f54600` | Content-graph-ai sync workflow + `workflow_dispatch`; repo-sync documented in the `-ai` plugin README | ✅ self-documented in-commit; verified only |

Build-only commits (ZIPs, temporary secret-probe workflow add/remove) need no doc action.

### Drift found in the Aug 18 catch-up

- **Media Worker version** was documented as "v3.0.0 (unchanged)" but `addons/media-worker/package.json` is **3.2.0** after PR #5892 (the proposals index even said 3.1.0 — three-way drift, now reconciled to 3.2.0 everywhere).
- **Tool counts** (~1,508) were stale: PR #5893 added 2 base tools and PR #5895 registered ~32 previously-orphaned base tools. New figures: **~300 base + ~1,243 Pro (~1,543 total)** with the live-registry caveat.
- `README.md` still carried four ~1,502-tool mentions from v1.1.57 (missed by the previous pass).
- `docs/reference/tools/tool-reference.md` header still said "~201 base + ~830+ Pro (~1,031+)" from July.

### Key scope rules for this pass

- **Historical entries stay.** Old changelog/README per-version blocks (`[1.1.58]` and earlier) are history and are not rewritten, including their tool counts and the "Media Worker v3.0.0 (unchanged)" line.
- **`addons/graphify/` remains unrenamed** (per the 1.1.58 pass) — only `plugins/nvoos-graphify*` were renamed.
- **Tool counts are approximations** ("~" + `WP_MCP_AI_Tool_Registry::get_tools()` authoritative). The QA container registry read (363 tools) reflects an unprovisioned environment and was not used as the source of truth; figures are deltas from the established 1.1.58 baseline (265 + 2 new + ~32 orphans ≈ ~300).
- **Version bump:** 1.1.59 everywhere (plugin headers, constants, Pro addon, `package.json`, `readme.txt` Stable tag, docs). Media Worker stays on its own 3.2.0 track.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.59] - 2026-08-19` section: Changed (PR #5892 media worker crawling + Crawl4AI facade), Changed (PR #5893 research tools multi-provider + 2 new tools), Fixed (PR #5894 Docs Hub rebuild/broken links + `wp_mcp_ai_plugin_updated`), Fixed (PR #5895 tool registration + QA), Versioning (1.1.59, Pro 1.1.59, Media Worker v3.2.0, ~300 base + ~1,243 Pro / ~1,543).
2. **`readme.txt`** — Stable tag 1.1.59; new `= 1.1.59 - August 19, 2026 =` changelog paragraph; header "265+ built-in tools" → "300+".
3. **`README.md`** — version/release-date header; v1.1.59 Highlights paragraph; new "What's New at a Glance (v1.1.59)" section; new "Latest Updates (v1.1.59)" section; **five stale tool-count mentions fixed** (tool classes ~1,502 → ~1,543; install/upgrade path counts; tool-reference link; Media Worker addon blurb → v3.2.0 + crawl endpoints).
4. **`docs/QUICK_REFERENCE.md`** — version + last-updated; v1.1.59 entry above v1.1.58.
5. **`docs/DOCUMENTATION_INDEX.md`** — Plugin Version + Last Updated; new AUGUST 19 UPDATE banner; section header v1.1.55–v1.1.59 (worker v2.2.0→v3.2.0); new "New and updated documents (August 19, 2026, v1.1.59)" block (plans 031 + research-tools plan, tool reference, media-worker README, TOOLKIT_MEMORY_TRACKING, docs-hub CHANGELOG/README, README/CHANGELOG/QUICK_REFERENCE/readme.txt); Docs Hub addon-history line → 0.4.1.

### P1 — Agent context files

6. **`.context/tool-registry.md`** — last-reviewed + counts; new v1.1.59 paragraph: `list_terms`/`list_taxonomies`, orphan registrations, legacy auto-wrap, `wp_mcp_ai_tools_init` hook, `unavailable_tool_slugs`.
7. **`.context/media-worker.md`** — last-reviewed + v3.2.0; new "Crawling & Crawl4AI Facade (v3.2.0)" section (endpoints, two-tier extraction, SSRF rule); route-group count + proposal 031 in the paths table.
8. **`CLAUDE.md`** — v1.1.59 notes: worker crawling, research-tools hardening, `wp_mcp_ai_plugin_updated` addon contract, tool-registration fixes.
9. **`.context/security-checklist.md`** — v1.1.59 note: crawl SSRF rule, `wp_mcp_ai_plugin_updated` scoping, legacy wrapper (no auth relaxation).
10. **`.context/README.md`** — tool-count fact ~1,508 → ~1,543.

### P2 — Operational & feature docs

11. **`docs/project/ADDON_INVENTORY.md`** — Media Worker 3.0.0 → 3.2.0 + crawl endpoints/facade.
12. **`docs/project/FOR_REVIEWERS.md`** — current version 1.1.59; total tools ~1,543; "~300+ tools" intro; Media Worker row → 3.2.0 + crawl; "~1,540 tools" registry mention.
13. **`docs/project/proposals/RELATED_PROPOSALS_INDEX.md`** — 031 row "worker v3.1.0" → v3.2.0. (Research-tools implementation plan lives in `docs/developer/`, outside this proposals-scoped index — indexed via DOCUMENTATION_INDEX instead.)
14. **`addons/docs-hub/README.md`** — caching/cron-rebuild rows (0.4.1 triggers); new "Rebuild triggers (since v0.4.1)" + "Broken-link detection & suggestions" subsections.
15. **`docs/reference/tools/tool-reference.md`** — header status/count/last-updated reconciled (July ~1,031 → August ~1,543). Tool entries for the PR #5893 changes were already added in-commit.
16. **`.agents/skills/design-media-workflow/SKILL.md`** — crawl/crawl4ai rows in the worker-fallback table.
17. **`.agents/skills/design-web-research/SKILL.md`** — "Media Worker fallback (worker v3.2.0+)" subsection under `run_crawl4ai_job`.
18. **`MAINTAINER_MAP.md` / `.bmad/README.md`** — last-reviewed bumps.

### P3 — Version bump (mechanical)

19. `mcp-ai-wpoos.php` (Version header + "300+ tools" description), `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`, `readme.txt` Stable tag.

### Verify-only (no changes expected)

- `plugins/nvoos-content-graph-ai-platform/README.md` sync-workflow mention — covered by the rename commit.
- `tests/qa/README.md` — compose path unchanged (memory limits are infra-only).
- `docs/features/MEDIA_RESPONSE_SYSTEM.md` / `media-command-center.md` — no route enumeration to update.
- `AGENTS.md` skill counts — still 51 (two skills edited in place).

---

## 3. Execution log (2026-08-19)

- **P3 version bump:** `mcp-ai-wpoos.php` (Version 1.1.59 + "300+ tools" description), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.59 ✅ (`package-lock.json` intentionally untouched, matching 1.1.58 precedent)
- **P0 release surfaces:** `CHANGELOG.md` [1.1.59] section (PRs #5892–#5895 + Versioning), `readme.txt` (Stable tag + 1.1.59 changelog + 300+ header), `README.md` (header/highlights/What's-New/Latest-Updates + 5 stale count fixes + Media Worker blurb), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md` (banner, section header, Aug 19 block, Docs Hub 0.4.1 line) ✅
- **P1 agent context:** `.context/tool-registry.md` (counts + v1.1.59 mechanics), `.context/media-worker.md` (crawl section + v3.2.0), `CLAUDE.md` (4 new notes), `.context/security-checklist.md` (v1.1.59 note), `.context/README.md` (count) ✅
- **P2 operational docs:** `ADDON_INVENTORY.md` (Media Worker row), `FOR_REVIEWERS.md` (5 fixes), `RELATED_PROPOSALS_INDEX.md` (031 → v3.2.0), `addons/docs-hub/README.md` (rebuild triggers + link suggestions), `docs/reference/tools/tool-reference.md` (header), `design-media-workflow` + `design-web-research` skills ✅
- **Verify-only items:** all confirmed no-change ✅
- **Tool counts:** authoritative live-registry read was attempted against the running QA container (`oos-qa-wp`, plugin mounted live). The registry there returns 363 tools because the QA database is unprovisioned (no toolkit seeds/optional plugins), so counts were derived as deltas from the 1.1.58 baseline: 265 + 2 (PR #5893) + ~32 orphan registrations (PR #5895) ≈ **~300 base**; Pro unchanged **~1,243**; total **~1,543**. Live-registry caveat retained on every count surface.
- **Commit:** changes committed 2026-08-19 in four commits mirroring the 1.1.58 structure: `chore: bump version to 1.1.59`, `docs: add 1.1.59 changelog and release notes`, `docs: update agent context for 1.1.59 changes`, `docs: reconcile 1.1.59 reference surfaces` (the last also carries this plan). Unrelated working-tree noise (`vendor/` composer/guzzle updates, untracked `design-elementor-template-kits` skill) intentionally excluded.
- **Validation:** see section 4.

## 4. Validation

- `php -l` on the three bumped PHP files ✅
- `package.json` JSON parse ✅
- `composer run docs:check-folder-readmes` ✅ (no folder READMEs created/edited)
- `git grep "1\.1\.58"` sweep — remaining hits are intentional history (previous-release sections, `@since` docblocks, archive) ✅
- `git grep -E "1,508|3\.0\.0 \(unchanged\)|265\+"` sweep — remaining hits are historical per-version blocks ✅

## 5. Open items for the next pass

- **Deferred by decision (2026-08-19):** the two items below were explicitly parked — do not reconcile old tags or run Docker-based live-count work until they are revisited in a later pass.
- **`@since 1.2.0` docblocks** in `includes/class-wp-mcp-ai-tool-registry.php` (hook + orphan registration comments) and `mcp-ai-wpoos.php` export-provider block suggest a planned 1.2.0; decide whether the next release continues 1.1.x or jumps to 1.2.0 and reconcile tags then.
- **Live-count re-derivation** on a fully provisioned environment (seeded toolkits + optional plugins) to replace the delta-based ~1,543 figure if it drifts.
