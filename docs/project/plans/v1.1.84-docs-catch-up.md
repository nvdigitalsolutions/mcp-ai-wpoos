# Docs & Release Catch-Up Plan — v1.1.84 (PRs #6745–#6747)

## 1. Context: What changed since the v1.1.83 post pass

The v1.1.83 post-window pass merged as `2ef3961b0b` ("chore: 1.1.83 post-window docs catch-up (PRs #6726–#6743) (#6744)") on 2026-09-22. **3 more PRs** (#6745–#6747) and **2 build commits** landed on `alpha-working` afterward. Per user direction, the TypeSafe Jev enhancement wave is a **new release** — 1.1.83 already carries a large changelog, so this window opens the `[1.1.84]` line (full version bump + new changelog section, not a 1.1.83 extension).

### Production-touching PRs

| PR | Files | Change |
|---|---|---|
| #6745 | `includes/tools/class-wp-mcp-ai-tool-typesafe-decide.php`, `includes/interfaces/interface-wp-mcp-ai-decision-client.php`, `docs/features/ai-providers/{README.md,typesafe.md}`, `docs/reference/tools/tool-status.txt` | **`typesafe_decide` capability metadata aligned with the enforced gate** — `get_required_capability()` returned `edit_posts` while `execute()` enforces `manage_options`; the declared metadata now matches (admin-only) and `test_tool_metadata` pins it. Decision-client interface docblock corrected (implementations live in `includes/`, not the chat-provider adapters dir). Docs: `typesafe.md` documents the `manage_options` requirement + the `ai_ml` preset; the ai-providers index links the TypeSafe guide; `tool-status.txt` gains `typesafe_decide = beta`. No runtime behavior change. |
| #6747 | `includes/class-wp-mcp-ai-typesafe-client.php` (+281), `includes/tools/class-wp-mcp-ai-tool-typesafe-guardrail.php` (new, 427), `includes/tools/class-wp-mcp-ai-tool-typesafe-decide.php` (+192), `includes/class-wp-mcp-ai-openrouter-client.php` (+97), `includes/class-wp-mcp-ai-tool-registry.php` (+1 registration), `includes/interfaces/interface-wp-mcp-ai-decision-client.php`, `includes/data/model-catalog.json` (→ v2026.09.22, +1 `jev-preview` entry), `includes/admin/sections/class-wp-mcp-ai-section-providers.php`, `includes/admin/class-wp-mcp-ai-provider-diagnostics.php`, `includes/helpers/class-wp-mcp-ai-tool-presets-helper.php`, `includes/bundled-skills/mcp-ai-wpoos-jev-decisions/SKILL.md` (new skill), Pro: `addons/pro/includes/services/class-wp-mcp-ai-pro-jev-{guardrail,eval}.php` (new) + `class-wp-mcp-ai-pro-jev-classifier.php` (+140), `addons/pro/includes/tools/jev/class-wp-mcp-ai-pro-tool-typesafe-{rerank,eval,skill-select}.php` (new), `addons/pro/includes/tools/research/class-wp-mcp-ai-tool-research-eca.php` + `.../class-wp-mcp-ai-tool-generate-research-report.php`, `addons/pro/mcp-ai-wpoos-pro.php` (+3 registrations + guardrail wiring), tests (`test-typesafe-{client,decide-tool,guardrail-tool}.php`, `test-openrouter-client.php`, `addons/pro/tests/test-pro-jev-phase2.php` new), docs (typesafe guide, tool-reference, tool-status, ai-providers) | **TypeSafe Jev enhancement plan 040 implemented (Phases 0–2)** — Phase 0 fidelity: noul criteria + structured EntryType fields with a recursive two-gate sanitisation walk; bounded 429/5xx retry honouring `retry-after` (filters `wp_mcp_ai_typesafe_retry_attempts`/`_retry_sleep`, shared native + OpenRouter bridge, 4xx/transport never retried); OpenRouter bridge defaults to `typesafe/jev-1.13` + normalises aliases; opt-in advisory decision cache (`enable_typesafe_cache`, 300 s TTL `wp_mcp_ai_typesafe_cache_ttl`, `cached: true` + zeroed usage, key embeds endpoint/base/model/payload); `typesafe_endpoint` setting + filter; `jev-preview` alias (catalog/settings/`list_models()`); cache state in provider diagnostics; `min_confidence` (below-threshold flags, values kept), `weights` (local composite scoring), advisory token warnings (`wp_mcp_ai_typesafe_warn_tokens`), `prompt_tokens`/`completion_tokens` usage aliases. **Phase 1:** new base tool **`typesafe_guardrail`** (batches one noul question per hazard category → advisory pass/review/block verdicts; default hazard set or custom map; registered in the registry, `ai_ml` preset, coverage manifest, tool-status) — **+1 base registration**; new bundled skill `mcp-ai-wpoos-jev-decisions` (question design, batching, thresholds, composite scoring, guardrails) — **bundled skills 74 → 75 base**. **Phase 2 (Pro):** guest-chat guardrail `WP_MCP_AI_Pro_Jev_Guardrail` on the Layer I `wp_mcp_ai_pre_chat_message` filter (opt-in `enable_jev_guest_guardrail`, vetoes only high-confidence blocks, fail-open); citation checking `WP_MCP_AI_Pro_Jev_Classifier::check_citations()` (`[n]` markers vs source passages, one noul per cited source cap 10, opt-in seams in `generate_research_report`/`research_eca`, fail-open, attached as `citation_checks`); three new Pro tools **`typesafe_rerank`** (batched candidate re-ranking, keep-min floor), **`typesafe_eval`** + `WP_MCP_AI_Pro_Jev_Eval` (overall + per-confidence-bucket accuracy on inline labeled examples, report-only), **`typesafe_skill_select`** (two-stage rank + re-check over the bundled catalog) — **+3 Pro registrations**, all `manage_options`-gated, both transports, canonical envelope + guidance interfaces. Deferred per the plan: extraction tools, the CG port cluster, NV Cloud passthrough (OpenRouter Decisions GA). |

### Docs-only PRs

- #6746 — Proposal 040 + implementation plan (`docs/project/proposals/040-typesafe-jev-enhancements.md` + `-implementation-plan.md`): six API-fidelity fixes verified against the current TypeSafe docs, official TypeSafe patterns as first-class tools/services, cost/reach work, five PR clusters (Phase 0–3), per-phase file/test tables, risk register, NOT-changed list. Proposals README registers 040 under "Currently In Progress". Markdown only.

### Build-only (2 commits)

`030c9722eb` — oOS WP.org packages v1.1.83 rebuilt post-#6744 (the 1.1.83 package set now contains the post-window code); `f86b750b24` — compiled assets and distributions. No direct commits in-window.

### Key scope rules for this pass

- **`[1.1.84]` is the new current release** — new changelog section at the top; `[1.1.83]` and earlier stay untouched.
- **Counts change:** +1 base (`typesafe_guardrail`, #6747) +3 Pro (`typesafe_rerank`, `typesafe_eval`, `typesafe_skill_select`, #6747) → **~308 base + ~1,282 Pro (~1,590 total)** with the live-registry caveat. Historical per-version blocks keep their counts.
- **Model catalog:** v2026.09.21 → **v2026.09.22** (+1 `jev-preview` typesafe decision entry, #6747).
- **Bundled skills:** 74 → **75** base (`mcp-ai-wpoos-jev-decisions`); Pro stays 41 (the 42nd `addons/pro/includes/bundled-skills/` entry is `THIRD_PARTY_NOTICES.md`).
- **Coding-time agent skills stay 59** — no new `.agents/skills/` entries in-window.
- **Sub-project tracks:** all unchanged — Docs Hub 0.4.7, nvoos-content-graph 1.0.8, nvoos-content-graph-ai 1.0.4, content-graph-ai-platform 2.0.0, content-graph-pro 1.0.0 (no port waves in-window), Checkout API 0.1.2, Comic Reader 0.5.0, Media Worker v3.2.0.
- **Housekeeping NOT executed this pass (user-directed)** — the 1.1.83 build ZIP set stays: it is the current wp.org release artifact set (rebuilt in-window by `030c9722eb`), and the 1.1.84 package builds land post-merge via CI. Stale-ZIP removal moves to the next catch-up once the 1.1.84 packages exist.
- **Parked items stay parked** (OI-1/OI-2): record new `@since` anomalies, never fix. OI-1 group 19 (`@since 1.1.84` ×4) **self-resolves** with this ship — recorded, not touched.
- **Unrelated working-tree noise excluded** — untracked `docs/user-guides/assistants/CONTENT-GRAPH-MARKETING-ASSISTANT.md`, `.codex-wordpress/` runtime copies, root `vendor/`, `package-lock.json` are never staged.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.84] - 2026-09-22` section: Added subsections for the Phase 0 API-fidelity fixes (#6747), the base `typesafe_guardrail` tool + Jev-decisions bundled skill (Phase 1, #6747), and the Pro Jev integrations (Phase 2, #6747); Fixed subsection for the `typesafe_decide` capability-metadata alignment (#6745); a docs subsection for proposal/plan 040 (#6746). Versioning block: version bump 1.1.84; tool count → **~308 base + ~1,282 Pro (~1,590 total)** (+1 base +3 Pro, #6747); model catalog → **v2026.09.22** (+1 `jev-preview`); bundled skills → **75 base + 41 Pro**; coding-time skills 59 (unchanged); no stale ZIP removal this pass (1.1.83 set retained as current).
2. **`readme.txt`** — `Stable tag: 1.1.84`; new `= 1.1.84 - September 22, 2026 =` changelog paragraph above the 1.1.83 entry (TypeSafe Jev enhancement wave: fidelity fixes, `typesafe_guardrail`, Pro guardrail/rerank/eval/skill-select, capability fix, counts).
3. **`README.md`** — `**Version:** 1.1.84` / `**Release Date:** 2026-09-22`; v1.1.84 Highlights paragraph replaces the v1.1.83 one at the top (v1.1.83 history preserved in Latest Updates); new "What's New at a Glance (v1.1.84)" section above the v1.1.83 one; "Latest Updates (v1.1.83 — September 2026)" heading → v1.1.84 with a new September 22 block on top; evergreen count lines ~307→~308 / ~1,279→~1,282 / ~1,586→~1,590 (skip `[1.1.83]`-and-earlier history).
4. **`docs/QUICK_REFERENCE.md`** — `**Version:** 1.1.84` / `**Last Updated:** September 22, 2026`; new v1.1.84 recent-updates entry above the v1.1.83 one; plan link gains this plan.
5. **`docs/DOCUMENTATION_INDEX.md`** — `**Plugin Version:** 1.1.84` / `**Last Updated:** September 22, 2026`; new SEPTEMBER 22, 2026 banner above the SEPTEMBER 21 one (with the updated-docs list: this plan, README/CHANGELOG/readme.txt/QUICK_REFERENCE + the in-window typesafe guide/tool-reference/proposal docs).
6. **`docs/project/FOR_REVIEWERS.md`** — "Current version" → 1.1.84; evergreen counts ~307→~308 / ~1,279→~1,282 / ~1,586→~1,590 (incl. the freemium-table Pro count).

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed → September 22, 2026 (v1.1.84); new v1.1.84 paragraph: +1 base `typesafe_guardrail` (#6747), +3 Pro `typesafe_rerank`/`typesafe_eval`/`typesafe_skill_select` (#6747); the Phase 0 client/settings/catalog changes are decision-side, not registrations; bundled skill `mcp-ai-wpoos-jev-decisions`; "Total tools" line → ~1,590 (~308 base + ~1,282 Pro).
8. **`.context/security-checklist.md`** — last-reviewed + v1.1.84 line: `typesafe_decide`/`typesafe_guardrail`/the three new Pro Jev tools are all `manage_options`-gated; the guest-chat Jev guardrail vetoes only high-confidence block verdicts and fails open; the advisory decision cache keys embed endpoint/base/model/payload and zero usage on hits; citation checks are advisory (`citation_checks`) and fail open.
9. **`.context/testing.md`** — last-reviewed + v1.1.84 note: `test-typesafe-client` + `test-typesafe-decide-tool` extended, new `test-typesafe-guardrail-tool`, `test-openrouter-client` extended, new Pro `test-pro-jev-phase2` (474 lines); #6747 validation reports 145 tests / 3,510 assertions green across the targeted suites (Docker full suite not run in-session).
10. **`.context/rest-api.md`** — verify-only: no new REST routes in-window (the Jev changes are tools, filters, settings, and a chat-pipeline filter seam).
11. **`CLAUDE.md`** — stamp → September 22, 2026 · 2.38; new v1.1.84 bullet (Jev plan-040 implementation — fidelity, `typesafe_guardrail`, Pro guardrail/citations/rerank/eval/skill-select, capability fix #6745; deferred extraction/CG port/NV Cloud); evergreen count lines ~307→~308 / ~1,279→~1,282 / ~1,586→~1,590; catalog mention → v2026.09.22.
12. **`MAINTAINER_MAP.md`** — last-reviewed → September 22, 2026 (v1.1.84).
13. **`.bmad/README.md`** — last-reviewed → September 2026 (v1.1.84).
14. **Three `mcp-ai-wpoos-plugin` SKILL.md copies** (`.agents/skills/`, `includes/bundled-skills/`, `plugins/nvoos-content-graph-ai-platform/data/bundled-skills/`) — `plugin-version`/`plugin-version-tested` → 1.1.84, `last-updated` stays 2026-09-22; the `.agents/skills/` copy's v1.1.83 note section gains a v1.1.84 line.
15. **`.agents/skills/mcp-ai-wpoos-updates/SKILL.md`** — references: latest executed plan → `v1.1.84-docs-catch-up.md`; template list gains it.
16. **`AGENTS.md` + `.github/copilot-instructions.md`** — runtime bundled-skills count 74 → 75 base (AGENTS.md inventory rows + the coding-time-vs-runtime paragraph; copilot-instructions repo-tree comment "~1,586 total built-in tool implementations" → ~1,590 + the Base/Full version counts ~307/~1,279 → ~308/~1,282). Coding-time skill count stays 59 (verify-only).

### P2 — Operational & feature docs

17. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" → September 22, 2026 (v1.1.84); addon rows unchanged.
18. **`docs/reference/tools/tool-reference.md`** — "Last Updated" → September 22, 2026 (v1.1.84 — TypeSafe Jev enhancement wave); the Tool Count line re-attributes the +2 base / +3 Pro from "v1.1.83 post" to v1.1.84.
19. **`docs/project/proposals/README.md`** — proposal 040 status text: Phases 0–2 implemented (PR #6747); Phase 3 cost/docs + extraction tools + CG port cluster remain deferred (still listed under In Progress).
20. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-22 (v1.1.84 pass); OI-1: mark group 19 (`@since 1.1.84` ×4) ✅ self-resolved by the 1.1.84 ship, extend group 22 (+1 `@since 1.9.0` in `class-wp-mcp-ai-pro-jev-classifier.php`, #6747), add group 24 (`@since 1.9.0` ×5 — new Jev Phase 2 files: pro-jev-eval, pro-jev-guardrail, tool-typesafe-eval, tool-typesafe-rerank, tool-typesafe-skill-select, #6747), extend group 23 (`@since 2026.09` +4 — openrouter-client +1, typesafe-client +3, #6747); OI-2 current-figure label → v1.1.84 (~308 base + ~1,282 Pro, ~1,590 total).
21. **`docs/project/plans/v1.1.84-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

22. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` (version) — 1.1.83 → 1.1.84. **Never** `package-lock.json`.

### Housekeeping (not executed this pass)

23. **Stale build ZIPs — SKIPPED by user direction.** The 1.1.83 ZIP set (30 files) stays as the current wp.org release artifacts; removal moves to the next catch-up once the 1.1.84 packages are built. Recorded in the changelog versioning line.

### Verify-only (no changes expected)

- Skill count: 59 directories under `.agents/skills/` (in-window adds a *bundled* skill, not a coding-time one).
- `addons/pro/README.md` — toolkit count stays 31.
- `docs/project/ecosystem-port-tracker.md` — no port-loop PRs in-window (CG port deferred by #6747, recorded in the plan/proposal).
- Sub-project changelogs — own tracks, unchanged.
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" description — base count 308 still satisfies "300+".
- Model suites — the in-window catalog bump (v2026.09.22, `jev-preview`) shipped with its own test updates in #6747; model-track files beyond the changelog mention are not edited here.
- `tool-status.txt` — the five typesafe slugs were added in-window (#6745/#6747); OI-5 (missing CRM slugs) stays parked.

---

## 3. Execution log (2026-09-22)

- **Orientation** — window mapped from `2ef3961b0b` (v1.1.83 post merge #6744) to `a653858178` (origin/alpha-working HEAD): 3 merged PRs (#6745–#6747) + 2 build commits, no direct commits. All PR descriptions read in full via `gh pr view` (`gh` CLI per the updates skill). `@since` sweep on the window diff found the new OI-1 records in §2.20.
- **Claims verified against the tree** — `typesafe_guardrail` registration confirmed in `includes/class-wp-mcp-ai-tool-registry.php`; the 3 Pro registrations confirmed in `addons/pro/mcp-ai-wpoos-pro.php`; model catalog read directly (version `2026.09.22`, +1 `jev-preview` entry); `includes/bundled-skills/` count = 75 dirs, `addons/pro/includes/bundled-skills/` = 41 dirs + `THIRD_PARTY_NOTICES.md`; `.agents/skills/` = 59 dirs; version constants confirmed at 1.1.83 pre-bump.
- **Commits 1–4** — version bump, release notes, agent context, reference surfaces (see §4); the count changes ride along in commits 1–2. No stale-ZIP commit (user-directed skip).
- **Post-pass sweeps** — `git grep "1\.1\.83"` + count sweeps executed after all commits (see §5).

## 4. Commit breakdown (mirror v1.1.58–v1.1.83)

1. `chore: bump version to 1.1.84` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.84 changelog and release notes` — `CHANGELOG.md` (new [1.1.84] section), `readme.txt`, `README.md` (highlights + September 22 block + evergreen count lines), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.84 changes` — `.context/*` (tool-registry, security-checklist, testing), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies, `.agents/skills/mcp-ai-wpoos-updates/SKILL.md`, `AGENTS.md` (bundled-skill count), `.github/copilot-instructions.md` (count lines)
4. `docs: reconcile 1.1.84 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/proposals/README.md`, `docs/project/plans/docs-catch-up-open-items.md`, `docs/project/plans/v1.1.84-docs-catch-up.md`

(No separate count-sweep commit — the count changes (~308/~1,282/~1,590, catalog v2026.09.22, bundled skills 75) are entangled with content edits in the same files and ride along in commits 1–3; historical per-version blocks, plans, proposals, and the archive are untouched.)

## 5. Validation

- `php -l` on every bumped PHP file; JSON-parse `package.json`.
- `git grep "1\.1\.83"` sweep on current-release surfaces — remaining hits must be intentional history only (the `[1.1.83]` changelog block, per-version README blocks, `@since` docblocks, plans, archive, and the retained 1.1.83 build ZIPs).
- Count sweep — `~308`/`~1,282`/`~1,590` surfaces carry the live-registry caveat; `git grep "~307\|~1,586\|~1,279"` remaining hits must be history-only.
- `git grep "v2026\.09\.21"` — remaining hits history-only (catalog bump to v2026.09.22 on current surfaces).
- Skill-count verification — 59 directories under `.agents/skills/`; 75 under `includes/bundled-skills/`.
- Stale-ZIP sweep — skipped by user direction; `build/` keeps the 1.1.83 set.

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue #5968) and Docker-based live-count re-derivation (OI-2, issue #5967) stay parked. OI-2's current-figure label refreshed to the v1.1.84 baseline (~308 base + ~1,282 Pro, ~1,590 total; +1 base `typesafe_guardrail` +3 Pro Jev tools from #6747).
- **New items raised this pass:** OI-1 group 19 marked ✅ self-resolved (the `@since 1.1.84` tags now match the shipping version); group 22 extended (+1 `@since 1.9.0` in the Jev classifier, #6747); **group 24** (`@since 1.9.0` ×5 — new Jev Phase 2 files, #6747); group 23 extended (+4 `@since 2026.09` — openrouter-client +1, typesafe-client +3, #6747). All recorded in the tracker, not fixed.
- **Noted observations (other workstreams):** #6747's deferred items (extraction tools, the CG port cluster, NV Cloud passthrough on OpenRouter Decisions GA) are documented in the PR/plan — ecosystem-port and provider tracks, not this pass. Stale-ZIP housekeeping (the 1.1.83 set) moves to the next catch-up once the 1.1.84 packages are built.
