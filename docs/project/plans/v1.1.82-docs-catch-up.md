# Docs & Release Catch-Up Plan — v1.1.82 (PRs #6645–#6674)

## 1. Context: What changed since the last docs catch-up

The Sep 17 catch-up merged as `8246edcc75` ("chore: 1.1.81 docs catch-up (#6644)") and covered PRs #6634–#6643. **17 merged PRs** (#6645, #6656, #6659, #6661–#6674), **10 build commits**, and **1 direct commit** landed afterward, none present on any release-note surface. The window is the **two WordPress Playground demo blueprints** (Content Graph "Project Asteria" #6662, NV oOS Complete × local Ollama #6663) with their follow-up fixes (#6666/#6668/#6670/#6674), the **Playground demos coding-time skill** (#6664, 59th skill), **Pro SPA `cron_monitor` + model-store seeding** (#6665/#6672), **token-tracking table hardening** (#6669, ported to nvoos-content-graph-ai), **schedule result-delivery duplicate-content fixes** (#6661), **portability coverage-guard fixes** (#6645), **Docs Hub 0.4.7** (third wp.org reviewer pass, #6659/#6667), the **updates-skill Track C codification** (#6656), the **end-user Playground walkthrough** (#6671), and the **README demo button** (#6673).

### Production-touching PRs

| PR | Production files | Change |
|---|---|---|
| #6645 | `includes/helpers/class-wp-mcp-ai-tool-presets-helper.php`, `tests/test-messaging-channels-ajax.php` (+9 tests), regenerated `docs/developer/testing-docs/ajax-handler-inventory.{md,csv}` + both `.coverage-manifest.txt` files | **Portability coverage guards fixed** — the assistant export/import merge had landed without updating the CI coverage guards (4 failures in run 35096081494). `export_assistant`/`import_assistant`/`duplicate_assistant`/`export_assistant_blueprint` join the `assistant_management` preset; 9 AJAX tests (nonce/capability/missing-connection per handler) for the three WhatsApp handlers in the base-suite location; both tool-class manifests regenerated (+3 base, +18 Pro entries incl. the CRM/financial classes from #6636/#6639). |
| #6661 | `addons/pro/includes/services/class-wp-mcp-ai-result-delivery-service.php` | **Schedule result delivery duplicate content fixed** — chat `summary` and SMS templates gain the `response_starts_with_summary()` dedupe the `full` template already had; new `delivery_safe_data()` (`@since 1.1.82`) strips the duplicate `response` copy plus `assistant_id`/`is_agentic` metadata from the envelope data before rendering (email `full`, chat `full`, Paper Store / WordPress post markdown); `envelope_to_markdown()` skips the `## Summary` section on prefix match and no longer emits an empty `## Details` section. Legitimate data keys (`steps`, broadcast summaries, `tool_calls`) pass through untouched. |
| #6665 | `addons/pro/includes/class-wp-mcp-ai-pro-spa-shortcode.php`, `-pro-spa-config.php`, `assets/spa-v2/src/{api/config.ts,features/chat/ChatPage.tsx}` + rebuilt dist, `addons/pro/tests/test-pro-spa-shortcode.php` (+2) | **`cron_monitor` flag on the Pro SPA shortcode** — `[nvoos_pro_spa cron_monitor="0"]` flows through `build()` into `config.cronMonitor`; `ChatPage` passes an empty cron-status base URL to `useJobBus()` when false, no-oping **both** the blocking SSE `cron-status` stream and the 15-second REST poll fallback (the failure cascade that kills Playground's WASM worker). Default stays `true` — no behavior change for existing uses. |
| #6669 | `includes/class-wp-mcp-ai-token-tracking-database.php` (ported 1:1 to `plugins/nvoos-content-graph-ai/src/Analytics/TokenTrackingDatabase.php`) + tests | **Token tracking table creation hardened** — dbDelta cannot introspect schemas on Playground's SQLite layer (`SHOW FULL COLUMNS` 1146 aborts before create) and the schema-version option advanced unconditionally, so the table stayed missing forever. Now verify-then-version (version advances only when the table verifiably exists; existing installs self-heal), hourly retry-backoff transient, quiet failure (`$wpdb->suppress_errors()` + single `error_log` line instead of the HTML error dump), and graceful reads (empty results when absent). Schema unchanged — no ALTERs. |
| #6672 | `addons/pro/assets/spa-v2/src/hooks/useBootstrap.ts` + rebuilt dist, `embedded.test.tsx` (+1) | **Pro SPA seeds the model store from assistant config** — `modelStore.ts` hardcoded `{provider:'openai', model:'gpt-4o'}` and the sidebar sync never rendered in embedded mode, so the SPA overrode the assistant's real Ollama config on every chat request. `useBootstrap.ts` now seeds provider + model from the preloaded assistant config after `setRuntime()`; the sidebar syncs remain for full mode. |

### Docs, demo & tooling PRs (no plugin production code)

| PR | What |
|---|---|
| #6662 | Content Graph Playground demo ("Project Asteria"): `plugins/nvoos-content-graph/blueprints/{seed-content.php,demo.json}`, `.wordpress-org/blueprints/blueprint.json` (preview-safe), `bin/generate-content-graph-blueprint.php`, `.distignore` excludes. Deterministic build via the public `nvoos_content_graph/initial_build` hook; 3 true degree-0 orphans. Validated: 49 nodes / 255 edges / 5 communities on WP 7.1.1. |
| #6663 | Ollama Playground demo for NV oOS Complete: root `blueprints/{ollama-demo.json,ollama-demo.php,README.md}`, `bin/generate-ollama-blueprint.php`, `.distignore`/`bin/build-plugin-zip.sh` excludes. "localhost is the user's machine" trick; pre-wires `enable_ollama` + `llama3.1:8b`, creates the Oma assistant, lands on the Ollama Test Lab page (`[ollama_status]` banner + `[nvoos_pro_spa]` embedded chat). Validated end-to-end against real Ollama 0.32.5. |
| #6664 | **New coding-time skill** `.agents/skills/mcp-ai-wpoos-playground-demos/` (59th) — blueprint authoring, CORS hosting rules, the CI Plugin Check gate, the three worker crash modes, local-Ollama policy matrix, validation harness. Bookkeeping 58 → 59 already done in-window (AGENTS.md, copilot-instructions, README). |
| #6666 | `cron_monitor="0"` on the demo's Pro SPA page (safe pre-#6665-ship because unknown attributes are ignored). |
| #6668 | **`[ollama_status]` banner frozen by `wptexturize`** — the inline `<script>` inside `the_content` had `&&` rewritten to `&#038;&#038;`. The shortcode now renders a placeholder div only; the checker JS is a footer-enqueued script (`wp_register_script(…, false, …)` + `wp_add_inline_script`), self-locating every `.nvoos-ollama-status` div. New `bin/analyze-render.php`; probe uses the block-theme render path. |
| #6670 | Demo copy corrected: the banner works in Firefox but chat is worker-side PHP and sandboxed from localhost — the validated full-chat path is the local CLI server (`npx @wp-playground/cli server`). |
| #6671 | New `docs/user-guides/playground-demo.md` end-user walkthrough; wired into `docs/user-guides/README.md`, `docs/DOCUMENTATION_INDEX.md`, `blueprints/README.md`. |
| #6673 | README top-badge-row **Demo NV oOS Complete** button pointing at the alpha-working blueprint. |
| #6674 | `bin/generate-ollama-blueprint.php` auto-discovers the newest Complete bundle ZIP (`version_compare` over the glob; `--bundle-url=` override); `build-assets.yml` regenerates the blueprint right after the ZIP rebuild and stages it, and its `paths` now cover `addons/**/*.{js,ts,tsx,css}`. |
| #6656 | `mcp-ai-wpoos-updates` skill gains **Track C — PR Deferred-Item Sweep** (C0–C4 + deferral-hotspots table). Docs-only. |
| #6659 | Docs Hub **0.4.7** (sub-project, own track): removes the invalid `Tested up to:` PHP header (readme.txt-only now); daily rebuild cron moved from `plugins_loaded` to `init` (kills the WP 6.7+ early-translation notice); deactivation clears the cron + pending tick events and no longer enqueues a rebuild that can never run. |
| #6667 | Docs Hub 0.4.7 polish: `== Third-Party Libraries ==` disclosure, exact `== External Services ==` trigger list, `Tested up to` major.minor rule in the wporg skill, staging-excludes parity. |

### Direct commit

- `fb3cd70ff5` — README housekeeping on `alpha-working`: blank line before the Demo badge (follow-up to #6673).

### Build-only (10 commits)

compiled assets/distributions ×5, **oOS WP.org packages v1.1.81 ×1** (`128a244dc1` — the 1.1.81 package builds landed in-window; the 1.1.81 ZIP set is current), SPA addon ZIPs ×2, nvoos-content-graph standalone ZIP v1.0.8 ×1, nvoos-content-graph-ai ZIP v1.0.4 ×1.

### Key scope rules for this pass

- **Historical entries stay.** Old CHANGELOG/README per-version blocks (`[1.1.81]` and earlier) are history and are not rewritten.
- **Tool counts DO NOT change this window.** No PR registers or removes tools (#6645 adds existing slugs to a preset; #6661/#6665/#6669/#6672 are service/shortcode/DB/SPA-level). Figures stay **~306 base + ~1,279 Pro (~1,585 total)** with the live-registry caveat. No count-sweep commit this pass.
- **CHANGELOG:** no in-window `[1.1.82]` section exists — the pass creates it from scratch (date 2026-09-18).
- **README highlight rotation:** the top keeps only the latest release's paragraph (in-window convention) — the pass replaces the v1.1.81 highlights paragraph with a v1.1.82 one, adds "What's New at a Glance (v1.1.82)" + "Latest Updates (v1.1.82)" above the v1.1.81 blocks.
- **Skill count is now 59** (+1 Playground demos skill, #6664) — the PR already did the AGENTS.md/copilot-instructions/README bookkeeping in-window. Bookkeeping: verify-only; the changelog Versioning line records 59.
- **Sub-project version tracks:** Docs Hub **0.4.6 → 0.4.7** (#6659; own changelog — verify-only for `addons/docs-hub/CHANGELOG.md`); nvoos-content-graph **1.0.8** + nvoos-content-graph-ai **1.0.4** (unchanged versions; ZIP builds landed in-window); content-graph-ai-platform **2.0.0**, content-graph-pro **1.0.0**, Checkout API **0.1.2**, Comic Reader **0.5.0**, Media Worker **v3.2.0** unchanged.
- **Model catalog:** unchanged (**v2026.09.10**) — no model PRs in-window.
- **`@since` sweep:** 7 new `@since 1.1.82` tags in-window (delivery `delivery_safe_data()` #6661; token-tracking base ×3 + content-graph-ai port ×3, #6669) — **all correct for the 1.1.82 ship**, no new OI-1 group. Docs Hub ×2 `@since 0.4.7` is its own correct track. OI-1 **group 17 self-resolves** this pass: the Shopify normalizers trait tagged `@since 1.1.82` "ahead of the shipping version" in the v1.1.81 pass now matches the ship — noted in the tracker, not fixed.
- **Housekeeping executed this pass** — remove stale build ZIPs: all `build/**/*1.1.80*` (root 9 + optional-components 2 + toolkit-addons 19 = 30 files), superseded by the in-window 1.1.81 wp.org package builds. The 1.1.81 ZIP set is current, not stale. **Same-pass follow-up per the established convention:** the superseded `build/nvoos-docs-hub-v0.4.6.zip` is removed (0.4.7 is current).
- **Parked items stay parked** (OI-1/OI-2 in `docs-catch-up-open-items.md`): no `@since` reconciliation, no Docker live-count re-derivation. OI-2's "current figure" label refreshed to the v1.1.82 baseline.
- **Unrelated working-tree noise excluded** — untracked `docs/user-guides/assistants/CONTENT-GRAPH-MARKETING-ASSISTANT.md` (excluded from every commit), root `vendor/`, `package-lock.json`, `.codex-wordpress/` runtime copies are never staged.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.82] - 2026-09-18` section: Added (WordPress Playground demos — Content Graph "Project Asteria" + Complete × Ollama, #6662/#6663/#6666/#6668/#6670/#6671/#6673/#6674; `cron_monitor` Pro SPA flag #6665; Playground demos skill #6664), Fixed (token-tracking table hardening #6669; result-delivery duplicate content #6661; portability coverage guards #6645; Pro SPA model-store seeding #6672), Changed (updates skill Track C #6656), Sub-projects (Docs Hub 0.4.7 #6659/#6667), Versioning block (1.1.82; counts **~306 base + ~1,279 Pro (~1,585 total)** unchanged; catalog unchanged v2026.09.10; skill count **59**; stale 1.1.80 ZIPs removed (30 files) + docs-hub 0.4.6).
2. **`readme.txt`** — Stable tag 1.1.82; new `= 1.1.82 - September 18, 2026 =` changelog paragraph (one paragraph, wp.org style).
3. **`README.md`** — version header → 1.1.82; Release Date 2026-09-18; new `🆕 v1.1.82 Highlights` paragraph replacing the v1.1.81 one; new "What's New at a Glance (v1.1.82)" section above v1.1.81; new "Latest Updates (v1.1.82 — September 2026)" block above v1.1.81; count mentions stay ~306/~1,279/~1,585.
4. **`docs/QUICK_REFERENCE.md`** — version + last-updated → 1.1.82/September 18; v1.1.82 entry above v1.1.81.
5. **`docs/DOCUMENTATION_INDEX.md`** — Plugin Version + Last Updated → 1.1.82/September 18; new SEPTEMBER 18 UPDATE banner above the v1.1.81 one; new `## 🆕 September 2026 — v1.1.82` section header above the in-window "New and updated documents (September 18, 2026, v1.1.82)" block (added by #6671) and extend that block with the pass's docs (plan, README, CHANGELOG, readme.txt, QUICK_REFERENCE).
6. **`docs/project/FOR_REVIEWERS.md`** — current version → 1.1.82 (September 2026).

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed stamp + "New in v1.1.82 (counts unchanged — ~306 base + ~1,279 Pro, ~1,585 total)" paragraph: no tools added or removed; #6645 is preset-level, #6661/#6665/#6669/#6672 service/shortcode/DB/SPA-level, the demo/docs/skill PRs add no slugs.
8. **`.context/security-checklist.md`** — v1.1.82 update line: #6669 token-tracking quiet failure (suppressed dbDelta probes, single `error_log` line, no HTML error dump, graceful reads); #6661 `delivery_safe_data()` strips internal metadata before rendering; #6668 footer-enqueued checker JS (shortcodes render markup only — inline scripts in `the_content` get texturized); Docs Hub 0.4.7 cron on `init` + deactivation cleanup (sub-project).
9. **`.context/testing.md`** — last-reviewed + v1.1.82 note: token-tracking 61 tests / 200 assertions WP 6.9 + 16/16 WP 7.1 + 13/13 ecosystem; messaging-channels-ajax 69/69 incl. +9; presets 9/9; delivery-format 45/45 both versions; pro-spa-shortcode 10/10 + Vitest 133→134; docs-hub 107/0 both versions. The `mcp-ai-wpoos-test-suite` skill stays at **47** patterns.
10. **`.context/rest-api.md`** — v1.1.82 note: no new REST routes this window (all changes are tool/service/shortcode/DB-level; the demo blueprints add no routes).
11. **`CLAUDE.md`** — header stamp → September 18, 2026 · 2.35; v1.1.82 notes (Playground demos + `cron_monitor`, token-tracking hardening, result-delivery dedupe, coverage guards, model-store seeding, Docs Hub 0.4.7).
12. **`MAINTAINER_MAP.md`** — last-reviewed bump → September 18, 2026 (v1.1.82).
13. **`.bmad/README.md`** — last-reviewed bump → v1.1.82.
14. **All three `mcp-ai-wpoos-plugin` SKILL.md copies** — `plugin-version`/`plugin-version-tested`/`last-updated` → 1.1.82/2026-09-18 (the `.agents/skills/` copy also gains a v1.1.82 note section above the v1.1.81 block).
15. **`.agents/skills/mcp-ai-wpoos-updates/SKILL.md`** — frontmatter → 1.1.82/2026-09-18; body references (distilled-plan range, template-plan names, commit-structure mirror, references list) → v1.1.82.
16. **Skill-count bookkeeping (AGENTS.md §6)** — verify-only: 59 skills, already updated in-window by #6664 (AGENTS.md, `.github/copilot-instructions.md`, README repo-map all say 59).

### P2 — Operational & feature docs

17. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" stamp → September 18, 2026 (v1.1.82); Docs Hub row 0.4.6 → **0.4.7** with the third-reviewer-pass note; other rows verified unchanged.
18. **`docs/reference/tools/tool-reference.md`** — Last Updated → September 18, 2026 (v1.1.82); Tool Count line → "unchanged in v1.1.82" appended.
19. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-18 (v1.1.82 pass); Scope line gains v1.1.82; OI-1 group 17 annotated self-resolved (tag now matches the 1.1.82 ship); OI-2 current-figure label → v1.1.82 (unchanged counts; no tool registrations in-window). No new OI groups this pass.
20. **`docs/project/plans/v1.1.82-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

21. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.82 (`package-lock.json` intentionally untouched).

### Housekeeping (executed this pass)

22. **Remove stale build ZIPs** — `git rm` all `build/**/*1.1.80*` (root 9 + optional-components 2 + toolkit-addons 19 = 30 files, ZIPs **and** `.sha256`), superseded by the in-window 1.1.81 wp.org package builds. Commit: `chore: remove stale 1.1.80 build ZIPs [skip ci]`.
23. **Remove superseded docs-hub ZIP** — `git rm` `build/nvoos-docs-hub-v0.4.6.zip` (0.4.7 is current; same convention as the v1.1.81 pass). Commit: `chore: remove superseded docs-hub build ZIP [skip ci]`. Versioning-line mentions folded into CHANGELOG/readme.txt/README + this plan.

### Verify-only (no changes expected)

- Skill count after bookkeeping equals the actual **59** directories under `.agents/skills/` (20 wp-* + 32 design-* + 7 mcp-ai-wpoos guides).
- `AGENTS.md` / `.github/copilot-instructions.md` / `README.md` repo-map skill counts — already 59 (done in-window by #6664).
- `addons/pro/README.md` — toolkit count stays 31 (no new tools or toolkits in-window).
- `docs/project/ecosystem-port-tracker.md` — no port-loop PRs in-window (the content-graph-ai token-tracking port in #6669 is the sub-project's own mirror, not a CG Pro port wave).
- `addons/docs-hub/CHANGELOG.md`, `plugins/nvoos-content-graph/CHANGELOG.md`, `addons/checkout-api/CHANGELOG.md` — own tracks (docs-hub 0.4.7 updated in-window by #6659/#6667).
- `docs/reference/models/*` — no model changes in-window.
- Proposals index + `docs/developer/testing-docs/TEST-SUITE-REMAINING-FIXES-PLAN.md` — no in-window changes (the ajax inventory + coverage manifests regenerated by #6645 are CI-facing, not proposal/tracker docs).
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" description — base count 306 still satisfies "300+".

---

## 3. Execution log (2026-09-18)

- **Orientation** — window mapped from `8246edcc75` (1.1.81 catch-up merge #6644) to `aa19304a18` (#6674 merge): 17 merged PRs (#6645, #6656, #6659, #6661–#6674) + 10 build commits + 1 direct README commit (`fb3cd70ff5`). All PR descriptions read in full via `gh pr view` (GitHub API MCP tools unavailable per the skill). `@since` sweep on every added PHP file found **no new anomaly group** — all 7 new `1.1.82` tags and both `0.4.7` docs-hub tags match their shipping versions; OI-1 group 17 self-resolves with this ship (noted in the tracker).
- **Commits 1–6** — version bump, release notes, agent context, reference surfaces, stale-ZIP removals (see §4).
- **Post-pass sweeps** — `git grep "1\.1\.81"` + count sweeps executed after all commits (see §5).

## 4. Commit breakdown (mirror v1.1.58–v1.1.81)

1. `chore: bump version to 1.1.82` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.82 changelog and release notes` — `CHANGELOG.md` (new [1.1.82] section; date 2026-09-18; Versioning block), `readme.txt`, `README.md`, `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.82 changes` — `.context/*` (4 files), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies, `.agents/skills/mcp-ai-wpoos-updates/SKILL.md`
4. `docs: reconcile 1.1.82 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/plans/docs-catch-up-open-items.md`, `docs/project/plans/v1.1.82-docs-catch-up.md`
5. `chore: remove stale 1.1.80 build ZIPs [skip ci]` — `build/**/*1.1.80*` (30 files: 9 root + 2 optional-components + 19 toolkit-addons)
6. `chore: remove superseded docs-hub build ZIP [skip ci]` — `build/nvoos-docs-hub-v0.4.6.zip` (0.4.7 current) + Versioning-line mentions in CHANGELOG/readme.txt/README + this plan

(No count-sweep commit this pass — tool counts unchanged.)

## 5. Validation

- `php -l` on every bumped PHP file
- `package.json` JSON parse
- `git grep "1\.1\.81"` sweep — remaining hits must be intentional history (previous-release sections, `@since` docblocks, plans, archive, the 1.1.81 build ZIP names)
- Count sweep — `git grep "1,572\|1,266"` reconciled on live surfaces; historical per-version blocks/plans/archive untouched
- Stale-ZIP sweep — `git ls-files 'build/*'` re-checked: no 1.1.80 files remain; the 1.1.81 set is current; docs-hub 0.4.6 gone, 0.4.7 current
- Skill-count verification — 59 directories under `.agents/skills/` == bookkeeping claims in AGENTS.md / copilot-instructions / README

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue #5968) and Docker-based live-count re-derivation (OI-2, issue #5967) stay parked. OI-2's current-figure label refreshed to the v1.1.82 baseline (~306 base + ~1,279 Pro, ~1,585 total; no tool registrations in-window).
- **New items raised this pass:** none — no new OI-1 anomaly groups (all in-window tags correct) and no new OI groups otherwise.
- **Self-resolution noted:** OI-1 group 17 (Shopify normalizers trait `@since 1.1.82`) now matches the shipping version — annotated in the tracker, not fixed in-pass.
- **Noted observations (other workstreams):** #6659's "deploy .wordpress-org blueprint to SVN + toggle the preview public" follow-up is the wporg-submission track's concern; #6665's "next Complete bundle build activates `cron_monitor="0"` in the demo" landed as #6666 + #6674 (bundle pin now auto-discovers).
- **Housekeeping executed this pass:** stale 1.1.80 build ZIPs removed (30 files) + superseded docs-hub 0.4.6 ZIP removed.
