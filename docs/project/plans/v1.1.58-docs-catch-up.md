# Docs & Release Catch-Up Plan — v1.1.58 (PRs #5881–#5890)

> **Prepared:** 2026-08-18 · **Status:** ✅ **Executed 2026-08-18** (all P0–P4 items complete; see execution log below)
> **Scope:** documentation/context updates + version bump to **1.1.58** (no production-code changes)
> **Input:** full review of every commit on `alpha-working` from 2026-08-15 through 2026-08-18
> **Predecessor:** [`docs-catch-up-post-1157-fixes.md`](docs-catch-up-post-1157-fixes.md) (executed Aug 17; P4 items 14–16 rolled forward into this plan)

---

## 1. Context: What changed since the last docs catch-up

The Aug 17 catch-up covered everything through PR #5884. Since then, **five PRs** merged on `alpha-working` that are absent from every release-note surface — two large features (OOS runtime consolidation, Composio Connect), a plugin-family rename, and two small fixes:

| PR | Commit(s) | Change | Doc status today |
|---|---|---|---|
| #5881 | `73fd45642` … `6894d7461` (8 commits) | **OOS runtime consolidation, Phases 0–5.8** — parity foundations, tool-surface parity, security-gate parity, event-sourced session log, shadow mode + canary routing, scoped tools + compaction seam, Pro composition & child binding, telemetry single-path | Proposal 029 + `includes/oos/README.md` + `addons/pro/includes/composition/README.md` exist ✅ · changelogs/README/QUICK_REFERENCE/`.context` ❌ |
| #5887 | `0df4b8020` | Security Center refresh JS fix — `wp-api` script now enqueued on the security tab (its inline scripts call `wp.apiRequest()` and fatalled with "apiFetch is not defined") | nothing ❌ |
| #5888 | `6f2885ea3` | `deepmerge-ts` CVE-2026-40345 — package override in `addons/media-worker` + `addons/pro` `package.json` | nothing ❌ |
| #5889 | `c618fe9ac` | **Composio Connect integration (Pro)** — new `includes/composio/` subsystem (auth handler, client, trigger bridge, webhook controller), 6 new beta tools, remote-sites admin + metabox, module-registry entry; +5,318 lines, 887 lines of tests | `docs/composio-connect.md`, `docs/toolkits/composio-connect.md`, proposal 030 + implementation plan, `docs/tool-status.txt` ✅ · changelogs/README/counts ❌ |
| #5890 | `16b2f4e24` | **Rename standalone Graphify plugins → Content Graph** — `plugins/nvoos-graphify*` → `plugins/nvoos-content-graph*` (3 plugins, ZIPs v1.0.2), 3 build + 2 sync workflow renames, 3 `bin/build-*` script renames | plugin-internal READMEs updated ✅ · main docs still reference `plugins/nvoos-graphify*` ❌ |

Plus build-only commits (compiled assets, 3 new Content Graph ZIPs) which need no doc action.

### Key scope rules for this pass

- **`addons/graphify/` (the Graphify Knowledge Graph addon) is NOT renamed.** Docs that reference `addons/graphify/`, `nvoos_graphify_*` addon filters, or `/wp-json/nvoos-graphify/v1` **stay as-is**. Only references to the standalone `plugins/nvoos-graphify*` family get renamed.
- **Historical entries stay.** Old changelog/QUICK_REFERENCE entries that mention Graphify (e.g. "v1.1.45 … Graphify standalone plugins v1.0.1") are history and are not rewritten.
- **Tool counts shift by +6 (Pro):** ~265 base + **~1,243 Pro** (~1,508 total). All count surfaces must be reconciled.
- **Pro addon and plugin bump together** (per 1.1.57 precedent): 1.1.58 everywhere, Media Worker stays v3.0.0.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing, must-have)

1. **`CHANGELOG.md`** — new `## [1.1.58] - 2026-08-18` section above `[1.1.57]`:
   - `### Added — Composio Connect Integration (PR #5889)` — 6 beta tools (`composio_list_tools`, `composio_get_tool_schema`, `composio_list_connected_accounts`, `composio_create_connect_link`, `composio_execute_tool`, `composio_manage_triggers`), OAuth auth handler, signed webhook controller, trigger bridge, remote-sites admin + metabox UI, module registry entry; links to `docs/composio-connect.md` + `docs/toolkits/composio-connect.md`.
   - `### Changed — OOS Runtime Consolidation, Phases 0–5.8 (PR #5881)` — one paragraph per phase group: parity foundations + tool-surface/security-gate parity; event-sourced session log (new domain contracts + `SessionLog`/`SessionTelemetry`); Phase 4 shadow mode (opt-in `includes/oos/` runner, `wp mcp-ai oos parity` CLI) + canary routing (opt-in `_wp_mcp_ai_engine` post meta, `wp_mcp_ai_oos_canary` filter, default off); Phase 5 scoped tools (`ToolScope`) + compaction seam (`CompactionProvider`); Pro composition & child binding (`addons/pro/includes/composition/`, `wp mcp-ai composition` CLI); telemetry single-path. Link proposal 029.
   - `### Changed — Graphify Standalone Plugins Renamed to Content Graph (PR #5890)` — 3 plugin renames, workflow + bin script renames, ZIPs v1.0.2; `addons/graphify` unaffected.
   - `### Fixed` — Security Center `wp.apiRequest` (PR #5887) and `deepmerge-ts` CVE-2026-40345 (PR #5888).
   - `### Versioning` — 1.1.58 across all files; Pro 1.1.58; Media Worker v3.0.0 (unchanged); tool count ~265 base + ~1,243 Pro (~1,508); provider count 15; addon count 26.
2. **`readme.txt`** — bump `Stable tag:` to 1.1.58 and add a compact `= 1.1.58 - August 18, 2026 =` changelog paragraph (wp.org style: one paragraph per the 1.1.57 pattern, no section headers).
3. **`README.md`** — bump Version badge; replace the "🆕 v1.1.57 Highlights" paragraph and "What's New at a Glance (v1.1.57)" + "Latest Updates (v1.1.57)" sections with v1.1.58 content (Composio, OOS consolidation, Content Graph rename, 2 fixes); update both tool-count notes (~1,243 Pro / ~1,508).
4. **`docs/DOCUMENTATION_INDEX.md`** — bump Plugin Version; add a `### New and updated documents (August 18, 2026, v1.1.58)` block linking `docs/composio-connect.md`, `docs/toolkits/composio-connect.md`, proposals 029/030, and this plan; rename the "Standalone Graphify Plugins" section to "Standalone Content Graph Plugins" with new slugs + v1.0.2.
5. **`docs/QUICK_REFERENCE.md`** — bump Version; add v1.1.58 entry above v1.1.57.
6. **`docs/project/FOR_REVIEWERS.md`** — bump "Current version" to 1.1.58 (August 2026).

### P1 — Agent context files (correctness for future agents, must-have)

7. **`.context/tool-registry.md`** — bump the total-tools fact to ~1,508 (~265 base + ~1,243 Pro); add the two new Pro tool dirs (`includes/tools/composio/` 6 beta tools, `includes/composition/` subsystem) and `includes/oos/` to the subsystem map if one is present.
8. **`.context/security-checklist.md`** — add a `v1.1.58 updates:` header note: OOS security-gate parity + audit-logged shadow runs (default off), Composio OAuth state-nonce + signed webhook controller (verify its checks before exposure), `deepmerge-ts` CVE-2026-40345 override.
9. **`.context/oos-engine.md` (NEW)** — one-page context file: opt-in shadow runner (`includes/oos/`, default off, stores up to 100 runs in `wp_mcp_ai_oos_shadow_runs`), canary routing gates, session log/telemetry contracts in `lib/core`, Pro composition subsystem, parity CLI (`wp mcp-ai oos parity`, `wp mcp-ai composition`), pointer to proposal 029. Register it in `.context/README.md`'s index table.
10. **`CLAUDE.md`** — bump the current-state v1.1.57 notes to v1.1.58 (or add a v1.1.58 note block alongside), update both tool-count mentions (~1,508), and fix the "PDO persistent connections in Graphify" reference if it points at the standalone plugins (verify which Graphify that is first).
11. **`bin/README.md`** — check for references to the renamed `bin/build-*-graphify*.sh` scripts and update to `build-*-content-graph*.sh` (only if referenced).

### P2 — Operational & feature docs

12. **Graphify → Content Graph reconciliation triage** — walk every remaining current-state reference from `git grep -i graphify` and update only the standalone-plugin ones: `docs/ROADMAP.md`, `CREDITS.md`, `CONTRIBUTING.md`, `SUBMISSION.md`, `SECURITY_AUDIT_REPORT.md`, `.context/cross-platform-extraction.md`, `.context/security-checklist.md`, `.zed/README.md`, `.github/agents/*.agent.md`. **Do not touch** `addons/graphify/`, `docs/features/integrations/graphify-remote-sources.md` (addon-scoped: `nvoos_graphify_*` filters + REST namespace), `.agents/skills/design-media-workflow/SKILL.md` (`addons/graphify` references), or historical changelog entries.
13. **Fix-history doc for PR #5887** — `docs/history/2026/fixes/security-center-wp-api-request-fix.md` (small doc: symptom = "apiFetch is not defined" on security-tab refresh, root cause = `wp-api` script not enqueued, fix = conditional enqueue on `?tab=security`). Per convention, #5888 (dependency override) is changelog-only.
14. **`MAINTAINER_MAP.md`** — bump "Last reviewed" to August 18, 2026 (v1.1.58).

### P3 — Version bump (mechanical, mirrors commit `1cf3ed937`)

15. **`mcp-ai-wpoos.php`** — `Version: 1.1.58` header.
16. **`includes/bootstrap/constants.php`** — `WP_MCP_AI_VERSION` → `'1.1.58'`.
17. **`addons/pro/mcp-ai-wpoos-pro.php`** — `WP_MCP_AI_PRO_VERSION` → `'1.1.58'`.
18. **`package.json`** — `"version": "1.1.58"`.
19. **`.bmad/README.md`** — "Last reviewed" line → v1.1.58.

### P4 — Leftovers from the previous plan (P4 items 14–16)

20. **`readme.txt` header reconciliation** — description says "13 AI providers" / "250+ built-in tools" while current facts are 15 providers / ~265 base (~1,508 total). Update the wp.org marketing copy to match.
21. **`addons/fleet-operator/.context/README.md`** — confirm no stale references to the removed nested `.context/.context/` path (verify + fix if found).

---

## 3. Execution notes

- **Docs + version-bump-only PR**, matching the established catch-up pattern (`chore/v1.1.57-docs-catch-up` → suggest `chore/v1.1.58-docs-catch-up`). No PHP/JS logic changes, so PHPUnit/JS suites are unaffected.
- **Version-bump rule:** bump every current-state `1.1.57` reference; **keep** historical ones (old changelog sections, archive docs, `docs/history/`, `.context/archive/`). After bumping, verify with `git grep "1\.1\.57"` that every remaining hit is intentional history.
- **Validation:**
  - `composer run docs:check-folder-readmes` — no folder READMEs are created/edited by this pass (new subsystems already ship READMEs), should stay green.
  - `composer run lint:errors-only` — cheap sanity on the 3 touched PHP files (headers/constants only).
  - `git grep -i "plugins/nvoos-graphify"` — expect zero remaining current-state hits after item 12.
- ~15 files touched across `CHANGELOG.md`, `readme.txt`, `README.md`, `docs/`, `.context/`, `CLAUDE.md`, plus the 5 version-bearing files.

## 4. Suggested commit breakdown

*(executed in this order on 2026-08-18)*

1. `chore: bump version to 1.1.58` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`, `readme.txt` Stable tag
2. `docs: add 1.1.58 changelog and release notes` — `CHANGELOG.md`, `readme.txt` changelog, `README.md`, `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for OOS engine, Composio, and content-graph rename` — `.context/oos-engine.md` (new), `.context/README.md`, `.context/tool-registry.md`, `.context/security-checklist.md`, `CLAUDE.md`, `.context/cross-platform-extraction.md`, `.context/media-worker.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`
4. `docs: reconcile graphify references and add security-center fix history` — README plugins table, `docs/history/2026/fixes/security-center-wp-api-request-fix.md`, `readme.txt` header counts, `includes/oos/README.md` conventions section (folder-readme compliance)

## 5. Execution log (2026-08-18)

- **P3 version bump:** `mcp-ai-wpoos.php` (Version header + description counts), `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`, `.bmad/README.md` — all 1.1.58 ✅
- **P0 release surfaces:** `CHANGELOG.md` new [1.1.58] section (PRs #5881, #5887–#5890 + Versioning), `readme.txt` (Stable tag + 1.1.58 changelog + header counts), `README.md` (header/highlights/What's-New/tool-counts/plugins table/Latest Updates), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md` (header block + Aug 18 docs block + Standalone Content Graph Plugins rename), `docs/project/FOR_REVIEWERS.md` (version, providers, tool count) ✅
- **P1 agent context:** `.context/oos-engine.md` (new) registered in `.context/README.md` (also fixed stale ~1,500 count), `.context/tool-registry.md` (+6 Composio tools, ~1,508 total, new subsystems), `.context/security-checklist.md` (v1.1.58 header note + Graphify rename), `.context/media-worker.md` (last-reviewed bump), `CLAUDE.md` (v1.1.58 notes, tool counts, Content Graph rename) ✅
- **P2 operational docs:** graphify triage — only standalone-plugin references updated (README plugins table, `.context/cross-platform-extraction.md`, `.context/security-checklist.md`, `CLAUDE.md`); `addons/graphify/` + historical entries kept; `docs/history/2026/fixes/security-center-wp-api-request-fix.md` (new); `MAINTAINER_MAP.md` bump ✅
- **P4 leftovers:** `readme.txt` description 13→15 providers / 250+→265+ tools ✅; fleet-operator `.context` — verified no stale nested-path references (no change needed) ✅
- **Extra:** `includes/oos/README.md` gained the required `## Conventions` section — `composer run docs:check-folder-readmes` was failing on it (missing-sections) since PR #5881 landed; now green ✅
- **Validation:** `php -l` on the 3 bumped PHP files ✅ · `package.json` JSON parse ✅ · `composer run docs:check-folder-readmes` base 43 folders + `--scope=pro` 35 folders ✅ · `git grep 1.1.57` — all remaining hits are intentional history (previous release sections, `@since` docblocks, design records) ✅
- **Left for a follow-up (optional):** `@since 1.1.57` docblocks in `addons/pro/includes/composition/*` — the subsystem first ships in 1.1.58; docblock tags were authored by PR #5881 and left untouched (no production-code changes in this pass).
