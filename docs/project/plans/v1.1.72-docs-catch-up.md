# Docs & Release Catch-Up Plan — v1.1.72 (PRs #6330–#6388)

> **Prepared:** 2026-09-07 · **Status:** ✅ **Executed & committed 2026-09-07** (all P0–P3 items complete; see execution log below)
> **Scope:** documentation/context updates + version bump to **1.1.72** (no production-code changes) + requested housekeeping (stale build ZIPs)
> **Input:** full review of every PR merged on `alpha-working` after the v1.1.71 catch-up (merge `45db25bfb1`, PR #6329) through PR #6388 (tip `b493049e44`), including a read of every PR description in the window
> **Predecessor:** [`v1.1.71-docs-catch-up.md`](v1.1.71-docs-catch-up.md) (executed Sep 5; covered PRs #6314–#6328)

---

## 1. Context: What changed since the last docs catch-up

The Sep 5 catch-up merged as PR #6329 and covered PRs #6314–#6328. **56 PRs** (#6330–#6388) plus build commits landed afterward, none present on any release-note surface. The window is the **ecosystem-port wave completion (D8, E6, E2-cont, E3, E5, E1, E4, E-UI-1/2/3) + two new Woo tools + post-catalog test drift + small production fixes**.

### Production-touching PRs (everything else in the window is test-only, docs-only, build-only, or sub-project)

| PR | Production files | Change |
|---|---|---|
| #6388 | `addons/pro/includes/tools/class-wp-mcp-ai-tool-update-woo-product-price.php`, `-qty.php`, `trait-wp-mcp-ai-woo-price-qty-updater.php`, `class-wp-mcp-ai-tool-bulk-update-products.php`, `includes/class-wp-mcp-ai-tool-presets-helper.php`, `addons/pro/mcp-ai-wpoos-pro.php` | **Two new WooCommerce tools** — `update_woo_product_price` (regular/sale price, all product types) and `update_woo_product_qty` (stock quantity + management), built on a shared `WP_MCP_AI_Woo_Price_Qty_Updater` trait; `bulk_update_products` refactored onto the trait; presets + Pro registration. **Pro tool count +2** (~1,263 → ~1,265; total ~1,568) |
| #6386 | Pro EZuite/FlowHub sync tools | **Scheduled sync connection-ID delivery** — scheduled EZuite/FlowHub inventory syncs failed with "No EZuite connection ID provided" (and an admin error email) even with a correctly assigned toolkit setting; the scheduled action now delivers the connection ID |
| #6338 | `includes/class-wp-mcp-ai-plugin-updater.php` | **Pro vendor integrity verification** — "Update Pro Now" (Settings → Advanced → Data Management) could white-screen the site when the fresh Pro package's `vendor/` was incomplete; the updater now verifies Pro vendor integrity before and after updating |
| #6339 | `includes/class-wp-mcp-ai-container.php` | **Stale tool-registry container binding** — the `tool_registry` binding is registered `transient` so every `get()` resolves the **live** `WP_MCP_AI_Tool_Registry` singleton instead of a cached reference (test suites swap `$instance` via reflection and a cached binding could permanently point at a discarded instance; production behavior unchanged) |
| #6365 | seven `package-lock.json` files | **Dependabot patch bumps** — `browserslist` + `qs` bumped to patched versions, closing 12 open alerts |
| #6332 | `includes/admin/sections/class-wp-mcp-ai-section-providers.php`, `includes/class-wp-mcp-ai-openai-client.php` | **gpt-image-2 defaults completion** — #6328 moved the OpenAI image-model default in the settings-base layer but left the Providers section field + OpenAI client on `gpt-image-1`; all three layers now agree (follow-ups #6331/#6336 are test-drift only) |
| #6340 | `lib/wordpress-adapter/src/Tool/*` + `Adapter/*` (base adapter hardening) + `plugins/nvoos-content-graph-ai/` (sub-project) | **Wave D8 — standalone tool execution** — the Content Graph AI plugin now serves + executes the base tool inventory standalone; base-side `lib/wordpress-adapter` tools register pre-ported core tools and are hardened for standalone use (`tools/call` execution port, adapter-tool guards) |
| #6330 | `plugins/nvoos-content-graph/*` | **Content Graph wp.org resubmission hardening** (sub-project track, v1.0.4 — no version bump): MEDIUM/LOW pre-submission audit fixes |

Port PRs (sub-project tracks only): #6333–#6335, #6337 (Wave E2 continuation — SlaManager, CronManager, OutboundWebhook, SchedulerBridge), #6341–#6342 (Wave D8 final tool batches + tracker close-out), #6344 (Wave E2 close — JobNotifier + REST surface), #6346 (Wave E3 — HITL approval queue), #6347 (Wave E5 — A2A REST receive routes), #6348–#6353 (Wave E1 — workflow/run/trigger CPTs, dispatcher, trigger registry, workflow engine V2, pattern templates, agentic optimizer), #6354–#6356, #6358, #6360–#6361 (Wave E4 — tenant isolation, OAuth manager + handlers, Google Calendar, content assistant, site builder, conversation import), #6362–#6364, #6367–#6369 (Wave E6 — shadow engine, markup, Paper Store, OKF, crawler, oos-bridge helpers), #6370–#6373 (E-UI-1 — dashboards, orchestration, slash commands, run timeline), #6374–#6378, #6381–#6382 (E-UI-2 — approvals, token/cron/DAG/DLQ managers, media columns, asset inventory), #6383–#6385, #6387 (E-UI-3 — profession/team pages, Elementor/Woo/JetEngine/Plugins screens).

Test-only: #6331, #6336 (post-catalog model/default drift), #6343 (platform standalone CPT-wiring with WooCommerce active). Docs-only: #6345 (ecosystem port cluster-loop workflow + plan), #6379 (proposal 035 — agentic engineering gap-closure). Build-only: ZIP build commits (nvoos-content-graph-ai v1.0.4, platform v2.0.0, oOS WP.org packages v1.1.71, compiled assets). **Excluded:** #6359 (main-branch sync — not merged to `alpha-working`).

### Key scope rules for this pass

- **Historical entries stay.** Old changelog/README per-version blocks (`[1.1.71]` and earlier) are history and are not rewritten.
- **Tool counts change this window.** Two new Pro tools (#6388): figures move to **~303 base + ~1,265 Pro (~1,568 total)** with the live-registry caveat — count mentions updated on every live surface; historical per-version blocks, plans, and proposals keep their old figures.
- **Sub-project version tracks:** nvoos-content-graph **1.0.4** (unchanged — wp.org hardening #6330 landed without a bump). nvoos-content-graph-ai stays **1.0.4** (Wave D8 + E6 ports landed without a version bump). nvoos-content-graph-ai-platform stays **2.0.0** (Wave E2-cont, E3, E5, E1, E4, E-UI-1/2/3 landed without a version bump). Media Worker v3.2.0, Checkout API v0.1.0, Docs Hub 0.4.2 unchanged.
- **Skill count unchanged (54)** — no new coding-time skills in-window; `AGENTS.md` §1 + README repo map verified no-change.
- **No new `@since` anomalies** — the in-window production files carry no new `@since` tags (the `container.php` `@since 1.0.0` tags pre-date the window); no OI-1 group.
- **Parked items stay parked** (OI-1/OI-2 in `docs-catch-up-open-items.md`): no `@since` reconciliation, no Docker live-count re-derivation. OI-2's "current figure" line refreshed to the new v1.1.72 delta baseline.
- **Housekeeping requested** — remove stale build ZIPs: all `build/**/*1.1.70*` + `build/**/*1.1.71*` (ZIPs + `.sha256`, incl. `optional-components/` + `toolkit-addons/`), mirroring the v1.1.71 cleanup, **plus** the v1.1.71 plan's flagged stale sub-project ZIPs (`nvoos-content-graph-ai-v1.0.2*`/`v1.0.3*`, `nvoos-content-graph-v1.0.3*`, `nvoos-content-graph-ai-platform-v1.0.2.zip`, `nvoos-docs-hub-v0.4.1.zip`).
- **Unrelated working-tree noise excluded** — the pre-existing modification to `addons/pro/tests/tools/.coverage-manifest.txt` (another agent's work) is not staged in any commit.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.72] - 2026-09-07` section above `[1.1.71]`: Added (Woo price/qty tools #6388), Fixed (scheduled sync connection ID #6386, Pro vendor integrity #6338, tool-registry container binding #6339), Security (browserslist/qs bumps #6365), Changed (gpt-image-2 defaults completion #6332, ecosystem port waves #6330–#6387), Versioning (1.1.72, counts ~303 + ~1,265 (~1,568), sub-tracks unchanged, stale 1.1.70 + 1.1.71 ZIPs removed).
2. **`readme.txt`** — Stable tag 1.1.72; new `= 1.1.72 - September 7, 2026 =` changelog paragraph (one paragraph, wp.org style).
3. **`README.md`** — version header → 1.1.72; v1.1.72 Highlights paragraph; new "What's New at a Glance (v1.1.72)" section above v1.1.71; new "Latest Updates (v1.1.72 — September 2026)" section above v1.1.71. Tool-count mentions → ~1,568 (~303 + ~1,265) on live surfaces (Overview paragraph, tool-count note, architecture bullets, tool-reference link, upgrade instructions).
4. **`docs/QUICK_REFERENCE.md`** — version + last-updated → 1.1.72/September 7; v1.1.72 entry above v1.1.71.
5. **`docs/DOCUMENTATION_INDEX.md`** — Plugin Version + Last Updated → 1.1.72/September 7; new SEPTEMBER 7 UPDATE banner; new `## 🆕 September 2026 — v1.1.72` section header + "New and updated documents (September 7, 2026, v1.1.72)" block (this plan, README/CHANGELOG/readme.txt/QUICK_REFERENCE).
6. **`docs/project/FOR_REVIEWERS.md`** — current version → 1.1.72 (September 2026); count mentions → ~1,568 (~303 + ~1,265).

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed stamp + "New in v1.1.72" paragraph (two new Woo tools #6388 — Pro count → ~1,265, total ~1,568; no base tool changes; `bulk_update_products` shares the new trait); total-tools line → ~1,568.
8. **`.context/security-checklist.md`** — v1.1.72 update line: Pro update path verifies vendor integrity before/after (#6338); dependency patch bumps browserslist/qs (#6365); the container binding change (#6339) is behavioral-equivalent (transient vs singleton resolution of the live registry).
9. **`.context/testing.md`** — last-reviewed + v1.1.72 note: post-catalog drift trio (#6331, #6332, #6336) — the gpt-image-2 default lesson (defaults live in three layers; change one, sweep the rest); #6339 container transient-binding rationale for suite isolation; #6343 platform standalone CPT wiring with Woo active; #6330 content-graph wp.org audit suites. Test-suite skill stays at 40 patterns.
10. **`.context/rest-api.md`** — v1.1.72 note: no base REST changes this window; the platform addon gains the JobNotifier REST surface port (#6344, sub-project).
11. **`.context/README.md`, `.context/conventions.md`, `.context/pro-vs-base.md`** — tool-count mentions → ~1,568 (~303 base + ~1,265 Pro).
12. **`CLAUDE.md`** — header stamp → September 7, 2026 · 2.25; v1.1.72 notes (Woo tools, sync fix, vendor integrity, container binding, deps bump, ecosystem waves); count mentions → ~1,568 (~1,265 Pro).
13. **`MAINTAINER_MAP.md`** — last-reviewed bump → September 7, 2026 (v1.1.72); count mentions → ~1,568 (~1,265 Pro).
14. **`.bmad/README.md`** — last-reviewed bump → v1.1.72.
15. **All three `mcp-ai-wpoos-plugin` SKILL.md copies** — `plugin-version`/`plugin-version-tested` → 1.1.72 (the `.agents/skills/` copy also gains a v1.1.72 note line in its recent-fixes section); registry-diagram count → ~1,568.
16. **`.agents/skills/mcp-ai-wpoos-updates/SKILL.md`** — frontmatter `plugin-version`/`plugin-version-tested` → 1.1.72.
17. **`.github/copilot-instructions.md`** — repo-tree comment + Full-Version bullet → ~1,568 (~303 base + ~1,265 pro).

### P2 — Operational & feature docs

18. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" stamp → September 7, 2026 (v1.1.72); no addon/toolkit count changes.
19. **`docs/reference/tools/tool-reference.md`** — header Tool Count → ~1,568 (~303 + ~1,265); Last Updated → September 7, 2026 (v1.1.72).
20. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-07 (v1.1.72 pass); no new `@since` groups; OI-2 current-figure line → ~303 base + ~1,265 Pro (~1,568 total).
21. **`docs/project/plans/v1.1.72-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

22. `mcp-ai-wpoos.php` (Version header + `WP_MCP_AI_VERSION`), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.72 (`package-lock.json` intentionally untouched).

### Housekeeping (requested this pass)

23. **Remove stale build ZIPs** — `git rm` all `build/**/*1.1.70*` + `build/**/*1.1.71*` (ZIPs + `.sha256`; main + optional-components + toolkit-addons) plus the v1.1.71-flagged stale sub-project ZIPs. Commit: `chore: remove stale 1.1.70 and 1.1.71 build ZIPs [skip ci]`.

### Verify-only (no changes expected)

- `AGENTS.md` §1 + `README.md` repo map + `.github/copilot-instructions.md` skill counts — already read 54; no change.
- `.agents/skills/mcp-ai-wpoos-test-suite/SKILL.md` — no in-window cluster entries (the Sept model-drift trio is drift-fix only); pattern count stays 40.
- `addons/pro/README.md` — toolkit count stays 31 (the two Woo tools extend the existing E-commerce toolkit).
- `docs/project/proposals/RELATED_PROPOSALS_INDEX.md` — proposal 035 is brand new; index it **only if** the index tracks proposals (check — verify-only either way).
- `addons/docs-hub/CHANGELOG.md` — no docs-hub changes (0.4.2 unchanged).
- `docs/developer/testing-docs/TEST-SUITE-REMAINING-FIXES-PLAN.md` — no new clusters this window.
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" description — base count unchanged.

---

## 3. Execution log (2026-09-07)

- **P3 version bump:** `mcp-ai-wpoos.php` (Version 1.1.72), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.72 ✅ (`package-lock.json` untouched)
- **P0 release surfaces:** `CHANGELOG.md` [1.1.72] section (Woo tools + sync fix + vendor integrity + container binding + deps bump + gpt-image-2 completion + ecosystem waves), `readme.txt` (Stable tag + 1.1.72 changelog paragraph), `README.md` (header/highlights/What's-New/Latest-Updates + count mentions), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md` (banner + section header + Sep 7 block), `docs/project/FOR_REVIEWERS.md` (version + counts) ✅
- **P1 agent context:** `.context/tool-registry.md`, `.context/security-checklist.md`, `.context/testing.md`, `.context/rest-api.md` (v1.1.72 notes + stamps), `.context/README.md` + `.context/conventions.md` + `.context/pro-vs-base.md` (count mentions), `CLAUDE.md` (2.25 stamp + v1.1.72 notes + counts), `MAINTAINER_MAP.md` (stamp + counts), `.bmad/README.md` (stamp), three `mcp-ai-wpoos-plugin` SKILL.md copies (1.1.72 frontmatter + diagram count; v1.1.72 note in the `.agents/skills/` copy), `mcp-ai-wpoos-updates` SKILL.md (frontmatter 1.1.72), `.github/copilot-instructions.md` (counts) ✅
- **P2 operational docs:** `ADDON_INVENTORY.md` (stamp), `tool-reference.md` (stamp + count), `docs-catch-up-open-items.md` (last-reviewed; OI-2 figure refresh; no new groups), this plan ✅
- **PR-description review:** all 56 PR bodies + build commits read; window classified (production vs sub-project vs test/docs/build); #6359 excluded (main-branch sync); no new `@since` anomalies; no new residual test-suite findings (the #6343 Woo-active standalone failures were fixed by that PR) ✅
- **Tool counts:** +2 Pro tools (#6388) — ~303 base + ~1,265 Pro (~1,568 total); every live count surface updated; historical per-version blocks untouched ✅
- **Skill-count bookkeeping (A5):** no new skills — count stays 54 (verify-only) ✅
- **Housekeeping:** stale 1.1.70 + 1.1.71 build ZIPs removed (main + optional-components + toolkit-addons, incl. `.sha256`) + v1.1.71-flagged stale sub-project ZIPs ✅
- **Verify-only items:** all confirmed no-change ✅
- **Validation:** see section 5.

## 4. Commit breakdown (mirror v1.1.58–v1.1.71)

1. `chore: bump version to 1.1.72` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.72 changelog and release notes` — `CHANGELOG.md`, `readme.txt`, `README.md`, `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.72 changes` — `.context/*` (7 files), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies, `.agents/skills/mcp-ai-wpoos-updates/SKILL.md`, `.github/copilot-instructions.md`
4. `docs: reconcile 1.1.72 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/plans/docs-catch-up-open-items.md`, `docs/project/plans/v1.1.72-docs-catch-up.md`
5. `chore: remove stale 1.1.70 and 1.1.71 build ZIPs [skip ci]` — `build/**/*1.1.70*` + `build/**/*1.1.71*` + flagged sub-project stale ZIPs (housekeeping)

> Note: no separate count-sweep commit this pass — every count surface shares a file with its narrative edit (commits 2–4), so counts folded into the natural commits to avoid split-file staging.

## 5. Validation

- `php -l` on the three bumped PHP files ✅
- `package.json` JSON parse ✅
- `git grep "1\.1\.71"` sweep — remaining hits are intentional history (previous-release sections, `@since` docblocks, plans, archive, main-branch refs) ✅
- `git grep "1,566"` sweep — remaining hits are historical per-version blocks/notes only ✅
- `git grep "1,263"` sweep — remaining hits are historical per-version blocks/notes only ✅

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue [#5968](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5968)) and Docker-based live-count re-derivation (OI-2, issue [#5967](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5967)) stay parked. OI-2's current-figure line refreshed to the v1.1.72 delta baseline (~303 base + ~1,265 Pro, ~1,568 total).
- **New this pass:** none — no new `@since` anomalies, no new residual test-suite findings.
- **Housekeeping executed this pass:** stale 1.1.70 + 1.1.71 build ZIPs removed, plus the v1.1.71-flagged stale sub-project ZIPs.
