# Docs & Release Catch-Up Plan — v1.1.87 (PRs #6776–#6786)

## 1. Context: What changed since the v1.1.86 pass

The v1.1.86 catch-up merged as PR #6774 ("chore: 1.1.86 docs catch-up (PRs #6761–#6773) + stale ZIP removal") on 2026-09-25T10:39. **11 PRs** (#6776–#6786) and **7 build commits** landed on `alpha-working` afterward (2026-09-25T14:10 → 2026-09-26T20:13). Per user direction this window opens the **`[1.1.87]` line** (full version bump + new changelog section) and executes the **stale-ZIP housekeeping** (the 1.1.86 package set now exists).

### Production-touching PRs

| PR | Files | Change |
|---|---|---|
| #6776 | `includes/class-wp-mcp-ai-rest.php`, `includes/class-wp-mcp-ai-tool-token-limits.php` (+ Token Manager UI notes), `includes/bundled-skills/{design-elementor-mcp-connection,design-elementor-template-kits}/SKILL.md` (new — committed untracked copies), `AGENTS.md`/`CLAUDE.md` count corrections, preset file, docs + tests | **Per-session budget warnings + bundled Elementor skills (3 commits).** The blocked-session message (both sites in `check_per_session_limit()`) now reports session state and points at the real reset path (Restriction Registry → Restricted Users / `wp mcp-ai restrictions lift`); the dormant 75% hook `wp_mcp_ai_session_limit_approaching` is wired to `handle_session_limit_approaching()` (logs once, flags the session) + `inject_session_budget_warning()` (one-shot system notice on the next turn); new `wp_mcp_ai_chat_messages` filter on the legacy + OOS chat paths; Token Manager Reset buttons clarify daily-vs-session semantics; the `per_session_token_limit` description documents the 20% buffer. The two Elementor bundled skills existed on disk untracked — committed byte-identical to their `.agents/skills/` copies (bundled base is now genuinely 75 tracked). The third commit adds the Higgsfield quartet to the media generation preset. **Deferred in the PR:** mirror the message/warning changes into `plugins/nvoos-content-graph-ai/src/Analytics/ToolTokenLimits.php` (ecosystem-port track — recorded as OI-9); `docs/features/agent-skills.md` "45 bundled skills" fix left for the docs catch-up (executed this pass). |
| #6777 | 30 new base tools under `includes/tools/` (comment CRUD ×5, user CRUD ×4, content/media/terms ×7, site settings + application passwords ×5, SEO toolkit ×9), registry map +30, ID-handoff manifest (new `comment_id`/`user_id`/`attachment_id` families), `docs/developer/mcp-wordpress-tool-parity.md`, `CREDITS.md` section, `tests/test-mcp-wordpress-parity-tools.php` + contract suites | **mcp-wordpress parity tool suite (30 base tools).** Ports the genuinely missing surface from docdyhr/mcp-wordpress (MIT) into the base plugin: `list_comments`/`get_comment`/`create_comment`/`update_comment`/`delete_comment`, `list_users`/`create_user`/`update_user`/`delete_user`, `get_post_revisions`/`get_term`/`delete_term`/`get_media`/`upload_media`/`update_media`/`delete_media`, `get_site_settings`/`update_site_settings`/`list_application_passwords`/`create_application_password` (log-masked)/`delete_application_password`, and the 9-tool SEO toolkit (`seo_analyze_content`, `seo_generate_schema`, `seo_validate_schema`, `seo_bulk_update_metadata`, `seo_site_audit`, `seo_test_integration`, `seo_get_live_data`, `seo_track_serp`, `seo_keyword_research`). Capability checks, multisite guards, canonical envelope, chat-client restriction trait on sensitive write tools; the two network SEO tools reuse the existing Brave key. Second commit wires the tools into presets. |
| #6778 | new `addons/mcp-wordpress-gateway/` (Node: `src/gateway-server.js`, `src/app.js`, Express `/healthz` + `/mcp`, timing-safe `X-MCP-Token` auth, 1 MB body cap, `MCP_TOOLS_ALLOW`/`MCP_TOOLS_DENY` registration-time policy, deny wins), `.github/workflows/sync-mcp-wordpress-gateway.yml` subtree split, `docs/operations/deployment/mcp-wordpress-velocity-setup.md`; second commit renames the mirror repo to `nvoos-mcp-wordpress` | **mcp-wordpress gateway addon for Cloudways Velocity.** Auth-gated Streamable HTTP MCP server on pinned docdyhr/mcp-wordpress internals (no stdio/child processes — resolves the Velocity PM2 question), deployed via the media-worker subtree-split pattern to the `nvdigitalsolutions/nvoos-mcp-wordpress` mirror. Follow-ups documented in the PR: `express-rate-limit` per-IP limits, multi-site `mcp-wordpress.config.json`, and the mirror repo + deploy secret (the mirror rename landed in-window). |
| #6779 | `includes/class-wp-mcp-ai-restriction-registry.php`, `addons/pro/includes/admin/class-wp-mcp-ai-pro-agent-command-center.php`, `tests/test-restriction-registry.php` +7 | **Restriction admin notice user count + stale display fixed.** `queue_admin_notice()` keeps one entry per user (re-flag refreshes); `render_admin_notice()` sweeps expired windows, drops entries for unrestricted users, dedupes legacy inflated queues, prunes the option, renders nothing when empty; `lift()` removes the user's queued notices on full lift (partial lifts keep it); new `count_active_users()` (distinct users, unlike `count_active()`'s user:type rows) also fixes the Pro command-center banner's rows-as-users bug. Existing sites self-heal on the next admin load. Second commit regenerates the base tool coverage manifest. |
| #6780 | 5 new base tools (`identify_image`, `get_image_metadata`, `find_similar_media`, `detect_image_content`, `describe_image_layout`), 2 new Pro tools (`ocr_image_classic` in `media/`, `search_similar_images` in `vision-analysis/`), new `includes/class-wp-mcp-ai-cloud-vision-client.php`, `vision_object_localization` delegate refactor, new `.context/image-identification.md`, registry/category/token-limit/recommendation updates, both coverage manifests, proposal 043 (+ implementation plan), folder READMEs, tool reference, `readme.txt` 1.1.87-Unreleased entry, 9 test files | **Non-LLM image identification ladder (Proposal 043).** Cheap-first deterministic ladder — metadata → perceptual-hash media lookup (pure-PHP dHash, post-meta caching, daily backfill cron) → classic Cloud Vision detection (labels/text/web entities/logos/landmarks/faces/safe search, normalized envelope) → deterministic bounding-box layout description → optional reverse-image web search (Bing Visual Search / SerpApi Google Lens). `identify_image` orchestrates, never calls a vision LLM, returns confidence + escalation hint. Every external rung is key-gated and fails closed (missing credentials = skipped, never an error, never an HTTP request); web search is opt-in per call with SSRF guards on server-side fetches. |
| #6781 | (release-sync) | **`main` ← `alpha-working` squashed sync** — tree-identical at merge (the only dropped files are the stale 1.1.84 package set `alpha-working` had already replaced). No production code change — classified build/release-only. |
| #6782 | `plugins/nvoos-content-graph/` (admin/frontend/theme JS + CSS, `Tokens.php` `ensure_contrast()` fix + Okabe-Ito palette, Appearance settings, docs, regenerated `.pot`), its own CHANGELOG/readme.txt | **Content Graph explorer motion & UX (sub-project track).** Bloom-style hover focus + tooltip (dims the rest, brightens incident edges), eased animated camera, 150 ms search debounce + live match count, opt-in edge flow animation (300-edge cap, hidden-tab pause), skeleton loading + fade-in, Okabe-Ito CVD-safe fallback palette, and the `ensure_contrast()` gamut-walk fix (pure-black/white user colors now actually reach WCAG 3:1). All motion gates through `theme.motionAllowed()`. Own track — flagged, not edited. |
| #6783 | `plugins/nvoos-content-graph/src/Remote/Drivers/{GenericRest,Wikidata,Sparql,RssSitemap}.php`, `RemoteAdmin.php`, remote-admin JS, new `tests/Unit/Remote/Drivers/` (45) + `RemoteAdminTest` (6), `docs/remote-sources.md`, its own CHANGELOG + `.pot` | **Content Graph remote source driver fixes (sub-project track).** Generic REST gained the edge-mapping schema fields it already read, `max_items` (500), page-number pagination, query-param auth, and an honest Test probe (JSON/results-path/label validation, item/edge counts); Wikidata + SPARQL double-encoding fixed (every reconciliation lookup had searched for a literal `%20`); SPARQL probe appends `LIMIT 1` only when absent; failed probes now return a proper error envelope (previously `success: true`); the Add Source modal gains `select` rendering + label-to-slug autofill, and sources can be tested unsaved or while disabled. Own track — flagged, not edited. |
| #6784 | `addons/pro/includes/tools/crm/upwork/class-wp-mcp-ai-tool-search-upwork-jobs.php`, `addons/pro/includes/class-wp-mcp-ai-upwork-client.php`, schedule manager tool/class/REST docs, `upwork_job_discovery_scan` preset, `.agents/skills/{design-pro-schedule-manager,design-crm}/SKILL.md`, CG Pro port (byte-identical), tests | **Upwork search refinement + in-place workflow step editing.** `update_pro_schedule` gains `workflow_steps` (tool_slug/arguments/label, replace semantics) — workflow schedules no longer need delete-and-recreate to fix a query. `search_upwork_jobs` gains an `exclude_keywords` boolean-NOT arg, an `all` sentinel normalization for job_type/experience_level, quoted-OR fallback skills, tier + budget-range snippet parsing (year-number guard), and criteria echoing (`criteria_provided`, "unfiltered marketplace noise" notice); the `upwork_job_discovery_scan` preset carries academic-noise exclusions. The Pro → CG Pro port is byte-identical (documented transforms only). The two coding-time skills are updated in-window. |
| #6785 | preset file (`Media Generation` image-analysis group), `tests/test-assistant-tool-presets.php` | **Image identification tools join the media preset.** Fixes the presets coverage test broken by #6780's five unaccounted base tools (the root CI failure blocking #6783). |
| #6786 | new `addons/pro/includes/tools/outbound-booking/` toolkit (settings, templates, angle CPT, booking-link CPT, outbox, channels, engine, notifications, booking, import, REST, dashboard, 3 tools, init), Pro module-registry + settings-toggle wiring, `instagram_dm` channel (additive), proposal 044, 8 test suites | **Outbound appointment booking toolkit (Proposal 044, Pro addon, gated by `enable_outbound_booking_toolkit`).** ICP list building (CSV → `mcp_ai_lead`, email-deduped, ICP-scored, optional sequence enrollment), hourly cron outbound engine over `mcp_ai_sequence` steps with send windows/daily caps/consent attestation, email + LinkedIn/Instagram DM channels (human approval board or webhook hand-off), `mcp_ai_oa_angle` CPT angle bank with champion/challenger A/B + weekly promotion cron, booking links + `[nvoos_oa_booking]` shortcode + rate-limited honeypotted consent-gated public booking REST endpoint into `mcp_appointment`, pipeline dashboard + Slack notifications + daily digest, and **3 new Pro tools** (`outbound_get_pipeline_stats`, `outbound_import_leads`, `outbound_manage_angle`). Deferred in the PR: CLI toolkit listings, AI first-line personalization filter, internal-booking availability engine. |

### Release-sync PR

- #6781 — `main` ← `alpha-working` squashed sync (tree-identical to `alpha-working` at `a98bc9e1`; the only dropped files are the stale 1.1.84 package set). Build/release-only.

### Build-only (7 commits)

wp.org packages v1.1.86 ×6 (`7e0e810f03`, `e6e699591d`, `7aa258dc0f`, `54a0efaa4d`, `5bb4986b88`, `7f6f775d5a`) + `nvoos-content-graph` standalone plugin ZIP v1.0.8 (`fa8a46b47c`) — all `[skip ci]`.

### Key scope rules for this pass

- **`[1.1.87]` is the new current release** — new changelog section at the top; `[1.1.86]` and earlier stay untouched. `readme.txt` already carries a `= 1.1.87 - Unreleased =` entry (added by #6780) — this pass converts it into the dated release entry and extends it with the full window.
- **Counts change in-window:** +35 base (30 parity, #6777 + 5 image ladder, #6780) and +5 Pro (2 image, #6780 + 3 outbound, #6786). Tool count moves **~312 → ~347 base + ~1,282 → ~1,287 Pro (~1,594 → ~1,634 total)**. Coding-time skills stay **60** (no new skills; #6784 updated two in place). Bundled skills stay **75 base + 41 Pro** (#6776 committed the two untracked Elementor copies — the counts were already correct; base is now genuinely 75 tracked dirs). Providers stay **15** (model catalog untouched at **v2026.09.22**). Addons move **27 → 28** (new `mcp-wordpress-gateway`, #6778).
- **Sub-project tracks (flagged, not edited):** nvoos-content-graph stays **1.0.8** (its CHANGELOG/readme.txt/.pot changed in-window, #6782/#6783); the CG Pro `search-upwork-jobs` port (#6784) landed byte-identical; the CG AI `ToolTokenLimits` mirror from #6776 is **deferred → OI-9**.
- **README 12-entry window:** the new `### v1.1.87` entry joins Release History; the oldest detailed entry (`### v1.1.75`) is demoted to the Previous Releases table; the Overview keeps a single current-release "What's New at a Glance" block.
- **Housekeeping EXECUTED this pass (user-directed — "clean up any stale zips"):** remove the stale **1.1.85 build set (30 files: 9 in `build/` incl. 3 `.sha256`, 2 in `build/optional-components/`, 19 in `build/toolkit-addons/`)**. The 1.1.86 wp.org set is the current release artifact set.
- **Parked items stay parked** (OI-1/OI-2). **New OI-1 groups 29–31** recorded from the window `@since` sweep: group 29 — `@since 2.12.0` ×139 (the outbound-booking toolkit ~129 across 13 files + 10 across 4 Upwork/CRM files, PRs #6784/#6786); group 30 — `@since 1.1.85` ×3 (`includes/class-wp-mcp-ai-rest.php` 1 + `includes/class-wp-mcp-ai-tool-token-limits.php` 2, PR #6776); group 31 — `@since 1.2.0` ×2 (`includes/class-wp-mcp-ai-restriction-registry.php`, PR #6779). Correctly-tagged in-window: `@since 1.1.87` ×40.
- **Unrelated working-tree noise excluded** from every commit.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.87] - 2026-09-27` section: Added subsections for the mcp-wordpress parity suite (#6777), the image identification ladder (#6780 + preset #6785), the outbound booking toolkit (#6786), and the mcp-wordpress gateway addon (#6778); a Fixed subsection for the session-budget warnings (#6776) and the restriction-notice fix (#6779); a Changed subsection for the Upwork refinements (#6784); a Docs & Skills subsection (bundled Elementor skills committed, agent-skills.md count, proposals 043/044, new `.context/image-identification.md`); a Sub-project flag paragraph for #6782/#6783 + the #6784 CG Pro port. Versioning block: bump to 1.1.87; tool count ~347 base + ~1,287 Pro (~1,634 total; +35 base +5 Pro); coding-time skills 60 (unchanged); bundled skills 75+41 (unchanged — now genuinely tracked); providers 15 (catalog v2026.09.22 unchanged); addons 27 → 28; sub-project tracks unchanged; stale 1.1.85 build set removed (30 files).
2. **`readme.txt`** — `Stable tag: 1.1.87`; the `= 1.1.87 - Unreleased =` entry becomes `= 1.1.87 - September 27, 2026 =` and is extended with the parity suite, budget warnings, restriction fix, outbound toolkit, gateway addon, Upwork refinements, and the count/versioning line.
3. **`README.md`** — `**Version:** 1.1.87` / `**Release Date:** 2026-09-27`; new v1.1.87 Highlights paragraph replaces the v1.1.86 one; "What's New at a Glance" heading → (v1.1.87) with new bullets; Release History gains `### v1.1.87 — September 27, 2026` at the top; **demote `### v1.1.75`** to the Previous Releases table; the evergreen architecture bullet (~1,594 tool classes) → ~1,634; repo-map `addons/` row 27 → 28 (+ gateway); line 37 "27 addons" → 28.
4. **`docs/QUICK_REFERENCE.md`** — `**Version:** 1.1.87` / `**Last Updated:** September 27, 2026`; new v1.1.87 recent-updates entry above the v1.1.86 one; plan link gains this plan.
5. **`docs/DOCUMENTATION_INDEX.md`** — `**Plugin Version:** 1.1.87` / `**Last Updated:** September 27, 2026`; new SEPTEMBER 27, 2026 banner above the SEPTEMBER 25 one (updated-docs list: this plan, README/CHANGELOG/readme.txt/QUICK_REFERENCE + in-window `docs/developer/mcp-wordpress-tool-parity.md`, `docs/features/performance/*`, `docs/operations/deployment/mcp-wordpress-velocity-setup.md`, `docs/reference/tools/tool-reference.md`, proposals 043/044).
6. **`docs/project/FOR_REVIEWERS.md`** — "Current version" → 1.1.87; tool counts → ~347 base / ~1,634 total; Pro-tools line ~1,282 → ~1,287; addon line "26 addon directories … (27 entries …)" → "28 addon directories … (29 entries …)".

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed → September 27, 2026 (v1.1.87); new v1.1.87 paragraph: #6777 registers **30 new base tools** (the parity suite) and #6780 **5 new base tools** (image ladder); Pro +5 (#6780's `ocr_image_classic`/`search_similar_images` + #6786's three outbound tools, gated toolkit registration); #6776 (session-budget warnings) and #6779 (restriction registry) add no slugs; #6785/#6776-preset changes are preset-level. "Total tools" line moves to ~1,634.
8. **`.context/security-checklist.md`** — last-reviewed + v1.1.87 line: parity suite native security (capability checks, multisite guards, canonical envelope, chat-client restriction trait on sensitive write tools; `create_application_password` log-masked); image ladder key-gated fails-closed rungs (missing credentials = skipped, never an HTTP request; SSRF guard on server-side URL fetches); outbound booking safety rails (manual approval default for DMs, daily caps, send windows, consent gates, rate-limited honeypotted public booking endpoint, token-authenticated reply webhook); gateway addon (timing-safe `X-MCP-Token` ≥32 chars with rotation overlap, 1 MB body cap, deny-wins registration-time tool policy).
9. **`.context/testing.md`** — last-reviewed + v1.1.87 note: `test-mcp-wordpress-parity-tools.php` (29) + contract suites (20, 846 assertions both versions); the image-identification cluster (9 suites: `test-image-dhash.php`, `test-tool-{identify-image,get-image-metadata,find-similar-media,detect-image-content,describe-image-layout,ocr-image-classic,search-similar-images}.php`, 64/219 + `test-vision-tools.php` regression); outbound-booking suites (8, 41 tests); `test-restriction-registry.php` +7; `test-assistant-tool-presets.php` (the #6785 fix); `test-tool-registry-coverage.php` re-validated with regenerated manifests (3,142 assertions).
10. **`.context/rest-api.md`** — note-only: no new **base** routes in-window (parity/image tools are tool-level; #6779 is registry-level; #6776 adds a chat-messages filter, not a route). Pro-level: #6786's public booking REST endpoint (rate-limited, honeypotted, consent-gated) + the gateway addon's standalone `/healthz` + `/mcp` HTTP surface (addon, not WP REST). Stamp refreshed to v1.1.87.
11. **`.context/README.md`** — file table gains the `image-identification.md` row (added by #6780 without inventorying).
12. **`CLAUDE.md`** — stamp → September 27, 2026 · 2.41; count lines ~1,634 (~347 base + ~1,287 Pro); new v1.1.87 bullet block (parity suite +30, image ladder +5/+2, outbound toolkit +3, gateway addon, session-budget warnings + `wp_mcp_ai_chat_messages`, restriction-notice fix, Upwork refinements); `.context/` load table + list gain `image-identification.md`.
13. **`MAINTAINER_MAP.md`** — last-reviewed → September 27, 2026 (v1.1.87).
14. **`.bmad/README.md`** — last-reviewed → September 2026 (v1.1.87).
15. **Three `mcp-ai-wpoos-plugin` SKILL.md copies** (`.agents/skills/`, `includes/bundled-skills/`, `plugins/nvoos-content-graph-ai-platform/data/bundled-skills/`) — `plugin-version`/`plugin-version-tested` → 1.1.87; the `.agents/skills/` copy's note section gains a v1.1.87 block (parity suite + image ladder + outbound tools, gateway addon, session-budget warnings).
16. **`.agents/skills/mcp-ai-wpoos-updates/SKILL.md`** — references: latest executed plan → `v1.1.87-docs-catch-up.md`; template list gains it; new scope rules learned this window (an introducing PR may pre-stage the `readme.txt` "Unreleased" entry — convert it, never duplicate; sub-project window PRs are flagged-not-edited even when they carry their own CHANGELOG/readme.txt updates; the stale-set scope follows the full build set — root + optional-components + toolkit-addons, matching the "30 files" shape).
17. **`AGENTS.md` + `.github/copilot-instructions.md`** — no skill-count change (60 stays); `.context/*.md` inventories gain `image-identification.md`.

### P2 — Operational & feature docs

18. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" → September 27, 2026 (v1.1.87); new row 29 for `addons/mcp-wordpress-gateway/` (0.1.0, Production, MIT-pinned upstream / Node 22, Velocity deployment, auth-gated Streamable HTTP MCP server).
19. **`docs/reference/tools/tool-reference.md`** — Tool Count header gains "+35 base +5 Pro in v1.1.87" (parity suite, image ladder, outbound tools); "Last Updated" stamp; **OI-8 coverage note** (the parity suite + outbound tools are not yet documented in the reference body; the image ladder's `identify_image` entry landed in-window via #6780).
20. **`docs/project/proposals/README.md`** — "Recently Completed" gains 043 (image identification ladder) + 044 (outbound appointment booking) entries.
21. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-27 (v1.1.87 pass); **OI-1 groups 29–31 added**; **OI-8** (tool-status.txt missing all 40 new window slugs + its stale header totals); **OI-9** (the #6776 CG AI `ToolTokenLimits` mirror — port-tracker row D3f "byte-identical" now stale); **OI-10** (`addons/pro/README.md` toolkit count 31 → 32 + missing `docs/toolkits/` page for the outbound-booking toolkit); OI-2 current-figure label → v1.1.87 (~347 + ~1,287, ~1,634); scope line extended through v1.1.87.
22. **`docs/features/agent-skills.md`** — the #6776-deferred "45 bundled skills" → **75** (base) + Pro line check.
23. **`docs/project/plans/v1.1.87-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

24. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` (version) — 1.1.86 → 1.1.87. **Never** `package-lock.json`.

### Housekeeping (user-directed — executed this pass)

25. **Stale build ZIPs** — delete the 1.1.85 build set (30 files: 9 in `build/` incl. 3 `.sha256` + `mcp-ai-wpoos-{,-base,-pro}-1.1.85.zip`, 2 in `build/optional-components/`, 19 in `build/toolkit-addons/`). Commit: `chore: remove stale 1.1.85 build ZIPs [skip ci]`.

### Verify-only (no changes expected)

- Skill counts: 60 dirs under `.agents/skills/`; 75 bundled base (77 entries incl. `LICENSE.txt` + `THIRD_PARTY_NOTICES.md`) + 41 Pro (42 entries incl. `THIRD_PARTY_NOTICES.md`) — unchanged.
- `docs/project/ecosystem-port-tracker.md` — no port-loop PRs in-window; the #6784 CG Pro port landed in-feature without a tracker row (noted in OI-9).
- Sub-project changelogs — content-graph's own CHANGELOG/readme.txt/.pot changed in-window (#6782/#6783, own track, flagged only).
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" — base count 347 still satisfies "300+".
- Model suites — no catalog changes in-window (catalog v2026.09.22); model-track files untouched.
- Proposals: 040/041/042 statuses unchanged (042 completed entry from the 1.1.86 pass stays).
- Docs Hub changelog — unchanged in-window.
- `addons/pro/README.md` toolkit count — flagged as **OI-10**, not fixed (needs a real toolkit section, not a count stub).

---

## 3. Execution log (2026-09-27)

- **Orientation** — window mapped from `198dcf00c3` (v1.1.86 catch-up tail, PR #6774) to `996b9aba76` (origin/alpha-working HEAD): 11 merged PRs (#6776–#6786; #6781 release-sync) + 7 build commits. All PR descriptions read in full via `gh pr list` (merged set `updated:>=2026-09-24`; merge-commit mapping via `gh pr view --json mergeCommit`). `@since` sweep on the window diff found the new OI-1 groups 29–31 records in §2.21.
- **Claims verified against the tree** — registry map +35 (30 parity + 5 image ladder; the JWT map line was an alignment artifact, not a new entry); Pro registry/init +5 (`ocr_image_classic` + `search_similar_images` in `addons/pro/mcp-ai-wpoos-pro.php`; 3 outbound tools via the gated toolkit init filter); `.agents/skills/` = 60 dirs; `includes/bundled-skills/` = 75 dirs + 2 files (the two Elementor copies now tracked, #6776); `addons/pro/includes/bundled-skills/` = 41 dirs + 1 file; catalog v2026.09.22 unchanged; `addons/` = 28 dirs (+`mcp-wordpress-gateway`); `build/` 1.1.85 set inventoried at exactly the "30 files" shape (9 + 2 + 19); content-graph 1.0.8 unchanged; gateway addon 0.1.0.
- **Commits 1–6** — version bump, release notes, agent context, reference surfaces, count sweep, stale-ZIP removal (see §4); all commits landed on `chore/1.1.87-docs-catch-up` (per A7 — `alpha-working` untouched).
- **Post-pass sweeps** — `git grep "1\.1\.86"` + count sweeps executed after all commits (see §5).

## 4. Commit breakdown (mirror v1.1.58–v1.1.86)

1. `chore: bump version to 1.1.87` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.87 changelog and release notes` — `CHANGELOG.md` (new [1.1.87] section), `readme.txt` (Stable tag + Unreleased→dated entry), `README.md` (highlights + What's New + Release History entry + v1.1.75 demotion + architecture-count line + addons rows), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.87 changes` — `.context/*` (tool-registry, security-checklist, testing, rest-api, README table), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies, `.agents/skills/mcp-ai-wpoos-updates/SKILL.md`, `AGENTS.md` + `.github/copilot-instructions.md` (context-inventory fold-in)
4. `docs: reconcile 1.1.87 reference surfaces` — `docs/project/ADDON_INVENTORY.md` (row 29), `docs/reference/tools/tool-reference.md`, `docs/project/proposals/README.md` (043/044), `docs/project/plans/docs-catch-up-open-items.md` (OI-1 groups 29–31 + OI-8/9/10 + OI-2 label), `docs/features/agent-skills.md` (45 → 75), `docs/project/plans/v1.1.87-docs-catch-up.md`
5. `docs: sweep 1.1.87 tool counts` — current-release count surfaces ~312→~347 / ~1,282→~1,287 / ~1,594→~1,634 (grep-verified; historical per-version blocks, plans, and proposals untouched)
6. `chore: remove stale 1.1.85 build ZIPs [skip ci]` — `build/*1.1.85*` (9), `build/optional-components/*1.1.85*` (2), `build/toolkit-addons/*1.1.85*` (19)

## 5. Validation

- `php -l` on every bumped PHP file; JSON-parse `package.json`.
- `git grep "1\.1\.86"` sweep on current-release surfaces — remaining hits must be intentional history only (the `[1.1.86]` changelog block, per-version README/readme.txt blocks, `@since` docblocks, plans, archive).
- Count sweep — current-release surfaces move to `~347`/`~1,287`/`~1,634`/`28`; `git grep` the old labels (`~312`, `~1,594`, `1,282 Pro`) only inside history blocks/plans/archive.
- README anchor check — `python3 bin/validate-readme-anchors.py` after the README edits; Release History must hold exactly the 12 most recent releases (v1.1.87 → v1.1.76) and the v1.1.75 row must be first in the Previous Releases table.
- Stale-ZIP sweep — `find build -name "*1.1.85*"` returns nothing; the 1.1.86 set and the non-versioned 1.0.0/SPA/toolkit zips remain.
- Sub-project files untouched by this pass (own tracks).

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue #5968) and Docker-based live-count re-derivation (OI-2, issue #5967) stay parked. OI-2's current-figure label refreshed to the v1.1.87 baseline (~347 base + ~1,287 Pro, ~1,634 total — +35 base +5 Pro).
- **New items raised this pass:** OI-1 **groups 29–31** (`@since 2.12.0` ×139 / `@since 1.1.85` ×3 / `@since 1.2.0` ×2); **OI-8** (`tool-status.txt` missing the 40 new window slugs + stale header totals); **OI-9** (#6776's CG AI `ToolTokenLimits` mirror — port-tracker row D3f stale); **OI-10** (`addons/pro/README.md` toolkit count + missing outbound-booking toolkit page). Recorded in the tracker, not fixed (except the #6776-deferred agent-skills.md count, fixed in-pass at the PR's explicit hand-off).
- **Noted observations (other workstreams):** #6778's follow-ups (express-rate-limit, multi-site config, deploy secret) documented in the PR; #6786's deferred items (CLI toolkit listings, personalization filter, availability engine) are in the PR + proposal 044; OI-5/OI-6/OI-7 stay with their owners.
