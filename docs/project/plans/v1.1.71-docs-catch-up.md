# Docs & Release Catch-Up Plan — v1.1.71 (PRs #6314–#6328)

> **Prepared:** 2026-09-05 · **Status:** ✅ **Executed & committed 2026-09-05** (all P0–P3 items complete; see execution log below)
> **Scope:** documentation/context updates + version bump to **1.1.71** (no production-code changes) + requested housekeeping (stale build ZIPs)
> **Input:** full review of every PR merged on `alpha-working` after the v1.1.70 catch-up (merge `e4dc77ed9f`, PR #6313) through PR #6328, including a read of every PR description in the window
> **Predecessor:** [`v1.1.70-docs-catch-up.md`](v1.1.70-docs-catch-up.md) (executed Sep 5; covered PRs #6280–#6312)

---

## 1. Context: What changed since the last docs catch-up

The Sep 5 catch-up merged as PR #6313 and covered PRs #6280–#6312. **14 merge PRs** (#6314–#6328) plus two direct build commits landed afterward, none present on any release-note surface. The window is the **post-K16 stabilization + Wave E2 / D-UI ecosystem port + September model-catalog refresh**.

### Production-touching PRs (everything else in the window is test-only, docs-only, build-only, or sub-project)

| PR | Production files | Change |
|---|---|---|
| #6314 | `includes/admin/sections/class-wp-mcp-ai-section-providers.php`, `includes/admin/class-wp-mcp-ai-admin-settings.php` | **Connectors screen links** — the "Settings → Connectors" links pointed at `admin.php?page=connectors` (not a registered page, wrong/blank page on click); both now use `admin_url( 'options-connectors.php' )`, the core Connectors screen |
| #6315 | `addons/checkout-api/includes/class-nvoos-checkout-api-token.php`, `-crypto.php`, `bin/build-addon-zips.sh`, `phpunit.xml.dist`, `tests/bootstrap.php`, `.github/workflows/build-nvoos-checkout-api.yml` | **Checkout API into the addon pipeline** — `build/nvoos-checkout-api-v<version>.zip` joins the addon build; `addons/checkout-api/tests` (6 suites) join the main PHPUnit gate; the build workflow gains a `tests` job. Production fix exposed by the new suite: the token + crypto classes read `AUTH_KEY . SECURE_AUTH_KEY` directly (latent fatal on installs whose `wp-config.php` omits salt constants) — both now use `wp_salt( 'auth' ) . wp_salt( 'secure_auth' )` |
| #6316 | `plugins/nvoos-content-graph-ai/src/Blocks/*`, `BuildAssistantPage`, `Blocks.php` hub, assets | **Content Graph AI assistant-builder block set** (sub-project track): `assistant-selector`, `tools-grid`, `knowledge-base`, `assistant-builder` blocks + the Build Assistant Prompt tab embeds the tools grid + KB components; 18 characterization tests in both ecosystem matrices (WP 6.9 + 7.1) |
| #6317 | `plugins/nvoos-content-graph-ai/src/Admin/Settings/*` | **Content Graph AI settings shell** (sub-project track): `SettingsValidator`, `AiSection` (validate-then-sanitize), `SettingsRegistry` facade consuming the parent registry; real validations on ProviderSelection/ChatSettings; 14 characterization tests both matrices |
| #6318 | `plugins/nvoos-content-graph/*` (theme engine, appearance tab, icons, explorer chrome, edges, export, frontend parity, checkout fallback) | **Content Graph v1.0.4 visual experience system** (sub-project track, 1.0.3 → 1.0.4): shared token registry (PHP/JS byte-identical), dark/light/auto themes with WCAG 2.2 SC 1.4.11 ≥3:1 palettes, appearance settings tab, 24 SVG type glyphs, legend/minimap/zoom/keyboard explorer chrome, `GET /edges` route + relationship color families, theme/transparent/white export at 1–3×, `[nvoos_graph]` + block parity attributes, 3 visual filters, checkout-fallback redirect to the vendor product page |
| #6319 | `plugins/nvoos-content-graph-ai-platform/src/Queues/AsyncJobQueue.php`, `AgentCapabilityBoundaryHooks.php` (extracted) | **Platform async job queue** (sub-project track): byte-identical port of `WP_MCP_AI_Async_Job_Queue` (schema, cron hooks, error codes, filters, SSE action); standalone-gated; executor filter seam for Wave E1; drive-by production fix — `AgentCapabilityBoundaryHooks` extracted to its own PSR-4 file (the multi-class file fatalled standalone `Plugin::register()`) |
| #6320 | `plugins/nvoos-content-graph-ai-platform/src/Queues/QueueManager.php` | **Platform tool-execution queue manager** (sub-project track): byte-identical port of `WP_MCP_AI_Queue_Manager` (mode/priority constants, thresholds, estimates table, capability-flag semantics, deferred envelope, `wp_mcp_ai_before_tool_execute` filter, AJAX action); per-mode RabbitMQ/tool-registry seams |
| #6321 | `plugins/nvoos-content-graph-ai-platform/src/Queues/JobQueueManager.php` | **Platform job-queue concurrency manager** (sub-project track): byte-identical `mcp_ai_concurrent_jobs` schema, atomic `SELECT … FOR UPDATE SKIP LOCKED` claiming, retry-then-fail, stale-job timeout cleanup, stats; custom-table only (legacy option fallback dropped, documented) |
| #6322 | `includes/class-wp-mcp-ai-rest.php`, `includes/class-wp-mcp-ai-restriction-registry.php`, `docs/features/security/user-restrictions.md` | **Rate-limit unlock + fixed-window timer** — the REST request limiter never flagged the restriction registry (no record → no Lift button) and used a sliding window (steady traffic kept the block alive; `retry_after` always the full window). `check_rate_limit()` now uses fixed-window accounting (`{count, first_seen}` payload, TTL never extended past window end, legacy integer transients normalized) and returns remaining time; a new `wp_mcp_ai_rest_request_rate_limit_exceeded` action lets `WP_MCP_AI_Restriction_Registry` flag a `rate_limit` restriction (scope `rest`, auto-release at window end) — the block appears in the Restrictions tab + Token Manager with the existing Lift button; lifting also deletes the `wp_mcp_ai_rate_limit_user_{id}` transient. Guest (IP-keyed) blocks are not user-attached and expire on their own |
| #6327 | `includes/tools/class-wp-mcp-ai-tool-wake-up-context.php` | **MemPalace wing-scope leak (production)** — `matches_wake_filters()` now enforces `wing`/`room` exclusions: the Graphify graph anchors only *boost* scores for wing/room membership and never excluded out-of-scope memories, so once the Graphify addon loaded, `wake_up_context` in auto mode leaked cross-wing memories into a wing-scoped block. Rest of the PR is test-only (transcript mock restore, shared-registry teardown, Site Health provider-key isolation, transcript-mining logging cache reset) |
| #6328 | `includes/data/model-catalog.json` (2026.07.14 → 2026.09.05, 217 → 228 models) + ~45 derived files (migration map, settings defaults, selector/router, provider clients, token-budget service, cost calculator, usage tracker, model-service lists, admin dropdowns, Gemini managed-agent service/tool, deep-research + discover-new-models tools, CLI examples, 9 pro research tools, ext-cog + blueprint installer, examples) | **September 2026 model catalog refresh (Track B)** — new models (gpt-5.6-sol/-terra/-luna, gpt-6-astra Sept 3 preview, gpt-image-2, claude-opus-5, claude-fable-5.1, claude-mythos-5, gemini-3.6/3.7/3.8-flash, gemini-3.5-flash-lite, gemini-3.1-flash-image-preview, deepseek-v4-flash-vision-exp, kimi-k3, kimi-k2.7-code); removed retired IDs (deepseek-chat/-reasoner/-coder retired 2026-07-24, gemini-3.1-flash sunset 2026-09-01, imagen-4 shutdown 2026-08-17); pricing drift fixes (deepseek-v4-flash input 10× too low, Claude Opus 4.5/4.6/4.7 input $15 → $5); lifecycle (gemini-2.5-flash-image sunset 2026-10-02; claude-sonnet-4-6 incorrect sunset cleared); defaults (`default_gemini_model` → gemini-3.6-flash, `openai_image_model` → gpt-image-2, Kimi → kimi-k3); migration map gains claude-mythos-preview → claude-mythos-5 + DeepSeek successor targets. `lib/core/`, `lib/wordpress-adapter/`, and the Content Graph AI plugin keep their own mirrored tracks (flagged, not edited). 517 model-related tests / 8,290 assertions via the cross-worktree Docker runner |
| #6325 | `plugins/nvoos-content-graph-ai-platform/src/Queues/DeadLetterQueue.php` | **Platform dead-letter queue** (sub-project track): byte-identical `mcp_ai_dead_letters` schema, retry dispatch per install mode, base-notifier/executor probes gated on `defined('WP_MCP_AI_PATH')`; completes the queue pipeline AsyncJobQueue → QueueManager → JobQueueManager → DeadLetterQueue |

Docs-only PRs: #6323 (new `mcp-ai-wpoos-updates` coding-time skill — skill count 53 → 54 in `AGENTS.md` + copilot-instructions + README repo map), #6324 (YAML frontmatter fix in the new skill). Build-only: direct commits `1e54e5e1a3` (content-graph-ai-platform ZIP v2.0.0 refresh) + `4c01fee23e` (compiled assets + 1.1.70 distribution ZIPs).

### Key scope rules for this pass

- **Historical entries stay.** Old changelog/README per-version blocks (`[1.1.70]` and earlier) are history and are not rewritten.
- **Tool counts unchanged.** No tools added or removed this window. Figures stay **~303 base + ~1,263 Pro (~1,566 total)** with the live-registry caveat — no count sweep commit needed.
- **Sub-project version tracks:** nvoos-content-graph **1.0.3 → 1.0.4** (#6318). nvoos-content-graph-ai stays **1.0.4** (D-UI-4/D-UI-5 landed without a version bump). nvoos-content-graph-ai-platform stays **2.0.0** (Wave E2 queue layer landed without a version bump). Docs Hub 0.4.2 unchanged. Media Worker v3.2.0 unchanged. Checkout API v0.1.0 unchanged.
- **Skill count fold-in (A5):** PR #6323 explicitly deferred it — the v1.1.71 release-note surfaces must fold the new skill + count **54** into the Versioning lines (mirroring how v1.1.66 handled `mcp-ai-wpoos-test-suite`). `AGENTS.md`/copilot-instructions/README repo map already read 54 (verify-only).
- **Model catalog mention (Track B fold-in):** #6328's catalog refresh gets a changelog + release-note mention (per v1.1.69's "model catalog fixes — #6274" precedent).
- **No new `@since` anomalies:** the in-window base-plugin tags (PR #6322) correctly read `@since 1.1.71` — matches the shipping version, no OI-1 group.
- **Parked items stay parked** (OI-1/OI-2 in `docs-catch-up-open-items.md`): no `@since` reconciliation, no Docker live-count re-derivation.
- **Housekeeping requested** — remove stale build ZIPs: all `build/**/*1.1.68*` + `build/**/*1.1.69*` (ZIPs + `.sha256`), mirroring the v1.1.65-era cleanup that removed two versions back. The 1.1.70 artifacts remain the current release artifacts.
- **Unrelated working-tree noise excluded** — no untracked noise present this window.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.71] - 2026-09-05` section above `[1.1.70]`: Fixed (rate-limit unlock + fixed window #6322, MemPalace wing scope #6327, checkout-api `wp_salt()` #6315, Connectors links #6314), Changed (September 2026 model catalog #6328, ecosystem ports #6316/#6317/#6318/#6319–#6321/#6325, coding-time skill 53 → 54 #6323/#6324), Versioning (1.1.71, counts unchanged, content-graph 1.0.4, skill count 54, stale ZIPs removed).
2. **`readme.txt`** — Stable tag 1.1.71; new `= 1.1.71 - September 5, 2026 =` changelog paragraph (one paragraph, wp.org style).
3. **`README.md`** — version/release-date header → 1.1.71/2026-09-05; v1.1.71 Highlights paragraph; new "What's New at a Glance (v1.1.71)" section above v1.1.70; new "Latest Updates (v1.1.71 — September 2026)" section above v1.1.70. Tool-count mentions stay ~1,566 (unchanged); skill count 54 in the Versioning line.
4. **`docs/QUICK_REFERENCE.md`** — version + last-updated → 1.1.71/September 5; v1.1.71 entry above v1.1.70.
5. **`docs/DOCUMENTATION_INDEX.md`** — Plugin Version + Last Updated → 1.1.71/September 5; new SEPTEMBER 5 UPDATE banner; new `## 🆕 September 2026 — v1.1.71` section header + "New and updated documents (September 5, 2026, v1.1.71)" block (this plan, README/CHANGELOG/readme.txt/QUICK_REFERENCE).
6. **`docs/project/FOR_REVIEWERS.md`** — current version → 1.1.71 (September 2026); counts unchanged.

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed stamp + "New in v1.1.71" paragraph (no count change; `wake_up_context` wing/room scope enforcement #6327, model-catalog refresh notes model lists not tool slugs, rate-limit flagging is REST-side).
8. **`.context/security-checklist.md`** — v1.1.71 update line: fixed-window rate limiting (`retry_after` = remaining time), `wp_mcp_ai_rest_request_rate_limit_exceeded` → restriction-registry flagging (auto-release at window end), lift clears the request window, guest IP-keyed blocks expire on their own; checkout-api `wp_salt()` salts (never raw salt constants).
9. **`.context/testing.md`** — last-reviewed + v1.1.71 note: post-K16 singles (#6327) — transcript repository mock restored unconditionally, shared tool-registry instance restored untouched (OKF tools were wiped by `clear_tools()`), Site Health provider-key isolation, transcript-mining settings-cache reset; checkout-api suites join `phpunit.xml.dist` (#6315); test-suite skill grew to **40** patterns (38–40 landed in-window via #6327).
10. **`.context/rest-api.md`** — v1.1.71 note: fixed-window request limiter + new `wp_mcp_ai_rest_request_rate_limit_exceeded` action + restriction-registry flagging; `retry_after` now reports remaining time (contract change).
11. **`CLAUDE.md`** — header stamp → September 5, 2026 · 2.24; v1.1.71 notes (rate-limit unlock, MemPalace wing scope, model catalog refresh, checkout-api salts, skill count 54).
12. **`MAINTAINER_MAP.md`** — last-reviewed bump → September 5, 2026 (v1.1.71).
13. **`.bmad/README.md`** — last-reviewed bump → v1.1.71.
14. **All three `mcp-ai-wpoos-plugin` SKILL.md copies** — `plugin-version`/`plugin-version-tested` → 1.1.71 (`.agents/skills/` copy also gains a v1.1.71 note line in its recent-fixes section); registry-diagram count stays ~1,566.
15. **`.agents/skills/mcp-ai-wpoos-test-suite/SKILL.md`** — cluster board: mark the `#6327 post-K16 singles` block merged (drop "(open)"); pattern count stays 40; `last-updated` stays 2026-09-05.
16. **`.agents/skills/mcp-ai-wpoos-updates/SKILL.md`** — frontmatter `plugin-version`/`plugin-version-tested` → 1.1.71.

### P2 — Operational & feature docs

17. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" stamp → September 5, 2026 (v1.1.71); no addon/toolkit count changes.
18. **`docs/reference/tools/tool-reference.md`** — header Last Updated → September 5, 2026 (counts unchanged).
19. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-05 (v1.1.71 pass); no new `@since` groups (in-window tags correctly read 1.1.71); **OI-4 gains item 4** — the `test-agents.php` Woo-loaded standalone failure noted in PR #6319 (hand to the test-suite workstream).
20. **`docs/project/plans/v1.1.71-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

21. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.71 (`package-lock.json` intentionally untouched).

### Housekeeping (requested this pass)

22. **Remove stale build ZIPs** — `git rm` all `build/**/*1.1.68*` + `build/**/*1.1.69*` (ZIPs + `.sha256`; main + optional-components + toolkit-addons). Commit: `chore: remove stale 1.1.68 and 1.1.69 build ZIPs [skip ci]`.

### Verify-only (no changes expected)

- `AGENTS.md` §1 + `README.md` repo map + `.github/copilot-instructions.md` skill counts — already read 54 (updated in-window by #6323); no further change.
- `.context/README.md` / `.context/pro-vs-base.md` / `.context/conventions.md` tool counts — stay ~1,566 (no count change this window).
- `addons/pro/README.md` — toolkit count stays 31 (no new toolkits).
- `docs/project/proposals/RELATED_PROPOSALS_INDEX.md` — no new proposals this window (034 already indexed by #6318's sub-project docs).
- `addons/docs-hub/CHANGELOG.md` — no docs-hub changes (0.4.2 unchanged).
- `docs/developer/testing-docs/TEST-SUITE-REMAINING-FIXES-PLAN.md` — already updated in-window by #6327 (post-K16 cluster row + residual block).
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" description — base count unchanged.

---

## 3. Execution log (2026-09-05)

- **P3 version bump:** `mcp-ai-wpoos.php` (Version 1.1.71), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.71 ✅ (`package-lock.json` untouched)
- **P0 release surfaces:** `CHANGELOG.md` [1.1.71] section (rate-limit unlock + MemPalace wing scope + checkout-api salts + Connectors links + model catalog + ecosystem ports + skill count), `readme.txt` (Stable tag + 1.1.71 changelog paragraph), `README.md` (header/highlights/What's-New/Latest-Updates), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md` (banner + section header + Sep 5 block), `docs/project/FOR_REVIEWERS.md` (version) ✅
- **P1 agent context:** `.context/tool-registry.md`, `.context/security-checklist.md`, `.context/testing.md`, `.context/rest-api.md` (v1.1.71 notes + stamps), `CLAUDE.md` (2.24 stamp + v1.1.71 notes), `MAINTAINER_MAP.md` + `.bmad/README.md` (stamps), three `mcp-ai-wpoos-plugin` SKILL.md copies (1.1.71 stamps + v1.1.71 note in the `.agents/skills/` copy), `mcp-ai-wpoos-test-suite` SKILL.md (#6327 block → merged), `mcp-ai-wpoos-updates` SKILL.md (frontmatter 1.1.71) ✅
- **P2 operational docs:** `ADDON_INVENTORY.md` (stamp), `tool-reference.md` (stamp), `docs-catch-up-open-items.md` (last-reviewed; no new groups), this plan ✅
- **PR-description review:** all 14 PR bodies + 2 build commits read; window classified (production vs sub-project vs test/docs/build); no new `@since` anomalies (in-window tags say 1.1.71); one residual test-suite finding from PR notes recorded as OI-4 item 4 (test-agents Woo-loaded standalone failure, #6319) ✅
- **Tool counts:** unchanged (no tools added/removed) — ~303 base + ~1,263 Pro (~1,566 total); no count sweep needed ✅
- **Skill-count fold-in (A5):** new `mcp-ai-wpoos-updates` skill + count 54 folded into the v1.1.71 Versioning lines (CHANGELOG + README + QUICK_REFERENCE) ✅
- **Housekeeping:** stale 1.1.68 + 1.1.69 build ZIPs removed (60 files incl. `.sha256` + optional-components + toolkit-addons) ✅
- **Verify-only items:** all confirmed no-change ✅
- **Validation:** see section 4.

## 4. Commit breakdown (mirror v1.1.58–v1.1.70)

1. `chore: bump version to 1.1.71` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.71 changelog and release notes` — `CHANGELOG.md`, `readme.txt`, `README.md`, `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.71 changes` — `.context/*` (4 files), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies, `.agents/skills/mcp-ai-wpoos-test-suite/SKILL.md`, `.agents/skills/mcp-ai-wpoos-updates/SKILL.md`
4. `docs: reconcile 1.1.71 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/plans/docs-catch-up-open-items.md`, `docs/project/plans/v1.1.71-docs-catch-up.md`
5. `chore: remove stale 1.1.68 and 1.1.69 build ZIPs [skip ci]` — `build/**/*1.1.68*` + `build/**/*1.1.69*` (housekeeping)

## 5. Validation

- `php -l` on the three bumped PHP files ✅
- `package.json` JSON parse ✅
- `git grep "1\.1\.70"` sweep — remaining hits are intentional history (previous-release sections, `@since` docblocks, plans, archive, build artifacts) ✅
- `git grep -E "1,566"` sweep — counts unchanged, no stale-count sweep required ✅

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue [#5968](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5968)) and Docker-based live-count re-derivation (OI-2, issue [#5967](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5967)) stay parked.
- **New this pass:** OI-4 item 4 — the `test-agents.php` "Audit trail cpt registered in standalone mode" failure when WooCommerce test plugins are loaded (PR #6319, "Known pre-existing failure (not fixed here)"; passes in CI where Woo isn't loaded). No new `@since` anomalies — the in-window tags correctly read 1.1.71.
- **Housekeeping executed this pass:** stale 1.1.68 + 1.1.69 build ZIPs removed. Optional follow-up for a future pass (not requested): stale sub-project ZIPs still in `build/` (`nvoos-content-graph-ai-v1.0.2/1.0.3*`, `nvoos-content-graph-v1.0.3*`, `nvoos-content-graph-ai-platform-v1.0.2.zip`, `nvoos-docs-hub-v0.4.1.zip`).
