# Docs & Release Catch-Up Plan — v1.1.85 (PRs #6749–#6759)

## 1. Context: What changed since the v1.1.84 pass

The v1.1.84 catch-up merged as `8bda010c5a` ("chore: 1.1.84 docs catch-up (PRs #6745–#6747) (#6748)") on 2026-09-22. **11 PRs** (#6749–#6759) and **13 build commits** landed on `alpha-working` afterward (2026-09-22T13:32 → 2026-09-23T20:33). Per user direction this window opens the **`[1.1.85]` line** (full version bump + new changelog section) and executes the **stale-ZIP housekeeping** deferred by the v1.1.84 pass (the 1.1.84 package set now exists).

### Production-touching PRs

| PR | Files | Change |
|---|---|---|
| #6753 | `addons/pro/includes/mcp-apps/{class-wp-mcp-ai-mcp-app-client,class-wp-mcp-ai-mcp-app-registry,class-wp-mcp-ai-rest-mcp-apps-controller,mcp-apps-init}.php`, `includes/assistants/metaboxes/class-wp-mcp-ai-metabox-mcp-apps.php`, `includes/admin/{class-wp-mcp-ai-admin-settings-base.php,sections/class-wp-mcp-ai-section-security.php}`, new `tests/mcp-apps/test-mcp-apps-connection-enhancements.php`, `tests/test-security-center.php` | **MCP Apps connection enhancements.** Metabox diagnostics (per-row Test Connection / Discover Tools with inline protocol/handshake/server/session/latency/tool-count results + verbatim errors; persisted `_wp_mcp_ai_mcp_app_status` badge + tool-count chip; Test All); basic auth end-to-end (raw user:pass auto-encoded or pre-encoded base64; token masking — stored credentials never echoed, empty = keep); `Mcp-Session-Id` capture + echo on subsequent requests; legacy-handshake fallback on -32601/-32600/session errors; JSON-RPC error bodies decoded on HTTP 4xx; spec-compliant `Accept`; `stdClass` params coercion fix; mcpServers JSON import; loopback detection with PHP-FPM deadlock warning. **Security Center → Network & Headers: MCP App Allowed Hosts** textarea (constant hard override → filter + saved setting merge). |
| #6754 | `addons/pro/includes/mcp-apps/class-wp-mcp-ai-mcp-app-client.php`, tests | **Protocol header after session fallback.** The client records the negotiated `protocolVersion` and advertises it post-initialize (falls back to 2026-07-28 stateless); the `_meta` envelope is suppressed once a legacy session is established. Test-infra fix: PHPUnit 11 discovers only the first class per file — the #6753 registry/REST suites were split into one-class-per-file and had never executed; they now run (and pass) with two real findings fixed. |
| #6755 | `includes/class-wp-mcp-ai-rest.php`, `addons/pro/includes/mcp-apps/class-wp-mcp-ai-mcp-app-registry.php`, `addons/pro/includes/mcp-apps/mcp-apps-init.php`, new `tests/mcp-apps/test-mcp-app-chat-exposure.php` | **Expose MCP App bridge tools to the chat payload.** Base: new `wp_mcp_ai_chat_effective_tools` filter seam in `build_tools_payload()` (after attention-based filtering, before the per-tool capability check; the empty-selection early return now runs after the filters). Pro: `wp_mcp_ai_mcp_apps_expose_tools()` resolves the assistant ID and appends the bridged slugs (`mcp_app_<label>_<tool>`) for the assistant's enabled apps. Registry: `collect_remote_tools()` extraction + `get_remote_tool_slugs()`. |
| #6756 | `addons/pro/includes/mcp-apps/class-wp-mcp-ai-mcp-app-client.php`, new `tests/mcp-apps/test-mcp-app-client-internal-bridge.php` | **In-process same-site bridge.** `dispatch_request()` routes the JSON-RPC call through `rest_do_request()` when the server URL host matches this site and the derived route is registered — no outbound socket, no TLS handshake, no second PHP-FPM worker (fixes the worker-starvation TLS deadlock on self-hosted MCP servers). Safety rails: outbound HTTP for remote hosts, unregistered routes, or when `wp_mcp_ai_mcp_app_disable_inprocess_bridge` returns true. |
| #6757 | `addons/pro/includes/mcp-apps/class-wp-mcp-ai-mcp-app-client.php`, `tests/mcp-apps/test-mcp-app-client-internal-bridge.php` | **`rest_post_dispatch` in the in-process bridge.** `dispatch_internal_rest()` re-applies `rest_post_dispatch` after `rest_do_request()` (mirroring `serve_request()`), so response-side filters (e.g. EMCP Tools' `Mcp-Session-Id`) reach the client and `tools/list` stops failing with "Missing Mcp-Session-Id header". |
| #6758 | `includes/class-wp-mcp-ai-rest.php`, `includes/tools/class-wp-mcp-ai-tool-list-mcp-tools.php`, `addons/pro/includes/mcp-apps/{mcp-apps-init.php,class-wp-mcp-ai-mcp-app-registry.php}`, `tests/mcp-apps/test-mcp-app-chat-exposure.php` | **Wire bridge tools into every chat tool surface.** The seam now carries a third `$assistant_id` argument; applied on `handle_tools_list()`, `handle_tool_request()`, `execute_tool_call_internal()`, and the `list_mcp_tools` catalogue filter (fixes the 403/404s from the assistant-ID-always-0 registration gap and the consumed-JSON-body resolution gap). Pro exposure registers bridges in the local registry (idempotent, transient-cached discovery); `rest_pre_dispatch` registers bridges before REST arg validation; 60 s negative discovery cache; `save_apps()` invalidates the `/tools` REST list cache. |
| #6749 | `addons/docs-hub/**` (sub-project) | **Docs Hub 0.5.0** — local-first by default (`sources = ['uploads']`, zero remote calls), remote GitHub import becomes explicit opt-in (`enable_remote_repos`, off by default, server-side enforcement incl. `/remote/tree` → 403), migration auto-enables the toggle for pre-0.5.0 remote installs. |
| #6750 | `addons/docs-hub/**` (sub-project) | **Docs Hub 0.5.0 hardening** — `is_path_safe()` symlink-escape fix (`realpath()` the candidate file) + regression test; short-description trim for PCP; 0 blocking Plugin Check errors. |
| #6759 | `addons/docs-hub/**` (sub-project) | **Docs Hub 0.5.1** — uploads folder moved to `wp-content/uploads/nvoos-docs-hub/content/` (slug-named), one-time non-destructive migration of the legacy `uploads/docs` folder (rename fast-path, never overwrites, never follows symlinks, drops the stale 0.5.0 guard, removes the legacy folder only when empty); settings UI renders the live path. |

### Docs/tooling PRs

- #6751 — README TOC anchor fixes (VS16 fallback-anchor cleanup) + `bin/validate-readme-anchors.py` repaired to GitHub's real anchor rules (leading hyphens kept, no collapsing, emoji/VS16/ZWJ stripped, h1–h4, full-file scan, code fences excluded) + `link-check.yml` runs the validator on README changes + updates-skill guardrail.
- #6752 — README consolidation: one `## 📜 Release History` section keeps the **12 most recent releases in full** (v1.1.84 → v1.1.73); the Previous Releases table gains 31 one-line rows; 4,423 → 3,301 lines; VS16 headings normalized; 6 stale manual anchors removed; the 12-entry maintenance rule codified in the README + the updates skill.

### Build-only (13 commits)

wp.org packages v1.1.84 ×5, SPA addon ZIPs ×2, compiled assets ×1, toolkit/addon re-builds, content-graph-ai-platform v2.0.0, docs-hub v0.5.1 — all `[skip ci]`. No direct commits in-window.

### Key scope rules for this pass

- **`[1.1.85]` is the new current release** — new changelog section at the top; `[1.1.84]` and earlier stay untouched.
- **Counts unchanged:** no static tool registrations in-window — the MCP App bridge slugs (`mcp_app_<label>_<tool>`) are registered **dynamically** at chat time and are not counted. Tool count stays **~308 base + ~1,282 Pro (~1,590 total)**; bundled skills stay **75 base + 41 Pro**; coding-time skills stay **59** (the updates skill gained the README-anchor guardrail + 12-entry maintenance rule in-window, #6751/#6752); model catalog stays **v2026.09.22**; providers **15**; addons **27**.
- **Docs Hub addon:** 0.4.7 → **0.5.1** (#6749 → 0.5.0, #6750 hardening, #6759 → 0.5.1). All other sub-project tracks unchanged (Media Worker v3.2.0, content-graph 1.0.8, content-graph-ai 1.0.4, content-graph-ai-platform 2.0.0, content-graph-pro 1.0.0 — no port waves in-window, Checkout API 0.1.2, Comic Reader 0.5.0).
- **README 12-entry window (per #6752's codified rule):** the new `### v1.1.85` entry joins Release History and the oldest detailed entry (`### v1.1.73`) is demoted to a one-line row in the Previous Releases table; the Overview keeps a single current-release "What's New at a Glance" block.
- **Housekeeping EXECUTED this pass (user-directed):** remove the stale **1.1.83 build ZIP set** (32 files: 9 in `build/` incl. 3 `.sha256`, 2 in `build/optional-components/`, 21 in `build/toolkit-addons/`) + the superseded docs-hub ZIPs (`nvoos-docs-hub-v0.4.7.zip`, `nvoos-docs-hub-v0.5.0.zip` — 0.5.1 is current). The 1.1.84 package set is the current release artifact set.
- **Parked items stay parked** (OI-1/OI-2): record new `@since` anomalies, never fix. **New OI-1 group 25** — `@since 1.9.1`/`1.9.2`/`1.9.4` tags (26 instances) introduced by the MCP Apps window across 6 files (mcp-app-client ×7, mcp-app-registry ×9, rest-mcp-apps-controller ×2, mcp-apps-init ×4, metabox-mcp-apps ×2, `class-wp-mcp-ai-rest.php` ×2, PRs #6753–#6758).
- **Unrelated working-tree noise excluded** — untracked `_anchor_test.md`, `cmark_autolink.c`, `docs/user-guides/assistants/CONTENT-GRAPH-MARKETING-ASSISTANT.md`, `gh_anchors*.txt`, `local_headings.txt`, `readme_*.html`, `readme_*_raw.md` are never staged.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.85] - 2026-09-24` section: Added subsections for the MCP Apps connection + exposure wave (#6753–#6758); a Fixed subsection for Docs Hub 0.5.1 (sub-project, #6749/#6750/#6759); a Docs subsection for the README anchor/consolidation work (#6751/#6752). Versioning block: bump to 1.1.85; Docs Hub 0.4.7 → 0.5.1; all other sub-projects unchanged; model catalog v2026.09.22 (unchanged); tool count unchanged (~308 base + ~1,282 Pro, ~1,590 total — dynamic bridge slugs not counted); bundled skills 75+41; coding-time skills 59 (unchanged); stale 1.1.83 ZIP set removed (32 files) + superseded docs-hub ZIPs.
2. **`readme.txt`** — `Stable tag: 1.1.85`; new `= 1.1.85 - September 24, 2026 =` changelog paragraph above the 1.1.84 entry (MCP Apps connection/exposure wave, Docs Hub 0.5.1, README consolidation, stale ZIP removal).
3. **`README.md`** — `**Version:** 1.1.85` / `**Release Date:** 2026-09-24`; new v1.1.85 Highlights paragraph replaces the v1.1.84 one at the top; "What's New at a Glance" heading → (v1.1.85) with new bullets; Release History gains `### v1.1.85 — September 24, 2026` (+ `#### Title` + bullets + versioning line) at the top; **demote `### v1.1.73`** to the first row of the Previous Releases table; evergreen count lines unchanged (no count change).
4. **`docs/QUICK_REFERENCE.md`** — `**Version:** 1.1.85` / `**Last Updated:** September 24, 2026`; new v1.1.85 recent-updates entry above the v1.1.84 one; plan link gains this plan.
5. **`docs/DOCUMENTATION_INDEX.md`** — `**Plugin Version:** 1.1.85` / `**Last Updated:** September 24, 2026`; new SEPTEMBER 24, 2026 banner above the SEPTEMBER 22 one (updated-docs list: this plan, README/CHANGELOG/readme.txt/QUICK_REFERENCE + the in-window docs-hub docs).
6. **`docs/project/FOR_REVIEWERS.md`** — "Current version" → 1.1.85; counts unchanged.

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed → September 24, 2026 (v1.1.85); new v1.1.85 paragraph: no new static registrations (the MCP App bridge slugs are dynamic, registered at chat time via the `wp_mcp_ai_chat_effective_tools` seam — not counted); "Total tools" line stays ~1,590.
8. **`.context/security-checklist.md`** — last-reviewed + v1.1.85 line: MCP App Allowed Hosts setting (constant hard override → filter + saved setting); basic-auth token masking (stored credentials never echoed; empty = keep, resolved server-side); in-process bridge safety rails (remote hosts/unregistered routes/disable filter fall back to HTTP); `rest_post_dispatch` parity in the bridge.
9. **`.context/testing.md`** — last-reviewed + v1.1.85 note: new MCP Apps suites (connection-enhancements split into one class per file, internal-bridge, chat-exposure); the PHPUnit-11 one-class-per-file discovery lesson (#6754); validation across WP 6.9 + 7.1.
10. **`.context/rest-api.md`** — verify-only: no new REST routes in-window (the `/mcp-apps/test` + `/discover` endpoints pre-existed #6753; the window's `register_rest_route` additions are test-only). Stamp untouched.
11. **`CLAUDE.md`** — stamp → September 24, 2026 · 2.39; new v1.1.85 bullet (MCP Apps wave + Docs Hub 0.5.1); count lines unchanged.
12. **`MAINTAINER_MAP.md`** — last-reviewed → September 24, 2026 (v1.1.85).
13. **`.bmad/README.md`** — last-reviewed → September 2026 (v1.1.85).
14. **Three `mcp-ai-wpoos-plugin` SKILL.md copies** (`.agents/skills/`, `includes/bundled-skills/`, `plugins/nvoos-content-graph-ai-platform/data/bundled-skills/`) — `plugin-version`/`plugin-version-tested` → 1.1.85, `last-updated` stays 2026-09-22; the `.agents/skills/` copy's note section gains a v1.1.85 line.
15. **`.agents/skills/mcp-ai-wpoos-updates/SKILL.md`** — references: latest executed plan → `v1.1.85-docs-catch-up.md`; template list gains it.
16. **`AGENTS.md` + `.github/copilot-instructions.md`** — verify-only: every count (59 coding-time, 75+41 bundled, ~308/~1,282/~1,590) unchanged in-window.

### P2 — Operational & feature docs

17. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" → September 24, 2026 (v1.1.85); Docs Hub row: version 0.4.7 → **0.5.1** with the 0.5.0 local-first + 0.5.1 folder-migration summary prepended.
18. **`docs/reference/tools/tool-reference.md`** — "Last Updated" → September 24, 2026 (v1.1.85 — MCP Apps wave); Tool Count line gains the "unchanged in v1.1.85 — the MCP App bridge slugs are dynamic chat-time registrations" clause.
19. **`docs/project/proposals/README.md`** — verify-only (proposal 040 status unchanged: Phases 0–2 done, Phase 3 deferred).
20. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-24 (v1.1.85 pass); **OI-1 group 25 added** (`@since 1.9.1`/`1.9.2`/`1.9.4` ×26, MCP Apps window, PRs #6753–#6758); OI-2 current-figure label → v1.1.85 (counts unchanged); scope line extended through v1.1.85.
21. **`docs/project/plans/v1.1.85-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

22. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` (version) — 1.1.84 → 1.1.85. **Never** `package-lock.json`.

### Housekeeping (user-directed — executed this pass)

23. **Stale build ZIPs** — delete the 1.1.83 set (32 files) + superseded docs-hub 0.4.7/0.5.0 ZIPs (2 files). Commit: `chore: remove stale 1.1.83 build ZIPs [skip ci]`.

### Verify-only (no changes expected)

- Skill counts: 59 directories under `.agents/skills/`; 75 bundled base + 41 Pro (73 dirs + `LICENSE.txt` + `THIRD_PARTY_NOTICES.md` accounting — unchanged).
- `addons/pro/README.md` — toolkit count stays 31.
- `docs/project/ecosystem-port-tracker.md` — no port-loop PRs in-window.
- Sub-project changelogs (docs-hub own track bumped 0.5.1 in-window by #6759; others unchanged).
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" description — base count 308 still satisfies "300+".
- Model suites — no catalog changes in-window; model-track files untouched.
- `tool-status.txt` — unchanged in-window; OI-5 (missing CRM slugs) stays parked.

---

## 3. Execution log (2026-09-24)

- **Orientation** — window mapped from `8bda010c5a` (v1.1.84 catch-up merge #6748) to `6c11a4c4eb` (origin/alpha-working HEAD): 11 merged PRs (#6749–#6759) + 13 build commits, no direct commits. All PR descriptions read in full via `gh pr list` (`gh` CLI per the updates skill). `@since` sweep on the window diff found the new OI-1 group 25 records in §2.20.
- **Claims verified against the tree** — no new static tool registrations (the `includes/class-wp-mcp-ai-tool-registry.php` + `addons/pro/mcp-ai-wpoos-pro.php` diffs carry no `register` additions; bridge registration is dynamic); model catalog read directly (version `2026.09.22`, unchanged); docs-hub header `Version: 0.5.1`; `.agents/skills/` = 59 dirs; `includes/bundled-skills/` = 73 dirs + 2 files (75 accounting, unchanged); version constants confirmed at 1.1.84 pre-bump; `build/` 1.1.83 set inventoried (9 + 2 + 21 = 32 files) + superseded docs-hub 0.4.7/0.5.0 ZIPs.
- **Commits 1–5** — version bump, release notes, agent context, reference surfaces, stale-ZIP removal (see §4); no count changes ride along (all counts unchanged in-window).
- **Post-pass sweeps** — `git grep "1\.1\.84"` + count sweeps executed after all commits (see §5).

## 4. Commit breakdown (mirror v1.1.58–v1.1.84)

1. `chore: bump version to 1.1.85` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.85 changelog and release notes` — `CHANGELOG.md` (new [1.1.85] section), `readme.txt`, `README.md` (highlights + What's New + Release History entry + v1.1.73 demotion to the Previous Releases table), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.85 changes` — `.context/*` (tool-registry, security-checklist, testing), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies, `.agents/skills/mcp-ai-wpoos-updates/SKILL.md`
4. `docs: reconcile 1.1.85 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/plans/docs-catch-up-open-items.md`, `docs/project/plans/v1.1.85-docs-catch-up.md`
5. `chore: remove stale 1.1.83 build ZIPs [skip ci]` — `build/**/*1.1.83*` (32 files) + `build/nvoos-docs-hub-v0.4.7.zip` + `build/nvoos-docs-hub-v0.5.0.zip`

(No separate count-sweep commit — no count changes in-window; historical per-version blocks, plans, proposals, and the archive are untouched.)

## 5. Validation

- `php -l` on every bumped PHP file; JSON-parse `package.json`.
- `git grep "1\.1\.84"` sweep on current-release surfaces — remaining hits must be intentional history only (the `[1.1.84]` changelog block, per-version README/readme.txt blocks, `@since` docblocks, plans, archive).
- Count sweep — `~308`/`~1,282`/`~1,590`/`75`/`41`/`59` surfaces unchanged; `git grep` the old labels only inside history blocks.
- README anchor check — `python3 bin/validate-readme-anchors.py` after the README edits; Release History must hold exactly the 12 most recent releases (v1.1.85 → v1.1.74) and the v1.1.73 row must be first in the Previous Releases table.
- Stale-ZIP sweep — `ls build/**/*1.1.83*` returns nothing; the 1.1.84 set and the non-versioned 1.0.0/SPA/toolkit zips remain; docs-hub 0.5.1 ZIP remains.
- Docs Hub sub-project files untouched by this pass (own track).

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue #5968) and Docker-based live-count re-derivation (OI-2, issue #5967) stay parked. OI-2's current-figure label refreshed to the v1.1.85 baseline (~308 base + ~1,282 Pro, ~1,590 total — unchanged, no registrations in-window).
- **New items raised this pass:** OI-1 **group 25** (`@since 1.9.1`/`1.9.2`/`1.9.4` ×26 across the 6 MCP Apps files, PRs #6753–#6758). Recorded in the tracker, not fixed.
- **Noted observations (other workstreams):** #6753's deferred items (auto re-test after editor save; design review of the in-process bridge — the latter shipped as #6756) are documented in the PR; nothing new to file. Docs Hub 0.5.1 continues its own wp.org review track.
