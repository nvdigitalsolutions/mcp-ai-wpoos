# Docs & Release Catch-Up Plan — v1.1.81 (PRs #6634–#6643)

## 1. Context: What changed since the last docs catch-up

The Sep 15 catch-up merged as `876aa0a83e` ("chore: 1.1.80 docs catch-up (#6633)") and covered PRs #6621–#6632. **9 merged PRs** (#6634–#6641, #6643), **6 build commits**, and **1 direct commit** landed afterward, none present on any release-note surface (except a partial `[1.1.81]` CHANGELOG section created in-window by #6634/#6643). The window is the **Shopify UCP mode-aware tool routing + image cards** (#6634/#6638), the **FlowHub Remote Sites resolver + connection proxy** (#6635/#6637), the **JobNavigator CRM adoption** (#6636, 5 new Pro tools) with the **CG Pro port** (#6640) and **Gmail reply poller** (#6641), the **OpenTerminal financial resilience batch** (#6639, 8 new Pro tools), and **multi-recipient result-delivery email** (#6643). #6642 is not in the window (not merged on `alpha-working`; PR status not re-verifiable without GitHub API access this pass).

### Production-touching PRs

| PR | Production files | Change |
|---|---|---|
| #6634 | `addons/pro/includes/class-wp-mcp-ai-shopify-client.php`, `addons/pro/includes/tools/ecommerce/class-wp-mcp-ai-pro-tool-shopify-{products,catalog,orders,customers,inventory}.php`, `-remote-connections/trait-wp-mcp-ai-shopify-connection-resolver.php`, `class-wp-mcp-ai-tool-remote-shopify-connection.php` + CG Pro port of all | **Shopify tools are UCP catalog mode-aware** — `storefront_catalog`/`global_catalog` connections route `shopify_products` list/search/get to live UCP queries (`search_catalog`/`lookup_catalog`/`get_product`; no caching per UCP guidelines, `live: true`); admin-only tools (`orders`/`customers`/`inventory`) refuse catalog connections with an actionable hint; `shopify_catalog` becomes the unified mode-aware live catalog tool (canonical `get_product`, batch `ids` + `not_found`, `lookup_by_variant`); UCP passthrough (buyer `context`, opaque `cursor`, per-mode clamps 250/50/10); `remote_shopify_connection` validates UCP modes via the MCP `tools/list` handshake and annotates connections. CG Pro byte-identical; new dual-matrix characterization suites. |
| #6635 | **NEW** `includes/class-wp-mcp-ai-flowhub-connection-helper.php`, `addons/pro/includes/class-wp-mcp-ai-flowhub-client.php`, Pro FlowHub tools (analytics/inventory/locations/products/settings/sync) + shared trait | **FlowHub tools resolve via Remote Sites connections** — a shared resolver chain (explicit `connection_id` → toolkit settings → configured sync connections → first enabled FlowHub connection) replaces the toolkit-settings-only lookup that failed with "credentials are not configured" for connection-only setups; Pro tools gain `connection_id`, the Pro client gains `from_connection()`, sync status reads per-connection freshness keys. |
| #6636 | **NEW 5 Pro CRM tools** (`bulk_move_deal_stages`, `create_tracked_link`, `record_crm_reply`, `get_crm_handover`, `get_pipeline_digest`) + **NEW** `class-wp-mcp-ai-crm-{stage-history,identity,link-tracker}.php`, `addons/pro/mcp-ai-wpoos-pro.php`, modified create/update-lead + move/delete-deal | **JobNavigator CRM adoption** — machine-readable deal stage history (from/to/at/source + `stage_changed_at`, undoable last move, no-op moves preserve ageing); `create_lead` dedups emails (pointer + `allow_duplicate` opt-out), links canonical companies with auto-create, stores source snapshots; leads/deals gain `last_email_received`/snippet/sentiment signals + settings-gated auto-disqualify; bulk stage moves with per-row reporting + undo; inbound reply recording with `email_reply` stage advancement; paste-ready handover bundles; pipeline digest with stalled-deal detection; tracked proposal links resolved by a front-end open tracker; deleting a won deal releases its lead to opportunity when no other won deal remains. Skill `design-crm` + CRM README updated; new `docs/developer/crm-toolkit-jobnavigator-adoption-plan.md`; WP1–WP9 test suite. |
| #6637 | `addons/pro/includes/class-wp-mcp-ai-pro-remote-site-manager.php`, `includes/class-wp-mcp-ai-flowhub-client.php`, flowhub connection helper + `flowhub_get_inventory` tool | **FlowHub connection proxy honored in live tools** — live requests ignored the Remote Sites connection proxy (server-IP egress → auth rejection while test/sync succeeded); proxy config now resolves from the connection (toolkit-settings fallback) and attaches via `http_api_curl` in the base client + `flowhub_get_inventory`; `record_health_metric()` made public for connection-bound client requests. |
| #6638 | `addons/pro/includes/tools/ecommerce/class-wp-mcp-ai-pro-tool-shopify-{products,catalog}.php`, **NEW** `trait-wp-mcp-ai-shopify-product-normalizers.php` + CG Pro mirror | **Shopify product image cards** — every product-returning path now carries `images[]` URLs plus a chat-rendered markdown card, capped at 10 cards for list/search (full payload intact); UCP + REST normalizers move into a shared trait used by `shopify_products`/`shopify_catalog`; UCP results stay live-only. CG Pro mirrors byte-identically; image-card test suites (10-card cap, raw-payload preservation, media-to-images mapping). |
| #6639 | **NEW 8 Pro financial tools** (`market_screener`, `macro_data_fetcher`, `economic_calendar_fetcher`, `earnings_calendar_fetcher`, `options_chain_fetcher`, `crypto_market_data`, `portfolio_transaction_log`, `price_alerts`) + **NEW** `class-wp-mcp-ai-financial-transaction-cpt.php`, `includes/services/class-wp-mcp-ai-market-data-providers.php`, `-technical-indicators.php`, yfinance service + node/python services reworked + CG Pro port | **OpenTerminal financial resilience** — provider fallback chains + stale-while-revalidate caching in the yfinance service; keyless microservice auth; six market-data tools (screener, macro, economic/earnings calendars, options, crypto); PHP technical indicators; news de-duplication; a portfolio transaction ledger with P&L (new CPT); price alerts with a daily cron. `docs/reference/tools/tool-status.txt` +8 slugs; new `docs/project/plans/financial-toolkit-openterminal-lessons-plan.md`; financial-planning README + TOOL_INDEX updated. CG Pro port byte-identical with standalone wiring. |
| #6640 | `plugins/nvoos-content-graph-pro/src/tools/crm/**` | **CG Pro CRM port** — byte-identical port of the stage-history/identity/link-tracker helpers + five new CRM tools, re-ports of the five modified tools and the engine; CRM init gains engine loads, the tracked-link resolver boot, and five registrations; 11 characterization tests green in both matrices. |
| #6641 | **NEW** `addons/pro/includes/tools/crm/inbound/class-wp-mcp-ai-crm-gmail-reply-poller.php`, `record_crm_reply` (`apply()` static core), CRM engine/init, **NEW** `addons/pro/config/pipeline-digest-recipe.json` | **Gmail reply poller + pipeline digest recipe** — cron-driven inbound reply classification polls Gmail for unread replies from known leads, classifies sentiment, and applies reply signals (optional positive-sentiment stage advancement); `record_crm_reply` gains a cron-safe static `apply()` core; ships a digest scheduling recipe asset for the Workflow Builder + Pro Schedule Manager; skill `design-crm` updated. |
| #6643 | `addons/pro/includes/services/class-wp-mcp-ai-result-delivery-service.php`, `-nodemailer-service.php`, `-pro-schedule-manager.php`, `assets/js/schedule-manager.js` | **Multiple email recipients in result delivery** — comma/semicolon/whitespace-separated lists, normalized on save and again in `sanitize_result_delivery()` via a shared `sanitize_email_recipients()` helper (per-address sanitize, empties dropped, dedupe); `send_email()` fans out through Nodemailer and the `wp_mail()` fallback; legacy `notify_email` failure notifications stay single-address. |

### Direct commit

- `e96121d574` — README housekeeping on `alpha-working`: removed the stale v1.1.79 highlights paragraph (the top now carries only the latest release's paragraph).

### Build-only (6 commits)

compiled assets/distributions ×5, **oOS WP.org packages v1.1.80 ×1** (`5abc15a6cc` — the 1.1.80 package builds landed in-window; the root/optional/toolkit-addon 1.1.80 ZIP sets landed with the v1.1.80 pass and are current).

### Key scope rules for this pass

- **Historical entries stay.** Old CHANGELOG/README per-version blocks (`[1.1.80]` and earlier) are history and are not rewritten.
- **Tool counts DO change this window.** #6636 registers **5 new Pro CRM tools** and #6639 registers **8 new Pro financial tools**. Figures move **~1,266 → ~1,279 Pro (~1,572 → ~1,585 total; base stays ~306)** with the live-registry caveat. The count-sweep commit (A3 §5) is included this pass.
- **CHANGELOG continuation (deliberate):** #6634/#6643 created the `## [1.1.81] - 2026-09-16` section in-window. The pass **completes that section** (folds #6635–#6641, reorders chronologically), updates the date to the pass date (2026-09-17), and appends the Versioning block. The released `[1.1.80]` block is untouched.
- **README highlight rotation:** the in-window direct commit removed the v1.1.79 highlights paragraph; the pass replaces the v1.1.80 paragraph with a v1.1.81 one (top keeps only the latest release's paragraph — the in-window convention).
- **Skill count stays 58** — no new skills in-window; `design-crm` gained JobNavigator + Gmail-poller content (#6636/#6641). Bookkeeping: verify-only.
- **Sub-project version tracks unchanged:** nvoos-content-graph **1.0.8**, nvoos-content-graph-ai **1.0.4**, nvoos-content-graph-ai-platform **2.0.0**, nvoos-content-graph-pro **1.0.0** (byte-identical port batches only — F2–F6/F2-E tracker rows updated in-window by the port loop), Checkout API **0.1.2**, Docs Hub **0.4.6**, Comic Reader **0.5.0**, Media Worker **v3.2.0**.
- **Model catalog:** unchanged (**v2026.09.10**) — no model PRs in-window.
- **`@since` sweep:** three new anomaly groups (OI-1 groups 16–18, recorded not fixed): CRM JobNavigator batch tagged `@since 3.2.0` (9 files + init/pro code comments, #6636/#6640/#6641); the Shopify normalizers trait tagged `@since 1.1.82` (ahead of the shipping version, #6638); the financial batch tagged `@since 1.1.80` (one behind the 1.1.81 ship, 11 files, #6639). The FlowHub helper is correctly tagged `@since 1.1.81`.
- **Housekeeping executed this pass** — remove stale build ZIPs: all `build/**/*1.1.79*` (root 9 + optional-components 2 + toolkit-addons 19 = 30 files), superseded by the in-window 1.1.80 wp.org package builds. The 1.1.80 ZIP set is current, not stale. (Observed, not touched: `build/nvoos-docs-hub-v0.4.3/0.4.4/0.4.5.zip` predate the ZIP-cleanup convention — flagged for the user.)
- **Parked items stay parked** (OI-1/OI-2 in `docs-catch-up-open-items.md`): no `@since` reconciliation, no Docker live-count re-derivation. OI-2's "current figure" label refreshed to the v1.1.81 baseline.
- **Unrelated working-tree noise excluded** — untracked `docs/user-guides/assistants/CONTENT-GRAPH-MARKETING-ASSISTANT.md` (still untracked; excluded from every commit), root `vendor/`, `package-lock.json` are never staged.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — complete the existing `## [1.1.81]` section: update the date to 2026-09-17; keep the two in-window Added blocks (#6634/#6643); add Added (FlowHub Remote Sites resolution #6635; Shopify product image cards #6638; JobNavigator CRM adoption + 5 tools #6636; OpenTerminal financial resilience + 8 tools #6639; Gmail reply poller + digest recipe #6641), Fixed (FlowHub connection proxy #6637), Ports (CG Pro #6634/#6638/#6639/#6640), and the Versioning block (1.1.81; counts **~306 base + ~1,279 Pro (~1,585 total)**; catalog unchanged v2026.09.10; skill count **58**; stale 1.1.79 ZIPs removed (30 files)).
2. **`readme.txt`** — Stable tag 1.1.81; new `= 1.1.81 - September 17, 2026 =` changelog paragraph (one paragraph, wp.org style).
3. **`README.md`** — version header → 1.1.81; Release Date 2026-09-17; new `🆕 v1.1.81 Highlights` paragraph replacing the v1.1.80 one; new "What's New at a Glance (v1.1.81)" section above v1.1.80; new "Latest Updates (v1.1.81 — September 2026)" block above v1.1.80; count mentions → ~306/~1,279/~1,585 (highlights paragraph, overview lines 678/680, architecture line 750).
4. **`docs/QUICK_REFERENCE.md`** — version + last-updated → 1.1.81/September 17; v1.1.81 entry above v1.1.80.
5. **`docs/DOCUMENTATION_INDEX.md`** — Plugin Version + Last Updated → 1.1.81/September 17; new SEPTEMBER 17 UPDATE banner above the v1.1.80 one; new `## 🆕 September 2026 — v1.1.81` section header + "New and updated documents" block (incl. the two new plan docs: `docs/developer/crm-toolkit-jobnavigator-adoption-plan.md`, `docs/project/plans/financial-toolkit-openterminal-lessons-plan.md`).
6. **`docs/project/FOR_REVIEWERS.md`** — current version → 1.1.81 (September 2026); count mentions → ~1,585/~306/~1,279 (lines 25 + 38).

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed stamp + "New in v1.1.81" paragraph: **+13 Pro tools** — #6636 five CRM tools (`bulk_move_deal_stages`, `create_tracked_link`, `record_crm_reply`, `get_crm_handover`, `get_pipeline_digest`) and #6639 eight financial tools (`market_screener`, `macro_data_fetcher`, `economic_calendar_fetcher`, `earnings_calendar_fetcher`, `options_chain_fetcher`, `crypto_market_data`, `portfolio_transaction_log`, `price_alerts`) — new figures ~306 base + ~1,279 Pro (~1,585 total); #6634/#6635/#6637/#6638/#6641/#6643 add no tool slugs; Total tools line updated.
8. **`.context/security-checklist.md`** — v1.1.81 update line: #6639 keyless microservice auth (OpenTerminal lessons — token never logged/exported, per-provider fallback chains); #6643 multi-recipient email sanitize (shared `sanitize_email_recipients()`, per-address sanitize + dedupe at save and sanitize boundaries); #6635/#6637 FlowHub credential resolution + proxy egress via the Remote Sites connection; #6634 Shopify UCP keyless modes stay live-only (no caching per UCP guidelines).
9. **`.context/testing.md`** — last-reviewed + v1.1.81 note: Shopify UCP mode-awareness 20 tests + dual-matrix CG Pro; image cards 8/8 + both CG Pro matrices; CRM JobNavigator WP1–WP9 + 11 CG Pro characterization tests; financial suites (market-data, portfolio, price-alerts, resilience 657, technical-indicators 255); Gmail poller +113 adoption tests; FlowHub client/helper/tools suites; multi-email 115 tests.
10. **`.context/rest-api.md`** — v1.1.81 note: no new REST routes this window (all changes are tool/service-level; the UCP profile route is 1.1.80).
11. **`.context/README.md`, `.context/conventions.md`, `.context/pro-vs-base.md`** — count mentions → ~306/~1,279/~1,585.
12. **`CLAUDE.md`** — header stamp → September 17, 2026 · 2.34; v1.1.81 notes (JobNavigator CRM adoption + Gmail poller, OpenTerminal financial resilience, FlowHub connection resolver/proxy, Shopify UCP mode-aware routing + image cards, multi-recipient email); count mentions → ~306/~1,279/~1,585 (lines 20/53/57/189).
13. **`MAINTAINER_MAP.md`** — last-reviewed bump → September 17, 2026 (v1.1.81); count mentions → ~1,585/~306/~1,279 (lines 75-81, 101-105, 283-293).
14. **`.bmad/README.md`** — last-reviewed bump → v1.1.81.
15. **All three `mcp-ai-wpoos-plugin` SKILL.md copies** — `plugin-version`/`plugin-version-tested`/`last-updated` → 1.1.81/2026-09-17 (the `.agents/skills/` copy also gains a v1.1.81 note section above the v1.1.80 block).
16. **`.agents/skills/mcp-ai-wpoos-updates/SKILL.md`** — frontmatter → 1.1.81/2026-09-17; body references (distilled-plan range, template-plan names, commit-structure mirror, references list) → v1.1.81.
17. **Skill-count bookkeeping (AGENTS.md §6)** — verify-only: 58 skills unchanged (no new skills in-window).

### P2 — Operational & feature docs

18. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" stamp → September 17, 2026 (v1.1.81); addon rows verified unchanged (no sub-project bumps in-window).
19. **`docs/reference/tools/tool-reference.md`** — Last Updated → September 17, 2026 (v1.1.81); Tool Count → ~306 base + ~1,279 Pro (~1,585 total) — +13 Pro from #6636/#6639.
20. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-17 (v1.1.81 pass); Scope line gains v1.1.81; OI-1 gains groups 16–18 (`@since 3.2.0` CRM ×9, `@since 1.1.82` Shopify trait ×1, `@since 1.1.80` financial ×11); OI-2 current-figure label → v1.1.81 (~306 + ~1,279 = ~1,585; delta: #6636 +5 Pro, #6639 +8 Pro); new OI-5 (tool-status.txt has the 8 financial slugs but not the 5 CRM slugs).
21. **`docs/project/plans/v1.1.81-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

22. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.81 (`package-lock.json` intentionally untouched).

### Count sweep (executed this pass — counts changed)

23. Grep `1,266`, `1,572`, `~303` across live surfaces and reconcile to ~306/~1,279/~1,585: `docs/getting-started/USE_CASES_AND_QUICKSTARTS.md` (prose + Rev table 3.2 row), `docs/getting-started/_USE_CASES_FACT_SHEET.md` (reconciled rows + guidance). Historical per-version blocks, plans, proposals, and archives stay.

### Housekeeping (executed this pass)

24. **Remove stale build ZIPs** — `git rm` all `build/**/*1.1.79*` (root 9 + optional-components 2 + toolkit-addons 19 = 30 files), superseded by the in-window 1.1.80 wp.org package builds. Commit: `chore: remove stale 1.1.79 build ZIPs [skip ci]`.

### Verify-only (no changes expected)

- Skill count after bookkeeping equals the actual **58** directories under `.agents/skills/` (20 wp-* + 32 design-* + 6 mcp-ai-wpoos guides).
- `addons/pro/README.md` — toolkit count stays 31 (the 13 new tools register in the existing CRM + financial-planner toolkits; no new toolkit).
- `docs/project/ecosystem-port-tracker.md` — maintained by the port loop itself (#6639 F2–F6 financial row + #6640 F2-E CRM row landed in-window).
- `addons/docs-hub/CHANGELOG.md`, `plugins/nvoos-content-graph/CHANGELOG.md`, `addons/checkout-api/CHANGELOG.md` — own tracks; no sub-project bumps in-window.
- `docs/reference/models/*` — no model changes in-window.
- `docs/toolkits/flowhub-integration.md` — updated in-window by #6635 (+2 lines).
- `docs/reference/tools/tool-status.txt` — updated in-window by #6639 (+8 financial slugs); the 5 CRM slugs' absence is recorded as OI-5, not fixed in-pass.
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" description — base count 306 still satisfies "300+".

---

## 3. Execution log (2026-09-17)

- **Orientation** — window mapped from `876aa0a83e` (1.1.80 catch-up merge #6633) to `de6b19f835` (#6643 merge): 9 merged PRs (#6634–#6641, #6643; #6642 not merged in-window) + 6 build commits + 1 direct README commit (`e96121d574`). All PR descriptions read in full from the squash-merge commit bodies (GitHub API unavailable this pass — bad credentials). `@since` sweep on every added file found three new anomaly groups (recorded as OI-1 groups 16–18); the FlowHub helper is correctly tagged 1.1.81.
- **Commits 1–6** — version bump, release notes, agent context, reference surfaces, count sweep, stale-ZIP removal (see §4).
- **Post-pass sweeps** — `git grep "1\.1\.80"` + count sweeps executed after all commits (see §5).

## 4. Commit breakdown (mirror v1.1.58–v1.1.80)

1. `chore: bump version to 1.1.81` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.81 changelog and release notes` — `CHANGELOG.md` (complete the in-window [1.1.81] section; date → 2026-09-17; fold #6635–#6641; Versioning block), `readme.txt`, `README.md`, `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.81 changes` — `.context/*` (7 files), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies, `.agents/skills/mcp-ai-wpoos-updates/SKILL.md`
4. `docs: reconcile 1.1.81 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/plans/docs-catch-up-open-items.md`, `docs/project/plans/v1.1.81-docs-catch-up.md`
5. `docs: reconcile remaining 1.1.81 tool counts` — `docs/getting-started/USE_CASES_AND_QUICKSTARTS.md`, `docs/getting-started/_USE_CASES_FACT_SHEET.md` (counts changed this window)
6. `chore: remove stale 1.1.79 build ZIPs [skip ci]` — `build/**/*1.1.79*` (30 files: 9 root + 2 optional-components + 19 toolkit-addons)

## 5. Validation

- `php -l` on every bumped PHP file
- `package.json` JSON parse
- `git grep "1\.1\.80"` sweep — remaining hits must be intentional history (previous-release sections, `@since` docblocks, plans, archive)
- Count sweep — `git grep "1,572\|1,266\|~303"` reconciled on live surfaces; historical per-version blocks/plans/archive untouched
- Stale-ZIP sweep — `git ls-files 'build/*'` re-checked: no 1.1.79 files remain; the 1.1.80 set is current
- Skill-count verification — 58 directories under `.agents/skills/` == bookkeeping claims in AGENTS.md / copilot-instructions / README

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue #5968) and Docker-based live-count re-derivation (OI-2, issue #5967) stay parked. OI-2's current-figure label refreshed to the v1.1.81 baseline (~306 base + ~1,279 Pro, ~1,585 total; delta: #6636 +5 Pro, #6639 +8 Pro).
- **New items raised this pass:** OI-1 groups 16–18 (the `@since` sweep) and OI-5 (tool-status.txt missing the 5 new CRM slugs) — all recorded in the tracker, none fixed in-pass.
- **Noted observations (other workstreams):** the #6634/#6638/#6639/#6640 CG Pro ports are the ecosystem-port loop's own tracker concerns (F2–F6/F2-E rows updated in-window); `build/nvoos-docs-hub-v0.4.3/0.4.4/0.4.5.zip` predate the ZIP-cleanup convention (flagged, not removed); the in-window README commit removed the v1.1.79 highlights paragraph (top keeps only the latest release's paragraph — convention followed this pass).
- **Housekeeping executed this pass:** stale 1.1.79 build ZIPs removed (30 files).
