# Docs & Release Catch-Up Plan — v1.1.63 (PRs #5919–#5929)

> **Prepared:** 2026-08-23 · **Status:** ✅ **Executed & committed 2026-08-23** (all P0–P4 items complete; see execution log below)
> **Scope:** documentation/context updates + version bump to **1.1.63** (no production-code changes)
> **Input:** full review of every commit on `alpha-working` after the v1.1.62 catch-up merge (PR #5918, 2026-08-22 04:44)
> **Predecessor:** [`v1.1.62-docs-catch-up.md`](v1.1.62-docs-catch-up.md) (executed Aug 22; covered PRs #5914–#5917)

---

## 1. Context: What changed since the last docs catch-up

The Aug 22 v1.1.62 catch-up (PR #5918) covered everything through PR #5917. **Six PRs merged afterward** (plus two same-day revert/re-land pairs) that were absent from every release-note surface, except where the feature PRs self-documented:

| PR | Commit(s) | Change | Doc status after this pass |
|---|---|---|---|
| #5919 | `ecb117b2a8` | **OKF skill-knowledge conformance** — `type: Skill` frontmatter added to 91 bundled SKILL.md files (OKF v0.2 requirement), 4 missing cross-linked reference files restored (wp-plugin-dto, wp-plugin-presenter, wp-action-scheduler, mcp-builder), frontmatter on `wp-security-audit/reference.md`; `.agents/skills` mirror sync now recursive + covers all project-owned design skills — the OKF Validate tab no longer reports missing concepts | self-documented in-commit ✅ · changelog/readme ❌ |
| #5920 | `2de1eec011` | **Pro SPA v2 slash-command composer fix** — `/chat`, `/new`, `/clear`, `/copy`, `/export` now insert reliably from `SlashCommandsDrawer` (also opens from the AgentPanel button); base `assets/js/slash-commands.js` fixes | fix-history doc ✅ in-commit · changelog ❌ |
| #5923 | `655866b2f9` + 4 | **Artifact Evolution Phases A–G — Darwinian self-improvement for skills, prompts & roles** — `evolve_harness` ↔ Evolver contract repair (constructor normalization, `analyze_failures()`, component-scoped `evolve()` with enforced `dry_run`), opt-in evolved-prompt consumption (`WP_MCP_AI_Evolved_Prompt_Resolver`) + evolved-skill merge into the Skill Registry, Refiner output sanitization/PII scrubbing, evolution budget; artifact-scoped eval suites + failure-case replay + post-mutation verification gate; artifact population + parent selection; mutators + learning log; pre-commit admission gate (three critics); holdout-gated deployment with shadow A/B + drift rollback; Phase G governor (unified budget, rate limits, site-wide cap), human approval queue, lineage graph, artifact-governance metabox on the assistant screen. 52 files, +11,666/-75; every layer opt-in and default off | proposal 007 + implementation plan + `includes/{harness,agents,assistants,tools}/README.md` + EU AI Act governance section ✅ in-commit · changelog/README/QUICK_REFERENCE/context ❌ |
| #5924 | `6f63ef2f96` | **Pro Addons admin page with one-click install** — registry-driven **NV oOS Pro Dashboard → Addons** page (`WP_MCP_AI_Addons_Page`, 813 lines) installs/activates standalone addons whose ZIPs ship in `build/`; nonce + `install_plugins` + allowlist; non-WordPress components listed read-only; PHPUnit coverage | `ADDON_INVENTORY.md` installer block + ajax-handler inventory ✅ in-commit · changelog ❌ |
| #5925 | `d44b0810ac` | **Artifact evolution settings UI** — `WP_MCP_AI_Evolution_Settings_Bridge` (254 lines) + orchestration section wiring: the evolution opt-in switches are manageable from Settings → Orchestration Layer (priority-5 filter bridge, save-to-filter semantics, governor summary + reset) | harness/agents/tools READMEs ✅ in-commit · changelog ❌ |
| #5926 | `4bc7486c10` | **Fix DeepSeek 400 on empty tool schema properties** — schema normalization produced `"properties": []` which DeepSeek rejects ("[] is not of type 'object'"); empty maps now encode as `{}` at all three payload boundaries (REST, Tool Service, ChatOrchestrator); legacy tools wrapped **before** the first register attempt so the fail-loud registry log no longer fires for tools that register successfully via the wrapper | nothing ❌ |
| #5928 | `46b6de7061` + `402b38d69a` + `1ab249731b` | **Chat storage worker offload (proposal 032, v2)** — saves ≥ `wp_mcp_ai_storage_worker_threshold` (default 10,000 chars) offload `JSON.stringify` to the browser `storage-worker.js` via `wpMcpAiStorageUtil`; small saves/unload flushes/worker failures fall back to the sync main-thread write; per-call threshold override; `storageWorkerUrl`/`storageWorkerThreshold` localized on every chat surface; `wp_mcp_ai_storage_worker_threshold` filter (0 = off) is the kill switch; LLM worker manager URL resolution fixed. Proposal 032 implementation plan + addons/embedded impact assessment shipped | `CHANGELOG.md` `[Unreleased]` + proposal 032 plan + performance doc ✅ in-commit · release-date/versioning/PR numbers ❌ |
| #5929 | `4762abb4f1` + 20 | **Test-suite exit traps** — new `bin/sweep-tests.php` (parallel per-file sweep with report output), AJAX test contracts in `tests/bootstrap.php`, 24+ test files aligned with catchable-die/admin-handler contracts; production fixes kept minimal and honest: bare-exit guards in 4 admin handlers (`WP_MCP_AI_TESTS_RUNNING` test seams), duplicate CRM workflow-rule class renamed, toolkit MCP scope check + resource URIs fixed, `wp_mcp_ai_sse_poll_max_*` + Veo polling-limit filters, export-provider `log_action()` made public | `docs/developer/testing-docs/TEST-SUITE-REMAINING-FIXES-PLAN.md` ✅ in-commit · changelog ❌ |
| `0e8c2b2b1d` | Build-only: oOS WP.org packages v1.1.62 | no doc action |

PRs #5921 (proposal 032) and #5927 (worker offload) merged and were **reverted the same day** (`c17c179bea`, `c566128ad2`); their content re-landed via #5928. No doc action for the reverted merges.

### Drift found while preparing

- `.context/testing.md` still says "Last reviewed: March 2026" — untouched by the 1.1.58–1.1.62 passes; PR #5929 is a testing-infrastructure change, so this pass must stamp it and add the sweep/exit-trap notes.
- `docs/project/ADDON_INVENTORY.md` header says "Last Updated: August 14, 2026" — PR #5924 added the installer block in-commit but did not bump the stamp.
- `docs/features/performance/performance-improvements.md` says "Web Worker Integration (Wired as of 1.1.62)" under "Future Improvements" — the wiring merged after the 1.1.62 release, so the released version is 1.1.63.
- New `@since 1.9.0` docblocks on the #5923 harness classes — same anomaly class as the parked `@since 1.2.0` tags; **parked**, noted in open items.

### Key scope rules for this pass

- **Historical entries stay.** Old changelog/README per-version blocks (`[1.1.62]` and earlier) are history and are not rewritten, including their tool counts.
- **Tool counts are unchanged** this window: no tools were added or removed → **~303 base + ~1,249 Pro (~1,552 total)** with the live-registry caveat retained on every count surface.
- **Media Worker keeps its own version track** (v3.2.0, unchanged); nvoos-content-graph stays on 1.0.3.
- **Parked items stay parked** (per v1.1.59 open items + user decision): no `@since` tag reconciliation (now including the new `@since 1.9.0` tags), no Docker-based live-count re-derivation in this pass.
- **No new skills** in this window — coding-time skill count stays **52**. (#5919 touched SKILL.md frontmatter only.)
- **Unrelated working-tree noise excluded** — the `vendor/` composer/guzzle updates in the working tree are not part of any commit.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — rename `[Unreleased]` → `## [1.1.63] - 2026-08-23`; add PR numbers to the storage-worker entries; add new entries: Added (Artifact Evolution Phases A–G, PRs #5923/#5925), Added (Pro Addons admin page, PR #5924), Fixed (DeepSeek empty-properties 400, PR #5926), Fixed (OKF skill-knowledge conformance, PR #5919), Fixed (Pro SPA v2 slash-command composer, PR #5920), Fixed (test-suite exit traps, PR #5929); Versioning block (1.1.63, Pro 1.1.63, Media Worker v3.2.0 unchanged, nvoos-content-graph 1.0.3 unchanged, ~303 + ~1,249 (~1,552), 15 providers, 26 addons, 74+41 bundled skills, 52 coding-time skills).
2. **`readme.txt`** — Stable tag 1.1.63; new `= 1.1.63 - August 23, 2026 =` changelog paragraph (one paragraph, wp.org style).
3. **`README.md`** — version/release-date header → 1.1.63/2026-08-23; v1.1.63 Highlights paragraph; new "What's New at a Glance (v1.1.63)" section above v1.1.62; new "Latest Updates (v1.1.63 — August 2026)" section; tool-count mentions stay ~303/~1,249/~1,552 (verified correct).
4. **`docs/QUICK_REFERENCE.md`** — version + last-updated; v1.1.63 entry above v1.1.62.
5. **`docs/DOCUMENTATION_INDEX.md`** — Plugin Version + Last Updated → 1.1.63/August 23; new AUGUST 23 UPDATE banner; section header v1.1.55–v1.1.62 → v1.1.55–v1.1.63 (+ Artifact Evolution, Chat Web Workers, Addons Installer); new "New and updated documents (August 23, 2026, v1.1.63)" block (proposals 007 + 032, TEST-SUITE-REMAINING-FIXES-PLAN, EU_AI_ACT, tool reference, this plan, README/CHANGELOG/readme.txt/QUICK_REFERENCE).
6. **`docs/project/FOR_REVIEWERS.md`** — current version 1.1.63 (August 2026); tool counts already correct (~1,552) — verify only.

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed stamp; new "New in v1.1.63" note (no new tools; DeepSeek empty-properties normalization — `properties` must encode as `{}` never `[]`; legacy wrapper now wraps BEFORE the first register attempt so the fail-loud registry log only fires for genuinely unregisterable classes).
8. **`.context/security-checklist.md`** — v1.1.63 note: artifact evolution governance (every layer opt-in/default off; human approval queue; pre-commit admission gate; governor budget/rate/mutation caps; PII scrub before storage; shadow A/B never writes the deployed artifact); Addons page nonce + `install_plugins` + allowlist; bare-exit test-seam guards (`WP_MCP_AI_TESTS_RUNNING`) do not relax auth; never reintroduce `"properties": []` in tool schemas.
9. **`.context/testing.md`** — stamp the stale March 2026 review + v1.1.63 note: `bin/sweep-tests.php`, AJAX test contracts, catchable-die exit-trap guards, SSE/Veo polling-limit filters, `TEST-SUITE-REMAINING-FIXES-PLAN.md` pointer.
10. **`.context/chat-ui.md`** — extend the "localStorage Persistence" section with the storage-worker offload mechanics (v1.1.63): threshold default, `wpMcpAiStorageUtil`, `storageWorkerUrl`/`storageWorkerThreshold`, kill-switch filter, sync fallback.
11. **`CLAUDE.md`** — header stamp (August 23, 2026 · 2.16); five v1.1.63 notes (artifact evolution, Pro addons page, storage worker offload, DeepSeek schema fix, test-suite exit traps).
12. **`MAINTAINER_MAP.md`** — last-reviewed bump; tree rows: `includes/harness/` gains the artifact-evolution classes + evolution settings bridge; `addons/pro/includes/admin/` gains the Addons page.
13. **Both `mcp-ai-wpoos-plugin` SKILL.md copies** — `plugin-version`/`plugin-version-tested` stamps → 1.1.63; new "Artifact Evolution, Addons Page & Storage Worker (v1.1.63+)" section.
14. **`.bmad/README.md`** — last-reviewed bump → v1.1.63.

### P2 — Operational & feature docs

15. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" stamp → August 23, 2026; Pro row gains artifact evolution + addons installer mention (installer block already added in-commit by #5924).
16. **`docs/reference/tools/tool-reference.md`** — header "Last Updated" → August 23, 2026 (counts already correct; the `evolve_harness` contract change is documented in `.context/tool-registry.md` in-commit).
17. **`docs/features/meta-harness-auto-optimization.md`** — add a light pointer to proposal 007 (artifact evolution Phases A–G upgrade the 7-phase pipeline into a gated Darwinian loop).
18. **`docs/features/performance/performance-improvements.md`** — reconcile "Wired as of 1.1.62" → 1.1.63 (merged after the 1.1.62 release).
19. **`docs/project/plans/v1.1.63-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

20. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.63 (`package-lock.json` intentionally untouched).

### P4 — Housekeeping

21. Delete all `build/**/*1.1.62*` files (30 total: 6 root ZIPs + 3 `.sha256`, 2 optional-components, 19 toolkit-addons) — mirrors the stale-ZIP cleanup convention (`chore: remove stale 1.1.62 build ZIPs [skip ci]`).

### Verify-only (no changes expected)

- `AGENTS.md` / `.github/copilot-instructions.md` / README repo-table skill counts — unchanged (52; no new skills this window).
- `docs/project/proposals/RELATED_PROPOSALS_INDEX.md` — no 007/032 rows: the index is scoped to the extraction/Laravel/Graphify/media-worker threads and 029/030 set the precedent of indexing via DOCUMENTATION_INDEX instead.
- `docs/features/okf-integration.md` — #5919 is frontmatter/Validate-tab conformance only; no feature-doc change needed (changelog covers it).
- Folder READMEs (`includes/harness/`, `includes/agents/`, `includes/assistants/`, `includes/tools/`, `addons/pro/includes/admin/`) — updated in-commit by #5923/#5924/#5925; `composer run docs:check-folder-readmes` should stay green.
- `.context/media-worker.md` — no worker-side changes this window.
- `docs/operations/compliance/EU_AI_ACT_2026.md` — self-evolution governance section added in-commit by #5923; verify only.

---

## 3. Execution notes

- **Docs + version-bump-only PR**, matching the established catch-up pattern (`chore/v1.1.63-docs-catch-up`). No PHP/JS logic changes, so PHPUnit/JS suites are unaffected.
- **Version-bump rule:** bump every current-state `1.1.62` reference; **keep** historical ones (old changelog sections, `@since` docblocks, `docs/history/`, archive, per-version blocks). Verify with `git grep "1\.1\.62"` that every remaining hit is intentional history.
- **Validation:**
  - `php -l` on the three bumped PHP files.
  - `package.json` JSON parse.
  - `composer run docs:check-folder-readmes` — no folder READMEs are created/edited by this pass, should stay green.
  - `git grep -E "1,543|1,547|1,508|~265 base"` sweep — remaining hits must be historical per-version blocks, plans, or parked artifacts.
- ~20 files touched across `CHANGELOG.md`, `readme.txt`, `README.md`, `docs/`, `.context/`, `CLAUDE.md`, `MAINTAINER_MAP.md`, skills, plus the 4 version-bearing files and the build cleanup.

## 4. Commit breakdown (mirror v1.1.58–v1.1.62)

1. `chore: bump version to 1.1.63` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.63 changelog and release notes` — `CHANGELOG.md`, `readme.txt`, `README.md`, `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.63 changes` — `.context/tool-registry.md`, `.context/security-checklist.md`, `.context/testing.md`, `.context/chat-ui.md`, `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, both `mcp-ai-wpoos-plugin` SKILL.md copies
4. `docs: reconcile 1.1.63 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/features/meta-harness-auto-optimization.md`, `docs/features/performance/performance-improvements.md`, `docs/project/plans/v1.1.63-docs-catch-up.md`
5. `chore: remove stale 1.1.62 build ZIPs [skip ci]` — `build/**/*1.1.62*` (30 files)
6. `docs: reconcile remaining 1.1.63 tool counts` — `MAINTAINER_MAP.md` (How-Pro-Differs row), `.context/pro-vs-base.md` (file-tree line)

## 5. Execution log (2026-08-23)

- **P3 version bump:** `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.63 ✅ (`package-lock.json` intentionally untouched, matching 1.1.58–1.1.62 precedent)
- **P0 release surfaces:** `CHANGELOG.md` ([Unreleased] → [1.1.63] with PR numbers, six PR entries + Versioning), `readme.txt` (Stable tag 1.1.63 + 1.1.63 changelog paragraph), `README.md` (header/highlights/What's-New/Latest-Updates), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md` (banner + section header + Aug 23 block), `docs/project/FOR_REVIEWERS.md` ✅
- **P1 agent context:** `.context/tool-registry.md` (stamp + v1.1.63 registry-adjacent note), `.context/security-checklist.md` (v1.1.63 note), `.context/testing.md` (stale March 2026 stamp fixed + v1.1.63 sweep/exit-trap note), `.context/chat-ui.md` (storage-worker offload subsection), `CLAUDE.md` (header 2.16 stamp + 5 v1.1.63 notes), `MAINTAINER_MAP.md` (stamp + harness/addons tree rows), `.bmad/README.md` (stamp), both `mcp-ai-wpoos-plugin` SKILL.md copies (stamps + v1.1.63 section) ✅
- **P2 operational docs:** `docs/project/ADDON_INVENTORY.md` (Last Updated stamp + Pro row: artifact evolution + addons installer), `docs/reference/tools/tool-reference.md` (Last Updated), `docs/features/meta-harness-auto-optimization.md` (proposal 007 evolution pointer), `docs/features/performance/performance-improvements.md` ("Wired as of 1.1.62" → 1.1.63), this plan ✅
- **P4 housekeeping:** all `build/**/*1.1.62*` files (30 total: 6 root ZIPs + 3 `.sha256`, 2 optional-components, 19 toolkit-addons) removed in a dedicated `[skip ci]` commit, mirroring the stale-ZIP cleanup convention ✅
- **Verify-only items:** all confirmed no-change ✅ (`AGENTS.md`/copilot-instructions skill counts stay 52; `RELATED_PROPOSALS_INDEX.md` keeps 007/032 out per the 029/030 precedent; folder READMEs updated in-commit by #5923/#5924/#5925; EU AI Act section verified in-commit)
- **Tool counts:** no tools added/removed this window — counts stay ~303 base + ~1,249 Pro (~1,552 total) with the live-registry caveat retained on every count surface. Docker-based live-count re-derivation remains parked per the v1.1.59 decision ✅
- **Count-sweep stragglers (separate commit, mirrors v1.1.62 commit 5):** `MAINTAINER_MAP.md` "How Pro Differs" table row still said "~201 core tools / +~830+ Pro (~1,031+ total)" and `.context/pro-vs-base.md` file-tree line still said "~201 core tools" — both July-era counts that survived the 1.1.58–1.1.62 sweeps; reconciled to ~303/~1,249/~1,552. Sweep of `~1,025|~201|~830+|~195|~635|~960|~990|~1,005|~1,031` across current-state surfaces shows all other hits are historical per-version blocks ✅
- **Validation:** see section 3 (validation notes) — `php -l` on the three bumped PHP files ✅, `package.json` JSON parse ✅, `git grep` sweeps for the old version string and stale counts (remaining hits are intentional history) ✅

## 6. Open items for the next pass

- **Carried over (user-deferred):** `@since 1.2.0` docblock reconciliation and Docker-based live-count re-derivation stay parked until a later pass.
- **New this pass (parked, same class):** `@since 1.9.0` docblocks authored by PR #5923 on the new `includes/harness/class-wp-mcp-ai-artifact-*.php` classes (and the `evolve_harness` tool entry in `.context/tool-registry.md`) — add to the deferred `@since` reconciliation when the user revisits it.
