# Docs & Release Catch-Up Plan — v1.1.86 (PRs #6761–#6773)

## 1. Context: What changed since the v1.1.85 pass

The v1.1.85 catch-up merged as `9cb717da87` ("chore: 1.1.85 docs catch-up (PRs #6749–#6759) + stale ZIP removal (#6760)") on 2026-09-24. **8 PRs** (#6761, #6766–#6768, #6770–#6773), **1 direct commit**, and **7 build commits** landed on `alpha-working` afterward (2026-09-24T16:48 → 2026-09-25T00:49). Per user direction this window opens the **`[1.1.86]` line** (full version bump + new changelog section) and executes the **stale-ZIP housekeeping** (the 1.1.85 package set now exists).

### Production-touching PRs

| PR | Files | Change |
|---|---|---|
| #6761 | `addons/pro/includes/{class-wp-mcp-ai-pro-remote-sites-admin.php,class-wp-mcp-ai-pro-remote-site-manager.php,class-wp-mcp-ai-export-provider-remote-sites.php}`, `addons/pro/includes/mcp-apps/{class-wp-mcp-ai-mcp-app-registry.php,mcp-apps-init.php}`, `includes/assistants/class-wp-mcp-ai-assistant-portability.php`, `includes/assistants/metaboxes/class-wp-mcp-ai-metabox-mcp-apps.php`, new `tests/mcp-apps/test-mcp-app-connection-refs.php` + `addons/pro/tests/test-remote-site-manager-mcp-server.php`, `docs/assistant-import-export.md`, `docs/features/remote-sites.md`, proposal 041 | **MCP Apps as a Remote Sites connection type (Proposal 041).** New `mcp_server` Remote Sites connection type with encrypted central credentials (AES-256-CBC, incl. the `mcp_oauth` blob), real JSON-RPC handshake Test Connection, tool discovery with persisted snapshots, restricted-host enforcement, and activity logging; auth mapping `basic_auth`/`application_password` → MCP `basic`, `custom_header` → `header`, `bearer`, `oauth`. **Reference mode** — assistant entries can carry `connection_ref` pointing at a central connection, resolved decrypt-on-use at chat time (credentials never written back to post meta); missing refs skip with error-status snapshots; imported bundles with missing refs auto-disabled with a warning (`wp_mcp_ai_mcp_apps_validate_imported_refs`). **Portability hardening** — `_wp_mcp_ai_mcp_apps` `token`/`oauth_data` redacted on export and stripped on import (default on, opt-out filters), stored credentials preserved on overwrite, reference entries survive import as non-secret pointers. Metabox gains "Add from Remote Sites" (reference rows, no credentials in the DOM) + read-only "Managed in Remote Sites" rows. **New coding-time skill `design-elementor-mcp-connection`** (two connection paths) + updates to `design-ai-assistant-admin`, `design-elementor-template-kits`, and `mcp-ai-wpoos-assistant-portability` skills. Deferred (tracked in proposal 041): Phase 3.2 server-card discovery, Phase 3.3 full admin OAuth flow, Phase 4 adopt-into-Remote-Sites flow. |
| #6767 | `includes/tools/class-wp-mcp-ai-tool-get-environment-status.php`, `plugins/nvoos-content-graph-ai/src/Tools/GetEnvironmentStatusTool.php` (own track), new `tests/test-get-environment-status-tool.php` (9 tests) | **Environment status warnings + model data fixed.** `summarise_assistants()` wraps its payload under an `environment` key but `build_warnings()` read flat keys — the "no assistants published" warning always fired and the "default assistant could not be loaded" branch could never fire (both fixed by unwrapping before reading, base + CG port kept in sync). DeepSeek added to the provider key/label warning maps. New `plugin.default_provider_model` field resolves the effective model for the configured default provider (e.g. `deepseek_model`) instead of implying the OpenAI-only `default_model`; the default assistant entry now carries its `_wp_mcp_ai_provider`/`_wp_mcp_ai_model` meta. |
| #6768 | `includes/tools/class-wp-mcp-ai-tool-get-system-logs.php` (+343), `includes/class-wp-mcp-ai-logger.php` (+132), `includes/validators/arguments/class-get-system-logs-arguments.php` (new), `includes/tools/class-wp-mcp-ai-tool-get-system-logs-validated.php`, `plugins/nvoos-content-graph-ai/src/Tools/GetSystemLogs{,.php}*` (own track), tests | **`get_system_logs` becomes searchable.** New optional AND-combined filters `since` (relative "2h"/"30m"/"3d" or absolute ISO 8601 / `Y-m-d H:i:s` UTC; bare number = minutes; malformed → `wp_mcp_ai_invalid_since`), `levels` (critical/error/warning/notice/deprecated, matching NV oOS entry types + PHP log-line markers), and `search` (case-insensitive 200-char substring across messages/context and file log lines). Filters apply to the structured NV oOS buffers + file logs (debug.log, PHP error log, plugin logs); responses gain a filters summary (resolved UTC cutoff) + per-file `filtered_out` counts; timestamp-less lines (stack-trace continuations) kept conservatively under time filters. `parse_since()` is the public static canonical parser shared by the Symfony-validated variant (no drift). CG AI ports base-identical. |
| #6771 | `includes/class-wp-mcp-ai-rest.php` (+77), `assets/js/chat.js` (+ rebuilt min/bundle/dist files), new `tests/test-tool-cost-envelope.php` (6 tests) | **Tool costs reach the final response label.** New `build_tool_cost_envelope()` converts extracted usage into the top-level `toolResult.cost` shape (`cost_usd`, `is_estimated`, `provider`, `model`) the client already aggregates — attached in both the non-streaming agentic loop and the SSE streaming path (live `tool_execution` `tool_result` events now carry usage/cost). The client's shared `aggregateToolUsageBadgeData()` replaces the two duplicated loops, aggregates the cost envelope (falls back to nested `cost_usd`, then the legacy content-nested shape), and carries tool model/provider into the final badge (fixes the streaming drop); tool bubbles render the same usage/cost badges across append/SSE/async/restore paths. |
| #6772 | `includes/class-wp-mcp-ai-higgsfield-client.php` (new, 554), 4 new base tools (`includes/tools/class-wp-mcp-ai-tool-{generate-higgsfield-video,generate-higgsfield-image,check-higgsfield-request,cancel-higgsfield-request}.php`), `includes/class-wp-mcp-ai-tool-registry.php` (+4), `includes/bootstrap/oos-bridge.php` (+4 lib/core wrappers), `includes/tools/class-wp-mcp-ai-tool-check-video-status.php`, `includes/bootstrap/cron.php`, `includes/admin/{class-wp-mcp-ai-admin-settings-base.php,class-wp-mcp-ai-admin-settings.php,sections/class-wp-mcp-ai-section-providers.php}`, `lib/core/src/Tool/*Higgsfield*.php` (4 wrappers), `tests/test-higgsfield-*.php` (4 suites), proposal 042, `docs/tool-status.txt` +4 | **Higgsfield provider integration.** New base tools `generate_higgsfield_video` (five verified models — Cinema Studio 4.0, Seedance 2.5/2.0, Wan 3.0, Kling 3.0 — behind one `model` param with per-model payload mapping/clamping, image/video/audio reference inputs, cinematic controls), `generate_higgsfield_image` (SOUL V2 + SOUL Cinema workflows, batch 1|4, style_id gating), `check_higgsfield_request`, `cancel_higgsfield_request` (queued/in_progress/completed/failed/nsfw/canceled contract). Shared `WP_MCP_AI_Higgsfield_Client` (two-part `Key ID:SECRET` auth, submit/status/cancel lifecycle, backoff+jitter polling 2s→10s, immediate download against the 7-day retention; credential chain settings → env → constants). Provider settings section + connector definition + SettingsStore mapping + 480 s async-executor timeout override; `check_video_status` resolves `async_*` executor job IDs; `lib/core` dual-layer wrappers registered via `oos-bridge`. |
| #6773 | `addons/pro/includes/services/class-wp-mcp-ai-result-delivery-service.php` (+406), `addons/pro/includes/{class-wp-mcp-ai-pro-schedule-manager.php,class-wp-mcp-ai-pro-schedule-rest-controller.php}`, `addons/pro/assets/js/schedule-manager.js`, `tests/test-pro-result-delivery-{chat,email}-format.php` extended | **Action-items template + smart excerpts.** Scheduled digests no longer blind-trim the first 80 words (usually the roundup intro). New `action_items` delivery template (email + chat) sends only the actionable section ("Action Items", "Needs your attention", "Next steps", …) with graceful fallbacks; the `summary` template excerpt now prefers the response's own distillation section ("Summary", "TL;DR", "Key points", "Results"), then the action block, then centroid-ranked sentences (MEAD-style word-frequency centrality × positional decay), with a lead trim as final fallback. Shared extraction engine (`line_is_heading()` + `extract_section_block()`; `extract_action_items()` is a thin wrapper). Schedule UI dropdowns, sanitizer allowlists, and REST docs updated. |

### Docs-only PRs

- #6766 — docs-only: the 2026-09-24 Track C deferred-item sweep findings recorded as **OI-6** in the open-items tracker (ICP/PM pinned-bug drift re-verified; nothing closed).

### Release-sync PR

- #6770 — `main` ← `alpha-working` squashed sync (tree-identical to `alpha-working` at merge; the only dropped files are the stale 1.1.81/1.1.82 build ZIPs `alpha-working` had already removed). No production code change — classified build/release-only.

### Direct commits (no PR)

- `667754f2b3` — "Fix restricted-host wording in Elementor MCP skill" (`.agents/skills/design-elementor-mcp-connection/SKILL.md` wording fix, 3 lines).

### Build-only (7 commits)

wp.org packages v1.1.85 ×3, nvoos-content-graph-ai plugin ZIP v1.0.4 ×2, compiled assets + distributions ×1 (includes the `mcp-ai-wpoos-1.1.85.zip`/`-base`/`-pro` set + rebuilt addon ZIPS + a 1-line `blueprints/ollama-demo.json` change) — all `[skip ci]`.

### Key scope rules for this pass

- **`[1.1.86]` is the new current release** — new changelog section at the top; `[1.1.85]` and earlier stay untouched.
- **Counts change in-window:** +4 base tools (the Higgsfield quartet, #6772 — registry +4; the four `lib/core` wrappers are the framework-agnostic core registry's own tools via `oos-bridge` and are **not** counted in the base/Pro totals). Tool count moves **~308 → ~312 base + ~1,282 Pro (~1,590 → ~1,594 total)**. Coding-time skills move **59 → 60** (new `design-elementor-mcp-connection`, #6761). Bundled skills stay **75 base + 41 Pro** (no bundled copies in-window). Providers stay **15** (Higgsfield is a media-generation provider, not a model-catalog chat provider; the catalog stays **v2026.09.22**). Addons stay **27**.
- **Skill-count bookkeeping (A5):** #6761 added the `design-elementor-mcp-connection` skill without the AGENTS.md §1 / copilot-instructions / README repo-map count updates — this pass folds the 59 → 60 bookkeeping into the release surfaces.
- **Sub-project tracks:** nvoos-content-graph-ai stays **1.0.4** (ZIP rebuilt in-window, version unchanged); its `GetEnvironmentStatusTool`/`GetSystemLogsTool`/`GetSystemLogsValidatedTool` ports changed in #6767/#6768 on its own track (flagged, not edited). All other sub-project tracks unchanged (Media Worker v3.2.0, content-graph 1.0.8, content-graph-ai-platform 2.0.0, content-graph-pro 1.0.0, Checkout API 0.1.2, Docs Hub 0.5.1, Comic Reader 0.5.0).
- **README 12-entry window (per #6752's codified rule):** the new `### v1.1.86` entry joins Release History and the oldest detailed entry (`### v1.1.74`) is demoted to a one-line row in the Previous Releases table; the Overview keeps a single current-release "What's New at a Glance" block.
- **Housekeeping EXECUTED this pass (user-directed):** remove the stale **1.1.84 wp.org package set** (6 files: 3 ZIPs + 3 `.sha256` in `build/`). The 1.1.85 package set is the current release artifact set.
- **Parked items stay parked** (OI-1/OI-2). **New OI-1 groups 26–28** recorded from the window `@since` sweep: group 26 — `@since 1.1.85` ×15 (13 production across 4 files + 2 test files, PRs #6761); group 27 — `@since 1.1.0` ×5 (4 Higgsfield tools + `includes/class-wp-mcp-ai-rest.php`, PRs #6771/#6772); group 28 — `@since 1.9.0` ×2 (`includes/class-wp-mcp-ai-logger.php`, PR #6768).
- **Unrelated working-tree noise excluded** from every commit.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.86] - 2026-09-25` section: Added subsections for the MCP Server Remote Sites wave (#6761), the Higgsfield provider toolkit (#6772), and the action-items/smart-excerpt delivery (#6773); a Fixed subsection for the environment-status (#6767) and tool-cost-label (#6771) work; an Added subsection for the `get_system_logs` filters (#6768); a Docs subsection for proposal 041/042 + the new skill (#6761/#6772). Versioning block: bump to 1.1.86; tool count ~312 base + ~1,282 Pro (~1,594 total; +4 base Higgsfield); coding-time skills 59 → 60 (`design-elementor-mcp-connection`); bundled skills 75+41; providers 15 (Higgsfield is media, not chat); model catalog v2026.09.22; addons 27; sub-project tracks unchanged; stale 1.1.84 ZIP set removed (6 files).
2. **`readme.txt`** — `Stable tag: 1.1.86`; new `= 1.1.86 - September 25, 2026 =` changelog paragraph above the 1.1.85 entry.
3. **`README.md`** — `**Version:** 1.1.86` / `**Release Date:** 2026-09-25`; new v1.1.86 Highlights paragraph replaces the v1.1.85 one; "What's New at a Glance" heading → (v1.1.86) with new bullets; Release History gains `### v1.1.86 — September 25, 2026` (+ `#### Title` + bullets + versioning line) at the top; **demote `### v1.1.74`** to the first row of the Previous Releases table; the evergreen architecture bullet (~1,590 tool classes) → ~1,594; the repo-map `.agents/skills/` row 59 → 60 (33 design-* skills).
4. **`docs/QUICK_REFERENCE.md`** — `**Version:** 1.1.86` / `**Last Updated:** September 25, 2026`; new v1.1.86 recent-updates entry above the v1.1.85 one; plan link gains this plan.
5. **`docs/DOCUMENTATION_INDEX.md`** — `**Plugin Version:** 1.1.86` / `**Last Updated:** September 25, 2026`; new SEPTEMBER 25, 2026 banner above the SEPTEMBER 24 one (updated-docs list: this plan, README/CHANGELOG/readme.txt/QUICK_REFERENCE + the in-window `docs/assistant-import-export.md`, `docs/features/remote-sites.md`, `docs/tool-status.txt`, and proposal 041/042).
6. **`docs/project/FOR_REVIEWERS.md`** — "Current version" → 1.1.86; count lines +4 base.

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed → September 25, 2026 (v1.1.86); new v1.1.86 paragraph: #6772 registers **four new base tools** (the Higgsfield quartet; the `lib/core` oos-bridge wrappers are the core registry's own tools, not counted); #6761/#6767/#6768/#6771/#6773 add no new slugs (connection-type, tool-level params/fixes, REST/chat response-shape, delivery-template-level). "Total tools" line moves to ~1,594.
8. **`.context/security-checklist.md`** — last-reviewed + v1.1.86 line: #6761 central MCP Server credentials AES-256-CBC encrypted, decrypt-on-use never written back to post meta; `token`/`oauth_data` redacted on export + stripped on import (opt-out filters); imported bundles with missing refs auto-disabled; #6772 Higgsfield two-part `Key ID:SECRET` credential chain (settings → env → constants), dotted model IDs sanitized with `sanitize_text_field` not `sanitize_key`.
9. **`.context/testing.md`** — last-reviewed + v1.1.86 note: new suites `test-mcp-app-connection-refs.php` + `test-remote-site-manager-mcp-server.php` (#6761, 93 tests WP 6.9 / 73 core on WP 7.1), `test-get-environment-status-tool.php` (9/9 both versions), `test-tool-cost-envelope.php` (6), the Higgsfield cluster (4 suites; registry coverage 56/56 + regression 121/121), and the `get_system_logs` filter suites (28/28 + 66/66 Logger + CG AI ecosystem 3/3) — all validated on WP 6.9 + 7.1.
10. **`.context/rest-api.md`** — verify-only: no new base REST routes in-window (#6761 reuses the existing MCP Apps Test Connection surface; #6771 is response-shape-level; #6768 is tool-schema-level; #6773 documents the `action_items` template on the existing Pro schedule REST docs). Stamp untouched.
11. **`CLAUDE.md`** — stamp → September 25, 2026 · 2.40; count lines ~1,594 (~312 base + ~1,282 Pro); new v1.1.86 bullet block (MCP Server remote-sites refs + portability hardening, Higgsfield provider +4 base, get_system_logs filters, cost labels, action-items excerpts, environment-status fixes).
12. **`MAINTAINER_MAP.md`** — last-reviewed → September 25, 2026 (v1.1.86).
13. **`.bmad/README.md`** — last-reviewed → September 2026 (v1.1.86).
14. **Three `mcp-ai-wpoos-plugin` SKILL.md copies** (`.agents/skills/`, `includes/bundled-skills/`, `plugins/nvoos-content-graph-ai-platform/data/bundled-skills/`) — `plugin-version`/`plugin-version-tested` → 1.1.86; the `.agents/skills/` copy's note section gains a v1.1.86 line.
15. **`.agents/skills/mcp-ai-wpoos-updates/SKILL.md`** — references: latest executed plan → `v1.1.86-docs-catch-up.md`; template list gains it; new scope-rule lines learned this window (media providers don't count in the provider figure; lib/core dual-layer wrappers don't count in base/Pro totals; release-sync PRs are build-classified; A5: the introducing PR may skip the count bookkeeping — fold it into the release).
16. **`AGENTS.md` + `.github/copilot-instructions.md`** — coding-time skill count 59 → 60 (design-* 32 → 33 + the `design-elementor-mcp-connection` guide in the inventory row + repo-tree comment).

### P2 — Operational & feature docs

17. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" → September 25, 2026 (v1.1.86); no addon version changes in-window (content-graph-ai ZIP rebuilt at 1.0.4, no bump).
18. **`docs/reference/tools/tool-reference.md`** — "Last Updated" → September 25, 2026 (v1.1.86 — Higgsfield toolkit + get_system_logs filters); Tool Count line gains the "+4 base in v1.1.86 — the Higgsfield quartet" clause; "Status" stamp.
19. **`docs/project/proposals/README.md`** — proposal 042 (Higgsfield) merged in-window with no index entry: add a "Recently Completed" bullet pointing at `042-higgsfield-video-provider-integration.md`; 041 stays In Progress (Phases 0–2 done per #6761).
20. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-25 (v1.1.86 pass); **OI-1 groups 26–28 added** (`@since 1.1.85` ×15, `@since 1.1.0` ×5, `@since 1.9.0` ×2 — see §1); OI-2 current-figure label → v1.1.86 (~312 base + ~1,282 Pro, ~1,594 total — +4 Higgsfield); scope line extended through v1.1.86.
21. **`docs/project/plans/v1.1.86-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

22. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` (version) — 1.1.85 → 1.1.86. **Never** `package-lock.json`.

### Housekeeping (user-directed — executed this pass)

23. **Stale build ZIPs** — delete the 1.1.84 wp.org package set (6 files: 3 ZIPs + 3 `.sha256` in `build/`). Commit: `chore: remove stale 1.1.84 build ZIPs [skip ci]`.

### Verify-only (no changes expected)

- Skill counts: 60 dirs under `.agents/skills/` (new `design-elementor-mcp-connection`); 75 bundled base + 41 Pro (73 dirs + `LICENSE.txt` + `THIRD_PARTY_NOTICES.md` accounting — unchanged).
- `addons/pro/README.md` — toolkit count stays 31.
- `docs/project/ecosystem-port-tracker.md` — no port-loop PRs in-window.
- Sub-project changelogs — docs-hub/others unchanged in-window (content-graph-ai ports changed files, not its changelog — own track, flagged in the plan only).
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" description — base count 312 still satisfies "300+".
- Model suites — no catalog changes in-window; model-track files untouched (catalog v2026.09.22).
- `tool-status.txt` — +4 Higgsfield slugs added in-window by #6772; OI-5 (missing CRM slugs) stays parked.
- Proposals: 040 status unchanged (Phases 0–2 done, cost/docs deferred); 041/042 handled in §2.19.

---

## 3. Execution log (2026-09-25)

- **Orientation** — window mapped from `9cb717da87` (v1.1.85 catch-up merge #6760) to `f56e79b8c1` (origin/alpha-working HEAD): 8 merged PRs (#6761, #6766–#6768, #6770–#6773) + 1 direct commit (`667754f2b3`) + 7 build commits. All PR descriptions read in full via `gh pr list` (`gh` CLI per the updates skill; `merged:>=2026-09-24` for the merged set, targeted searches for the direct commits). `@since` sweep on the window diff found the new OI-1 groups 26–28 records in §2.20.
- **Claims verified against the tree** — registry +4 (the Higgsfield quartet in `includes/class-wp-mcp-ai-tool-registry.php`); `lib/core` wrappers registered in `includes/bootstrap/oos-bridge.php` (core registry, not counted); catalog read directly (version `2026.09.22`, unchanged; 17 catalog providers incl. decision/special entries — the "15 chat providers" figure unchanged, and Higgsfield is a media-generation provider outside the catalog); `.agents/skills/` = 60 dirs (new `design-elementor-mcp-connection`, #6761 — count surfaces still said 59, so the A5 bookkeeping folded into this release); `includes/bundled-skills/` = 73 dirs + 2 files (75 accounting, unchanged); version constants confirmed at 1.1.85 pre-bump; `build/` 1.1.84 set inventoried (6 files: 3 ZIPs + 3 `.sha256`; `find build -name "*1.1.84*"` — no 1.1.84 artifacts in `build/optional-components/` or `build/toolkit-addons/`).
- **Commits 1–6** — version bump, release notes, agent context (+ skill-count bookkeeping), reference surfaces, count sweep, stale-ZIP removal (see §4); all commits landed on `chore/1.1.86-docs-catch-up` (per A7 — `alpha-working` untouched, reset to origin after the bump commit moved to the branch).
- **Post-pass sweeps** — `git grep "1\.1\.85"` + count sweeps executed after all commits (see §5).

## 4. Commit breakdown (mirror v1.1.58–v1.1.85)

1. `chore: bump version to 1.1.86` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.86 changelog and release notes` — `CHANGELOG.md` (new [1.1.86] section), `readme.txt`, `README.md` (highlights + What's New + Release History entry + v1.1.74 demotion + architecture-count line + repo-map skills row), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.86 changes` — `.context/*` (tool-registry, security-checklist, testing), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies, `.agents/skills/mcp-ai-wpoos-updates/SKILL.md`, `AGENTS.md` + `.github/copilot-instructions.md` (skill-count 59 → 60 bookkeeping)
4. `docs: reconcile 1.1.86 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/proposals/README.md` (042 entry), `docs/project/plans/docs-catch-up-open-items.md` (OI-1 groups 26–28 + OI-2 label), `docs/project/plans/v1.1.86-docs-catch-up.md`
5. `docs: sweep 1.1.86 tool counts` — current-release count surfaces ~308→~312 / ~1,590→~1,594 (grep-verified; historical per-version blocks, plans, and proposals untouched)
6. `chore: remove stale 1.1.84 build ZIPs [skip ci]` — `build/nvdigital-open-operator-system-oos-1.1.84.zip` + `.sha256`, `-complete-1.1.84.zip` + `.sha256`, `-pro-1.1.84.zip` + `.sha256`

## 5. Validation

- `php -l` on every bumped PHP file; JSON-parse `package.json`.
- `git grep "1\.1\.85"` sweep on current-release surfaces — remaining hits must be intentional history only (the `[1.1.85]` changelog block, per-version README/readme.txt blocks, `@since` docblocks, plans, archive).
- Count sweep — current-release surfaces move to `~312`/`~1,594`/`60`; `git grep` the old labels (`~308`, `~1,590`, `59 coding-time`) only inside history blocks/plans/archive.
- README anchor check — `python3 bin/validate-readme-anchors.py` after the README edits; Release History must hold exactly the 12 most recent releases (v1.1.86 → v1.1.75) and the v1.1.74 row must be first in the Previous Releases table.
- Stale-ZIP sweep — `find build -name "*1.1.84*"` returns nothing; the 1.1.85 set and the non-versioned 1.0.0/SPA/toolkit zips remain.
- Sub-project files untouched by this pass (own tracks).

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue #5968) and Docker-based live-count re-derivation (OI-2, issue #5967) stay parked. OI-2's current-figure label refreshed to the v1.1.86 baseline (~312 base + ~1,282 Pro, ~1,594 total — +4 Higgsfield).
- **New items raised this pass:** OI-1 **groups 26–28** (`@since 1.1.85` ×15 / `@since 1.1.0` ×5 / `@since 1.9.0` ×2). Recorded in the tracker, not fixed.
- **Noted observations (other workstreams):** #6761's deferred items (server-card discovery, admin OAuth flow, adopt-into-Remote-Sites) are tracked inside proposal 041; #6772 recommends a post-merge live smoke test against a funded Higgsfield account (recorded in the PR); OI-5/OI-6 stay with their owners.
