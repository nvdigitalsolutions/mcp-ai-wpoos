# Docs & Release Catch-Up Plan — v1.1.64 (PRs #5931–#5965)

> **Prepared:** 2026-08-26 · **Status:** ✅ **Executed & committed 2026-08-26** (all P0–P4 items complete; see execution log below)
> **Scope:** documentation/context updates + version bump to **1.1.64** (no production-code changes)
> **Input:** full review of every commit on `alpha-working` from 2026-08-24 through 2026-08-26 (after the v1.1.63 catch-up merge)
> **Predecessor:** [`v1.1.63-docs-catch-up.md`](v1.1.63-docs-catch-up.md) (executed Aug 23; covered PRs #5919–#5929)

---

## 1. Context: What changed since the last docs catch-up

The v1.1.63 catch-up (PR #5930, merged 2026-08-24 00:22 +0530) covered everything through PR #5929. **Eighteen PRs merged afterward** that were absent from every release-note surface, except where the feature PRs self-documented:

| PR | Commit(s) | Change | Doc status after this pass |
|---|---|---|---|
| #5931 | `21dd249d17` + ~30 | **Test-env registration fixes** — Load Pro admin classes in AJAX suites directly, harden AJAX dispatch against leaked state, WordPress.com staging APIs, WP All Import no longer kills the test bootstrap, alignment across vault/phase5/token/peer-sync/submenu/stripe/regulatory/remote-site suites | changelog + `.context/testing.md` note ✅ · changelog-only for the test mechanics |
| #5932 | `26745b9b4b` + `e8cf3b4d7d` | **Composio Connect Link auth config resolution** — the link endpoint needs a project auth config ID, not a toolkit slug; resolve toolkit auth config (prefer Composio-managed default), send `auth_config_id`/`user_id`/`callback_url`; real 401/403 error surfaces, API-key trim/validate on save, egress guidance | changelog ✅ (folded into the #5932–#5934 block) |
| #5933 | `25210bae06` | **Composio connected-accounts listing fix** — unwrap v3.1 `{ items: [...] }` envelope, map legacy filters to `toolkit_slugs`/`statuses`/`user_ids`, render nested toolkit slug; Connected Apps list on the Remote Sites edit form (nonce-gated cache refresh); `clear_accounts_cache()` replaces hardcoded flushes | changelog ✅ |
| #5934 | `2a19de927a` | **Composio app removal & execute identity** — `from_connection()` identity binding (shared connections failed with "User ID is required with connected account"), per-account owner preference; Identity/Actions columns + nonce-gated Remove that deletes upstream + revokes the provider grant; auto-resolution skips non-ACTIVE accounts | changelog ✅ |
| #5935 | `51411ff26f` | **Test-suite follow-ups post-#5931** — point moved-file `require`s at current paths; workflow + install-script updates | changelog ✅ (folded into the test-env entry) |
| #5936 | `5c5eedc3db` | **Composio verified account health & real tool discovery** — `WP_MCP_AI_Composio_Account_Health` ledger + live probe engine, new `composio_manage_accounts` tool (7th Composio tool), in-band failure conversion, envelope unwrapping, curated Google Calendar probe, Health column with Verify/Reconnect actions | CHANGELOG `[Unreleased]` + composio docs + folder READMEs + 3 test suites ✅ in-commit · release-date/versioning/PR numbers ❌ |
| #5952 | `be2a084efe` + 3 | **Log-context bloat** — per-entry context budget on the persistence path (fingerprint `assistant_config`/`system_prompt`, truncate, drop largest values, 12 diagnostic keys preserved), Extended budget 16→8 KB, `previous_results` no longer embedded (O(N²) fix), Data Management Compact/Delete buttons via new `wp_mcp_ai_maintain_log_buffers` AJAX, MCP JSON-RPC `-32700`/`-32600` error paths reachable again, diagnostics page re-wired | nothing ❌ |
| #5953 | `62c0234d05` | **Composio proxied provider failures** — provider refusals proxied inside `data` are now authoritative failures (401/403 → auth-required + reconnect machinery), bounded execution-error path walk, anchored status scan, stale `connected_account_id` fast-fail | CHANGELOG `[Unreleased]` + composio docs ✅ in-commit · PR numbers ❌ |
| #5954 | `5cfac629f1` | **Secret URL query-param redaction** — 26 credential-bearing parameter names masked inside any logged string in the shared redactor (scheme/host/path/non-secret params preserved); ships the Google Calendar implementation plan doc | nothing ❌ |
| #5957 | `ac42874eae` | **REST controller restore + MCP suites as real clients** — `/mcp` `jsonrpc`/`method` no longer `required` so the JSON-RPC error envelope is reachable; MCP Server Diagnostics page/assets/AJAX re-wired after the entry-file rename orphaned its `init()` | nothing ❌ |
| #5958 | `fb666a16c3` | **Composio zero-argument payload & probe determinism** — empty `arguments` encode as `{}` (not `[]`), `stringify_error_detail()` flattens validation `detail` lists, bounded `upstream` body on `WP_Error`, cache-index invalidation for filtered listings, `flush_cache()` fixed, deterministic probe ranking + `CURATED_PROBE_TOOLS` | CHANGELOG `[Unreleased]` ✅ in-commit · PR numbers ❌ |
| #5959 | `22bccffa0f` | **Google Calendar connection & shared Google services** — new `includes/google/` foundation (OAuth service, Calendar v3 client, scope registry, credential resolver, sync + push) replacing four drifted OAuth copies; `google_calendar` connection on both connection surfaces; **6 new Pro tools** in `addons/pro/includes/tools/google-workspace/` (list calendars/events, update, delete, check availability, quick add) + reworked `create_google_calendar_event` + real `sync_google_calendar` | `includes/google/README.md`, google-workspace README, `docs/developer/architecture/integrations/google-calendar-connection.md`, `docs/reference/google-calendar-api-v3.md`, tool-reference entries, tests ✅ in-commit · changelog/readme/counts ❌ |
| #5960 | `df2d14249a` | **Validated-tool argument validation restored on Symfony 5.4** — `enableAttributeMapping` guard never fired on 5.4, so constraint loading was silently skipped and every validated tool accepted invalid input; version-appropriate helper + `WPCapability` constructor fix | nothing ❌ |
| #5961 | `33ceb9ba64` | **Tool-declared non-loggable result fields** — new `WP_MCP_AI_Tool_Sensitive_Result_Interface` (`get_sensitive_result_fields()` dot-notation paths, wildcards, subtree masking); logger masks before preview truncation; legacy wrapper forwards it; additive-only filter; 8 declarers | `.context/tool-registry.md` section ✅ in-commit · changelog ❌ |
| #5962 | `b97a53f4a5` | **Pro SPA v2 conversation & assistant sync** — `selectSession` carries `assistant_id`, stored session hydrates on mount, assistant switch starts a new conversation | nothing ❌ |
| #5963 | `0e4f3cc799` | **Content Graph wp.org listing assets** — banner/icon/screenshot assets under `plugins/nvoos-content-graph/.wordpress-org/` | nothing ❌ |
| #5964 | `1be6f366ba` | **Vision tools timeout parameter** — `analyze_image`/`extract_image_text`/`generate_image_alt_text`/`generate_image_caption` (validated + legacy) gain a 5–300s `timeout` arg and inherit global `request_timeout` via new `WP_MCP_AI_Vision_Request_Timeout` trait; self-hosted OCR client download/health paths covered (cURL error 28 on large images) | nothing ❌ |
| #5965 | `bc0f963932` + `704bba036d` | **WP_Error envelope drift & cron/memory/elementor tool bugs** — vault/cron/memory/system-logs tests aligned to the canonical envelope; `plan_schedules_from_workflow` regex parsing bug; double `wpdb::prepare()` in Elementor form-submissions total; both `create_cron_job` tools return tracked `job_id`; prune cron jobs missing `first_timestamp`; `TranscribeOpenAIAudioArguments` `model` type constraint | nothing ❌ |

Plus build-only commits (compiled assets, `nvoos-content-graph` ZIP v1.0.3) which need no doc action. PRs #5937–#5951 do not exist on this branch (other streams), so the gap is not missing coverage.

### Drift found while preparing

- `.context/settings-storage.md` still says "Last reviewed: July 2026" — PR #5952 changed how the rolling log-buffer options are written/maintained, so this pass stamps it and adds the compaction note.
- New `@since 1.0.0` docblocks on the #5959 `includes/google/*` classes — same anomaly class as the parked `@since 1.2.0` / `@since 1.9.0` tags; **parked**, noted in open items.
- `.context/tool-registry.md` gained the "Non-Loggable Result Fields" section in-commit (#5961) but its last-reviewed stamp and counts still say v1.1.63/~1,552.

### Key scope rules for this pass

- **Historical entries stay.** Old changelog/README per-version blocks (`[1.1.63]` and earlier) are history and are not rewritten, including their tool counts.
- **Tool counts shift by +7 Pro** (PR #5959: 6 google-workspace tools; PR #5936: 1 `composio_manage_accounts`): **~303 base + ~1,256 Pro (~1,559 total)** with the live-registry caveat retained on every count surface. No base tools added or removed.
- **Media Worker keeps its own version track** (v3.2.0, unchanged); nvoos-content-graph stays on 1.0.3.
- **Parked items stay parked** (per v1.1.59/v1.1.63 open items + user decision): no `@since` tag reconciliation (now including the new `@since 1.0.0` tags in `includes/google/`), no Docker-based live-count re-derivation in this pass.
- **No new skills** in this window — coding-time skill count stays **52**.
- **Unrelated working-tree noise excluded** — untracked `nul`, `page.html`, `page2.html` are not part of any commit.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — rename `[Unreleased]` → `## [1.1.64] - 2026-08-26`; add PR numbers to the in-commit Composio entries (#5936/#5953/#5958) and reorder Added → Changed → Fixed; add new entries: Added (Google Calendar connection & shared Google services, PR #5959), Added (tool-declared non-loggable result fields, PR #5961), Added (Content Graph wp.org assets, PR #5963), Changed (vision timeout parameter, PR #5964), Fixed (Composio auth config/listing/app-removal & execute identity, PRs #5932–#5934), Fixed (log-context bloat, PR #5952), Fixed (secret URL query-param redaction, PR #5954), Fixed (MCP JSON-RPC error paths & diagnostics re-wiring, PR #5957), Fixed (validated-tool validation restored on Symfony 5.4, PR #5960), Fixed (Pro SPA v2 conversation/assistant sync, PR #5962), Fixed (WP_Error envelope drift & cron/memory/elementor bugs, PR #5965), Fixed (test-env registration + follow-ups, PRs #5931/#5935); Versioning block (1.1.64, Pro 1.1.64, Media Worker v3.2.0 unchanged, nvoos-content-graph 1.0.3 unchanged, ~303 + ~1,256 (~1,559), 15 providers, 26 addons, 74+41 bundled skills, 52 coding-time skills).
2. **`readme.txt`** — Stable tag 1.1.64; new `= 1.1.64 - August 26, 2026 =` changelog paragraph (one paragraph, wp.org style).
3. **`README.md`** — version/release-date header → 1.1.64/2026-08-26; v1.1.64 Highlights paragraph; new "What's New at a Glance (v1.1.64)" section above v1.1.63; new "Latest Updates (v1.1.64 — August 2026)" section; tool-count mentions ~303/~1,249/~1,552 → ~303/~1,256/~1,559 (current-state only).
4. **`docs/QUICK_REFERENCE.md`** — version + last-updated; v1.1.64 entry above v1.1.63.
5. **`docs/DOCUMENTATION_INDEX.md`** — Plugin Version + Last Updated → 1.1.64/August 26; new AUGUST 26 UPDATE banner; section header v1.1.55–v1.1.63 → v1.1.55–v1.1.64 (+ Google Calendar Connection, Composio Account Health, Log Redaction & Compaction, Non-Loggable Fields, Vision Timeouts); new "New and updated documents (August 26, 2026, v1.1.64)" block (google-calendar docs ×3, tool reference, this plan, README/CHANGELOG/readme.txt/QUICK_REFERENCE).
6. **`docs/project/FOR_REVIEWERS.md`** — current version 1.1.64 (August 2026); total tools ~1,559 (~303 base + ~1,256 Pro).

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed stamp → August 26 (v1.1.64); counts → ~1,559 (~303 + ~1,256); new "New in v1.1.64" note: 6 google-workspace Pro tools + `composio_manage_accounts`, shared `includes/google/` foundation (scope enforcement), non-loggable-fields section landed in-commit, `evolve_harness`/registry unaffected otherwise.
8. **`.context/security-checklist.md`** — v1.1.64 note: URL query-param redaction (26 names, shared redactor), sensitive-result-fields interface (masking is logging-only, additive-only filter), log-buffer budgets (persistence path only; `wp_mcp_ai_log_entry` still receives full context), Composio health engine (probe verdicts, remove revokes upstream grant), Google OAuth scope enforcement (`WP_MCP_AI_Google_Calendar_Scopes`).
9. **`.context/settings-storage.md`** — stamp the stale July 2026 review; v1.1.64 note: rolling log buffers (`wp_mcp_ai_recent_errors`/`wp_mcp_ai_recent_activity`) now cap per-entry context (byte budget, fingerprinting) + `wp_mcp_ai_maintain_log_buffers` AJAX (Compact/Delete).
10. **`.context/testing.md`** — brief v1.1.64 note: #5931/#5935 (AJAX suite class loading, moved-file requires, WP All Import bootstrap guard) and #5960 (validated-tool tests now actually validate — constraint loading restored on Symfony 5.4).
11. **`.context/rest-api.md`** — v1.1.64 note: `/mcp` `jsonrpc`/`method` no longer `required` REST args (the JSON-RPC handler owns -32700/-32600 validation); MCP Server Diagnostics page re-wired.
12. **`CLAUDE.md`** — header stamp (August 26, 2026 · 2.17); ~1,552 → ~1,559 in the three count spots; five v1.1.64 notes (Google Calendar foundation, Composio health/hardening, logger redaction + compaction, non-loggable fields, Symfony validator restore).
13. **`MAINTAINER_MAP.md`** — last-reviewed bump; tree rows: `includes/google/` gains the shared Google services row; counts ~1,249/~1,552 → ~1,256/~1,559.
14. **Both `mcp-ai-wpoos-plugin` SKILL.md copies** — `plugin-version`/`plugin-version-tested` stamps → 1.1.64; new "Google Calendar Connection & Composio Hardening (v1.1.64+)" section; `~1,552` → `~1,559` full-tools line.
15. **`.bmad/README.md`** — last-reviewed bump → v1.1.64.

### P2 — Operational & feature docs

16. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" stamp → August 26, 2026; Pro row gains Google Calendar connection (shared Google foundation + 6 new tools).
17. **`docs/reference/tools/tool-reference.md`** — header "Last Updated" → August 26, 2026; counts → ~1,559 (entries for the Google Calendar tools already added in-commit by #5959).
18. **`docs/project/plans/v1.1.64-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

19. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.64 (`package-lock.json` intentionally untouched).

### P4 — Housekeeping

20. Delete all `build/**/*1.1.63*` files (30 total: 6 root ZIPs + 3 `.sha256`, 2 optional-components, 19 toolkit-addons) — mirrors the stale-ZIP cleanup convention (`chore: remove stale 1.1.63 build ZIPs [skip ci]`).

### Verify-only (no changes expected)

- `AGENTS.md` / `.github/copilot-instructions.md` / README repo-table skill counts — unchanged (52; no new skills this window). Count mentions in copilot-instructions get the tool-count sweep only.
- `docs/project/proposals/RELATED_PROPOSALS_INDEX.md` — Google Calendar has no proposal (implementation plan lives in `docs/developer/`), consistent with the 029/030 precedent.
- `docs/composio-connect.md` / `docs/toolkits/composio-connect.md` / `addons/pro/includes/composio/README.md` / `addons/pro/includes/tools/composio/README.md` — updated in-commit by #5933/#5934/#5936/#5953/#5958.
- Folder READMEs (`includes/google/`, `addons/pro/includes/tools/google-workspace/`) — shipped in-commit by #5959; `composer run docs:check-folder-readmes` should stay green.
- `.context/media-worker.md`, `.context/oos-engine.md`, `.context/chat-ui.md` — no changes in those subsystems this window.

---

## 3. Execution notes

- **Docs + version-bump-only PR**, matching the established catch-up pattern (`chore/v1.1.64-docs-catch-up`). No PHP/JS logic changes, so PHPUnit/JS suites are unaffected.
- **Version-bump rule:** bump every current-state `1.1.63` reference; **keep** historical ones (old changelog sections, per-version blocks, `docs/history/`, plans, `@since` docblocks). Verify with `git grep "1\.1\.63"` that every remaining hit is intentional history.
- **Validation:**
  - `php -l` on the three bumped PHP files.
  - `package.json` JSON parse.
  - `composer run docs:check-folder-readmes` — no folder READMEs are created/edited by this pass, should stay green.
  - `git grep -E "1,552|1,249 Pro|~1,543|~1,547"` sweep — remaining hits must be historical per-version blocks, plans, or parked artifacts.
- ~25 files touched across `CHANGELOG.md`, `readme.txt`, `README.md`, `docs/`, `.context/`, `CLAUDE.md`, `MAINTAINER_MAP.md`, `.github/`, skills, plus the 4 version-bearing files and the build cleanup.

## 4. Commit breakdown (mirror v1.1.58–v1.1.63)

1. `chore: bump version to 1.1.64` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.64 changelog and release notes` — `CHANGELOG.md`, `readme.txt`, `README.md`, `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.64 changes` — `.context/tool-registry.md`, `.context/security-checklist.md`, `.context/settings-storage.md`, `.context/testing.md`, `.context/rest-api.md`, `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, both `mcp-ai-wpoos-plugin` SKILL.md copies
4. `docs: reconcile 1.1.64 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/plans/v1.1.64-docs-catch-up.md`
5. `docs: reconcile remaining 1.1.64 tool counts` — `.context/README.md`, `.context/conventions.md`, `.context/pro-vs-base.md`, `.github/copilot-instructions.md`, `MAINTAINER_MAP.md` tree rows (if stragglers remain)
6. `chore: remove stale 1.1.63 build ZIPs [skip ci]` — `build/**/*1.1.63*` (30 files)

## 5. Execution log (2026-08-26)

- **P3 version bump:** `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.64 ✅ (`package-lock.json` intentionally untouched, matching 1.1.58–1.1.63 precedent)
- **P0 release surfaces:** `CHANGELOG.md` ([Unreleased] → [1.1.64] with PR numbers + 12 new PR entries + Versioning), `readme.txt` (Stable tag 1.1.64 + 1.1.64 changelog paragraph), `README.md` (header/highlights/What's-New/Latest-Updates + counts), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md` (banner + section header + Aug 26 block), `docs/project/FOR_REVIEWERS.md` ✅
- **P1 agent context:** `.context/tool-registry.md` (stamp + counts + v1.1.64 note), `.context/security-checklist.md` (v1.1.64 note), `.context/settings-storage.md` (stale July 2026 stamp fixed + log-buffer compaction note), `.context/testing.md` (v1.1.64 note), `.context/rest-api.md` (v1.1.64 note), `CLAUDE.md` (header 2.17 stamp + 5 v1.1.64 notes + counts), `MAINTAINER_MAP.md` (stamp + google tree row + counts), `.bmad/README.md` (stamp), both `mcp-ai-wpoos-plugin` SKILL.md copies (stamps + v1.1.64 section + counts) ✅
- **P2 operational docs:** `docs/project/ADDON_INVENTORY.md` (Last Updated stamp + Pro row: Google Calendar connection), `docs/reference/tools/tool-reference.md` (Last Updated + counts), this plan ✅
- **P4 housekeeping:** all `build/**/*1.1.63*` files (30 total: 6 root ZIPs + 3 `.sha256`, 2 optional-components, 19 toolkit-addons) removed in a dedicated `[skip ci]` commit ✅
- **Verify-only items:** all confirmed no-change ✅ (skill counts stay 52; proposals index keeps Google Calendar out per the 029/030 precedent; composio docs + folder READMEs updated in-commit; media-worker/oos/chat-ui context untouched)
- **Tool counts:** +7 Pro tools this window (6 google-workspace + 1 `composio_manage_accounts`) → ~303 base + ~1,256 Pro (~1,559 total) with the live-registry caveat retained on every count surface. Docker-based live-count re-derivation remains parked per the v1.1.59 decision ✅
- **Validation:** see section 3 (validation notes) — `php -l` on the three bumped PHP files ✅, `package.json` JSON parse ✅, `git grep` sweeps for the old version string and stale counts (remaining hits are intentional history) ✅

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated and tracked in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues — future passes should read that file first instead of re-copying items.**

- **Carried over (user-deferred):** `@since 1.2.0` / `@since 1.9.0` docblock reconciliation and Docker-based live-count re-derivation stay parked until a later pass. → **OI-1 / OI-2 in the tracker** (issues [#5968](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5968) / [#5967](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5967)).
- **New this pass (parked, same class):** `@since 1.0.0` docblocks authored by PR #5959 on the new `includes/google/*` classes — add to the deferred `@since` reconciliation when the user revisits it. → **OI-1 group 4 in the tracker** (issue #5968).
- **New finding this pass:** the `@since` anomaly class is far broader than the four parked groups (~1,928 × `1.0.0`, ~1,707 × `1.2.0`, ~734 × `1.9.0` across PHP source) — a full-tree audit needs explicit scoping sign-off; recorded inside issue #5968, not a catch-up-pass task.
