# Docs & Release Catch-Up Plan — v1.1.65 (PRs #5979–#6007)

> **Prepared:** 2026-08-28 · **Status:** ✅ **Executed 2026-08-28** (all P0–P3 items complete; see execution log below)
> **Scope:** documentation/context updates + version bump to **1.1.65** (no production-code changes)
> **Input:** full review of every commit on `alpha-working` from 2026-08-26 (post-v1.1.64-catch-up merge `0e0e16222e`) through 2026-08-28 (`660f3538f6`)
> **Predecessor:** [`v1.1.64-docs-catch-up.md`](v1.1.64-docs-catch-up.md) (executed Aug 26; covered PRs #5931–#5965)

---

## 1. Context: What changed since the last docs catch-up

The Aug 26 catch-up covered everything through PR #5965 and wrote the `[1.1.64]` changelog. **Twenty-three PRs merged afterward** that were absent from every release-note surface. No PR self-documented in CHANGELOG (no `[Unreleased]` section existed), so this pass writes the whole section. Most PRs are test-suite cluster fixes that also carry small production fixes; four are production features/fixes; three are docs/assets-only.

| PR | Commit(s) | Change | Doc status after this pass |
|---|---|---|---|
| #5979 | `135e30819f` | **Docs-only** — doc open items mapped to GitHub issues (tracker + proposals verification) | ✅ self-documented; verify-only |
| #5980 | `0f349dec7a` + 4 | **Content Graph wp.org listing assets** — icons, main screenshot, generated page-preview HTML, readme copy | changelog ✅ · assets self-documented |
| #5981 | `3c73d8605c` | **Security-posture findings closure (issue #5972)** — F-AI-01: Algorave Tone.js raw-eval now requires a per-session confirmation + warning banner (capability-scoped via `nvoosAlgoraveConfig`); F-CMP-04: TMA markdown source-map removal; F-AUTHZ-01: webhook `__return_true` justification comments; SECURITY_POSTURE.md updated | changelog + `.context/security-checklist.md` ✅ |
| #5982 | `ce9d2c16e6` | **Content Graph AI chat tester fixes** — settings store, graph tool adapter, chat interface/settings sections, SSE handling, core bridge | changelog ✅ (folded into Content Graph block) |
| #5983 | `3aabb09303` | **Media Worker Q5 + 031 Phase 3** — env-gated `POST /api/crawl/full` + `GET /api/crawl/full/task/:id` full-Crawl4AI proxy (SSRF-validated targets, token-gated, 503/502/400 envelopes), `TEMP_ROOT` allowlist for strict-path single-tenant mode; proposals 028/031 + worker README updated in-commit | changelog + `.context/media-worker.md` + ADDON_INVENTORY ✅ · proposal drift fixed (see below) |
| #5984 | `a16dc42ae3` | **Docs-only** — Ralph Wiggum CCT-orchestration proposal recorded as implemented | ✅ self-documented; verify-only |
| #5985 | `33c3327873` + 4 | **OpenAI reasoning-model parameter rejection + embeddings resilience** — `lib/core` `OpenAiCompatibleClient` gains `applyModelConstraints()` (o-series/gpt-5 reject `max_tokens`/`temperature`) + `sendWithParameterCorrection()` retry on 400; Content Graph AI: embedding provider/model resolution (OpenAI preferred, configured `embeddings_model` honored), `\Throwable` guards (no more 500s), keyword-search graph-context fallback without embeddings index (`graph_context_mode`); reindex AJAX `.fail()` handler restores the button; batch reindex embeds arrays in one request | changelog ✅ |
| #5986 | `eaad3f752f` | **Graphify** — provider-prefixed `*_key` fields treated as sensitive by the remote-source crypto; `sync_remote_source` returns canonical `WP_Error` envelope (no more `array( 'success' => false, ... )`) | changelog + `.context/tool-registry.md` ✅ |
| #5987 | `5ddca2fd60` | **REST validator** — legacy `input_text` content segments normalized to `text` (documented compatibility behaviour) | changelog + `.context/rest-api.md` ✅ |
| #5988 | `87691a21ea` | **Content Graph icon v5** — wp.org asset refresh | changelog ✅ (folded into Content Graph block) |
| #5990 | `65aab08853` | **Webhook/shortcode/pro-tool fixes** — Google Chat `verification_token` shared-secret webhook auth when OIDC is disabled; Slack conversion: anchor placeholders survive `wp_strip_all_tags()` + italic-before-bold ordering; paper-store import/export tolerate missing `collection`; scheduled-result block registers idempotently | changelog ✅ |
| #5991 | `952dc85ee1` | **REST chat/attachments** — top-level `attachments` arg no longer declared (legacy clients tolerated; embedded segments authoritative); role enum validation moved to the sanitize layer (`wp_mcp_ai_allowed_message_roles` filter works again); attachment segment prep errors propagated to the client; guardrail pre-screening skips array content | changelog + `.context/rest-api.md` ✅ |
| #5992 | `1d8a6ed61a` | **Content Graph readme Contributors fix** | changelog ✅ (folded into Content Graph block) |
| #5993 | `d27ec291ec` | **Transcript pagination** — sign-preserving `page`/`per_page` sanitizer (negative values clamp to default instead of flipping positive); guest-token identification helper | changelog ✅ |
| #5994 | `14a65170f9` | **Chat service/auth** — orphaned tool messages silently discarded before provider dispatch (`filter_tool_messages_without_matching_calls()`), validator pairing rule relaxed (legacy payloads tolerated, no hard 400); orchestrator lazy load-monitor | changelog + `.context/rest-api.md` ✅ |
| #5995 | `1c2c3b2ccb` | Test-only — SSE streaming suites aligned to the explicit stream-param contract | changelog Tests ✅ |
| #5996 | `c711a8297f` | Test-only — WP 7.1 icon init replay neutralised in bootstrap | changelog Tests ✅ |
| #5997 | `30a89d235b` | **Block re-registration guards** — assistant-builder + Pro toolkit blocks skip already-registered names (WP 7.1 notices) | changelog ✅ |
| #5998 | `8cd7f02511` | Test-only — transcript suite follow-ups for CI environment differences | changelog Tests ✅ |
| #5999 | `f777e5b306` | Test-only — Phase 4 slash-command workflow tests vs current API | changelog Tests ✅ |
| #6000 | `d0a7fc661d` | **Slash commands** — toolkit manager re-resolves the handler at registration (init re-fire sync); CSV lists accept `"1,2"` and `"1, 2"` (comma split + filter) | changelog ✅ |
| #6001 | `5bc4c3c1c8` | Test-only — SSE tool-result text extraction tests for the REST constructor | changelog Tests ✅ |
| #6002 | `33bb8e6c5f` | Test-only — transcript retrieval roundtrip skipped when the CCT table is missing | changelog Tests ✅ |
| #6003 | `0499c2a887` | **Remote-connection tool guidance** — `remote_wp_connection` errors instruct callers to run `list_connections` first | changelog ✅ |
| #6004 | `94d155f57c` | **AJAX handlers** — workflow-execution `wp_send_json_*` moved out of the try block (no double output on exception); model-manager accepts `overwrite` as `1`/`true`/`yes`/`on`; `research_model` checks the provider `WP_Error` first | changelog ✅ |
| #6005 | `ee2be65bb6` | **Model TPM fallback** — token-budget service falls back to the bundled model catalog (longest-prefix match) when the rate-limits CCT has no entry | changelog ✅ |
| #6006 | `db3b6b9612` | **Admin settings cleanup** — dangling legacy high-token + security-monitor section registrations removed; WP 7.0 bridge checks `WP_Connector_Registry::is_registered()` before `wp_get_connector()` (doing-it-wrong notice fix) | changelog ✅ |
| #6007 | `afb6366e15` | **Messaging/AJAX test clusters** — Messenger webhook controller, async-AJAX provider testing, RabbitMQ client suites reworked; one production-side additive API: `WP_MCP_AI_RabbitMQ_Client::refresh_config()` re-reads settings + resets the cached availability flag (merged after the main pass was cut; folded in via a follow-up commit) | changelog Tests ✅ |

Plus build-only commits (compiled assets, `nvoos-content-graph` ZIPs v1.0.3, `-ai` v1.0.2, oOS packages 1.1.64) which need no doc action. PRs #5989 and #6007+ do not exist on this branch (other streams), so the gap is not missing coverage.

### Drift found while preparing

- **Proposal 031 says "worker 3.2.1+"** (`docs/project/proposals/031-media-worker-crawl4ai-integration-plan.md`, authored in-commit by #5983) but `addons/media-worker/package.json` (authoritative) is still **3.2.0** — the PR did not bump the worker. The proposal note is corrected to **v3.2.0** in this pass (Media Worker keeps its own version track; catch-up passes don't bump it).

### Key scope rules for this pass

- **Historical entries stay.** Old changelog/README per-version blocks (`[1.1.64]` and earlier) are history and are not rewritten, including their tool counts.
- **Tool counts unchanged** — no tools added or removed this window: **~303 base + ~1,256 Pro (~1,559 total)** with the live-registry caveat retained on every count surface. No count-sweep commit needed.
- **Media Worker keeps its own version track** (v3.2.0, unchanged); nvoos-content-graph stays on 1.0.3.
- **Parked items stay parked** (tracker OI-1/OI-2): no `@since` tag reconciliation, no Docker-based live-count re-derivation.
- **No new skills** in this window — coding-time skill count stays **52**.
- **Housekeeping skipped:** the v1.1.64 pass's stale-ZIP cleanup was deliberately reverted in the same PR (`chore: restore build dir to alpha-working state`) — the build directory is managed by the release workflow. Stale `build/**/*1.1.64*` removal is therefore not part of this pass unless explicitly requested.
- **Unrelated working-tree noise excluded** — vendor/ composer/guzzle updates and untracked `nul`, `page.html`, `page2.html` are not part of any commit.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.65] - 2026-08-28` section above `[1.1.64]`: Changed (Media Worker full-Crawl4AI proxy + TEMP_ROOT allowlist, PR #5983), Fixed (OpenAI reasoning-model parameter rejection + embeddings resilience, PR #5985), Fixed (security-posture findings closure F-AUTHZ-01/F-AI-01/F-CMP-04, PR #5981), Fixed (chat/REST/transcript hardening, PRs #5991/#5993/#5994), Fixed (webhook/shortcode/pro-tool fixes incl. Google Chat verification token, PR #5990), Fixed (slash commands + block registration idempotency, PRs #6000/#5997), Fixed (AJAX handlers, TPM catalog fallback, admin settings cleanup, PRs #6004/#6005/#6006), Fixed (Graphify sensitive fields + canonical WP_Error, remote-connection guidance, PRs #5986/#6003), Changed (Content Graph wp.org assets + chat tester, PRs #5980/#5988/#5982/#5992), Tests (PRs #5995/#5996/#5998/#5999/#6001/#6002/#6007), Versioning block (1.1.65, Pro 1.1.65, Media Worker v3.2.0 unchanged, counts ~303 + ~1,256 (~1,559), 15 providers, 26 addons, 74+41 bundled skills, 52 coding-time skills).
2. **`readme.txt`** — Stable tag 1.1.65; new `= 1.1.65 - August 28, 2026 =` changelog paragraph (one paragraph, wp.org style).
3. **`README.md`** — version/release-date header → 1.1.65/2026-08-28; v1.1.65 Highlights paragraph; new "What's New at a Glance (v1.1.65)" section above v1.1.64; new "Latest Updates (v1.1.65 — August 2026)" section; tool-count mentions stay ~303/~1,256/~1,559.
4. **`docs/QUICK_REFERENCE.md`** — version + last-updated → 1.1.65/August 28; v1.1.65 entry above v1.1.64.
5. **`docs/DOCUMENTATION_INDEX.md`** — Plugin Version + Last Updated → 1.1.65/August 28; new AUGUST 28 UPDATE banner; section header v1.1.55–v1.1.64 → v1.1.55–v1.1.65 (+ Full-Crawl4AI Proxy, Security-Posture Closure, Chat/REST Hardening); new "New and updated documents (August 28, 2026, v1.1.65)" block (this plan, README/CHANGELOG/readme.txt/QUICK_REFERENCE, SECURITY_POSTURE, proposals 028/031, tool reference).
6. **`docs/project/FOR_REVIEWERS.md`** — current version 1.1.65 (August 2026).

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed stamp → August 28 (v1.1.65); "New in v1.1.65" note: no new tools; Graphify `sync_remote_source` canonical `WP_Error`; paper-store import/export missing-`collection` guard; `remote_wp_connection` list_connections guidance; TPM bundled-catalog fallback.
8. **`.context/security-checklist.md`** — v1.1.65 note: Algorave Tone.js per-session confirmation gate + warning banner (F-AI-01 accepted-with-rationale path); Google Chat `verification_token` shared-secret fallback (must be set when OIDC disabled); orphaned tool messages silently discarded (validator relaxation is payload-tolerance, never auth/capability relaxation); webhook `__return_true` callbacks carry justification comments.
9. **`.context/testing.md`** — v1.1.65 note: test-cluster PRs (#5995–#6001), WP 7.1 icon-init replay neutralisation, idempotent block registration guards, AJAX double-output fix.
10. **`.context/rest-api.md`** — v1.1.65 note: `attachments` arg undeclared on the chat route; role enum validation lives in the sanitize layer; attachment segment prep errors propagate; sign-preserving pagination sanitizer; orphaned-tool-message filter; legacy `input_text` segment normalization.
11. **`.context/media-worker.md`** — last-reviewed stamp → August 28 (v1.1.65, worker v3.2.0); new "Full-Crawl4AI Proxy (031 Phase 3)" section (`/api/crawl/full`, `CRAWL4AI_FULL_URL`, SSRF-before-proxy, 503/502 envelopes) + TEMP_ROOT allowlist note.
12. **`CLAUDE.md`** — header stamp → August 28, 2026 · 2.18; v1.1.65 notes (reasoning-model constraints, full-crawl proxy, security-posture closure, chat/REST hardening, TPM fallback).
13. **`MAINTAINER_MAP.md`** — last-reviewed bump → August 28, 2026 (v1.1.65).
14. **Both `mcp-ai-wpoos-plugin` SKILL.md copies** — `plugin-version`/`plugin-version-tested` stamps → 1.1.65; new "v1.1.65 Fixes" section (reasoning-model params, full-crawl proxy, security-posture closure).
15. **`.bmad/README.md`** — last-reviewed bump → v1.1.65.

### P2 — Operational & feature docs

16. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" stamp → August 28, 2026; Media Worker row gains the env-gated `/api/crawl/full` full-Crawl4AI proxy + TEMP_ROOT allowlist.
17. **`docs/reference/tools/tool-reference.md`** — header "Last Updated" → August 28, 2026 (counts unchanged ~1,559).
18. **`docs/project/proposals/031-media-worker-crawl4ai-integration-plan.md`** — fix "worker 3.2.1+" → "worker v3.2.0" (package.json authoritative; drift from #5983).
19. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-08-28 (v1.1.65 pass); no new open items.
20. **`docs/project/plans/v1.1.65-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

21. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.65 (`package-lock.json` intentionally untouched).

### P4 — Housekeeping

- **Skipped by decision:** the v1.1.64 pass's ZIP cleanup was reverted in the same PR (`chore: restore build dir to alpha-working state [skip ci]`), so stale `build/**/*1.1.64*` removal is not part of this pass unless the user requests it.

### Verify-only (no changes expected)

- `AGENTS.md` / `.github/copilot-instructions.md` / README repo-table skill counts — unchanged (52; no new skills this window). Tool-count mentions already at ~1,559.
- `docs/operations/security/SECURITY_POSTURE.md` — updated in-commit by #5981 (findings closure); stamp already current.
- Proposals 028/031 + `addons/media-worker/README.md` — updated in-commit by #5983; only the 031 "3.2.1+" drift is fixed (item 18).
- `.context/README.md`, `.context/settings-storage.md`, `.context/chat-ui.md`, `.context/oos-engine.md`, `.context/cross-platform-extraction.md` — no changes in those subsystems this window.

---

## 3. Execution notes

- **Docs + version-bump-only PR**, matching the established catch-up pattern (`chore/v1.1.65-docs-catch-up`). No PHP/JS logic changes, so PHPUnit/JS suites are unaffected.
- **Version-bump rule:** bump every current-state `1.1.64` reference; **keep** historical ones (old changelog sections, per-version blocks, `docs/history/`, plans, `@since` docblocks). Verify with `git grep "1\.1\.64"` that every remaining hit is intentional history.
- **Validation:**
  - `php -l` on the three bumped PHP files.
  - `package.json` JSON parse.
  - `composer run docs:check-folder-readmes` — no folder READMEs are created/edited by this pass, should stay green.
  - `git grep -E "1,552|1,249 Pro|~1,543|~1,547"` sweep — remaining hits must be historical per-version blocks, plans, or parked artifacts.
- ~21 files touched across `CHANGELOG.md`, `readme.txt`, `README.md`, `docs/`, `.context/`, `CLAUDE.md`, `MAINTAINER_MAP.md`, skills, plus the 4 version-bearing files.

## 4. Commit breakdown (mirror v1.1.58–v1.1.64)

1. `chore: bump version to 1.1.65` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.65 changelog and release notes` — `CHANGELOG.md`, `readme.txt`, `README.md`, `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.65 changes` — `.context/tool-registry.md`, `.context/security-checklist.md`, `.context/testing.md`, `.context/rest-api.md`, `.context/media-worker.md`, `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, both `mcp-ai-wpoos-plugin` SKILL.md copies
4. `docs: reconcile 1.1.65 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/proposals/031-media-worker-crawl4ai-integration-plan.md`, `docs/project/plans/docs-catch-up-open-items.md`, `docs/project/plans/v1.1.65-docs-catch-up.md`

*(no tool-count sweep commit — counts unchanged; no housekeeping commit — skipped by decision)*

## 5. Execution log (2026-08-28)

- **P3 version bump:** `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.65 ✅ (`package-lock.json` intentionally untouched, matching 1.1.58–1.1.64 precedent)
- **P0 release surfaces:** `CHANGELOG.md` [1.1.65] section (PRs #5979–#6007 + Versioning), `readme.txt` (Stable tag 1.1.65 + 1.1.65 changelog paragraph), `README.md` (header/highlights/What's-New/Latest-Updates), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md` (banner + section header + Aug 28 block), `docs/project/FOR_REVIEWERS.md` ✅
- **P1 agent context:** `.context/tool-registry.md` (stamp + v1.1.65 note), `.context/security-checklist.md` (v1.1.65 note), `.context/testing.md` (v1.1.65 note), `.context/rest-api.md` (v1.1.65 note), `.context/media-worker.md` (stamp + full-crawl proxy section), `CLAUDE.md` (header 2.18 stamp + v1.1.65 notes), `MAINTAINER_MAP.md` (stamp), `.bmad/README.md` (stamp), both `mcp-ai-wpoos-plugin` SKILL.md copies (stamps + v1.1.65 section) ✅
- **P2 operational docs:** `docs/project/ADDON_INVENTORY.md` (stamp + Media Worker full-crawl row), `docs/reference/tools/tool-reference.md` (Last Updated), proposal 031 "3.2.1+" → v3.2.0 drift fix, `docs-catch-up-open-items.md` (last-reviewed stamp), this plan ✅
- **P4 housekeeping:** skipped by decision (v1.1.64 restore precedent) ✅
- **Verify-only items:** all confirmed no-change ✅ (skill counts stay 52; SECURITY_POSTURE + proposals 028/031 + media-worker README updated in-commit; no new tools)
- **Tool counts:** unchanged this window — no tools added or removed → ~303 base + ~1,256 Pro (~1,559 total) with the live-registry caveat retained. Docker-based live-count re-derivation remains parked per the v1.1.59 decision (OI-2) ✅
- **Validation:** see section 3 — `php -l` on the three bumped PHP files ✅, `package.json` JSON parse ✅, `git grep` sweeps for the old version string and stale counts (remaining hits are intentional history) ✅

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues — this pass added none. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue [#5968](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5968)) and Docker-based live-count re-derivation (OI-2, issue [#5967](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5967)) stay parked.
- **New this pass:** none. (The proposal 031 "worker 3.2.1+" drift was fixed in this pass, not parked.)
- **OI-1 note:** PR #6007 added another `@since 1.2.0` docblock instance (`includes/class-wp-mcp-ai-rabbitmq-client.php` → `refresh_config()`) — same parked anomaly class, recorded in the tracker's group 2 row; not reconciled.
