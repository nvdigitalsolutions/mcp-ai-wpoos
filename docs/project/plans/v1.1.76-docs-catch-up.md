# Docs & Release Catch-Up Plan — v1.1.76 (PRs #6504–#6550)

> **Prepared:** 2026-09-10 · **Status:** ✅ Executed
> **Scope:** documentation/context updates + version bump to **1.1.76** (no production-code changes) + requested housekeeping (stale build ZIPs)
> **Input:** full review of every PR merged on `alpha-working` after the v1.1.75 catch-up (merge `e97b8d86e5`, PR #6503) through PR #6550 (tip `8e3bac02f8d1`), including a read of every PR description and the two unnumbered direct commits in the window
> **Predecessor:** [`v1.1.75-docs-catch-up.md`](v1.1.75-docs-catch-up.md) (executed Sep 9; covered PRs #6476–#6502)

---

## 1. Context: What changed since the last docs catch-up

The Sep 9 catch-up merged as PR #6503 and covered PRs #6476–#6502. **47 PRs** (#6504–#6550, #6519 never merged) plus two unnumbered direct commits and 14 build commits landed afterward, none present on any release-note surface. The window is the **Wave F2 continuation** (comic-creation, dj-management, ai-tool-builder, architect-agent, architectural-design, site-creator, document-generation, regulatory-registration, healthcare, and law-firm toolkits completing in `nvoos-content-graph-pro`) **plus scheduled-delivery polish (full report template + per-channel formats, duplicate-summary fix), the Comic Creation toolkit settings toggle, the checkout launch-complete series (consent/buyer-email/EU-billing/legal docs), four security sweep PRs, and a playbook-seeder idempotency fix**.

### Production-touching PRs (everything else is sub-project ports, docs, or build-only)

| PR | Production files | Change |
|---|---|---|
| #6525 | `addons/pro/includes/services/class-wp-mcp-ai-result-delivery-service.php`, `addons/pro/includes/class-wp-mcp-ai-pro-schedule-manager.php`, `addons/pro/includes/tools/chat-channels/class-wp-mcp-ai-pro-tool-send-telegram-message.php`, `addons/pro/includes/rest/class-wp-mcp-ai-telegram-webhook-controller.php`, `-pro-schedule-rest-controller.php`, `addons/pro/assets/js/schedule-manager.js` | **Chat delivery full report + per-channel format selectors** — chat channels now support the `full` template (summary + substantive response + envelope data, mirroring email); new per-channel `format` setting with allowlists (Telegram `html`/`markdown`/`markdown_v2`/`plain`; WhatsApp/Slack/Discord/Teams `markdown`/`plain`; Messenger/Google Chat `plain`); Telegram delivery routes through `send_telegram_message` directly (the broadcast tool strips HTML) so `parse_mode` works — the cron capability waiver extends to `wp_mcp_ai_send_telegram_message_capability` for the internal delivery context; `send_telegram_message` accepts `MarkdownV2` with reserved-char escaping; group messages dropped by `require_mention` now log `telegram_group_mention_required_ignored` (previously silent). 13 new tests. Schema extensions only — no new tool slugs. |
| #6548 | `addons/pro/includes/services/class-wp-mcp-ai-result-delivery-service.php` | **Skip duplicate summary in full delivery templates** — assistant-run envelopes derive `summary` from the response's first 25 words, and the email/chat `full` templates prepended it again above the full response. New `response_starts_with_summary()` normalizes tags/whitespace + strips the trailing ellipsis and skips the summary when the response already opens with it (non-derived summaries still prepend). |
| #6512 | `includes/admin/sections/class-wp-mcp-ai-section-tools.php`, `includes/admin/class-wp-mcp-ai-pro-settings.php`, `addons/pro/includes/cli/class-wp-mcp-ai-pro-cli-toolkit-command.php`, `-status-command.php`, `addons/pro/docs/toolkits/README.md` | **Comic Creation toolkit settings toggle** — the toolkit (12 tools, 4 CPTs, settings/research/consolidate pages) had shipped but its `enable_comic_creation_toolkit` toggle was never registered, so it could never be enabled. Now registered in the Tools section field list, Pro Features subtab, toolkit memory estimator (128 MB), `get_individual_toolkit_status()`/`get_toolkit_details()`, and both WP-CLI toolkit maps. No new tools. |
| `fe4d0ee880` (direct, no PR) | `includes/professions/class-wp-mcp-ai-profession-playbook-seeder.php` | **Playbook seeder idempotency** — `build_playbook()` stamps a `Generated: <time> UTC` header so the stored content hash changed every second and each sync deleted + recreated the attachment. New `hash_playbook_content()` strips the timestamp line before hashing; regression test added. |

### Checkout launch-complete series (sub-project `plugins/nvoos-content-graph/` + `addons/checkout-api/` + `docs/legal/`)

- **#6507** — required ToS/refund **consent checkbox** + required **buyer-email** field on the purchase modal (`receipt_email` on the PaymentIntent); `/payments/verify` sends `terms_agreed_at` + `buyer_email`; vendor stores both fill-once on the license (DB version 3, `dbDelta` migrates) incl. webhook-issued licenses; new `docs/legal/TERMS-OF-SERVICE.md` + `REFUND-POLICY.md`; prepared-SQL phpcs fixes in the license store.
- **#6520** — manual install promoted to the primary documented path (signed ZIP download always offered; `/verify` returns `download_url` on success); readme.txt disclosure now lists buyer email + consent timestamp; new `WPORG-REVIEW-COMMERCE-NOTES.md` + `SUBMISSION.md` commercial-model section.
- **#6523** — EU country selector on the modal (EU-27, filterable) reveals a required billing-address block (Stripe `billing_details`); new `buyer_country` license column (fill-once); Stripe Product/Price creation admin action + product/price IDs and statement descriptor attached as PaymentIntent metadata; contract doc + disclosures + tests.
- **#6550** — docs-only: `docs/legal/PRIVACY-POLICY.md` (CCPA/CPRA + GDPR-aware), `ACCEPTABLE-USE-POLICY.md` (AI-specific AUP), `CLICKWRAP-IMPLEMENTATION.md`, `COMPLIANCE-CHECKLIST.md` (Florida LLC, FDUTPA/FIPA, ISO generative-AI exclusions); ToS gains §8.4 AI-output disclaimer + Florida governing law + AUP/privacy incorporation. Checkout API version constant stays **0.1.0** (DB schema v3 migrates via `dbDelta`).

### Security / dependency sweeps (no runtime behavior changes beyond patched builds)

- **#6515** — 9 Dependabot alerts: `@tiptap/*` 3.30.4→3.30.5 (ReDoS), `multer` 2.2.0→2.3.0 (FD-leak DoS), `csv-parse` 5.6.0→7.0.2 (prototype replacement; Pro vendor copy refreshed), `react-router-dom` 6.30.4→6.30.6 (open redirect/XSS) across canvas-toolkit, document-editor, media-worker, pro (incl. assets/spa, spa-v2), schedule-anything-spa, cloudways-dashboard.
- **#6532** — SVGO XSS bypass CVE-2026-84370: `svgo` override floor → `>=4.1.0` in media-worker (new override), pro, saas-controller; three lockfiles regenerated. Build-time-only transitive dep.
- **#6546** — `svgo` → `>=4.1.0` (root + pro/assets/spa), `hono` → `^4.13.7` (cloud-worker, tenant-router), `vitest`/`@vitest/mocker` → 4.1.11 (11 lockfiles incl. schedule-anything-spa 3.2.6→4.1.11) + schedule-anything-spa build blockers fixed (unused imports, `esbuild.supported.destructuring: true`). extract-zip/adm-zip have no patched release — recommend dismissing.
- **#6504** — docs-hub ZIP packaging: exclude `*.md` (keep `readme.txt`) via tri-sync (`.distignore`, `bin/build-addon-zips.sh`, `build-spa-addons.yml`); `nvoos-docs-hub-v0.4.3.zip` rebuilt (39→37 entries). Docs Hub version unchanged.

### Test-only / docs-only

- **#6529** — mempalace recall fixtures backdated one minute + fixture filters at `PHP_INT_MAX` (clock-skew flake; no production change).
- **#6527** (port PR) — also stabilized `Test_Pro_Privacy` export order-independence (`assertEqualsCanonicalizing`; privacy class untouched).
- **`7b9195d1ac`** (direct, no PR) — **new coding-time skill** `.agents/skills/mcp-ai-wpoos-ecosystem-port/` (144-line base+pro→CG-Pro port-loop playbook) + extends the `mcp-ai-wpoos-test-suite` skill (cross-worktree runner section). **Skill count 55 → 56; the A5 count bookkeeping was NOT done in-window and is executed this pass.**

### Port PRs (sub-project track only — byte-identical into `plugins/nvoos-content-graph-pro/`, v1.0.0 unchanged)

#6505 (image harmonization sub-toolkit), #6506 (image-production admin slice), #6508–#6511 (comic-creation data layer → final slice, 12 tools), #6513/#6514 (dj-management jukebox + admin slices), #6516 (ai-tool-builder toolkit), #6517 (architect-agent toolkit), #6518/#6521/#6522 (architectural-design data/tools/admin), #6524/#6526 (site-creator data layer + tool batch — **site-creator complete**, 33 tools), #6528/#6530/#6531 (document-generation data/tools/QMS+admin — **document-generation complete**), #6533–#6535 (regulatory-registration data/tools/admin — **regulatory-registration complete**), #6536–#6544 (healthcare data layer → interop/OpenMed, 9 batches — **healthcare complete**), #6545/#6547/#6549 (law-firm data layer + matter-management + billing-trust — **law-firm complete**). Each carries its own `tests/` suite. `docs/project/ecosystem-port-tracker.md` updated by the port loop itself (no action).

### Build-only

compiled-assets/distributions ×5, SPA addon ZIPs ×2, nvoos-content-graph v1.0.6 ZIP rebuild ×3 (in-place), nvoos-content-graph-ai-platform v2.0.0 ZIP rebuild ×2 (in-place), **oOS WP.org packages v1.1.75 ×2 (the current ZIP set — supersedes 1.1.74)**. **Excluded:** none.

### Key scope rules for this pass

- **Historical entries stay.** Old changelog/README per-version blocks (`[1.1.75]` and earlier) are history and are not rewritten.
- **Tool counts do NOT change this window.** #6512 registers an existing toolkit's settings toggle (the 12 Comic Creation tools shipped in an earlier window); #6525 extends delivery schemas (`template: full` for chat channels + per-channel `format`). No new slugs. Figures stay **~303 base + ~1,265 Pro (~1,568 total)** with the live-registry caveat — count mentions verified no-change on every surface.
- **Sub-project version tracks:** nvoos-content-graph **1.0.6**, nvoos-content-graph-ai **1.0.4**, nvoos-content-graph-ai-platform **2.0.0**, nvoos-content-graph-pro **1.0.0**, Checkout API **0.1.0** (license DB v3 via `dbDelta`; version constant unchanged), Docs Hub **0.4.3**, Comic Reader **0.5.0**, Media Worker **v3.2.0** — all unchanged this window.
- **Skill count 55 → 56** — new `mcp-ai-wpoos-ecosystem-port` coding-time skill added in-window (`7b9195d1ac`). A5 bookkeeping (AGENTS.md §1, `.github/copilot-instructions.md`, `README.md` repo map) executed this pass and folded into the 1.1.76 Versioning line.
- **New `@since` anomaly group (OI-1 group 14):** six new methods shipping in 1.1.76 carry **no `@since` tag** — `resolve_chat_format()`, `telegram_parse_mode()`, `escape_markdown_v2()`, `send_telegram_direct()`, `response_starts_with_summary()` in `addons/pro/includes/services/class-wp-mcp-ai-result-delivery-service.php` (PRs #6525/#6548) + `hash_playbook_content()` in `includes/professions/class-wp-mcp-ai-profession-playbook-seeder.php` (direct commit `fe4d0ee880`). Recorded in the open-items tracker — **not** fixed in-pass (parked).
- **Parked items stay parked** (OI-1/OI-2 in `docs-catch-up-open-items.md`): no `@since` reconciliation, no Docker live-count re-derivation. OI-2's "current figure" label refreshed to the v1.1.76 baseline (figures unchanged).
- **Housekeeping executed this pass** — remove stale build ZIPs: all `build/**/*1.1.74*` (30 files: 9 top-level incl. 3 `.sha256` + 2 optional-components + 19 toolkit-addons), superseded by the in-window 1.1.75 wp.org package builds. The 1.1.75 ZIP set is current, not stale.
- **Unrelated working-tree noise excluded** — untracked `docker-tmp-logs/`, `logs-run-*`, `logs_92042798220/` dirs are not staged; `package-lock.json` untouched.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.76] - 2026-09-10` section above `[1.1.75]`: Added (chat delivery full template + per-channel format selectors #6525; Comic Creation toolkit settings toggle #6512; checkout consent + buyer email + EU billing + Stripe product metadata + commercial legal docs #6507/#6520/#6523/#6550), Fixed (duplicate summary in full delivery templates #6548; playbook seeder idempotency `fe4d0ee880`), Security (9 Dependabot alerts #6515; SVGO CVE-2026-84370 #6532; svgo/hono/vitest #6546), Changed (docs-hub ZIP markdown exclusion #6504; Wave F2 port completions #6505–#6549), Versioning (1.1.76, counts unchanged ~303 + ~1,265 (~1,568), all sub-project versions unchanged, skill 55 → 56, stale 1.1.74 ZIPs removed).
2. **`readme.txt`** — Stable tag 1.1.76; new `= 1.1.76 - September 10, 2026 =` changelog paragraph (one paragraph, wp.org style).
3. **`README.md`** — version header → 1.1.76; Release Date 2026-09-10; new `🆕 v1.1.76 Highlights` paragraph; new "What's New at a Glance (v1.1.76)" section above v1.1.75; new "Latest Updates (v1.1.76 — September 2026)" block above v1.1.75. Count mentions verified unchanged (~1,568). **Repo-map skill row 55 → 56** (add mcp-ai-wpoos-ecosystem-port).
4. **`docs/QUICK_REFERENCE.md`** — version + last-updated → 1.1.76/September 10; v1.1.76 entry above v1.1.75.
5. **`docs/DOCUMENTATION_INDEX.md`** — Plugin Version + Last Updated → 1.1.76/September 10; new SEPTEMBER 10 UPDATE banner above the v1.1.75 one; new `## 🆕 September 2026 — v1.1.76` section header + "New and updated documents" block.
6. **`docs/project/FOR_REVIEWERS.md`** — current version → 1.1.76 (September 2026); count mentions verified unchanged (~1,568).

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed stamp + "New in v1.1.76 (counts unchanged)" paragraph (chat delivery gains `full` template + per-channel `format` — schema extensions, no new slugs; `send_telegram_message` gains `MarkdownV2`; Comic Creation toolkit toggle now registered — toolkit itself pre-dates this window).
8. **`.context/security-checklist.md`** — v1.1.76 update line: Telegram delivery routes through `send_telegram_message` directly for parse-mode fidelity; the cron capability waiver now covers `wp_mcp_ai_send_telegram_message_capability` for the internal `pro_schedule_manager_result_delivery` context only; `MarkdownV2` content is reserved-char escaped; group-mention skips now log instead of silently dropping; delivery diagnostics still never carry secrets.
9. **`.context/testing.md`** — last-reviewed + v1.1.76 note: new `test-pro-result-delivery-chat-format.php` (13 tests) + `test-pro-result-delivery-email-format.php` extended (duplicate-summary skip); mempalace recall fixtures backdated + `PHP_INT_MAX` filters (#6529); privacy-export order-independence (#6527); playbook-seeder hash regression (direct commit). Test-suite skill stays at 40 patterns.
10. **`.context/rest-api.md`** — v1.1.76 note: `POST /mcp-ai-pro/v1/schedules` documentation only (documents `template`/`format` result-delivery fields — #6525); no base REST changes; no new routes.
11. **`CLAUDE.md`** — header stamp → September 10, 2026 · 2.29; v1.1.76 notes (delivery formats + duplicate-summary fix, checkout legal/EU series, security sweeps, Wave F2 completions, new ecosystem-port skill); count mentions verified unchanged (~1,568).
12. **`MAINTAINER_MAP.md`** — last-reviewed bump → September 10, 2026 (v1.1.76).
13. **`.bmad/README.md`** — last-reviewed bump → v1.1.76.
14. **All three `mcp-ai-wpoos-plugin` SKILL.md copies** — `plugin-version`/`plugin-version-tested` → 1.1.76 (the `.agents/skills/` copy also gains a v1.1.76 note line in its recent-fixes section).
15. **`.agents/skills/mcp-ai-wpoos-updates/SKILL.md`** — frontmatter `plugin-version`/`plugin-version-tested` → 1.1.76, `last-updated` → 2026-09-10.
16. **A5 skill-count bookkeeping** — `AGENTS.md` §1 (inventory row 55 → 56 + coding-time-vs-runtime paragraph, enumerate the ecosystem-port guide), `.github/copilot-instructions.md` (multi-agent-awareness bullet + repo-tree comment 55 → 56 + ecosystem-port guide named), `README.md` repo map row (55 → 56 + ecosystem-port named).

### P2 — Operational & feature docs

17. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" stamp → September 10, 2026 (v1.1.76); version table verified unchanged (checkout-api stays 0.1.0 despite license DB v3).
18. **`docs/reference/tools/tool-reference.md`** — Last Updated → September 10, 2026 (v1.1.76); Tool Count verified unchanged.
19. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-10 (v1.1.76 pass); Scope line gains v1.1.76; **new OI-1 group 14** (missing `@since` ×6 — recorded, not fixed); OI-2 current-figure label → v1.1.76 (figures unchanged).
20. **`docs/project/plans/v1.1.76-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

21. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.76 (`package-lock.json` intentionally untouched).

### Housekeeping (executed this pass)

22. **Remove stale build ZIPs** — `git rm` all `build/**/*1.1.74*` (30 files: 9 top-level incl. 3 `.sha256` + 2 optional-components + 19 toolkit-addons), superseded by the in-window 1.1.75 wp.org package builds. Commit: `chore: remove stale 1.1.74 build ZIPs [skip ci]`.

### Verify-only (no changes expected)

- Skill count after bookkeeping equals the actual 56 directories under `.agents/skills/`.
- `.agents/skills/mcp-ai-wpoos-test-suite/SKILL.md` — pattern count stays 40.
- `addons/pro/README.md` — toolkit count stays 31 (no new tools).
- `docs/project/proposals/` — no new proposals in-window.
- `.context/README.md`, `.context/conventions.md`, `.context/pro-vs-base.md` — count mentions (~1,568) unchanged.
- `addons/docs-hub/CHANGELOG.md`, `addons/comic-reader/CHANGELOG.md` — own tracks; docs-hub ZIP rebuilt without a version bump.
- `docs/project/ecosystem-port-tracker.md` — maintained by the port loop itself (2-line in-window change); no action.
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" description — base count unchanged.

---

## 3. Execution log (2026-09-10)

- **Orientation** — window mapped: 47 PRs (#6504–#6550, #6519 never merged) + 2 unnumbered direct commits (`fe4d0ee880` playbook seeder, `7b9195d1ac` ecosystem-port skill) + 14 build commits; all 13 production-touching PR descriptions read in full; classification recorded in §1.
- **P3** — version bump committed `012abe9891` (`chore: bump version to 1.1.76`); `package-lock.json` untouched.
- **P0** — release-note surfaces committed `b6f5f7df6a` (`docs: add 1.1.76 changelog and release notes`); README repo-map skill row 55 → 56 included.
- **P1** — agent context committed `b189dc9480` (`docs: update agent context for 1.1.76 changes`): 4 `.context/` files, `CLAUDE.md` (2.29), `MAINTAINER_MAP.md`, `.bmad/README.md`, 3 `mcp-ai-wpoos-plugin` SKILL.md copies (frontmatter + new v1.1.76 section in the `.agents` copy), `mcp-ai-wpoos-updates` SKILL.md (frontmatter + executed-plan references → v1.1.75), A5 bookkeeping (`AGENTS.md` §1 ×2, `.github/copilot-instructions.md` ×2 — 55 → 56 + ecosystem-port guide named).
- **P2** — this commit (`docs: reconcile 1.1.76 reference surfaces`): `ADDON_INVENTORY.md` + `tool-reference.md` stamps, open-items tracker (last-reviewed, scope, OI-1 group 14, OI-2 label refresh), this plan.
- **Housekeeping** — stale 1.1.74 build ZIPs removed in the following commit (`chore: remove stale 1.1.74 build ZIPs [skip ci]`).
- **Validation** — §5 checks all green (see below).

## 4. Commit breakdown (mirror v1.1.58–v1.1.75)

1. `chore: bump version to 1.1.76` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.76 changelog and release notes` — `CHANGELOG.md`, `readme.txt`, `README.md` (incl. repo-map skill row 55 → 56), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.76 changes` — `.context/*` (4 files), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies, `.agents/skills/mcp-ai-wpoos-updates/SKILL.md`, `AGENTS.md`, `.github/copilot-instructions.md`
4. `docs: reconcile 1.1.76 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/plans/docs-catch-up-open-items.md`, `docs/project/plans/v1.1.76-docs-catch-up.md`
5. `chore: remove stale 1.1.74 build ZIPs [skip ci]` — `build/**/*1.1.74*` (30 files), housekeeping

> Note: no separate count-sweep commit this pass — tool counts did not change.

## 5. Validation

- `php -l` on the three bumped PHP files ✅
- `package.json` JSON parse ✅
- `git grep "1\.1\.75"` sweep — remaining hits are intentional history (previous-release sections, `@since` docblocks, plans, archive) ✅
- `git grep "1,265"` / `"1,568"` sweep — live surfaces only + historical per-version blocks; no stale counts introduced ✅
- Stale-ZIP sweep — `git ls-files 'build/*'` re-checked: no 1.1.74 files remain; 1.1.75 set is current ✅
- Skill-count verification — 56 directories under `.agents/skills/` == updated counts in AGENTS.md / copilot-instructions / README ✅

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue [#5968](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5968)) and Docker-based live-count re-derivation (OI-2, issue [#5967](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5967)) stay parked. OI-2's current-figure label refreshed to the v1.1.76 baseline (figures unchanged — ~303 base + ~1,265 Pro, ~1,568 total).
- **New this pass:** six missing `@since` tags — `resolve_chat_format()`, `telegram_parse_mode()`, `escape_markdown_v2()`, `send_telegram_direct()`, `response_starts_with_summary()` in `addons/pro/includes/services/class-wp-mcp-ai-result-delivery-service.php` (PRs #6525/#6548) + `hash_playbook_content()` in `includes/professions/class-wp-mcp-ai-profession-playbook-seeder.php` (direct commit `fe4d0ee880`) recorded as OI-1 group 14 (parked, not fixed).
- **Noted observation (sub-project track):** #6527 reports pre-existing platform-suite failures in `nvoos-content-graph-pro` (SLA-manager missing table, 3 standalone CPT tests) — reported separately by the port workstream; follow that track, not the docs pass.
- **Housekeeping executed this pass:** stale 1.1.74 build ZIPs (30 files) removed.
