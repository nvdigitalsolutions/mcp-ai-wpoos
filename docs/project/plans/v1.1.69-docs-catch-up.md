# Docs & Release Catch-Up Plan — v1.1.69 (PRs #6260–#6278)

> **Prepared:** 2026-09-04 · **Status:** ✅ **Executed 2026-09-04** (all P0–P3 items complete; see execution log below)
> **Scope:** documentation/context updates + version bump to **1.1.69** (no production-code changes)
> **Input:** full review of every commit on `alpha-working` merged after the v1.1.68 catch-up (merge `d11a1f913d`, PR #6261) through PR #6278
> **Predecessor:** [`v1.1.68-docs-catch-up.md`](v1.1.68-docs-catch-up.md) (executed Sep 3; covered PRs #6223–#6259)

---

## 1. Context: What changed since the last docs catch-up

The Sep 3 catch-up merged as PR #6261. The following **18 merge PRs** (#6260, #6262–#6278) landed after its documented scope: one front-end surface feature (the Vision Analysis Pro toolkit), a fourth PHPUnit repair-campaign wave (~9 PRs, mostly test-only), and a set of small production fixes — several security-relevant.

### Production-touching PRs (everything else in the window is test-only or build-only)

| PR | Production files | Change |
|---|---|---|
| #6265 | `includes/class-wp-mcp-ai-rest.php`, `includes/security/class-wp-mcp-ai-nefarious-usage-monitor.php`, `includes/oos/class-wp-mcp-ai-oos-shadow-runner.php`, `includes/harness/class-wp-mcp-ai-harness-trace-capture.php`, `includes/chat/class-wp-mcp-ai-chat-continuation-llm-re-entry.php` | **Rate-limiter classification + counter separation** — `check_rate_limit()` accepts the dispatching request's HTTP method (internal dispatches classified by their real verb; GET/HEAD exempt); the nefarious monitor keeps its own `wp_mcp_ai_nefarious_rate_limit_` counter (sharing the chat limiter's halved the chat budget); `wp_mcp_ai_before_chat_request` subscribers default every parameter and tolerate the legacy 2-arg emitter shape; continuation re-entry guard moved to a resettable static (test seam, production idempotency unchanged) |
| #6266 | `includes/admin/class-wp-mcp-ai-admin-scripts.php`, `assets/js/vendor/jquery-ui-sortable.min.js` (new) | **jQuery UI sortable inline fallback** — bundled jQuery UI 1.14.2 Sortable (MIT, license banner) printed at `admin_head` + `wp_print_footer_scripts` (priority 1) whenever the final queue state shows the core handle won't load (registered + `src` + enqueued checks); zero cost on healthy sites |
| #6267 | `addons/pro/includes/tools/vision-analysis/**` (new), `addons/pro/includes/services/class-wp-mcp-ai-hf-vision-inference-service.php`, `addons/pro/includes/admin/class-wp-mcp-ai-vision-analysis-settings.php` (new), module registry, `bin/run-tests-docker.sh` | **Vision Analysis Toolkit (Pro)** — `analyze_image_objects` tool (detection/vlm/hybrid modes, `annotate=true` GD bounding-box attachment), HF `count_objects()` wrapper, count normalizer, VLM client, annotator; gated by `enable_vision_analysis_toolkit` (off by default) with SSRF URL guards; dev runner now composer-installs dev deps for production-only clones. **Pro tool count +1.** |
| #6270 | `includes/okf/class-wp-mcp-ai-okf-bundle-manager.php`, 4 Pro admin pages (comic/media consolidate, skill manager ×2) | **ZipSlip guard was dead code** — `ZipArchive::$num_files` doesn't exist on PHP 8.x, so the entry loops silently never ran and unsafe archives imported successfully; `count( $zip )` restores path-traversal rejection (security-relevant) |
| #6271 | `includes/assistants/class-wp-mcp-ai-assistant-cpt.php` | assistant edit screen no longer fatals when one tool's `get_shortcut_tasks()` (or a shortcut-filter callback) throws — per-tool `try/catch (\Throwable)`, non-array coercion, last-resort metabox guard |
| #6272 | 29 tool files (Gmail/Drive, Cloudways ×4, DietPi ×7, WebChat, graphify, OKF, abilities), `lib/wordpress-adapter/src/Tool/LegacyToolAdapter.php`, `lib/core/src/Tool/ProfessionStatsTool.php`, scaffold template | **DeepSeek 400 fix** — argument-less tools now encode `properties: {}` (never `[]`); `LegacyToolAdapter` preserves object maps and upgrades empty arrays; markup-elicitation + Gemini-managed-agent fallbacks emit `{}`; AI Tool Builder scaffold emits `new stdClass()` |
| #6274 | `includes/services/class-wp-mcp-ai-token-budget-service.php` (`$model_limits`), `includes/tools/class-wp-mcp-ai-tool-generate-video-caption.php`, `includes/data/model-catalog.json` | **Model catalog drift** — `gpt-4o` 128k context entry (prefix match returned 8192); duplicate `gemini-2.5-flash` typo dropped `gemini-2.0-flash` from the video-capable list; `claude-sonnet-4-6`/`gpt-4o` restored to `active`; two Qwen Hugging Face entries added |
| #6277 | `includes/admin/class-wp-mcp-ai-settings-dashboard.php` | **Settings save left cache additions suspended** — `wp_suspend_cache_addition()` returns the *new* state, so capturing it after suspending re-set `true` at restore; WP 7.1's `WP_Object_Cache::add()` returns `false` while suspended, jamming the inline-async tick locks (transcript mining, SaaS apply, VEO polling). State is now read before suspending and restored unconditionally |
| #6278 | `includes/admin/class-wp-mcp-ai-admin-scripts.php`, `includes/assistants/class-wp-mcp-ai-assistant-cpt.php`, 4 SiteKit tool files | **tagDiv admin compat + metabox crash** — SiteKit tools returned the non-existent `CAPABILITY_CAN_USE_IF_ADMIN` constant (string flag arrays now; AdSense adds `financial-impact`), killing the assistant tools metabox at `sitekit_get_adsense` on nugl.com; sortable fallback prints whenever `td_wp_admin` is enqueued (print-queue detection); media script chain forced as direct head tags from core-registered handles; tools metabox CSS hoisted to `admin_enqueue_scripts` |

Test-only PRs: #6260 (count-tokens params), #6262 (continuation seam), #6263 (chat SSE handler), #6264 (deterministic cache expiration), #6268 (coverage manifest + slug integrity), #6269 (incorrect-usage notices, 9 suites), #6273 (admin registration drift), #6275 (subtab sanitizer routing), #6276 (14 Veo suites) — 9 PRs, the fourth campaign wave. Build-only commits: compiled assets ×6, oOS WP.org packages v1.1.68 ×2, SPA addon ZIPs, `nvoos-content-graph-ai-platform` ZIP v2.0.0 re-add, `nvoos-content-graph` standalone ZIP v1.0.3.

### Key scope rules for this pass

- **Historical entries stay.** Old changelog/README per-version blocks (`[1.1.68]` and earlier) are history and are not rewritten, including their tool counts and versioning lines. (PR #6278 extended the 1.1.68 Admin-compat bullet in-commit; that edit is in the historical section and untouched by this pass.)
- **Tool counts change: +1 Pro.** The Vision Analysis toolkit adds `analyze_image_objects` (conditional, off by default). New figures: **~303 base + ~1,263 Pro (~1,566 total)** with the live-registry caveat. All current-state count surfaces swept; historical per-version blocks untouched.
- **No coding-time skills added** — PR #6265 updated the existing `mcp-ai-wpoos-test-suite` (27 → 37 patterns) and `mcp-ai-wpoos-plugin` skills in-commit. Skill counts stay 53; no `AGENTS.md`/README/copilot-instructions count changes.
- **Docs Hub keeps its own addon track**: 0.4.2 unchanged this window (no docs-hub PRs). Media Worker v3.2.0 unchanged. Standalone plugins unchanged (nvoos-content-graph 1.0.3, -ai 1.0.4, -ai-platform 2.0.0).
- **Parked items stay parked** (OI-1/OI-2 in `docs-catch-up-open-items.md`): no `@since` reconciliation, no Docker live-count re-derivation. One new in-window `@since` anomaly is recorded under OI-1 (group 10: `@since 1.1.68` in the vision-analysis files, which ship in 1.1.69).
- **Housekeeping not requested this pass** — no stale-ZIP deletion commit.
- **Unrelated working-tree noise excluded** — `plugins/nvoos-content-graph-ai/vendor.bak-phpunit-conflict/` (untracked, another agent's) is not part of any commit.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — the PR #6267 `[Unreleased]` section folded into a new `## [1.1.69] - 2026-09-04` section: Added (Vision Analysis toolkit, #6267), Fixed (tagDiv admin compat #6278, DeepSeek schema #6272, ZipSlip revival #6270, rate-limiter #6265, shortcut containment #6271, model catalog #6274, settings-save suspension #6277, sortable fallback #6266), Changed (fourth PHPUnit wave, ~9 test PRs, skills to 37 patterns), Versioning (1.1.69, counts ~303 + ~1,263 = ~1,566, Docs Hub 0.4.2 unchanged, 15 providers, 27 addons, 74+41 skills, 53 coding-time skills).
2. **`readme.txt`** — Stable tag 1.1.69; new `= 1.1.69 - September 4, 2026 =` changelog paragraph (one paragraph, wp.org style).
3. **`README.md`** — version/release-date header → 1.1.69/2026-09-04; v1.1.69 Highlights paragraph; new "What's New at a Glance (v1.1.69)" section above v1.1.68; new "Latest Updates (v1.1.69 — September 2026)" section above v1.1.68; **seven current-state tool-count mentions** ~1,565/~1,262 → ~1,566/~1,263 (Pro-addon paragraph, count note, tool classes, upgrade path ×2, architecture + tool-reference links).
4. **`docs/QUICK_REFERENCE.md`** — version + last-updated → 1.1.69/September 4; v1.1.69 entry above v1.1.68.
5. **`docs/DOCUMENTATION_INDEX.md`** — Plugin Version + Last Updated → 1.1.69/September 4; new SEPTEMBER 4 UPDATE banner; new `## 🆕 September 2026 — v1.1.69` section header; new "New and updated documents (September 4, 2026, v1.1.69)" block (this plan, vision-analysis proposal + toolkit doc, README/CHANGELOG/readme.txt/QUICK_REFERENCE).
6. **`docs/project/FOR_REVIEWERS.md`** — current version 1.1.69 (September 2026); tool counts → ~1,566 (~303 + ~1,263) in 4 spots (current version line, total tools, tools/ tree line, registry review item, Pro freemium row).

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed stamp + counts → ~1,566/~1,263; new "New in v1.1.69" paragraph (vision toolkit +1, schema-contract sweep `{}` not `[]`, LegacyToolAdapter object-map preservation, model-catalog fixes, SiteKit string flag arrays).
8. **`.context/security-checklist.md`** — v1.1.69 update line: ZipSlip guard revival (`count($zip)`, dead-code warning), vision-toolkit SSRF rule, rate-limiter real-verb classification + separate nefarious counter, `properties: {}` schema encoding.
9. **`.context/testing.md`** — last-reviewed + v1.1.69 note: fourth wave (~9 PRs) + new production seams (method-aware rate limiter, nefarious counter, dual-shape chat hooks, count-tokens params contract, continuation guard, `{}` schema assertions, gpt-4o/video-capable catalog, cache-suspension restore, SiteKit flags, sortable fallback, shortcut containment); test-suite skill now 37 patterns.
10. **`.context/rest-api.md`** — v1.1.69 note: no new routes; `check_rate_limit()` method-aware classification + nefarious counter separation + dual-shape chat hook contract.
11. **`.context/media-worker.md`** — last-reviewed stamp bump only (no worker changes this window).
12. **`.context/README.md`** — tool-count fact ~1,565 → ~1,566.
13. **`.context/pro-vs-base.md`** — counts → ~1,566 (~303 + ~1,263).
14. **`.context/conventions.md`** — two count spots → ~1,566/~1,263.
15. **`CLAUDE.md`** — header stamp → September 4, 2026 · 2.22; v1.1.69 notes (vision toolkit, tagDiv compat, DeepSeek schema, ZipSlip revival, rate limiter + chat hooks, misc fixes, fourth wave).
16. **`MAINTAINER_MAP.md`** — last-reviewed bump → September 4, 2026 (v1.1.69).
17. **`.bmad/README.md`** — last-reviewed bump → v1.1.69.
18. **All three `mcp-ai-wpoos-plugin` SKILL.md copies** — `plugin-version`/`plugin-version-tested` → 1.1.69 + registry-diagram count → ~1,566 (`.agents/skills/`, `includes/bundled-skills/`, `plugins/nvoos-content-graph-ai-platform/data/bundled-skills/`); the `.agents/skills/` copy gains a "Vision Analysis, tagDiv Compat, DeepSeek Schemas & ZipSlip Revival (v1.1.69+)" section and its `last-updated` → 2026-09-04.

### P2 — Operational & feature docs

19. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" stamp → September 4, 2026; Pro row 30 → **31 toolkits** + Vision Analysis mention.
20. **`addons/pro/README.md`** — toolkit count 30 → **31** (3 spots) + new "#### 31. Vision Analysis Toolkit ✅" section.
21. **`docs/reference/tools/tool-reference.md`** — header counts → ~1,566/~1,263 + Last Updated → September 4, 2026.
22. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-04 (v1.1.69 pass); OI-1 group 10 (`@since 1.1.68` in the vision-analysis files, ships 1.1.69); OI-2 current figure → ~303 + ~1,263 (~1,566).
23. **`docs/project/plans/v1.1.69-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

24. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.69 (`package-lock.json` intentionally untouched).

### Verify-only (no changes expected)

- `AGENTS.md` §1 + §6 + `README.md` + `.github/copilot-instructions.md` skill counts — stay 53 (no new coding-time skills this window; the test-suite/plugin skills were updated in-commit by #6265).
- `docs/project/proposals/RELATED_PROPOSALS_INDEX.md` — no row for the vision-analysis proposal: the index is scoped to cross-platform-extraction-relevant threads, and the proposal lives in `docs/proposals/` (indexed via DOCUMENTATION_INDEX instead, consistent with 033).
- `addons/docs-hub/CHANGELOG.md` — no docs-hub changes this window (0.4.2 already dated 2026-09-03).
- `docs/operations/security/SECURITY_POSTURE.md` — the ZipSlip revival (#6270) is a bug fix to an existing guard, not a new posture finding; noted in `.context/security-checklist.md` instead.
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" description — base count unchanged; no edit.

---

## 3. Execution log (2026-09-04)

- **P3 version bump:** `mcp-ai-wpoos.php` (Version 1.1.69), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.69 ✅ (`package-lock.json` untouched)
- **P0 release surfaces:** `CHANGELOG.md` [1.1.69] section (PRs #6260–#6278 + Versioning), `readme.txt` (Stable tag + 1.1.69 changelog paragraph), `README.md` (header/highlights/What's-New/Latest-Updates + 7 count mentions), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md` (banner + section header + Sep 4 block), `docs/project/FOR_REVIEWERS.md` (5 count/version fixes) ✅
- **P1 agent context:** `.context/tool-registry.md` (counts + v1.1.69 mechanics), `.context/security-checklist.md` (v1.1.69 line), `.context/testing.md` (stamp + v1.1.69 seam list), `.context/rest-api.md` (v1.1.69 note), `.context/media-worker.md` (stamp), `.context/README.md` + `.context/pro-vs-base.md` + `.context/conventions.md` (counts), `CLAUDE.md` (2.22 stamp + v1.1.69 notes), `MAINTAINER_MAP.md` (stamp), `.bmad/README.md` (stamp), three `mcp-ai-wpoos-plugin` SKILL.md copies (version stamps + counts; `.agents/skills/` copy gained the v1.1.69 section) ✅
- **P2 operational docs:** `ADDON_INVENTORY.md` (stamp + Pro row 31 toolkits + vision mention), `addons/pro/README.md` (30 → 31 ×3 + new section), `tool-reference.md` (header counts + date), `docs-catch-up-open-items.md` (last-reviewed + OI-1 group 10 + OI-2 figure), this plan ✅
- **Tool counts:** delta-derived from the 1.1.68 baseline: ~303 base unchanged; Pro 1,262 + 1 (PR #6267 `analyze_image_objects`) = **~1,263**; total **~1,566**. Live-registry caveat retained on every count surface. No dedicated sweep commit — all count-bearing surfaces were touched by commits 2–4 anyway.
- **Verify-only items:** all confirmed no-change ✅
- **Validation:** see section 4.

## 4. Commit breakdown (mirror v1.1.58–v1.1.68)

1. `chore: bump version to 1.1.69` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.69 changelog and release notes` — `CHANGELOG.md`, `readme.txt`, `README.md`, `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.69 changes` — `.context/*` (7 files), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies
4. `docs: reconcile 1.1.69 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `addons/pro/README.md`, `docs/reference/tools/tool-reference.md`, `docs/project/plans/docs-catch-up-open-items.md`, `docs/project/plans/v1.1.69-docs-catch-up.md`

## 5. Validation

- `php -l` on the three bumped PHP files ✅
- `package.json` JSON parse ✅
- `git grep "1\.1\.68"` sweep — remaining hits are intentional history (previous-release sections, `@since` docblocks, the in-window vision-analysis `@since 1.1.68` anomaly recorded under OI-1 group 10) ✅
- `git grep -E "1,565|1,262"` sweep — remaining hits are historical per-version blocks only (v1.1.63–v1.1.68 changelog/README/readme.txt/skill sections) ✅

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues — this pass added OI-1 group 10 there. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue [#5968](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5968)) and Docker-based live-count re-derivation (OI-2, issue [#5967](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5967)) stay parked.
- **New this pass (recorded, not fixed):** `@since 1.1.68` in the vision-analysis toolkit files (PR #6267, ships in 1.1.69) plus the "Vision Analysis Toolkit … 1.1.68" code comment in `addons/pro/mcp-ai-wpoos-pro.php` — OI-1 group 10, parked per the standing decision.
- **Housekeeping not requested this pass** — the 1.1.68 build ZIPs remain in `build/` (they are the current release artifacts); the next pass can remove them once 1.1.69 ships, if requested.
