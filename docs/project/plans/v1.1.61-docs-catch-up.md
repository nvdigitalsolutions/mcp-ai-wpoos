# Docs & Release Catch-Up Plan — v1.1.61 (PRs #5906, #5910–#5912 + memory-identity bridge)

> **Prepared:** 2026-08-21 · **Status:** ✅ **Executed & committed 2026-08-21** (all P0–P3 items complete; see execution log below)
> **Scope:** documentation/context updates + version bump to **1.1.61** (no production-code changes)
> **Input:** full review of every commit on `alpha-working` from 2026-08-21 after the v1.1.60 catch-up merge (PR #5907)
> **Predecessor:** [`v1.1.60-docs-catch-up.md`](v1.1.60-docs-catch-up.md) (executed Aug 21; covered PRs #5897–#5905)

---

## 1. Context: What changed since the last docs catch-up

The Aug 21 v1.1.60 catch-up (PR #5907) covered PRs #5897–#5905. **Five change groups landed after it** (plus one missed by it) and were absent from every release-note surface:

| PR / Commit | Change | Doc status after this pass |
|---|---|---|
| `ee3c41f0c0` (direct commit) | **Agent identity bridging in memory store & recall** — new `WP_MCP_AI_Agent_Identity_Resolver` (`includes/services/`, 212 lines) canonicalises virtual agent keys (e.g. `nvoos-pro-spa-memory-drawer`, `virtual_planner_1`) to the canonical assistant post ID; `store_agent_context` resolves on store + echoes `original_agent_id` / `agent_id_resolved`; REST chat-memory recall merges alias buckets with per-record `stored_under` stamps + `merged_sources`; memory drawers (base, chat-spa, pro-spa) gain scope/`stored_under` chips, an agent-ID diagnostic, a show-all-scopes toggle, and store-triggered refresh; graph-bridge + scoped-recall failures degrade gracefully | ✅ this pass (changelog, README/readme.txt/QUICK_REFERENCE, chat-ui/rest-api/tool-registry context, CLAUDE.md, MAINTAINER_MAP, skills, chat-client-integration doc) |
| #5910 | **undici pinned to ^7.29.0 for jsdom compat** — jsdom 29.1.1 deep-requires `lib/handler/wrap-handler.js` (removed in undici 8), breaking vitest in 7 addons; 7.29.0 is the newest 7.x and keeps all CVE fixes | ✅ this pass (changelog, README, security-checklist context, CLAUDE.md) |
| #5911 | **OKF skill-knowledge bundle generator** — new `includes/okf/class-wp-mcp-ai-okf-skill-knowledge-generator.php` (456 lines) auto-generates the `skill-knowledge` bundle from bundled skills on bootstrap (priority 32, when missing or version changed) and after admin bundled-skill reinstall; fixes "OKF bundle not found: skill-knowledge" on every okf_* tool | ✅ this pass (changelog, README/readme.txt, CLAUDE.md, MAINTAINER_MAP, skills, okf-integration doc) |
| #5912 | **nvoos-content-graph wp.org review reply + detailed report** — `WPORG-REVIEW-REPLY.md` + `WPORG-REVIEW-DETAILED-REPORT.md`; `WPORG-REVIEW-*.md` excluded from distribution ZIPs (`.distignore` + both build exclude lists) | ✅ this pass (changelog line only — reply/report are self-contained plugin docs) |
| #5906 (missed by 1.1.60) | **Content-graph CI ZIP checksum sync** — build jobs wrote `${ARTIFACT}.zip.sha256` but uploaded `${ARTIFACT}.sha256`; publish job committed only the ZIP, so checksums drifted. Same fix in the `-ai` workflow | ✅ this pass (changelog line, retroactive) |

Build-only commits (SPA addon ZIPs, comic-reader v0.2.0 ZIP, oOS 1.1.60 packages, compiled-asset rebuilds, stale-ZIP deletions) need no doc action.

### Drift found while executing

- `docs/DOCUMENTATION_INDEX.md` header still said "Last Updated: August 19, 2026 / Plugin Version: 1.1.59" — the v1.1.60 pass added an Aug 21 documents block but never bumped the header. Reconciled to August 21, 2026 / 1.1.61.
- `readme.txt` still carried two current-state "250+ tools" mentions (Versions body + Screenshots caption) from before the 1.1.59 header fix. Reconciled to 300+.
- `CHANGELOG.md` `[1.1.60]` covers #5897–#5905 but not #5906 (merged just before the catch-up). Covered retroactively in the `[1.1.61]` section.

### Key scope rules for this pass

- **Historical entries stay.** Old changelog/README per-version blocks (`[1.1.60]` and earlier) are history and are not rewritten.
- **Tool counts unchanged** — no tools were added or removed in this window; every count surface keeps **~300 base + ~1,247 Pro (~1,547 total)** with the live-registry caveat. No count-sweep commit needed beyond the two stale readme.txt "250+" mentions.
- **Version bump:** 1.1.61 everywhere (plugin headers, `WP_MCP_AI_VERSION`, `WP_MCP_AI_PRO_VERSION`, `package.json`, `readme.txt` Stable tag, docs). Media Worker stays on its own 3.2.0 track; nvoos-content-graph on its own 1.0.3 track.
- **Parked items stay parked** (per v1.1.59 open items + user decision): no `@since 1.2.0` tag reconciliation, no Docker-based live-count re-derivation in this pass.
- **No new skills** in this window — coding-time skill count stays **52**.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.61] - 2026-08-21` section: Changed (agent identity bridging in memory store & recall), Added (OKF skill-knowledge bundle generator, PR #5911), Fixed (undici ^7.29.0 jsdom compat, PR #5910), Changed (nvoos-content-graph wp.org review reply & report, PR #5912), Fixed (content-graph CI checksum sync, PR #5906 — retroactive), Versioning (1.1.61, Pro 1.1.61, Media Worker v3.2.0 unchanged, ~300 + ~1,247 (~1,547), 15 providers, 26 addons, 74+41 bundled skills, 52 coding-time skills).
2. **`readme.txt`** — Stable tag 1.1.61; new `= 1.1.61 - August 21, 2026 =` changelog paragraph; two stale "250+ tools" mentions → 300+.
3. **`README.md`** — version/release-date header; v1.1.61 Highlights paragraph; new "What's New at a Glance (v1.1.61)" section; new "Latest Updates (v1.1.61)" section.
4. **`docs/QUICK_REFERENCE.md`** — version + v1.1.61 entry above v1.1.60.
5. **`docs/DOCUMENTATION_INDEX.md`** — header bump (fixes the 1.1.60 drift), new banner, section header v1.1.55–v1.1.61, new "New and updated documents (August 21, 2026, v1.1.61)" block.
6. **`docs/project/FOR_REVIEWERS.md`** — current version 1.1.61.

### P1 — Agent context files

7. **`.context/chat-ui.md`** — Memory Drawer section: v1.1.61 additions (scope + `stored_under` chips, agent-ID diagnostic, show-all-scopes toggle, store-triggered refresh).
8. **`.context/rest-api.md`** — chat-memory bridge note: alias-bucket merge (`stored_under`, `merged_sources`), default limit 25, unscoped fallback.
9. **`.context/security-checklist.md`** — v1.1.61 note: undici ^7.29.0 pin (keep CVE fixes; do not float addons to 8.x until jsdom compat resolves), agent alias option bounded/non-autoloaded/sanitised.
10. **`.context/tool-registry.md`** — last-reviewed stamp + "New in v1.1.61" paragraph (store resolution, wake-up graph degradation; no count change).
11. **`CLAUDE.md`** — v1.1.61 notes: agent identity bridging, OKF skill-knowledge generator, undici pin.
12. **`MAINTAINER_MAP.md`** — new `WP_MCP_AI_Agent_Identity_Resolver` + `WP_MCP_AI_OKF_Skill_Knowledge_Generator` rows; "Last reviewed" bump.
13. **Skills** — both `mcp-ai-wpoos-plugin` SKILL.md copies: version stamps → 1.1.61; new "Agent Identity Bridging & OKF Bundle (v1.1.61+)" operational section.
14. **`.bmad/README.md`** — "Last reviewed" → August 2026 (v1.1.61).

### P2 — Operational & feature docs

15. **`docs/features/okf-integration.md`** — Bootstrap section + file tree: skill-knowledge bundle auto-generation (priority 32, missing/version-change trigger, admin reinstall trigger).
16. **`docs/features/memory/chat-client-integration.md`** — new v1.1.61 section: agent identity bridging (store resolution, recall merge, chips/toggle/refresh, graceful fallbacks).
17. **`docs/project/plans/v1.1.61-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

18. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.61 (`package-lock.json` intentionally untouched).

### Verify-only (no changes expected)

- `.context/media-worker.md` — no worker-side changes this window (stays v1.1.59/v3.2.0 stamp).
- `AGENTS.md` / `.github/copilot-instructions.md` skill counts — unchanged (52).
- `docs/reference/tools/tool-reference.md` header — counts unchanged this window.
- `docs/ROADMAP.md` / `docs/project/ADDON_INVENTORY.md` — no current-state version mentions to bump.
- `addons/chat-spa/README.md` — memory-drawer change is additive UI; no version claims to update.

### Housekeeping

19. Delete all `build/**/*1.1.60*` files (3 main ZIPs, 3 oOS ZIPs + 3 `.sha256`, 2 optional-components, 20 toolkit-addons) — mirrors the stale-ZIP cleanup convention (`chore: remove stale 1.1.60 build ZIPs [skip ci]`).

---

## 3. Execution notes

- **Docs + version-bump-only PR**, matching the established catch-up pattern (`chore/v1.1.61-docs-catch-up`). No PHP/JS logic changes, so PHPUnit/JS suites are unaffected.
- **Version-bump rule:** bump every current-state `1.1.60` reference; **keep** historical ones (old changelog sections, `docs/history/`, `@since` docblocks, archive). Verify with `git grep "1\.1\.60"` that every remaining hit is intentional history.
- **Validation:**
  - `php -l` on the three bumped PHP files.
  - `package.json` JSON parse.
  - `composer run docs:check-folder-readmes` — no folder READMEs created/edited (new classes ship in folders whose READMEs were updated in-commit), should stay green.
  - `git grep -E "1\.1\.60"` sweep — remaining hits must be historical per-version blocks, `@since` tags, or parked artifacts.
- ~20 files touched across `CHANGELOG.md`, `readme.txt`, `README.md`, `docs/`, `.context/`, `CLAUDE.md`, skills, plus the 4 version-bearing files and the build cleanup.

## 4. Commit breakdown (mirror v1.1.58/v1.1.59)

1. `chore: bump version to 1.1.61` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.61 changelog and release notes` — `CHANGELOG.md`, `README.md`, `readme.txt`, `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.61 changes` — `.context/chat-ui.md`, `.context/rest-api.md`, `.context/security-checklist.md`, `.context/tool-registry.md`, `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, both `mcp-ai-wpoos-plugin` SKILL.md copies
4. `docs: reconcile 1.1.61 reference surfaces` — `docs/features/okf-integration.md`, `docs/features/memory/chat-client-integration.md`, `docs/project/plans/v1.1.61-docs-catch-up.md`
5. `chore: remove stale 1.1.60 build ZIPs [skip ci]` — `build/**/*1.1.60*`

## 5. Execution log (2026-08-21)

- **P3 version bump:** `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.61 ✅ (`package-lock.json` intentionally untouched, matching 1.1.58–1.1.60 precedent)
- **P0 release surfaces:** `CHANGELOG.md` new [1.1.61] section (agent identity bridging, OKF generator, undici pin, content-graph review reply/report, retroactive #5906 checksum fix, Versioning), `readme.txt` (Stable tag 1.1.61 + changelog + two stale "250+" mentions → 300+), `README.md` (header/highlights/What's-New/Latest-Updates), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md` (header drift fix + banner + section header + Aug 21 block), `docs/project/FOR_REVIEWERS.md` ✅
- **P1 agent context:** `.context/chat-ui.md` (drawer v1.1.61 additions), `.context/rest-api.md` (merge/fallback note), `.context/security-checklist.md` (v1.1.61 note), `.context/tool-registry.md` (stamp + memory-tool note), `CLAUDE.md` (3 notes), `MAINTAINER_MAP.md` (2 new class rows + last-reviewed), `.bmad/README.md` (stamp), both `mcp-ai-wpoos-plugin` SKILL.md copies (stamps + new operational section) ✅
- **P2 operational docs:** `docs/features/okf-integration.md` (tree + §4.5 generator), `docs/features/memory/chat-client-integration.md` (v1.1.61 section), this plan ✅
- **Verify-only items:** all confirmed no-change ✅
- **Tool counts:** unchanged this window — ~300 base + ~1,247 Pro (~1,547 total), live-registry caveat retained; the only count drift was the two current-state "250+" mentions in readme.txt (fixed in the release-surfaces commit). Live-count re-derivation remains parked per the v1.1.59 decision ✅
- **Housekeeping:** all `build/**/*1.1.60*` files (30 total: 3 main ZIPs, 3 oOS ZIPs + 3 `.sha256`, 2 optional-components, 19 toolkit-addons) removed in a dedicated `[skip ci]` commit, mirroring the stale-ZIP cleanup convention ✅
- **Validation:** see section 4 in the PR body / commit 5 below; `php -l` on the three bumped PHP files ✅ · `package.json` JSON parse ✅ · `git grep` sweeps — remaining `1.1.60` hits are intentional history (previous-release sections, `@since` docblocks, archive) ✅

## 6. Open items for the next pass

- **Carried over (user-deferred):** `@since 1.2.0` docblock reconciliation and Docker-based live-count re-derivation stay parked until a later pass.
