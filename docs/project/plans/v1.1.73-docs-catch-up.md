# Docs & Release Catch-Up Plan — v1.1.73 (PRs #6397–#6449)

> **Prepared:** 2026-09-08 · **Status:** ✅ **Executed & committed 2026-09-08** (all P0–P3 items complete; see execution log below)
> **Scope:** documentation/context updates + version bump to **1.1.73** (no production-code changes) + requested housekeeping (stale build ZIPs)
> **Input:** full review of every PR merged on `alpha-working` after the v1.1.72 catch-up (merge `f37ec629ee`, PR #6396) through PR #6449 (tip `23e0cd7b89`), including a read of every PR description in the window. The two open PRs requested for inclusion — #6447 and #6448 (issue #6389 items 1 + 2) — were merged to `alpha-working` before this pass and are covered in-window.
> **Predecessor:** [`v1.1.72-docs-catch-up.md`](v1.1.72-docs-catch-up.md) (executed Sep 7; covered PRs #6330–#6388)

---

## 1. Context: What changed since the last docs catch-up

The Sep 7 catch-up merged as PR #6396 and covered PRs #6330–#6388. **53 merges** (#6397–#6449) plus build commits landed afterward, none present on any release-note surface. The window is the **ecosystem-port Wave F2** (7 pro-core slices + ~28 CRM slices + the full e-commerce fan-out, creating the new `nvoos-content-graph-pro` standalone addon) **plus two Woo tool upgrades (#6447/#6448), one production fix (#6423), the Comic Reader 0.5.0 upgrade (#6402), and docs/CI work**.

### Production-touching PRs (everything else in the window is test-only, docs-only, build-only, or sub-project)

| PR | Production files | Change |
|---|---|---|
| #6447 | `addons/pro/includes/tools/ecommerce/class-wp-mcp-ai-tool-bulk-update-products.php`, `-ecommerce/README.md`, new `addons/pro/tests/test-bulk-update-products-tool.php` (18 tests), new proposal `037-bulk-update-products-variable-scope-expansion.md` | **`bulk_update_products` variable-scope expansion (issue #6389 item 1)** — new `scope` argument (`all` default \| `product` legacy) sanitized at entry. Under `scope=all`, price/stock fields expand variable parents to their variations and grouped parents to their children via the shared `WP_MCP_AI_Woo_Price_Qty_Updater::resolve_update_targets()`; `WC_Product_Variable::sync()` re-syncs variable parents (direct variation IDs re-sync their parent). Status/featured/category/tag updates always apply to the selected product itself; updates without price/stock fields never expand. Acknowledged response-shape change: `updated_products[]` entries report `targets[]` per input ID, and the response gains `scope` + `updated_targets` keys. `is_available()`/`get_unavailable_reason()` gain a raw-option fallback (testable without the toolkit bootstrap). Canonical envelope unchanged. |
| #6448 | `addons/pro/includes/tools/ecommerce/class-wp-mcp-ai-tool-update-woo-product-qty.php`, `trait-wp-mcp-ai-woo-price-qty-updater.php`, `-ecommerce/README.md`, extended `addons/pro/tests/test-update-woo-product-qty-tool.php`, new proposal `038-update-woo-product-qty-notify-suppression.md` | **`update_woo_product_qty` `notify` suppression flag (issue #6389 item 2)** — new boolean `notify` (default `true`) sanitized at entry and passed per target. `apply_stock_quantity()` gains `$notify = true`; when `false`, `woocommerce_should_send_low_stock_notification` / `woocommerce_should_send_no_stock_notification` are scoped to `__return_false` (priority 999) for the write and removed in a `finally` (early returns/errors can never leak a global filter). Covers the positive-quantity `wc_update_product_stock()` path and the zero-quantity CRUD path; the `woocommerce_low_stock`/`woocommerce_no_stock` actions still fire (push triggers unaffected). Envelope/error codes/response shape unchanged; `woo_products` + `bulk_update_products` keep default `true`. |
| #6423 | `includes/bootstrap/loader.php`, `includes/bootstrap/activation.php`, `includes/class-wp-mcp-ai-async-job-queue.php`, new `tests/test-async-job-queue-table-resilience.php` | **Async job queue table bootstrap fix** — the class was never wired into the boot loader, so its `plugins_loaded` init (table creation + queue cron scheduling) registered after the hook fired and never ran on production; the Load Guard's `get_queue_stats()` then queried the missing `wp_mcp_ai_job_queue` table on every REST dispatch (SQL-error flood after the 1.2.0 hardening update). Now: require at boot (side-effect file), activation creates the table, `dbDelta` gated behind a `DB_VERSION` option, `use_custom_table()` `SHOW TABLES` probe with static-cache reset, `get_queue_stats()` fails soft with zeroed stats. Already-updated sites self-heal on the first page load. |
| #6402 | `addons/comic-reader/**` (+ self-guarding test bootstrap wired into the root bootstrap; 45 tests) | **Comic Reader 0.2.0 → 0.5.0 Komga-parity upgrade** — Phase 0 correctness/security (archive magic-byte upload validation, filterable size cap, owner-scoped delete, HTTP Range 206 serving, server-side CBZ cover extraction); Phase 1 Komga reader parity (persisted per-comic settings, vertical scroll + webtoon modes, four scale types, double-page rules, gestures, continue-reading shelf); Phase 2 metadata/organization (ComicInfo.xml parsing + automatic RTL detection, series/collection taxonomies, per-user server-side progress); Phase 3 admin/ops (settings page, capability overrides, README/CHANGELOG). Production fixes: WP 6.9 `rest_ensure_response()` `$status` removal (uploads return 201 again), real-file-path extension checks. |

Port PRs (sub-project tracks only): #6398–#6401 (F1 Pro-core slices — skill admin, vault storage/wiring, vector storage), #6404, #6410–#6413, #6414–#6417, #6419–#6422, #6424–#6433 (CRM fan-out — leads, companies, deals, activities, analytics/routing, REST controller, ICP admin, per-CPT settings pages, blueprints page, command center, message log, inbound/outbound, core email, Upwork, sequences, workflow rules, consent/DNC, hygiene, import, customers, engagement, command-center extras), #6406–#6409 (D8-compat tool infra, tool proof, Pro validator service), #6434–#6435 (MCP toolkit server core + CRM settings base), #6436–#6438, #6440–#6442, #6444, #6445, #6449 (e-commerce fan-out — data layer, product, order, shipping, marketing, customer/inventory, Woo, Shopify, always-on extras batch; 43 e-commerce tools registered). All byte-identical ports into the new `plugins/nvoos-content-graph-pro/` (v1.0.0) with monolith-gated seams and D8-compat guards.

Docs/tooling: #6397, #6403 (Docs Hub 0.4.3 wp.org readiness — External Services section, static asset for settings scripts, text-domain fix, packaging excludes, CI plugin-check gate, .pot, `.wordpress-org` checklist), #6418 (new `mcp-ai-wpoos-wporg-submission` skill — skill count 54 → 55 — plus the legacy release.yml plugin-check gate DB-bootstrap repair; ~31k TextDomainMismatch backlog documented, not allowlisted), #6443 (use-cases Rev 3.0 refresh tested against 1.1.72), #6446 (broken-links repair across 5 files after the docs folder reorg), #6439 (docs-hub + content-graph-pro subtree-sync workflows), #6405 (F2 CRM tools D8 block — tracker-only). Build-only: SPA addon ZIP rebuilds, oOS WP.org packages v1.1.72. **Excluded:** #6359/#6357 (main-branch sync — not merged to `alpha-working`).

### Key scope rules for this pass

- **Historical entries stay.** Old changelog/README per-version blocks (`[1.1.72]` and earlier) are history and are not rewritten.
- **Tool counts do NOT change this window.** #6447/#6448 upgrade existing tools (no new slugs): figures stay **~303 base + ~1,265 Pro (~1,568 total)** with the live-registry caveat — count mentions verified no-change on every surface; historical per-version blocks, plans, and proposals keep their old figures.
- **Sub-project version tracks:** nvoos-content-graph **1.0.4** (unchanged), nvoos-content-graph-ai **1.0.4** (unchanged), nvoos-content-graph-ai-platform **2.0.0** (unchanged), **nvoos-content-graph-pro 1.0.0 (new — Wave F2)**, Media Worker v3.2.0, Checkout API v0.1.0. **Docs Hub 0.4.2 → 0.4.3** (#6397/#6403 — wp.org readiness; the addon header and its own CHANGELOG were bumped in-window). **Comic Reader 0.2.0 → 0.5.0** (#6402).
- **Skill count changed in-window: 54 → 55** (#6418 added `mcp-ai-wpoos-wporg-submission`; `AGENTS.md`, `README.md`, `.github/copilot-instructions.md` were bumped by the PR itself). This pass only records the new count on the v1.1.73 release surfaces — **verify-only for the inventory files, no re-bump**.
- **No new `@since` anomalies** — the in-window production edits add no `@since` tags (the diff adds `@since 1.1.0`/`1.0.0` tags only to the new test file); the `@since 2.2.0` tags on the qty tool/trait pre-date the window. No OI-1 group.
- **Parked items stay parked** (OI-1/OI-2 in `docs-catch-up-open-items.md`): no `@since` reconciliation, no Docker live-count re-derivation. OI-2's "current figure" label refreshed to the v1.1.73 baseline (figures unchanged).
- **Housekeeping requested** — remove stale build ZIPs: `build/nvoos-comic-reader-v0.2.0.zip` (superseded by the in-window `-v0.5.0.zip`) and `build/nvoos-docs-hub-v0.4.2.zip` (superseded by the in-window `-v0.4.3.zip`). Final `git ls-files 'build/*'` sweep confirmed no other superseded pairs; the v1.1.72 ZIPs (`build/**/*1.1.72*`) are current, not stale.
- **Unrelated working-tree noise excluded** — none present at pass start (clean tree); `package-lock.json` untouched.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.73] - 2026-09-08` section above `[1.1.72]`: Added (Woo tool upgrades #6447/#6448), Fixed (async job queue table bootstrap #6423), Changed (Comic Reader 0.5.0 #6402, Content Graph ecosystem port Wave F2 #6397–#6445/#6449), Versioning (1.1.73, counts unchanged ~303 + ~1,265 (~1,568), Docs Hub 0.4.3, Comic Reader 0.5.0, content-graph-pro 1.0.0 new, skills 55, stale ZIPs removed).
2. **`readme.txt`** — Stable tag 1.1.73; new `= 1.1.73 - September 8, 2026 =` changelog paragraph (one paragraph, wp.org style).
3. **`README.md`** — version header → 1.1.73; Release Date → 2026-09-08; new `🆕 v1.1.73 Highlights` paragraph; new "What's New at a Glance (v1.1.73)" section above v1.1.72; new "Latest Updates (v1.1.73 — September 2026)" block above v1.1.72. Count mentions verified unchanged (~1,568).
4. **`docs/QUICK_REFERENCE.md`** — version + last-updated → 1.1.73/September 8; v1.1.73 entry above v1.1.72.
5. **`docs/DOCUMENTATION_INDEX.md`** — Plugin Version + Last Updated → 1.1.73/September 8; new SEPTEMBER 8 UPDATE banner above the SEPTEMBER 7 one.
6. **`docs/project/FOR_REVIEWERS.md`** — current version → 1.1.73 (September 2026); count mentions verified unchanged (~1,568).

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed stamp + "New in v1.1.73 (counts unchanged)" paragraph (`bulk_update_products` `scope` expansion + `update_woo_product_qty` `notify` flag — both modify existing tools, no slug changes; counts stay ~1,568).
8. **`.context/security-checklist.md`** — v1.1.73 update line: notify-suppression is scoped-filter suppression removed in a `finally` (no global filter leak; `woocommerce_*_stock` actions still fire); #6423 table-bootstrap fix (fail-soft queue stats; Load Guard can no longer spam SQL errors); bulk `scope` expansion keeps capability gates and canonical envelope.
9. **`.context/testing.md`** — last-reviewed + v1.1.73 note: new `addons/pro/tests/test-bulk-update-products-tool.php` (18 tests, #6447); extended `test-update-woo-product-qty-tool.php` (`notify` schema default, scoped-suppression recorder pattern with the `finally` cleanup, #6448); `tests/test-async-job-queue-table-resilience.php` (real-DDL filter-lift pattern, #6423); comic-reader test bootstrap wired into the root bootstrap (#6402). Test-suite skill stays at 40 patterns.
10. **`.context/rest-api.md`** — v1.1.73 note: no base REST changes this window; #6447's response-shape change is tool-level (not REST); sub-project: the content-graph-pro addon gains the MCP toolkit server core + CRM REST controller ports (F2 wave).
11. **`CLAUDE.md`** — header stamp → September 8, 2026 · 2.26; v1.1.73 notes (bulk scope expansion, notify flag, queue bootstrap fix, Comic Reader 0.5.0, F2 wave/content-graph-pro, Docs Hub 0.4.3, wporg skill 55); count mentions verified unchanged (~1,568).
12. **`MAINTAINER_MAP.md`** — last-reviewed bump → September 8, 2026 (v1.1.73).
13. **`.bmad/README.md`** — last-reviewed bump → v1.1.73.
14. **All three `mcp-ai-wpoos-plugin` SKILL.md copies** — `plugin-version`/`plugin-version-tested` → 1.1.73 (+ `last-updated` → 2026-09-08; the `.agents/skills/` copy gains a v1.1.73 note line in its recent-fixes section).
15. **`.agents/skills/mcp-ai-wpoos-updates/SKILL.md`** — frontmatter `plugin-version`/`plugin-version-tested` → 1.1.73 (+ `last-updated` → 2026-09-08).

### P2 — Operational & feature docs

16. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" stamp → September 8, 2026 (v1.1.73); **Comic Reader row 13: 0.2.0 → 0.5.0** with a Komga-parity description note (the Docs Hub 0.4.3 row was already updated in-window by #6397/#6403 — verify-only).
17. **`docs/reference/tools/tool-reference.md`** — Last Updated → September 8, 2026 (v1.1.73); Tool Count verified unchanged.
18. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-08 (v1.1.73 pass); Scope line gains v1.1.73; OI-2 current-figure label → v1.1.73 (figures unchanged); no new groups.
19. **`docs/project/plans/v1.1.73-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

20. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.73 (`package-lock.json` intentionally untouched).

### Housekeeping (requested this pass)

21. **Remove stale build ZIPs** — `git rm` `build/nvoos-comic-reader-v0.2.0.zip` + `build/nvoos-docs-hub-v0.4.2.zip` (superseding versions verified present). Commit: `chore: remove stale build ZIPs [skip ci]`.

### Verify-only (no changes expected)

- `AGENTS.md` §1 + `README.md` repo map + `.github/copilot-instructions.md` skill counts — already read 55 (bumped by #6418); no re-bump.
- `.agents/skills/mcp-ai-wpoos-test-suite/SKILL.md` — no in-window cluster entries (the #6423 suite + #6447/#6448 suites are new-coverage only); pattern count stays 40.
- `addons/pro/README.md` — toolkit count stays 31 (the two Woo tools extend the existing E-commerce toolkit; #6447/#6448 modify existing tools).
- `docs/project/proposals/RELATED_PROPOSALS_INDEX.md` — extraction-thread index only; proposals 037/038 are Woo-tool scoped and out of scope for the index.
- `.context/README.md`, `.context/conventions.md`, `.context/pro-vs-base.md`, `.github/copilot-instructions.md` — count mentions (~1,568) unchanged.
- `addons/docs-hub/CHANGELOG.md` — 0.4.3 entry added in-window by #6397/#6403 (own track); no catch-up edit.
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" description — base count unchanged.

---

## 3. Execution log (2026-09-08)

- **P3 version bump:** `mcp-ai-wpoos.php` (Version 1.1.73), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.73 ✅ (`package-lock.json` untouched)
- **P0 release surfaces:** `CHANGELOG.md` [1.1.73] section (Woo tool upgrades + queue bootstrap fix + Comic Reader 0.5.0 + F2 wave/content-graph-pro + Docs Hub 0.4.3), `readme.txt` (Stable tag + 1.1.73 changelog paragraph), `README.md` (header/date/highlights/What's-New/Latest-Updates), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md` (banner), `docs/project/FOR_REVIEWERS.md` (version) ✅
- **P1 agent context:** `.context/tool-registry.md`, `.context/security-checklist.md`, `.context/testing.md`, `.context/rest-api.md` (v1.1.73 notes + stamps), `CLAUDE.md` (2.26 stamp + v1.1.73 notes), `MAINTAINER_MAP.md` (stamp), `.bmad/README.md` (stamp), three `mcp-ai-wpoos-plugin` SKILL.md copies (1.1.73 frontmatter; v1.1.73 note in the `.agents/skills/` copy), `mcp-ai-wpoos-updates` SKILL.md (frontmatter) ✅
- **P2 operational docs:** `ADDON_INVENTORY.md` (stamp + Comic Reader 0.5.0 row), `tool-reference.md` (stamp), `docs-catch-up-open-items.md` (last-reviewed + scope line + OI-2 label refresh; no new groups), this plan ✅
- **PR-description review:** all 53 PR bodies + build commits read; window classified (production vs sub-project vs docs/tooling vs build); #6359/#6357 excluded (main-branch sync); no new `@since` anomalies; no new residual test-suite findings ✅
- **Tool counts:** unchanged — ~303 base + ~1,265 Pro (~1,568 total); #6447/#6448 upgrade existing tools; every count surface verified (no edits needed) ✅
- **Skill-count bookkeeping (A5):** 54 → 55 landed with #6418 in-window (`AGENTS.md`, `README.md` repo map, `.github/copilot-instructions.md`); verify-only this pass — new count recorded on the v1.1.73 release surfaces ✅
- **Housekeeping:** stale `nvoos-comic-reader-v0.2.0.zip` + `nvoos-docs-hub-v0.4.2.zip` removed (superseding 0.5.0/0.4.3 ZIPs verified present) ✅
- **Verify-only items:** all confirmed no-change ✅
- **Validation:** see section 5.

## 4. Commit breakdown (mirror v1.1.58–v1.1.72)

1. `chore: bump version to 1.1.73` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.73 changelog and release notes` — `CHANGELOG.md`, `readme.txt`, `README.md`, `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.73 changes` — `.context/*` (4 files), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies, `.agents/skills/mcp-ai-wpoos-updates/SKILL.md`
4. `docs: reconcile 1.1.73 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/plans/docs-catch-up-open-items.md`, `docs/project/plans/v1.1.73-docs-catch-up.md`
5. `chore: remove stale build ZIPs [skip ci]` — `build/nvoos-comic-reader-v0.2.0.zip`, `build/nvoos-docs-hub-v0.4.2.zip` (housekeeping)

> Note: no separate count-sweep commit this pass — tool counts did not change.

## 5. Validation

- `php -l` on the three bumped PHP files ✅
- `package.json` JSON parse ✅
- `git grep "1\.1\.72"` sweep — remaining hits are intentional history (previous-release sections, `@since` docblocks, plans, archive, the use-cases Rev 3.0 docs tested against 1.1.72) ✅
- `git grep "1,265"` / `"1,568"` sweep — live surfaces only + historical per-version blocks; no stale counts introduced ✅
- Stale-ZIP sweep — `git ls-files 'build/*'` re-checked: no superseded version pairs remain ✅

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue [#5968](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5968)) and Docker-based live-count re-derivation (OI-2, issue [#5967](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5967)) stay parked. OI-2's current-figure label refreshed to the v1.1.73 baseline (figures unchanged — ~303 base + ~1,265 Pro, ~1,568 total).
- **New this pass:** none — no new `@since` anomalies, no new residual test-suite findings.
- **Housekeeping executed this pass:** stale comic-reader 0.2.0 + docs-hub 0.4.2 build ZIPs removed.
