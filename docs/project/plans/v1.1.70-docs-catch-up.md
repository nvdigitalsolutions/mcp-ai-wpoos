# Docs & Release Catch-Up Plan — v1.1.70 (PRs #6280–#6312)

> **Prepared:** 2026-09-05 · **Status:** ✅ **Executed & committed 2026-09-05** (all P0–P3 items complete; see execution log below)
> **Scope:** documentation/context updates + version bump to **1.1.70** (no production-code changes)
> **Input:** full review of every PR merged on `alpha-working` after the v1.1.69 catch-up (merge `e02336f47b`, PR #6279) through PR #6312, including a read of every PR description in the window
> **Predecessor:** [`v1.1.69-docs-catch-up.md`](v1.1.69-docs-catch-up.md) (executed Sep 4; covered PRs #6260–#6278)

---

## 1. Context: What changed since the last docs catch-up

The Sep 4 catch-up merged as PR #6279 and covered PRs #6260–#6278. **33 merge PRs** (#6280–#6312) landed afterward, none present on any release-note surface: two docs PRs that would have folded them into the 1.1.69 notes (#6309/#6310, "wave-2 fold-in") were **closed unmerged**, so this pass promotes the window to its own release, **1.1.70**. The window is the **fifth PHPUnit repair-campaign wave** (CI triage runs `91006542428` + `91771001271`, clusters K1–K16) carrying ~20 grouped production fixes, plus a Content Graph AI ecosystem test alignment.

### Production-touching PRs (everything else in the window is test-only, docs-only, or build-only)

| PR | Production files | Change |
|---|---|---|
| #6280 | `includes/rest/class-wp-mcp-ai-rest-validator.php`, `includes/class-wp-mcp-ai-openai-client.php` | **Transcript display-metadata + model-aware token caps** — `sanitize_display_metadata()` keeps the `attachments` key (video/image attachment metadata was silently stripped on save); the resource-aware default `max_completion_tokens` is now capped by the model's max output tokens + window (small-window models no longer hard-reject small prompts) |
| #6281 | `includes/admin/class-wp-mcp-ai-pro-dashboard.php` | **Pro dashboard event stats** — `get_monitoring_event_stats()` accepts both underscore (`plugin_update`) and dashed (`plugin-updates`) event slugs, so the dashboard's own emitters now appear in stats |
| #6282 | `includes/tools/class-wp-mcp-ai-tool-web-search-validated.php` | **`web_search` validated-variant drift** — `profession_tags` aligned with the base tool (`writer`, `librarian` added) so profession-based lookups (`get_tools_by_risk_level`) see `web_search` |
| #6283 | `addons/pro/includes/class-wp-mcp-ai-pro-schedule-presets.php` | **Schedule-preset bugs** — `crm_data_cleanup` drops a phantom `validate_contact_data` step (scheduled workflows would fail); `auto_import_scheduled_sources` hook moved out of the recurrence slot into `schedule_data.hook` |
| #6285 | `includes/class-wp-mcp-ai-mesh-peer-sync.php`, `-mesh-peer-tester.php` | **Mesh URL validation** — raw peer URLs validated with `FILTER_VALIDATE_URL` **before** `esc_url_raw()` (scheme-less strings were silently prefixed with `http://` and accepted; `test_connection()` returns `invalid_url` instead of probing) |
| #6286 | `includes/services/class-wp-mcp-ai-memory-auto-capture-service.php` | **Guest memory auto-capture** — `capture()` no longer bails on empty `agent_id`; opt-in guests (`wp_mcp_ai_memory_auto_capture_guests_allowed`) write under a stable shared `guest` bucket (`DEFAULT_GUEST_AGENT_ID`); the dedup window is entered only for eligible captures |
| #6287 | `addons/pro/includes/admin/class-wp-mcp-ai-pro-cpt-ai-integration.php` | **CPT AI integration test seam** — `reset_for_tests()` (`@internal`) unhooks + drops the singleton (the constructor caches its registration state forever) |
| #6293 | `addons/pro/includes/tools/harness/class-wp-mcp-ai-tool-export-fine-tune-curriculum.php`, `harness/README.md` | **Curriculum exporter** — `stringify_payload()` returns `''` for empty arrays so empty `input`/`expected` structures are skipped (`skipped_no_input`/`skipped_no_expect`) instead of emitting `'[]'` rows |
| #6295 | `addons/pro/includes/npm-integration-filters.php` (new helpers), 6 Pro admin pages, `addons/pro/includes/services/class-wp-mcp-ai-ocr-service.php`, 7 CPT settings pages | **exec-disabled host hardening** — canonical `wp_mcp_ai_check_nodejs_available()` / `wp_mcp_ai_get_nodejs_version()` (exec-first, `WP_MCP_AI_Process_Service` proc_open fallback) replace unguarded `@exec()` in six admin pages; OCR gains `is_cli_tool_available()` / `run_cli_command()` (Windows-aware `where`/`which`, Process Service fallback); `execute_node_service_with_timeout()` guards `proc_open`/`proc_close`/`proc_terminate`; `max( 0, (int) $value )` clamps replace `absint()` (which flips negatives to positives) for `assistant_id`/time limits/passing scores |
| #6296 | `includes/integrations/class-wp-mcp-ai-jetengine-cct.php`, conversation-import deleter/tool, Data & Analytics preset | **JetEngine CCT storage probe** — new `WP_MCP_AI_JetEngine_CCT::is_storage_available()` checks the physical CCT table exists (`SHOW TABLES LIKE`); deleter/writer/`conversation_import_delete` gate on it (CI's `Jet_Engine` class shim previously made gates pass against a non-existent table, reporting success instead of `wp_mcp_ai_import_jetengine_missing`); four `conversation_import_*` tools added to the preset |
| #6298 | `includes/class-wp-mcp-ai-openai-client.php` (enhanced), model router defaults | **Complexity routing restored** — the enhanced OpenAI client fed the settings default model as a concrete base, short-circuiting light/advanced routing (every request stayed on `gpt-4.1`); the explicit model only is passed now, falling back to the site default only when routing is disabled; router default priority list derives from the Model Config catalog |
| #6300 | 6 tool/service files (`get_jetengine_items`, Shopify CCT manager, Pro JFB submissions, Pro tool registry, token-usage service), `tests/helpers/jetengine-stubs.php` (new) | **JetEngine/Graphify cluster — real bugs + gates** — availability gates require `JET_ENGINE_VERSION` (the real plugin's load marker; file-scope stubs can no longer flip them); `cct_table_exists()` probes with a direct `SELECT 1 … LIMIT 1` (transactional-DDL safe on MySQL 8.0); JFB submissions tool restores the dropped `id` + flat `per_page`/`status` params (HTTP path always failed `missing_id`); `wp_mcp_ai_pro_get_tool_map()` caches the Pro tool map and `get_unregistered_tools()` uses it (the old filter source was never registered — settings-gated Pro tools vanished from the token-manager listing) |
| #6301 | `includes/tools/class-wp-mcp-ai-tool-generate-auth0-token.php`, `includes/integrations/class-wp-mcp-ai-github-oauth-handler.php` | **OAuth drift** — Auth0 audience validation is scheme + syntax only (audiences are API identifiers, not endpoints — `wp_http_validate_url()` DNS resolution rejected valid ones; the Auth0 domain keeps the full SSRF guard); new `wp_mcp_ai_github_oauth_redirect_terminate` filter seam on the OAuth handler's terminating `exit` |
| #6302 | `includes/tools/skills/class-wp-mcp-ai-skill-pack-registry.php`, `includes/rest/class-wp-mcp-ai-markup-rest-controller.php`, `includes/rest/class-wp-mcp-ai-rest-mcp-controller.php` | **REST + skill-pack bugs** — skill-pack `normalise_packs()` no longer keeps numeric-keyed entries without slugs; `install_pack()` fires `wp_mcp_ai_skill_pack_installed` for every install attempt including empty member lists; markup REST validation `WP_Error`s annotated `status => 400` (server returned 500); `legacy_sse_enabled()` now filterable (`wp_mcp_ai_legacy_sse_enabled`), default unchanged |
| #6303 | `addons/pro/includes/admin/class-wp-mcp-ai-admin-plugins-integration.php`, `WP_MCP_AI_CPT_Settings_Page_Base`, quiz settings page | **Admin/settings drift** — clamp-based sanitizers (`max( 0, … )`) for quiz passing score + CPT `assistant_id`; new `wp_mcp_ai_plugins_integration_redirect_terminate` filter seam (same pattern as the GitHub OAuth handler); test contract updates for renamed section IDs, new tabs, Meta subtab fields, flag vocabulary |
| #6304 | `includes/class-wp-mcp-ai-shortcode.php`, `includes/tools/class-wp-mcp-ai-tool-import-elementor-template-kit.php` | **Elementor drift** — render-time editor detection uses `is_elementor_editor_init()` (honours the `action=elementor` request flag); kit-import `execute()` resolves the availability gate via `static::is_available()` |
| #6305 | `includes/helpers/class-wp-mcp-ai-user-context-helper.php`, `includes/tools/class-wp-mcp-ai-tool-generate-auth0-token.php`, `includes/tools/class-wp-mcp-ai-tool-image-base.php` | **Permission gates** — candidate user IDs validated as positive ints (`absint()` flipped `-1` → `1`, switching to the wrong account); Auth0 audience structural validation (see #6301); image tools check `read_post` against the **acting user** (`user_can( $user_id, … )`) instead of the global current user (cron/CLI/token-authenticated executions) |
| #6306 | `includes/class-wp-mcp-ai-http.php`, `includes/services/class-wp-mcp-ai-token-budget-service.php`, `includes/class-wp-mcp-ai-ollama-client.php`, `includes/tools/class-wp-mcp-ai-tool-scrape-product.php`, `class-wp-mcp-ai-tool-web-search.php`, `addons/pro/.../class-wp-mcp-ai-pro-tool-validate-image-for-product.php` | **HTTP/provider-client drift** — timeout detection matches the hyphenated `timed-out` pattern; `get_model_limit()` retries the slug without its provider prefix (`nvidia/nemotron-3-nano-30b-a3b` → `nemotron-3`, ending false 8192-token conservative-limit trips); Ollama `build_payload()` no longer re-runs the orphan-tool-group filter on converted native `tool_calls` (dropping matching tool-role messages); `css_to_xpath()` descendant-combinator + bare-predicate fixes; `validate_and_normalize_result()` preserves `published_date`; `resolve_image_input()` rejects with `wp_mcp_ai_missing_image` when the resolver returns nothing (instead of a misleading `wp_mcp_ai_missing_api_key`) |
| #6308 | `includes/class-wp-mcp-ai-logger.php`, `includes/harness/class-wp-mcp-ai-harness-trace-store.php` | **Logging/events** — recent-activity allowlist gains the gated `agentic_*` event types + `token_db_optimized` (granular toggles were ineffective — the allowlist silently dropped the events); trace run durations computed from a millisecond-precision `started_at_ms` with a 1ms floor (sub-second runs no longer record 0) |
| #6311 | `includes/class-wp-mcp-ai-outbound-webhook.php`, `includes/repositories/class-wp-mcp-ai-settings-repository.php`, `includes/tools/class-wp-mcp-ai-tool-create-chart.php`, `addons/pro/.../class-wp-mcp-ai-social-media-optimization.php`, `includes/services/class-wp-mcp-ai-orchestration-presets.php`, `includes/site-builder/nodes/class-wp-mcp-ai-site-node-text-block.php`, `includes/measurement/verifiers/class-wp-mcp-ai-schema-verifier.php` | **K16 misc-singles cluster — 7 real production fixes** — webhook IDs are `sanitize_key`-stable (lowercase) with dotted event names preserved (unsubscribe failed ~50% of the time); `WP_MCP_AI_Settings_Repository::get()` falls back to the canonical `wp_mcp_ai_settings` blob (runtime gates — request guard, security manager, destructive-ops gate — never saw dashboard-saved settings like IP blacklists; per-key values still win, fallback not cached); create-chart HTML attachments gated on the acting user's `unfiltered_html` (mirroring WP core) with `html` temporarily re-allowed around `wp_upload_bits()`; social-media publish resets `post_date`/`post_date_gmt` (WP coerced `publish` back to `future`, leaving posts scheduled forever); PSO keyword matching covers inflected forms (`self-tuning`, `improve`/`improving`, `evolving`); site-builder heading tags (h1–h6) allowlisted for the documented `tag` input; schema verifier enforces `required` independently of `properties` |
| #6312 | `tests/test-skill-integration.php`, `tests/test-skill-progressive-disclosure.php` | **K15 completion (test-only)** — per-test upload-dir isolation for the two skill suites (installed skills land in `<test-dir>/mcp-ai-skills` and are removed by tearDown) |

Test-only PRs: #6284 (Cloudflare), #6288 (PayHere vs Remote Connections), #6289 (Mubert music contract), #6290 (OpenAI classic Images API), #6291 + #6297 (composer metadata drift), #6292 (Content Graph AI ecosystem tests), #6299 (Gemini), #6307 (orchestration budget), #6312 (above). Docs-only: #6294 (test-suite cluster board). Build-only: compiled assets ×6, content-graph-ai ZIP v1.0.4, -ai-platform ZIP v2.0.0. **Closed-unmerged docs PRs:** #6309/#6310 (1.1.69 wave-2 fold-in — superseded by this 1.1.70 release).

### Test-suite PR-description review (task requirement — runs 91006542428 + 91771001271)

Every PR description in the window was read. Findings folded into the tracking surfaces:

- **`@since` anomalies:** five new `@since 1.1.69` tags were added in this window (PRs #6295, #6300, #6301, #6303) but the code ships in **1.1.70** → recorded as **OI-1 group 11** in [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) (parked per standing decision — recorded, not fixed).
- **Untracked residual test issues** (from PR Notes sections) → new **OI-4** entry: the order-dependent `test-skill-manager-ajax` `TypeError` (#6312), the 14 WP 7.1-only local chat-transcript failures not present in the CI log (#6280), and the unhealthy `oos-wp` container causing `Lock wait timeout` retries on the shared test DB (#6295). Items verified closed: K16 #33/#107/#108 (fixed by #6311) and triage #50 mempalace (explained by the Graphify precondition fix in #6302).
- **Cluster board compaction:** the `mcp-ai-wpoos-test-suite` skill board gained the two session blocks in-window but was missing #6280–#6285, #6292, #6295–#6303 and still marked the session clusters "(open)". Compacted into one wave-5 block with merged status. Pattern count stays **37** (no new patterns authored this window).

### Key scope rules for this pass

- **Historical entries stay.** Old changelog/README per-version blocks (`[1.1.69]` and earlier) are history and are not rewritten.
- **Tool counts unchanged.** No tools added or removed this window (the `conversation_import_*` preset additions re-list existing tools; the Pro coverage manifest only gains vision-analysis classes from #6267). Figures stay **~303 base + ~1,263 Pro (~1,566 total)** with the live-registry caveat — no count sweep commit needed.
- **No coding-time skills added** — skill counts stay 53; the `mcp-ai-wpoos-test-suite` skill board and the `mcp-ai-wpoos-plugin` copies are updated, pattern count stays 37.
- **Docs Hub keeps its own addon track** (0.4.2 unchanged). Media Worker v3.2.0 unchanged. Standalone plugins unchanged (nvoos-content-graph 1.0.3, -ai 1.0.4, -ai-platform 2.0.0).
- **Parked items stay parked** (OI-1/OI-2 in `docs-catch-up-open-items.md`): no `@since` reconciliation, no Docker live-count re-derivation. One new in-window `@since` anomaly recorded under OI-1 (group 11).
- **Housekeeping not requested this pass** — no stale-ZIP deletion commit (the 1.1.68 ZIPs remain the current release artifacts in `build/`).
- **Unrelated working-tree noise excluded** — `plugins/nvoos-content-graph-ai/vendor.bak-phpunit-conflict/` (untracked, another agent's) is not part of any commit.

---

## 2. Work items (priority order)

### P0 — Release-note surfaces (user-facing)

1. **`CHANGELOG.md`** — new `## [1.1.70] - 2026-09-05` section above `[1.1.69]`: Fixed groups (shell-call hardening #6295, transcript metadata + token caps #6280, mesh URL validation #6285, guest memory bucket #6286, JetEngine gates + CCT probes + Pro tool map #6296/#6300, complexity routing + provider-prefix limits #6298/#6306, OAuth audience + acting-user checks #6301/#6305, REST 400s + legacy-SSE filter + skill-pack slugs #6302, clamp sanitizers + terminate seam #6303, Elementor #6304, logging allowlist + ms traces #6308, K16 misc bundle #6311, dashboards/presets/curriculum/web_search #6281/#6282/#6283/#6293), Changed (fifth PHPUnit wave, ~29 test-only PRs, two CI triage docs, skill board), Versioning (1.1.70, counts unchanged, all sub-tracks unchanged).
2. **`readme.txt`** — Stable tag 1.1.70; new `= 1.1.70 - September 5, 2026 =` changelog paragraph (one paragraph, wp.org style).
3. **`README.md`** — version/release-date header → 1.1.70/2026-09-05; v1.1.70 Highlights paragraph; new "What's New at a Glance (v1.1.70)" section above v1.1.69; new "Latest Updates (v1.1.70 — September 2026)" section above v1.1.69. Tool-count mentions stay ~1,566 (unchanged).
4. **`docs/QUICK_REFERENCE.md`** — version + last-updated → 1.1.70/September 5; v1.1.70 entry above v1.1.69.
5. **`docs/DOCUMENTATION_INDEX.md`** — Plugin Version + Last Updated → 1.1.70/September 5; new SEPTEMBER 5 UPDATE banner; new `## 🆕 September 2026 — v1.1.70` section header + "New and updated documents (September 5, 2026, v1.1.70)" block (this plan, both CI-TRIAGE docs, README/CHANGELOG/readme.txt/QUICK_REFERENCE).
6. **`docs/project/FOR_REVIEWERS.md`** — current version → 1.1.70 (September 2026); counts unchanged.

### P1 — Agent context files

7. **`.context/tool-registry.md`** — last-reviewed stamp + "New in v1.1.70" paragraph (no count change; validated `web_search` profession-tags alignment, JFB `id`/flat params, cached Pro tool map, `published_date` preservation, conversation-import preset additions, skill-pack slug normalization).
8. **`.context/security-checklist.md`** — v1.1.70 update line: exec/proc_open hardening, acting-user permission checks (image `read_post`, chart `unfiltered_html`), Auth0 audience structural validation (domain keeps the SSRF guard), mesh URL pre-sanitization validation, clamp-not-absint sanitizers, positive-int user validation, webhook ID canonicalization.
9. **`.context/testing.md`** — last-reviewed + v1.1.70 note: fifth wave (two CI triage runs, clusters K1–K16, ~29 test-only PRs) + new production seams (`JET_ENGINE_VERSION` gates, `is_storage_available()` probe, `reset_for_tests()`, `wp_mcp_ai_legacy_sse_enabled` filter, two redirect-terminate filter seams, `wp_mcp_ai_pro_get_tool_map()` cache, `started_at_ms` trace floor, `tests/helpers/jetengine-stubs.php` + pinned test-clock helpers, pre_http_request wp-cron loopback guard); triage docs referenced; skill stays 37 patterns + completed board.
10. **`.context/rest-api.md`** — v1.1.70 note: markup REST 400 annotations, `wp_mcp_ai_legacy_sse_enabled` filter (default unchanged), transcript GET table-existence-aware expectations.
11. **`CLAUDE.md`** — header stamp → September 5, 2026 · 2.23; v1.1.70 notes (wave-5 grouped production fixes + fifth repair wave + OI-4 residuals).
12. **`MAINTAINER_MAP.md`** — last-reviewed bump → September 5, 2026 (v1.1.70).
13. **`.bmad/README.md`** — last-reviewed bump → v1.1.70.
14. **All three `mcp-ai-wpoos-plugin` SKILL.md copies** — `plugin-version`/`plugin-version-tested` → 1.1.70 + `last-updated` → 2026-09-05 (`.agents/skills/` copy also gains a "Wave-5 Repair Campaign & Host-Hardening Fixes (v1.1.70+)" section); registry-diagram count stays ~1,566.
15. **`.agents/skills/mcp-ai-wpoos-test-suite/SKILL.md`** — cluster board compaction: merge the two session blocks into one wave-5 block covering all 33 PRs with merged status (adds #6280–#6285, #6292, #6294, #6295–#6303; drops the "(open)" markers); no pattern-count change.

### P2 — Operational & feature docs

16. **`docs/project/ADDON_INVENTORY.md`** — "Last Updated" stamp → September 5, 2026 (Pro row unchanged — 31 toolkits).
17. **`docs/reference/tools/tool-reference.md`** — header Last Updated → September 5, 2026 (counts unchanged).
18. **`docs/project/plans/docs-catch-up-open-items.md`** — "Last reviewed" → 2026-09-05 (v1.1.70 pass); OI-1 **group 11** (five `@since 1.1.69` tags added in-window, ships 1.1.70); new **OI-4** (wave-5 residual test-suite findings from the PR-description review: skill-manager-ajax order-dependent TypeError, 14 WP 7.1-only chat-transcript failures, unhealthy `oos-wp` container lock-waits; verified-closed list: #33/#107/#108 via #6311, triage #50 via #6302); OI-2 figure unchanged.
19. **`docs/developer/testing-docs/TEST-SUITE-REMAINING-FIXES-PLAN.md`** — compact "Wave-5 residual findings" pointer block in the Out-of-scope section (cross-ref OI-4).
20. **`docs/project/plans/v1.1.70-docs-catch-up.md`** — this plan.

### P3 — Version bump (mechanical)

21. `mcp-ai-wpoos.php` (Version header), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.70 (`package-lock.json` intentionally untouched).

### Verify-only (no changes expected)

- `AGENTS.md` §1 + §6 + `README.md` + `.github/copilot-instructions.md` skill counts — stay 53 (no new coding-time skills this window).
- `.context/README.md` / `.context/pro-vs-base.md` / `.context/conventions.md` / `.github/copilot-instructions.md` tool counts — stay ~1,566 (no count change this window).
- `addons/pro/README.md` — toolkit count stays 31 (no new toolkits).
- `docs/project/proposals/RELATED_PROPOSALS_INDEX.md` — no new proposals this window.
- `addons/docs-hub/CHANGELOG.md` — no docs-hub changes (0.4.2 unchanged).
- `readme.txt` header "300+ built-in tools" / `mcp-ai-wpoos.php` "300+ tools" description — base count unchanged.

---

## 3. Execution log (2026-09-05)

- **P3 version bump:** `mcp-ai-wpoos.php` (Version 1.1.70), `includes/bootstrap/constants.php` (`WP_MCP_AI_VERSION`), `addons/pro/mcp-ai-wpoos-pro.php` (`WP_MCP_AI_PRO_VERSION`), `package.json` — all 1.1.70 ✅ (`package-lock.json` untouched)
- **P0 release surfaces:** `CHANGELOG.md` [1.1.70] section (wave-5 grouped fixes + fifth wave + Versioning), `readme.txt` (Stable tag + 1.1.70 changelog paragraph), `README.md` (header/highlights/What's-New/Latest-Updates), `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md` (banner + section header + Sep 5 block + CI-TRIAGE docs indexed), `docs/project/FOR_REVIEWERS.md` (version) ✅
- **P1 agent context:** `.context/tool-registry.md`, `.context/security-checklist.md`, `.context/testing.md`, `.context/rest-api.md` (v1.1.70 notes + stamps), `CLAUDE.md` (2.23 stamp + v1.1.70 notes), `MAINTAINER_MAP.md` + `.bmad/README.md` (stamps), three `mcp-ai-wpoos-plugin` SKILL.md copies (version stamps + new v1.1.70 section in the `.agents/skills/` copy), `mcp-ai-wpoos-test-suite` SKILL.md cluster-board compaction ✅
- **P2 operational docs:** `ADDON_INVENTORY.md` (stamp), `tool-reference.md` (stamp), `docs-catch-up-open-items.md` (last-reviewed + OI-1 group 11 + OI-4 + verified-closed), `TEST-SUITE-REMAINING-FIXES-PLAN.md` (wave-5 residual pointer), this plan ✅
- **PR-description review:** all 33 PR bodies read; wave-5 residuals compacted into OI-4 + the skill cluster board; new `@since` anomaly recorded as OI-1 group 11 ✅
- **Tool counts:** unchanged (no tools added/removed) — ~303 base + ~1,263 Pro (~1,566 total); no count sweep needed ✅
- **Verify-only items:** all confirmed no-change ✅
- **Validation:** see section 4.

## 4. Commit breakdown (mirror v1.1.58–v1.1.69)

1. `chore: bump version to 1.1.70` — `mcp-ai-wpoos.php`, `includes/bootstrap/constants.php`, `addons/pro/mcp-ai-wpoos-pro.php`, `package.json`
2. `docs: add 1.1.70 changelog and release notes` — `CHANGELOG.md`, `readme.txt`, `README.md`, `docs/QUICK_REFERENCE.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/project/FOR_REVIEWERS.md`
3. `docs: update agent context for 1.1.70 changes` — `.context/*` (4 files), `CLAUDE.md`, `MAINTAINER_MAP.md`, `.bmad/README.md`, three `mcp-ai-wpoos-plugin` SKILL.md copies, `.agents/skills/mcp-ai-wpoos-test-suite/SKILL.md`
4. `docs: reconcile 1.1.70 reference surfaces` — `docs/project/ADDON_INVENTORY.md`, `docs/reference/tools/tool-reference.md`, `docs/project/plans/docs-catch-up-open-items.md`, `docs/developer/testing-docs/TEST-SUITE-REMAINING-FIXES-PLAN.md`, `docs/project/plans/v1.1.70-docs-catch-up.md`

## 5. Validation

- `php -l` on the three bumped PHP files ✅
- `package.json` JSON parse ✅
- `git grep "1\.1\.69"` sweep — remaining hits are intentional history (previous-release sections, `@since` docblocks incl. the new OI-1 group 11 anomaly, plans, archive) ✅
- `git grep -E "1,566"` sweep — counts unchanged, no stale-count sweep required ✅

## 6. Open items for the next pass

> **As of 2026-08-26 all standing open items are consolidated in** [`docs-catch-up-open-items.md`](docs-catch-up-open-items.md) **+ GitHub issues — this pass added OI-1 group 11 and OI-4 there. Future passes should read that file first.**

- **Carried over (user-deferred):** `@since` docblock reconciliation (OI-1, issue [#5968](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5968)) and Docker-based live-count re-derivation (OI-2, issue [#5967](https://github.com/nvdigitalsolutions/mcp-ai-wpoos/issues/5967)) stay parked.
- **New this pass (recorded, not fixed):** OI-1 group 11 — five `@since 1.1.69` tags added by PRs #6295/#6300/#6301/#6303, shipping in 1.1.70; OI-4 — wave-5 residual test-suite findings (skill-manager-ajax order-dependent TypeError, 14 WP 7.1-only chat-transcript failures, unhealthy `oos-wp` container lock-waits).
- **Housekeeping not requested this pass** — the 1.1.68 build ZIPs remain in `build/` (current release artifacts); the next pass can remove them once 1.1.70 ships, if requested.
