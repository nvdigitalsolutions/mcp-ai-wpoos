{
  "mcpServers": {
    "wordpress-mcp": {
      "url": "https://your-site.com/wp-json/mcp-ai/v1/mcp",
      "headers": {
        "Authorization": "Bearer cred_xxxxx.SECRET"
      },
      "timeout": 30000,
      "comment": "Updated to use /mcp endpoint which now supports both JSON-RPC POST and SSE GET. This configuration works with LM Studio 0.3.x+ which supports MCP 2024-11-05 specification."
    }
  }
}
