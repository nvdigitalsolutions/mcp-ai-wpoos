/**
 * Barrel export for the NV oOS shared TypeScript module.
 *
 * Import everything from `@shared` (or `./shared`) in one line:
 *
 * ```ts
 * import { type AiProvider, buildAuthHeaders, chatEndpoint } from '@shared';
 * ```
 *
 * @package NV_MCP_AI
 * @since   1.2.0
 */

export * from './types';
export * from './wp-rest';
export * from './api';
