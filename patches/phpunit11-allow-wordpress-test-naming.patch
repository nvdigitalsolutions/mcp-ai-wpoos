--- a/src/Runner/TestSuiteLoader.php	2026-04-23 05:14:51.054105248 +0000
+++ b/src/Runner/TestSuiteLoader.php	2026-04-23 05:15:09.858236152 +0000
@@ -48,6 +48,8 @@
         $suiteClassName = $this->classNameFromFileName($suiteClassFile);
         $loadedClasses  = $this->loadSuiteClassFile($suiteClassFile);
 
+        $fallback = null;
+
         foreach ($loadedClasses as $className) {
             /** @noinspection PhpUnhandledExceptionInspection */
             $class = new ReflectionClass($className);
@@ -64,15 +66,29 @@
                 continue;
             }
 
-            if (!str_ends_with(strtolower($class->getShortName()), strtolower($suiteClassName))) {
-                continue;
+            if (!$class->isAbstract()) {
+                // WordPress-style test files use hyphens in file names but underscores in class
+                // names (e.g. test-foo-bar.php / class Test_Foo_Bar). Normalise both sides so
+                // the name-matching check also passes for such files.
+                $normalisedShortName  = str_replace('-', '_', strtolower($class->getShortName()));
+                $normalisedSuiteName  = str_replace('-', '_', strtolower($suiteClassName));
+                if (str_ends_with($normalisedShortName, $normalisedSuiteName)) {
+                    return $class;
+                }
+
+                // Keep as a fallback: the first non-abstract TestCase subclass in the file.
+                if ($fallback === null) {
+                    $fallback = $class;
+                }
             }
 
-            if (!$class->isAbstract()) {
-                return $class;
+            if ($class->isAbstract() && str_ends_with(strtolower($class->getShortName()), strtolower($suiteClassName))) {
+                $e = new ClassIsAbstractException($class->getName(), $suiteClassFile);
             }
+        }
 
-            $e = new ClassIsAbstractException($class->getName(), $suiteClassFile);
+        if ($fallback !== null) {
+            return $fallback;
         }
 
         if (isset($e)) {
