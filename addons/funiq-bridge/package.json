{
  "name": "funiq-bridge",
  "version": "1.0.0",
  "description": "Funiq Bridge — React SPA admin panel that replicates Payload CMS admin inside WordPress.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.cjs --prod",
    "build:dev": "node esbuild.config.cjs",
    "watch": "node esbuild.config.cjs --watch"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
