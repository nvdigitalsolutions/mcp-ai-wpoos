{
  "name": "@nvoos/tenant-router",
  "version": "0.1.0",
  "private": true,
  "description": "Schedule Anything — Cloudflare Worker tenant router. Maps subdomains to WordPress Multisite instances with KV-backed tenant→origin resolution and per-tenant rate limiting.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.12.21"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.3",
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5",
    "wrangler": "^4.90.0"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
