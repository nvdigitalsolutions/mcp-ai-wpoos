{
  "name": "@nvoos/cloud-worker",
  "version": "1.0.0",
  "private": true,
  "description": "NV oOS Cloud — Cloudflare Worker SaaS backend for managed-tokens (proxy → Cloudflare AI Gateway → OpenRouter).",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.12.4",
    "stripe": "^17.4.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.40",
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.9",
    "wrangler": "^3.114.17"
  }
}
